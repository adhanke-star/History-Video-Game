# E49 design law — envelopment-surrender + straggler-shedding (AARON-LOCKED, design session 2026-07-04, D255)

**Status: DESIGN LAW.** Aaron drove the E49 design session 2026-07-04 and locked all eight decisions below (every one the recommended option — D255 records the popup answers verbatim). Input evidence: `docs/design/e49-design-evidence.md` (committed with this law) + a 3-lens default-refute Opus panel run in-session over the candidate shapes (the D246 design-session pattern; its material corrections are §4 and are baked into §2). The build milestone honors §2 verbatim and ships behind §3; nothing here is built yet — the build session assigns its own D###.

**Build status (D256→D261):** the combined build went honestly RED on this law's own §3.3 gate — attributed 100% to SL-1 by the isolated legs (D256) — and Aaron SPLIT the ledger (D257). **SL-2..SL-10 SHIPPED as E49a (D258, every §3 gate green); SL-1's original form below is SUPERSEDED by §5 SL-1v2 (D259).** The first SL-1v2 landing went red on §3.3's zero-headroom Chancellorsville clause ONLY — every structural/named gate green — and was reverted per this law (D260); **Aaron re-anchored that one gate as its owner (§6) and SL-1v2 SHIPPED as E49b (D261: probe-field 22/22 · the §5.4 battery ×2 byte-identical, ALL GATES GREEN under §6 · all standing probes · 3-lens panel).**

## §1 — The eight locked answers (D255, popup answers 2026-07-04)

1. **Shed fraction:** fixed universal **f = 0.05 per rout event** (over f=0.03 and the morale-tied form).
2. **Surrender predicate:** the **panel-sharpened shape** (directional blocker test · blocked-suppresses-rally · steady-state blockers, no men floor · RALLY_SECS grace · no RNG) — over a 2× grace and a probabilistic roll.
3. **Accounting:** **subset ledgers** — `battleCas` keeps meaning TOTAL men lost; new `captured` + `missing` ledgers are labeled subsets (over carving them out of battleCas, and over captured-only).
4. **Presentation:** **full teaching surface** — announce + prisoner marker + after-action killed/wounded/captured/missing columns + battle-card narration where documented.
5. **Symmetry:** **fully symmetric** — one rule, both sides, all tiers; B-5 cushions stay upstream, no new cushion constants.
6. **Acceptance battery:** **packet + panel additions** (§3 — isolated A/B legs, standing probes re-run, tally-identity tooth, campaign probes, determinism re-run, no-back-solve clause).
7. **E53 assault-abandonment:** **filed, designed later** — after E49 + the E47 flips land; E49 stays exactly the two D242-approved mechanics.
8. **Build timing:** **D255 docs now, build in a FRESH session** (D171 boundary; the battery is ~3× D252's A/B). Queue after E49 unchanged and Aaron-locked: Shiloh/Gettysburg flips + Piper guns (E47/E46 completion) → PM2 rebuild + re-A/B → PARITY-M3.

## §2 — The design law (the build milestone honors this verbatim)

- **SL-1 · STRAGGLER-SHEDDING — ⚠ SUPERSEDED by §5 SL-1v2 (D256 red → D259 redesign; kept verbatim for the record, never build this form).** On every rout EVENT — the T0:553 transition into `state="routing"` — the unit immediately and permanently sheds `round(u.men * 0.05)` men to the missing pool (`__FIELD.missing[side] += shed; u.men -= shed`). Deterministic, no RNG draw, fires at the moment `routEverCount` increments. **f = 0.05 is ONE universal documented constant:** a normal battle's 1–2 breaks/unit yields ~5–10% aggregate straggling (order-consistent with the Shiloh under-the-bluff literature, thousands of ~40,000 engaged); the D251 eroded traces (~3–8 breaks/unit per side) shed ~17–33% side-level — dissolution instead of cycling. The rationale is a PER-EVENT rate whose normal-battle aggregate matches the literature; the Shiloh per-battle aggregate is NOT read directly as a per-event number (§4 correction 1). *[Why it died (D256): the premise failed measurement — real battles break units 0.45–16×, so the aggregate is set by an emergent unbounded cycle count, not by f, and the mechanic feeds back (a weakened assault fights longer → breaks more → sheds more). See §5.]*
- **SL-2 · ENVELOPMENT-SURRENDER.** While `state==="routing"`: the unit's flight lane is the constant-x ray toward `fldHomeEdgeZ(u.side)` (T0:588-589). A **blocker** is an enemy unit with `state==="steady"` (the existing state enum — no new men-floor constant) whose x lies within a RALLY_R-class lateral half-band of the lane AND whose z lies **strictly between** the router's z and the home edge (the DIRECTIONAL clause — a pursuer behind the router never blocks). The corridor is **blocked** when ≥1 blocker exists AND no friendly steady formation is nearer to the router than the nearest blocker (the rescue clause). While blocked: `surrenderT += dt` and **`rallyT` does NOT accrue — blocked ≠ safe** (the rally-collision fix; when the corridor clears, surrenderT resets and normal rally resumes). At `surrenderT ≥ RALLY_SECS` (6s, reused; no leader modifier v1 — decide-&-log: fewest terms) the unit surrenders: its men bank to `__FIELD.captured[side]`, then `alive=false, men=0, state="captured"` (a `fldSurrender(u)` sibling of `fldKill`), announce + prisoner marker. Batteries surrender the same way; their `guns` are logged as captured guns in the after-action. Cavalry gets NO exemption (one rule — the mobility-fidelity debt is this line's documented compromise). No pathfinding, no RNG, O(N) scans mirroring the rally-danger scan (T0:529-533).
- **SL-3 · SCOPE GUARD.** Both mechanics fire ONLY when `__FIELD.attacker` is set (asymmetric scenario battles, incl. phased and campaign-linked). The symmetric sandbox (`attacker` null) is byte-identical — the E48 `holdLive` / `fldGoneCheck` precedent. Within asymmetric battles the rules are UNIVERSAL: baselines move wherever routs occur; there is no per-battle switch of any kind.
- **SL-4 · ACCOUNTING (subset-ledger law).** `battleCas` semantics are UNCHANGED: total men lost = fielded − survivors (T8:108-113) — shed and captured men flow in automatically with zero new wiring. `__FIELD.captured = {US,CS}` and `__FIELD.missing = {US,CS}` are **labeled SUBSETS for reporting, never added to battleCas** (the double-count guard). Killed/wounded is DERIVED: `cas − captured − missing`. Both ledgers reset in `fldResetRun`; T8 accumulates cumulative `battleCaptured`/`battleMissing` beside `battleCas` per phase. **The identity `fielded = survivors + killed/wounded + captured + missing` is a mandatory probe tooth.** T2 campaign fractions are UNCHANGED v1 (total-loss based — the OR convention: documented casualty bands include captured/missing, so A/B ratios stay comparable to sources). Straggler-recovery and POW-exchange wiring (captured men eroding enemyWill/manpower differently; the D161 exchange system) are EXPLICITLY DEFERRED to the post-E49 PM2 rebuild decision set.
- **SL-5 · SYMMETRY (§27).** Identical rules for player and AI units at every difficulty tier. The existing B-5 rout-save cushions stay upstream (fewer player routs at Recruit → less exposure) — difficulty expressed through accurate inputs, never a new cheat constant.
- **SL-6 · NO NEW RNG.** Neither mechanic draws from `fldRng`. Shed fires on the deterministic rout event; surrender on deterministic geometry + timer. The seeded stream is untouched → A/B and byte-comparability legs stay valid.
- **SL-7 · NO BACK-SOLVING (D74 extended).** f and every geometry term (band width, grace) are universal AND chosen from documented rationale — **never tuned to land any battle's casualty ratio or band** (target-tuning a universal constant is D74 outcome-engineering laundered through universality). Casualty bands are LOGGED, never gated; an honest under/overshoot is a divergence note, not a tuning license.
- **SL-8 · PRESENTATION.** Surrender announce ("X surrenders — N men captured"); a prisoner marker at the yield point (reduceMotion-safe, CVD-safe, not color-alone); after-action + interphase cards + end screens report killed/wounded · captured · missing columns; battle cards narrate the documented mass captures where sourced (Hornets' Nest ~2,200, Gettysburg Day-1 ~3,600, the Sunken Road ~300 — citation-stamped) once their flips land.
- **SL-9 · EDGE-CASE RULINGS (panel-verified).** Officers (`__FIELD.leaders`) and supply trains (`__FIELD.trains`) live outside `__FIELD.units` → excluded by construction. The predicate is **fog-blind** (ground truth — consistent with the rally/objective/gone scans, which never call fldVisible). Reinforcement spawns are safe (not routing on arrival); `surrenderT` initializes 0 at spawn and resets in `fldResetRun` (determinism). Phased battles inherit per-phase `homeEdge` via the existing `_fldBuildPhase → fldHomeEdgeSpec` route. Mass surrender MAY trip `fldGoneCheck` → `by=destroy` (desired at the Hornets' Nest class); the 9-battle sweep must show it bounded elsewhere. E48 interaction is structurally safe (panel-confirmed: the timeout transition is terminal, holdLive cannot oscillate; surrendered defenders during overtime accelerate an honest hold).
- **SL-10 · E53 STAYS OUT.** Assault-abandonment (the attacker calling off a failed assault — the D254 3.59 overshoot evidence) is FILED as ledger E53, designed after E49 + the E47 flips land. E49 must NOT touch the D64 attacker-commit logic (`effLocal`, T0:829-830).

## §3 — The acceptance battery (the locked E49-landing gate; red → revert-file-halt)

1. `node --check` on touched files · `node tools/build.mjs` **GATE OK**.
2. **probe-field new fixtures** (original assertions untouched): surrender fixture (blocked corridor → captured, removed, tallied; unblocked corridor → rallies exactly as today; rallyT provably suppressed while blocked); shed fixture (N forced routs → `men` matches the round-compounded arithmetic; missing pool matches); the **tally-identity tooth** (fielded = survivors + kw + captured + missing, run through T8 and T2); sandbox byte-identity (attacker null → both mechanics inert).
3. **The isolated A/B:** 9 battles × the 11-seed union of both documented sets × **{base(HEAD), surrender-only, shedding-only, both}**. The BOTH leg is the shipping config and must hold EVERY documented direction: **Chancellorsville CS 8/8 both sets and Malvern Hill US ≥6/8 both sets HOLD**; all other battles' documented directions hold. The isolation legs attribute the D249/D251-class fix honestly (surrender = removal mechanism, shedding = attrition partner — §4 correction 3). Named watch metrics: CS arrival strength on the Chancellorsville objective (the tightest at-risk attacker gate); the MH atk/def ratio movement vs 3.59 (logged, not gated); `by=destroy` frequency (SL-9 boundedness).
4. **All standing direction probes re-run on the E49 build** (before any flip): probe-malvern-hill 27/27 (`usWins ≥ 6`) · probe-chancellorsville 27/27 · probe-antietam 16/16 · probe-shiloh (CS ≥4/8, still unflipped) · probe-gettysburg · probe-field. No assertion weakened.
5. **probe-campaign-link + probe-conditioning** (T2 fractions with captured/missing present; fresh-state behavior unchanged).
6. **Determinism re-run:** the full A/B run twice, byte-identical to itself.
7. 0 pageerrors everywhere · JSON readback · `git diff --check` clean.
8. **3-lens Opus panel** (sim-affecting, default-refute): no-fudge/§27 · history/teaching · probe-integrity.
9. **THEN, separate milestones (the E49 acceptance tests proper):** the Shiloh flip (gate: CS ≥4/8 with the Nest falling + `captured > 0` on the documented mass-capture seeds) → the Gettysburg flip (Day-1 direction restored + `captured > 0`) → the Piper guns restore (`cs_piper_guns` behind the same A/B; ratio logged; registry re-pin) — each behind its own both-seed-set A/B + battle-probe re-vet, one battle at a time → PM2 rebuild + re-A/B (the D249 gate verbatim; E51 rides the same battery) → PARITY-M3.

**Expected honest movements (logged, never tuned):** every asymmetric baseline moves (all 9 battles rout somewhere — byte-identity is retired as the frame for E49, per the packet §5.5 and panel C3b); envelopment-surrender receives near-ZERO positive validation at unflipped Shiloh/Gettysburg (proven harmless only — its flagship validation is the flip milestones); the MH ratio direction from 3.59 is genuinely open (shedding thins the CS grind, but total-loss accounting adds US captured/missing — either direction is an honest data point for E53).

## §4 — Panel corrections to the evidence packet (recorded so the packet ships verbatim)

1. **Compounding math (packet §4/§2):** the D251 40-47 (and 11-14) rout events are per-SIDE totals, not per-unit (`routEverCount` is a field-level counter). Honest per-unit math at f=0.05: eroded ~3.3–7.8 breaks/unit → 67–84% retention → ~17–33% side-level shed; base ~1–2.3 breaks/unit → ~5–11%. The packet's "leaves ~10%" required one unit breaking ~44 times, which does not occur.
2. **The MH-ratio expectation (packet §5.5) was conditional on the accounting decision**, not settled — resolved by the Q3 subset-ledger lock: battleCas stays total-loss, so documented bands remain comparable, and the ratio's direction is an open measurement (§3 watch metric).
3. **Attribution:** "shedding is precisely what flips the D249/D251 inversion" is downgraded — surrender is the REMOVAL mechanism (the cycling unit leaves the field); shedding is the attrition partner. Hence the isolated A/B legs in §3.
4. **Two mechanic-blocking specification gaps closed in SL-2:** the directional-blocker clause (without it, ordinary pursuit false-triggers surrender everywhere) and the blocked-suppresses-rally clause (without it, a router with blockers >RALLY_R off rallies before it can surrender — the exact cycling E49 exists to stop).

## §5 — SL-1v2 · the E49b redesign (AARON-LOCKED, design session 2026-07-04, D259 — all 4 popup answers = the recommended options)

### §5.1 The evidence (measured this session on the live E49a build, headless AI-vs-AI, per-unit break instrumentation, 4 battles × 3 seeds, 0 pe; full table in D259; script `.tmp/measure-e49b.mjs`, re-derive from D259 if absent)
- **Engine rout frequency (first-hand, worse than D256's 3–8):** breaks/unit spans **0.45–16 per side per battle** (Bull Run US 13–16 breaks/unit, one unit broke 22 times; MH CS 6.2–7.6; Chancellorsville US 5.6–12.3). ANY per-event rate is therefore uncontrolled — the effective shed is set by an emergent, unbounded cycle count, not by f.
- **The feedback amplifier (new):** the dead form's real Chancellorsville-s42 shed (4,378) ran 2.2× its unperturbed-dynamics prediction (2,027) because per-event shedding weakens the assault → longer fight → more breaks → more shed. First-break-only has NO feedback channel (once per unit regardless of dynamics).
- **First-break-only measures a stable ~4–5% side-level** in heavy battles (Bull Run US 875–889 of 18,320 fielded; MH CS ~1,319–1,349 of 30,610; Shiloh CS 1,120–1,201 of 24,840; Chancellorsville US 775–786 of 18,560), bounded ≤ f×fielded (+rounding slack) by construction — the order of the Shiloh straggler literature AND of documented missing columns (MH US historical ~725 of ~26,800 ≈ 2.7% vs ~4.4% modeled — logged, never tuned). The instrumentation counted per-OBJECT first breaks over per-phase-rebuilt rosters, i.e. exactly the shipped mechanic's per-phase semantics, on the accumulated-fielded denominator (the T8/E45 convention).
- **maxMen-anchored once-per-battle is DOMINATED (panel-refuted):** muster-anchoring can order a mauled unit to shed more men than it has (needs a `min(shed, men)` clamp — an extra edge rule), over-taxes depleted units, and its "roster-predictable" selling point fails exactly where the clamp bites. `round(men×f) ≤ men` needs no clamp for all men ≥ 1 (sheds 0 below 10 men).
- **Rally never restores men (code-verified):** T0:547 rally touches state/morale only; NO code path increases `u.men` — so the shed→rally identity hole the math lens hypothesized does not exist; it becomes a fixture tooth, not a code change.

### §5.2 The 4 locked answers (D259 popups, 2026-07-04)
1. **Form: FIRST-BREAK-ONLY** — `shed = Math.round(u.men * FLD.SHED_FRAC)` fired exactly ONCE per unit, on its first transition into `state="routing"` (the T0:563 rout roll), gated by a per-unit sticky flag (`shedDone`, spawn-initialized `false` beside `surrenderT` — T0:240). Re-breaks shed NOTHING (the anti-compounding law). Deterministic, no fldRng, attacker-gated (SL-3) at the call site AND in the function, `_e49NoShed` isolation hook honored. All 3 default-refute Opus panel lenses recommended it unanimously.
2. **f = 0.05 KEPT, RE-ANCHORED (the meaning changed, the number survives):** f is the **one-time collapse fraction — 5% of the men PRESENT scatter when a formation first breaks** (the Shiloh bluff-crowd reading), superseding the D255 per-EVENT rationale. The ~4–5% side aggregate is an EMERGENT measurement at the floor of the 5–10% literature band. **SL-7 extension (explicit): f is NEVER raised to move the aggregate toward the band, toward any battle's documented column, or to make any gate pass.**
3. **Phase scope: PER-PHASE RE-ARM** — phased battles field fresh rosters per phase (T8:98), so the spawn-initialized flag re-arms naturally: **one straggler collapse per fresh commitment**, matching the T8 accumulate-per-phase `battleFielded` convention the identity tooth reads; historically honest (multi-day battles re-straggled each day); zero cross-phase state. Bound: `battleMissing ≤ f × battleFielded` (+rounding slack), gated per-phase too.
4. **Presentation: SILENT LEDGER + ANNOTATIONS** — no per-shed announce, no marker (stragglers are diffuse; the rout announce already fires). The live MISSING column + after-action sentence carry the teaching; the sentence sharpens to **"when a brigade first broke"** (precludes a per-event reading — the ONE E49a-surface word-change authorized). Band comparisons stay keyed on TOTAL-LOSS (the OR convention, SL-4) with honest re-annotations (§5.4 watch rows).

### §5.3 Panel corrections folded in (3-lens default-refute Opus panel, this session — all three verdicts SHARPEN_FIRST → sharpened, unanimous on form)
1. The shed→rally "identity hole" REFUTED against live code (§5.1 last bullet) — pinned by fixture instead.
2. Per-phase re-arm named the one genuine sub-decision (it reintroduces a bounded per-commitment multiplier) → resolved by Aaron as answer 3, with the bound anchored to ACCUMULATED fielded and the rationale documented, and the evidence table confirmed to already use those semantics.
3. f's meaning-shift must be recorded as a supersession, not silently presented as "the anchor survives" → §5.2 answer 2 wording.
4. kw-flat is a WATCH, not a hard gate (shed men leave the fight — kw is second-order on the shed legs; a hard gate would flake).
5. The ≤f bound gate needs rounding headroom (`+ 0.5×unitsBroke`) and per-phase anchoring, or it false-reds on Antietam's 3 phases.
6. The `both` leg is non-additive (shed changes who routs/surrenders) — the isolation legs stay the attribution mechanism; never assert additivity.

### §5.4 E49b build battery — ADDITIONS on top of §3 verbatim (red → revert-file-halt; the two shipped protective teeth re-target in the SAME commit, the D258 precedent — never deleted)
- **probe-field re-targets:** the SL-1-ABSENT tooth (probe-field ~:163) flips to **FIRST-BREAK EXACT with men≠maxMen load-bearing** (men 600/maxMen 1000 → men 570, missing 30 — reds if the maxMen-anchored form is built by mistake); the phased `missing===0` assertion (~:241) flips to consistency+bound (missing>0 allowed; phaseLog sums == cumulative; ≤ f×battleFielded headroom; captured≤battleCas kept).
- **New teeth:** anti-compounding (the SAME unit re-broken sheds ZERO; `routEverCount==2` proves the second break fired — non-vacuous); 5-break regression guard (missing stays EXACTLY `round(1000×0.05)=50`; the dead form read 227 — reds if per-event returns); identity-exact (fielded = survivors + kw + captured + missing on shed→rally / shed→kill / shed→surrender); shed→rally men-unchanged (the §5.3-1 fixture); symmetry (US and CS units both shed in one asymmetric launch — the truthy-gate class); combined shed→surrender (captured banks the POST-shed 950, missing 50, no double-count); `_e49NoShed` honored (break fires, shed suppressed); sandbox inert.
- **Battery hard gates added:** missing>0 on the shed AND both legs at the heavy asymmetric battles (non-vacuity) · missing==0 on base AND surr legs EVERYWHERE (attribution) · `battleMissing[s] ≤ f×battleFielded[s] + 0.5×unitsBroke` per side/battle/seed (+ per-phase `phaseLog ≤ f×phaseFielded + nUnits`) — the structural invariant that would have caught D256's 15–30% · identity per side/battle/seed · determinism cmp ×2 on ALL FOUR legs · the §3.3 directions PLUS named pre-flip rows: **Gettysburg US 8/8 and Shiloh's current CS-majority direction MUST HOLD on the both leg** (shedding touches them before the E47 flips).
- **Watch rows (logged, never gated — SL-7):** kw base-vs-shed (expected ~flat; a large delta flags investigation, never auto-fails) · Chancellorsville per-seed CS clear/hold-TIME margins vs the timeout on base AND both (near-misses visible even without a flip; seed 42 named) · the MH ratio movement from 3.59 logged as **straggler-accounting compression, NOT an assault-physics fix — E53 assault-abandonment stays OPEN** · MH US total-loss (~3,807 expected) vs the Disputed 2,100–3,007 band annotated (kw ~2,561 stays in-band) · side-level shed fraction per battle (~4–5% heavy / ~0 light) · CS-vs-US missing comparable-order at MH/Shiloh/Chancellorsville (the anti-Lost-Cause row) · the D243/D254 ratio notes re-checked post-shed.
- **Flip-A/B guard (carried to the E47 flip milestones):** each Shiloh/Gettysburg flip A/B runs shedding ON and OFF — a flip that only holds WITH shedding is E49b EVIDENCE, never a tuning license (D257); a flip that shedding BREAKS is a real regression to surface. While Gettysburg sits unflipped, shedding adds a SECOND backwards column (missing) beside the D258 backwards captures — the D258 "flips must not slip" rider tightens.

## §6 — The Chancellorsville gate re-anchor (AARON, gate-owner decision, 2026-07-04, D261 — resolving `DECISION-NEEDED-e49b-chancellorsville.md`, answer "a")

The D260 build proved SL-1v2 healthy on EVERY structural gate (surr≡base on all 110 rows · the ≤f bound per side/battle/seed AND per-phase · shed fractions 2.8–4.8% = the §5.1 prediction · Gettysburg/Shiloh/MH named rows all holding · MH ratio 3.59→2.86 toward the band · MH US kw 2,547 ≈ in-band) yet red on §3.3's zero-headroom Chancellorsville clause — the SAME seed 42 that killed the D256 form, at **5.3× smaller and nearly symmetric amplitude** (CS 830/US 807), flipping the base's outlier t222 clear to an honest contested E48 timeout (buzzer menObj CS 3,406 vs US 3,252, both armies on the ring; base already clears in E48 overtime on 4/8 probe seeds). Two independent forms failing one seed identified the ZERO-HEADROOM GATE, not the form, as the binding constraint — a gate that decides shedding out of the game by acceptance. Aaron, as the gate's owner (D241/D254 earned it at 8/8; D256-(b) declined to relax it while the FORM was sick), re-anchored it for attrition-class changes — his answer "a" (chat, 2026-07-04), applying the fork note's first-listed MINIMAL wording:

- **The re-anchored clause (supersedes §3.3's "Chancellorsville CS 8/8 both sets" for E49b and later attrition-class landings):** Chancellorsville **CS 8/8 on the DEFAULT set AND CS ≥7/8 on the PROBE set**, and **any seed that fails to go CS on the shipping leg MUST be the honest-contested class** — `by=timeout` with BOTH sides' men physically on the objective ring at the buzzer (`menObj.US > 0 AND menObj.CS > 0`). **The D241 false-containment class stays a HARD RED at any count.**
- MH US ≥6/8 both sets and every other §3.3/§5.4 gate stand VERBATIM. **SL-7 stands untouched:** this re-anchor is a documented gate-owner decision about acceptance HEADROOM on one knife-edge seed — never a license to tune f or any term, and never a precedent for relaxing a gate to rescue a sick form (D256 kept that line).
- probe-chancellorsville's own standing tooth (CS ≥4/8 on the probe set) required NO change — it reads 7/8 under shedding. No probe assertion was weakened by this amendment.
- The s42 mechanism (a thinned assault grinding past the clock instead of breaking off; Chancellorsville kw UP on the shed legs while flat-to-down at 7/9 battles) is logged as **E53 assault-abandonment evidence** — the honest physics fix stays the E53 milestone, after the flips (SL-10).
