# GRAPHICS RUN — CONTINUATION KICKOFF (paste §PASTE into a fresh VS Code Claude Code session)

Written 2026-06-13 end of run g. The cinematic Modern-3D battlefield is **shipped + GPU-verified**; this continues the **3 queued items** Aaron locked in a mid-run 5-round discovery. **Personal project — NOT MJI.** Keep Blender.app open.

## STATE (verified — don't redo)
8 append-only chunks spliced into `civil_war_generals.html` at the single `/*__ENGINE_END__*/` anchor, each GPU-verified on the real Mac GPU (Intel UHD 617), 0 real pageerrors, **Classic proven no-regression**, default boot `ok=true`:
`M3D_POSTFX` (ACES+bloom+SSAO+grade+SMAA+grain, perf-tiered, DOF removed) · `M3D_TERRAIN_PBR` (9 CC0 material sets) · `M3D_HDRI_SELECT` (day/overcast/dusk by weather/tod) · `M3D_PARTICLES` (rain/snow + drifting smoke banks) · `M3D_PROPS` (instanced forests + buildings) · `M3D_PROPS2` (earthworks/stone walls/orchards/snake-rail fences) · `M3D_CAMERA` (idle drift + combat auto-frame) · `M3D_UI_SKIN` (additive HUD engraving).
Assets: `assets/3d/env/sky_{day,overcast,dusk}.hdr` + `assets/3d/materials/terrain/{clear,field,woods,hills,ridge,town,road,swamp,fort}_{albedo,normal,rough}.png` (all CC0 Poly Haven).
**READ FIRST:** `RUN-LOG.md` §"run g" (esp. ROUND-2 DISCOVERY + EXECUTED), `REVIEW-QUEUE.md` top, `3D-ASSET-PLAN.md` §5 (naming is LAW).

## THE 3 QUEUED ITEMS (locked decisions — honor exactly)
1. **Units = $0, FREE ONLY** (Aaron: "im not paying"). Tripo balance 0 (300 free already spent) → no paid animation. Path: **free Rodin/Hyper3D MAIN_SITE** (`secrets.local.json → hyper3d_free_trial.key = "vibecoding"`) **STATIC** models for the 8 core (`{soldier,cavalry,cannon,general}_{us,cs}.glb` → `assets/3d/models/units/`) + **procedural engine motion** (cannon recoil+smoke, horse bob/sway, idle bob) wired via the reserved `_m3dMixerUpdate(dt,now)` sub-hook (the `_m3dFrameUpdate` dispatcher already calls it guarded). **Hybrid LOD: real model close/selected, billboard far.** FIRST verify the free trial actually generates (switch Blender `blendermcp_hyper3d_mode='MAIN_SITE'` + the vibecoding key, submit one soldier, poll, import, export glb); if blocked/poor → billboards stay, log it. GLTFLoader + per-(type×side) cache already in `_m3dUnitModel` — a correctly-named `.glb` drop auto-loads.
2. **Portraits = widest net.** Real PD Brady/LoC photos for EVERY historical figure with a verifiable public-domain portrait (pull the full OOB leader roster — grep the OOB/leader data for names; the engine keys leaders by last name e.g. Lee/Grant/Jackson). **Clean photo in a simple period frame** (minimal processing). ALSO **upgrade the procedural engraving fallback**. The lead-badge MutationObserver (~line 9389) calls the CLOSURE `portraitFor` by bare name (not `window.portraitFor`) → needs a deliberate refactor: add a `name→PD-image` lookup INSIDE the portrait closure (`_renderPortrait`/`portraitFor`, ~line 8767/9369). Download photos to `assets/portraits/<lastname>.jpg` (same-origin → no canvas-taint).
3. **UI = fuller art-directed pass, self-verified** (richer engraved frames, period display type, drawn order-icons, card visual language) on top of the shipped `M3D_UI_SKIN` conservative base.
**Then:** keep pushing the graphics ceiling (water shader for rivers/fords, day/night). **Publish DEFERRED** (Aaron triggers DEPLOY.md). **Aaron won't eyeball/test** → self-verify everything.

## TOOLCHAIN / GOTCHAS (banked, reuse)
- **Splice:** `node .scratch/splice.mjs chunks/out/<NAME>.js` (asserts single anchor, inserts before it). `.bak` before each overwrite.
- **Gate:** extract `<script>` (single tag, line 298→end) → `node --check`; grep `0x[0-9a-fA-F]*[g-z]`=0; collision-grep every new `function` name = 0 before splice.
- **Real-GPU verify (you READ the PNGs):** `node tools/shot-postfx.mjs [battle] [tier] [wx] [fx]` (waits for the composer; `wx`=rain/snow/etc, `fx`=seed fire events for smoke banks). `node tools/shot-gpu.mjs <scene>`. `node tools/bootprobe.mjs` (default path). `node tools/diag-classic.mjs` (Classic canvas). **If a probe times out on page.goto, kill the stale server: `lsof -ti tcp:8765 | xargs kill`.** Classic shots need `_m3dDeactivate()` (already patched into both shot harnesses — modern is the boot default and hides #map).
- **Override-by-redeclaration:** this codebase wins by hoisting (last top-level `function` def wins). To extend my code, redeclare the fn (replicate prior body + add) OR define a guarded sub-hook the dispatcher already calls. `_m3dFrameUpdate` is the per-frame dispatcher (calls `_m3dWeatherUpdate`/`_m3dSmokeBankUpdate`/`_m3dMixerUpdate`/`_m3dCameraUpdate` + battle-change props rebuild).
- **CRITICAL:** `G`, `HEX`, `PALETTE`, `THREE_BASE` are `const`/`let` LEXICAL globals — reference by BARE NAME, never `window.G`. THREE loads ASYNC after the spliced script → NEVER use `new THREE.*` at chunk top level (build in factory fns at runtime). Never put a literal `*/` inside a block comment. Guard EOF-module TDZ. Honor `reduceMotion` + `gfxQuality` Auto/High/Low (target Intel UHD 617 — instancing/LOD mandatory, perf-tier everything).
- **Engine seams:** `__M3D` module ~10336; original `_m3dBuildTerrain`/`_m3dLoadEnv`/`_m3dUnitModel`/`_m3dBuildUnits` ~10500-10720; winning parity overrides + my chunks after ~11800 → end. Modern is the default renderer; Classic is the guarded fallback (NEVER regress it).
- **Keys:** `secrets.local.json` (gitignored) — Tripo (dry), fal.ai (PAID, don't use — $0 lock), Sketchfab (free CC0), Hyper3D free trial "vibecoding". Re-apply to Blender at startup if the scene reset.

## §PASTE (first message for the new session)
> Continue "The Civil War" graphics run (personal project, NOT MJI; full autonomy, Ultracode on, $0/free-CC0 only, single-file/CDN, build bar 200/100). Read `GRAPHICS-RUN-CONTINUE.md` (this file) fully, then `RUN-LOG.md` §"run g". The cinematic Modern-3D battlefield is shipped+verified (8 chunks). Execute the 3 queued items in order — (1) free Rodin static units (test the free trial FIRST; billboards stay if blocked), (2) widest-net PD-photo portraits + procedural-fallback upgrade, (3) fuller UI pass — honoring the locked decisions verbatim. Self-verify every splice on the real GPU (`tools/shot-postfx.mjs`, you READ the PNGs); `.bak` + parse/hex/collision gate before each; NEVER regress Classic. Aaron won't test — you self-verify. Confirm Blender reachable + free-Rodin viable, give a 3-line plan, then build continuously; surface 3 questions only at a genuine design fork.
