# AUTONOMOUS-RUN.md — the 12-hour max-productivity build plan ("The Civil War")

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D533 next-lane=LANE-021 state=DRIVE owner=Claude -->
> **Live status (D533, 2026-07-23):** LANE-021 parked-review-closeout opens with a committed Claude Code DRIVE contract for the E51 raidSupply honesty slice; the fork is adjudicated copy-amend, never a combat-model change.
>
> **Boundary:** D525 remains the product head. Only the `src/85-battle-bridge.js` hint copy, mechanical game/srcTree re-pins, and closeout docs may move in D534; the -6 morale and x0.8 ammo formulas, T2/T4/86 mechanics, all data, and every D74 surface are barred.
>
> **Authority:** D514 authorizes the formerly parked queue. ARC 7 Historical movement (D526/D528/D530 zero proved windows), ARC 9 Slice 5, and E46 remain blocked; the D249 washout receipt and D266 re-measure ground the copy-amend adjudication.
<!-- LIVE-HEAD-SUMMARY:END -->

**D514 completion rule:** `HANDOFF.md` owns the work-conserving loop and terminal condition. This manual executes the whole live ledger, not one bundle; each slice remains independently contracted, gated, documented, committed, and pushed.

**Written run i (2026-06-13). This is the operating manual for a long, unsupervised, self-perpetuating autonomous build run.** Aaron is away ~12 hours and wants the strategic Owner-Mode war built as far as possible, ship-quality, content-complete, fully self-verified. Personal project — NOT MJI. Ultracode on. $0/single-file. Build bar **200/100 — go above and beyond.**

---

## 0 · THE PRIME DIRECTIVE
Run the D514 completion loop until every live feature and issue is shipped or evidence-closed. Resolve reversible in-scope forks yourself and log them. Contract, gate, document, commit, and push each slice independently, then immediately cross into the next eligible slice or phase. A browser/full-suite gate, ordinary design decision, or milestone count is work—not a stopping point. Quality is non-negotiable; work conservation means blocked items never idle independent work.

## 1 · OPERATING PARAMETERS (Aaron-locked — do not re-ask)
- **Run the completion ledger, not one group** — use all unchecked D514-authorized items in `V1-CHECKLIST.md`, unresolved `REVIEW-QUEUE.md` findings, and open lane contracts. Continue across groups and phases after every clean push. A new capability still needs a complete contract and committed DRIVE take; the authorization removes the need to ask Aaron to select or approve reversible in-scope work.
- **Always decide & log** — never pause mid-run for a design fork. Make the best call, append it to `DECISIONS.md` with rationale, and mark the actual owning tool/model. Aaron overrides in review.
- **Commit AND push every milestone — but ONLY after PROPER VETTING.** (SUPERSEDES the old run-i "local commits only / no remote" line.) The remote is LIVE and PUBLIC (Aaron approved publishing 2026-07-03, D237; Pages serves the game at https://adhanke-star.github.io/History-Video-Game/): `origin` → **github.com/adhanke-star/History-Video-Game**, branch `main`. **PROPER VETTING gate is tiered and batched (D160 + D176; precondition for commit + push):** always confirm `git status`, honor the source/doc read order, edit only source/data (never `build/base.html`, never hand-edit `civil_war_generals.html`), run `node tools/build.mjs` → `GATE OK`, run any relevant importer/schema gate, run the focused probe, read outputs/JSON for `ok`/pageerrors, and run `git diff --check`. JS/runtime changes also require `node --check` on touched JS/probe files and 1-3 adjacent probes. During Aaron's explicit same-chat queue loop, do not spend the full `npm run vet:noreg` battery after each queued item, even when shared surfaces are touched; keep it intact and run it at the end-of-queue planned-work batch/release checkpoint or when Aaron explicitly asks. Never push an unvetted / red / regressed / pageerror focused build.
- **D514 work-conserving loop supersedes D171/D307 cadence.** Do not stop because 2–4 slices shipped, a phase changed, a browser/full-suite checkpoint arrived, or a reversible design fork surfaced. At each clean push, refresh the ledger and continue. If one item is blocked, mark the exact blocker and choose the next independent item. Stop only at the D514 terminal/hard-stop conditions or when capacity requires a clean provider relay.
- **Mandatory session closeout + provider relay (Aaron, 2026-07-11; D514 cadence).** Every actual closeout returns a fenced, disk/git-grounded continuation prompt, but a normal milestone is not a closeout. Continue while capacity is healthy. At a D514 terminal/hard-stop or capacity boundary, name the provider/top loop, HEAD/origin and clean/dirty state, exact active/next item, read order, lane owner/state and locks, resume command, gates, completion-loop authority, and no-simultaneous-edits rule; hand off before hard exhaustion.
- **Load-bearing prompt depth (Aaron, 2026-07-11).** Every continuation prompt is a zero-context execution packet that matches or exceeds the run opener's operational detail. Carry the full phase plan, exact baselines/invariants, file seams/exclusions, every gate command and artifact readback, bind/A-B/restore laws, HALT conditions, docs/commit/push/lane release, and final-report schema. Do not compress these into pointers to a spec, earlier prompt, or private context. Prompt fidelity outranks brevity. Audit the prompt against the opener and live contract before delivery.
- **Content-complete each phase** — real citation-grade data (Verified/Inferred provenance, named scholars) + PD images, gathered via research workflows, written to disk *before/while* building the system. Never ship placeholder numbers as if real.
- **Above and beyond** — polish to ship-quality each phase: period art (R30), ambient teaching (R26), ant the balance principle (§27: simple core stands alone, all depth optional/advisor-managed/non-blocking).

## 1A · UNIVERSAL AI IMPLEMENTER STANDARDS (all VS Code agents)
These rules apply to Codex, Claude, Cline, DeepSeek, and any other model implementing project instructions in VS Code.

- **One standard, many models:** every model must follow `START-HERE.md`'s Universal AI implementer standards, this manual, the latest `HANDOFF.md` top block, and the latest `DECISIONS.md`. Smaller models do not get a lower quality bar; they only get smaller, prepackaged tasks.
- **Recursive repo-learning:** each work loop reads the repo's current lessons, makes the smallest safe change, verifies it, studies failures, then writes durable learning back to the repo (`DECISIONS.md`, `RUN-LOG.md`, `HANDOFF.md`, `WAKE-UP.md`, checklist notes, or probes). No model may depend on hidden memory as the project record.
- **Self-audit before success:** review the diff against the user's ask, identify unintended simulation/gameplay/UI/history changes, inspect generated outputs/JSON/screenshots, scan console warnings/pageerrors, and state residual risk. A model that did not run a check must say so. Full-suite deferral must be stated plainly when a slice ships on focused gates.
- **Guarded implementation:** preserve Classic, avoid broad refactors, keep seams no-op when inactive, honor reduceMotion/accessibility/performance, keep history provenance honest, and use only $0/free/clear-license assets unless Aaron explicitly approves otherwise.
- **Model-appropriate routing (D336 + D361):** run the main ChatGPT/Codex integration loop on **5.6 Sol at Ultra (the highest effort available in that UI)** for complex architecture, important code, historical judgment, UI/UX and accessibility decisions, adversarial review, final verification, and any quality-critical work. Configure helper model and effort explicitly when controls exist. If the surface has no per-helper controls, inheriting helpers may perform bounded, output-insensitive evidence work such as read-only inventories, log/artifact summaries, gate execution, and independent mechanical audits; state the inheritance once. Every helper gets exact files, constraints, acceptance checks, and a stated purpose. The main loop verifies every packet and reThe exact current boundary is the first ⚡ amendment in [`HANDOFF.md`](HANDOFF.md). The only current task map is [`V1-CHECKLIST.md`](V1-CHECKLIST.md). This manual supplies execution discipline; it does not independently choose the next feature.

2 refreshes (D417 and earlier, plus the retained run-i state tail):** moved byte-verbatim to [`legacy/AUTONOMOUS-RUN-ARCHIVE.md`](legacy/AUTONOMOUS-RUN-ARCHIVE.md) — D417 moved at D420; D413 moved at D418; D412 at D417; older blocks moved in D412. This section keeps the latest refresh plus exactly one prior; the canonical live boundary is `HANDOFF.md`'s top ⚡ block.

## 3 · THE LOOP (run this every cycle)
For each phase, in order:
1. **Plan** only the next approved item from `V1-CHECKLIST.md`, the first HANDOFF amendment, the relevant COORDINATION lane, and that task's design law. This file's historical roadmaps do not authorize work.
2. **Content** — if the data isn't already in `data/*.json`/`HISTORICAL-DATA*.md`, launch a **research workflow** (8-ish parallel citation-grade agents → synthesis writes `data/<topic>.json` + a `HISTORICAL-DATA-<TOPIC>.md` digest to disk). Run it in the background; build other parts meanwhile.
3. **Build** — author new `src/NN-*.js` module(s) (bare-name globals `G`/`GAME_DATA`, never `window.G`; unique helper-name prefixes; NO literal comment-closer inside block comments; extend existing systems, don't duplicate — register ticks via the `_t1InitAll`/`_t1Resolve` overrides in `src/90-president-register.js`). Add to `src/00-manifest.json` (+ `overrides` if redeclaring a base fn). Surface in the desk (a tab or an existing overview); keep it advisor-managed + non-blocking (§27).
4. **Gate** — `node tools/build.mjs` (must print `GATE OK`; it parse/hex/collision-checks in memory before writing). Fix any failure; never proceed red.
5. **Verify** — write a focused empirical `tools/probe-<phase>.mjs` (model on `probe-economy.mjs`/`probe-production.mjs`: boot real Chrome on :8765 when the probe does not auto-start it; run the system over turns via `_t1Resolve`; assert the intended behavior + the historical asymmetry; screenshot the UI). **READ the JSON + PNGs.** Apply the D160 tier: importer/schema gate where relevant; `node --check` plus 1-3 adjacent probes for JS/runtime changes; full `npm run vet:noreg` only for shared lifecycle/manifest/build/save/bridge/tactical/render/accessibility, suite-probe changes, or large milestone/release-batch pushes. Require `ok=true` and 0 pageerrors; require Classic paints when the selected tier touches visual/engine/Classic paths or runs the full suite.
6. **Commit and push** (clear message). Append to `RUN-LOG.md` (§"run i" or a new run header) + `DECISIONS.md`.
7. **Update the live boundary** — write the full boundary once in HANDOFF, update V1 when roadmap status changes, and copy the exact delimited live-summary block into all six routing docs. The build's doc-coherence gate must pass.
8. **Reconcile and continue (D514 — every cycle).** After each clean push, re-read the live summary, completion ledger, relevant lane states, and fresh artifacts. Select the highest-value dependency-ready item, contract/take it, and continue even when it crosses a phase or requires browser/full-suite work. If one item blocks, write its exact state and recommendation to disk, release/quarantine its lane safely, and select another independent item.
9. **Relay only for a real terminal or capacity boundary.** Stop only when the ledger is exhausted; every remaining item shares one hard blocker; destructive/paid/account/licensing authority is required; a genuine source/design contradiction cannot be solved conservatively; or provider context/usage cannot carry the next bounded slice. Capacity produces a clean, load-bearing continuation prompt that resumes the same completion loop; it never narrows authorization back to one isolated feature.

**Use available delegation deliberately, never ceremonially.** Use independent research, exact-file mechanical work, and adversarial verification only when the task benefits from it; explicit model/effort controls are mandatory whenever the surface exposes them. The **5.6 Sol Ultra** main loop owns synthesis, judgment, and integration. Periodically run a no-regression review over built systems. Long autonomous runs embed the `OPUS-PLAYBOOK.md` standard snippets.

## 4 · HISTORICAL RUN-I PHASE ROADMAP — non-actionable reference

> This section records the original run-i plan. Many items later shipped or were superseded. It never selects current work; use V1-CHECKLIST plus HANDOFF.
- **S1c — cotton / blockade / foreign** (`data/economy.json` → `cottonBlockade`). New `src/60-blockade.js`, `G.campaign.blockade`. King-Cotton 1861 self-embargo trap; blockade capture-prob schedule 0.10→0.33; runner economics → **gate the CS small-arms `importFactor`** that `prodOnResolve` already reads (wire the S1b placeholder); cotton revenue → funds; foreign-recognition pressure (battlefield + cotton + antislavery) → `clock.intervention`. Blockade-depth **toggle** (full / flat-modifier / off, R14). Diplomacy view on the desk.
- **S1d — manpower / conscription** (`data/economy.json` → `manpower`). `src/70-manpower.js`, `G.campaign.manpower`. Recruitment pools, the draft (US 1863 / CS 1862+exemptions), the **late-war CS replacement-ratio collapse 1.0→0.1** (a war-ending pressure), immigrant pool, enslaved-labor base (ties to S2 emancipation/contraband). Feeds army strength (→ the pre-battle bridge in S5).
- **S2 — executive decisions + 3-layer morale + 1864 election + the ADVISOR SYSTEM (the linchpin, R25).** Research workflow for political/decision content (cabinet recommendations + teaching, decision cards, home-front events). Each secretary auto-manages + recommends + TEACHES its domain (generalize the Treasury delegate seed across all domains). Per-turn curated decision cards via `president.pendingChoices` (the S0 stub). 3-layer morale (troop seed / leader reputation+ambition / public — public partly in `clock.weariness`). Wire the 1864 election hinge (`clk.resolved1864`) to consequences.
- **S3 — alt-history engine** (§5). Tiered divergence (plausible/long-shot/fantastical) + hinge-point forks + emergent-only toggle + curated scenarios; deterministic-by-performance with consensus framing; the living **"your war vs history" tracker** + end report card.
- **S4 — education layer** (§7, R26). Multi-voice debate cards (the 28 economy cards exist; add more from `HISTORICAL-DATA.md`) + deep-dives + a growing **codex**; difficulty/realism sliders; **play-style presets** (General/Commander/President/Historian, R27) that configure all toggles. Full a11y (R33).
- **S5 — victory/defeat + the strategy↔battle BRIDGE** (§8, §11). Asymmetric win paths incl. **negotiated peace (both sides)**; hard-loss = military-only. Wire economy/production/manpower/morale → **pre-battle conditioning** (army strength/weapons/morale/fatigue) → battles use the EXISTING engine via a clean bridge → results feed back → **a playable full Owner-Mode war.**
- **THEN tactical P0→P5** (`MODERN-UGG-PLAN.md`): the real-time UG:G field engine, First Bull Run vertical slice first, swapping in behind the bridge.

## 5 · GUARDRAILS (a 12-hour unsupervised run lives or dies on these)
- **Never commit a red build** (gate + probes must pass; 0 pageerrors; Classic paints via `diag-classic.mjs`).
- **Never regress Classic or the existing systems** — extend by new modules + the registered `_t1InitAll`/`_t1Resolve` hooks; redeclare a base fn ONLY when necessary (add to manifest `overrides`); use the D160 tiered gate and run the full probe suite whenever shared behavior or a release batch requires it.
- **Commit per phase** = revertible checkpoints. **Update the handoff per phase** = recoverable across context summarization (your disk state — commits + `DECISIONS.md` + `RUN-LOG.md` + this file — is the source of truth, not your context).
- **Decide & log; don't ask.** Stop only under D514: an unavoidable destructive/irreversible or paid/account/licensing action, a genuine locked-law contradiction with no conservative resolution, all remaining work sharing one hard external blocker, or capacity requiring a clean relay. Record the exact blocker and recommendation; otherwise quarantine the blocked item and continue independent work.
- **Probes flake** on headless-swiftshader `page.goto` (heavy 3D boot) — bootprobe goto is 60s; if a probe times out, start the server yourself + retry; an A/B against `build/base.html` distinguishes a real regression from flakiness.
- **Gotchas:** scan `src/` for stray `*/` before each build (it parse-bombs); `portraitFor` → `window.portraitFor` from modules; THREE loads async; honor `reduceMotion`/`gfxQuality`.

## 6 · SUCCESS CRITERIA (what "above and beyond" looks like at hour 12)
A maximally-complete, ship-quality, **playable Owner-Mode war**: pick a side → run economy/production/manpower/blockade/diplomacy/politics through a characterful advisor-managed, non-blocking, deeply-teaching desk → fight the existing battles through the bridge → win via an asymmetric path (incl. negotiated peace) → get a "your war vs history" report. Every system is content-complete + self-verified + committed/pushed, with a clean `DECISIONS.md` trail and current handoff. D514 success requires zero unadjudicated live ledger items, a green full release battery, clean artifact/a11y/history/code audits, all confirmed findings repaired or evidence-closed, and clean `HEAD == origin/main`.

**To resume after a stop/summarization:** read START-HERE, the relevant COORDINATION lane, HANDOFF's first amendment, V1-CHECKLIST, unresolved REVIEW-QUEUE entries, the task law/probes, the latest DECISIONS entry, and live git. Resume the exact active slice if dirty/owned; otherwise choose the next dependency-ready ledger item and continue the D514 loop.

## 7 · HISTORICAL RUN-J BACKLOG — non-actionable reference

> The old ordering and every `NEXT` statement below are chronology only. They do not reopen shipped work or outrank V1-CHECKLIST/HANDOFF.

> **⚑ CHARTER-INTEGRATED PRIORITY ORDER (run j; the §8 charter expands this with ~30 new systems — build them folded into the phases below, honoring §8.12 "max systems shipped"):**
> **0. FINISH A1 Cannon Corps** (scaffolded — see §2; build→probe→bug-hunt→commit). **0b. GitHub repo setup + first push** (`adhanke-star/History-Video-Game`, private — §8.4; do EARLY so every milestone backs up). **0c. Write root `WAKE-UP.md`** at session end (§8.13).
> **1. BATTLE LAYER (A2–A6):** Engineering Works Corps → terrain-cover hierarchy → wire Armory→field → targeting → battle-day conditioning, INCLUDING the new **bridge auto-resolve** (§8.19) so the war is playable end-to-end before P0–P5.
> **2. STRATEGIC ARC (S2–S5), charter-expanded:** S2 = the FULL historical cabinet (§8.22) w/ one-line-rec+delegate+why advisor UX (§8.54) + 3-layer morale (§8.61, public-will-decides-late §8.67) + the 1864 election + the **press/public-opinion system** (§8.57) + **command: appoint/promote/relieve generals** (§8.58) + **emancipation as a dated decision** (§8.27) + **civil-liberties dilemmas** (§8.62) + **named-generals system** (§8.24) + **curated home-front event deck** (§8.25, cadence §8.68). S3 = alt-history + the **alt-history gallery/milestones** (§8.44). S4 = the **multi-axis codex** (§8.41) + **inline glossary** (§8.47) + **full guided tutorial** (§8.53) + **play modes & realism/teaching layer** (§8.60) + **FULL WCAG 2.2 AA** (§8.18) + the **a11y modes** (§8.46) + difficulty presets. S5 = victory (multiple paths incl. negotiated peace §8.52) + the **rich graded end report** (§8.26) + the **Reconstruction coda** (§8.35) + wire all battles through the bridge.
> **3. CONTENT SYSTEMS (fold in where they fit):** shipped so far include logistics/rail, POW exchange-collapse, disease/medical, hard war, irregular war, under-told perspectives, flagship named units, the M1-M7 graduate-history corrections/source/codex/readout pass, war-finance civics readout, lived-slavery + Black combat-agency, the CS finance toolkit/Erlanger system (§8.36), real diplomacy (§8.63), human-cost-with-gravity treatment (§8.50), and Western-theater strategic readouts; the planned Group 4 batch/release gate shipped as D192. Current order remains Group 5 Phase H polish/media/perf with media-budget/profile gates, then meta/deferred tooling, then the last battle-build queue only after Aaron go/no-go.
> **4. THEATERS (ordered):** Eastern → Western (same mechanics, new battles: Antietam, Vicksburg, Chickamauga/Chattanooga, Atlanta/March — §8.66) → Trans-Miss → naval system (§8.28). Cavalry = DEFERRED (§8.30).
> **5. TACTICAL P0–P5** — only after the battle layer + S2–S5 are solid (§8.10).
> Every item: auto-tune balance + log (§8.7) · adversarial bug-hunt before every commit (§8.9) · ≥2-source provenance (§8.51) · period-but-tight voice (§8.17) · push to GitHub. The §7 A/B/C below is the original battle-layer/strategic/tactical skeleton these phases extend.

**A · BATTLE LAYER (Aaron's live directives — do these FIRST; they make the war playable end-to-end):**
1. **Cannon Corps** — an artillery tier in The Armory: 12-pdr Napoleon, 3-inch Ordnance Rifle, 10-pdr Parrott, Whitworth (rare). Buildable as a corps (battery counts) feeding the bridge "firepower" + battle artillery. Additive, like `55-weapons.js`.
2. **Engineering Works Corps** — Haupt's USMRR Construction Corps: rail/bridge repair, fieldworks/entrenchment, pontoons, siege. A buildable branch that raises the bridge's entrench/supply/fatigue facets + slows CS rail decay (ties `production` rail). Additive.
3. **Terrain cover hierarchy** — add named cover: **stone wall (strong), forest (strong), wood fence (less strong), trenches, boulders, sunken road**, slotted into the EXISTING `TERRAIN` def/cover model (base.html line 305: woods ×1.40 / ridge ×1.55 / town ×1.60 / works ×2.10 already exist). NOTE: `TERRAIN` is a frozen `const` — you cannot redeclare it; add cover via authored-map features (`authoredFeatures`) or an override of the map/combat fn. Investigate `genMap`/`authoredMap`/`fireTargets` first.
4. **Wire The Armory → the battlefield** — the weapons bought in `C.armory.loadout` become what the player's brigades carry + fire (connect `data/weapons.json` quality → the base `WEAPONS` table / `genForce` unit weapon assignment). Probe that Spencers bought → higher unit firepower in battle. (Touches the frozen engine → override carefully, gate with `diag-classic`.)
5. **Targeting** — the engine already has `fireTargets` (base 1003); verify the player can pick specific targets + expose/polish it in the UI.
6. **Battle-day CONDITIONING application** — override `startBattleRuntime` (base 772) to scale the player force (strength/morale/firepower from `bridgeArmy`) + apply `C.battlePrep` (entrench/forced-march/…) AFTER `genForce`; results feed back to `manpower`/`clock`/`strategy.enemyWill`. THIS closes the strategy↔battle loop. Gate hard with `diag-classic` (NEVER regress Classic).

**B · STRATEGIC ARC (the S-phases, GRAND-STRATEGY-PLAN §12):**
7. **S2** — executive decisions + 3-layer morale + the 1864-election hinge + **the advisor/cabinet system (R25 linchpin)**. CONTENT ALREADY RESEARCHED: recover it via `Workflow({scriptPath:"<session>/workflows/scripts/s2-content-research-wf_6f68a916-ce9.js", resumeFromRunId:"wf_6f68a916-ce9"})` (cached → 8 advisor profiles + 8 decision cards) → write `data/cabinet.json` + `data/decisions.json`. If the run-id is gone, re-run a fresh advisor+decision-card research workflow. Generalize the per-domain Delegate + ambient teaching across all secretaries; wire `president.pendingChoices` decision cards; 3-layer morale (troop/leader-reputation+ambition/public); wire `clk.resolved1864` → consequences.
8. **S3** — alt-history engine (tiered divergence + hinge-point forks + emergent-only toggle + curated scenarios; the living "your war vs history" tracker + end report card). The `C.strategy.wildsPlayed` + `enemyWill` + `victoryReady` are the seeds.
9. **S4** — education/codex (the 15 citation-grade cards in `data/{diplomacy,manpower-teaching}.json` + economy cards feed it; difficulty/realism sliders; **play-style presets** General/Commander/President/Historian; full a11y R33).
10. **S5** — victory/defeat resolution (negotiated peace for both sides — `victoryReady` hook already fires) + wire all battles through the bridge → a playable full Owner-Mode war.

**C · TACTICAL ARC:**
11. **P0–P5** — the real-time UG:G tactical engine (`MODERN-UGG-PLAN.md`), First Bull Run vertical slice first, swapping in behind the bridge. This is where the true *Ultimate General: Gettysburg* real-time feel (continuous maneuver, real-time firefights, free aiming) lands — currently ~0% built (battles run on the frozen hex/turn "Classic" engine + a 3D skin).

**Working norms for run j:** ultracode ON — use research/build/adversarial-verify workflows aggressively; content-complete each system (citation-grade data + named scholars); decide & log every fork to `DECISIONS.md`; gate (`node tools/build.mjs` → `GATE OK`) + empirical probe (you READ the JSON/PNG) + the D160 tiered no-regression policy + commit locally, **per item**; keep the handoff (§2 + §7 + `HANDOFF.md`) current; **git stays clean — `tools/shots/*` + `.tmp/` are gitignored, commit promptly so the pending view stays at ~0.**

## 8 · OVERNIGHT AUTONOMOUS-RUN CHARTER (standing operating constraints; its old scope order is historical)
**The standing authorization for all-night unsupervised runs. Read this FIRST each run; it governs every fork below so the run never stalls. Recorded as D30 in `DECISIONS.md`.**

1. **Scope order** — BATTLE LAYER first (A1 Cannon Corps → A2 Engineering Corps → A3 terrain cover → A4 Armory→field → A5 targeting → A6 battle-day conditioning) → STRATEGIC ARC S2→S5 → TACTICAL P0–P5 last.
2. **Frozen-engine edits — AUTHORIZED.** Override `base.html` functions via NEW `src/*.js` modules (never edit `base.html` directly), gate every change with `diag-classic`, **revert instantly on any Classic regression.** Required for A4/A6.
3. **Halt rule** — stop & notify ONLY for (a) genuinely irreversible/destructive actions git can't undo, or (b) anything costing money / creating an external account. **Everything else: decide, log to `DECISIONS.md`, keep going all night.**
4. **Backup → GitHub (authorized).** PRIVATE repo **`History-Video-Game` under the `adhanke-star` account**; add as remote; **push after EACH committed milestone** all night. (This is the one pre-cleared external action.)
5. **Save schema** — bump `_SAVE_VER` WITH an idempotent lazy migration when a feature needs it (never silently erase saves — D7 pattern). Prefer additive when possible.
6. **Compute** — UNLIMITED; use research / parallel-build / adversarial workflows as aggressively as quality demands.
7. **Balance** — AUTO-TUNE via simulation sweeps toward §27 + historical targets; **log every number changed** to `DECISIONS.md`.
8. **Visual art** — LIGHT TOUCH, code-only (CSS / inline SVG / procedural canvas) that gates + verifies like code. **No external/Blender/3D asset pipelines unattended.**
9. **Verification** — ADVERSARIAL BUG-HUNT workflow for nontrivial code (skeptics attack the new system's math/edges) + the D160 tiered probe gate. Full no-regression is mandatory for shared surfaces, suite changes, and release batches; narrow content slices use focused/importer/schema gates with periodic full-suite sweeps. Near-zero regressions by morning.
10. **Tactical engine** — start P0–P5 ONLY after the battle layer + S2–S5 are solid.
11. **If the backlog clears** — POLISH + DEEPEN within the existing design (teaching, a11y, balance, more wild-cards/battles, bug-hunts). **Do not invent major new systems unattended.**
12. **Morning goal = MAXIMUM SYSTEMS SHIPPED** (breadth), each gated + probed + verified.
13. **Handoff** — keep `RUN-LOG.md` + `DECISIONS.md` live; write a root **`WAKE-UP.md`** each session: what shipped, what's playable, a demo click-through, open questions, anything flagged.
14. **History stance** — UNFLINCHING, scholarly, ANTI-Lost-Cause: slavery-as-cause stated plainly, Black agency/USCT centered, myths named + countered, citation-grade multi-voice. CS playable without endorsing its cause.
15. **Difficulty** — ACCESSIBLE default; the General/Commander/President/Historian presets HARDEN realism + teaching for experts.
16. **Content conflicts** — AUTO-CORRECT on-disk numbers to better-sourced values + log the citation (the Erlanger 72%→45% pattern).
17. **Player voice** — period-flavored but TIGHT; depth opt-in (codex/cards); never a wall of text on an action screen.
18. **Accessibility — FULL WCAG 2.2 AA** (keyboard, visible focus, contrast, ARIA, reduced-motion honored, SR labels), built-in as I go; run `wcag-auditor` on new UI. (Aaron does this professionally — hold the MJI bar.)
19. **Battle resolve — ADD auto-resolve from the bridge** (conditioned army + variance) so the owner-mode war is PLAYABLE end-to-end NOW; keep the option to fight the existing tactical battle. (New backlog item, folds into A6.)
20. **Onboarding — build a FULL guided tutorial** scenario (layered, skippable for experts). (New backlog item, in S4/education.)

**New backlog items spawned by the charter (insert into §7 ordering):** GitHub repo setup+push (do EARLY so milestones back up), auto-resolve battle path (with A6), difficulty presets + WCAG-AA pass + full tutorial (S4), a balance auto-tune harness, root `WAKE-UP.md` each session.

### §8 addendum — design-scope decisions (popup rounds 8–9, Aaron-locked 2026-06-13; recorded as D31)
21. **Theaters (ordered expansion)** — Eastern first → Western core (Shiloh/Vicksburg/Chattanooga/Atlanta) → Trans-Mississippi → coastal/naval. Build outward in that order.
22. **Cabinet = FULL historical roster** per side, incl. the churn (Davis's revolving-door War secretaries). Each advisor: voice + recommendations + ambition + ambient teaching.
23. **Start modes (all three)** — the 1861 campaign (default) + curated "what-if" scenario hinge-starts (post-Antietam recognition crisis, 1864 election eve, Vicksburg siege…) + a FULL-SANDBOX toggle (pick year/side/conditions).
24. **Named-generals system** — historical commanders (Grant/Lee/Sherman/Jackson/McClellan/Bragg/Thomas…) with traits (aggression/caution/skill), evolving reputation, and ambition (the political-general problem). Feeds the bridge **leadership** facet + S2 morale + promotions. (Replaces the leadership placeholder.)
25. **Home-front = curated event deck** — real possibilities (NYC draft riots, Richmond bread riot, Trent affair, Sanitary Commission, Copperhead surges) firing on conditions, presenting real choices. Teaching, not random noise.
26. **End report = rich graded after-action** — turn-by-turn divergence from history, a letter grade per domain (military/economy/diplomacy/home front), your casualties vs historical, and "what really happened" citations. The teaching payoff lands here.

### §8 addendum 2 — content-system decisions (popup rounds 10–11; recorded as D32)
27. **Emancipation = a dated Union player decision** with real trade-offs (radical 1861 vs historical Sept 1862 vs never): border-state loyalty ↔ USCT manpower ↔ foreign opinion ↔ war aims. The moral+strategic heart, taught honestly. (CS mirror = the existing Cleburne arm-the-enslaved gambit.)
28. **Naval = a dedicated system built at theater-expansion** (coastal/Western phase): ironclads (Monitor/Virginia), commerce raiders (Alabama), the river war (gunboats→Vicksburg); feeds the blockade + Western campaigns. NOT now.
29. **Disease/medical = a real attrition driver + teaching layer** — disease killed ~2× combat; camp sanitation/hospitals/Sanitary Commission as levers; the USCT's higher disease toll; Clara Barton. Corrects the romance of war.
30. **Cavalry = DEFERRED** to a supervised session (the one round-8–11 "later", not "now").
31. **Logistics = a rail/supply-network system** — rail as arteries (Haupt's USMRR repair vs CS iron decay; ties A2 Engineering Corps + production), supply depots, marches-on-the-stomach effects on the bridge supply/fatigue facets.
32. **POW = the exchange-cartel collapse thread** — the 1863 cartel breakdown over USCT prisoners (a moral fault line) + its manpower consequence (no exchanges = both armies bleed and can't refill) + Andersonville/Elmira honestly.

### §8 addendum 3 — systems & atmosphere (popup rounds 12–13; recorded as D33)
33. **Hard war = a system with strategic + moral weight** — the 1864–65 escalation (Sherman's March, Sheridan in the Valley): a lever to break enemy will/logistics AND an honest reckoning with total war's cost to civilians and the enslaved who followed Sherman. Ties `enemyWill` + logistics.
34. **Turn pacing = FIXED ~monthly turns** (Aaron chose simple over the §22 variable weekly/seasonal design). The S0 placeholder cadence becomes the design.
35. **Reconstruction coda = a short honest epilogue** — emancipation made real (13th/14th/15th), the promise + betrayal of Reconstruction, Foner's "unfinished revolution"; winning the war ≠ winning the peace. Scales to how your war ended.
36. **Confederate finance toolkit = BUILT** — the Erlanger cotton loan (corrected ~45% net already on disk), cotton bonds, impressment, the produce loan, the printing spiral. Real, doomed CS choices: the South couldn't pay for its war.
37. **Irregular war = a contained thread** — Mosby tying down Union troops, the Missouri/Kansas bushwhacker war (Quantrill, Lawrence), the Union's hard anti-guerrilla policy. Events + a small system; the war's ugliest face, honestly.
38. **Audio = RICHER** (Aaron upgraded from light) — public-domain period tunes (Battle Hymn, Dixie, When Johnny Comes Marching Home, Bonnie Blue Flag) + ambient battlefield/camp soundscapes via the code-only synth; toggleable, off by default, honor audio/reduce-motion prefs.

### §8 addendum 4 — perspectives, AI, scope (popup rounds 14–15; recorded as D34)
39. **Under-told perspectives = ALL FOUR threads, woven in + sourced** (not tokenized): enslaved people's agency (self-emancipation, the general strike — Du Bois — spies/guides, beyond USCT enlistment); immigrant/ethnic units (Irish Brigade, German regiments, the ~25% foreign-born Union army + nativist tension); Native nations (Ely Parker, Watie's Cherokee, Trans-Miss, divided loyalties); women's roles (Barton/Dix nurses, Van Lew/Boyd spies, soldiers in disguise, home-front managers).
40. **Enemy AI = a reactive strategist** — makes its own strategic choices (shifts resources, raids, fortifies, sues for peace when `enemyWill` breaks, exploits the player's weak points); difficulty presets tune how sharply it plays.
41. **Codex = multi-axis** — browse by timeline + topic + person + battle, cross-linked, Verified/Inferred provenance shown.
42. **Single-file may GROW** — honor $0/single-file; let `civil_war_generals.html` reach whatever content needs (several MB is fine for a local open-and-play file). Don't trade scope for bytes; keep the build gates.
43. **Information = MOSTLY TRANSPARENT** (Aaron chose this over a fog-of-war toggle) — show the player accurate strategic state; a clean strategy puzzle, no fog model.
44. **Replay = an alt-history gallery + teaching milestones** — collect triggered divergences/wild-card outcomes ("sparked the Northwest Conspiracy"; "held Vicksburg") + milestones rewarding historical exploration. Replay + learning, not grind.

### §8 addendum 5 — saves, a11y, presentation (popup rounds 16–17; recorded as D35)
45. **Saves = autosave + named slots + undo-last-turn on the accessible difficulty** (undo disabled on harder/ironman presets). Forgiving for learners, stakes for experts. As of D145, named slots/export/import exist in `src/91-save-slots.js`; remaining work is hardening/probing reinjection/export/import and adding the accessible undo-last-turn preset.
46. **A11y modes = ALL FOUR on top of baseline AA** — a high-contrast theme toggle; CVD-safe palettes (never rely on Union-blue/CS-red alone — add patterns + labels); screen-reader ARIA-live narration of turn/battle events; a dyslexia-friendly font + spacing toggle.
47. **Inline glossary = every term** (enfilade, defilade, canister, habeas corpus, greenback, contraband…) tappable → short definition + Verified/Inferred provenance, linked to the codex. Opt-in surface teaching.
48. **Devices = responsive, desktop-first, tablet/phone playable** — design for the desk/maps on a big screen, but responsive + touch-friendly; fully playable on tablet, readable/usable on phone.
49. **Art direction UPDATED (D145 supersedes broadsheet-everywhere):** the app shell should become a more video-game-like historical command interface: brighter, rounder, more dramatic, icon-led, and centered on public-domain photos/illustrations where they clarify people/place/stakes. Broadsheet remains the in-world language for press/editorials/dispatches/provenance, not the whole interface. Preserve Civil War gravity, anti-Lost-Cause framing, WCAG 2.2 AA, reduceMotion, CVD-safe cues, and the Intel UHD-617 floor.
50. **Human cost rendered WITH GRAVITY** — beyond aggregate numbers: a regiment's losses, an occasional named soldier/letter, the scale made legible (a county emptied) — restrained, no spectacle, no trivializing. ~750,000 dead is the teaching heart.

### §8 addendum 6 — content standard, victory, tutorial (popup round 18; recorded as D36)
51. **Provenance standard** — a claim is **Verified** only with ≥2 independent reputable sources; single-source or my own calibration = **Inferred**; **nothing ships untagged**. The adversarial-verify pass strips fabricated/misattributed citations (as already done for diplomacy/manpower).
52. **Victory = multiple honest paths per side, incl. negotiated peace** — CS wins by exhausting Northern will (negotiated peace), foreign recognition, or holding to 1865; US wins by conquest, breaking the rebellion, or CS political collapse; hard-loss = military destruction. (The §8/§29 design; `enemyWill`/`victoryReady` already seed it.)
53. **Tutorial = an interactive guided scenario, skippable** — a real short slice of 1861 that teaches the desk + war loop by DOING, with optional deeper "why" callouts; fully skippable for experts. Learn by playing, not by reading.

### §8 addendum 7 — advisor UX, scenarios, thresholds (popup round 19; recorded as D37)
54. **Advisor UX** — each advisor gives a one-line recommendation + one-click Accept or Delegate + an expandable "why" (reasoning + teaching); an advisor's AMBITION surfaces when they push self-interest over the war. Fast to play, deep on demand (§27).
55. **Scenario build order = ALL FOUR hinge-starts** — Antietam/recognition-crisis (Sept 1862), Vicksburg siege (1863), 1864 election eve, First Bull Run (July 1861, doubles as the tutorial scenario).
56. **Win thresholds = accessible default, harden with presets** — on default, victory paths are genuinely reachable with good play (the South CAN win by exhausting Northern will; the North can win faster than history); presets tighten for experts.

### §8 addendum 8 — press, command, units (popup round 20; recorded as D38)
57. **Press = a public-opinion system** — period papers (Greeley's Tribune, the Richmond Examiner, Copperhead sheets) react to your war → shape public morale + the 1864 election; editorials as a feedback surface, censorship/Vallandigham as a lever. Ties the broadsheet UI to a real mechanic + the 3-layer morale.
58. **Command = appoint / promote / relieve generals** — promote winners, relieve failures (the McClellan problem), navigate political generals you can't easily fire, live with the morale/political fallout. A real lever on the bridge LEADERSHIP facet + the generals system.
59. **Flagship named units** — famous regiments/brigades with identity, battle history, and teaching (54th Massachusetts; the Iron Brigade; the Irish Brigade; the Stonewall Brigade) that you field and whose fate you feel. Ties human-cost + the perspective threads.

### §8 addendum 9 — modes, morale, civil liberties (popup round 21; recorded as D39)
60. **Play modes (Aaron REVISED the R27 4-preset model)** — **General and Commander collapse into ONE battle/operations-focused mode**; **President = the full grand-strategy mode** (adds the political/economic/diplomatic desk). **Historian is NOT a mode** — it's a SETTINGS / DYNAMICS layer (realism + teaching depth + provenance/codex surfacing) applied over whichever mode you pick. So: two play modes + a realism/teaching overlay, not four role-modes.
61. **3-layer morale = three interacting, visible layers** — troop morale (battle outcomes, supply, pay), leader reputation (wins, politics, ambition), public will (casualties, economy, the press, elections); they interact (a beloved general lifts troops; a bloody loss sinks the public) and feed the bridge + the 1864 election.
62. **Civil-liberties trade-offs = real dilemmas with costs** — suspending habeas corpus, jailing Copperheads (Vallandigham), enforcing the draft, press censorship: each buys war-making capacity at a cost to legitimacy/morale. Lincoln's AND Davis's hardest domestic choices (Davis faced them too), taught honestly.

### §8 addendum 10 — diplomacy, the West, war finance (popup round 22; recorded as D40)
63. **Diplomacy = BUILT as a real system (D189)** — Mason & Slidell working London/Paris, the Trent affair as a war-scare crisis, the Laird rams, the Russian fleet's 1863 visit, cotton diplomacy vs King Wheat; CS chases recognition, US works to prevent it, feeding `clock.intervention`. Builds on the S1c blockade/diplomacy model.
64. **Western theater = SAME mechanics, new maps/battles** (Aaron chose reuse over a distinct Western texture) — the systems carry over; the West is new content (Henry/Donelson → Shiloh → Vicksburg → Chattanooga → Atlanta → the March), not new rules.
65. **War finance = civic + political** — Jay Cooke's mass war-bond drives, the first US income tax, greenbacks vs gold, the political cost of taxation: the Union financed its war through popular investment, the South couldn't. A teaching-rich extension of the S1a finance model.

### §8 addendum 11 — battle roster, morale resolution, cadence (popup round 23; recorded as D41)
66. **Battle build priority (next)** — Antietam, Vicksburg, Chickamauga/Chattanooga, Atlanta/the March — added to the existing Bull Run / Shiloh / Fredericksburg / Gettysburg / Malvern Hill / Franklin.
67. **Morale resolution** — troop morale = battle performance (the bridge); **public will = the strategic outcome** (1864 election, negotiated peace) → you can win battles yet lose the war if the home front breaks; leader reputation modulates both. (Implements §8.61's interaction.)
68. **Event cadence** — 1–2 meaningful decisions per turn, MORE at hinge-turns (Emancipation, the 1864 election, a recognition crisis). Each choice weighty, never a clicking chore (period-but-tight + accessible default).

> **D510 authority note:** the numbered charter above remains design and operating history. It cannot select work or reopen shipped systems; current scope comes from V1-CHECKLIST, HANDOFF, and the relevant COORDINATION lane.
