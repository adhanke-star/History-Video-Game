# RUN-LOG — 2026-06-14 (run k, Opus 4.8 — the STRATEGIC ARC S2, built incrementally)

**Context:** Continues the §8 overnight charter (battle layer A1–A6 shipped in run j). S2 is large, so it's built one sub-system per gated + empirically-probed + adversarially-bug-hunted + committed + pushed milestone. Per the owner's directive, **web-search/fetch grounding is folded into the content-research workflows** (real public sources, then adversarially verified). Ultracode on.

## PHASE E (S3 alt-history) — E1 deepening: INTERACTIVE HINGE-FORK DECISION CARDS — 4 citation-grade strategic forks that STEER the war toward the endings — 2026-06-23 (D118)
The E1 "Remaining" item: interactive HINGE-FORK decision cards on the proven `decResolve`/`_decApply` engine (pure data → byte-identical by construction, the prompt's spec). 4 new citation-grade strategic hinges (2 US, 2 CS), each a genuine war-turning-point fork that shapes the war's trajectory toward the ending spectrum: **us-hard-war** (total war / conciliation / Grant's grind → hard-war collapse); **us-reconstruction-terms** (Ten Percent Plan / Wade-Davis / forty acres → a Reconstruction that holds / forty acres enforced); **cs-king-cotton** (embargo / export / burn → recognized independence / King Cotton); **cs-peace-feelers** (negotiate / fight-on / husband → negotiated peace / Fabian survival). All `trigger.hinge:true`, each with the full rich `card` multi-voice block + a named-and-countered Lost-Cause myth voice. Deck 8 → 12. PURE DATA: the cards use only the existing `_decApply` vocabulary (weariness/capital/intervention/importFactor/strength) — which moves momentum (the dominant ending gate) + the foreign/blockade/manpower trajectory — and each option's prose names the ending it steers toward. The ending-opener strategy levers (armEnslaved/enemyWill/recognition) are combat-adjacent and stay on the Paths-to-Victory surface (scope decision). Only src touch = a 1-line `_decCAT_LABEL` extension (byte-identical for the 8 existing cards).
- **Citation-grade content (research+verify Workflow, 8 agents):** 4 Opus historians (WebSearch-grounded) + 4 adversarial verifiers; ALL corrections applied — fabrication-risk URLs → book/verified cites (misattributed ABT "The Burning"; generic NARA/ABT landing pages; the 404'd Hampton Roads URL; the wrong bread-riot slug); the Erlanger nominal-vs-realized fix; the Hammond quote restored; Rhea named for the Grant casualty arithmetic; the dubious Schweikart attribution dropped.
- **Bug-hunt (5 diverse-lens Opus finders incl. citation/anti-Lost-Cause × default-refute verify + critic; 6 agents) → invariantsHold=TRUE, ZERO confirmed defects** (all 5 invariants re-confirmed: no fabrication, accuracy, anti-Lost-Cause [each card a named+countered myth voice], byte-identical combat [no tactical file; `_decApply` untouched], render/escaping [the "Peace & Terms" double-escape avoided]). One critic-LOW (the contested Erlanger net figure) softened pre-commit.
- **Vet (all GREEN):** GATE OK · no-fudge ✓ · citations ✓; `probe-decisions` 19/19 (+5 D118 steps), 0 pe; `probe-desk` 13/13, `diag-classic` `nonBlank:346`; no tactical file in the diff → battles byte-identical by construction. **`wcag-auditor` auto-fixed 2 PRE-EXISTING AA contrast failures** in the decision-card render (category label #8a7350 4.15:1 → #957d57; hinge badge #9c3b2e 2.75:1 → #cc5f50) — a genuine a11y improvement affecting all 12 cards. Committed + pushed after the full gate. ✅

## PHASE E (S3 alt-history) — E1 deepening: ALTERNATE ENDINGS (GROUNDED tier, US-half) — the 8 plausible/long-shot Union end-states; the labeled spectrum now complete on both sides — 2026-06-23 (D117)
The 4th build against the D114 catalog completes the grounded tier (US-half; the CS-half shipped D116). Finished from the on-disk WIP — the 8 Union end-states were already inserted in `_END_CATALOG` and the US teaser already flipped; this milestone is the probe-guard fix + the D117 probe block + the full vet + bug-hunt + doc trail (NOT a re-insert). The 8: A Reconstruction that holds · An early abolition war · Hard war, early · Reunion without emancipation (named as the moral failure it would have been, NOT a triumph) · The 1862 knockout · Forty acres enforced · A compensated, bloodless end · The general strike triumphant. 3 plausible + 5 longshot. The US empty-state teaser was flipped so both sides advertise the full labeled spectrum. The early-abolition opener keys off the `us-radical-emanc` WILD (not the auto-tripping `usctUnlocked`); compensated-bloodless uses `em.year≤1862`. PURE READ-OUT → no tactical file touched, no combat path reads `end*` (re-confirmed: only `81-divergence.js:293` + `82-after-action.js:331`, both render-only) → 9 battles byte-identical by construction.
- **Probe-guard fix (task 1):** the stale D116 guard (the US-fantastical-only assertions that would now falsely fail) replaced with positive "US advertises the full spectrum" checks mirroring CS; the Maximilian/British-war cross-side leak guards kept. +8 D117 probe steps.
- **Bug-hunt (5 diverse-lens Opus finders incl. citation/anti-Lost-Cause × default-refute verify + critic; 8 agents) → invariantsHold=TRUE** (purity grep-clean, side-gate + fresh-zero, every cite spot-checked real & on-claim, the WILD-vs-usctUnlocked distinction sound, defensive render). **1 confirmed MED, FIXED:** the `us-general-strike` counterfactual said "tens of thousands took up arms for the Union" — understating the canonical ~180,000 USCT figure AND inconsistent with the game's own after-action text (`82-after-action.js:163`) + the cited Du Bois → changed to "some 180,000 took up arms for the Union — roughly one in ten Union soldiers by war's end." Critic gaps all LOW/non-blocking (probe hand-sets victoryReady; the 1862 endings are organically reachable per `victory.js:129`).
- **Vet (all GREEN):** GATE OK · no-fudge ✓ · citations ✓; `probe-endings` 24/24 (+8 D117 steps), 0 pe (re-run green after the fix); `probe-divergence` 14/14, `probe-afteraction` 10/10, `diag-classic` `nonBlank:346`; no tactical file in the diff → battles byte-identical by construction; `wcag-auditor` AA (data-only, no new colours, no edits). Committed + pushed after the full gate. ✅

## PHASE E (S3 alt-history) — E1 deepening: ALTERNATE ENDINGS (GROUNDED tier, CS-half) — the 8 plausible/long-shot Confederate end-states + the labeled spectrum — 2026-06-23 (D116)
The 2nd build against the D114 catalog: the GROUNDED Confederate tier (CS-half first, the prompt's two-vetted-increments split). 8 Confederate end-states authored the same way in `_END_CATALOG` (`tier:"plausible"/"longshot"` + a play-opened `precond` + a performance `gate` + a citation-grade counterfactual): Recognized independence · A negotiated peace · An emancipated Confederacy (slavery named plainly) · Stalemate/two Americas · Fabian survival · A Trans-Mississippi redoubt · King Cotton coerces Europe · The Northwest secedes. `endRenderSection` rewritten to band endings along a CVD-safe labeled spectrum (Plausible → Long shot → Fantastical), so grounded + fantastical read as one honest spectrum. New accessors `_endEnemyWill`/`_endYear`/`_endLever`/`_endRecogForeclosed` (defensive; `_endEnemyWill` defaults HIGH so a missing will never falsely opens). PURE READ-OUT → no tactical file touched, no combat path reads `end*` → 9 battles byte-identical by construction.
- **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + critic; 11 agents) → invariantsHold=TRUE** (all 8 counterfactuals + every cited book verified real & on-topic; fresh-campaign-clean reproduced). 4 confirmed + 2 critic-additional, ALL fixed + probe-locked: (MED) cs-trans-mississippi opened from passage-of-time (`victoryReady==null` is the fresh default) → moved `won≥3` into the precond + a probe negative; (LOW) the US empty-state teaser promised unbuilt US grounded tiers → side-aware teaser; (LOW) cs-fabian opened on `fortifyPorts` (the OPPOSITE of Fabian) → amnesty-only; (LOW) negotiated-peace secureHint over-pinned Nov 1864 → softened; (critic-LOW ×2) added the trans-miss probe coverage + relaxed cs-stalemate's momentum band for reachability.
- **Vet (all GREEN):** GATE OK · no-fudge ✓ · citations ✓; `probe-endings` 16/16 (+8 D116 steps), 0 pe; `probe-divergence` 14/14, `probe-afteraction` 10/10, `diag-classic` `nonBlank:346`; no tactical file in the diff → battles byte-identical by construction; `wcag-auditor` AA (no edits). **[AARON] popup tuning (×4):** Phase E stays the locked order; E4-i2 frozen-loop = proceed autonomously; full 9-agent bug-hunt every time; **even fewer interruptions** (stop only for irreversible/money; log contradictions and proceed). Committed + pushed after the full gate.

## PHASE E (S3 alt-history) — E1 deepening: ALTERNATE ENDINGS (fantastical tier) — the 8 boldest alt-history results a well-played war earns — 2026-06-23 (D115)
The E1 deepening's first build, against Aaron's locked D114 catalog (build order: fantastical tier first). A catalog of **8 fantastical alternate ENDINGS** (4 CS, 4 US) — A British war · Foreign legions (Maximilian) · Stonewall lives · The Golden Circle; The rapid-fire war · A Russo-American century · Lincoln lives Reconstruction · The 13th early — each **EARNED**: a `precond` (a played gambit / an emancipation choice / a reachable victory) OPENS it, a performance `gate` (`vicMomentum` / recognition / won-count / the 1864 election won) SECURES it → ★ Reached or ◇ Within reach (with a "to secure it" hint). Surfaced in the "Your War vs History" tab + a compact after-action line, each labeled *fantastical* beside its real-history counterfactual.
- **Architecture (PURE READ-OUT; byte-identical combat by construction):** new `src/83-endings.js` (`_END_CATALOG`/`endScan`/`endRenderSection` + the `_end*` accessors). Reads strategic state, writes nothing; no tactical file touched; no combat path reads `end*` (grep-confirmed). Wired via one guarded line each in `divRenderTab` (81) + `aarRenderReport` (82). Side-gated. NO RNG. Citation-grade anti-Lost-Cause (the Golden Circle named honestly, never valorized; Howard Jones, Robertson, Foner, Robert May).
- **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + critic; 9 agents) → invariantsHold=TRUE** (all 8 counterfactuals re-verified accurate). 2 confirmed, both fixed + probe-locked: (MED) the cs-british-war source double-escaped (`&amp;`→`&amp;amp;` through `_endEsc`) → plain `&`; (LOW) the empty-state teaser hardcoded both sides' examples → side-filtered.
- **Vetting (all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-endings` **9/9**, 0 pe; no-regression — `probe-divergence` 14/14, `probe-afteraction` 10/10, `diag-classic` `nonBlank:346`. No combat/tactical file in the diff → 9 battles byte-identical by construction. `wcag-auditor` AA (5.77/9.20/4.85:1). Committed + pushed. ✅

## §12.3 — THE ELECTION-SUPPORT RELIEF-BIND — a commissioned UNION political general costs extra capital to relieve before the 1864 vote — 2026-06-23 (D113)
The GM §12.3 command-politics keystone the Commission move (D109) was built around, now unblocked by the 1864-election state (E4/D112). A commissioned POLITICAL general (Banks/Butler/Sigel/McClernand) delivers his constituency's votes while he serves, so relieving him before the 1864 election forfeits that support — an ADDITIVE surcharge on `_cmdReliefCost` that rises year over year (1862<1863<1864) and falls to 0 once `clock.resolved1864` is set. UNION-ONLY (the CSA held no 1864 presidential election); EXACTLY 0 for every roster general, the whole CS side, and outside the window → `_cmdReliefCost` byte-identical for the roster; the relief cost is never read on the combat path → the 9 battles byte-identical by construction. A teaching tell on the active-general card names the constituency + the +N capital cost. `ratings.json` `electionReliefBind` (Inferred + 2 sources).
- **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + critic; 10 agents) → 3 confirmed, ALL FIXED:** (MED, anti-Lost-Cause) the bind fired on CS Floyd/Pillow with a fabricated "Lincoln lived this bind" attribution → gated Union-only (probe-locked); (MED) a NaN `windowByYear` escaped the clamp (`typeof NaN==="number"`) → restored the `isFinite` guard (probe-locked, defensive); (LOW) "rises toward November 1864" prose → softened to the year-granular truth.
- **Vetting (all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-command` **76/76** (+7 D113 steps incl. the Union-only gate + the NaN safety), 0 pe; `diag-classic` `nonBlank:346`; `probe-desk` 13/13, `t1probe` 14/14. No combat path reads the relief cost → 9 battles byte-identical by construction. `wcag-auditor` AA (`#cbb27a` 8.41:1). Committed + pushed. ✅

## PHASE E (S5 victory) — E4: THE GRADED AFTER-ACTION REPORT — an A–F report card on the whole war + the divergence read-back + the Reconstruction coda — 2026-06-23 (D112)
With E1's divergence ledger shipped (D111), the run advances to **E4 / S5 victory** and builds the strategic arc's closing teaching payoff (D31.6). A period **"report card"** grades the whole war A–F (the D94 scale, neutral 64 = C, CVD triple-encoded) across the domains the President actually ran — **The Battlefield · The Treasury · Diplomacy & the Blockade · The Home Front (+ the 1864 election) · the war's PURPOSE · High Command** — plus a headline overall grade, a **read-back of the D111 divergence ledger** (`divScan`/`divIndex`), the human cost beside the war's true ~750,000 toll (Hacker 2011), and a forward-looking **Reconstruction coda** keyed to the emancipation choice (Foner). The US war-purpose domain is graded by emancipation *timing* (declined < late < 1863); the CS side is an honest, NON-graded **"Confederate Cause"** panel (Stephens's cornerstone; slavery stated plainly, never valorized, excluded from the GPA).
- **Architecture (PURE READ-OUT + one authorized frozen override; byte-identical combat by construction):** new `src/82-after-action.js` (`aarGrade`/`_aarDomains`/`aarOverall`/`_aarReconstruction`/`aarRenderReport`/`aarRenderTab` + a `warWonScreen` OVERRIDE). It reads already-resolved aggregate state and writes nothing to the sim (only the base-preserved `G.campaign=null`); no tick, no own state; NO tactical file touched; `aar*` grep-confirmed in exactly 3 files with zero combat/resolve/tick hits → the 9 battles byte-identical by construction. Surfaced as a live **"After-Action"** desk tab (the 16th) AND the war-end final report (the `warWonScreen` override reproduces the base Main-Menu wiring + nullify verbatim). Citation-grade anti-Lost-Cause throughout (Hacker, Foner, McPherson, Howard Jones, Dew).
- **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + critic; 7 agents) → invariantsHold = TRUE** (no-fudge + byte-identity + citations all independently confirmed). 1 confirmed LOW + 2 critic-additional LOW, **all one root cause** (a `typeof`-only guard let a NaN `moraleCompute().public` through — `typeof NaN==='number'` — risking a "NaN of 100" leak, a graded-count desync, and a mis-rendered non-graded badge) → FIXED with an `isFinite` guard at the read + the panel count hardened to match `aarOverall`, probe-locked by a new NaN-stub regression step. One citation tightened (Gallagher → McPherson, the off-target non-defect).
- **Vetting (all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-afteraction` **10/10** (+ the NaN regression step), 0 pageerrors; full no-regression — `diag-classic` `nonBlank:346`, `bootprobe` ok, `t1probe` 14/14, `probe-desk` 13/13, `probe-divergence` 14/14, `probe-victory` 12/12, `probe-command` 69/69, `probe-officers` 15/15 — all 0 pe. No tactical file in the diff → 9 battles byte-identical by construction (`probe-ratings`/`probe-camp` were the documented THREE-CDN `load`-stall, share no code path, GREEN at D111). `wcag-auditor` AA (1 contrast fix). Committed + pushed after the full gate. ✅

## PHASE E (S3 alt-history) — E1: "YOUR WAR vs HISTORY" — the alternate-history divergence ledger + tracker + the emergent-only toggle — 2026-06-23 (D111)
The GM §12 depth-chart arc complete (Appoint · Promote · Corps · Commission · Division), the run pivots to the **Phase-E strategic arc** (D91: after C breadth → E; Chattanooga/USCT last) and ships E1 / S3 alt-history's named flagship first increment. A new **"Your War vs History"** desk tab (the 15th): a living, dated DIVERGENCE LEDGER recording every point at which the player's war has left the documented record — category (Emancipation/Foreign/Northern politics/trajectory/Wild cards) × tier (minor △ / major ◆ / radical ★, CVD-safe glyph+word+colour) × a `when` × the historical counterfactual ("In history, …"). A headline DIVERGENCE INDEX (0..100 saturating tier-weight sum + a 5-step word ladder) measures distance from the past (NOT a score — the no-thumb-on-the-scale D54 law is explicit). The emergent-only toggle (`G.settings.altHistoryEmergentOnly`) withholds the wild-card gambits so the war diverges only through play.
- **Architecture (PURE READ-OUT, the D106/D99 pattern; byte-identical combat by construction):** new `src/81-divergence.js` — `divScan` reads `C.strategy`/`C.president.emancipation`/`C.blockade`/`C.clock`/`C.stats` and WRITES NOTHING; the only write anywhere is `divWireTab` flipping the global setting + `saveLocal`. No tick, no own per-campaign state; NO tactical file touched → the combat layer + 9 battles byte-identical by construction. The toggle is one guarded line atop `_vicWildSection` (80-victory.js; default OFF = byte-identical). Wired as one tab in 30-president-shell.js. Detected hinges: emancipation declined/early/1862/late (US), CS arm-the-enslaved, foreign recognition, the 1864 election (US) / CS home-front break, the CS trajectory, each wild card. Citation-grade anti-Lost-Cause counterfactuals (inline-prose trust model of the sibling `_vicWILDCARDS`; failures named both sides).
- **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + critic; 11 agents) → invariantsHold = TRUE** (no-fudge grep-confirmed; byte-identical-when-off by git diff + probe). 2 confirmed + 4 critic-surfaced; resolved: **(HIGH)** the `election-1864` entry had no `side==='US'` gate (a CS home-front break fired the Lincoln counterfactual) → US-gated + a side-correct `cs-homefront-breaks` branch; **(MED)** a late (1864/65) emancipation was silently dropped → an `ey≥1864` "delayed" branch (a Union failing); **(MED)** Davis's six-year term began Feb 1862 (not "1861–1867") → corrected; **(LOW)** the toggle `aria-pressed` inverted → stable `aria-label` + correct state; **(MED)** inline-counterfactual citation-gate gap → trust boundary documented; **(LOW, declined w/ rationale)** a recognition side-gate (the entry is side-neutral + historically correct either way + unreachable for US).
- **Vetting (all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-divergence` **14/14** (+2 bug-hunt regression steps), 0 pageerrors; full no-regression — `diag-classic` 346, `probe-desk` 13/13, `probe-victory` 12/12, `probe-command` 69/69, `t1probe` 14/14, `bootprobe` ok, all 0 pe. No tactical file in the diff → 9 battles byte-identical by construction (the headless `load` battle battery is the documented THREE-CDN stall, shares no code path). `wcag-auditor` AA (2 contrast fixes). Committed + pushed after the full gate. ✅

## RATING SYSTEM — Q12: THE DIVISION SUB-TIER — seat pool generals into division billets nested under each seated corps (the next rung of the GM depth-chart) — 2026-06-23 (D110 — Codex code + Claude vetting/doc close-out)
The next rung of RATING-SYSTEM-DESIGN §12.1's billet tree (Army → Corps → **DIVISION** → Brigade), built on the Q10 corps chart (D108). Beyond the army's I–IV Corps, the President SEATS pool generals into each seated corps's **3 DIVISION billets** (`cmdSeatDivision`/`cmdVacateDivision`, `cmd.divisions = {corpsIdx:{divIdx:id}}`), spending `seatCost` (2) political capital. A division is staffable ONLY under a SEATED corps (the chain of command → the depth chart is a tree built top-down). Each seated division commander adds a small BOUNDED lift to `commandLeadership` (`_cmdDivLift` = clamp(Σ(effDivRating−64)×0.03, ±2)) — an INPUT, never the scoreboard (build-gate 4d scans 35-command.js); deliberately SMALLER than the corps lift (cap 2 vs 4) so the influence hierarchy holds (army ±17 > corps ±4 > division ±2). A division was a **Maj. Gen.'s** billet in BOTH armies (distinct from the corps tier's side-aware grade), so any Brig. Gen. seated over a division leads `belowGrade` until promoted (the Q9 synergy). **Byte-identical until a division is seated** (lift 0; a vacant division = 0, NOT a penalty; pre-Q12 saves with no record read identically via the `_cmdDivLift` fast-path). One billet per man; vacating a corps CASCADES (orphaned divisions stop contributing lift at the READ layer `cmdDivSeated`/`_cmdDivLift`, `_cmdDivClean`:795); `cmdInit` sanitizes on load; no `_SAVE_VER` bump; NO RNG. `data/ratings.json` `divisionCommand` (Inferred + 2 sources: Glatthaar, Weigley).

**PROVENANCE / CLOSE-OUT (a Codex hand-off):** the Q12 CODE + DATA + PROBE were authored in an interrupted Claude-Code session and finished + committed (`7b64d2e`) + pushed by **Codex** — a sound, builds-`GATE OK`, byte-identical-to-source feature — but Codex ran NO adversarial bug-hunt and wrote NO doc trail. This session is the close-out: the bug-hunt + the full docs + the one fix below. No code reverted.

**Bug-hunt (5 diverse-lens Opus finders × default-refute verify + a completeness critic; 7 agents):** 4 lenses CLEAN; **1 confirmed LOW, FIXED** — the division teaching exemplars were all-Confederate (A. P. Hill / Hood / Pickett) while the rating mechanism is side-neutral and the sibling corps tier named both sides (the Lost-Cause over-celebration pattern CLAUDE.md #4 guards against). FIXED copy-only (byte-identical): added the Union counterpart (Hancock's II Corps divisions, Gibbon/Barlow — accurate at Gettysburg/Wilderness/Spotsylvania) to the `divFoot` UI string + the `divisionCommand` `_note`. The save-tamper lens ran an 18-vector battery against `_cmdDivClean` (all sanitize); **critic invariantsHold = TRUE** (independently grep-confirmed no combat path reads `cmd.divisions`, the byte-identity fast-path, the read-layer cascade, no-fabricated-officers, escaping, sanitize-on-load — and verified the fix in 3 places).

**Vetting (offline gates GREEN; the headless `load` suite ENV-BLOCKED):** `GATE OK · no-fudge ✓ · citations ✓`; committed HTML **byte-identical to the source build**; `probe-command` **69/69** (+11 Q12 steps), **0 pageerrors**; `diag-classic` `nonBlank:346`; `t1probe` + `bootprobe` ok. **No tactical file in the D110 diff** → the 9 battles are byte-identical to Q11 by construction. **ENV:** the strategic no-regression `load` battery + the 9 battle probes were blocked by the documented **THREE-CDN headless `load`-stall** (jsdelivr rate-limited the headless client after the first clean load — re-running the known-good `probe-command` reproduced it); these probes share NO Q12 code path and were all GREEN at Q11. Committed + pushed (the close-out commit on top of `7b64d2e`). ✅

## RATING SYSTEM — Q11: THE COMMISSION MOVE — bring documented political generals into the officer pool for political capital (the GM "Commission" move) — 2026-06-23 (D109)
The GM depth-chart move of §12.2/§12.3 ("Commission — bring a new officer into the pool"). The President can now **COMMISSION the documented POLITICAL GENERALS** — US **Banks**, **Butler**, **Sigel**, **McClernand**; CS **Floyd**, **Pillow** — into his officer pool for **political capital** (`C.clock.capital`, no new resource), after which they're appointable/promotable/seatable like any general. A Command-desk **"Commission an officer — the political generals"** section lists each with his LOW combat OVR beside his HIGH political value, his constituency, an honest record, and a Commission control (gated on capital + the `maxCommissions` 3-slate). Teaches the GM layer's missing keystone — the §12.3 command-politics bind: political generals were cheap to commission and weak in the field (kept for the factions/votes/immigrant regiments they brought, not generalship), so appointing one drags the army's leadership (a commissioned Banks fields **49** vs Grant's **87**). **Pick rationale (decide-&-log):** chose Commission (C) over the symmetric AI-GM (B, [hard]) and the division sub-tier (thinner) — it fills the central teaching gap, reuses the proven `cmdAppoint`/`cmdSeatCorps`/`cmdPromote` byte-identical-until-used spine + the §8 person concept, and adds 6 citation-grade figures; scoped to a pure pool-addition (no economy/election fork — the §12.3 election-support relief-bind logged as the next increment; the relief COST is already taught via each officer's `relief` class).
- **Architecture (byte-identical until commissioned):** the commissionable officers live in a SEPARATE `data/generals.json` → `sides[side].commissionPool` (so `_cmdSideGenerals` + every byte-identity path is untouched); `cmdCommission` writes ONLY `cmd.commissioned` + capital + a reputation seed (build-gate 4d scans 35-command.js). With nobody commissioned, `_cmdRosterPlusCommissioned`===the bare roster → the appoint/promote/corps pools + commandLeadership + the 9 battles identical to HEAD `8eb913a`. `_cmdById` widened to resolve the pool (so a commissioned-then-appointed officer resolves); `_cmdByIdRoster` keeps the roster-only lookup for the seeds + the commission guard. `cmd.commissioned` rides the save; `cmdInit` seeds `[]` + sanitizes on load (no `_SAVE_VER` bump). NO RNG.
- **Citation-grade content (12-agent research+verify Workflow):** 6 Opus historians + adversarial source-check + a completeness critic; all dossiers verified (sources real & on-claim, records honest both sides, no fabrication). The verifier's one HARD correction applied — **Pillow → Brig. Gen.** (his "Maj. Gen." was Mexican-War/state-militia, never CSA — CLAUDE.md #4); Banks's Black-officer purge folded into his bio; Sigel's unconfirmed reprint year dropped. Ratings stamped Inferred (calibrated); bios carry ≥2 real sources (gate-4e clean); persona seeds calibrate EXACTLY to each entry skill (oracle verified for all 6).
- **Bug-hunt (12 Opus agents: 5 diverse lenses × default-refute verify + completeness critic; invariantsHold = TRUE — the critic grep-confirmed no combat path reads `cmd.commissioned`, cmdCommission writes only commissioned+capital+reputation, byte-identity-when-empty, Pillow Brig. Gen., every string escaped, zero id-collision).** 5 confirmed (2 MED + 3 LOW), ALL FIXED + the behavior-affecting ones probe-locked: **(MED)** the commission economy was bypassable at the function level — `cmdAppoint`/`cmdSeatCorps`/`cmdPromote` accepted an un-commissioned pool officer via the widened `_cmdById` → added a `_cmdByIdRoster`+`cmdCommissioned` guard to all three (byte-identical for roster ids; probe-locked). **(MED)** a seated commissioned officer always showed "I Corps" (string for-in key → `_cmdNum`→0) → `parseInt(kk,10)` (probe-locked). **(LOW)** the shared `cap` gate idiom passed NaN (tampered-save only, pre-existing) → `&& isFinite(clk.capital)` at all 6 sites + cmdAppoint debits the rounded cap. **(LOW)** `maxCommissions` fallback 4→3. **(LOW)** a commissioned officer past his window now reads muted "no longer in service" (was a green false "in your pool").
- **Vet (all GREEN):** GATE OK · no-fudge ✓ · citations ✓; **byte-identity STRUCTURAL + EMPIRICAL** — 132-cell A/B (`.tmp/q11-ab.mjs` vs HEAD `8eb913a`) byte-identical with nobody commissioned, re-confirmed after the bug-hunt guards (inert for roster ids), 0 pe; `probe-command` **58/58** (+8 Q11 steps + the bug-hunt gate-lock step); FULL no-regression GREEN (oob 16 · desk 13 · ratings 21 · camp 8 · officers 15 · field 13 · t1 14 · boot ok · the 9 battles + csplayer — bullrun 15 · antietam 16 · fredericksburg 22 · gettysburg 16 · chancellorsville 24 · malvern 24 · shiloh 29 · vicksburg 18 · chickamauga 18 · csplayer 16); `diag-classic nonBlank:346`; 0 pe throughout; `wcag-auditor` AA (all new Commission-UI values measured ≥ AA, no changes; inactive-row nested-opacity covered by the WCAG inactive-component exemption). ENV: intermittent THREE-CDN `load`-stalls (vicksburg 3D-readability + the A/B) cleared by retry/server-restart — the documented gotcha; the `domcontentloaded` A/B is the authoritative byte-identity spine. Committed + pushed after the full empirical gate. (DECISIONS D109.)

## RATING SYSTEM — Q10: THE CORPS DEPTH-CHART — seat pool generals into corps billets over the D106 OOB tree (the GM depth-chart MOVE) — 2026-06-23 (D108)
The GM depth-chart MOVE of §12.1/§12.2/§12.4 ("an army version of waiver/trading/transfer"). Beyond appointing the ARMY commander, a new Command-desk **"The Corps Command — Depth Chart"** lets the President SEAT pool generals into the army's I–IV Corps billets, spending **political capital** (`C.clock.capital` — the existing GM currency, no new resource). Each billet shows its commander (name + grade + OVR + a below-grade tell) + a Vacate control, or "Vacant" + a pool `<select>` + a Seat control; a live read-out shows the net leadership the staff add. Teaches real command organization — Lee's two-corps ANV vs the AoP's churning corps commands; Howard's XI Corps routed at Chancellorsville. **Pick rationale (decide-&-log):** chose the depth-chart MOVES (A) over the symmetric AI-GM (B, [hard]) — A extends the proven `cmdAppoint`/`cmdPromote` spine and is byte-identical-until-moved like Q9; the missing officer-slot concept is the `cmd.corps` slot map + a side-aware grade-fit.
- **Reaches the fight (no new combat path; D74/D94):** seated corps commanders add a small BOUNDED lift to `commandLeadership(C)` — `_cmdCorpsLift` = clamp(Σ over seated slots of (effRating−64)×perSlotWeight, ±liftCap) (0.05/cap 4) — an INPUT the bridge already reads, NEVER the scoreboard (build-gate 4d scans 35-command.js + T15-oob.js). `effRating` = the general's GM OVR (carries the Q9 promotion lift) minus `belowGradePenalty` (6) when below the **side-aware** preferred grade. The army commander still weighs most (~±17); the corps lift (±4) is secondary (§27).
- **Side-aware grade-fit (the Q9 synergy):** a US corps = Maj. Gen.'s command, a CS corps = Lt. Gen.'s (the CS authorized Lt. Gen. Sept. 18, 1862, promoting Longstreet/Jackson that Oct.). On the shipped roster only CS **Stuart** seats below grade; promoting him to Lt. Gen. clears it. `data/ratings.json` `corpsCommand` (slots 4 · seatCost 3 · preferredGrade {US:Maj.Gen.,CS:Lt.Gen.} · perSlotWeight 0.05 · liftCap 4 · belowGradePenalty 6 · Inferred · 2 sources).
- **Byte-identical until a corps is seated:** lift 0 → `commandLeadership` unchanged → the sandbox + 9 battles identical to HEAD `2d11bd7` (a vacant billet = 0, NOT a penalty). `cmd.corps` rides the save (no `_SAVE_VER` bump); `cmdInit` sanitizes a tampered record on load; one corps per general; the army commander can't double-hold (`cmdAppoint` clears his corps). Seated commanders NAME the player's DERIVED corps on the OOB board (pure display; edge unchanged; the enemy + a "Garrison" node never named). New fns in the `35-command.js` Q10 block (`cmdSeatCorps`/`cmdVacateCorps`/`_cmdCorpsLift`/`cmdCorpsCommanderFor`/`_cmdCorpsDepthHTML`/…) + the T15 attach + render. NO RNG.
- **Bug-hunt (11 Opus agents: 5 diverse lenses × default-refute verify + completeness critic; 5 raw → 3 confirmed; invariantsHold = TRUE — the critic grep-confirmed no combat path reads `cmd.corps`, seat/vacate write only `cmd.corps`+capital, byte-identity airtight, no fabricated officers, the 2 sources real).** ALL 3 FIXED: **(MED)** the seat-log said "leads **above** his grade" for a BELOW-grade general → "below". **(LOW)** a real general could be named over a derived **"Garrison"** node → the attach guards to `/ Corps$/` nodes only (probe-locked). **(LOW)** the CS Lt. Gen. date ("created Oct 1862") → the Sept. 18, 1862 authorization + Oct. promotions + Nov. 6 reorg. (Critic-"missed" non-blocking: the agents couldn't run browser probes — the main session ran the full suite; the ±4 cap is secondary to ±17 [§27]; probe step #5 already proved cmdPromote reaches Lt. Gen.)
- **Vet (all GREEN):** GATE OK · no-fudge ✓ · citations ✓; **byte-identity STRUCTURAL + EMPIRICAL** — a 132-cell A/B (`.tmp/q10-ab.mjs` vs HEAD `2d11bd7`) over (side×appointee×date) confirms `commandLeadership`/`bridgeArmy.overall`/`leadership`/margin reads byte-identical with no corps seated; `probe-command` **49/49** (+9 Q10 steps), `probe-oob` **16/16** (+the Q10 step: names the derived CORPS, edge unchanged, the Garrison guard, enemy never named); FULL no-regression GREEN (desk 13 · ratings 21 · camp 8 · officers 15 · field 13 · t1 14 · boot ok · the 9 battles + csplayer); `diag-classic nonBlank:346`; 0 pe throughout; `wcag-auditor` AA (3 contrast fixes on the new UI: `#c9712e`→`#d17936`, two T15 opacities .82/.9→.92). ENV: intermittent THREE-CDN load-stalls on the heaviest battle probes cleared by retry; the `domcontentloaded` A/B harness is the authoritative spine. Committed + pushed after the full empirical gate. (DECISIONS D108.)

## RATING SYSTEM — Q8b: THE BETWEEN-BATTLE CAVALRY RECONNAISSANCE ("scout the enemy") — tier-reveal the enemy OOB, scaled by the appointed general's cavalry — 2026-06-23 (D107)
The camp loop's deferred SCOUTING (§15), built on the D106 OOB board. An **"Order a reconnaissance"** control on the Command-desk Order-of-Battle board spends a small amount of **political capital** (`C.clock.capital` — the existing GM currency, no new resource) to reveal the **enemy** order of battle to a TIER scaled by the appointed army commander's persona **`cavalry`**. Teaches cavalry-as-the-army's-eyes — Stuart's rides, and the Gettysburg intelligence vacuum when his cavalry was absent (a real Confederate command failure, not glorified). **Pick rationale (decide-&-log):** chose scouting over the GM depth-chart MOVES (both unblocked by D106) because its scaling input (the `cavalry` persona attribute) + its `reveal` hook both already existed, the D106 board already advertised a dead "Scout the enemy" CTA, and scouting is pure-display + an economy spend (safest byte-identity); the corps-billet MOVES need a new officer-slot concept the derived tree lacks — deferred next.
- **Three recon tiers (§15):** **light** (unscouted — the passive D106 estimate) → **better** (cavalry < 65: enemy commander named + per-corps grade + posture/terrain advisory, no per-brigade detail) → **full** (cavalry ≥ 65: the complete enemy OOB). With the 9 generals' cavalry, Grant (66) + Lee (70) → full, the rest → better; a no-appointee campaign uses the HISTORICAL DEFAULT commander's cavalry. Intel is fresh per engagement (the scout record is keyed to the next-battle id → reverts to light when the battle changes).
- **Architecture (the D106 wall preserved):** the WRITE `cmdScout` (`src/35-command.js`) gates on capital like `cmdAppoint`, reads the appointee's cavalry via `_cmdGenPersona`, computes the tier via the pure `_cmdScoutTierForCavalry`, and writes ONLY `cmd.scout` + debits capital — never the scoreboard. **T15-oob.js stays a PURE READ-OUT** — `fldCampaignOOBHtml(C,{reveal,reconHtml})` is told the tier + the control HTML by `cmdRenderTab`; T15 only ADDS the "better"-tier renderer (`_fldOOBSideScouted` + `_fldScoutPosture`). No combat path reads `cmd.scout` (grep-confirmed). `cmd.scout` rides the save; `cmdInit` sanitizes a malformed/tampered record (no `_SAVE_VER` bump). Data: a `scout` block in `data/ratings.json` (cost 3 · threshold 65 · baseline 54 · 2 real sources). NO RNG.
- **Bug-hunt (9 Opus agents: 5 diverse lenses × default-refute verify + completeness critic; invariantsHold = TRUE — the critic grep-confirmed no combat path reads `cmd.scout`, the 9 battles byte-identical, no fabricated officers, citations real).** 1 confirmed LOW + 2 critic-surfaced LOW; actionable ones FIXED: **(LOW)** `cmdScout` gated on `round(capital)` but debited RAW — the Q9 gate/debit rounding divergence `cmdPromote` was hardened against → debit `cap - cost` now. **(LOW, critic)** `_fldScoutPosture` ignored `C.flipAtk` (the alt-history attacker swap) → the "better"-tier posture could mis-describe who attacks after a recovery refight → honors `C.flipAtk` with the SAME non-mutating swap the resolver uses (a probe locks it). **(LOW, deferred — critic agreed)** the cosmetic `cmd.scout.turn` stamp nothing reads.
- **Vet (all GREEN):** GATE OK · no-fudge ✓ · citations ✓; **combat byte-identity STRUCTURAL** (no combat path reads `cmd.scout`) + EMPIRICAL (all 10 battle probes land on their historical results, unchanged: bullrun 15 · antietam 16 · fredericksburg 22 · gettysburg 16 · chancellorsville 24 · malvern 24 · shiloh 29 · vicksburg 18 · chickamauga 18 · csplayer 16); `probe-oob` **15/15** (+SCOUT TIERS: the ladder + no per-brigade leak + the flipAtk posture-flip + non-mutation), `probe-command` **40/40** (+6 Q8b steps); FULL no-regression GREEN (desk 13 · ratings 21 · camp 8 · officers 15 · field 13 · t1 14 · boot ok); `diag-classic nonBlank:346`; 0 pe throughout; `wcag-auditor` AA (1 contrast fix `#d66040`→`#e86840` on the new "Needs capital" span; the existing `#d66040` usages noted sub-AA for a future pass). ENV: PAUSED mid-vet on an Aaron "pause internet" directive (the battle probes fetch THREE from a CDN; the bug-hunt + wcag are API calls) and RESUMED cleanly; THREE-CDN load-stalls cleared by retry. Committed + pushed after the full empirical gate. (DECISIONS D107.)

## RATING SYSTEM — THE OOB-MAPPING SUBSTRATE: the strategic next-battle maps to a structured Order of Battle + a read-only roster board — 2026-06-22 (D106)
The shared blocker for BOTH the GM depth-chart MOVES (§12.2) and Q8b scouting (§15). New module **`src/tactical/T15-oob.js`** maps the strategic next-battle (`_brgNextBattle`) → a STRUCTURED order of battle (commander → corps → brigade) for both sides, each billet carrying its OVR/grade/strength/provenance (the T14 spine). Surfaced as a read-only **"Order of Battle — the Next Engagement"** board on the Command desk (one `cmdRenderTab` line). Scoped SMALL per the run plan: a read-only query layer + board, **no combat wiring** (the procedural skirmish in T2 UNCHANGED). No new fork surfaced (`BATTLES[]` already carries real `cmdUS`/`cmdCS`; the 9 scenarios carry real OOBs).
- **Two sources, one shape:** authored scenario → its REAL brigade OOB (real commanders + authored provenance via `_fldOOBProvFromNote`, reusing `_fldOOBUnits`); procedural → a deterministic corps/brigade tree from `bd.us`/`bd.cs`/year (seeded, NO RNG), FORMATION-labeled with `commander:null` — **never a fabricated officer** (CLAUDE.md #4/D92). Army-commander headline = the player's appointed general (+ his OVR) or the historical `cmd<Side>` (Verified). **Enemy-FUZZY (§15):** player exact, enemy outline (grade+count+commander) — the hook scouting deepens via a `reveal` tier; the substrate returns full data for both sides.
- **Byte-identical BY CONSTRUCTION:** a PURE READ-OUT called ONLY from `cmdRenderTab`; the generated-html diff vs HEAD `7df4372` is ALL-ADDITIONS (T15 block + 1 desk line, **0 removed/modified lines**). build-gate 4d `RATING_MODULES` now scans T15.
- **Bug-hunt (15 Opus agents: 5 diverse lenses × default-refute verify + completeness critic → "NOT SAFE AS-IS"; 5 confirmed [1 HIGH + 4 MED], ALL fixed + re-vetted; the hard invariants independently CONFIRMED HOLD).** **(HIGH, anti-Lost-Cause)** a multi-phase battle's phase-0 OOB shown under the BARE name could forecast a CS edge for Union-won battles (Gettysburg/Antietam/Vicksburg/Chickamauga) — the Q7 failure at the display layer; FIXED by threading `phased`/`phaseName` + scoping the headline/edge/note/footer to "the opening engagement". **(MED)** the build-gate change my T15 comments claimed was never applied (a failed Edit) → actually applied. **(MED)** the derived artillery battalion destroyed ~12-15% of a side's men → carved off the TOP now (conserves EXACTLY). **(MED)** a mixed "(Verified identity; Inferred strength)" note overclaimed as Verified → downgrades to Inferred. **(MED, defensive)** one-sided authored OOB → decide ONE source for both sides. + 2 LOW probe adds.
- **Vet (all GREEN):** GATE OK · no-fudge ✓ (4d scans T15) · citations ✓; byte-identity 0 removed lines vs HEAD `7df4372`; `probe-oob` 14/14; FULL no-regression GREEN (command 34 · desk 13 · ratings 21 · camp 8 · officers 15 · field 13 · t1 14 · boot ok · all 9 battles + csplayer); `diag-classic nonBlank:346`; 0 pe; wcag AA. ENV: healthy window; one fredericksburg load-stall cleared by a standalone retry. Committed + pushed. (DECISIONS D106.)

## RATING SYSTEM — LIVE DEV-TRAITS (the Madden development arc): a general's OVR now develops over the war, shaped by a hidden potential ceiling — 2026-06-22 (D105)
The last open item on the R-6 rating track (RATING-SYSTEM-DESIGN §9-R-6: "reputation level-up → OVR still TODO") + the D94-fork-#5 lock. `cmdOnResolve` already evolved a general's reputation by battle outcome and `_cmdGenRating`→`commandLeadership` already read it; D105 adds the **Madden DEV-TRAIT** that SHAPES the arc: a hidden **ceiling** / **floor** / development **rate** / **attritionDrag**. **ACCURATE INPUTS, never an output gate (D74/D92)** — it shapes the general's REPUTATION (an input the pipe already reads), never the scoreboard.
- **Keystone (byte-identical-when-off):** an UNASSIGNED general (or absent devTraits data) takes the LITERAL pre-D105 path `clamp(cur+delta,5,98)` — proven across all 5 outcome quadrants + both clamp rails. The deliberate change is the citation-grade `data/ratings.json` `devTraits.assign` map (20 generals → 12 archetypes). Ceiling/floor are RELATIVE to the seeded start (`devTrack.start`); every archetype keeps `ceilingAbove≥5 & floorBelow≥5` so the pre-D105 win-raises/loss-lowers invariant holds for all. D105 touches NO tactical code → the 9 battles are byte-identical by construction (`git diff` confirms strategic-only).
- **Anti-Lost-Cause thesis:** the Union's elite are RISERS (Grant/Sherman/Sheridan/Thomas — 4 high-ceiling developers rated as high as Lee/Jackson, the honest claim that the Union found+elevated its best command) while command failures are named plainly on BOTH sides (US McClellan plateau / Burnside-Hooker overpromoted; CS Bragg quarrelsome / Hood volatile-disaster). All 20 stamped Inferred (calibrated readings, gate-4e-exempt) with real sources; nothing laundered.
- **Read-out:** a new Command-desk "Career Arc" section (`_cmdCareerArcHTML`) — the dev-trait tier + the OVR arc (start→now→peak on a floor..ceiling band) + a trend verdict (Rising/Holding/Fading/Untested); triple-encoded, CVD-safe, wcag AA.
- **Bug-hunt (29 Opus agents: 5 diverse lenses × default-refute verify + critic → SAFE TO COMMIT, both hard invariants confirmed via grep).** 8 confirmed + 3 critic-missed (1 HIGH, 3 MED, 7 LOW), ALL fixed: the HIGH = a fabricated-by-misattribution Thomas citation (corrected to Daniel's *Days of Glory*); the Burnside note de-laundered (Marye's Heights named); the keystone save-tamper hardening (devTrack.start coerced + cmdInit sanitizes on LOAD + render coerces); build-gate 4d extended to scan `35-command.js` (the no-fudge wall made structural for the strategic rating code); + 7 probe-strengthening LOWs.
- **Vet:** GATE OK · no-fudge ✓ (4d scans 35-command.js) · citations ✓; `probe-command` 34/34 (9 D105 steps), `probe-ratings` 21/21, `probe-desk` 13/13, `probe-camp` 8/8, `probe-officers` 15/15, `t1probe` 14/14, `bootprobe` ok; `diag-classic` nonBlank:346; 0 pe; wcag AA. Committed + pushed. (DECISIONS D105.)

## RATING SYSTEM — R-6 (the badge ASSIGNMENT to the 9 rosters): the rating system finally BITES in the historical battles — 2026-06-22 (D104)
Now that the catalog is citation-clean (D103), the documented commander/unit traits are assigned to the 9 shipped battles' brigade tokens — the first time the rating system changes combat in the real battles (byte-identical-when-unassigned since R-3). **43 assignments** in a central `data/ratings.json` `rosterBadges` map { scenario → unit → [badge] }; the seam stamps via `fldBrSpec`→`fldMakeUnit` (a `.slice()` copy, null when absent → byte-identical); the existing R-3/R-4 engine bites (gated on `__FIELD.badges`, per-lever-capped). **ACCURATE INPUTS, never an output gate (D74/D92).**
- **Design (ultracode):** a 10-agent citation Workflow (one Opus historian per battle, source-cited → an anti-Lost-Cause/flip-risk critic; its 5 edits applied). Anti-Lost-Cause: **CS 12 clear virtues : 6 flaws : 4 double-edged; US 11 : 10** — the Union's command flaws (McClellan `the_slows`, Burnside `rigid_plan`, McClernand/Sherman `piecemeal`) named as plainly as the CS's virtues. Calls: `the_slows` on Antietam `us_sturgis` (no McClellan army token; not forced onto every division); `grand_charge` on Chickamauga `cs_johnson_gap` (the breakthrough that succeeded), DECLINED on Pickett; **Shiloh** — the surprise rides the scenario not a combat penalty (which flipped it against history), the Hornet's-Nest stand (Prentiss `rock`/Wallace `stonewall`) is modeled. New: an accessible badge-chip HUD (`fldRatingBadgesHtml`, wcag PASS).
- **Bug-hunt (16 Opus agents, 5 lenses × default-refute verify + critic) → SAFE TO COMMIT** (both hard invariants intact). 8 confirmed + 3 critic-missed, all LOW/MED, fixes applied: `grand_charge` note de-overclaimed (the dual downside is NOT modeled — upside-only, deferred); the probe now resolves unit-ids in all 9 scenarios; `_spdMul` reset; `cs_colston_div` `stonewall` removed (role mismatch); the `_rosterNote` honesty pass.
- **Probe maintenance:** badges is now a default-ON optional combat layer that legitimately shifts bullrun1 (CS stonewalls → CS 8/8), breaking 3 LAYER-ISOLATION byte-identity guards (presets/arms/logistics) that hardcoded the pre-badge CS-5/8 / [4,6] baseline → added `__FIELD._badgesOff` to those guards (badges joins officers/logistics/arms in the optional-layer set turned off for the pristine baseline).
- **Vet (all GREEN):** GATE OK · no-fudge ✓ · citations ✓; byte-identity harness IDENTICAL all 30 (sandbox + 9 battles × 3 seeds) badges-OFF vs HEAD `610ae6c` + 0 winner flips (the only single-phase flip — Chancellorsville seed 1 US→CS — is Jackson's flank march, TOWARD history); all 9 per-battle probes PASS with badges ON (Pickett repulsed, Burnside breaks, the Rock holds); `probe-ratings` 21/21 (R-6 step) · `probe-presets` 26 · `probe-arms` 23 · `probe-logistics` 14; wcag clean; `diag-classic nonBlank:346`; bootprobe ok; 0 pe. **ENV:** the 8 GB Mac was memory-starved (the user's apps held the RAM) so headless-Chrome probes stalled/crashed intermittently — each confirmed by retrying to a memory window; the `domcontentloaded` A/B harness was the reliable spine. Committed + pushed.

## RATING SYSTEM — R-6 (citation-provenance): the CATALOG-WIDE citation hardening + a permanent build-gate — 2026-06-22 (D103)
The citation-grade prerequisite for assigning combat-biting badges: every `Verified` claim in the data catalog now carries **≥2 independent real sources**, enforced forever by the build. An audit found the debt was catalog-wide, not just badges — **47 records across 6 files** (badges 13 · personas 2 · weapons 9 · artillery 9 · engineering 8 · terrain 1 · cabinet 5) stamped Verified with <2 sources. PURE METADATA + a little teaching-card text → **byte-identical sim** (all 10 deterministic battles IDENTICAL to committed HEAD `5b12916`; no numeric combat field touched).
- **Citation workflow (ultracode):** 15 Opus agents (7 domains → per-domain finders → default-refute verifiers → completeness critic; web-checked; NEVER-fabricate / downgrade-over-guess). **0 fabrication** (critic-confirmed). `beloved` held *Inferred* (Freeman 1934 held out as Lost-Cause-tinged); `grand_charge` upgraded *Verified* (Stewart 1959 · Hess 2001 · Sears 2003); `feared` swapped Wyeth 1899 → Hurst 1993; the negative badges on real men carry the firmest pairs.
- **The gate (`tools/build.mjs` 4e):** recursive scan of `data/*.json`; any `Verified` record (trim+case-normalized on either `prov`/`provenance`, masking-proof) with <2 sources → `die(5)`. Verified firing on all 47 before the fix; passes after (`GATE OK · … · citations ✓`). The ~72 prose "Verified (high) — …" forms cite inline and are exempt by design. Mirrors the 4d no-fudge wall.
- **Accuracy fixes (critic-surfaced):** the Fredericksburg cover card's "almost impregnable, and a deathtrap" was misattributed to the NPS (traces to a fan site) → paraphrased; `massed_battery` note dropped Pelham; `earned_in_blood` → "14 of the 16 (Army)"; Springfield/art-cs-handicap precision softened.
- **Bug-hunt:** 34 Opus agents (4 lenses × refute-verify + critic), **16 confirmed, all LOW except 1 MED, all latent**; critic *"SAFE TO COMMIT — every citation web-verified real, no fabrication, gate green, byte-identity proven."* Fixed the MED (gate strict-equality evasion) + the `||`-masking + the doc count + the terrain/feared optics; deferred the rest (no live offender). `probe-ratings` 20/20 + the catalog-wide invariant step; eng-corps/cabinet/weapons/cannon/arms/decisions all green; diag-classic `nonBlank:346`. PROCESS NOTE: a research agent wrote the 5 cabinet edits directly (accepted — match the verified output).

## RATING SYSTEM — Q9: the GM PROMOTION economy (the first depth-chart MOVE) — 2026-06-22 (D102)
The first real GM *move* beyond appoint/relieve, on the existing `cmdAppoint`/political-capital spine. The President promotes a general one grade up a clean ladder, spending a multi-currency budget, merit-gated. PURE INPUT-SEEDING + display — **byte-identical until the player promotes** (sandbox + all 9 battles proven identical to committed HEAD `fc1d251`). Design law: RATING-SYSTEM-DESIGN §12.2/§14.3/§15.
- **The ladder + move (`src/35-command.js`):** `_cmdBaseGrade` parses the verbose `generals.json` rank → **Brig. → Maj. → Lt. Gen. → General**. `cmdPromote` raises one grade, spending BOTH currencies — **political capital** (`C.clock.capital`) + a NEW **seniority** pool (`cmd.seniority`, accrues over the campaign, capped) — gated on both, with a `seniorityLeapfrog` surcharge for jumping a passed-over senior.
- **The merit gate (honest history):** each grade carries a reputation `meritBar`. EARNED → +confidence rep; ABOVE merit → ×1.8 capital + a jealousy rep HIT — so over-promoting a favorite can LOWER his OVR (rank ≠ competence; anti-Lost-Cause). Reaches the fight ONLY via a small BOUNDED skill lift (≤6, `_cmdEffectiveSkill(gen,C)` → `_cmdGenRating` → `commandLeadership` → the bridge); never the scoreboard (no-fudge wall). 0 lift un-promoted → byte-identical.
- **Surface:** a new Command-desk "officer corps — promotions" section (`_cmdPromotionsHTML`/`_cmdPromoRow`) — current grade + OVR + a Promote control + the dual-currency read-out; period broadsheet, triple-encoded, CVD-safe. `data/ratings.json` gains the `promotion` config. Rides the save (no `_SAVE_VER` bump). **DEFERRED-&-logged** (needs absent substrate): the symmetric AI-GM, cross-theater Transfer, Commission + the corps/division depth-chart tree.
- **Bug-hunt (7 Opus agents: 4 lenses × refute-verify + critic; 2 raw → 2 confirmed + 1 actionable critic gap, ALL LOW + latent; 0 HIGH/MED; critic: "SOUND, may commit — all invariants hold").** ALL 3 fixed: capital gate/spend lens asymmetry (round both); the unrecognized-rank fallback over-graded → lowest grade; `cmdInit` now re-clamps a tampered seniority on load. The critic's 4th note (diag `404`s) = the pre-existing tolerated asset-layer noise, not Q9.
- **Vet:** GATE OK (no-fudge ✓); `probe-command` 25/25 (9 new Q9); byte-identity ALL 10 vs HEAD `fc1d251` + the Q9 live sanity (promotes "Lt. Gen.", lead 79→80, un-promoted identical, 0 pe); ratings 19/desk 13/camp 8/t1probe 14 (0 pe each); diag `nonBlank:346`; bootprobe ok; wcag 0 AA failures (`#c2502e`→`#d66040`). `load` suite intermittently stalled (THREE-CDN gotcha); the domcontentloaded harness is authoritative. Next: a follow-up GM increment (AI-GM + Transfer/Commission + depth-chart tree) once the substrate exists, or Q8b scouting → R-6.

## RATING SYSTEM — R-5: the PROSOPOGRAPHY SCALE-OUT (bulk per-person rows + lazy materialization + play-as-anyone) — 2026-06-22 (D101)
The D93 "Soldier's Story" substrate: a brigade can now be inspected down to its men, and any person materialized + promoted. PURE DATA + DISPLAY — **byte-identical** (no scenario assigns bulk persons; sandbox + all 9 battles proven identical to committed HEAD `84c5e95`). Design law: RATING-SYSTEM-DESIGN §5/§8/§9 R-5.
- **The lazy-materialization keystone (§8):** a brigade token carries ONE `fldMenMeanOVR` (O(1), from xp + arm + year + a deterministic id-jitter, clamped [20,88]). `fldMaterializePerson` builds exactly ONE row on demand; `fldBrigadeMuster` a HARD-CAPPED (≤6) sample — NEVER `u.men` rows (proven: a 100,000-man brigade → 3 rows). The mean is recomputable, never the source of truth.
- **New `src/tactical/T14-ratings.js` fns:** `fldMenMeanOVR` · `fldMaterializePerson` (generated → deterministic synth period name + Inferred + a `latentCommand` seed; authored → Verified pass-through) · `fldPromotePerson` (latent-command promotion = the play-as-anyone hook; pure) · `fldPersonTeam` (EA-style team journey hook) · `fldProvenanceStyle` (Inferred HATCHED vs Verified SOLID vs Disputed dashed; pattern + glyph + word, CVD-safe) · `fldMusterHudLine` + `fldMusterRollHtml`. **Data:** `musterTemplate` + `_r5Note`. **Live wiring:** one guarded line in `fldRenderHud` adds the **MEN OVR** line (provenance-hatched) beside the R-2 combat OVR. NO new combat path; NO RNG.
- **Bug-hunt (11 agents: 4 Opus lenses × refute-verify + critic; 6 raw → 2 confirmed + 2 critic, ALL LOW + latent; 0 HIGH/MED).** ALL 4 fixed — defensive copies so a FUTURE bulk-person/career write can't corrupt canonical `GAME_DATA`: persona returned by-reference → shallow-copied at the materialize boundary; unknown `toRank` mapped to the 64 anchor → now a guarded no-op clone; the `sources` array aliased → `.slice()`'d; the persona-less promote guard returned the input ref → `_fldClonePerson` for every no-op path. Completeness verdict: no no-fudge write, no determinism leak, no lazy breach, no escaping gap.
- **Vet:** GATE OK (no-fudge ✓); `probe-ratings` 19/19 (R-5 + the 4 hardening guards, `aliasGuarded:true`); byte-identity ALL 10 vs HEAD + R-5 live UI (0 pe); diag `nonBlank:346`; field 13/officers 15/command 16/desk 13/camp 8 (0 pe each); wcag 0 AA failures (5 opacity lifts `.55/.6→.65`). DECIDED: the full muster roll + promotion + team hooks ship as probe-vetted pure functions, live-wired as the compact MEN-OVR HUD line; a dedicated inspect-expand / Army Register is a later UI pass. Next: post-R-5 GM depth-chart moves + Q8b scouting → R-6.

## RATING SYSTEM — Q8: the between-battle CAMP LOOP (drilling + progression) — 2026-06-22 (D100)
The agency that grows your rating over a campaign. A new "Camp" desk tab: between battles the President DRILLS the army (4 foci toward a ceiling, costing fatigue; or DELEGATE to the officer) + sees the army's drill grade + the general's OVR growth. Drilling raises the army he fields via a guarded, bounded `bridgeArmy` term — pure input-seeding, never the scoreboard, **byte-identical until the player engages the camp** (an `engaged` flag makes the loop inert until first drill).
- **`src/36-camp.js`** (new): campInit / campDrill / campSetDelegate / campTrainingBonus (the bridge seam, 0 when undrilled) / campOnResolve (rest + delegate auto-drill + win-seasons / bled-the-lion's-share-erodes by casualty SHARE) / campRenderTab. Wired: campInit in _t1InitAll, campOnResolve in _t1Resolve, a "Camp" tab in 30-president-shell, "36-camp.js" in the manifest. Rides the save (no _SAVE_VER bump). SCOUTING deferred to Q8b.
- **Bug-hunt (21 Opus agents, 15 raw → 1 MED + 4 critic), ALL fixed:** the real one — the "bloody" attrition test divided a raw men-count by the 0-100 strength INDEX (also read post-attrition), so nearly every battle read bloody and the win-seasons branch was DEAD (training only decayed). FIXED to the casualty SHARE (own / both-sides total; PIVOT 0.55) + the probe re-tooled with realistic casualties + a pyrrhic-win case. Plus: removed dead `lastTurn`, fixed a comment, logged player-only drilling.
- **Vet:** GATE OK; probe-camp 8/8; byte-identity ALL 10 vs Q7; bridge 6/6 + conditioning 6/6 (un-engaged unchanged) + command 16/16 + desk 13/13; diag nonBlank:346; 0 pe; wcag 0 failures. Shipped across a context-budget handoff (the new ≤25%-context rule, adopted this session). Next: R-5 prosopography scale-out.

## RATING SYSTEM — Q7: the GM MATCHUP layer (dual Attack/Defend OVR + the pre-battle matchup screen) — 2026-06-22 (D99)
The GM layer's read-out payoff: PURE DISPLAY over the rating spine (no sim change — sandbox + all 9 battles byte-identical to committed R-4).
- **Dual Attack/Defend OVR on the Command desk** (`src/35-command.js` `_cmdGenDualOVR`): the headline `_cmdGenRating` + an ATK/DEF split (the situational truth). Tilt from the persona (64-anchored, new `fldDualTilt` in T14) when rated, else generals.json aggression/caution (50-anchored). ATK/DEF pills on the active card + every appoint-pool row.
- **Pre-battle matchup screen** (`src/tactical/T7-command-side.js` injects `fldMatchupHtml(sd)`; engine in T14: `fldOOBSideOVR`/`fldMatchupBoard`/`fldMatchupEdgeWord`/`_fldMatchupCommander`): each army's FORCE OVR (men-weighted mean brigade OVR from the OOB) + strength + commander + a labeled predicted-edge bar. Forecasts the odds from the historical inputs; never read at resolve-time. Triple-encoded + CVD-safe; wcag-auditor 0 AA failures.
- **Bug-hunt (21 Opus agents, 15 raw → 3 confirmed + 1 HIGH critic), ALL fixed:** the HIGH — the multi-phase matchup summed ALL phases (per-sector day-slices) → forecast a CS edge at Gettysburg (which the larger Union army won; anti-Lost-Cause violation) → FIXED: scope to the OPENING phase + label "the Opening Engagement" (Gettysburg phase-0 CS edge = day 1, historically true). Plus: commander line phase-aware (Lee/Bragg/Grant restored); anon dedup global counter; comment reworded off the unrated Thomas.
- **Vet:** GATE OK (no-fudge ✓); probe-ratings 18/18 (Q7 step incl. the anti-Lost-Cause framing guard); byte-identity ALL 10 vs committed R-4; command 16/16, csplayer 16/16, desk ok; diag-classic nonBlank:346; 0 pe. Scope: Q7 = the read-out; the depth-chart MOVES economy (promote/transfer/AI-GM) depends on R-5 prosopography → deferred post-R-5.

## RATING SYSTEM — R-4: the X-FACTORS (the dramatic "in the zone" surge, inside the no-fudge wall) — 2026-06-22 (D98)
The dramatic Madden increment: an X-Factor badge SURGES "in the zone" — a visible, announced swing that provably can never breach the no-fudge wall. **Byte-identical when off/unassigned** — proven across sandbox + all 9 battles (R-4 build vs committed R-3, IDENTICAL deterministic results, 0 pe).
- **`fldXFactorStep(dt)`** (T14, gated `__FIELD.badges`) — per unit carrying an `xfactor` badge, tests activation (`_fldXFactorInZone`: moving / charging / ammo<12 / last-stand / flanking) and surges ONE channel: NON-speed → `_xfActive = fldRatingRealismCap(tier,"xfactor")` (arcade 1.45 / balanced 1.20 / historian 1.08); SPEED → `_spdMul` only (capped [0.85,1.15]). One-shot ⚡ announce + banner; `_xfGlow` marker pulse; deterministic decay out of zone. NO RNG. Units without an X-Factor are untouched → byte-identical.
- **`fldXFactorApplyCmd(u)`** — scales the already-summed `cmdBonus` toward the **hard 0.9 wall, NEVER beyond**; strict no-op unless active. Called in `fldSimStep` after `fldOfficersStep`, gated on `badges` (one gate for both surge channels).
- **Seams (T0):** `fldSimStep` runs the surge + apply around `fldOfficersStep`; `fldMoveFactor` clamps the COMBINED speed factor (surge × static badge) to **[0.75,1.30]** (no-op when 1 → byte-identical); `fld2dDraw` draws a guarded `_xfGlow` — a CVD-safe amber ring + a VECTOR lightning bolt (no emoji tofu), reduceMotion-honored.
- **Data:** ADDED `first_with_the_most` (Forrest, cav). 7 X-Factors total; none assigned to a shipped roster yet (R-6) → byte-identical.
- **NO-FUDGE REPLAY GATE (probe-ratings 17/17):** a tagged foot_cavalry brigade flipped a deterministic US win → CS win (the surge BITES) yet bounded (17.2% casualty delta) with the wall intact (maxCmd 0.47 < 0.9). Plus the byte-identity harness (10/10 identical vs committed R-3), tactical-visuals 10/10, diag-classic nonBlank:346, 0 pe. Env note: the `load` suite stalled intermittently (THREE-CDN) — vetted via the domcontentloaded byte-identity harness (the authoritative no-regression proof).
- **Bug-hunt:** 23 Opus agents (6 finders × refute-verify + critic), 16 raw → 4 confirmed + 3 critic, 0 HIGH, all LATENT (fire at R-6). All 7 fixed: combined-speed clamp; cmdBonus surge gated to non-speed (one channel/badge); apply decoupled from the officers gate; vector bolt; grand_charge prov→Inferred + dual-downside deferred-note. Catalog-wide badge-provenance sweep logged for the R-6 content pass. Re-vetted after fixes.

## RATING SYSTEM — R-3: the badge engine (the first rating→combat seam) + the no-fudge build-gate — 2026-06-22 (D97)
The first place a rating TOUCHES combat. A guarded one-token factor seeds the levers the engine already reads; the no-fudge OUTPUT wall is now enforced by the build. **Byte-identical when off** — proven across sandbox + all 9 battles (R-3 build vs committed R-2, identical deterministic results).
- **`fldBadgeFactor(u,lever)`** (T14) — guarded multiplicative factor, IDENTITY 1.0 when badges off / no matching badge; sums the unit's badge `mag`s for a lever, gates by trigger, clamps the sum to the realism-scaled per-lever cap (arcade .16 / balanced .10 / historian .06). Negatives are dampers (≥1−cap), never enemy buffs. Plus `fldUnitCohesion` (rally term, 0 when unauthored → byte-identical).
- **Four guarded seams** (T0): fire (fldResolveFire), rally+cohesion (fldMoraleStep), melee (fldResolveMelee), speed (fldMoveFactor). `__FIELD.badges` gate (default ON, `_badgesOff` hook) + `__FIELD.realismTier` (T6 fldPresetsApply).
- **Badge data** (data/ratings.json): 25 badgeDefs gain `fldLever` + signed `mag`. Ships Star/Superstar positives + the Verified negatives with bite (McClellan Slows / Bragg Piecemeal / Burnside Rigid / Green Levies / Powder-Shy). NOT yet assigned to shipped rosters (that's R-6) → battles byte-identical.
- **No-fudge OUTPUT-WALL build-gate** (build.mjs gate 4d): scans the rating module for any cas/aCas/bCas/.victory/.men/sev.* write (dot AND bracket notation), build FAILS (exit 5) if found. Verified firing on planted `u.men=0`, `u["men"]=0`, `F.sev["attrition"]=2`; clean build prints `GATE OK · … · no-fudge ✓`.
- **Vetting — ENVIRONMENTAL DEVIATION:** the standard `load`-based probe suite was blocked this session (headless-Chrome `load` never fires for the game page — the THREE-CDN script stalls in headless; broke the committed R-2 build identically; trivial page loads in 73ms; domcontentloaded loads everything). Contributors: single-threaded `python http.server` chokes on the page's ~170 asset requests (use ThreadingHTTPServer), + the THREE-CDN `load` stall. R-3 was vetted via a **domcontentloaded byte-identity harness** (`.tmp/r3-verify.mjs`): sandbox + all 9 battles deterministic, R-3 vs committed R-2 → IDENTICAL casualties/winner/steps for all 10, + the full badge-logic assertions, 0 pageerrors (a stronger cross-build byte-identity check than the standard probes). Adversarial bug-hunt (4 Opus lenses × verify + critic): 2 confirmed (gate-4d bracket bypass → fixed; phantom `sharpshooters` key → fixed) + 0 critic. Committed + pushed. **Follow-up for the next session: re-run the standard `load` suite to confirm green once the env is healthy, and/or harden the probe runner (ThreadingHTTPServer + domcontentloaded settle wait, or vendor THREE locally).** (DECISIONS D97; design law RATING-SYSTEM-DESIGN §4/§6/§9.)

## RATING SYSTEM — R-2: the OVR read-out UI (Command-desk grades + tactical HUD brigade OVR) — 2026-06-22 (D96)
The display increment of the D94 Madden layer — PURE DISPLAY, no sim change (the full no-regression suite confirms all 9 battles byte-identical). The rating numbers the R-1 spine already produces are now surfaced and graded A–F across two surfaces.
- **Command desk:** the active-general card shows the officer OVR (round(_cmdGenRating) — A+ ≥90 reachable) + the A–F report-card grade + the leadership word, with the band colour as a decorative left-border accent and the 42–88 bridge value kept as a secondary "Fields command at NN" line; the appoint-pool rows add a colour dot + bold A–F letter + word + "(NN OVR)".
- **Tactical HUD:** a new `fldRatingHudSelected(u)` line shows the selected brigade's computed OVR (fldUnitRatingOVR, pure read) + grade, wired into fldRenderHud (guarded → byte-identical when absent).
- **WCAG 2.2 AA + CVD-safe:** triple-encoded everywhere (number + letter-grade + word in high-contrast --parch text; colour decorative only, on aria-hidden stripes/dots). wcag-auditor PASSED all three new surfaces. Two pre-existing sub-AA secondary labels (--rule #8a7350 = 3.25:1 on the real desk ground) were lifted to #b3925e (5.0:1 worst-case, measured against the ACTUAL .sheet radial gradient + 4% overlay, not pure black — the R-2 bug-hunt caught the auditor's first fix #8d7551 being computed against pure black and still failing).
- **Vetting:** GATE OK; probe-ratings 14/14 + probe-command 16/16 (new R-2 UI steps); FULL no-regression GREEN (28 probes, byte-identical); diag-classic nonBlank:346; 0 pageerrors. Adversarial bug-hunt (3 Opus lenses × refute-verify + critic): 2 confirmed (the false-AA contrast claim, fixed + honestly measured) + 2 critic (an audit /* */ comment leaking into the rendered DOM → moved outside the JS string; gr.word/g.word unescaped → escaped for defense-in-depth). All fixed + re-vetted. Committed + pushed. (DECISIONS D96; design law RATING-SYSTEM-DESIGN §3/§9/§10.)

## RATING SYSTEM — R-1: officer derivation into the existing command + field pipes — 2026-06-22 (D95)
The first WIRING increment of the D94 Madden layer (R-0 shipped the pure-function data spine on 2026-06-21). The documented persona now SEEDS the two pipes the engine already reads — and only the inputs, never the outputs (the D74/D94 no-fudge keystone holds), with every unrated entity byte-identical.
- **Strategic:** `_cmdGenRating` now derives a general's `skill` from a linked persona (`generalPersonas` in `data/ratings.json`, keyed by the `generals.json` id) via `_cmdGenPersona`/`_cmdEffectiveSkill`. The 9 personas (CS Lee/Jackson/A.S. Johnston/Bragg/Longstreet · US McClellan/Grant/Burnside/Meade — the principal commanders of the 9 D92-hardened battles) are calibrated so `round(0.45*command+0.35*tactical+0.20*discipline) === gen.skill EXACTLY` → the whole strategic layer (commandLeadership, auto-resolve, conditioning, the appoint-pool words) stays byte-identical while the persona becomes the source of truth. `cmdActiveTraits.skill` + the Command-desk Skill bar also route through `_cmdEffectiveSkill` (one source of truth — the bug-hunt critic's fix).
- **Tactical:** `fldMakeOfficer` derives a field leader's `quality`/`radius`/`fate` from his persona — but ONLY via an **explicit `pid` opt-in** (no `id` fallback), the fix for a real cross-scenario id collision (`ld_jackson` is reused at Bull Run / Antietam / Chancellorsville). The 7 Bull Run leaders are opted in (their auras shift ≤0.005, the R-0 calibration tolerance); every other scenario stays byte-identical.
- **The 9 strategic personas** carry Verified documented character (the named negatives `the_slows`/`rigid_plan`/`piecemeal` plainly stated, real sources cited; the attribute vector is Inferred calibration anchored to each Verified `gen.skill`; aggression tracks the temperament and is excluded from the headline OVR — anti-Lost-Cause).
- **Vetting:** GATE OK; `probe-ratings` 13/13 (3 new R-1 steps incl. a cross-scenario collision guard), `probe-command` 15/15 (incl. a source-of-truth guard); FULL no-regression GREEN (all 9 battles byte-identical incl. chancellorsville 24/24, + systems suite); diag-classic `nonBlank:346`; 0 pageerrors. Adversarial bug-hunt (5 Opus lenses × refute-verify + critic): 0 confirmed finder findings, 1 LOW critic finding fixed. Committed + pushed. (DECISIONS D95; design law RATING-SYSTEM-DESIGN §5/§6/§9.)

## ROSTER HISTORY-ACCURACY HARDENING (all 9 shipped battles) + "The Soldier's Story" pillar captured — 2026-06-21 (D92/D93)

Aaron asked whether the full Chattanooga-grade rigor (citation-grade research + adversarial verify) had been applied to the already-built battles. It had not for the 7 pre-Opus battles (the Codex/early Eastern marquee + Shiloh + Bull Run); only Vicksburg + Chickamauga had. Launched an adversarial audit Workflow (9 Opus historians, one per battle, + a synthesis critic) and a consolidated casualty-direction diagnostic (`tools/sweep-all-battles.mjs`). The audit found **15 HIGH wrong facts under "Verified" stamps**, overwhelmingly the rank-at-the-battle class — all fixed + annotated in-file (Malvern: Hunt→Col., Jackson→Maj.Gen., Porter→Brig.Gen., Hooker→Maj.Gen.; Chancellorsville: Rodes→Brig.Gen., Berry→III Corps, Stonewall Bde→Colston's div, dead-officer Hazzard→Best; Shiloh: Breckinridge→Brig.Gen., Bragg→right wing, Wallace died Apr 10; Gettysburg: Doubleday & Early→Maj.Gen., Stannard→I Corps; Vicksburg: M.L. Smith→Brig.Gen.) + ~12 medium/low. Surfaced (not auto-fixed): Bull Run Heintzelman + Antietam Mansfield (genuine source conflict). **Aaron locked the input/output principle:** accurate default equipment (counts/guns/weapons/terrain) drives the probable historical result via the D74 model — never an output gate forcing it; dropped the planned Antietam balance refight; `sweep-all-battles.mjs` stays diagnostic; kept guards are system validations only. Captured **D93 "The Soldier's Story"** pillar (EA-Sports prosopography + play-as-anyone whole-war mode, scheduled after C+E). Verified: GATE OK; all 9 battle probes + roster + builder + broad systems suite + diag-classic green, outcomes byte-preserved, 0 pageerrors. Edits labels/notes only except Antietam Benjamin 6→4 guns + Shiloh Wood→Chalmers (stats kept). (DECISIONS D92/D93.)

**D92-verify / commit (resumed session, 2026-06-21):** the prior session died on a 529 API error with the D92 work on disk but uncommitted. Resumed; before committing, ran an **independent adversarial re-verification** (Workflow: one Opus historian per battle re-checking every asserted correction against 2+ sources + an adjudicating critic; the 2 that 529'd — Malvern, Chancellorsville — re-run as standalone Opus agents). It caught **4 more items, all fixed pre-commit:** (1) Bull Run `cs_elzey` — the "(Elzey's own regiment)" gloss belongs to the 1st Maryland (Elzey's old regiment), not the 10th/13th Virginia (13th = A.P. Hill's, 10th = Gibbons's) → gloss moved (a wrong fact D92 *introduced*); (2) Shiloh `us_buell_ammen` → **Col. Jacob Ammen** (was wrongly Brig. Gen.; his BG dated Jul 16 1862, after the battle — the anachronistic-rank class, *missed*); (3) Antietam `us_battery_b` **`rifled→smooth`** (Gibbon's Battery B = six 12-pdr Napoleons; its own note already said "double canister" — the smoothbore signature; an accurate-weapon input per the D92 principle, and the antietam probe stayed 16/16 so the Cornfield outcome is byte-preserved); (4) Malvern Hill `us_hooker` reverted **Maj.Gen.→Brig.Gen.** (D92 over-promoted him by his May-5-1862 date-of-rank, but his MG commission was *issued* Jul 26 1862 after the battle; the Seven Days OOB lists him BG, exactly as the file keeps Porter/Couch/Kearny — *introduced*, internal-consistency fix). Full re-vet GREEN (9 battle + roster + custom + systems suite + diag-classic `nonBlank:346` + boot/t1, 0 pageerrors). Committed + pushed.

## CHICKAMAUGA (Phase C-2 Western breadth) — the river of death, a 3-phase epic (the woods → Longstreet's breakthrough → Thomas at Snodgrass Hill) — 2026-06-21
- **What it is:** the bloodiest battle of the Western theater, built as a 3-phase battle on the T8 multi-phase engine: **P0 The Battle in the Woods (Sep 19)** → **P1 Longstreet's Breakthrough (Sep 20, the Brotherton gap)** → **P2 The Rock of Chickamauga (Sep 20 afternoon, Thomas holds Snodgrass Hill / Horseshoe Ridge)**. New `data/chickamauga.json` + one registry line in `src/tactical/T1-bull-run.js` (menu rank 60, after Vicksburg) + `tools/probe-chickamauga.mjs` + `tools/sweep-chickamauga.mjs`. The teaching is **CONTINGENCY OVER GENIUS** (the breakthrough was handed to Longstreet by the Wood gap order, not Confederate martial superiority) and **the squandered victory** (Bragg failed to pursue → the siege of Chattanooga → the November reversal). Universal gun-count model only; **no per-battle damage fudge** (D74).
- **Framing decision (decide-&-log, no fork surfaced):** Chickamauga + Chattanooga = the C2 "Chickamauga/Chattanooga" item built as a matched PAIR of separate 3-phase epics (the Antietam/Gettysburg precedent), each its own vetted milestone; this milestone = Chickamauga, next = Chattanooga. **Top-level `attacker: CS` / `defender: US` across all 3 phases** — the Army of the Cumberland fought on the defensive throughout, so the CS player takes the offensive (Bragg/Longstreet) and the US player holds (Thomas); cleaner than Vicksburg's US-attacker-throughout and sets up Chattanooga as the US-offensive MIRROR.
- **Research+verify (Workflow, Sonnet finders × Opus adversarial verify + synthesis):** corrected real errors before authoring — **Hood wounded Sep 20 / Maj. Gen.** (not Sep 19 / Lt. Gen.), **Helm in Breckinridge's division** (not Cleburne's), **Cheatham restored** to the OOB, **Granger & W.H.T. Walker = Maj. Gen.** (not Brigadier), **Loring removed** (not at Chickamauga), the gap-reporter left flagged (Kellogg vs Bond disputed), and the **"River of Death"** Cherokee gloss shipped only as disputed folk etymology, never as fact.
- **Balance (AI-vs-AI, 32 seeds, deterministic — the D74 principle; outcomes from OOB/terrain/timing/weight, NO fudge):** P0 woods US-holds 32/32 (mutual bloodbath ~2,400 each — "nobody could see enough to win"; the Union line held Sep 19, historical); P1 Brotherton breakthrough **CS 32/32** (the US routed cheaply, US:CS ~5.1; scoreWeight **3** — the decisive phase); P2 the Rock **US-holds 32/32** with the CS assault paying **CS:US ~3.0** (the failed-assault lesson, mirrored from Fredericksburg). **Aggregate CS 32/32** (CS 3 / US 2 via the weight-3 breakthrough) — a clear Confederate tactical victory in which Thomas's stand prevents annihilation; and a human US player who closes the gap in P1 OVERTURNS the battle (the Wood-gap alt-history hinge). The CS, who won the field, **bled MORE in aggregate** (~70k vs ~63k over 8 seeds) — the costly-victory teaching, now probe-guarded.
- **Adversarial bug-hunt (Workflow, 10 Opus agents — 6 diverse-lens finders × default-refute verify + a completeness critic): 2 confirmed + 3 critic gaps, ALL fixed.** HIGH (critic, the Vicksburg-class defect): **Charles Harker mis-ranked "Brig. Gen." under a Verified stamp — he was a COLONEL at Chickamauga** (like Hans Heg, whom the data gets right; his brigadier-generalcy was dated to the battle but came afterward) → corrected to **Col. Charles Harker** + a fact-check probe guard. Plus 4 probe-rigor guards added: the P1 breakthrough casualty-direction (US routed cheaply, US:CS ≥2.5), the P0 winner gate (the line holds ≥7/8), the P1 win-by-SEIZE check (winBy "hold", not a degenerate destroy/timeout), and the aggregate "the winner bled more" guard (CS aggregate casualties > US).
- **Verification (PROPER VETTING):** build GATE OK; `probe-chickamauga` **18/18**, 0 pe (with the tightened guards); full no-regression GREEN — bullrun 15 · malvern 24 · antietam 16 · fredericksburg 22 · chancellorsville 24 · gettysburg 16 · shiloh 29 · vicksburg 18 · roster 8 · field 13 · officers 15 · logistics 14 · arms 23 · presets 26 · csplayer 16 · campaign-link 16 · fog 13 · ai 15 · autopause 13 · custom-builder 9 (its EXPECTED registry baseline updated to include `chickamauga` — the recurring D86/D88 gotcha) · engineering-corps 22 · tactical-visuals (now 10 scenes incl. live Chickamauga 2D/3D, textureWarnings:0); diag-classic nonBlank 346; boot/t1 ok; **0 pageerrors**. WCAG: no new color CSS (the data flows into the already-AA shared T1/T8 components). Committed + pushed (DECISIONS **D90**).

## ENGINEERING CORPS (Phase F) — increment 3: PONTOON BRIDGING (the pontoon train) → CORPS COMPLETE — 2026-06-21
- **What it is:** the third and last increment of "all of the above" — the Engineering Corps is now complete (entrench D87 · abatis D88 · pontoons D89). An OPT-IN river (`__FIELD.terrain.rivers[]`) gates movement: impassable except at a crossing — a shallow ford (slow + fatiguing), a deep ford (fordable only at Arcade; needs a bridge at Balanced/Historian), or a player-laid pontoon bridge (key **N** / "Pontoon"): an infantry pioneer detail on the near bank lays a bridge over realism-scaled time → opens a fast corridor. Surfaced via Skirmish → Ground → "River crossing" (+ `opts.river` for the probe).
- **B-5 coupling:** `fldEngRealism()` scales sapper/pontoon build speed, ford slow, AND deep-ford fordability (`DEEP_FORD_CAP` 0.85). The strategic Engineer Works Corps (`bridgeArmy.engineering`) → `__FIELD.engCorpsSpeed` (set in T2 `fldCampaignCondition`) lays bridges faster on a campaign launch (A1 anchor); standalone stays 1 (byte-identical).
- **Byte-identical / player-only:** the active gate reuses the T0 seams reserved for it (`fldEngMoveFactor` ford slow + `fldEngMoveGate` bank clamp); no shipped scenario declares `terrain.rivers` and the AI never lays a pontoon, so every AI-vs-AI baseline is byte-for-byte unchanged (reads gated by `rv && rv.length` / `engPontoons`; reset per-launch + per-phase). One T0 seam edit: `fldStepMovement` settles a no-progress river clamp to HOLD (the soft-deadlock fix), `eg`-gated → riverless identical. 2D water+boats / 3D water plane+pontoon (leak-managed, fog/low-tier aware); citation-grade Fredericksburg/James-River teaching card (research+verify workflow, 30/38 claims survived adversarial verification).
- **DECISIONS (decide-&-log):** Bull Run's creek/Stone Bridge stay cosmetic (live would regress a shipped battle); AI river-pathing/AI-built works is the named extension point (player-tool this increment), so a river skirmish frames the PLAYER as the attacker (river placed between the player's home edge and the objective, either side) while the AI defends the far bank.
- **Bug-hunt (19 agents, Opus): 8 confirmed + 6 critic, all fixed** — the open-ford→deep-water sidestep leak (+ midpoint sample for tangential bends), the buried 3D water plane (raised to a thin on-ground sheet, fords lifted), the march-in-place soft-deadlock at the bank (settle to HOLD), the degenerate CS-player river framing (placed by player side), a silent crossing-refusal (aria-live + HUD halt line), dupe-span message, pontoon `side` + per-side cap.
- **Verification:** build GATE OK; `probe-engineering-corps` 22/22, 0 pe; full no-regression GREEN (21 probes byte-identical: field 13 · bullrun 15 · shiloh 29 · malvern 24 · antietam 16 · fredericksburg 22 · chancellorsville 24 · gettysburg 16 · vicksburg 18 · roster 8 · visuals ok[texWarn 0] · custom 9 · fog 13 · ai 15 · autopause 13 · campaign-link 16 · officers 15 · logistics 14 · arms 23 · presets 26 · csplayer 16; diag-classic nonBlank 346; boot/t1 ok; 0 pageerrors); WCAG-AA on all new UI (≥4.5:1, CVD-safe); help overlay gains the B/X/N keys. Committed + pushed (DECISIONS **D89**).

## ENGINEERING CORPS (Phase F) — increment 2: ABATIS / OBSTACLES (the axe) — 2026-06-21
- **What it is:** increment 2 of "all of the above". A selected player INFANTRY brigade's pioneer detail builds a capped timber belt across its front (key **B** / "Abatis"); enemies forcing it are slowed (`fldEngMoveFactor`), disordered once per crossing (morale + fatigue, latched on `_engObsTouch`), and fire raggedly (`fldEngFireFactor` ×0.72) until they recover; a nearby brigade clears a lane (key **X**). 4-belt/side cap. The abatis half of `src/tactical/T13-engineering.js` + guarded T0 seams + a T8 `_fldBuildPhase` per-phase reset.
- **B-5 coupling:** build/clear time, slow factor, and disorder magnitude all scale with `fldEngRealism()` (Historian bites harder + works slower than Arcade).
- **Byte-identical / player-only:** no AI builds; the move/fire seams return exact identities (1 / null) with no live belt, so every AI-vs-AI baseline is byte-for-byte unchanged. 2D stake-line + 3D crossed-timber (fog-gated, low-tier aware), Mahan-sourced teaching card.
- **Provenance:** authored 2026-06-20, left uncommitted/unvetted when the prior Codex session ran out of usage (Codex's only landed work = the browser-probe Seatbelt guard, committed separately as `679ee7d`). This session vetted + pushed it (`02b2dd8`).
- **Bug-hunt (16 agents, Opus): 8 confirmed + 4 critic, all fixed.** HIGH: belts leaked across phases in multi-phase battles (Vicksburg/Antietam/Gettysburg) → `fldEngPhaseReset()` from `_fldBuildPhase` (keeps `_engUsed` for the end-card). MED: abandoned belts never decayed (ate the cap) → decay like abandoned entrenchments. MED: render ignored fog → fog-gated 2D+3D. LOW: same-tick id collision → monotonic `_engObsSeq`. LOW: routing-unit morale dock → routing guard. Kept (deliberate): entrench+abatis on one brigade (historically correct), one disorder shock per crossing.
- **Verification:** build GATE OK; probe-engineering-corps 15/15 (added: multi-phase reset, abandoned decay, unique ids, routing immunity); full no-regression GREEN (50 probes, 0 pageerrors, diag-classic nonBlank 346). Fixed a D86 leftover: probe-custom-battle-builder was red since Vicksburg shipped (EXPECTED registry omitted `vicksburg`) → 9/9. (D88.)
- **Next:** PONTOON bridging (new river/water terrain + bridge-laying + crossing gate) — the last engineering increment extending T13.

## ENGINEERING CORPS (Phase F) — increment 1: FIELD ENTRENCHMENTS — 2026-06-20
- **What it is:** the first increment of Aaron's Tactical Engineering Corps directive (he locked "all of the above" — entrench + abatis + pontoons). A player brigade ordered to ENTRENCH (key **E** + control-bar button + help copy) digs in over ~70 sim-seconds and earns parapet-grade COVER; marching off the works abandons them. New `src/tactical/T13-engineering.js` + 9 guarded T0 seams + `tools/probe-engineering-corps.mjs`.
- **B-5 coupling:** dig speed + cover strength read the realism slider (Arcade fast/×1.43, Balanced ×1.62, Historian slow/×1.81), derived from `__FIELD.sev.attrition`.
- **Facing-aware (bug-hunt fix):** the parapet protects its FRONT arc only — flank = half, rear = none — so flanking still counters fixed works (matches the one-sided berm render).
- **Byte-identical:** entrenchment is player-only; no scenario/AI triggers it, and `fldEngCover` returns exactly 1 for any non-entrenched unit, so every AI-vs-AI baseline is byte-for-byte unchanged.
- **Bug-hunt (17 agents):** 1 confirmed (omnidirectional cover → fixed facing-aware); critics triaged (melee charger-exposed is correct base behavior; multi-phase 3D self-heals; fog-ghost-works deferred to the AI-entrench increment).
- **Verification:** build GATE OK; probe-engineering-corps 8/8; full no-regression suite byte-identical (field/bullrun/shiloh/fog/ai/autopause + all battles + officers/logistics/arms/presets/csplayer/campaign-link); tactical-visuals 8/8 (textureWarnings:0); diag-classic nonBlank 346; boot/t1 ok; 0 pageerrors. (D87.)
- **Next:** ABATIS / obstacles, then PONTOON bridging (new river terrain) — each its own vetted increment extending T13.

## VICKSBURG — Phase C-2 Western breadth: the 3-phase river-fortress SIEGE, finished + adversarially hardened — 2026-06-20
- **What it is:** the first SIEGE in the game and the last open Codex project. Vicksburg, May 19–July 4 1863, as a THREE-PHASE battle on the T8 multi-phase engine: the Stockade Redan (May 19) → the Forlorn Hope / Great Redoubt (May 22) → the Saps and the Mine vs the 3rd Louisiana Redan (June 25). New `data/vicksburg.json` + registry line + menu rank 55 in `src/tactical/T1-bull-run.js` + `tools/probe-vicksburg.mjs` (18 steps). Universal gun-count model only; anti-Lost-Cause ("dug out, not starved out").
- **Finished the Codex draft:** switched the readability probe's WebGL screenshot from the hanging Playwright `page.screenshot()` to the proven render→`canvas.toDataURL()` same-tick capture; updated `tools/probe-tactical-roster.mjs` to guard `vicksburg` (the Codex left it out, so the menu-order step was failing).
- **Adversarial bug-hunt (25 agents, 4 lenses × default-refute verify + critic): 8 confirmed, all material fixes applied —**
  - **History (HIGH):** Hebert was wrongly placed at the Stockade Redan and tagged "Verified." Installed **M.L. Smith's division** there (its real defender) and relocated **Hebert** to the **3rd Louisiana Redan** under Forney (his real sector, named for his old regiment, site of the June 25 mine); honest provenance.
  - **Balance/teaching (HIGH):** the failed May assaults were inverted — the dug-in defender bled more than the exposed attacker, teaching the opposite of the cards. Rebalanced toward fire parity + the fortress's historical pre-sited gun edge, and moderated the siege bloodbath (6.84:1 → ~3.8:1). 8-seed sweep: P0 US:CS **1.93**, P1 **2.64** (attacker now pays), redans hold **8/8**, siege wins **8/8**, Vicksburg falls **8/8**, score US 3 / CS 2.
  - **Scoring (MED):** siege `scoreWeight` 2.5 → 3 to pull the historical result a clear margin off the draw-band edge.
  - **Probe rigor (MED):** added a casualty-DIRECTION gate (attacker must pay on the held assault phases) + the new `tools/sweep-vicksburg.mjs` per-phase A/B harness.
  - **T12 readability (MED):** fixed the per-sync GPU leak (disposal registry `__M3D._t12Disp`), gated the selection halo on the low GPU tier, and put the strength count on a fixed dark pill for WCAG-AA.
- **Verification:** build GATE OK; probe-vicksburg 18/18, 0 pe; tactical-roster 8/8; bullrun 15/15; malvern-hill 24/24; antietam 16/16; fredericksburg 22/22; chancellorsville 24/24; gettysburg 16/16; shiloh 29/29; field/officers/logistics/arms/presets/csplayer/campaign-link green; tactical-visuals 8/8 (`textureWarnings:0`, 0 pe); bootprobe ok (10 tolerated 404s); t1probe ok; diag-classic `nonBlank:346`; `git diff --check` clean. Sim engine T0–T8 untouched. (D86.)
- **Next (non-battle):** the Tactical Engineering Corps on the field (pontoons/entrenchments/abatis/clearing) × the B-5 sliders — Aaron's run-k directive.

## LEFTOVER VETTING + 3D OBJECTIVE BEACON — 2026-06-19
- **First job completed:** Codex inspected the dirty working tree before new work. The uncommitted D77-D84/C4/Malvern/Chancellorsville/custom-builder/visual/help docs were treated as lower-model/session leftovers, audited, and vetted rather than reverted. No unrelated changes were reset.
- **Issue found/fixed:** `tools/probe-gettysburg.mjs` had a probe-output bug: it returned `sandboxPhases` after launching Antietam, so the summary misleadingly displayed Antietam phase data for a single-objective Gettysburg contract. The probe now captures Gettysburg's phase state before the Antietam control launch and reports `antietamPhases` separately. No gameplay behavior changed.
- **New Codex-owned graphics task:** added a procedural 3D `objectiveBeacon` in `src/tactical/T0-field-sandbox.js` so real-time 3D scenes show the objective clearly. It uses simple Three.js primitives/materials only: no assets, no textures, no downloads, no Blender, no simulation/input/balance change.
- **Visual probe expanded:** `tools/probe-tactical-visuals.mjs` now captures 8 scenes: Shiloh, Malvern Hill, Gettysburg, and Chancellorsville in both 2D and 3D. Each 3D capture asserts the objective beacon exists at the scenario objective coordinates and still fails on page errors, blank canvases, or the known Three.js flag texture warning.
- **Verification:** `node tools/build.mjs` GATE OK; `node tools/probe-custom-battle-builder.mjs` 9/9; `node tools/probe-tactical-roster.mjs` 8/8; `node tools/probe-chancellorsville.mjs` 24/24; `node tools/probe-malvern-hill.mjs` 24/24; `node tools/probe-shiloh.mjs` 29/29; `node tools/probe-gettysburg.mjs` 16/16; `node tools/probe-antietam.mjs` 16/16; `node tools/probe-fredericksburg.mjs` 22/22; `node tools/probe-bullrun.mjs` 15/15; `node tools/probe-field.mjs` 13/13; officers/logistics/arms/presets/csplayer/campaign-link green; `node tools/probe-tactical-visuals.mjs` 8/8 with 0 pageerrors and `textureWarnings:0`; broad legacy strategy/tactical probes (`ai`, `autopause`, `fog`, `auto-resolve`, `armory-field`, `blockade`, `bridge`, `cab-exploit`, `cabinet`, `cannon`, `command`, `conditioning`, `cover`, `decisions`, `desk`, `economy`, `engineering`, `manpower`, `morale`, `motion`, `portraits`, `press`, `production`, `targeting`, `victory`, `weapons`) all passed; `node tools/bootprobe.mjs` ok with known tolerated resource 404 console noise; `node tools/t1probe.mjs` ok; `node tools/diag-classic.mjs` Classic paints (`nonBlank:346`); `git diff --check` clean; `node tools/build.mjs --check` GATE OK.
- **Next:** Phase C Western breadth remains the best Codex-owned path, likely Vicksburg/siege architecture and probes. A graphics follow-up could improve 3D unit label/selection readability after the objective beacon.

## CUSTOM BATTLE BUILDER C4 — single-phase scenario authoring tool — 2026-06-18
- **What it is:** Codex added a first-class **Custom Battle Builder** main-menu flow for the real-time `__FIELD` tactical engine. It is a real builder surface, not a landing page: author scenario name/date/place, field size, objective, sides/briefs, terrain hills/woods/walls/markers, OOB units, artillery/guns, reinforcement timing, optional leaders, supply trains, teaching cards, and provenance.
- **Implementation:** new `src/tactical/T11-custom-battle.js` plus a manifest entry. Custom scenarios stay out of `fldScenarioRegistry()` so the historical roster remains exact; `fldScenarioData(id)` now falls back to `fldCustomScenarioData(id)` only for explicit `custom_...` IDs. Valid custom scenarios launch through the existing single-phase `fldScenarioInit` path, using the same `oob`, `reinforcements`, `terrain`, `objective`, leaders, supply, and universal gun-count artillery model as historical battles. `FLD.BASE_FIELD_W/H` reset field size every launch, while single-phase scenario data may safely set `field.w/h`.
- **Schema/validation:** C4 is intentionally **single-phase V1**. Phase authoring is deferred until a phase editor can be probed as safely as authored `phases[]` data. Validation blocks missing required metadata, unsafe IDs/prototype keys, duplicate IDs, objective overflow, out-of-bounds/clamped numerics, invalid reinforcement timing, side ownership mistakes, unrealistic artillery crew ratios (12-40 crew per gun), malformed JSON, `phases[]`, and forbidden battle-specific combat/damage keys. There are no per-custom-battle damage fudges.
- **Import/export/persistence:** export produces shareable JSON (`{schema:"cw_custom_battle_v1", scenario:{...}}`); import round-trips canonical scenario data and rejects malformed/unsafe JSON without page errors. Six local persistence slots live in `localStorage` under `cw_custom_battles_v1`; saved scenarios are discoverable only by explicit custom ID and can be launched through the field path.
- **Focused probe:** new `tools/probe-custom-battle-builder.mjs` verifies builder render, validation failures, valid scenario creation, export/import round trip, UI malformed-import rejection, slot save/list/load/delete, explicit custom-ID launch contract, shared gun model, field-size reset, no phase leak, no Classic `G.battle`, and no historical registry contamination. Current result: **9/9 steps ok, 0 fail**; JSON/screenshot artifacts in `tools/shots/probe-custom-battle-builder.*`.
- **Verification:** `node tools/build.mjs` GATE OK; `node tools/probe-custom-battle-builder.mjs` 9/9; `node tools/probe-tactical-roster.mjs` 8/8; `node tools/probe-field.mjs` 13/13; `node tools/probe-bullrun.mjs` 15/15; `node tools/probe-malvern-hill.mjs` 24/24; `node tools/probe-antietam.mjs` 16/16; `node tools/probe-fredericksburg.mjs` 22/22; `node tools/probe-chancellorsville.mjs` 24/24; `node tools/probe-gettysburg.mjs` 16/16; `node tools/probe-shiloh.mjs` 29/29; `node tools/probe-officers.mjs` 15/15; `node tools/probe-logistics.mjs` 14/14; `node tools/probe-arms.mjs` 23/23; `node tools/probe-presets.mjs` 26/26; `node tools/probe-csplayer.mjs` 16/16; `node tools/probe-campaign-link.mjs` 16/16; `node tools/probe-tactical-visuals.mjs` 6/6; `node tools/bootprobe.mjs` ok=true with known/tolerated 404 resource console entries and no real errors; `node tools/t1probe.mjs` ok=true; `node tools/diag-classic.mjs` Classic paints (`nonBlank:346`, only pre-existing 404 resource messages); `git diff --check` clean.
- **Next:** C4 is complete. Continue Phase C breadth with Western theater work (Vicksburg / Chickamauga-Chattanooga / Atlanta-March / Franklin-Nashville), C3 USCT battles, or the next Codex-owned roadmap item.

## MALVERN HILL — Phase C-1 Eastern marquee battle (Seven Days artillery duel) — 2026-06-18
- **What it is:** Codex added the remaining Eastern marquee battle as a data-driven single-phase scenario: Malvern Hill, July 1, 1862, the Seven Days' culminating artillery duel. It models Lee's failed, poorly coordinated Confederate assault against Fitz John Porter's Union line and Henry Hunt's massed artillery on the open Malvern plateau. The scenario is CS attacker / US defender, fog off by default, and uses the established cautious-assault pattern to represent piecemeal attacks without touching combat math.
- **Implementation:** new `data/malvern-hill.json` with historical menu/side-choice/brief/end-note text, leaders/fates, terrain (Malvern Hill plateau, Crew House ridge, open killing fields, approach woods/ravines, roads, James River gunboat marker), OOB, reinforcements, teaching cards, and codex entry. `src/tactical/T1-bull-run.js` registers `malvernHill` from `GAME_DATA["malvern-hill"].malvernHill` and orders the public roster First Bull Run → Malvern Hill → Antietam → Fredericksburg → Chancellorsville → Gettysburg → Shiloh. Help copy now names the new battle.
- **Probe:** new `tools/probe-malvern-hill.mjs` verifies data/historical guardrails, single-phase launch, cautious CS attacker, terrain cover, no wall-pattern import, universal gun-count artillery (US 50 guns vs CS 22 in the scenario abstraction), no battle-specific damage/fudge keys, reinforcement timing/idempotency, deterministic AI-vs-AI resolution, US-defender-favored 8-seed balance, passive US/CS player launches, side-aware objective/end-note framing, menu/side-card rendering, and light roster regressions. `tools/probe-tactical-roster.mjs` now guards seven historical battles and the updated order.
- **Balance result:** `probe-malvern-hill` reports seed 101 as US timeout at 500s, US losses 1,336, CS losses 6,545; 8-seed balance US 7/8, CS 1/8. That gives the intended historical shape: the gun line usually holds, but a better-coordinated Confederate attack can overturn history.
- **Verification:** `node tools/build.mjs` GATE OK; `node tools/probe-malvern-hill.mjs` 24/24, 0 fail, 0 pageerrors; `node tools/probe-tactical-roster.mjs` 8/8, 0 fail; `node tools/bootprobe.mjs` ok=true with 10 known/tolerated 404 resource console entries; `node tools/t1probe.mjs` ok=true, 14 steps, 0 errors/pageerrors; `node tools/diag-classic.mjs` Classic paints (`nonBlank:346`, only pre-existing 404 resource messages); `node tools/probe-bullrun.mjs` 15/15; `node tools/probe-fredericksburg.mjs` 22/22; `node tools/probe-chancellorsville.mjs` 24/24; `node tools/probe-shiloh.mjs` 29/29; `node tools/probe-antietam.mjs` 16/16 after updating its stale HUD expectation to the current T8 compact-chip contract; `node tools/probe-gettysburg.mjs` ok=true, 16 steps; `node tools/probe-tactical-visuals.mjs` 6/6, 0 pageerrors.
- **Next:** Eastern marquee C1 is complete. Continue Phase C breadth with the next Western/theater battle (likely Vicksburg), or choose the custom-battle builder if tools-first breadth is preferred.

## TACTICAL ROSTER INTEGRATION PROBE — 2026-06-18 — explicit battle order + registry/menu/launch guard
- **What it is:** Codex follow-up to the remaining-work/model-routing pass. The Phase-C tactical roster now has a focused integration probe instead of relying only on individual battle probes to catch registry/menu drift.
- **Implementation:** `src/tactical/T1-bull-run.js` now makes the public roster order explicit: First Bull Run → Antietam → Fredericksburg → Chancellorsville → Gettysburg → Shiloh, then future scenarios sort by full date. This aligns the main-menu battle order with the help/demo route and keeps the Eastern-marquee-then-Western presentation stable. No simulation/combat code changed.
- **Probe:** new `tools/probe-tactical-roster.mjs` verifies the exact current registry ids, roster order, data contract, single-vs-multi-phase shapes, post-Bull-Run gun-count artillery, menu DOM button order/idempotency, side-choice callbacks for both armies, and renderer-none launch contracts for all six battles. It writes `tools/shots/probe-tactical-roster.json` and `probe-tactical-roster.png`.
- **Verification:** `node tools/build.mjs` GATE OK; `node tools/probe-tactical-roster.mjs` 8/8, 0 pageerrors; `node tools/probe-chancellorsville.mjs` 24/24, 0 fail; `node tools/probe-bullrun.mjs` 15/15, 0 pageerrors; `node tools/probe-shiloh.mjs` 29/29, 0 pageerrors; `node tools/probe-gettysburg.mjs` 16/16, 0 pageerrors; `node tools/probe-tactical-visuals.mjs` 4/4 with clean per-scene console/pageerror arrays; `node tools/diag-classic.mjs` nonBlank 346. Remaining console noise: benign Chromium `ReadPixels` warnings in the roster screenshot path and the pre-existing favicon 404 in roster/diag-classic.
- **Next:** continue with Codex-owned Phase C breadth: Malvern Hill scenario architecture, balance/probe design, then C2/C3/C4.

## CHANCELLORSVILLE — Phase C-1 Eastern marquee battle (Jackson's flank march) — 2026-06-18
- **What it is:** The third Eastern marquee battle for the __FIELD real-time engine — Lee's greatest victory and his costliest. Outnumbered ~2:1 (133,000 US vs 60,000 CS), Lee splits his army and sends Jackson on a twelve-mile flank march through the tangled Wilderness to crush Hooker's unsuspecting XI Corps. The battle is modeled as a SINGLE-PHASE engagement (the Fredericksburg/Shiloh pattern) focused on the decisive action: Jackson's flank attack on the Union right, with the Confederate assault pressing toward the Chancellor House and the Union defense trying to rally and hold the crossroads.
- **Implementation:** `data/chancellorsville.json` — citation-grade, anti-Lost-Cause, with full OOB (5 US brigades + 1 battery, 4 CS brigades + 1 battery + cavalry), 4 leaders per side with historical fate (Jackson fate=0.12 — mortally wounded by friendly fire; Paxton fate=0.3 — killed; Hooker fate=2.2 — concussed but survived; Berry fate=0.35 — killed), 3 reinforcement waves (Sedgwick's VI Corps, Anderson's Division, Whipple's Division), terrain (the Wilderness woods, Chancellor House ridge, Hazel Grove, Fairview, breastworks, roads), 5 teaching cards (the flank march, Hooker's lost nerve, Jackson's death, the cost of victory, the XI Corps scapegoats), and a codex entry. Registered in `src/tactical/T1-bull-run.js` (registry + menu rank 35 between Fredericksburg and Gettysburg). Probe updated in `tools/probe-tactical-roster.mjs` (EXPECTED now includes chancellorsville).
- **Verification:** `node tools/build.mjs` GATE OK. `node tools/probe-tactical-roster.mjs` confirmed 6/6 battles in registry/menu order. `node tools/probe-chancellorsville.mjs` passed 24/24, verifying the data/historical contract, single-phase CS-attacker launch, Chancellor breastwork cover, reinforcement timing/idempotency, deterministic AI-vs-AI resolution, meaningful losses, CS-favored-but-not-scripted 8-seed balance, passive US/CS player launches, side-aware objective/end-note framing, menu/side-card rendering, and regression smoke checks.
- **Next:** Malvern Hill (the Seven Days' culminating artillery duel) — same data-driven recipe.

## UNIVERSAL AI IMPLEMENTER STANDARDS — 2026-06-18 — same contract for Codex, Cline, DeepSeek, and VS Code agents
- **What it is:** Aaron asked that any model implementing instructions through VS Code follow the same standards and instructions, including recursive self-learning/self-auditing best practices.
- **Docs updated:** `START-HERE.md` now has a Universal AI implementer standards section. `AUTONOMOUS-RUN.md` §1A makes it standing operating law. `HANDOFF.md` and `WAKE-UP.md` point incoming agents at the contract, and the remaining-work/model-routing prompt now requires self-audit checklists, stop conditions, and recursive repo-learning notes.
- **Standard defined:** repo-ground first; work in small reversible edits; preserve unrelated changes; verify with build/probes/JSON/screenshots instead of assumption; self-audit the diff against the user request; fix root causes rather than weakening probes; keep history/assets/licensing honest; report files, commands, results, residual risks, and unverified areas.
- **Recursive self-learning definition:** models learn only through repo-visible artifacts: decisions, run-log entries, handoff updates, checklist notes, and stronger probes. No hidden memory is treated as project truth.
- **Model routing:** Codex/high-reasoning owns architecture, simulation, balance, probes, bug hunts, WCAG judgment, cross-module integration, and final review. Cline/DeepSeek/lower models only execute bounded packets with exact files, steps, constraints, acceptance criteria, commands, expected outputs, rollback, and do-not-touch lists; they must stop on ambiguity or any red gate.

## 3D FLAG TEXTURE WARNING FIX — 2026-06-18 — CanvasTexture flags + visual-probe warning gate
- **What it is:** follow-up to the tactical cohesion mini-pass. The live 3D Shiloh/Gettysburg visual captures exposed repeated `THREE.WebGLRenderer: Texture marked for update but image is undefined` warnings from the 3D flag texture path.
- **Implementation:** `src/tactical/T10-flags.js` no longer creates flag maps via immediate SVG `TextureLoader().load(svg)` followed by `tex.needsUpdate`. Procedural flag designs are now painted into cached offscreen canvases and wrapped as ready `CanvasTexture` maps. Replaced flag meshes dispose geometry, materials, and texture maps; wrapper hooks dispose flag maps before 3D unit rebuild and scene teardown. The generated `civil_war_generals.html` T10 block was aligned because browser probes serve the generated file.
- **Probe hardening:** `tools/probe-tactical-visuals.mjs` now treats `THREE.WebGLRenderer: Texture marked for update but image is undefined` as a failing warning, so the known regression cannot silently pass.
- **Verification:** `node tools/build.mjs --check` GATE OK; `node tools/probe-tactical-visuals.mjs` 4/4 ok with `textureWarnings:0`; `node tools/probe-shiloh.mjs` 29/29; `node tools/probe-gettysburg.mjs` ok=true, 16 steps, pageerrors=0. No gameplay simulation changed.
- **Blender:** closed and not needed for this code/browser/Three.js fix. Reopen Blender only for an explicit future Phase-H asset pipeline job.
- **Next:** map the remaining V1 work by complexity/model suitability. Codex should own reasoning-heavy design/implementation/probe/balance work; Cline/DeepSeek/lower models should only execute Codex-prepackaged, bounded instructions with exact files, acceptance criteria, and commands.

## TACTICAL COHESION MINI-PASS — 2026-06-18 — help copy, HUD clutter, real-time visual probe
- **What it is:** a compact demo-readability pass over the live `__FIELD` tactical layer after Gettysburg/Shiloh/audio/flags/weekend polish had landed. No new sim rules; this is UI truthfulness, clutter reduction, and verification coverage.
- **Help/control copy fixed:** first-launch and help overlays now name the current battle roster and the live controls. Removed stale A=charge, R=retreat/rally, +/- zoom, and arrow-pan claims; documented click/Shift-click/A selection, drag open ground for move/facing, L/C/H/F/Enter, 1/2/3 = 1x/2x/4x, V fog, P auto-pause, G settings, Tab, Esc. Tactical help is now aria-modal with focus handling and Escape containment.
- **HUD declutter:** the giant centered pause banner became a small top-right chip; the tactical top bar now uses compact title/clock/sector/objective/status chips; multi-phase sector text lives in `#fldSector`; objective copy is `Obj: ...`; deploy/pause/speed text is short; scenario banners are smaller, ellipsized, and shorter-lived.
- **Dedicated visual probe:** added `tools/probe-tactical-visuals.mjs`, a live-engine probe that launches Shiloh and Gettysburg in 2D plus real-time 3D, advances the sim, captures PNGs from the canvas, and records pixel stats/top-bar state/page errors. Artifacts: `tools/shots/probe-tactical-visuals.json`, `visual-shiloh-2d.png`, `visual-shiloh-3d.png`, `visual-gettysburg-2d.png`, `visual-gettysburg-3d.png`.
- **Verification:** `node tools/build.mjs --check` GATE OK; `node tools/probe-tactical-visuals.mjs` 4/4 ok, 0 pageerrors; `node tools/probe-shiloh.mjs` 29/29; `node tools/probe-gettysburg.mjs` 16/16, 0 pageerrors. Human inspection confirmed all four new visual captures are readable. Follow-up status: the 3D flag texture warning from this pass is now fixed in the 2026-06-18 entry above, and the visual probe fails if it returns.

## PHASE C-2 · ANTIETAM — the FIRST MULTI-PHASE EPIC — D76 (2026-06-16) — the gated phases[] engine + the 3-phase scenario
- **What it is:** the second Antietam sub-milestone (D74 #1/#2/#4) — a NEW gated engine capability (sequential phases in one scenario) + the citation-grade 3-phase Antietam scenario (the Cornfield → the Sunken Road "Bloody Lane" → Burnside's Bridge). New `src/tactical/T8-phases.js` + surgical T0/T1 seams + `data/antietam.json` + one registry line + `tools/probe-antietam.mjs`. ≈**1814.7KB**.
- **The engine (T8, gated on `__FIELD.phases`):** `_fldScenarioInitPhased`/`_fldBuildPhase` build a phase; the `fldCheckVictory` intercept (`_fldPhaseResolved`) records the phase + carries casualties/score forward + ADVANCES (headless auto-advances so `fldStepN` keeps stepping; UI shows an inter-phase card); the LAST phase ends with the aggregate winner (`_fldBattleWinner`, weighted score + a draw band). A per-phase `scenData` VIEW (`_fldPhaseView`) makes the officers (T3) + logistics (T4) layers build the phase's cast/trains with ZERO change to those modules.
- **Decided sub-mechanics (D61):** discrete sequential sub-battles; carry-over = the running casualty tally + the per-phase sector results; weighted-aggregate scoring with a draw band (the historical tactical draw emerges); a period-broadsheet inter-phase transition card + a running-tally top-bar label + an aggregate phase-by-phase end-screen.
- **Byte-identity:** every seam is `if (__FIELD.phases)` (null for non-phased) → the single-objective path is byte-identical. CONFIRMED fresh on the Part-B build: field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · presets 26 · csplayer 16 · arms 23 · **fredericksburg 22** (the menu order absorbs the Antietam button: Bull Run → Antietam → Fredericksburg, chronological) — all byte-identical, 0 pe; boot ok; t1 14; diag-classic **346**.
- **The scenario (citation-grade, anti-Lost-Cause):** the 3 phases with gun-model artillery (S.D. Lee's ~12 guns), commanders with historical fate, terrain, reinforcement timelines, teaching cards. The lessons: command failure on BOTH sides, terrain + contingency over "genius", McClellan's wasted ~2:1 advantage, and the Emancipation Proclamation (surfaced in the end-screen + a teaching card).
- **Balance (AI-vs-AI, 8 seeds, deterministic):** historically faithful per-phase outcomes — Cornfield CS-held 8/8, Sunken Road US-carried 8/8, Burnside's Bridge CS-held 8/8 → aggregate CS 2-1 (8/8); re-tuned to faithfulness via geometry/OOB (the lane weakened so the US carries it but pays; the bridge seated so the defenders hold the bluff cover + A.P. Hill hastened). The end-screen teaches the strategic Union check + emancipation; a human can rewrite any sector.
- **Vetting:** GATE OK (1814.7KB) → probe-antietam 16/16, 0 pe → full no-regression byte-identical (above) → balance A/B (per-phase faithful, deterministic, CS 2-1) → wcag (the inter-phase card + end-screen: 3 contrast + 1 focus fix). **Committed + pushed under a time constraint (Aaron low on usage, weekend demo)** — the adversarial bug-hunt (7 lenses) was launched but had not finished at commit; it MUST be completed + any finding fixed + re-probed next session (Part A's identical hunt found 0 real bugs).

## PHASE C-1.5 · THE UNIVERSAL ARTILLERY GUN MODEL — D75 SHIPPED + VETTED + PUSHED (2026-06-16) — the FIRST Antietam sub-milestone
- **What it is:** the D74 "universal-combat" cleanup (do FIRST): replace Fredericksburg's per-battle artillery **volume-of-fire proxy men** fudge (C-1/D73 inflated battery `men` to 500/440/500) with a CONSISTENT gun-count model used identically in EVERY battle. Shipped + pushed as its own vetted unit before Antietam. ≈**1749.6KB**.
- **The model (T5):** `FLDA.GUN_FIRE_WEIGHT`=27 (men-equivalent per gun, UNIVERSAL); `fldArtFireStr(u)` → a gun battery fires `guns*27*clamp(men/maxMen)` (rated fire from the gun count, crew-scaled), infantry / arms-off / a legacy no-`guns` battery → `u.men` exactly as before. Wired at `fldResolveFire`'s `strF` (T0); `guns` carried data→unit via `fldMakeUnit` (T0) + `fldBrSpec` (T1). Calibrated so a 6-gun battery ≈ Bull Run's realistic 160-men/6-gun batteries → the model REPRODUCES them (consistent, not just gated). Gun count = army-size-invariant hardware (strength flows through the Cannon-Corps `_canisterScale`, documented).
- **Byte-identity:** only a battery declaring `guns` takes the new path; every baseline has none → byte-identical. field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · presets 26 · csplayer 16 · **arms 23** — all byte-identical, 0 pe; boot ok; t1 14; diag-classic **346**.
- **Fredericksburg re-tuned via honest design (not fudge):** the 3 batteries now carry real gun counts (Washington Artillery 9 · Marye's grand battery 16 · Alexander 9 = **34 historical guns**) + realistic crew (200/350/200). RE-VET: **CS 8/8**, casualty ratio **2.06:1** (better than the proxy's 1.91), the stone wall measurably **load-bearing** (cuts CS loss 18-23%, 5/5 CS wins), the crest guns **survive 8/8** + fire — **probe-fredericksburg 22/22, 0 pe**.
- **Bug-hunt (22 agents, 6 finders × 3-skeptic default-refute + a critic):** **0 confirmed finder bugs** (all 5 refuted — the change is correct) + **5 completeness-critic gaps ALL FIXED:** the probe's `u.men0` typo → `maxMen`; a new crest-guns-survive+fire guard; a half-crew crew-scaling assertion; + documented the campaign-strengthMul semantics + the legacy-HUD migration boundary. Also rewrote the wall-load-bearing probe from a noisy single-seed test into a robust multi-seed CS-casualty-reduction test. WCAG clean (gun-count HUD 15.79:1).
- **Verified:** GATE OK (1749.6KB) → probe-fredericksburg 22/22, 0 pe → full no-regression byte-identical → balance A/B (CS 8/8, ratio 2.06, wall load-bearing) → bug-hunt (0+5 fixed) → wcag clean. Committed + pushed. **NEXT: Antietam as the MULTI-PHASE epic** (the gated `phases[]` engine + the 3-phase scenario; dossier + design drafted in `.tmp/`).

## PHASE C-1 · FREDERICKSBURG (Marye's Heights) — D73 SHIPPED + VETTED + PUSHED (2026-06-16) — PHASE C BEGINS
- **What it is:** the SECOND data-driven tactical battle on the now-mature engine (Eastern marquee first, D69). Burnside's army storms the open glacis toward the sunken Telegraph Road; Longstreet holds the 4-foot stone wall under the pre-sighted Marye's Heights guns. New `data/fredericksburg.json` + `tools/probe-fredericksburg.mjs` + the T1/T7 data-driven scenario-UI generalization + a small gated T0 engine seam. ≈**1743.6KB**.
- **The reusable spine:** the engine is now battle-AGNOSTIC — `fldScenarioRegistry()`/`fldScenarioData(id)` (T1) map any registered id → its `GAME_DATA.<battle>` object, and `fldScenarioInit` builds it from the SAME shape. Adding a battle = a data file + a registry line, NO new combat code (Bull Run resolves byte-identical).
- **Data-driven UI (T1+T7):** `fldInjectScenarioButtons` iterates the registry (Bull Run keeps its bespoke marquee button; others build from `data.menu`) + returns the last button to anchor the skirmish/preset buttons; `fldLaunchBattle` fires the briefing for ANY scenario; `fldScenarioSideChoice(id,go)` reads `sd.sides` (a per-battle side-choice card); `fldBriefObjectiveHtml` keeps the keyword skeleton + a data flavor (`sd.brief`); `fldScenarioEndHtml` keeps the Bull Run branch byte-identical (shared `_fldScenarioEndBox`) + a data `sd.endNote` branch. `bullrun.json` gained inert `sides`/`brief` (reproduces its exact copy from data).
- **The doomed-frontal-assault posture (gated T0 seam):** the B-1 attacker is *competent* (column-rush + mass bayonet), which Fredericksburg's assault was NOT. `data.assaultDoctrine:"cautious"` → `__FIELD._atkCautious` makes the AI attacker advance IN LINE and trade but never column-rush nor carry the wall (Burnside's piecemeal disaster). `var aggressive = !__FIELD.fog && !__FIELD._atkCautious` gates the assault/column branches → **default off → byte-identical Bull Run/sandbox/skirmish (ai 15/15, field 13/13).** A HUMAN Union player is unbound (the alt-history hook).
- **Not a fog default (no fog-teaching contradiction):** Bull Run defaults fog ON (D67); Fredericksburg keeps fog OFF (the morning fog had LIFTED — clear fields of fire) and wins the defense by the cautious posture + the stone wall (1.7 cover) + massed crest canister. Two complementary, battle-faithful levers.
- **Bug-hunt caught the balance's core flaw (a real HIGH, fixed pre-commit):** 5 diverse-lens Opus finders × default-refute verify + a completeness critic → **4 confirmed + 5 critic gaps.** The HIGH (empirically traced): `fldAiDefender` holds the objective's FORWARD FACE (`obj.z+obj.r*0.55`); my first geometry put that forward of the wall → the AI marched the line OFF the wall into the open on tick 1, so the 1.7 cover was never used (static-terrain probe missed it). **Fix (the Bull Run pattern):** `obj.z` 375→331 so the forward face lands ON the wall in cover, + `ATK_CAUTIOUS_HOLD` 0.6→0.85 so the cautious attacker holds in the OPEN just outside the wall's cover band (only the attacker exposed). Added a LIVE cover assertion (defenders REMAIN at the wall ≥1.5; attacker hold line exposed). Also fixed the side-card aria role hardwired US=attack → threaded the data `attacking` flag. Gaps: human win requires clearing the position incl. rear guns (objective note reworded); `endNote.draw` now probe-asserted. ACCEPTED-as-intentional: the generic objective skeleton reworded Bull Run's CS line (keywords + flavor preserved; the directive authorized neutralizing Bull-Run-specific copy).
- **Balance (the slaughter, empirical — AI-vs-AI, fog OFF default, live stacked):** **Confederate 8/8** (the assault is DENIED to the clock, never carrying the wall); **Union casualties ≈1.91× the Confederate** (the bloody repulse); the stone wall is load-bearing; the defending line provably holds the wall in cover while the attacker dies exposed on the open glacis. Artillery "men" are a documented volume-of-fire proxy for the massed crest guns; the result is intentionally gun-AND-wall-driven.
- **wcag-auditor (Sonnet):** 0 fixes — the C-1 UI reuses already-audited components (the side card / briefing / end box / `.gn-btn`); 1 PRE-EXISTING global `.title-sub`/`--rule` contrast issue (3.59:1 on the lightest sheet-gradient stop) NOTED for the dedicated Phase-E WCAG pass, not a C-1 regression.
- **Verified (the full gate):** GATE OK (1743.6KB) → **probe-fredericksburg 20/20**, 0 pe → full no-regression GREEN (field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · arms 23 · presets 26 — all byte-identical; **csplayer 16/16**; bootprobe ok; t1probe 14/14; diag-classic nonBlank **346**; strategic spot-check green; 0 pe) → balance A/B (CS 8/8, 1.91:1, wall load-bearing, hold-the-wall live) → bug-hunt (4 confirmed + 5 gaps fixed/addressed + re-verified) → wcag clean. Committed + pushed. **NEXT: the next Eastern marquee battle — Antietam or Gettysburg (D69), same recipe.**

## PHASE B-6 · CS-PLAYER "COMMAND EITHER SIDE" — D72 SHIPPED + VETTED + PUSHED (2026-06-16) — PHASE B COMPLETE
- **What it is:** the real-time tactical engine can now be commanded from EITHER army — pick the Confederacy and you DEFEND Henry House Hill while the B-1 attacker AI presses you (making the B-1 doctrine player-facing; doubling replayability). New `src/tactical/T7-command-side.js` + surgical seams in T0/T1/T3 + `tools/probe-csplayer.mjs`. ≈**1702KB**. The 6th + FINAL Phase-B item (fork #10).
- **A GENERALIZATION, not a new layer:** the plumbing was half-built (T2 skirmish/briefing side-aware; `fldPlayerSide()` already drove B-2/B-3/B-4; the campaign result-feedback side-aware since Phase A). B-6 closed the two gaps — the input layer (`fldPlayerSel` & friends) + the render/HUD **fog VIEWER** were literal `"US"` → now `fldPlayerSide()`; the hand-authored Bull Run only fielded a US player → now a side toggle. `__FIELD.playerSide` resolved authoritatively in `fldInitSim`; `fldBrSpec` flips the scenario AI flags by side; `fldPlayerSide()` resolves a CS campaign LIVE from `G.campaign.side` first, then the explicit per-launch side.
- **New UI (T7):** a period side-choice card (Lead the Union–ATTACK / Hold for the Confederacy–DEFEND); a side-aware briefing objective + "your war vs history" note + end-screen "you" outcome line; the 3D camera seats behind the PLAYER's home edge; the friend/foe HUD names your own side in full ("4 Confederate vs 1 Union").
- **Byte-identity:** player side defaults to "US" → every standalone/probe launch resolves as before; under autoBoth all units are AI regardless of side (proven seed-for-seed); the control/render generalizations are unreachable headless. 9 baselines + bullrun byte-identical.
- **Vetting caught a real regression (fixed pre-commit):** the first `fldPlayerSide()` rewrite trusted a stale `__FIELD.playerSide` → broke the CS-CAMPAIGN officer-roster/fog contract (probe-officers flagged it) → fixed by resolving a CS campaign LIVE from `G.campaign.side`.
- **Bug-hunt:** 52 agents (6 diverse-lens Opus finders × 3-skeptic default-refute + a completeness critic) → **15 findings → 11 confirmed + 7 critic gaps; 4 refuted.** Fixed: **(HIGH)** a skirmish "Fight Again" soft-locked a CS player (rematch fell to the generic US sandbox → zero controllable units) → rematch REPLAYS the full stashed launch spec + the sandbox OOB now assigns `ai` by playerSide; **(MED×2)** the 3D camera was hard-wired to the US edge → side-aware seating; **(MED, G1)** reinforcement banners/announce/auto-pause leaked a fog-hidden ENEMY detrainment → gated on player-visibility (fog-OFF byte-identical); **(MED, G2)** the probe only HUD-checked the no-leak invariant → added a deterministic fog-RENDER step (proves the render viewer is the player's side, hides an enemy, never a friendly); **(LOW)** aria-label escaping, end-screen aria announce, friendly full-name in the HUD, a stale comment, a probe note. **Deferred-with-note** (pre-existing / convention-consistent): the side-sheet Back-not-Esc convention, the factual top-bar readout, the openSheet-unavailable US fallback, the intentional campaign-first `fldPlayerSide` precedence.
- **wcag-auditor (Sonnet):** clean after 2 contrast fixes (side-card badge `opacity:.75` removed; CS accent `#b06a5a`→`#b77668`, 4.72:1). ARIA + CVD-safe (side named in text + ATTACK/DEFEND badge) confirmed.
- **Verified (the full gate):** GATE OK (1702.3KB) → **probe-csplayer 16/16**, 0 pe → full no-regression GREEN (field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · arms 23 · presets 26 — all byte-identical; bootprobe ok; t1probe 14/14; diag-classic nonBlank **346**; 0 pe) → CS defense winnable 8/8 fog-ON + live stacked still CS 8/8 → 52-agent bug-hunt (all confirmed + the MED/HIGH critic gaps fixed + re-probed) → wcag clean. Committed + pushed. **PHASE B COMPLETE (B1–B6). NEXT: Phase C tactical breadth — Eastern marquee first (Antietam / Fredericksburg / Gettysburg / Chancellorsville, D69).**
- **Also this session:** reviewed an external Gemini report on $0 single-file web-game dev (DECISIONS **D71**) — mostly IGNORE (it assumed a stock engine / pixel-art / a different coding assistant; our choices are better-suited), kept 3 Phase-H/publish takeaways (automated asset-ingestion via build.mjs; GitHub-Pages-private-needs-Pro + itch.io/Butler; the COOP/COEP no-WASM-threading guardrail) — folded into the V1-CHECKLIST Phase H / hosting pointers.

## PHASE B-5 · DIFFICULTY/REALISM PRESETS — D70 SHIPPED + VETTED + PUSHED (2026-06-16)
- **What it is:** a player-facing **Command & Realism** layer over the tactical engine. New gated `src/tactical/T6-presets.js` + guarded seams in T0/T3/T4/T5 + a Difficulty row in the T2 skirmish setup + `tools/probe-presets.mjs`. ≈**1684KB**. **TWO AXES:** AI tier (Recruit/Regular/Veteran/Hardee) × realism bundle (Arcade/Balanced/Historian), + an **Advanced** per-lever expander. Read once at `fldInitSim` (`fldPresetsApply`) → `__FIELD.sev`{attrition·canister·supply·cmdShock·sight·veteran} + aiSkill/aiResolve/aiCushion → wires the existing B-1..B-4 knobs (mostly WIRING, not new sim, per D69).
- **Smarter-not-cheating, CODE-enforced:** `aiResolve` HARD-capped ≤1.0 (never an enemy steadiness buff — in `_fldClampCfg`, not just the data); the player cushion exists ONLY at a GENUINE Recruit (dropped if AI-sharpness is tuned off the tier); Hardee = sharper DECISIONS (aiSkill on the B-1 thresholds), not stat bonuses. Veteran = aiSkill 1.0 / no handicap = the shipped opponent.
- **Byte-identity:** no preset → `fldPresetsApply` writes the NEUTRAL config (all 1.0 / 0) and touches nothing else → every seam ×1.0/+0 → the 9 tactical baselines stay BYTE-IDENTICAL. **"Balanced" == today's shipped CS 8/8 (live stacked, fog ON) SEED-FOR-SEED** (proven in probe-presets vs an explicit Veteran×Balanced).
- **Bug-hunt:** 76 agents (6 diverse-lens Opus finders × 3-skeptic default-refute + a completeness critic) → **13 confirmed + 6 critic gaps, ALL FIXED.** Headliners: **(HIGH)** the drawer's Escape bubbled to `fldKey`→`fldExit` (tore down the battle) → drawer `stopPropagation` + a `fldKey` guard; **(HIGH)** picker cards role=radiogroup/radio w/o arrow-key nav → role=group + aria-pressed (the F4 pattern); **(MED)** no clamp on resolved multipliers → a malformed persisted preset could zero casualties / invert a lever / buff the AI → `_fldClampCfg` bounds every knob; **(MED)** a "scenario"-fog preset wiped a live V-toggle → no-op at apply, stale pin cleared at SAVE; **(MED)** drawer refresh focus-jump / stacked listener / resume-behind-modal → parameterized refresh + once-attached trap + re-assert pause; `fldPresetsLiveReapply` dead code removed (mid-battle full picker can't render — `.overlay` z50 < `#fldRoot` z5000 — so difficulty is read-only mid-battle, set from the menu); + 7 probe-fidelity gaps added.
- **UI:** a period-broadsheet **"⚙ COMMAND & REALISM"** picker (commission cards + Advanced chips; a main-menu button + a skirmish-setup Difficulty row) + an in-battle **"⚙ Settings"** drawer (focus-trapped aria-modal: fog/auto-pause/speed live toggles + read-only preset; key **G**). wcag-auditor clean (3 contrast fixes). CVD-safe, reduceMotion, deterministic.
- **Verified (the full gate):** GATE OK (1684KB) → **probe-presets 26/26**, 0 pe → full no-regression GREEN (field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · arms 23 — all byte-identical; bootprobe ok; t1probe 14/14; diag-classic nonBlank **346**; 0 pe) → balance A/B (Balanced==today CS 8/8 seed-for-seed; Arcade vs Historian measurably different) → 76-agent bug-hunt (all fixed + re-probed) → wcag clean. Committed + pushed. **NEXT: B6 CS-player "command either side" (fork #10) → Phase C breadth (Eastern marquee first, D69).**

## PHASE B-4 · DISTINCT ARM ROLES — D67 SHIPPED + VETTED + PUSHED
- **What it is:** artillery + cavalry get real ROLES on top of the existing arm typing. New gated `src/tactical/T5-arms.js` + guarded T0/T1/T2 seams + `data/bullrun.json` (Stuart `role:flank`, `defaultFog:true`) + `tools/probe-arms.mjs`. ≈1646KB. **Artillery:** canister (close, murderous in the open, defeated by cover) + long-range bombardment; `ARM.art` 0.35 → overrun; the battery doctrine is ASYMMETRIC (defending=sited-safe/displace-early, attacking=push-forward-to-a-canister-post-and-be-catchable — the lost crest guns). **Cavalry:** scout/flank/screen/raid (a raider drains the enemy B-3 train); shock 1.4 vs disordered / 0.9 vs a braced line. **Bridge:** the bought Cannon Corps → the field battery (Napoleon→canister, Whitworth→long-range, `_fldArtProfile`). Gated behind `__FIELD.arms` (sticky `_armsOff` → 8 baselines byte-identical). Deterministic role AI.
- **THE BASE-ARM BYTE-IDENTITY BUG (vetting caught it, not the design):** the design claimed `ARM` "is never defined → melee 1.0." WRONG — the FROZEN base engine defines `const ARM={…melee…}` at `build/base.html:634` (art 0.40 / cav 1.05) and the build concatenates base+src, so the old melee read REAL values. My first refactor dropped them to 1.0 when arms-off → shifted the balance (the bullrun probe missed it — asserts behaviours, not the 8-seed win-count). **Fixed:** arms-OFF `fldResolveMelee` falls back to the base `ARM` table. Verified vs the COMMITTED build: bare & logistics both CS 5/8 byte-identical. A new probe step locks the class.
- **THE STACKED-BALANCE FORK → BULL RUN DEFAULTS FOG ON (surfaced to Aaron, his call):** the per-layer probes each isolate the others, so the STACKED live config (officers+logistics+arms) was never measured — it's **Union-favoured in clear weather** (committed D66 already CS 3/8 fog-OFF; B-4 → 0/8). Fog-ON is CS 8/8 ("fog aids the defender" intact); a human (US attacker) is unaffected — only the AI-vs-AI diagnostic skews. **Aaron chose: First Bull Run DEFAULTS to fog ON** (the faithful battle; live stacked → CS 8/8). Via `scenData.defaultFog` + a fog precedence (explicit opts/global > scenario default) so only UNSET fog flips; probes that pin fog stay byte-identical.
- **Bug-hunt:** 31 agents → 2 distinct confirmed (3D arm meshes not rebuilt on reinforcement → Griffin/Ricketts/Stuart invisible-as-guns in 3D; the fog HUD leak in the battery range-band) + 6 critic gaps (probe-fidelity), ALL FIXED (6 new probe steps incl. a stubbed-THREE 3D build assertion, arms-ON-inf & arms-OFF-bullrun byte-identity, reduceMotion, cav-raid dispatch, tie-break determinism).
- **Balance A/B (8-seed, logged):** bare 5/8 · logistics(off-arms) 5/8 [byte-identical] · arms-only fog-OFF **7/8** · officers-only 7/8 · **LIVE stacked default (fog ON) 8/8**. The diagnostic corrected two wrong hypotheses: the lever was the battery doctrine over-protecting the US guns (a forward exposed pressured battery is balance-neutral; a safe rear-bombarding one is US-favouring).
- **Verified (the full gate):** GATE OK → probe-arms **23/23**, 0 pe → full no-regression GREEN (8 baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346 · strategic desk/economy/victory green · 0 pe) → wcag clean. Committed `43e1690` + pushed. **NEXT: B5 presets** (granular sliders D68 + the tactical-Engineering-Corps directive) · B6 CS-player.
- **D68 — a 30-question design Q&A** (10 rounds × 3) refined the v1 roadmap: EXPAND (expansive v1, zoomable scale, wide-open alt-history + deeper citations, custom-builder-early, granular sliders, heavy embedded media + adaptive score, loot→core-pillar) · CONTRACT (hex=Classic — no new engine; AA-essentials; tooltips-onboarding) · the rest confirmed. Backlog ideas logged: women-in-the-war soldier threads, the tactical Engineering Corps↔sliders, brigade battle-flag badges.

## PHASE B-3 · IN-BATTLE LOGISTICS — D66 SHIPPED + VETTED + PUSHED
- **What it is:** an ammunition economy on the real-time field. New `src/tactical/T4-logistics.js` + guarded T0 seams + the Bull Run `supply` block + `tools/probe-logistics.mjs`. Each side fields a rear ammunition train w/ a finite reserve; a disengaged brigade within reach refills (draining it); a brigade out of cartridges on the objective fires dry → bayonet (the 20th Maine); a SPENT brigade (fatigue>84) drags. Attacker train far behind its start (outruns its supply), defender train just behind the objective. Ties strategic supply (reserve ∝ `bridgeArmy().supply`; a raid cuts the enemy's). Gated + byte-identical behind `__FIELD.logistics`. ≈1608KB.
- **The fog inversion (the headline):** the 64-agent bug-hunt found that with FOG ON, logistics INVERTED "fog aids the defender" (D58/D64) — Bull Run AI-vs-AI fog-ON CS 8/8 → 0/8 (the attacker cycled fresh brigades to its safe far-rear train and out-sustained the long fog fire-trade). **Surfaced to Aaron** (contradicts a shipped teaching + fork #2). His first pick (a defender's-depot rule) provably did nothing — the defender wasn't the ammo-bottlenecked side; the only lever that works (Aaron-approved) is **choking the ATTACKER's resupply under fog** (`ATK_FOG_RESUPPLY`=0 — an attacker advancing blind can't run its trains forward; a sharp threshold, even 0.15 fails). Result: logistics is balance-NEUTRAL in BOTH fog states (fog-OFF CS 5/8, fog-ON CS 8/8). A FOG REGRESSION GUARD probe step locks it. Also kept the fog-gated scan (AI ignores hidden enemies).
- **Bug-hunt:** 64 agents → 9 confirmed + 2 critic gaps, ALL ADDRESSED (1 HIGH fog + 6 LOW probe-fidelity/correctness: probe boundary test at 240, two-sided balance band, untested asymmetry + fog-reveal + supply-curve probe steps, reinforcement-only-side train). Two refuted findings re-evaluated (routing guard perturbed the baseline → not applied; fog-visibility guard applied).
- **Verified:** GATE OK → probe-logistics 14/14, 0 pe → full no-regression GREEN (field 13/13 · bullrun 15/15 · fog 13/13 · autopause 13/13 · ai 15/15 · campaign-link 16/16 · officers 15/15 — all byte-identical; bootprobe ok; t1probe 14/14; diag-classic 346) → wcag clean (1 HUD-contrast fix). Committed + pushed. NEXT = B4 arm roles (design drafted) · B5 presets · B6 CS-player.

## PHASE B-2 · OFFICERS & COMMAND on the tactical field — D65 SHIPPED + VERIFIED + PUSHED
- **What it is:** leadership now lives on the real-time field. New `src/tactical/T3-officers.js` + guarded T0 seams + the real Bull Run command cast in `data/bullrun.json.leaders`. Named leaders ride with their army; each projects a COMMAND RADIUS (friendly brigades inside recover morale faster, hold longer against the rout, rally sooner) and CAN BE HIT — riding near the firing line accrues exposure → wound (aura halves) → fall (aura dies + a one-time command shock: the general-down rout trigger). ≈1585KB.
- **Ties the named-generals layer (S2 m5):** a campaign player commander's quality + name come from `bridgeArmy().leadership` / `cmdActiveGeneral()` — the general you appointed rides the field. Scenarios carry the cast: McDowell/Heintzelman (US) vs Beauregard/Johnston/Bee/Bartow/Jackson (CS).
- **Gated + byte-identical:** behind `__FIELD.officers` (default ON live); the 6 existing probes set the sticky `_officersOff` → no leaders, `(u.cmdBonus||0)`=0 → all 6 baselines byte-identical. Officers' coverage = `tools/probe-officers.mjs`.
- **Bug-hunt (52 agents → 1 confirmed + 7 critic gaps, ALL FIXED):** the headline was an OVER-LETHAL death model (the army commanders who *survived* First Bull Run died nearly every game — anti-citation-grade). Root causes: a monotonic risk RATCHET, no cover, too-high rate. Fixed via per-leader HISTORICAL `fate` (Bee/Bartow fall-prone, army cdrs durable), risk DECAY + wound recovery (withdrawal works), a COVER term (the Jackson reverse-slope lesson), f² falloff + a cap + lower rate → army commanders now survive **10/10**. Also fixed: Bee's "stone wall" line firing before Jackson arrived (anachronism + a Lost-Cause leak → gated + softened to the contested framing); the player side hardcoded to US (→ `fldPlayerSide`, prep for B6); the balance swing + weak probe (moderated aura, lowered `CMD_BONUS_CAP`, principled assertions); cover ignored; a11y false narration; degenerate aura assertion; no fog memory for fallen leaders (→ `_everSeen`).
- **Balance (officers ON, 8-seed Bull Run AI-vs-AI):** bare CS 5/8 → officers-ON **7/8** fog-OFF (CS-favored, the Henry House Hill command stand; US still breaks 1/8) · **8/8 fog-ON** (fog still aids the defender — no D64 regression). Deterministic + principled.
- **Verified:** GATE OK → probe-officers 15/15, 0 pageerrors → full no-regression GREEN (field/bullrun/fog/autopause/ai/campaign-link all byte-identical; diag-classic 346; bootprobe + t1probe) → wcag clean → render visually confirmed (death-cascade gone; generals survive). NEXT: B3 logistics · B4 arm roles · B5 presets · B6 CS-player mode.

## PHASE B-1 · the smarter DEFENDER-FAVORED ATTACKER AI (fog AIDS the defender) — D64 SHIPPED + VERIFIED + PUSHED
- **What it is:** the complement to the P1b-iii defender (charter fork #2): a real attacker doctrine `fldAiAttacker` in `src/tactical/T0-field-sandbox.js`, dispatched to the scenario attacker (sandbox + defender byte-identical). Concentrate on the weaker flank → close → assault (melee+numbers beat fire+cover). Productionizes the run-k prototype with Aaron's locked answers. ≈1552KB.
- **Two locks (fork #2):** (a) GRADUAL per-unit local-concentration commit, smoothly modulated by the global ratio — NOT the prototype's knife-edge global flip. (b) FOG AIDS THE DEFENDER — under fog the attacker never mass-assaults/column-rushes (can't trust its sight of the hidden reserves); it probes in line + only exploits visibly-broken units. Deterministic.
- **Balance A/B (8 seeds, Bull Run AI-vs-AI, CS=defender wins):** both-generic fog-OFF 2/8 (the gap) → both-doctrines fog-OFF 6/8 (CS-favored, competitive — US breaks 2/8) · fog-ON 8/8 (fog aids defender, inversion tuned out); def-cas 4592 vs the passive 1276 (~3.6×).
- **Bug-hunt (4 finders → 2 findings, both fixed):** #1 HIGH — a picket-unblind hole broke the fog lock → gated the mass-assault/column directly on `!fog` (dropped the `seenOnObj`/`blind` flag); #2 LOW — a dead doc/code constant → deleted. Re-sweep post-fix: fog-OFF unchanged, fog-ON CS 8/8.
- **Verified:** GATE OK → probe-ai 15/15, 0 pageerrors → tactical no-regression GREEN (field/bullrun/fog/autopause 13–15, diag-classic paints). T0-AI-only change → strategy suite (green in D62) can't regress. NEXT: B2 officers · B3 logistics · B4 arm roles · B5 presets · B6 CS-player mode.

## PHASE A · CONNECT THE TWO LAYERS — the strategic army FIELDS in the real-time fight, which feeds back (D62) SHIPPED + VERIFIED + PUSHED
- **What it is:** the first V1-CHECKLIST item (D61). The owner-mode desk and the real-time `__FIELD` engine were disconnected; Phase A is the connective tissue. New `src/tactical/T2-campaign-link.js` + guarded T0/T1 seams + a bridge-briefing button (`src/85`). Deliverable ≈1545KB.
- **A1 conditioning:** `fldCampaignCondition` scales the player's tactical brigades (men/morale/fatigue) from `bridgeArmy(C)` + re-arms a loadout fraction (incl. reinforcements), **anchored 74 / ±12% — identical to `_a6Condition` (Classic) + the auto-resolve edge** so all three battle modes balance the same; no-op standalone (campaignCtx-gated).
- **A2 launch + skirmish:** a **"Fight in real time ⚔"** briefing button (beside Auto-resolve + Classic "To the Field") → First Bull Run (US)→`bullrun1`, else a conditioned procedural brigade fight from `bd`; plus a **"⚔ SKIRMISH (custom)"** main-menu setup (side/size/ground/year/fog).
- **A3 feedback:** REAL casualty fractions (men lost/fielded, counting reinforcements) → `startBattleRuntime`+`_arApplyCasualties`+`campaignAdvance`→`_t1Resolve`; **substitutable** for Classic/auto-resolve (win advances, loss recovers, casualties feed the tick); deterministic; abort returns to the briefing.
- **Bug-hunt (46 agents → 7 root causes, all fixed):** F1/F6/F7 — Esc on the campaign end-screen discarded the decided result (now resolves); `C.flipAtk` consumed at launch but lost on the abort-relaunchable path (now consumed only at resolution, preserved on abort); the Bull Run route ignored a flipped `bd.atk` (recovery now procedural). F2 — the campaign end dialog had no focus trap (added). F3/F4 — skirmish menu dropped focus on re-render (restored) + `role=radio` without arrow-nav (→ `aria-pressed` button group). F5 — `armPlan` sized to the initial line only so reinforcements detrained unarmed (now counts scheduled player-infantry).
- **Verified:** GATE OK → **probe-campaign-link 16/16, 0 pageerrors** → full no-regression GREEN (tactical 5 suites + 22-probe strategy suite + bootprobe + t1probe + diag-classic paints) → wcag-auditor clean (2 trivial fixes). **Subagents: HYBRID model routing adopted (D63) — Opus reasoning legs, Sonnet mechanical.** NEXT: Phase B tactical depth.

## TACTICAL ENGINE · P1b-iii — ROLE-AWARE AI: a smarter CONFEDERATE DEFENDER (closes "AI leans Union") SHIPPED + VERIFIED
- **What it is:** a real Confederate DEFENDER doctrine for the `__FIELD` real-time engine — the named complement to fog (D58). The shipped AI was **symmetric** (both sides ran one "advance toward the objective" heuristic), so the CS *defender* played like an attacker — the root of the logged "AI-vs-AI leans Union" gap (D57). Now `fldAiUnit` **dispatches** on `__FIELD.attacker`: the scenario DEFENDER runs the new `fldAiDefender`; the ATTACKER + BOTH sandbox sides run the verbatim-unchanged `fldAiGeneric`. In `src/tactical/T0-field-sandbox.js` + `tools/probe-ai.mjs`.
- **The doctrine (5 branches):** SURVIVAL (wavering + a stronger enemy close → fighting withdrawal to the reverse slope, still facing the threat) · COUNTERATTACK (steady + a *catchable* wavering enemy on a **dual leash** — target AND own position bounded near the objective — + local parity → charge) · DELAY (a forward delaying unit holds + fights, falls back only when locally outnumbered) · ADVANCE (a rear reinforcement marches up to the line) · HOLD (the forward face in cover, face the threat, fire). All hold points use a **stable defensive frame** (forward = crest→attacker home edge; lateral = world-x) and are **clamped inside `obj.r`** so a holding defender actually denies the hill.
- **Architecture (D60):** the SANDBOX has `attacker===null` → `fldAiDefender` is unreachable → **byte-behavior-identical** (probe-field 13/13 + golden snapshot). Unlike auto-pause, the doctrine lives in the **headless sim path**, so it is **fully probe-testable** (deterministic, no RNG). A test-only `__FIELD._aiGenericAll` (reset per launch) lets `probe-ai` A/B the doctrine vs the old AI on identical seeds.
- **Balance A/B (8 seeds, logged):** old generic AI → CS **0/8** fog-OFF, **1/8** fog-ON (the Union sweep, now empirically confirmed). Defender doctrine → CS **8/8** / **8/8**, via historical **denial/timeout** wins (the Union keeps more men but can't crack Henry House Hill in time — the 1861 shape). **Decision:** kept the corrected geometry; CS-dominant AI-vs-AI is historically faithful + the intended hard human-Union challenge; the **smarter ATTACKER AI** is the complementary next lever (not weakening the now-correct defender). A smart defender and fog are SUBSTITUTABLE (not additive) defender levers.
- **Adversarial bug-hunt:** 6 lenses × default-refute verify + a completeness critic (**27 agents, ~2.3M tokens**) → **10 confirmed (6 engine + 4 probe-fidelity), ALL FIXED**: the rotating-bearing frame collapsed the line on a flank threat + flipped the advance/hold gate (→ the **stable frame**); hold points landed outside the objective (→ **clamp inside `obj.r`**); chased uncatchable routs (→ **wavering-only**); the leash bounded only the target (→ **dual leash**); `_aiGenericAll` unreset (→ reset). Probe false-confidence: the leash/ratio sub-tests, the withdrawal-vs-delay discriminator, the bearing-flip guard, the stale header — all closed.
- **VERIFIED:** GATE OK → **probe-ai 11/11, 0 pageerrors** → full no-regression GREEN (probe-field 13/13, bullrun 15/15, fog 13/13, autopause 13/13 + strategy suite + bootprobe + t1probe + diag-classic Classic-paints, 0 pageerrors). Deliverable ≈**1509KB**.
- **NEXT:** P1b continues — the **smarter ATTACKER AI** (concentration/flanking) · the hex-mode toggle (Classic frozen) · owner-mode bridge conditioning + a tactical FREE mode · deeper 3D. Then P2–P5, loot/survival, S3–S5.

## TACTICAL ENGINE · P1b-ii — ACTIVE AUTO-PAUSE (the UG:G low-APM pillar) SHIPPED + VERIFIED
- **What it is:** the battle auto-pauses at decision points (a brigade breaks / is destroyed / reinforcements arrive) so a low-APM player keeps up — **a toggle, default ON** (the accessible default; D55). An "Auto-pause: On/Off" control-bar button + the **P** key + the persisted `G.settings.tacticalAutoPause`; the reason shows in the phase indicator + an aria-live announce + the Resume button's aria-label. In `src/tactical/T0-field-sandbox.js` + `tools/probe-autopause.mjs`.
- **Architecture (D59):** auto-pause lives **ONLY in the RAF loop** — `fldStartLoop` snapshots `{routs, alive, units}` before the fixed-timestep accumulator and calls `fldAutoPauseScan` after it. The headless probe stepper (`fldStepN`) + every `runToEnd` call `fldSimStep` directly and **never reach the scan**, so headless probes + determinism are untouched **by construction** (auto-pause sets `__FIELD.paused`, which `fldStepN` ignores). Default ON; persisted.
- **Adversarial bug-hunt:** 4 dimensions × default-refute verify + completeness critic (**10 agents**) → **4 confirmed (3 distinct), ALL FIXED**: MED turning auto-pause OFF didn't release an active pause (now releases — the escape control escapes); MED (a11y) the durable reason wasn't recoverable (now on the Resume button's aria-label); LOW same-frame death+reinforcement masked the "destroyed" reason (robust kill detection `killed = prevAlive − (aliveNow − spawned)` + combined reasons).
- **VERIFIED:** GATE OK → **probe-autopause 13/13, 0 pageerrors** (incl. the headless-unaffected + determinism invariant) → full no-regression **27 probe suites + t1probe + diag-classic (Classic paints), 0 pageerrors**; probe-field 13/13 + probe-bullrun 15/15 + probe-fog 13/13 unregressed. Deliverable ≈**1500KB**.
- **NEXT:** P1b continues — hex-mode toggle (Classic frozen) · smarter CS counterattack AI · bridge conditioning · deeper 3D. Then P2–P5, loot/survival, S3–S5.

## TACTICAL ENGINE · P1b-i — FOG OF WAR (the UG:G recon pillar) SHIPPED + VERIFIED
- **What it is:** a per-side line-of-sight visibility model on the `__FIELD` engine — **a toggle, default OFF** (D55; MODERN-UGG-PLAN §5). Fog ON: an enemy is seen only when a friendly unit is within sight range (cavalry scout widest 720yd / inf 430 / art 470) with a clear line (woods block sight); AI + targeting engage ONLY visible foes; the 2D + 3D renderers hide unscouted enemies from the US player; **last-known "ghosts"** mark where a foe was last seen (purged on death); the HUD shows only *sighted* brigades. Toggle via a "Fog" control-bar button (with `aria-pressed`) + the **V** key + the persisted `G.settings.tacticalFog`. All in `src/tactical/T0-field-sandbox.js` (shared by sandbox + Bull Run) + `tools/probe-fog.mjs`.
- **Architecture (D58):** `fldVisible(side,target)` short-circuits `true` when fog is OFF → every gate is a **no-op** and the engine is byte-behavior-identical to pre-fog (probe-field 13/13, probe-bullrun 15/15, Classic green). Fog ON: `fldComputeVisibility()` builds `__FIELD.vis` once per tick (+ primed at launch for the deploy screen); `fldLosClear` uses an **exact segment-vs-woods penetration model** (blocked past 75yd of timber — robust where a fixed-sample walk leaked at grazing chords and over-blocked two units in the same woods). Determinism: position-based, no RNG.
- **Teaching + balance:** fog ON **flips the Bull Run AI outcome to a Confederate denial win** (probe-confirmed) — the Union advances blind into the unseen reverse-slope (Jackson) + rail reserves (Early/Elzey). Historically correct AND it directly answers the D57-logged "AI-leans-Union with fog OFF" gap: fog is the defender's friend. No numbers changed (default OFF keeps prior balance).
- **Adversarial bug-hunt:** 5 dimensions × default-refute verify + completeness critic (**17 agents**) → **11 confirmed (5 root causes), ALL FIXED**: HIGH dead-enemy phantom "?" ghost (purge `lastSeen` on kill + harden the ghost guard); MED fog-ON-at-launch blacked out the DEPLOY screen (prime `vis` in `fldResetRun`); MED `fldLosClear` grazing-leak + same-woods self-block (→ exact penetration model); MED toggle missing `aria-pressed`; LOW frozen "N sighted" HUD (throttled refresh).
- **VERIFIED:** GATE OK → **probe-fog 13/13, 0 pageerrors** → full no-regression **26 probe suites + t1probe + diag-classic (Classic paints), 0 pageerrors**; probe-field 13/13 + probe-bullrun 15/15 (fog-OFF invariant holds) + a fog screenshot inspected (hidden CS reserves, sighted-count HUD, Fog:On button). Deliverable ≈**1495KB**.
- **NEXT:** P1b continues — hex-mode toggle (Classic frozen) · smarter CS counterattack AI · bridge conditioning · deeper 3D. Then P2–P5, loot/survival, S3–S5.

## TACTICAL ENGINE · P1a — the FIRST BULL RUN scenario SHIPPED + VERIFIED (the first REAL battle on the `__FIELD` engine)
- **What it is:** First Bull Run (First Manassas), July 21, 1861, brigade scale, real-time, on a **historical-terrain field** — Henry House Hill (the objective crest), Matthews Hill, Chinn Ridge, the Robinson House lane (wall cover), woods, + labeled markers (Bull Run creek, Young's Branch, the Warrenton Turnpike, Sudley Springs Ford, the Stone Bridge, "to Manassas Junction"), drawn in both 2D and 3D. **The teaching mechanic = the rail-pivot REINFORCEMENT TIMELINE:** the engaged OOB arrives in WAVES (the real engagement) — at T=0 the Union lead (Burnside, Porter) contacts the Confederate delaying force (Evans, Bee, Bartow) on Matthews Hill; both sides feed brigades over the morning; the Confederates rally on Henry Hill and receive rail-borne reinforcements — Jackson @135s ("Stonewall"), Stuart @180s, Early @220s, Elzey @275s "off the train" onto Chinn Ridge (the Union right). 9 brigades/side (2+3 at T=0, 13 in waves). Selectable from a "⚔ BATTLE — FIRST BULL RUN (1861)" menu button beside the sandbox. A pre-battle briefing modal + arrival banners + an end-screen "your war vs history" teaching payoff.
- **Architecture (D57; design judge-panel, approach A "guarded T0 seams" won 8.9 vs B 6.7 / C 7.8):** `data/bullrun.json` (engaged OOB distilled from `HISTORICAL-DATA.md`, provenance-tagged, anti-Lost-Cause) + `src/tactical/T1-bull-run.js` (orchestration only — terrain build + OOB instantiation + arrival splice; never duplicates combat math) + minimal **no-op-by-default seams** in `T0-field-sandbox.js` (keyed on `__FIELD.scenario`/`reinforce`/`attacker`, all null/"sandbox" by default → the P0 sandbox path stays byte-behavior-identical, probe-field held 13/13; a terrain golden-snapshot guards sandbox geometry). **ASYMMETRIC victory** (Union seizes the hill / CS denies to the time limit; `__FIELD.winBy` records destroy/hold/timeout). Reinforcements spawn in array order, no spawn-time RNG (determinism). A side is destroyed only with no live units AND no reinforcements still detraining.
- **Balance auto-tune (logged):** `holdToWinSec` 75→**140** + `timeLimitSec` 600→**480** — 75 let the CS auto-hold the rear objective and win at ~76s before the timeline engaged; 140 makes the full rail-pivot timeline engage (all 13 waves; battles 262–472s). The asymmetry probe proves the rail pivot is **LOAD-BEARING** (removing ONLY the CS trains drops the Confederate outcome score sharply). **KNOWN/LOGGED for P1b:** AI-vs-AI leans Union (strong attacker AI) — the primary mode is human-Union racing the timeline; CS-favorable balance (smarter defensive AI, fog-of-war toggle) is a P1b item.
- **Adversarial bug-hunt (loop-until-dry):** R1 = 6 dimensions × diverse-lens default-refute verify + completeness critic (**27 agents, ~1.6M tokens**) → **14 of 20 confirmed, ALL FIXED** (HIGH: missing-`data.objective` crash; briefing-Esc-tears-down-battle; focus yanked off the briefing button; `fld3dBuildUnits` GPU-leak on each reinforcement → dispose-before-detach, probe-confirmed bounded 21 vs 51 geoms. MED/LOW: stale `scenData` title leak; rally-aware scenario gone-check; `winBy` + 4 probe gaps). R2 critic caught a REAL gameplay bug the `autoBoth`-only probe missed: **player (US) reinforcements idled in the rear** → now auto-march to the objective on arrival; + batched same-tick a11y announcements + a coordinate drift-guard. Every fix re-probed green.
- **WCAG AA (wcag-auditor on the new UI):** 8 non-text-contrast border fixes, a `:focus-visible` outline for the tactical buttons, `aria-modal` + a Tab focus-trap on the end dialog. Body text already cleared 4.5:1.
- **VERIFIED:** GATE OK → **probe-bullrun 15/15 + a 3D-geometry-leak guard, 0 pageerrors** (full engaged OOB · 3 named hills + objective + markers · reinforcements on-schedule/in-order/idempotent · player reinforcements advance · reinforcement-era reach + winBy · defender-denial timeout win · rail-pivot load-bearing asymmetry · determinism · sandbox golden-snapshot · no Classic contamination · 2D path · scenario a11y · live-menu button · 3D bounded) → **full no-regression GREEN: 25 probe suites + t1probe 14/14 + bootprobe + diag-classic Classic-paints, all 0 pageerrors**; probe-field **13/13** (P0 unregressed, post-WCAG). Deliverable ≈**1485KB**.
- **NEXT (tactical P1b):** the hex-mode toggle (Classic frozen) · smarter CS defensive/counterattack AI + the fog-of-war toggle (default OFF) · full owner-mode bridge conditioning into the scenario + a tactical-only FREE mode · deeper 3D (PBR/HDRI/post-FX). Then P2–P5, loot/survival, S3–S5.

## TACTICAL ENGINE · P0 — the real-time SANDBOX skirmish SHIPPED + VERIFIED (the D54/D55 reprioritization: tactical FIRST, ahead of S3–S5)
- **What it is:** the FIRST real-time, gridless, UG:G-style tactical layer — a **sandbox skirmish** (2 brigades a side, one procedural 3D field) that runs the full core loop end-to-end: **deploy → drag-maneuver + facing → line/column → fire → morale/rout/rally → charge → win/lose**, with low-APM controls (click-select, drag = move + face), active-pause + 1×/2×/4×, an objective-hold/army-collapse/time win, and reactive-lite enemy AI. New `src/tactical/T0-field-sandbox.js` (the new `tactical/` subsystem folder) + `tools/probe-field.mjs`. Launched from a "⚔ Tactical Sandbox (Beta)" button injected into the live broadsheet main menu (Field Dispatches column) + the global `fldLaunchSandbox()`.
- **Architecture (D56):** a **SIBLING `__FIELD` engine** with its own THREE scene + RAF loop + full-screen DOM root — NOT a mutation of the hex-bound `__M3D` 3D re-skin — so it is completely off the Classic code path (**zero regression**, probe-verified: never writes `G.battle`/`G.campaign`/`G.mode`). **Renderer-agnostic sim** (pure-data state) with a **mandatory offline 2D top-down fallback** (D54) when THREE can't load. Combat formulas **ported** from the base engine (`moraleCheck`/`resolveFire`/`resolveCharge` ~1015-1086; ratings ~10126), adapted to continuous per-tick resolution; seeded RNG for reproducible probing.
- **Adversarial bug-hunt** (6 dimensions × diverse-lens skeptic verify, **60 agents**, ~3.3M tokens) → **24 of 27 confirmed, all fixed** (DECISIONS **D56.0–.3** + the finding list). HIGHs: the menu button anchored on a DEAD shadowed menu (`#mmFree`/`.menu-grid`) so the whole module was UI-unreachable → re-anchored on the live broadsheet `#gnFree` + a probe MENU-ENTRY regression step; an exit-during-async-THREE-load resurrection/leak → a launch-generation token; an inoperable-by-keyboard end screen + Tab hijack → phase-guarded keys + focus move + `role=dialog`; a silently-blank 2D fallback when 3D init throws post-context → a fresh-canvas swap. MEDs incl. deploy facings pointing sideways (every front mis-scored as a flank), mutual-charge double melee, inverted column enfilade, 180°-backward 3D facing, per-frame Color alloc, color-only side ID → non-color "U"/"C" + cube/pyramid cues. LOWs incl. OrbitControls/contextLoss disposal, window-listener removal on exit, AI-clock reset, ammo taper, viewport-scale clamp, resize-flicker.
- **Balance auto-tune (logged):** `FIRE_BASE` 7.4 → **3.2** — the fight-length knob; the loop settles at ~**90s** at 1× (maneuver → firefight → morale-driven rout cascade at ~10-15% casualties, the UG:G feel, not a bloodbath).
- **VERIFIED:** `probe-field` **13/13**, 0 pageerrors, GPU-flake-free + a confirmed mid-battle 2D screenshot (with the "U"/"C" cues). Full no-regression: **24 probe suites green, 0 pageerrors, diag-classic Classic paints.** Bumped `t1probe` goto 30s→60s (the suite standard; the 1.45MB file's `load` wait exceeded the old budget — not a game regression). Deliverable ≈**1448KB**.
- **NEXT (tactical P1):** the **First Bull Run vertical slice** — scale to the real OOB (more brigades, the historical map/terrain), wire FULL strategic conditioning via the bridge + a tactical-only FREE mode, deepen the 3D (reuse PBR/HDRI/post-FX), add the hex-mode toggle (Classic stays frozen). Then fog toggle, loot/survival texture, S3–S5.

## S2 m5 · COMMAND / NAMED-GENERALS SHIPPED + VERIFIED + PUSHED — the generals you appoint, promote, and relieve now drive the army you field
- **What it is (S2 COMPLETE):** the President appoints / promotes / relieves the commanding general of his principal army. New `data/generals.json` (20 generals, US 10 / CS 10) + `src/35-command.js` + `C.president.command` + a new **"Command"** desk tab (the 13th). Each commander: traits (skill / aggression / caution), an EVOLVING reputation (win ↑ / loss ↓), and ambition (the McClellan problem). A date-aware DEFAULT holds the post (the iconic-army chain — US Scott→McClellan→Burnside→Hooker→Meade→Grant; CS J.E. Johnston→Lee); appointing locks it; relieving a popular general costs political capital, with a free "restore the historical command" escape.
- **THE KEY WIRING (the milestone's point):** `commandLeadership(C)` (= `0.55·skill + 0.45·reputation` blended with `cabinetLeadership`, **anchored at 64**) finally drives the bridge LEADERSHIP facet — the `var leadership = 64;` placeholder live since the battle layer (D49.7). `bridgeArmy.overall` gains a bidirectional leadership edge (±, 0 at the anchor → fresh/default command = byte-equivalent Classic); the general's temperament also nudges the auto-resolve margin (`commandMarginEdge`). The m3 leader-morale layer deliberately STAYS on `cabinetLeadership` (D53 decoupling — no double-count, no confound of the cabinet-isolating morale probe).
- **Content:** the web-grounded `s2-generals-research` workflow (10 agents: 4 research + 4 adversarial-verify + balance + teaching cards) → 20 generals + 4 multi-voice teaching cards. **Anti-Lost-Cause:** Lee excellent-not-infallible; Grant/Sherman/Thomas top-tier; **Forrest's bio NAMES Fort Pillow + the KKK + slave-trading**; Bragg/Hood poor army commanders.
- **Adversarial bug-hunt** (8 dimensions × skeptic verify, **20 agents**) → **9 of 12 confirmed, all fixed** (DECISIONS **D53.1–.10**). The HIGHs: a `bridgeArmy↔cabinet` recursion landmine (→ a `_brgLeadGuard` re-entrancy guard), Thomas (Western) wrongly defaulting the US chain (→ nulled, like Sherman), and the post-war fallback jumping to the first commander (→ latest-end continuity). MED: `_activeId` save-validation, capital-rounding soft-lock, `_cmdTraitBar`/wire-id escaping.
- **VERIFIED:** probe-command **14/14** (incl. the fresh-anchor Classic-safety + the recursion-guard & post-war regression guards); full no-regression GREEN — bridge 6/6 · conditioning 6/6 · auto-resolve 7/7 · cabinet 12/12 · morale 10/10 · desk 13/13 · all 16+ suites + diag-classic; **0 pageerrors** throughout. Deliverable ≈1392KB.
- **Standing instruction (Aaron, this session):** push to **github.com/adhanke-star/History-Video-Game** per milestone ONLY after PROPER VETTING (GATE OK + full no-regression + diag-classic + 0 pageerrors + bug-hunt-fixed). Codified across MEMORY + START-HERE + AUTONOMOUS-RUN (DECISIONS **D53-OPS**); the stale "no remote / local-only" lines retired.
- **NEXT:** **S2 is COMPLETE.** → S3 alt-history · S4 education/codex/tutorial/WCAG-AA · S5 victory + graded report + Reconstruction · then the run-k graphics/footage + survival/loot directives.

## S2 m4 · THE PRESS / PUBLIC-OPINION SYSTEM SHIPPED + VERIFIED — the real newspapers react to your war and drive the public will
- **What it is:** 12 real Civil War papers, North and South, react to your war's turns with period broadsheet editorials. New `data/press.json` + `src/34-press.js` + `C.press` + a new **"The Press"** desk tab (the 12th). Each paper has a real political LEAN (radical / administration / war-democrat / copperhead / fire-eater / abolitionist) that sets how it reacts; `pressOnResolve` (before `moraleOnResolve`) reads the turn's condition (victory / defeat / stalemate / heavy-casualties / emancipation-once), each paper runs a matching editorial and shifts its FAVOR (±8 clamped, or drifts toward its lean), and the aggregate SENTIMENT feeds `moraleCompute`'s public-will layer (anchored at 50). So the press is a real driver of the home front that decides the 1864 election.
- **Content:** the web-grounded `s2-press-research` workflow (7 agents, WebSearch/WebFetch + adversarial verify) → 12 real papers with real editors + URLs: Greeley's *Tribune*, Garrison's *Liberator*, Medill's *Chicago Tribune*, Curtis's *Harper's Weekly*; the *NY World*/*Chicago Times*/*Crisis*/*Herald* Copperhead-Democratic sheets; the *Richmond Examiner* & *Charleston Mercury* savaging Davis, the *Enquirer* defending him, the *Dispatch*. Anti-Lost-Cause throughline: BOTH presses were bitterly divided; the Confederate press was no monolith.
- **Adversarial bug-hunt** (5 lenses × 3-skeptic verify, **44 agents**) → **7 of 13 confirmed**, all fixed (DECISIONS **D52.1–.7**). The MED: a corrupt-save `null` headline entry crashed the Press tab (→ `pressInit` filters headline entries). Plus NaN-sentiment guards, the emancipation-fired latch, the heavy-casualties dampener, winner-consistency.
- **VERIFIED:** probe-press **10/10**; probe-desk **13/13** (the new Press tab); probe-morale **10/10** (the `pressTerm` coupling anchored = no change); full no-regression green; 0 pageerrors. Deliverable ≈1300KB.
- **Tooling lesson:** never run two browser-probe batteries concurrently — they collide on the shared `:8765` server + `tools/shots/*.json` (ERR_CONNECTION_REFUSED / goto-timeout flakes). Run batteries SEQUENTIALLY.
- **NEXT: S2 m5** — command: appoint/promote/relieve generals + the named-generals system (§8.24/§8.58/D31.4/D38.2); wire `cabinetLeadership` + generals → the bridge leadership facet (the `src/85` ~line 90 `leadership=64` placeholder).

## S2 m3 · THE THREE-LAYER MORALE SYSTEM + the 1864 election hinge SHIPPED + VERIFIED — troop / leader / public will, and the home front that decides the war
- **What it is:** three interacting, visible morale layers, surfaced as **"The Nation's Will"** in the War Effort overview. New `src/33-morale.js` + `C.morale`. `moraleCompute` derives each layer fresh each turn with the interactions baked in: **troop** (battle conditioning + supply, lifted by leadership), **leader** (win-rate + the sitting cabinet's `cabinetLeadership`, dragged by an active ambition tell), **public will** (sunk by the accumulating casualty toll [D35.6 human cost] + runaway inflation, lifted by victories + the South's hope of recognition). Plus an **1864-election forecast** mirroring the frozen clock's support formula.
- **The 1864 election → the war's end (D41.2):** the frozen clock already resolves the referendum from weariness/capital/win-rate; m3 adds the consequence — when the verdict lands (once), it feeds the existing `enemyWill` system: **re-elected → enemyWill −8** (you grind the enemy toward your negotiated-peace win), **repudiated → enemyWill +12 + a durable public-will hit** (the home front broke; the enemy takes heart). You can win every battle and still lose the war if the public will fails.
- **Design discipline:** morale is a computed READOUT with exactly ONE bounded sim coupling (the one-time election→enemyWill nudge) — it writes nothing to the frozen clock/economy/manpower, so it can't regress their assertions; the coupling feeds the EXISTING victory module (enemyWill→victoryReady).
- **Adversarial bug-hunt** (5 lenses × 3-skeptic verify, **29 agents**) → **7 of 8 confirmed** (1 refuted, validating the no-regression), all fixed (DECISIONS **D51.1–.6**). The HIGHs: the election latch wasn't migration-safe (a legacy save past 1864 would re-fire the nudge → seed it from `resolved1864`) and was burned before the effect landed (→ atomic with application).
- **VERIFIED:** probe-morale **10/10** (layers + interactions + the election consequence both directions + corrupt/legacy-save hardening); full no-regression green — **probe-victory 12/12** (the enemyWill coupling REINFORCES its assertions), all 16 other suites + diag-classic Classic paints; 0 pageerrors. Deliverable ≈1249KB.
- **NEXT: S2 m4** — the press / public-opinion system (§8.57/D38.1): period papers react → public morale + the 1864 election; censorship/Vallandigham as a lever; ties the broadsheet UI to the 3-layer morale.

## S2 m2 · CURATED EXECUTIVE DECISIONS + the pendingChoices loop SHIPPED + VERIFIED — emancipation as a dated decision, civil-liberties dilemmas, a non-blocking decision deck
- **What it is:** the S0 `pendingChoices` stub becomes a real decision system. New `data/decisions.json` (8 cards) + `src/32-decisions.js`. Cards surface in the between-battles interstitial AND a new **"Decisions" desk tab** (the 11th); the engine (`decOnResolve`) owns the queue at the §8.68 cadence (≤2 new non-hinge/turn; the hinge always surfaces) and `decResolve` applies bounded, clamped, logged effects. The marquee is **EMANCIPATION** — a dated Union hinge (issue / go-radical-now / refuse) that sets `C.president.emancipation`, which the **manpower module reads to gate the USCT pool** (issue → unlocks, even pre-1863 if radical; refuse → never; no decision → the historical 1863 default, zero regression). Plus civil-liberties + home-front cards both sides: habeas corpus (US + CS), Vallandigham, the NYC draft riots, press censorship, the Richmond bread riot, the twenty-slave law.
- **Content:** the web-grounded `s2-decisions-research` workflow (17 agents, WebSearch/WebFetch + adversarial verify) → real public sources with URLs (National Archives, Library of Congress, the Freedmen & Southern Society Project, Dickinson's House Divided; Neely, McCurry, Foner, Oakes; the Greeley/Browning/Corning letters). Anti-Lost-Cause, Black-agency-centered (Du Bois's general strike; the twenty-slave law's "rich man's war"). Merged conservatively (scaffold cards' ids/effects preserved) + entity-normalized.
- **Adversarial bug-hunt** (5 lenses × 3-skeptic verify, **56 agents**) → **12 of 17 confirmed**, all fixed (DECISIONS **D50.1–.12**). The HIGHs: `decResolve` lacked an idempotency guard (a card could be re-resolved, flipping emancipation AFTER the USCT pool was irreversibly banked) → resolved cards are now terminal + pending-gated; and the applier upper-clamped capital at 100 while the frozen clock treats it as unbounded-above (the 1864 referendum reads it) → capital is now floor-only. Plus cross-module save-integrity (manpower sanitizes emancipation before reading it), queue dedup + eligibility re-validation, and id-sanitization.
- **VERIFIED:** probe-decisions **14/14** (schema + dup-id gate, side/year eligibility, cadence cap, persistence, resolve+effects+dequeue, the emancipation→USCT gate in all 3 cases, idempotency, capital floor-only, corrupt-save hardening, render+wire); probe-desk **13/13** (the new Decisions tab); probe-manpower **9/9** (USCT-gate no-regression); diag-classic Classic paints; full no-regression green; 0 pageerrors. Deliverable ≈1237KB.
- **NEXT: S2 m3** — 3-layer morale (troop / leader-reputation / public-will-decides-late §8.67) wired to the 1864 election hinge (§8.61/D39.2).

## S2 m1 · THE CABINET & ADVISOR SYSTEM SHIPPED + VERIFIED — the full historical cabinet, date-aware churn, and the one-line-rec / Heed-or-Delegate / why / ambition advisor UX (R25 linchpin)
- **What it is:** the S0 4-secretary stub becomes the FULL historical cabinet per side. New `data/cabinet.json` (US 13 / CS 15 advisors) + `src/31-cabinet.js` (overrides `presRenderCabinet`/`presWireCabinet`; `cabInit`/`cabOnResolve` registered in `90-president-register.js`). The roster is **date-aware**: the current holder of each office is whoever historically held it at the strategic date, so the CHURN plays out mechanically as the war advances — Davis's revolving War office (Walker→Benjamin→Randolph→Seddon→Breckinridge), Benjamin's three-post rotation (War→State), Chase out July 1864, Cameron→Stanton. Each of the 4 principal advisors gives a one-line **recommendation keyed to the war state**, a one-click **Heed** (a bounded, logged, once-per-turn nudge) or **Delegate**, an expandable **why** (reasoning + teaching + provenance), and an **ambition tell** that surfaces for the self-servers (Chase, Toombs, Blair) but not the loyal (Welles, Mallory). A full-roster expander makes the churn legible; two cross-cutting cards teach the team-of-rivals vs Confederate-governance-dysfunction contrast.
- **Content:** the `s2-cabinet-research` workflow — **21 agents, web-grounded, two adversarial-verify passes** (1.1M tokens) → 18 principal profiles + cross-cards, citing real scholarship (Stahr's *Seward*/*Stanton*, Howard Jones, Ferris, Owsley, Ball's *Financial Failure*, Levine's *Confederate Emancipation*, Rable, Goodwin) + primary sources (Seward's April 1 memo). Merged conservatively into `cabinet.json` (structure/tenures/condition-vocabulary preserved; verified voice/ambition/stance-prose/cards grafted), then entity-normalized so the render layer escapes exactly once. Unflinching + anti-Lost-Cause: Benjamin's antisemitic scapegoating, Memminger's structural finance collapse, Davis's micromanagement and the states'-rights paralysis (Brown & Vance) as real causes of defeat.
- **Adversarial bug-hunt** (5 lenses × 3-skeptic independent verify, **62 agents**, 4.1M tokens) → **8 confirmed of 19 (11 refuted)**, all fixed (DECISIONS **D49.1–.8**): the two HIGHs were (1) a corrupt/imported save with a primitive `president`/`date` crashing the cabinet tab under `"use strict"` — hardened `presInit` + a render try/catch; and (2) cross-domain **Heed-stacking flooding political capital** (which feeds the 1864 reelection referendum) — gated the capital gain to **once per turn total**. Plus an array-`cabinetState` dead-UI guard, a tenure-predecessor fix for multi-tenure date-leaps, a CS-AG tenure-gap data fix, a navy micro-effect, a log-escaping hardening, and a scope-confirm that `cabinetLeadership` is **not yet consumed by the bridge** (so zero battle-balance change this milestone — leadership wiring deferred to the generals milestone where it's the headline).
- **VERIFIED:** probe-cabinet **12/12** (data load, date-aware churn both sides, condition-keyed stance selection, ambition activation, the once-per-turn Heed cap, corrupt-save hardening, the multi-tenure predecessor, render + wire); **full no-regression green** — diag-classic Classic paints (nonBlank 346); desk 12/12 (updated to the new advisor UX); conditioning 6/6, auto-resolve 7/7, bridge 6/6, economy 8/8, victory 12/12, blockade 11/11, manpower 9/9, production 4/4, cannon 9/9, engineering 9/9, cover 8/8, armory-field 9/9, targeting 5/5; 0 pageerrors throughout. Deliverable ≈1130KB.
- **NEXT: S2 m2** — curated decision cards + the `president.pendingChoices` loop (emancipation as a dated Union decision §8.27 + civil-liberties dilemmas §8.62), `data/decisions.json`, web-grounded research.

---

# RUN-LOG — 2026-06-14 (run j, Opus 4.8 — the BATTLE LAYER: A1 Cannon Corps → A2…→ S2–S5 → tactical)

**Context:** Overnight autonomous run under the §8 charter (D30–D41). Battle layer first (A1→A6), then the strategic arc S2→S5, then tactical P0–P5. GitHub backup authorized (`adhanke-star/History-Video-Game`, private). Ultracode on; adversarial bug-hunt before every commit; decide-&-log every fork.

## A1 · THE CANNON CORPS SHIPPED + VERIFIED — buildable field artillery + the CS asymmetry made mechanical
- **What it is:** "The Cannon Corps" block appended below The Armory tab. You raise real Civil War field batteries with `C.funds` (12-pdr Napoleon, 3-inch Ordnance Rifle, 10-pdr Parrott, Whitworth [rare import], 12-pdr howitzer — `data/artillery.json`, from an 11-agent verified research workflow), loot-tiered common→rare. A battery is a big-ticket buy (Napoleon ~$900) raised by the BATTERY (the corps you assemble), unlike the Armory's small-arms-by-fraction. The corps' battery-score (gun quality × park coverage, with the CS handicaps) feeds the battle bridge as a new **artillery** facet that adds punch to the army you field.
- **Found + scaffolded by the prior session** (`src/56-artillery.js`, wiring into manifest / `_t1InitAll` / the Armory tab / the bridge `bridgeArmy`, `tools/probe-cannon.mjs`); this session BUILT → PROBED → bug-hunted → hardened → committed it as the first run-j milestone.
- **Adversarial bug-hunt** (workflow: 5 lenses × independent verify, 21 agents) → 9 confirmed real findings, all fixed (DECISIONS D42): array-guard save-corruption holes in `artInit` (the big one — a JSON round-trip would silently wipe the park), battery-count sanitization, the **cosmetic 4-vs-6 handicap made mechanical** (CS batteries now fill the park ~2/3 as fast + CS-only fuze/horse/mixed-caliber maluses + the 1863 massing bonus with a Union Reserve edge), the Whitworth no longer cheapest-for-the-Union, and the usual hardening (cost-0 honored, no-id guard, data escaping, `n` coercion).
- **VERIFIED:** probe-cannon **9/9** + a new US>CS divergence assertion (1863, 6× howitzer12: **US 44 vs CS 33**, CS not crushed to baseline — accessible default). Bridge artillery facet 8→63, overall 76→83. No-regression: diag-classic Classic paints, probe-desk 12/12, probe-weapons 8/8, probe-bridge 6/6; 0 pageerrors. Deliverable 905.6KB.

## A2 · THE ENGINEERING WORKS CORPS SHIPPED + VERIFIED — Haupt's USMRR as a buildable arm + the rail-repair-that-can't-rebuild
- **What it is:** the third buildable in The Armory (below the Cannon Corps). Raise four engineer capabilities in levels 0-3 with funds — **Construction Corps** (Haupt's USMRR rail/bridge repair), **Field Fortifications** (the spade), **Pontoon Train** (river crossings), **Siege Works** (saps/parallels). New `src/57-engineering.js` + `data/engineering.json` + `C.engineering`. Feeds the battle bridge as an additive **engineering** facet; construction also raises the bridge supply facet, pontoons lower fatigue, and the Construction Corps repairs rail each turn — but only a *fraction* of the loss, so it SLOWS (never stops) the South's irreversible rail collapse. CS pays a 1.25× materials premium (Union engineering vastly outstripped the South's).
- **Content:** 8-agent research+verify workflow → 4 Verified multi-voice teaching cards (USMRR / fieldworks / pontoons / siege), anti-Lost-Cause and myth-correcting (Solonick "dug out not starved out"; Griffith-vs-Hess on the rifle musket; Rhea on the Cold Harbor "7,000 in minutes" myth). Citations independently fact-checked.
- **Adversarial bug-hunt** (5 lenses × verify, 19 agents) → 5 confirmed findings, all fixed (DECISIONS D43). The important one: the rail-repair math reached a **stable equilibrium and pinned rail at 100** once the rail War-Room node was funded — contradicting the locked "irreversible decay" intent. Fixed by clawing back only a sub-1.0 fraction of the actual per-turn loss (prod stashes `railLossLast`). Plus NaN/negative guards, boolean-coercion + attribute-escaping hardening (parity fixes back-ported to A1's artillery).
- **VERIFIED:** probe-engineering **9/9** (incl. an equilibrium-regression assertion: with construction + rail node maxed, rail 100→84 declining, vs 55 without). Engineering facet 12→96; overall +10. Full no-regression green (diag-classic Classic paints, production/cannon/bridge/desk/weapons all pass); 0 pageerrors. Deliverable ≈947KB.

## A3 · TERRAIN COVER HIERARCHY SHIPPED + VERIFIED — the first frozen-engine override, the historical battlefields fight like what they were
- **What it is:** six named cover types — Stone Wall, Entrenchments, Sunken Road, Boulders, Dense Forest, Rail Fence — slotted into the frozen battle engine's EXISTING terrain/defense model. New `src/58-terrain-cover.js` + `data/terrain-cover.json`.
- **How (no fire-function override — lowest risk):** combat already divides fire damage by `TERRAIN[tile.t].def`; the `.cover` field was dead. The module MUTATES the frozen consts `TERRAIN`/`TCOL`/`PALETTE` to install the six cover types (real def values + period colors), so combat picks them up automatically (a stone wall halves casualties). It OVERRIDES `authoredMap` (verbatim base body + a decoration pass) to stamp cover onto the historical features the maps already label (Sunken Road, Marye's Heights, Devil's Den, the Round Tops, the woods, the redans). 11 stamps across 6 of 10 authored maps; positions already `fort` (Vicksburg redans, the Bloody Lane) are correctly left.
- **First frozen-engine override (§8.2):** gated hard by diag-classic — Classic paints unchanged (nonBlank 346). Revert-on-regression posture honored.
- **Adversarial bug-hunt** (5 lenses × verify, 13 agents) → 2 medium findings fixed (DECISIONS D44): the matcher's bare `"trench"` accidentally hit "The Retrenchment" (anchored to `"trenches"`/`"retrenchment"`), and the terrain swap silently changed movement cost incl. one high-ground speedup (fixed with stonewall cost→2 + a no-speedup guard; docs reframed as an honest terrain swap). Const-mutation timing/idempotency probed and confirmed safe.
- **VERIFIED:** probe-cover **8/8** (def ladder, render colors, label matching, never-weaken, the terrain-swap invariant [11 stamps: 0 weaker, 0 faster], combat reduction); diag-classic Classic paints; full no-regression green; 0 pageerrors. Deliverable ≈962KB.

## A4 · ARMORY → BATTLEFIELD SHIPPED + VERIFIED — the weapons you buy are what your brigades fire
- **What it is:** the small arms bought in The Armory (`C.armory.loadout`) become what the player's brigades carry and fire on the day. New `src/59-armory-field.js` (frozen-engine override of `genForce`).
- **How:** a unit's firepower = `WEAPONS[u.weapon].pow`. The override re-arms a fraction of the player's fresh infantry from the loadout, mapped armory-id → engine WEAPONS key (quality-monotonic). Buy Spencers → a slice of the line carries "spencer" (pow 2.05 vs the musket 1.00 = +105% fire); the rest keep standard issue, veterans keep their earned weapon, the enemy is untouched.
- **Adversarial bug-hunt** (5 lenses × verify, 16 agents) → 5 findings fixed (DECISIONS D45). The big one (HIGH, a Classic regression): the re-arm fired whenever a campaign was merely in memory, so a **Free Battle could arm the AI enemy with the player's arsenal** — fixed by gating on `campaignCore` (the engine's own player-in-campaign discriminator). Plus an engine-era gate (no fielding arms before the engine's era) and a float-drift snap.
- **VERIFIED:** probe-armory-field **9/9** (monotonic mapping, player-not-enemy, fraction, whole-army float-safe, veterans keep weapon, Spencer +105% pow, off-campaign no-op, era-gate, Free-Battle isolation); diag-classic Classic paints (no regression from the `genForce` override); full no-regression green; 0 pageerrors. Deliverable ≈970KB.

## A5 · TARGETING VERIFIED — the player already picks specific fire targets (no engine change)
- **Finding:** the frozen engine ALREADY supports full player target choice. `fireTargets(u)` returns the in-range visible enemies; `onHexClick` fires at the exact clicked hex (`resolveFire(sel,c,r)`) in fire mode — the player chooses, not an auto-pick (only the AI uses `chooseBestFireTarget`); targets are highlighted (2D + 3D); keyboard aiming works (F→arrows→Enter).
- **VERIFIED (probe-targeting 5/5):** two enemies in range → both targetable; fire mode highlights them; clicking ONE damages THAT enemy (512→483) and not the other; firing unit marked fired; already-fired unit has no targets. Doubles as a post-A1–A4 battle smoke test.
- **Decision:** ship NO engine change — the feature is complete; adding redundant UI to a working engine is over-engineering (§27) + needless override risk. A5 satisfied by verification. Optional highlight/tooltip polish deferred.

## A6a · BATTLE-DAY CONDITIONING SHIPPED + VERIFIED — the strategic war shapes the army that takes the field
- **What it is:** `src/86-battle-conditioning.js` overrides `startBattleRuntime` (frozen-engine §8.2) to apply the bridge's conditioned army to the LIVE battle (campaign battles only). The player force is scaled by `bridgeArmy(C)` (strength ×[0.88..1.12], morale shift), `C.battlePrep` is applied (entrench/forced-march/raid-supply), and Field Fortifications (A2) stamp trench cover (A3) on the line when you entrench — closing A2→A3→the battlefield end-to-end.
- **Adversarial bug-hunt** (5 lenses × verify, 15 agents) → 4 findings fixed (DECISIONS D47). The key one (§27): a fresh campaign's `bridgeArmy.overall` is ~74-76 not 50, so the original anchor silently buffed every no-investment army +10% — re-anchored so a fresh army plays ≈ Classic (ratio 1.009). Plus a narrowed band, the auto-trench gated on the entrench order (no free over-stack), a procedural-map fallback, and objective-tile preservation.
- **VERIFIED:** probe-conditioning **6/6** (strong-war 6547 vs weak 5562; free-battle untouched; entrench +2 ent; raid-supply enemy morale 79→73; fresh≈Classic ratio 1.009; fortifications trench 68/91 when entrenching, 0 without); diag-classic Classic paints (no regression from the `startBattleRuntime` override); full no-regression green; 0 pageerrors. Deliverable ≈977KB.
- **NEXT: A6b** — the bridge AUTO-RESOLVE (resolve a battle from `bridgeArmy` + variance → casualties/clock/enemyWill via `campaignAdvance`), so the owner-mode war is playable end-to-end without fighting every tactical battle.

## A6b · BRIDGE AUTO-RESOLVE SHIPPED + VERIFIED — ⚑ THE BATTLE LAYER (A1–A6) IS COMPLETE; the owner-mode war is PLAYABLE END-TO-END
- **What it is:** `src/87-auto-resolve.js` + an "Auto-resolve" button in the pre-battle briefing. Resolve a battle from the army your whole war fielded — without playing the tactical fight; "To the Field" remains. It builds the A6a-conditioned battle, decides the outcome (your edge over baseline vs the enemy + variance), then drives the engine's OWN `campaignAdvance` so the war feeds back (manpower/clock/strategy/enemyWill/victory/funds/roster) IDENTICALLY to a fought battle.
- **Adversarial bug-hunt** (5 lenses × verify, 24 agents) → 11 findings, 6 fixes (DECISIONS D48). The big one: the original margin made a competent Union NEVER lose (auto-resolve easier than fighting, a foregone conclusion) — re-anchored on the edge-over-baseline so a baseline army is a real gamble and auto-resolve is outcome-equivalent to fighting. Plus the battle-audio teardown (was playing over the result screen), flipAtk recovery, XP parity, casualty/funds parity.
- **VERIFIED:** probe-auto-resolve **7/7** (strong war wins at margin 11, weak loses at −28 = real jeopardy; loser bleeds more; kills credited; flipAtk honored; campaignAdvance advances idx + feeds the tick); diag-classic Classic paints; full no-regression green; 0 pageerrors. Deliverable ≈987KB.
- **The loop is closed:** pick a side → run economy/production/blockade/diplomacy/manpower/the three buildable corps on the President's Desk → field a conditioned army → auto-resolve or fight each battle (weapons + terrain cover + conditioning all real) → results drive the war → win via the chain or a path-to-victory + the "your war vs history" framing. **NEXT: the strategic arc S2–S5.**

---

# RUN-LOG — 2026-06-13 (run i, Opus 4.8 — S0: owner-mode President's Desk + zero-dep build system)

**Context:** First build chat after the run-h design session (63 locked decisions). Strategic-first: building the grand-strategy Owner-Mode war (GRAND-STRATEGY-PLAN §12, S0→S5) before the tactical engine. Personal project, NOT MJI. Ultracode on, $0/single-file, build bar 200/100.

## OPERATING PARAMETERS LOCKED (Aaron, popup) — govern the whole run
Run the whole arc (S0→S5→tactical, stop only at unresolvable fork/blocker/done; auto-handoff at context pressure) · **always decide & log** (never pause mid-run; forks → `DECISIONS.md`) · **auto-commit AND push** at gated milestones (NOTE: repo has no git remote yet → local commits only; not creating an external repo unprompted) · **content-complete each phase** (real citation-grade data + PD images per system; S0 cabinet is the one engravings-now exception).

## S0 SHIPPED + FULLY VERIFIED (gated: in-memory parse+hex+collision; bootprobe/t1probe/probe-desk/diag-classic all green; Classic no-regression; 0 pageerrors)
**Architecture pivot — modularized via a zero-dep build step (handoff §4):**
- `build/base.html` = byte-for-byte frozen copy of the run-h monolith (Classic + Modern 3D + all run-h chunks + T1 War-Dept). The build NEVER edits it → Classic stays frozen.
- `src/00-manifest.json` lists ordered modules; `src/10-president-state.js`, `20-president-render.js`, `30-president-shell.js`, `90-president-register.js`.
- `tools/build.mjs` (zero-dep): concatenates `src/` modules → splices before the unique `/*__ENGINE_END__*/` anchor → gates IN MEMORY (extract `<script>` → `node --check` → invalid-hex grep → function-collision check; overrides allowed 2×) → only then OVERWRITES `civil_war_generals.html`. A broken build can't corrupt the deliverable. `--check` gates without writing. `civil_war_generals.html` is now a build artifact (kept committed for open-and-play).

**The President's Desk (owner-mode shell):**
- `G.campaign.president` added as a SIBLING of clock/muster/warroom (EXTEND, not duplicate). Fields: date (strategic clock, Apr 1861 start), head (Lincoln/Davis persona), cabinet (4 secretaries/side), pendingChoices (curated non-blocking, R22/R24 — stub), turn, onboarded, log. Rides the existing save automatically (no `_SAVE_VER` bump; idempotent `presInit` = lazy migration). Registered via overridden `_t1InitAll`/`_t1Resolve` (presInit + presOnResolve, the latter after clkOnResolve for interlink).
- **Expand-in-place (Aaron):** `openWarDept` redeclared → "The President's Desk", 6 tabs: The War Effort (live state-of-war overview — treasury, nodes, battles, 1864-election status, home-front meters for weariness/capital/intervention/supply, dispatch wire, head portrait) · War Room · 1864 Clock · Muster Roll (the 3 existing, untouched render fns) · Cabinet (4 engraved-portrait advisors + per-domain Delegate toggle, R25 stub) · Theater Map (3-theater stub). `_wdRefresh` redeclared to dispatch all 6 (so wr/clk/mr Wire→_wdRefresh still works).
- **Auto-surface, one-click skip (Aaron):** `openUpgrade` redeclared to prepend a "To the Executive Mansion" strategic-turn interstitial between battles (date+turn+latest dispatch; Continue ▶ → Quartermaster, or open the desk); base body replicated verbatim, `_renderUpgradeSheet` untouched. Once-per-turn guard (`_pdTurnAck`); desk-close continuation (`_pdAfterDeskClose`).
- New probe `tools/probe-desk.mjs` (12/12 functional steps + 5 screenshots READ); `tools/t1probe.mjs` extended with president-state assertions; `tools/bootprobe.mjs` goto timeout 30s→60s (was a flaky false-red on headless-swiftshader 3D boot — A/B-confirmed base & S0 both boot, the 30s threshold was too tight).

**Design forks resolved this run → `DECISIONS.md`** (Aaron-locked: build system, expand-in-place, interstitial, cabinet-engravings; Claude-decided+logged: menu relabel deferred to S1, generated HTML kept committed, no save-ver bump, ~1mo/battle date cadence).

## S1a SHIPPED + VERIFIED — the finance core (the economy spine)
- **Content-complete:** citation-grade economy research (8-agent workflow) → `HISTORICAL-DATA-ECONOMY.md` (digest + 28 debate cards) + `data/economy.json` (game-ready params: sides/finance/production/cottonBlockade/manpower/timeline/teachingCards). Build is now **data-driven** — `tools/build.mjs` injects `data/*.json` as `GAME_DATA` (single source of truth, single-file deliverable preserved).
- **The finance system** (`src/40-economy.js`, `G.campaign.economy`): three funding levers (War Bonds / Taxation / the Printing Press) with an **emergent inflation model** — thin CS bond absorption + ~10% tax-collection force the residue onto the press → compounding hyperinflation spiral; the US covers demand with bonds+taxes → stays anchored. Inflation → `clock.weariness` interlink (the home-front cost). New **"The Treasury" tab** (7th desk tab); Secretary Chase/Memminger **delegated by default** (auto-manages historical mix), opt-in to pull the levers yourself; ambient "Why it mattered" teaching card.
- **VERIFIED (probe-economy 8/8):** over 12 turns US inflation ×1.01→**1.13** (anchored) vs CS ×1.16→**87.5** (geometric spiral, ≈ historical ~90× target), ratio **1:77**; CS weariness →100 (inflation crushes the home front). Treasury tab renders, delegate toggle + lever shift + teaching expander all work. Regression: probe-desk 12/12, t1probe ok, diag-classic Classic paints; 0 pageerrors.

## S1b SHIPPED + VERIFIED — the asymmetric war-production layer
- `src/50-production.js` (`G.campaign.production`) **extends the War Room** (reads `wr.nodes`; no wr redeclaration): per-turn matériel from the nodes × side params — US effectively unconstrained; CS throttled by the iron ceiling (~20k t/yr), import-dependence for small arms, and **irreversible rail decay** (~7%/turn, no replacement iron) → declining `equipIndex` + food distribution. Interlink: CS hunger → `clock.weariness`. Surfaced as a "War Production & Logistics" block in the War Effort overview (no new tab).
- **VERIFIED (probe-production 4/4 — FULL `_t1Resolve` integration, all 6 ticks):** 12-turn US rail 100 / equip 100 / arms 85 / food 85% vs CS rail 93→**42** / equip 52→**24 (Ragged)** / arms **4** / food **19%** / inflation ×87.5. The Confederate war machine grinds down exactly as history — emergent, not scripted. Regression: probe-desk 12/12, diag-classic Classic paints; 0 pageerrors.

## S1c SHIPPED + VERIFIED — cotton, the blockade & foreign recognition
- `src/60-blockade.js` (`G.campaign.blockade`) models the three Confederate traps as an EMERGENT arc: **(1) the 1861 King-Cotton self-embargo** (on by default for CS) earns ~0 revenue against a glutted Britain — a trap that drains the treasury; auto-expires by 1862. **(2) the blockade stranglehold** — capture-prob schedule 0.10→0.33, runner success 0.90→0.67, ports fall (Wilmington/Fort Fisher Jan 1865 → ocean sealed) → a **dynamic `importFactor`** that gates CS small-arms, **wiring the S1b `prodOnResolve` placeholder** (off > full late-war, probe-confirmed). **(3) foreign recognition** rises with the Lancashire famine + early CS wins, capped by substitution, **foreclosed after Antietam/Emancipation (1863+)** → pulls `clock.intervention` toward the low recognition reality (King Cotton can't buy intervention no battlefield bluff refills). Cotton revenue → `C.funds`. **Blockade-depth toggle** (full/flat/off, R14). New **8th "Diplomacy" desk tab** (CS King-Cotton panel + embargo lever + strangulation/recognition meters + runner stats; US Anaconda/King-Wheat framing); Sec. of State delegated by default; multi-voice "Why it mattered" teaching expander.
- **Content-complete:** 11-agent research+verify+audit workflow → `data/diplomacy.json` (5 citation-grade multi-voice cards — Owsley/Surdam/Wise/Howard Jones/Foreman/McPherson/Ball/Gentry; consensus + scholarly dissent + named-and-countered Lost Cause + primary sources) + a 9-claim **numbers audit** that CORRECTED 3 sim figures — notably **Erlanger net realized ~45% not 72%** (77 = Erlanger's buy price, 90 = public offer; fixed in `data/economy.json` foreignLoan) — and CONFIRMED the recognition-window-closes-1863 model. Digest: `HISTORICAL-DATA-DIPLOMACY.md`.
- **VERIFIED (probe-blockade 11/11 — FULL `_t1Resolve`, 16 turns):** CS 1861 embargo rev≈4 (trap) → 1862 recognition window opens (recog→25, intervention→19) → 1863 Antietam forecloses (recog→0) → 1864 ports 2, importFactor **0.28**, cotton rev 129→53, CS arms throttled **2 vs 6** blockade-off; US importFactor 1.0 / 0 cotton; teaching card surfaces (4356 chars). Regression: probe-desk 12/12, probe-economy 8/8, probe-production 4/4, diag-classic Classic paints; 0 pageerrors.

## S1d SHIPPED + VERIFIED — manpower, conscription & the replacement-ratio collapse (the war-ender)
- `src/70-manpower.js` (`G.campaign.manpower`) models the decisive demographic asymmetry: the Union refills its armies (deep 4M pool + ~25% immigrant inflow + a one-time **+180k USCT pool at Emancipation 1863** from the South's own labor base) and **replaces every casualty**; the Confederacy's **replacement ratio collapses 0.9→0.6→0.3→0.1** by 1865 (no immigration; won't arm the 3.5M enslaved) → its armies melt. Union draft = a **coercive STIMULUS** to volunteering (×1.2; only ~6% direct, Geary), CS conscription load-bearing (1862, a year earlier), age-band widening 18-35→17-50 as a crisis 'tell'; CS desertion rises with home-front distress. `C.manpower.strength` (0-100) is **the army-strength index the S5 pre-battle bridge will read.** Surfaced as a "The Ranks" block in the War Effort overview (delegated to the Sec. of War; ambient teaching). Registered after prod (reads `B.casualties` + year); no new overrides.
- **Content-complete:** 11-agent research+verify+audit workflow → `data/manpower-teaching.json` (5 multi-voice cards — Geary/Murdock/Moore/Anbinder/Lonn/Glatthaar/Levine; Black Americans as full agents, Lost Cause erasure countered; the draft-as-stimulus, the Twenty-Slave Law, commutation-as-price-ceiling, the impossibility of CS Black enlistment) + a 9-claim audit (7 CONFIRMED, 1 DISPUTED [the 0.9→0.1 ratios are direction-confirmed but not a published statistic — kept as calibrated `_inferred` values], 1 CORRECTED [date precision only]). **No sim data correction needed** (unlike S1c's Erlanger fix). Digest folded into the card store + `HISTORICAL-DATA-DIPLOMACY.md` lineage.
- **VERIFIED (probe-manpower 9/9 — FULL `_t1Resolve`, 20 turns to 1865):** US strength **holds 100** / pool 4000→2082 (deep) / USCT unlocks 1863 / ~2,098k enlisted (≈ real 2.13M ceiling) vs CS ratio **0.9→0.1** / strength 99→**53 (melting)** / pool→**0** / desertion→**145k** / ~933k enlisted. The dying-Confederacy overview (1865) reads Ragged equipment 14 · Army strength 15 · weariness 100 · foreign-intervention 0 — every system interlocking. Regression: probe-desk 12/12, probe-economy 8/8, probe-production 4/4, probe-blockade 11/11, diag-classic Classic paints; 0 pageerrors. New gate `tools/probe-manpower.mjs`.

**S1 COMPLETE (a/b/c/d).** The full asymmetric economy is built: finance/inflation · production/logistics · cotton/blockade/diplomacy · manpower/conscription — all emergent, content-complete, probe-verified, interlinked through `clock.weariness`/`clock.intervention`/`C.funds` and now producing an army-strength index for the battle bridge.

**NEXT: S2** — executive decisions + 3-layer morale + the 1864 election hinge + **the advisor/cabinet system** (the linchpin R25: each secretary auto-manages + recommends + TEACHES its domain, per-domain delegation — generalize the Treasury/Diplomacy/War delegate seeds + the ambient teaching pattern across all domains; wire `president.pendingChoices` curated decision cards; 3-layer morale; wire `clk.resolved1864` to consequences). Then S3 (alt-history) · S4 (education/codex — the 10 economy+diplomacy+manpower card sets feed it) · S5 (victory/defeat + the strategy↔battle bridge, reading `manpower.strength`/`production.equipIndex`).

---

# RUN-LOG — 2026-06-13 (run h, Opus 4.8 — 3 queued polish items, THEN the UG:G pivot)

**Context:** Continued the run-g graphics arc (the 3 queued items), then Aaron redirected the entire project (see PIVOT below). Single-file / $0 / free-CC0 / Ultracode on.

## THE 3 QUEUED ITEMS — ALL SHIPPED + GPU-VERIFIED (gated: `.bak` + parse + hex + collision; Classic no-regression)
- **Item 1 — units (free path):** free Hyper3D MAIN_SITE "vibecoding" trial is **drained** (`API_INSUFFICIENT_FUNDS` ×2, verified this run); Hunyuan disabled; Sketchfab's CW catalog too thin/heavy (one 385k-face CC-BY soldier) for a uniform 8-type set on the UHD 617 → per the lock, **billboards stay**. Delivered the OTHER half: `chunks/out/M3D_UNITS_MOTION.js` defines the reserved `_m3dMixerUpdate(dt,now)` sub-hook — idle bob, cavalry sway, cannon **recoil + muzzle smoke** on the `u.fired` edge; stateless/time-driven (survives the per-frame unit rebuild); touches only the billboard sprite; reduceMotion plants all at rest. Empirical GPU probe `tools/probe-motion.mjs` PASS (20/20 bob, art recoil+smoke fires, rm still, 0 pageerrors) + visual no-breakage shot. GLB/LOD/cache hooks stay wired for any future `.glb` drop.
- **Item 2 — portraits (widest net):** **131 real PD photos** for the full leader roster. Pipeline: extracted 131 surnames (GEN_NAMES pool + every cmdUS/cmdCS) → **disambiguation workflow** (12 resolve agents + 5 audit agents, 186 web searches, web-confirmed canonical figure + en.wikipedia title per surname; 132/133 high-confidence, 1 audit fix Taylor→Richard Taylor CS) → deterministic fetch `.scratch/fetch_portraits.mjs` (Wikipedia REST summary → Commons imageinfo **PD-license gate** → download original → `sips` resize ~300px → `assets/portraits/<key>.jpg`). Side-split keys for the only 2 both-sides surnames (Anderson, Gregg). 131 saved (3.1 MB), 2 genuine CC-BY-SA cases (benning, franklin) correctly fall back to engraving. **Canonical IIFE refactor** (the deliberate one the handoff flagged): added a name→photo lookup INSIDE the portrait closure (`PHOTO_MANIFEST`/`PHOTO_CACHE`/`_photoLookup`/`_framePhoto`/`_photoMount`/`_warmPhotos`/`_upgradeVisiblePortraits`), prepended a photo-check to `portraitFor` (returns the framed photo synchronously after warm; the lead-badge MutationObserver gets photos transitively since it calls the same closure fn), `_warmPhotos()` kicked after `window.portraitFor`, + late-load badge swap. **Upgraded the procedural engraving fallback** with an engraved spandrel frame (corner cross-hatch + brass keyline + pins). Verified: `tools/probe-portraits.mjs` montage — real faces in period brass-oval frames, Anderson US (Robert) vs CS (Richard H.) distinct, engraving fallback framed; 0 pageerrors. Files: `assets/portraits/*.jpg`, `.scratch/portraits_manifest.json` (provenance: fullName/wikiTitle/source/license per key).
- **Item 3 — UI (fuller art pass):** `chunks/out/M3D_UI_SKIN2.js` — **drawn engraved order-icons** (March=advancing chevrons, Fire=crossed muskets, Charge=saber, Entrench=spade, Stand-Down=furled colors; inline SVG over icon-tile buttons), richer engraved panel frames (double inset + brass border-image), small-caps period headings + masthead crest, brass portrait mat on the lead-badge, struck-brass chips. Additive `<style id="m3dUiSkin2">`, reversible. GPU-verified via `tools/shot-ui.mjs` (order-icon panel + info panel render clean, no layout breakage, 0 pageerrors).
- **New tools:** `tools/probe-motion.mjs`, `tools/probe-portraits.mjs`, `tools/shot-ui.mjs`. Splice/gate discipline unchanged; 10 chunks now spliced (added M3D_UNITS_MOTION, M3D_UI_SKIN2) + the portrait-closure canonical edit.

## ⚑⚑⚑ PIVOT — MODERN MUST PLAY LIKE *ULTIMATE GENERAL: GETTYSBURG* (locked 2026-06-13, Aaron)
Aaron (PS4 UG:G muscle memory): **Modern's primary play = real-time, gridless, drag-to-move + facing, line/column formations, morale/rout/flanking, active pause — NOT hex turn-by-turn** (hex/turn is CLASSIC ONLY). The shipped "Modern" is only a **3D re-skin of the hex-turn engine** → it does NOT satisfy this; the hex+turn screenshots are exactly what he rejected (he's fine with hexes for Classic). All 4 UG:G pillars are **must-haves**. Building a real-time tactical **"field" engine** is now the #1 build — bigger than all graphics combined.
- **Decision (Aaron, run h):** the **hex/turn-based lineage is FINISHED** as of run h (Classic + the run-g/run-h 3D-hex Modern skin are the frozen hex deliverable). The real-time UG:G Modern will be **coded in a NEW chat**. THIS session: (1) finalize hex (done — this entry), (2) lock the Modern design via popup Q&A with recs, (3) write the spec doc + new-chat kickoff prompt citing the proper docs.
- **Proposed architecture (run h, pre-answers):** two engines / one dataset (Classic hex-turn untouched; Modern = new real-time field sim reading the same battles/OOBs/terrain, rendering into the same 3D scene). Reuse: cinematic 3D + 131 portraits + UI; OOBs→brigades; authored hex terrain→continuous heightmap + cover/speed field; ratings→combat modifiers (FIR→fire rate, DIS→morale, MOB→speed, ÉLA→charge, STA→fatigue); casualties→campaign plumbing unchanged. Perf ceiling (Intel UHD 617) forces brigade-grain + instancing + LOD. See `MODERN-UGG-PLAN.md` for the tactical spec.

## DESIGN SESSION + RESEARCH + HANDOFF (run h, same session)
- **PhD research workflow:** 18 agents (10 dossiers + adversarial fact-check, ~826k tokens, 293 web lookups) → **`HISTORICAL-DATA.md`** (211KB: First Bull Run full OOB Union+CSA, weapons/ballistics w/ ranges, USCT structure+battles+criticism, historiography framework, PD image sources; 5 corrections applied). Reusable PD-fetch pipeline established (Wikipedia REST → Commons PD-license gate → sips).
- **MISSION EXPANDED → three-in-one game:** UG:G real-time combat + Paradox-depth **owner-mode grand strategy (player = President Lincoln/Davis)** + PhD-level history & **alternate-history** teaching (consensus favored-but-not-guaranteed).
- **35-round popup design session, 63 decisions LOCKED** → **`GRAND-STRATEGY-PLAN.md`** Parts I (rounds 1–10), II (11–20), III (21–33) + §27 balance principle. Central depth↔simplicity tension RESOLVED: simple low-micro UG:G battle = always-fun core that stands alone; all depth optional (cabinet advisors auto-manage + TEACH; per-domain delegation; play-style presets General/Commander/President/Historian; fluid mid-campaign dialing); education ambient/never-blocks/confers-mastery. Asymmetric win paths incl. **negotiated peace (both sides)**; military-only hard-loss; full economy/diplomacy/politics/naval/map/intel; slavery front-and-center as the cause + Black agency; real OOBs + weapons-driven combat.
- **Build order = STRATEGIC-FIRST** (S0–S5 owner-mode wrapping existing battles, then tactical P0–P5). **ARCHITECTURE DECISION: modularize via a zero-dep node concat build step → one shippable HTML (set up in S0); Classic FROZEN.**
- **NEW-CHAT ENTRY POINT: `HANDOFF.md` §PASTE** (master handoff; cites GRAND-STRATEGY-PLAN + MODERN-UGG-PLAN + HISTORICAL-DATA). `NEXT-CHAT-PROMPT.md` banner repointed. Scope ambition: "protect everything (no cuts)," ship-quality iteratively. **No new-engine code written yet — next chat builds from the spec.**

---

# RUN-LOG — 2026-06-13 (run g, Opus 4.8 — CINEMATIC GRAPHICS RUN, fresh VS Code session)

**Context:** Executing `GRAPHICS-RUN-PROMPT.md` §D — take the Modern 3D renderer to a high-end, stylized-realistic, cinematic finished look. Single-file / $0 / free-CC0 / full-autonomy / Ultracode on. Phase 0 verified: Blender 4.5.10 bridge LIVE (keys re-applied from `secrets.local.json` — PolyHaven/Sketchfab/Hyper3D FAL_AI), real-GPU verify confirmed (`shot-gpu.mjs` GL renderer = `ANGLE Metal Renderer: Intel(R) UHD 617`, NOT SwiftShader), splice anchor `/*__ENGINE_END__*/` unique, node 25.9. Research fan-out (4 agents + adversarial verify) banked the r0.128 post-FX load order, Poly Haven shortlist, period unit dossier, wet-plate grade GLSL.

## G1 — CINEMATIC POST-PROCESSING ✅ SHIPPED + GPU-verified
`chunks/out/M3D_POSTFX.js` — lazy EffectComposer stack on the SAME jsdelivr three@0.128 tree (15 example scripts, dependency-correct load order **adversarially verified**: caught two real load-time deps — r128 `SMAAPass` needs `SMAAShader.js`, `BokehPass` needs `BokehShader.js` — that a naive list omits → black screen). **ACES tone-map** (renderer.toneMapping=4) + **UnrealBloom** (high threshold 0.88 so daylit grass/sky don't fog) + **period wet-plate grade** ShaderPass (cool steel shadows / warm-khaki mids / ivory highlights, sat pull, S-curve) + **SMAA** + **film-grain+vignette** ShaderPass. **PERF-TIERED for UHD 617:** LOW = direct ACES render (no composer); AUTO (desktop default) = bloom+grade+SMAA+vignette (sprite-safe); HIGH = +SSAO (kernelRadius 16) +grain; DOF/Bokeh implemented but opt-in (`__M3D.postDOF`, default off — blurs the tactical read). reduceMotion freezes/zeroes grain. **Trap avoided:** grade/grain shader defs use `new THREE.Color/Vector2`, so they're built by FACTORY FNS at composer time, never at chunk eval (THREE loads async AFTER the spliced script — a top-level `new THREE.*` would throw and kill the whole game). Override-by-redeclaration of `_m3dStartLoop`+`_m3dResize` (last wins; both replicate prior body + add composer); a guarded `_m3dFrameUpdate(dt,now)` hook lets G4/G5 plug the loop without re-overriding. New tool `tools/shot-postfx.mjs` waits for the composer to actually build (post scripts async) then shoots the true full-stack frame + reports pass list. **Verified:** auto passes=[Render,Bloom,grade,SMAA,grain], high=[Render,SSAO,Bloom,grade,SMAA,grain], toneMapping=4, 0 pageerrors, no washout (r128 encoding gotcha did NOT bite), no SSAO sprite-halos at gameplay zoom.

## G2 — HDRI SKIES ✅ SHIPPED + GPU-verified
3 Poly Haven CC0 HDRIs (2k, verified `#?RADIANCE`) → `assets/3d/env/`: `sky_day.hdr` (syferfontein_18d_clear_puresky), `sky_overcast.hdr` (overcast_soil_puresky), `sky_dusk.hdr` (belfast_sunset_puresky), via direct PolyHaven CDN fetch (file-naming needs exact names; Blender import drops into the scene, not my asset dir — direct fetch is the right tool, still CC0/$0). `chunks/out/M3D_HDRI_SELECT.js` re-declares `_m3dLoadEnv` + `_m3dApplyAtmosphere` (winning; atmosphere body replicated verbatim + one `_m3dSelectEnv()` call) → the sky now picks rain/fog/snow→overcast, dawn/dusk→dusk, else day, cached PMREM per name. **GPU-verified:** clear→bright daylit sky; rain→cohesive grey overcast sky + grey light (was an odd near-black before — the day HDRI under storm fog/grade).

## G5 — CINEMATIC PARTICLES ✅ SHIPPED + GPU-verified
`chunks/out/M3D_PARTICLES.js` — defines the per-frame dispatcher `_m3dFrameUpdate(dt,now)` (the post-FX loop's guarded hook) which fans to guarded sub-hooks (`_m3dWeatherUpdate`, `_m3dSmokeBankUpdate`, future `_m3dMixerUpdate`) so later chunks add behavior by DEFINING a sub-hook, never re-overriding the loop. (A) **Weather** — rain streaks / drifting snow as a camera-following recycled Points volume driven by `G.battle.wx`, perf-tiered counts (high 1700/1100, auto 850/600, low 0), reduceMotion-off. (B) **Lingering powder-smoke banks** (locked look #8) — seeds a large long-lived (5-8s) soft smoke puff from fresh `G.fx` fire/smoke events (marks `e.__bank`), drifts downwind + rises + expands + bell-curve fades; capped (high 36/auto 18/low 0). Muzzle/cannon flash already blooms (fire FX is additive bright, above the 0.88 bloom threshold). **GPU-verified:** rain falls under the overcast sky (0 err); seeded 16 fire events → 18 live banks visible as drifting powder smoke over the line (`shot-postfx.mjs` gained `wx` + `fx` args). reduceMotion + low tier verified off.

## G4 — UNITS: Tripo dry → billboards stay (FLAGGED for Aaron)
**Tripo3D free-tier credits EXHAUSTED** (`/user/balance` = `{balance:0,frozen:0}`; key valid). fal.ai/Rodin (Hyper3D FAL_AI) is enabled but the key is **private/pay-as-you-go** → using it bills Aaron, which violates the locked **$0/free-CC0-only** budget (lock #3: free-tier OK, paid NOT). So the budget lock itself decides it: **no paid generation.** Units stay as the existing painted billboards — free AND far kinder to the UHD 617 than 30+ skinned animated GLBs (animated units would also need aggressive LOD on that GPU). The `_m3dMixerUpdate` loop hook is reserved so a future GLB-with-clips drop lights up. **AARON DECISION:** to get rigged+animated Tripo units this run, top up Tripo credits at platform.tripo3d.ai; otherwise billboards + Sketchfab CC0 static hero pieces are the free path. Proceeding on the free path.

## G3 — PBR TERRAIN ✅ SHIPPED + GPU-verified
`chunks/out/M3D_TERRAIN_PBR.js` — re-declares `_m3dBuildTerrain` (winning) with a per-key `MeshStandardMaterial` path (albedo+normal+rough), **vertex-color MeshLambert fallback intact** (zero-asset = identical to before). Lazy per-key texture load (like `_m3dUnitModel`), coalesced single rebuild on arrival, geometry+fallback-material disposal added (the original leaked). 27 PNGs (9 terrain keys × albedo/normal/rough, 1k, CC0) via new `tools/fetch_polyhaven_terrain.mjs`. **GPU-verified:** flat poker-chip hexes → real textured ground (leafy/sparse grass on clear/field, forest floor in woods, rocky trail on roads, cobblestone in town, mud in swamp), lit by the HDRI env + toned by the grade. Combined G1+G2+G3 = dramatic jump from the flat baseline. 0 pageerrors. Classic pixel-unchanged (re-shot).

## G6 — PROPS / BATTLEFIELD DRESSING ✅ SHIPPED + GPU-verified
`chunks/out/M3D_PROPS.js` — $0 procedural, **instanced** (forest = ~4 draw calls). FORESTS on every `woods` hex: 2 species (conifer cone / broadleaf faceted icosahedron canopy) scattered with deterministic per-tree jitter/rotation/scale + per-tree green tint (`InstancedMesh.setColorAt`), baked geometry so one matrix drives trunk+canopy. BUILDINGS on `town` hexes (clapboard body + gable roof, capped 14) + named building features (label contains Church/House/Farm/Mill → distinct structure; Dunker Church gets a white steepled church). MeshStandard so props catch HDRI IBL + grade + shadows. Tree budget perf-tiered (high 460 / auto 240 / low 0). Integrated by redeclaring the `_m3dFrameUpdate` dispatcher (winning) = replicate G5 sub-hook dispatch verbatim + a battle-change props rebuild (no sync/terrain re-override). **GPU-verified:** Antietam (West Woods timber + Sharpsburg buildings) + Gettysburg high (forests across Seminary/ wooded ground + town buildings), 0 pageerrors. Decision: chose reliable procedural over the Sketchfab-download→Blender-export→quality-lottery path (Tripo dead, fal.ai paid) — procedural + IBL + post-FX reads stylized-realistic and is perf-safe on the UHD 617; a `church_dunker.glb`-style CC0 drop can be wired later.

## ROUND-2 DISCOVERY (Aaron, 5 rounds × 3 Q, mid-run g) — LOCKED for the next execution pass
- **Units = $0, FREE TIERS ONLY ("im not paying").** No paid Tripo animation. Path: free **Rodin/Hyper3D MAIN_SITE** ("vibecoding" trial key) **STATIC** models for all 8 core (inf/cav/art/general × US/CS), + **procedural engine motion** (cannon recoil+smoke, horse bob/sway, idle bob) for life. **Hybrid LOD: real model close/selected, billboard far** (keeps 30+ regiments smooth on UHD 617). If free Rodin is rate-limited/blocked → billboards stay. (Cost facts banked: Tripo $1=100cr; full-animate 8 ≈ $10-12; balance 0, 300 free credits already spent. Aaron declined to pay.)
- **Portraits = widest net.** Real PD Brady/LoC photos for EVERY historical figure with a verifiable public-domain portrait (incl. lesser-known — pull the full OOB leader roster), **clean photo in a simple period frame** (minimal processing), AND **upgrade the procedural engraving fallback** too. NOTE: lead-badge observer calls the CLOSURE `portraitFor` by bare name → needs a deliberate portrait-subsystem refactor (add a name→PD-image lookup layer), not an append hack.
- **Signature props = the chosen next polish** (over a water shader): snake-rail fences, **stone walls** (Marye's Heights), orchards (the Cornfield/Peach Orchard), **earthwork redoubts** on fort hexes. Extend `M3D_PROPS`.
- **Remove DOF entirely** — drop the Bokeh pass + BokehShader/BokehPass from the post-FX load list (it was already opt-in/off).
- **Camera = subtle idle drift + auto-frame the action** during the AI phase, with a lock toggle. Respect reduceMotion.
- **UI = fuller art-directed pass, self-verified** (richer engraved frames, period display type, drawn order-icons, card language) — I verify via shots since Aaron isn't testing.
- **After all this: keep pushing the graphics ceiling** (water shader, day/night, replay). **Publish DEFERRED** until Aaron is satisfied (he triggers DEPLOY.md; no outward action from me).
- **Aaron won't eyeball/test this pass** ("you do it or defer") → I self-verify everything via `shot-postfx.mjs` real-GPU reads.

**EXECUTED this session (round-2 pass 1) — 3 of 7 done, GPU-verified, 0 pageerrors:**
- ✅ **DOF removed** — dropped `BokehShader.js`+`BokehPass.js` from the post-FX load list + the bokeh build/resize blocks (both HTML + `M3D_POSTFX.js`); parse PASS, post-FX still builds (Render/[SSAO]/Bloom/grade/SMAA/grain). Inert `var dof`/key + one doc comment remain (harmless).
- ✅ **Cinematic camera** (`chunks/out/M3D_CAMERA.js`): idle drift (OrbitControls.autoRotate after 5s no input) + combat auto-frame (eases look-target toward the centroid of fresh `G.fx` events). Gated on `G.settings.follow` + reduceMotion; player input always wins (lazy pointerdown/wheel listeners mark `_lastInteract`). Redeclares `_m3dFrameUpdate` (winning) = camera+props dispatch.
- ✅ **Signature props** (`chunks/out/M3D_PROPS2.js`): earthwork redoubts (parapet arc) on `fort` hexes; fieldstone **walls** at Wall/Sunken/Heights/Angle features (Marye's Heights, the Sunken Road); **orchards** (ordered tree grid) at Orchard/Cornfield features; snake-rail **worm fences** along sampled `road` hexes. Added to `__M3D.propGroup` (rebuilt with forests/buildings). Perf-tiered off on low. Verified on Fredericksburg High (`postfx-fredericksburg-high.png`).
- **QUEUED (3 — large, fully scoped above; recommend a focused continuation):** (1) **free Rodin static units** — first verify the MAIN_SITE "vibecoding" free-trial actually generates ($0); if so batch 8 → `assets/3d/models/units/`, the GLTFLoader + hybrid-LOD + `_m3dMixerUpdate` procedural-motion hooks are wired to light up; if blocked, billboards stay. (2) **widest-net PD portraits** — pull the full OOB leader roster, download PD photos, refactor the portrait closure to a name→image lookup, upgrade the procedural engraving. (3) **fuller UI art-directed pass**. NOTE: 8 chunks now spliced (added M3D_CAMERA, M3D_PROPS2); single `<script>` still 298→end; anchor unique.

## G8 — UI/HUD PERIOD-ENGRAVING SKIN ✅ SHIPPED (safe v1) + verified
`chunks/out/M3D_UI_SKIN.js` — the 2D HUD already had a cohesive period system (brass/parchment tokens, Iowan serif, engraved `.sheet::before` frames) from prior runs, so a blind "full pass" risked regressing a working aesthetic (and UI is Aaron's-taste territory). Did a CONSERVATIVE, ADDITIVE, reversible skin: one injected `<style id="m3dUiSkin">` adding struck-brass sheen + engraved inset hairlines to `#topbar`/command panels/buttons/`.lead-badge`/minimap + letterpress depth on display headings. Every rule re-states the original box-shadow then ADDS an inset (no layout shift). **GPU-verified** no layout breakage (Modern + Classic). Full art-directed taste-pass remains Aaron's call.

## G7 — PORTRAITS: fallback already ships; PD photos flagged
The procedural portrait engine `_renderPortrait`/`portraitFor` (closure) ALREADY supports `opts.tintype` (wet-plate plate + oval brass ring + vignette) — so locked decision #9's **tintype-framed procedural fallback is already shipped**. PD Brady/LoC photos for real figures (Lee/Grant/Jackson/… keyed by last name) can't be injected cleanly from an append chunk: the lead-badge MutationObserver calls the CLOSURE `portraitFor` by bare name (not `window.portraitFor`), so a photo layer needs a deliberate refactor of the portrait closure (a canonical-file edit + name→PD-image map + framing), not a fragile second-observer race. **FLAGGED for Aaron** (taste territory + deliberate refactor).

## CLASSIC NO-REGRESSION — proven; + harness bug fixed (good catch)
Caught a scare: Classic shots came back EMPTY (#map blank). Root-caused via `tools/diag-classic.mjs`: NOT a regression — run-f flipped the **default renderer to modern**, so on boot modern auto-activates and `_m3dShow` sets `#map` `display:none`; the shot harness forced `G.settings.gfx='classic'` by property only, never calling `_m3dDeactivate()` to un-hide `#map` (the real game's Settings toggle does). Diagnostic proved the 2D board DOES paint (346/714 non-blank px) — just hidden. **Fixed both harnesses** (`shot.mjs` + `shot-gpu.mjs`: when gfx≠modern, call `_m3dDeactivate()`); re-shot `gpu-classic-antietam.png` = **1017KB, pixel-comparable to the 1013KB baseline** — Classic fully intact. (My 6 chunks are `_m3d*` + additive CSS; the classic 2D `draw()` path is structurally untouched.) This harness bug predated my session and would have masked real Classic regressions in any run since run-f.

**Tooling/gates this run:** `.bak` before each splice (rolling = last-known-good); parse gate (`node --check` on extracted `<script>` — single tag 298→12927); invalid-hex grep 0; collision grep per chunk; `tools/shot-postfx.mjs` real-GPU READs (gained `wx`+`fx` args); `tools/bootprobe.mjs` (default path = ok, 9 expected unit-GLB 404s, 0 real errors); `tools/diag-classic.mjs` (new). Single committer; every new fn collision-checked 0 before splice. **6 chunks spliced + GPU-verified: M3D_POSTFX, M3D_TERRAIN_PBR, M3D_HDRI_SELECT, M3D_PARTICLES, M3D_PROPS, M3D_UI_SKIN.** Assets: `assets/3d/env/sky_{day,overcast,dusk}.hdr` + `assets/3d/materials/terrain/{9 keys}_{albedo,normal,rough}.png` (all CC0 Poly Haven). New tools: `tools/fetch_polyhaven_terrain.mjs`, `tools/shot-postfx.mjs`, `tools/diag-classic.mjs`.

### AARON — what to open + judge (final eye on fidelity/perf)
1. `play.command` → Settings → Graphics = **Modern**, **3D Quality = High** → open **Gettysburg** (and **Antietam**): forests on the woods/ridges, buildings in towns, PBR ground, HDRI sky, ACES+grade+bloom+SSAO+grain, drifting smoke when units fire, the Dunker Church. Pan/zoom; watch FPS on the UHD 617 — if High stutters, set **3D Quality = Auto** (drops SSAO/DOF/grain, keeps bloom+grade+SMAA).
2. Try a **rainy/foggy** battle → overcast HDRI + rain streaks; a **dusk** battle → golden HDRI.
3. **Decisions for you:** (a) **Tripo credits = 0** — top up at platform.tripo3d.ai if you want rigged+animated 3D unit models this is the only blocker (billboards otherwise; the `_m3dMixerUpdate` hook + GLB loader are wired to light up on drop); (b) **portrait PD photos** + **full UI taste-pass** — your eye; I left both as deliberate follow-ups rather than risk the working 2D aesthetic.

---

# RUN-LOG — 2026-06-13 (run f, Opus 4.8 autonomous — MODERN IS NOW THE DEFAULT + audio score)

**Context:** Fable persona retired (project + standards kept). Full autonomy, Ultracode on. Opening 3-question round locked: **(1) flip Modern to default, (2) install+open Blender to open the 3D-asset ceiling, (3) full adaptive period score.** Then "continue with new intensity."

## TRACK A — MODERN 3D NOW BEATS CLASSIC → flipped to DEFAULT ✅ SHIPPED + verified
Closed the three parity gaps the run-e handoff named as the only things keeping Modern behind Classic, then flipped the default renderer. New module `chunks/out/M3D_PARITY.js` (Opus-authored by me; deeply engine-coupled so I owned it directly, not an executor) — append-override of two tiny fns (`_m3dSync`, `_m3dStartLoop`; faithful-body + additions) + ~15 new `_m3d*` helpers:
- **(1) Named-feature labels in 3D** — reads `M.authoredFeatures` (same source as Classic's `_drawFamousFeatures`), draws each as a camera-facing parchment plaque on a thin brass stake above its exact hex; Verified vs **Inferred** styled distinctly (lighter + dashed border). VERIFIED in pixels: Cornfield / Sunken Road / Dunker Church / Sharpsburg / Antietam Creek / Burnside's Bridge / Hagerstown Pike all placed.
- **(2) Live unit badges in 3D** — a dark map-room plate above each regiment: side-colored strip + serif name + colorblind side-glyph + **strength bar** (green→amber→red w/ number) + morale/ammo/xp glyph row. Same live data Classic paints (`u.strength/maxStr`, `morale`, `ammo`, `xp`, `routed`). Texture-cached by stat-signature (rebuild only on change, evict >120). VERIFIED: "1st Infantry" plate w/ green "412" bar + side glyph.
- **(3) 3D battle FX** — consumes the SAME renderer-agnostic `G.fx` queue `emitFX()` already fills (muzzle flash = additive sprite, drifting powder smoke = soft puffs rising+downwind, melee dust, **floating −casualty numbers**). Animated in the overridden render loop; prunes `G.fx` itself (Classic's `drawFX` never runs in Modern). VERIFIED: "−73" gold casualty number rising off a unit. Honors reduceMotion implicitly (emitFX creates nothing → empty FX group).
- **DEFAULT FLIPPED:** `G.settings.gfx` `"classic"→"modern"` (line 523). New profiles boot into the 3D battlefield; saved "classic" choices still win (applySave runs before the boot-activate timeout); `_m3dRevert` still falls back to Classic on offline/no-WebGL. Classic remains a Settings toggle and is **pixel-unchanged** (re-shot, no regression — chunk only touches `_m3d*`).
- **Gates:** full-file parse PASS; invalid-hex 0; only `_m3dSync`/`_m3dStartLoop` duplicated (build fns untouched); `tools/shot.mjs` re-shot modern-antietam/-hero/-fx + gettysburg + a close badge/FX probe, READ every PNG; new `tools/bootprobe.mjs` confirms the **default** path boots Modern with `__M3D.ready`, 32 units, ZERO real errors (7×404 = expected absent-asset probe). `.bak` taken before splice.
- **Caught at gate (banked lesson):** a chunk banner comment literally contained the engine-end marker token, whose `*/` closed the block comment early → parse fail. LESSON: never embed a literal `*/` (or the `/*…*/` marker) inside a chunk's block comment.
- **Env:** the documented tmpfs 0MB-quota gotcha recurred hard (the live background executor's streaming transcript + capture files filled the per-task dir). Remedy that worked: `find /private/tmp/claude-501 -name '*.output' ! -name '<RUNNING-AGENT-ID>*' -delete` — MUST exclude any running background agent's file or you can break its result delivery. Kept commands' stdout tiny + used `node -e` single-write JSON for multi-value checks.

## TRACK C — Adaptive period audio score ✅ SHIPPED + verified
`chunks/out/AUDIO_SCORE.js` (Opus executor, ~810 lines) + `chunks/out/AUDIO_WIRE.js` (my glue). Own AudioContext (never touches the existing SOUND IIFE), lookahead scheduler. API: `scoreInit / musicStart(menu|march|battle|camp) / musicStop / bugleCall(reveille|assembly|charge|recall|taps|to_the_colors|tattoo) / dinSet / dinStop / scoreSampleBattle`. **Content:** original C-major fife-and-drum quickstep (menu/march), low detuned-saw drone + slow drum + sparse bugle (battle, mixed under the din), lonely triangle air (camp); **bugle calls = harmonic-series pitches ONLY** (valveless-bugle fact); **battle din** = brown-noise rumble + cannon thumps + musket crackle whose density/gain scale with combat intensity. Gated on `G.settings.sound` + new `G.settings.music` (default on; Settings toggle added). **Hooks (guarded core edits I own):** `_audEnterBattle` at `startBattleRuntime` tail (music+din+ambient + a 1.2s `dinSet(scoreSampleBattle())` sampler), `_audLeaveBattle`+win/loss bugle (`to_the_colors`/`taps`) in `endBattle`, charge bugle in `resolveCharge` (player only), `_audMenu` in the newspaper `openMainMenu`. **Also fixed a latent gap:** `ambientStart()` was only ever called from the Settings toggle — now starts on battle entry.
- **Audit caught 2 real executor bugs + I caught a 3rd that nearly shipped:** (1+2) `scoreSampleBattle` guessed `G.battle` field names — fixed to the real shape (`casualties:{US,CS}` uppercase; engagement via `u.fired`). (3) **CRITICAL — the whole module gated on `window.G`, but `G` is `const G` (lexical global, NOT a `window` property) → `window.G` is `undefined` → `sndOn()` always false → the entire score was silently muted.** Fixed: `window.G`→bare `G` (the IIFE closes over the outer const). The empirical `tools/audioprobe.mjs` caught it — a static read had passed it twice.
- **LESSON (banked, recurring):** this game declares `G` (and `HEX`, etc.) with `const`/`let` at script top-level → they are **lexical globals, NOT on `window`**. Reference them by **bare name**, never `window.G`. Same trap bit `tools/bootprobe.mjs`. Any appended chunk that reads engine state must use bare `G`.
- **Gates:** full parse PASS; hex 0; no dup symbols; `tools/audioprobe.mjs` (real Chrome WebAudio) — API present, `scoreSampleBattle` 0→0.64 on a real casualty bump, battle entry+exit with the full score live = ZERO pageerrors; `tools/bootprobe.mjs` — default boots **Modern 3D + audio together**, 32 units, zero real errors. (I cannot hear it — musicality is Aaron's ear-test, the audio analog of the WebGL screenshot loop.)

## TRACK D — Modern atmosphere (Round-2 lead "push the Modern look further") ✅ SHIPPED + verified
`chunks/out/M3D_ATMOSPHERE.js`: (A) **weather + day-phase lighting** — `G.battle.wx` (clear/hot/rain/fog/snow) + optional `bd.tod` (dawn/day/dusk/night) drive sun color+intensity, sky color, fog density, hemisphere fill via `_m3dApplyAtmosphere` (HDRI-safe: leaves background to the env if one loaded, always tunes fog). (B) **3D objective flags** — staff + banner over each `M.objs` hex, cloth recolored by `o.owner` (US blue / CS red / neutral grey) each sync, so objective control reads in 3D. Hooked via 3 guarded `typeof` calls added to the parity `_m3dSync` (NOT a re-override — the new chunk only DEFINES the hook fns). VERIFIED in pixels: `modern-weather-fog.png` washes the field into grey-white haze with dim light vs the warm-bright `modern-antietam.png` (clear) — dramatic, correct per-weather mood; objective banners build clean. Full parse PASS, hex 0, `_m3dSync` still 2 defs (atmosphere added calls, not a 3rd def). Classic untouched. NOTE: the parity chunk's spliced `_m3dSync` in the HTML gained the 3 hook lines; the `chunks/out/M3D_PARITY.js` source file mirrors this. Queued next under this track: smoke banks, animated billboard poses, rain/snow particles.

## TRACK E — Blender MCP pipeline LIVE ✅ (Aaron's "do the installs from here" + Q3 "full battlefield kit")
Did the entire Blender setup from the CC terminal (Aaron asked if I could install from here — I could; Cowork has no edge since neither can click inside Blender's GUI):
- `brew install --cask blender` (background) → **Blender 4.5.10 LTS** in /Applications.
- Headlessly installed + enabled the MCP addon (`blender --background --python .scratch/enable_addon.py` → `BLENDERMCP_ENABLED: True`, saved to userprefs). Patched `tools/blender_mcp_addon.py`: (1) **skip auto-start under `bpy.app.background`** so the headless enable can't choke on the socket/timer; (2) **Poly Haven default ON**. The addon already auto-starts its server on GUI `register()`, so launching Blender needs ZERO clicks.
- `open -a Blender` → GUI up (PID), server **LISTENING on 127.0.0.1:9876** after splash.
- **Bridge verified end-to-end** (no session restart needed — the `blender` MCP server was already connected to this session): `get_scene_info` read the default scene; `execute_blender_code` exported a test `.glb` into `assets/3d/` (1936 B, removed); `get_polyhaven_status` = enabled.
- **The full 3D-asset ceiling is OPEN.** NEXT ARC = battlefield kit: PolyHaven CC0 PBR terrain + day/dusk HDRIs (`assets/3d/env/sky_day.hdr` already auto-loads via `_m3dLoadEnv`), Sketchfab CC0, Hyper3D/Hunyuan props (need API keys — not yet set), core unit `.glb` → batch to `assets/3d/`, then wire a PBR-terrain path in the Modern renderer (currently vertex-color `MeshLambertMaterial`) + verify. CAVEAT: HDRI/PBR visual verification via `shot.mjs` is limited by SwiftShader software-WebGL — file-lands + engine-loads-without-error is self-verifiable; the actual lit look is Aaron's real-GPU browser. **Aaron's only standing requirement: keep Blender.app open while I run asset jobs.**
- **Asset engines all live:** PolyHaven (CC0, no key), Hyper3D via **fal.ai** (private key, FAL_AI mode), **Sketchfab** (logged in as adhanke), **Tripo3D** (key held — NOT in the Blender addon; direct REST). Hyper3D.ai-direct + Meshy APIs are paywalled (skip; fal.ai = same Rodin); Hunyuan = Tencent-cloud friction (skip). Keys persisted to Blender addon prefs + **`secrets.local.json` (gitignored)**.

## TRACK F — GRAPHICS RUN planned + GPU-verify built + handoff prompt (5-round discovery)
Aaron directed a 5-round design-discovery to scope the next run as a **high-end cinematic graphics push** in a fresh VS Code session. **15 locks** (see `GRAPHICS-RUN-PROMPT.md` §A): stylized-realistic · **max three.js single-file** (no build, CDN post-FX) · **free/CC0 only** · units = **Tripo3D** rig+animate+multiview+retopo (fal.ai/Rodin static fallback) · terrain/sky = **PolyHaven** PBR+HDRI · animation = idle/march/fire/fall/charge/rout via `AnimationMixer` · post-FX = **cinematic max** (bloom/SSAO/ACES/grade/SMAA/DOF/vignette/grain) **perf-tiered** · particles (smoke banks/weather/muzzle) · portraits = **PD Brady/LoC tintypes** + tintype-frame fallback · **full art-directed UI** · **deep period research** · full-blast + full-autonomy + continuous · **real-GPU verify**.
- **Built `tools/shot-gpu.mjs`** — headed GPU Chrome (no SwiftShader); GL-renderer confirmed **`ANGLE Metal Renderer: Intel(R) UHD Graphics 617`** = the Mac's REAL GPU. So true bloom/SSAO/HDRI/PBR is now self-verifiable, $0, zero gen-credits. **HARDWARE CONSTRAINT (banked):** target = Intel UHD 617 integrated / 8GB → cinematic-max = High-tier ceiling; Auto/Low MUST strip DOF/SSAO/grain + cut shadows/particles/figures; instancing+LOD mandatory.
- **`GRAPHICS-RUN-PROMPT.md` written** = locks (§A) + full options-evaluation & resource-assignment table (§B) + hardware/prereqs (§C) + the **self-contained paste-in kickoff prompt (§D)** for the new session (every tool, key-location, file path, engine seam, citation, gate, and the bare-name-globals lesson). Aaron pastes §D into a fresh VS Code Claude Code session with Blender open.

---

# RUN-LOG — 2026-06-14 (run e, Fable autonomous — pantheon maps batch 2 + T1 War Department)

**Context:** Full autonomy granted ("run the loop without stopping"). Ultracode on → workflow-orchestrated. Two background workflows dispatched up front: (1) parallel battlefield research (6 agents), (2) T1 executor wave (3 build + 3 adversarial-audit agents). Aaron mid-run: broadened permissions to allow all bash + URL searches (settings.local.json updated).

## TRACK 2 — Six pantheon maps authored + dual-render verified ✅
Authored **bullrun1, bullrun2, chancellorsville, vicksburg, chickamauga, franklin** into `AUTHORED_MAPS` (all 22×20), completing the pantheon batch (R46 order). Pipeline: 6 parallel web-research agents → structured geography briefs (NPS / American Battlefield Trust / LoC maps, Verified/Inferred tagged) → I hand-authored each grid via a new self-validating generator `tools/build_pantheon2.mjs` (emits complete entries; row-width validated) → programmatic splice (asserted single anchor) → gates.
- **Gates:** parse PASS (`node --check`), invalid-hex grep clean, **all 12 scenes (classic+modern) booted `[setup] OK` / `modern:ready`, no pageerror.** READ every PNG myself — each renders to the real ground:
  - **bullrun1** — Bull Run on the E, Sudley Ford NW, Warrenton Tpk + Stone House crossroads, Henry House Hill (obj★3) S-center, Chinn Ridge SW.
  - **bullrun2** — Unfinished Railroad cut (Jackson's breastwork, obj★3) NE-diag, Stony Ridge woods N, Dogan Ridge center, Chinn/Bald Hill S, Henry Hill rally SE.
  - **chancellorsville** — Wilderness woods blanket, crossroads (obj★3) center, XI Corps flank strung W on the Turnpike, Hazel Grove (obj★3)/Fairview SW, Rappahannock + U.S. Ford N.
  - **vicksburg** — Mississippi W edge + city; fort arc bellying E (Stockade Redan/3rd La Redan obj★3, Railroad Redoubt, Fort Garrott, South Fort); radial road-gates; loess ridges.
  - **chickamauga** — LaFayette Road N-S spine, W. Chickamauga Creek + Reed's/Alexander's Bridges E, cleared fields (Kelly/Poe/Brotherton obj★3/Dyer/Viniard), Snodgrass Hill+Horseshoe Ridge NW.
  - **franklin** — Harpeth moat wrapping town N/E, Union earthwork arc (Carter House/Cotton Gin obj★3), Winstead Hill S, the open killing-plain, Fort Granger NE.
- Each has Verified/Inferred feature ledger + "The Ground" prose. **Classic regression check: classic-antietam re-shot, unchanged.** Generator + briefs banked; `tools/shots.json` extended with 12 new scenes.

## TRACK 1 — T1 "War Department" systems (1864 Clock / Muster Roll / War Room) ✅ SHIPPED + verified
One cohesive **War Department** strategic shell (Aaron locked 2026-06-14: Fable designs on best judgment, corrects after). Three namespaced append-only chunks (`clk`/`mr`/`wr`, each exposing Init/OnResolve/RenderHTML/Wire) + Fable-authored glue (`chunks/out/T1_SHELL.js`: `openWarDept` tabbed sheet + `_t1Resolve`/`_t1InitAll` hooks + `_wdRefresh`). State lives under `G.campaign.{clock,muster,warroom}` → rides saves free, defensively initialized for old saves, `_SAVE_VER` NOT bumped. Integration = 3 small guarded core edits (campaignAdvance tick, startCampaign init, **newspaper menu** entry "THE WAR DEPARTMENT CONVENES").

**Design choices (Fable's v1 judgment — Aaron, correct anything here):**
- **1864 Clock:** weariness/political-capital/foreign-intervention meters; per-battle deltas (win −6 wear/+10 cap, decisive/objwin bigger; +casualty pressure; intervention asymmetric — CS wins court Europe, US wins cool it). The 1864 referendum is a **recorded verdict, NOT game-over** (support = 100−weariness + cap/2 + winrate·20 ≥ 60 → sustained). War-Bond Drive spends capital → +150 funds (escalating cost).
- **Muster Roll:** permanent ledger of every regiment + fate; auto-names unnamed regiments with period names (e.g. "14th Ohio Infantry"); records battles/credited-kills, marks fallen with where they fell. Read-only v1.
- **War Room:** 5 buildable nodes (side-labeled US Factory/Arsenal/Farm/Railroad/Depot vs CS Tredegar/Niter Bureau/Plantation/...); spend funds → economy output (funds + supply drift + a political-capital trickle into the Clock — the interlink); raider events cut the line. **Guardrail (deliberate): v1 War Room is CAMPAIGN-LAYER only — funds/supply/capital, NOT in-battle combat numbers** (avoids battle-engine regression). Course-correct candidate: make supply feed a small in-battle ammo/morale edge later.

**Audit trail (trust-nothing discipline paid off):** my own read passed all three; the workflow's adversarial auditor caught **2 real `mr` bugs I'd missed**, both verified against live engine before fixing: (1) unnamed regiments' generated period name was clobbered by the engine's generic in-battle label — gate on `u.vetName` (player-supplied) not `u.name` (engine line 953 confirms); (2) `u.kills` is **per-battle** (roster carries no kills field, lines 2655-2661) so `Math.max` undercounts — accumulate instead. Both fixed (<15 lines, patched in place per AUDIT-PROTOCOL).

**Gates:** parse PASS; no invalid hex; no duplicate defs (each fn = 1); functional probe (`tools/t1probe.mjs`) drove a real 3-battle campaign — all 16 fns present, init/tick math correct, **1864 election fires**, both mr fixes confirmed live (name "1st Maine Infantry" preserved, kills 2+1=3 accumulated), War Room build/spend + bond/spend Wire paths work, capital trickle interlink works, **zero errors/pageerrors**. Visual: caught + fixed a real dark-on-dark contrast bug (War Room panel had no bg) — all 3 tabs now parchment "map-room paper", legible + cohesive (`tools/shots/t1-{clock,muster,warroom}.png`). Menu entry reachable + opens (`tools/menuprobe.mjs`, `t1-menu.png`). **Classic + Modern battle regression clean** (antietam/gettysburg re-shot). NOTE: the older shadowed `openMainMenu` (CHUNK-08, line ~3245) carries an inert `mmWarDept` edit — dead code (G6's newspaper `openMainMenu` wins); left as a harmless fallback.

---

# RUN-LOG — 2026-06-14 (Fable, autonomous overnight run)

**Context:** Aaron granted full autonomy ("pre-approve all bash", "going to bed"). Single-committer; every change parse-gated (`node --check` on extracted `<script>`), invalid-hex-grepped (`0x[0-9a-fA-F]*[g-z]`), and — for anything visual — screenshot-verified. Classic (2D) never regressed (verified pixel-identical after the renderer edits). File: `civil_war_generals.html` → **10,282 lines / 425 KB**.

## THE UNLOCK — Fable can now SEE the WebGL renderer
The Playwright **MCP** the kickoff assumed is **not exposed to this session**, and no Playwright browsers were cached. Worked around it: **`tools/shot.mjs`** drives the *installed* Google Chrome via `playwright-core` (small JS driver, **no browser download**) with SwiftShader software-WebGL flags, dismisses the "The Ground" overlay, polls `__M3D.ready`, screenshots + dumps console. One command (`node tools/shot.mjs [scene…]`) re-shoots any named scene in `tools/shots.json` → real visual regression. This is both Phase 0's eyes-on mechanism AND its stretch goal. `playwright-core` added as a **dev** dep (not loaded at runtime).

## SHIPPED (all gated)
**Phase 1 — Modern 3D baseline tuned** (engine core, modern-only `_m3d*`; Classic untouched):
- Closed terrain gaps (cylinders were using the hex *inradius*; `CylinderGeometry` radius = *circumradius* = `HEX`).
- Units sit on the ground (shared `_m3dTileH(elev)`; fixed the float where units were at `elev*EH` ≠ tile top).
- Camera pulled in to a ¾ battlefield angle (fills the frame).
- Token legibility: figure-ish body + contrasting cap + side-colored ownership disc + bigger flag → US/CS instantly distinct.
- Grounding sun-shadows (PCFSoft, frustum framed to the field); +elevation drama; subtle per-tile color variation.
- Before/after: `tools/shots/baseline-modern-antietam.png` → `after-modern-antietam.png`. Verdict logged in `PLAYTEST-LOG.md`.

**Phase 3 — Modern fidelity scaffolding (no Aaron assets needed):**
- **3a painted billboards** — reuse the 2D sprite-theater regiment art (`_buildSprite().cv`) as camera-facing `THREE.Sprite`s (transparent cutout) over the ownership disc. Fidelity ladder per unit: `.glb` model → billboard → token. `milestone-billboards-overview.png` + `milestone-billboards-hero.png` (close-up shows ranks of soldiers + flags). **This is what flips Modern from "tech demo" to "worth it."**
- **3b loaders** — optional GLTFLoader (`assets/3d/models/units/*.glb`) + RGBELoader (`assets/3d/env/sky_day.hdr`) load alongside Three core; failures can't abort Modern; per-asset probe → cache-absent → fallback. Drop a file in → it lights up, no code change. (The ~7× `404` at battle start is the *expected* one-time probe; no `pageerror`.)
- **3c mobile tier** — `_m3dLowTier()` (explicit `gfxQuality` wins; auto → Low on ≤720px / coarse-pointer): pixelRatio 1, no AA, no shadows, fewer figures. Settings → Graphics → **3D Quality (Auto/High/Low)** live-applies. Verified `modern-antietam-low.png`.

**Phase 2 T2 — three pantheon maps authored** (Fable, web-knowledge + Antietam `AUTHORED_MAPS` pattern; Verified/Inferred tagged; generators in `tools/build_*.mjs`, row-width validated):
- **Gettysburg** (22×20) — the fishhook (Culp's/Cemetery Hill/Cemetery Ridge/Round Tops vs Seminary Ridge; Peach Orchard/Wheatfield/Devil's Den; the Angle). 4 objs, 12 features.
- **Shiloh** (22×20) — Tennessee River back, Owl/Lick Creeks, Corinth Rd, Hornet's Nest/Sunken Road, Peach Orchard, Bloody Pond, Shiloh Church. 4 objs, 11 features.
- **Fredericksburg** (22×20) — Rappahannock + pontoons, the killing-ground plain, Sunken Road/Stone Wall at Marye's Heights, Prospect Hill. 4 objs, 10 features.
- Each renders in **Classic AND Modern**, deploy zones place forces correctly, "The Ground" panel shows. `milestone-<battle>-{classic,modern}.png`.

**Phase 0b — Blender MCP prereqs:** installed `uv`/`uvx` (`~/.local/bin`), fetched the addon (`tools/blender_mcp_addon.py`), wrote `BLENDER-CONNECT-STEPS.md`. **Blender is NOT installed** → MCP can't run; noted + continued (didn't block).

**Phase 4 — hosting scaffold (local-only):** `index.html` redirect, `tools/deploy.sh` (default-safe; `remote` is HALT-gated), `.gitignore`, local `git init` + staged (no commit, no remote). `gh` present. Steps in `DEPLOY.md`.

## Screenshots (tools/shots/)
`baseline-*` / `after-*` (Phase-1 before/after), `milestone-billboards-{overview,hero}`, `milestone-{gettysburg,shiloh,fredericksburg}-{classic,modern}`, plus live scene shots. Re-shoot all: `node tools/shot.mjs`.

## HALT / Aaron's call
- **PHASE4_HALT.md** — publishing to the web (public repo + Pages) is yours to trigger; scaffold is ready (`DEPLOY.md`).
- **Blender** not installed (`BLENDER-CONNECT-STEPS.md`) — optional; nothing blocked.
- **3D assets** (Meshy `.glb`, Poly Haven HDRI/PBR) — loaders are wired and waiting; drop into `assets/3d/` per `3D-ASSET-PLAN.md`.

## DEFERRED (deliberate, with Why)
- **T1 Systems executor wave (War Room / 1864 Clock / Muster Roll) NOT dispatched.** Why: splicing executor-generated *new systems* into the canonical file unattended violates "own every merge / re-derive every claim" when Aaron can't review it, and several v1.5 systems lack a locked design spec (designing them is Aaron's territory). Chose three fully-verifiable maps instead. Contracts to pre-flight next session noted in `NEXT-CHAT-PROMPT.md`.

## Notes / honesty
- The pre-session original `civil_war_generals.html` was edited before `git init`, so git's baseline is the (verified-good) edited file; the untouched pre-session original lives in the `Documents/Claude/Projects` archive copy. Going forward, local git is the history.
- All renderer edits were direct-to-engine-core (I own the merge), not append-override — appropriate for tuning the renderer I authored this session; map data uses the existing `AUTHORED_MAPS` literal.
