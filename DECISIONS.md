# DECISIONS LOG — "The Civil War" autonomous build run

Per Aaron's locked operating parameters (run i, 2026-06-13): **run the whole arc · always decide & log · auto-commit AND push at gated milestones · content-complete each phase.** Every design fork I resolve mid-run is recorded here with rationale so Aaron can override in review. Aaron-locked decisions (via popup) are marked **[AARON]**; ones I made under "always decide & log" are **[CLAUDE]**.

Format: `Dn · [who] · phase · decision — rationale (reversible? / impact)`

---

## Run-i operating parameters — [AARON] (popup, 2026-06-13)
- **D0a** Run scope = **run the whole arc** (S0→S5 then tactical), stop only at an unresolvable fork / unfixable gate failure / done; auto-handoff at context pressure.
- **D0b** Fork policy = **always decide & log** (never pause mid-run; this file is the log).
- **D0c** Commits = **auto-commit AND push** at each gated milestone (clear message + RUN-LOG entry).
- **D0d** Content = **content-complete each phase** (real citation-grade data + PD images per system; slower per phase, less rework). NOTE: the S0 cabinet is the one explicit exception — Aaron chose engravings-now / PD-photos-in-S2 in the same popup round (D4); the content-complete policy governs S1 onward.

## S0 — President's-Desk shell + build system
- **D1 · [AARON]** Build system = **frozen `build/base.html` (byte copy of the run-h monolith) + ordered `src/*.js` modules + zero-dep `tools/build.mjs`** that concatenates + splices before the unique engine-end anchor and re-emits `civil_war_generals.html`, gated in-memory (parse/hex/collision) before writing. — Honors the handoff's "modularize via a zero-dep concat build step, keep the current HTML as the base." Classic stays frozen (base never edited). (Reversible.)
- **D2 · [AARON]** Desk structure = **expand the War Department in place** — `openWarDept` redeclared to render the President's Desk; its 1864 Clock / Muster Roll / War Room become tabs alongside new The-War-Effort / Cabinet / Theater-Map tabs. One owner-mode screen, no parallel system (§1/§11). (Reversible.)
- **D3 · [AARON]** Strategic-turn surface = **auto-surface, one-click skip** — a "To the Executive Mansion" interstitial between battles (wraps `openUpgrade`); Continue proceeds to the Quartermaster, or open the desk. (Reversible.)
- **D4 · [AARON]** Cabinet (S0) = **stub now with procedural engraved portraits** (4 named secretaries per side); real advisor auto-manage/teach (R25) + PD photos in S2. (Reversible.)
- **D5 · [CLAUDE]** Main-menu button relabel **DEFERRED to S1** — `openMainMenu` is a 124-line broadsheet builder; redeclaring it to rename one cosmetic button (`#gnWarDept` → "The President's Desk") is the highest-churn override in S0 for a purely visual gain. The existing button already opens the (now-expanded) desk; the desk is also reachable from the interstitial. — Risk/benefit. (Reversible; do in S1 once stable.)
- **D6 · [CLAUDE]** Keep the generated `civil_war_generals.html` **committed** (not gitignored) — the deliverable premise is open-and-play from a fresh clone with no build step for the player; the build is a developer tool. `build/base.html` is the source of truth for the frozen foundation. (Reversible.)
- **D7 · [CLAUDE]** **No `_SAVE_VER` bump** for S0 — new `G.campaign.president` is additive plain data that rides `serializeSave` automatically; back-compat = idempotent `presInit` (lazy migration on next desk-open or battle-resolve). Bumping the version would silently erase every existing ver-1 save. (Reversible only forward.)
- **D8 · [CLAUDE]** Strategic-date cadence = **~1 month per battle placeholder**, synced up to the 1864 Clock's year. — S0 is a shell; real variable pacing (weekly in season, compressed in winter, §22) lands with the strategic-turn-loop content in S1. (Reversible.)

## S1 — economy (content-complete from research)
- **D9 · [CLAUDE]** Economy content gathered via a **citation-grade research workflow** (8 agents) → `HISTORICAL-DATA-ECONOMY.md` (digest + 28 debate cards) + `data/economy.json` (game-ready params). — Honors "content-complete each phase." (Source of truth on disk.)
- **D10 · [CLAUDE]** Build is now **data-driven**: `tools/build.mjs` injects `data/*.json` as a `GAME_DATA` global so `data/economy.json` is the single source of truth while the deliverable stays single-file. — Cleaner than inlining params in modules; sets up all of S1. (Reversible.)
- **D11 · [CLAUDE]** S1a = **the finance core first** (the spine + the most teachable system): three funding levers (bonds/taxes/printing), an EMERGENT inflation model (thin CS bond absorption + ~0.10 tax efficiency force the printing press → compounding spiral; US bonds+taxes cover demand → anchored), inflation→`clock.weariness` interlink. Production/cotton-blockade/manpower are S1b–S1d. — The asymmetry must be emergent from real params, not scripted (R29). (Reversible.)
- **D12 · [CLAUDE]** Treasury is **delegated to the Secretary by default** (auto-managed historical mix), opt-in to self-manage (balance principle §27). New "The Treasury" tab (7th desk tab). (Reversible.)
- **D14 · [CLAUDE]** S1b production layer **EXTENDS the War Room** (reads `wr.nodes` as inputs; does NOT redeclare wr → zero regression risk) rather than a parallel system. Asymmetric matériel: US well-found; CS throttled by iron ceiling + import-dependence + IRREVERSIBLE rail decay (~7%/turn) → ragged/hungry armies, emergent. Surfaced in the War Effort overview (no new tab). Probe-verified: US rail100/equip100/arms85 vs CS rail42/equip24/arms4/food19%. (Reversible.)
- **D15 · [CLAUDE]** Local commits only, **no GitHub push** — Aaron confirmed (run i): "just use local commit, should be fine not pushing to github." Supersedes the D0c push intent. (Reversible.)
- **D13 · [CLAUDE]** Inflation **calibration**: per-turn `_ecDEMAND=100`, US `inflCoeff≈0.06`/cap 4%, CS `inflCoeff≈0.18`×`spiralCoefficient^hiPrintTurns`. Probe-verified over 12 turns: US ×1.13, CS ×87.5 (≈ historical ~90× target), ratio 1:77. These are calibration starts per `economy.json` designerGaps — playtest to tune; turnCount still unset (rates documented for a 16-turn war). (Reversible.)

## S1c — cotton / blockade / foreign recognition
- **D16 · [CLAUDE]** S1c **EXTENDS, does not redeclare**: new `src/60-blockade.js` + `G.campaign.blockade` (sibling state); the production `importFactor` placeholder is wired by EDITING the `prodOnResolve` body (a src module I own, not the frozen base) to read `C.blockade.importFactor` with the static `cfg.importFactor` as fallback — no new manifest override. `blockadeOnResolve` registered in the existing `_t1Resolve` override BEFORE `prodOnResolve` so the fresh importFactor gates CS arms the same turn. — Lowest-regression wiring; zero new overrides. Probe-confirmed off>full arms. (Reversible.)
- **D17 · [CLAUDE]** The 1861 cotton **self-embargo is ON by default for CS** (historical: planters/committees drove it) and **auto-expires at year≥1862** (Richmond's hunger for hard currency overrode coercion). Player can lift it early via the Diplomacy tab. Recognition gained while the embargo is active = 0 (the trap: coercion against a glutted Britain bought nothing). — Models the self-sabotage as a *tempting* lever that quietly bleeds the treasury (per the `cotton-self-embargo` card takeaway), not a scripted penalty. (Reversible.)
- **D18 · [CLAUDE]** Foreign recognition is **foreclosed at year≥1863** (decay 0.5, boosts→0), with a 1862 window where it rises (Lancashire famine + early CS wins) then collapses. The S1c numbersAudit endorsed exactly this ("model a recognition window that narrows sharply after Sep 1862 and effectively closes by spring 1863," not a hard Proclamation cutoff). Recognition pulls `clock.intervention` toward itself (×0.30/turn) so even CS battlefield wins can't conjure intervention. — Honest contingency: the one real window, then the door shuts. (Reversible.)
- **D19 · [CLAUDE]** **Erlanger correction applied to `data/economy.json`**: `netProceedsPct` 0.72 → **0.45**, added `issuePriceToErlanger:77` / `publicOfferPrice:90`. The S1c numbersAudit (Gentry 1970) found 77 was Erlanger's BUY price (90 the public offer), and after the post-Vicksburg/Gettysburg slide + Confederate price-propping repurchases the net realized was ~£1.4M of £3M (~45%, not 72%). The Erlanger LEVER itself is deferred to S1d/S2 finance; the corrected figure is on disk for when it's built. Audit also flagged the `cottonBlockade.aggregateExportThroughputVsPeacetime: 0.11` as muddled (true blockade-run cotton to Europe ~3-4% of normal), but the sim doesn't read that field — the revenue-collapse curve (129→53) carries the teaching point; flagged for playtest, not changed. (Reversible.)
- **D20 · [CLAUDE]** **Citation-grade content via an 11-agent workflow** (5 research → 5 adversarial-verify → 1 numbers-audit) → `data/diplomacy.json` (multi-voice cards: consensus + scholarly dissent + named-and-countered Lost Cause + primary sources, Verified/Inferred provenance) + `HISTORICAL-DATA-DIPLOMACY.md`. The Verify pass removed 3 fabricated/misattributed citations (e.g., Egnal's *Clash of Extremes* mis-cited; a fabricated "Weidenmier & Brown" co-author). — Honors content-complete + the adversarial-verify quality bar. (Source of truth on disk.)

## S1d — manpower / conscription (the war-ender)
- **D21 · [CLAUDE]** S1d EXTENDS: new `src/70-manpower.js` + `G.campaign.manpower` (sibling); `manpowerOnResolve` registered in `_t1Resolve` AFTER prod (reads `B.casualties[side]` + the year). No new manifest overrides. **`C.manpower.strength` (0-100) is the army-strength index the S5 pre-battle bridge will read** — S1d produces the strength number that conditions battles. (Reversible.)
- **D22 · [CLAUDE]** The **replacement-ratio collapse is the war-ender**, modeled as the decisive asymmetry: US holds 1.0 (deep refillable pool); CS falls **0.9→0.6(1863)→0.3(1864)→0.1(1865)** by year. The S1d numbersAudit marked the precise ratios DISPUTED ("DIRECTION confirmed; precise ratios not a published statistic") — so they stay **calibrated designer values** (economy.json `replacementRatio._inferred:true`), directionally correct. Probe-verified: CS strength 99→53 (melting), pool→0 by 1865; US strength holds 100. (Reversible — playtest to tune.)
- **D23 · [CLAUDE]** Mechanics from the verified data: **Union draft = a coercive STIMULUS** (×1.2 volunteer inflow at 1863), NOT a direct manpower add — only ~6% of Union soldiers were direct draftees (Geary; audit CONFIRMED); **CS draft load-bearing** (1862, a year earlier); **USCT one-time +180k pool at 1863** (a new pool from the South's labor base); **immigrant inflow US-only** (~25% foreign-born). CS age-band widens 18-35→17-50 (1864) as a visible crisis 'tell'. Surfaced as a **"The Ranks" overview block** in the War Effort tab (NOT a 9th desk tab — tab economy; pairs with the production block), delegated to the Sec. of War, ambient teaching line (R26). (Reversible.)
## S1e — Paths to Victory (the South can win; performance, not script) — [AARON directive]
- **D25 · [AARON]** **The asymmetric collapse must be EMERGENT, not scripted.** Aaron caught that S1c/S1d keyed the Confederate death spiral to the calendar (ratio by year, recognition foreclosed 1863, ports fall on schedule), making defeat feel inevitable — contra §5 (deterministic by performance), §8 (the South wins by not losing), §29 (fair sandbox). FIX: `src/80-victory.js` adds `vicMomentum(C)` (a 0..1 war-fortunes signal from win-rate/will/capital); blockade + manpower now read it. **Winning → desertion ~0, ranks refill, ports hold, recognition stays open.** History is only the default-if-losing. Probe-verified (winning South: str 93/desert 9/ratio 0.83/ports held; losing: str 38/desert 297/ratio 0.22/foreclosed). (Reversible — but this is the locked design intent.)
- **D26 · [CLAUDE]** The replacement-ratio year curve became a "if losing" FLOOR (raised 1865 floor 0.1→0.18 so a passive South isn't instant-doomed) that momentum lifts toward 0.95; desertion = `(1-momentum)^1.6 * 16`. New `C.strategy.enemyWill` (the opponent's resolve) — winning erodes it → at ≤30 (1864+) the **negotiated-peace victory** becomes reachable (`victoryReady`). The S5 win-screen reads this. (Reversible.)
- **D27 · [CLAUDE]** **Executable Southern counter-levers** (new "Paths to Victory" 9th desk tab, side-aware): invest in blockade-runners (+importFactor), commission commerce raiders, fortify ports (hold past historical fall), pursue recognition (keep window open), deserter amnesty, arm-and-free the enslaved (Cleburne, +600 pool +0.3 ratio floor, one-time legitimacy shock). The US gets a mirror (Conquer / Hold Political Will / Strangle by Blockade). — Gives the player real avenues to execute each path (Aaron). (Reversible.)
- **D28 · [AARON]** **Wild-card catalog — alt-history gambits for BOTH sides, tiered plausible→long-shot→fantastical/preposterous** (Aaron: "creative and absurd to preposterous okay if I'm entertained… this is a game"). 11 CS (Trent crisis, invade North, burn-cotton, Cleburne, repeater smuggling, Northwest Conspiracy, Hunley fleet, Maximilian's legions, Stonewall-lives, **the Decapitation Plot** [momentum-gated: lands or backfires], Arizona gold) + 10 US (Hard War, radical 1861 emancipation, Anaconda overdrive, repeater army, Grant-early, greenback firehose, Gatling legions, Russian alliance, rail artillery, the general strike of the enslaved). Each one-shot, effect in `_vicApplyWild`. (Reversible; expandable.)
- **D29 · [CLAUDE]** probe-manpower/probe-blockade reframed to drive a LOSING South (the historical-default trajectory) for their collapse assertions; the WINNING-escapes case is proven by the new `tools/probe-victory.mjs` (12/12). (Test-only.)

## S1d — manpower / conscription (the war-ender)
- **D24 · [CLAUDE]** Manpower teaching content via an 11-agent workflow → `data/manpower-teaching.json` (`GAME_DATA["manpower-teaching"]`; 5 multi-voice cards — Geary/Murdock/Moore/Anbinder/Lonn/Glatthaar/Levine; Black agency front-and-center, Lost Cause erasure countered) + a 9-claim audit (7 CONFIRMED, 1 DISPUTED [ratio direction], 1 CORRECTED [date precision: CS Black-soldier statute Mar 13 vs GO No. 14 Mar 23 1865 — `economy.json` already lists "Mar 13/23 1865", no change needed]). Verify pass removed a misattribution (Fry's Final Report is a standalone 1866 doc, not OR Series III). **Unlike S1c, no sim data correction was required.** (Source of truth on disk.)

## D30 — OVERNIGHT AUTONOMOUS-RUN CHARTER — [AARON] (7 popup rounds, 2026-06-13)
The standing authorization for all-night unsupervised runs. Full text lives in `AUTONOMOUS-RUN.md` §8 (the operating manual reads it first each run). Captured here so the decisions log is complete. All 20 are **[AARON]** unless noted; Aaron overrode the recommended option on three (marked ⚑).
- **D30.1** Scope order = battle layer → S2–S5 → tactical P0–P5.
- **D30.2** Frozen-engine overrides AUTHORIZED (new modules only, gated by `diag-classic`, revert on regression).
- **D30.3** Halt only for irreversible/destructive OR money/external-account actions; else decide-&-log and never stop.
- **D30.4** GitHub backup AUTHORIZED — PRIVATE repo `History-Video-Game` under `adhanke-star`, push every committed milestone. (The one pre-cleared external action; supersedes D15's no-remote for this project going forward.)
- **D30.5** `_SAVE_VER` bumps OK with idempotent lazy migration (D7 pattern); prefer additive.
- **D30.6** Compute UNLIMITED (ultracode aggressive).
- **D30.7** Auto-tune balance via sim sweeps + log every changed number.
- **D30.8** Visual art = code-only light touch (CSS/SVG/procedural); NO Blender/3D/external pipelines unattended.
- **D30.9** Adversarial bug-hunt before EVERY commit + full no-regression suite.
- **D30.10** Tactical engine starts only after battle layer + S2–S5.
- **D30.11** Backlog cleared ⇒ polish/deepen existing design; don't invent major new systems.
- **D30.12 ⚑** Morning goal = MAXIMUM SYSTEMS SHIPPED (breadth), each verified — Aaron chose breadth over the recommended "playable end-to-end war" (but D30.19 auto-resolve still delivers a playable loop cheaply).
- **D30.13** Handoff = live `RUN-LOG.md`/`DECISIONS.md` + a root `WAKE-UP.md` (what shipped / what's playable / demo click-through / open Qs / flags) each session.
- **D30.14** History stance = unflinching, scholarly, anti-Lost-Cause; CS playable without endorsement.
- **D30.15 ⚑** Difficulty = ACCESSIBLE default; presets HARDEN for experts — Aaron chose accessible-default over the recommended punishing-default.
- **D30.16** Conflicting on-disk numbers ⇒ auto-correct to better-sourced value + log citation (Erlanger pattern).
- **D30.17** Player voice = period-flavored but tight; depth opt-in.
- **D30.18** Accessibility = FULL WCAG 2.2 AA, built-in; run `wcag-auditor` on new UI (hold Aaron's MJI bar).
- **D30.19** ADD auto-resolve from the bridge (conditioned army + variance) so the war is playable end-to-end now; keep the option to fight the tactical battle. (New item, folds into A6.)
- **D30.20 ⚑** Onboarding = build a FULL guided tutorial — Aaron chose full over the recommended light/skippable (keep it skippable for experts regardless).

## D31 — DESIGN-SCOPE DECISIONS — [AARON] (popup rounds 8–9, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum. All chose maximal scope (consistent with D30.12 "max systems shipped").
- **D31.1** Theaters in order: Eastern → Western core → Trans-Miss → coastal/naval.
- **D31.2** FULL historical cabinet per side, incl. churn (each: voice/recs/ambition/teaching).
- **D31.3** Start modes = 1861 campaign + scenario hinge-starts + full-sandbox toggle (all three).
- **D31.4** Named-generals system (traits/reputation/ambition) → bridge leadership facet + S2 morale + promotions (replaces the leadership placeholder).
- **D31.5** Home-front = curated conditional event deck (real choices, not random noise).
- **D31.6** End-of-war = rich graded after-action (per-domain grades, turn-by-turn divergence, casualties vs historical, citations).

## D32 — CONTENT-SYSTEM DECISIONS — [AARON] (popup rounds 10–11, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 2.
- **D32.1** Emancipation = a dated Union player decision (radical 1861 / historical 1862 / never) trading border loyalty ↔ USCT manpower ↔ foreign opinion ↔ war aims.
- **D32.2** Naval = dedicated system built at theater-expansion (ironclads, commerce raiders, river war) — not now.
- **D32.3** Disease/medical = real attrition driver (~2× combat) + teaching layer (sanitation/hospitals/Sanitary Commission/Clara Barton; USCT toll).
- **D32.4** Cavalry = DEFERRED to a supervised session.
- **D32.5** Logistics = rail/supply-network system (USMRR vs CS decay, depots → bridge supply/fatigue).
- **D32.6** POW = exchange-cartel-collapse thread (1863 USCT fault line) + manpower effect + Andersonville/Elmira.

## D33 — SYSTEMS & ATMOSPHERE — [AARON] (popup rounds 12–13, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 3.
- **D33.1** Hard war = system (Sherman/Sheridan; break enemy will/logistics + honest civilian/enslaved reckoning).
- **D33.2 ⚑** Turn pacing = FIXED ~monthly (Aaron chose simple over the recommended §22 variable weekly/seasonal).
- **D33.3** Reconstruction coda = short honest epilogue (13/14/15th, Foner unfinished revolution), scales to ending.
- **D33.4** CS finance toolkit BUILT (Erlanger loan + cotton bonds + impressment + produce loan + printing spiral).
- **D33.5** Irregular war = contained thread (Mosby; Quantrill/Lawrence; anti-guerrilla policy) — events + small system.
- **D33.6 ⚑** Audio = RICHER (Aaron upgraded from light): period tunes + ambient soundscapes, code-only synth, toggleable/off-by-default.

## D34 — PERSPECTIVES, AI, SCOPE — [AARON] (popup rounds 14–15, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 4.
- **D34.1** ALL FOUR under-told perspective threads (enslaved agency, immigrant/ethnic units, Native nations, women's roles), woven in + sourced.
- **D34.2** Enemy AI = reactive strategist (own choices, sues for peace at broken will, exploits weakness; presets tune sharpness).
- **D34.3** Codex = multi-axis (timeline + topic + person + battle, cross-linked, provenance shown).
- **D34.4** Single-file may grow to several MB (don't trade scope for bytes; keep gates).
- **D34.5 ⚑** Information = mostly transparent (Aaron chose this over the recommended fog-of-war toggle).
- **D34.6** Replay = alt-history gallery + teaching milestones.

## D35 — SAVES, A11Y, PRESENTATION — [AARON] (popup rounds 16–17, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 5.
- **D35.1** Saves = autosave + named slots + undo-last-turn on accessible difficulty (off on harder/ironman).
- **D35.2** A11y = all four modes on baseline AA (high-contrast theme, CVD-safe palettes w/ patterns+labels, screen-reader ARIA-live narration, dyslexia-friendly font/spacing).
- **D35.3** Inline glossary = every specialized term → definition + provenance, linked to codex.
- **D35.4** Devices = responsive desktop-first; tablet/phone playable + touch.
- **D35.5** Art direction LOCKED = period broadsheet/engraving aesthetic (procedural engraved portraits, broadsheet type, antique-chart maps; CSS/SVG/canvas).
- **D35.6** Human cost rendered with gravity (numbers + regiment losses + occasional named soldier/letter + scale made legible; restrained, no spectacle).

## D36 — CONTENT STANDARD, VICTORY, TUTORIAL — [AARON] (popup round 18, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 6.
- **D36.1** Provenance: Verified needs ≥2 independent reputable sources; single-source/own calibration = Inferred; nothing untagged; adversarial-verify strips fabrications.
- **D36.2** Victory = multiple honest paths per side incl. negotiated peace (CS: exhaust Northern will / recognition / hold to 1865; US: conquest / break rebellion / CS political collapse); hard-loss = military destruction.
- **D36.3** Tutorial = interactive guided scenario (a real 1861 slice), learn-by-doing, optional why-callouts, skippable.

## D37 — ADVISOR UX, SCENARIOS, THRESHOLDS — [AARON] (popup round 19, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 7.
- **D37.1** Advisor UX = one-line rec + one-click accept/delegate + expandable why; ambition surfaces on self-interest.
- **D37.2** Build all four scenario hinge-starts (Antietam/recognition 1862; Vicksburg 1863; 1864 election eve; First Bull Run 1861 = tutorial).
- **D37.3** Win thresholds = accessible default (paths genuinely reachable), harden via presets.

## D38 — PRESS, COMMAND, UNITS — [AARON] (popup round 20, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 8.
- **D38.1** Press = public-opinion system (papers react → public morale + 1864 election; censorship/Vallandigham lever; ties broadsheet UI + 3-layer morale).
- **D38.2** Command = appoint/promote/relieve generals (McClellan problem, political generals, fallout) → bridge leadership facet.
- **D38.3** Flagship named units (54th Mass, Iron Brigade, Irish Brigade, Stonewall Brigade) with identity/history/teaching; ties human-cost + perspectives.

## D39 — MODES, MORALE, CIVIL LIBERTIES — [AARON] (popup round 21, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 9.
- **D39.1 ⚑** Play modes REVISED by Aaron: General+Commander = ONE battle/ops mode; President = full grand-strategy mode; Historian = a settings/realism/teaching LAYER over either mode, NOT a standalone mode. (Supersedes the R27 four-preset framing.)
- **D39.2** 3-layer morale = troop + leader-reputation + public-will, interacting + visible; feeds bridge + 1864 election.
- **D39.3** Civil-liberties = real dilemmas with costs (habeas corpus, Vallandigham/Copperheads, draft enforcement, censorship); both Lincoln and Davis.

## D40 — DIPLOMACY, THE WEST, WAR FINANCE — [AARON] (popup round 22, 2026-06-13)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 10.
- **D40.1** Diplomacy = a real system (Mason/Slidell, Trent war-scare, Laird rams, Russian fleet 1863, cotton vs King Wheat) feeding clock.intervention; builds on S1c.
- **D40.2 ⚑** Western theater = SAME mechanics + new maps/battles (Aaron chose reuse over the recommended distinct Western texture).
- **D40.3** War finance = civic+political (Jay Cooke bond drives, first income tax, greenbacks vs gold, taxation politics); Union popular finance vs CS failure.

## D41 — BATTLE ROSTER, MORALE RESOLUTION, CADENCE — [AARON] (popup round 23, 2026-06-14)
Full text in `AUTONOMOUS-RUN.md` §8 addendum 11.
- **D41.1** Build next: Antietam, Vicksburg, Chickamauga/Chattanooga, Atlanta/the March (on top of existing Bull Run/Shiloh/Fredericksburg/Gettysburg/Malvern/Franklin).
- **D41.2** Morale resolution: troop morale → battle (bridge); public will → strategic outcome (1864 election, negotiated peace); leader reputation modulates both.
- **D41.3** Event cadence: 1–2 meaningful choices/turn, more at hinges.

## D42 — A1 CANNON CORPS: hardening + the CS artillery asymmetry made mechanical — [CLAUDE] (run j, 2026-06-14)
Finishing the scaffolded A1 Cannon Corps. An adversarial bug-hunt workflow (5 lenses × independent verify, 21 agents) confirmed **9 real findings** (7 correctly refuted as unreachable/cosmetic); all 9 fixed in `src/56-artillery.js` + `data/artillery.json`.
- **D42.1 (save integrity)** `artInit` now guards arrays explicitly (`Array.isArray`) at BOTH the `C.artillery` and `.batteries` levels — `typeof [] === "object"` let a corrupt/imported array survive, and a JSON round-trip serializes an array as `[]` and drops every named property → the whole park silently wiped on the next save. Battery COUNTS are now coerced/sanitized at the same chokepoint (`"3"`→3; drop null/NaN/objects/≤0).
- **D42.2 (the balance bug)** the 4-vs-6 guns-per-battery handicap was **purely cosmetic** (shown in the UI, zero mechanical effect — CS and US scored identically). `artBatteryScore` now scales coverage by the side's gun ratio (CS 4/6 ≈ 0.667), so a CS battery fills the park ~2/3 as fast; the South needs ~18 batteries to match a full Union park.
- **D42.3 (CS stacking penalties — designer calibration, Inferred)** the STRATEGIC pre-battle score now also models, CS-only: fuze unreliability ×0.96 (expected-value haircut for shell/case duds; solid shot & canister immune), horse/forage mobility ×0.95 from 1862 / ×0.88 from 1864, mixed-caliber resupply ×0.94 at 3+ calibers; plus the **1863 massing reform** as a both-sides bonus (US ×1.08 / CS ×1.04 — the Union's larger factor stands in for the army-level Artillery Reserve the ANV abolished after Chancellorsville). The per-shot fuze roll, the Whitworth ammunition cap, and gun-loss-on-retreat are **DEFERRED to the battle-day layer (A6) / tactical engine (P-phases)** where they belong as live rolls, not score haircuts. `_designNote` updated to record implemented-vs-deferred.
- **D42.4 (Whitworth import)** the rare British import now costs BOTH sides a premium (CS ×1.5 blockade / US ×1.2 foreign-import) instead of the Union buying the catalog's best gun at base price; `csOnly` kept false (both sides fielded a handful).
- **D42.5 (hardening)** `_artCost` honors a configured cost of 0; no-`id` guns are skipped in scoring + refused at purchase; `_artEsc` escapes data fields before `innerHTML`; `artBuy` coerces `n` via `Number()`; the baseline is clamped into [0,100]. *(The parallel unescaped-data gap in `55-weapons.js` is the same latent, non-triggering issue — flagged as a follow-up hardening item, not fixed in this A1-scoped commit.)*
- **Verified:** probe-cannon now **9/9** (added a US>CS divergence assertion that also asserts CS isn't crushed to baseline — accessible default per D35/§8.15); full no-regression suite green (diag-classic Classic paints, desk 12/0, weapons 8/0, bridge 6/0); 0 pageerrors. Sample (1863, 6× howitzer12 Q58): **US 44 vs CS 33**; bridge artillery facet 8→63 lifts overall 76→83.

## D43 — A2 ENGINEERING WORKS CORPS: buildable engineer arm + the rail-repair-that-can't-rebuild — [CLAUDE] (run j, 2026-06-14)
The Engineering Works Corps — the third buildable in The Armory (after small arms + the Cannon Corps). Four capabilities raised in levels 0-3 with funds: Construction Corps (Haupt's USMRR rail/bridge repair), Field Fortifications (the spade), Pontoon Train (river crossings), Siege Works (saps/parallels). New `src/57-engineering.js` + `data/engineering.json` + `C.engineering`. Feeds the battle bridge as an additive `engineering` facet (weight 0.10); construction raises the supply facet, pontoons lower fatigue; the Construction Corps claws back a FRACTION of each turn's rail loss (slows but never stops the CS rail collapse). CS pays a 1.25× materials premium — Union engineering vastly outstripped the South's (the USMRR rebuilt faster than the CS could wreck).
- **Content:** an 8-agent research+verify workflow → **4 Verified multi-voice teaching cards** (Haupt/USMRR; the spade & fieldworks; pontoon mobility; siege engineering). Anti-Lost-Cause: Solonick's "Vicksburg was dug out, not starved out"; the rifle-musket-vs-entrenchment debate surfaced (Griffith vs Hess); the Cold Harbor "7,000 in minutes" myth corrected (Rhea, ~6,000 Union for the day); Vauban dated correctly (17th c.). Every citation independently fact-checked; the verify pass fixed real errors (rank anachronisms, Petersburg mileage, the Foote attribution).
- **Mechanics (designer calibration, Inferred):** baseline 12; scorePerLevel construction 8 / fortifications 7 / pontoons 6 / siege 7 (full park ≈ 96); construction supply +4/level; pontoons fatigue −3/level; costPremiumCS 1.25.
- **Adversarial bug-hunt** (5 lenses × independent verify, 19 agents) → **5 confirmed findings, all fixed** (9 correctly refuted as dev-console-only / intended-design):
  - **D43.1 (the real one)** the rail-repair was a CONSTANT additive (`level × 1.6`) against MULTIPLICATIVE decay → a stable equilibrium, and with the rail War-Room node funded it **pinned rail at 100 forever** (and recovered net-positive from a low point) — flatly contradicting the locked "irreversible decay, still declines" intent. **Fix:** `prodOnResolve` now stashes `railLossLast`; `engOnResolve` claws back only `min(0.70, 0.24×level)` of that loss (a sub-1.0 fraction), so the net trend is strictly negative for every decay/level/node combination. Probe asserts rail never pins at 100 and declines monotonically with the rail node maxed (verified: 100→84 with full construction, vs 55 without).
  - **D43.2** `perLevelEffect`/`scorePerLevel` lacked NaN/negative guards (a data typo could NaN or *invert* supply/fatigue) → Number/isFinite/floor-guarded; a configured `scorePerLevel` of 0 is now honored.
  - **D43.3** boolean-in-save coerced to level 1 (`Number(true)===1` → a free capability) → `engInit` rejects non-number/string before coercion. **Parity fix applied to `artInit` (A1) too.**
  - **D43.4** `_engEsc` didn't escape quotes but `b.id` sits in a `data-engbuy="…"` attribute → quote-escaping added. **Parity fix to `_artEsc` + the `data-artbuy` attribute (A1) too.**
  - **D43.5** a bare `&` in static render text → `&amp;`.
- **Verified:** probe-engineering **9/9** (incl. the new equilibrium-regression assertion); full no-regression green (diag-classic Classic paints; production/cannon/bridge/desk unaffected — the prod `railLossLast` + artillery parity edits caused no regression); 0 pageerrors. Deliverable ≈947KB.

## D44 — A3 TERRAIN COVER HIERARCHY: the first frozen-engine override — [CLAUDE] (run j, 2026-06-14)
The first frozen-engine override under §8.2. Six named cover types (Stone Wall, Entrenchments, Sunken Road, Boulders, Dense Forest, Rail Fence) slotted into the engine's EXISTING terrain/defense model. New `src/58-terrain-cover.js` + `data/terrain-cover.json`.
- **Mechanism (NO fire-function override — lowest engine risk):** combat reduces fire damage by dividing by `TERRAIN[tile.t].def`; the `.cover` field was defined but UNUSED. The module MUTATES the frozen consts `TERRAIN`/`TCOL`/`PALETTE` at load (adding keys to a const object is legal) to install the 6 cover types with real `def` values + period colors, so combat applies them automatically through the existing `.def` path. def ladder: stonewall 2.00 > trench 1.95 > sunkenroad 1.85 > boulders 1.70 > forest 1.50 > woodfence 1.25 (between clear 1.00 and works/fort 2.10). A stone wall halves casualties (probe: 100→50).
- **Placement:** OVERRIDES `authoredMap` (verbatim base body + a `tcDecorateMap` pass; added to manifest overrides → 2× collision-gate). `_tcCoverFor` matches feature labels → cover: Sunken Road→sunkenroad, Marye's Heights/Stone Wall→stonewall, Devil's Den/Round Top→boulders, the woods/Wilderness→forest, redan/redoubt/lunette/salient/retrenchment→trench. 11 stamps across 6 of 10 authored maps. Positions already `fort` (def 2.10) — the Vicksburg redans, Antietam's Bloody Lane — are correctly LEFT by the no-weaken guard. Procedural `genMap` battles aren't decorated (already carry woods/town/works cover).
- **Content:** 1 Verified teaching card (cover as graded killing ground — Fredericksburg's stone wall & sunken road; Hess / NPS-ABT / Griffith, the A2-vetted sources).
- **Adversarial bug-hunt** (5 lenses × independent verify, 13 agents) → 2 confirmed findings (both medium; the const-mutation timing + idempotency were probed and confirmed SAFE by the refuted set), both fixed:
  - **D44.1** `"trench"` substring-matched "The Retrenchment" (an accidental hit stamping a road tile). Fixed: dropped bare `"trench"` → `"trenches"` + added explicit `"retrenchment"` (anchored intent — The Retrenchment is a genuine Franklin fieldwork, kept as cover).
  - **D44.2** stamping swaps `tile.t`, which the engine also reads for MOVEMENT cost — and one case (Marye's ridge→stonewall) made high ground CHEAPER to cross, contradicting the "def only" claim. Fixed: stonewall cost 1→2 (a wall is an obstacle) + a **no-speedup guard** (never stamp if `cover.cost < tile.cost`) so decoration never reduces traversal cost; reframed the docs as an honest terrain swap (def↑, cost never↓, never water, preserves objectives/elevation).
- **Verified:** probe-cover **8/8** (def ladder, render colors, label matching incl. a Dunker-Church negative, stamping across 6 maps, Antietam Sunken Road strong-cover, never-weaken, the terrain-swap invariant [11 stamps: 0 weaker def, 0 cheaper-to-cross], combat reduction); diag-classic Classic paints (no regression from the override); full no-regression suite green; 0 pageerrors. Deliverable ≈962KB.

## D45 — A4 WIRE THE ARMORY → THE BATTLEFIELD: the weapons you buy are what your brigades fire — [CLAUDE] (run j, 2026-06-14)
The small arms bought in The Armory (`C.armory.loadout`) become what the player's brigades actually carry and fire. New `src/59-armory-field.js`. Frozen-engine override (§8.2): `genForce`.
- **Mechanism:** a battle unit's firepower is decided entirely by `WEAPONS[u.weapon].pow` (resolveFire ~1037). The module OVERRIDES `genForce` (verbatim base body + a re-arm pass) so a fraction of the PLAYER's fresh infantry are re-armed from the loadout, mapped armory-id → engine WEAPONS key (`smoothbore`→smooth … `henry`→henry, quality-MONOTONIC so a better buy is never a worse field weapon). Buy Spencers → a slice of the line carries "spencer" (pow 2.05 vs the musket's 1.00 = +105% fire). The remainder keeps standard issue; veterans keep their earned weapon; the enemy is untouched (no arsenal to draw on).
- **Adversarial bug-hunt** (5 lenses × independent verify, 16 agents) → 5 confirmed findings (the override fidelity confirmed byte-identical by the refuted set), all fixed:
  - **D45.1 (HIGH — Classic regression)** the re-arm fired whenever `G.campaign` merely existed (gated on `G.campaign.side`), so a **Free Battle with a saved campaign in memory could re-arm the AI ENEMY with the player's arsenal** (or silently self-buff a same-side free-battle force) — breaking Classic/Free-Battle independence from campaign state. Fixed: gate on `campaignCore` (the engine passes it non-null ONLY for the player force in a real `fromCampaign` battle; the enemy force and BOTH Free-Battle forces get null) — the exact discriminator.
  - **D45.2 (MED)** engine-era bypass: the armory's `data/weapons.json` year-gates are looser than the engine's `WEAPONS.era`, so the player could field e.g. Sharps at an 1861 battle the era-locked AI couldn't. Fixed: skip any weapon whose `WEAPONS.era` > the battle year (the bought arm fields only once the engine would issue it — fair vs the AI, no anachronism).
  - **D45.3 (LOW)** float drift: 0.1×10 = 0.9999… → `floor` left one brigade un-armed on a "100%-armed" line. Fixed: snap `fr>0.9995→1` + an epsilon on the floor.
- **Verified:** probe-armory-field **9/9** (monotonic mapping, player-not-enemy re-arm, fraction lower-bound, whole-army float-safe, veterans keep their weapon, Spencer +105% pow, off-campaign no-op, the engine-era gate [1861 Sharps→0 fielded], Free-Battle isolation [`{spencer:1.0}` live but core=null → not all armed = no leak]); diag-classic Classic paints (no regression from the `genForce` override); full no-regression suite green; 0 pageerrors. Deliverable ≈970KB.

## D46 — A5 TARGETING: VERIFIED the player already picks specific fire targets — NO engine change — [CLAUDE] (run j, 2026-06-14)
The backlog asked to "verify the player can pick specific targets + expose/polish it." Investigation + an empirical probe confirm the frozen engine ALREADY supports full player target choice, so A5 ships as a **verification**, not new code.
- **Mechanism (already in the engine):** `fireTargets(u)` (base ~1003) returns every alive, in-range, visible enemy hex; `onHexClick` (base ~1273) fires at the EXACT clicked hex (`resolveFire(sel,c,r)`) when in fire mode — the PLAYER chooses the target, not an auto/best pick (only the AI uses `chooseBestFireTarget`). Valid targets are highlighted (red circle + crosshair, both 2D Classic and 3D); full keyboard aiming (F → arrows → Enter).
- **Verified (probe-targeting 5/5):** two enemies placed in a player unit's weapon range → `fireTargets` returns BOTH (a real choice); entering fire mode populates/highlights `G.fireT`; clicking ONE specific enemy damages THAT enemy (512→483, 29 cas) and NOT the other (no auto-target leak); the firing unit is marked fired; an already-fired unit offers no targets (ammo/turn discipline). 0 pageerrors; the probe doubles as a post-A1–A4 battle smoke test (boot → select → fire → resolve all work).
- **Decision (decide-&-log):** ship NO engine change. The feature is complete and correct; bolting redundant targeting UI onto a working frozen engine would be over-engineering (§27) and needless §8.2 override risk. Optional future polish (a louder target highlight / hover "click to fire" tooltip) is deferred — not worth the risk now. No adversarial bug-hunt run (no new game code to attack; the empirical probe is the verification). Deliverable unchanged; only `tools/probe-targeting.mjs` added.

## D47 — A6a BATTLE-DAY CONDITIONING: the strategic war shapes the army that takes the field — [CLAUDE] (run j, 2026-06-14)
The bridge already COMPUTES the conditioned army (`bridgeArmy`) + prep; A6a APPLIES it. New `src/86-battle-conditioning.js` overrides `startBattleRuntime` (frozen-engine §8.2; verbatim base body + a `_a6Condition()` pass after `G.battle` is built). Campaign battles only (`B.fromCampaign`) — Free Battle / Classic / diag-classic untouched.
- **What it does:** scales the player force by `bridgeArmy(C)` — strength ×[0.88..1.12], morale +(facet−60)×0.3 — and applies `C.battlePrep` (entrench → +2 ent; forcedMarch → −6 morale; raidSupply → enemy −6 morale & −20% ammo). Field Fortifications (A2) → trench cover on ~25%·level of the line when the entrench order is given (the A2→A3→battlefield tie, end-to-end).
- **Adversarial bug-hunt** (5 lenses × independent verify, 15 agents) → 4 confirmed findings (the override confirmed byte-faithful by the refuted set), all fixed:
  - **D47.1 (§27 calibration — the key one)** a fresh campaign's `bridgeArmy.overall` is ~74-76, NOT 50, so anchoring `strengthMul` at 50=1.0 silently buffed every no-investment army ~+10% vs the frozen enemy. Re-anchored at 74 → a fresh army plays ≈ Classic (probe ratio **1.009**); a well-run war earns up to +12%, a failing one −12%.
  - **D47.2 (§27 over-stack)** narrowed the strength band ±20%→±12%, and **gated the auto-trench on the entrench prep order** (a deliberate slot, not a free always-on stack) so the tactical fight still decides at the extremes.
  - **D47.3 (feature gap)** the Field-Fortifications→trench tie was inert on the ~29/40 procedural-map battles (no `M.deploy`); now falls back to the player's own occupied hexes (the line they dig in on).
  - **D47.4** entrenching no longer re-terrains objective tiles (skip `t.obj`; preserve authored terrain).
- **Verified:** probe-conditioning **6/6** (strong>weak in-band; free-battle untouched; entrench +2 ent; raid-supply saps enemy morale; **fresh ≈ Classic, ratio 1.009**; fortifications trench gated on entrench — 68/91 when ordered, 0 without); diag-classic Classic paints (no regression from the `startBattleRuntime` override); full no-regression suite green; 0 pageerrors. Deliverable ≈977KB. **A6b — the bridge AUTO-RESOLVE (playable end-to-end) — is next.**

## D48 — A6b BRIDGE AUTO-RESOLVE: the owner-mode war is playable end-to-end — [CLAUDE] (run j, 2026-06-14)
The §8.19 keystone. New `src/87-auto-resolve.js` + an "Auto-resolve" button in the pre-battle briefing (`src/85`). Resolve a battle from the army your war fielded, without playing the tactical fight; "To the Field" (fight it) remains.
- **Mechanism:** `bridgeAutoResolve` builds the CONDITIONED battle (`startBattleRuntime` → A6a), `bridgeResolveOutcome` decides the result, then it drives the engine's OWN `campaignAdvance` — so an auto-resolved battle reconciles the roster, runs `_t1Resolve` (manpower/clock/strategy/enemyWill/victory), awards funds, and advances the chain IDENTICALLY to a fought one. Seeded RND → deterministic-by-performance (your strategy, not luck, decides it).
- **Adversarial bug-hunt** (5 lenses × independent verify, 24 agents) → 11 confirmed findings (deduped to 6 fixes; the `campaignAdvance` contract + no-field-flash confirmed sound by the refuted set), all fixed:
  - **D48.1 (HIGH — the big one)** the original margin compared the raw 0-100 `overall` ~1:1 vs the enemy, so a competent Union could NEVER lose and auto-resolve was strictly *easier than fighting* (a foregone-conclusion click-through). Re-anchored on the EDGE OVER BASELINE (me−74 vs foe−64 — the same anchors A6a / `_arEnemyRating` use), so a baseline army is a real gamble (losable like Classic) and auto-resolve is outcome-equivalent to fighting. Verified: strong war wins (margin 11), weak war loses (margin −28) — real jeopardy restored.
  - **D48.2 (HIGH — audio)** auto-resolve bypassed `endBattle`, so the battle music/wind bed played over the result/interstitial. Now calls `_audLeaveBattle()` + sets `G.mode="result"` + a win/loss bugle, matching `endBattle`.
  - **D48.3 (MED)** honors `C.flipAtk` (recovery role-flip) exactly as `launchCampaignBattle` — a recovery battle resolves in the correct defend orientation.
  - **D48.4 (MED)** XP parity — a win credits surviving regiments by magnitude (decisive → kills 2 → the +2 XP a decisive win deserves; normal → +1), matching the fought path.
  - **D48.5 (LOW)** symmetric casualty clamps + a winner-shatters-loser floor (decisive 72% / win 42%) so funds / `stats.infl` / enemyWill-erosion rewards match fighting (enemy bleeds ~40% on a win).
  - **D48.6** removed dead `_arSideStrength`.
- **Verified:** probe-auto-resolve **7/7** (strong out-resolves weak WITH jeopardy; both bleed, loser more; kill attribution; flipAtk honored; enemy-rating asymmetry CS 61→45 / US 55→63; `campaignAdvance` advances the war + feeds the tick; briefing offers Auto-resolve + Fight); diag-classic Classic paints; full no-regression green; 0 pageerrors. Deliverable ≈987KB.
- **⚑ THE BATTLE LAYER (A1–A6) IS COMPLETE — the owner-mode war is now PLAYABLE END-TO-END:** run the strategic desk → field a conditioned army (manpower/production/blockade/arms/cannon/engineers/morale) → at each battle auto-resolve or fight → the result feeds manpower/clock/strategy/victory → win via the chain or a path-to-victory. **NEXT: the strategic arc S2–S5** (cabinet/advisors, 3-layer morale, 1864 election, press, command/generals, emancipation, civil liberties, events).

## D49 — S2 m1 THE CABINET & ADVISOR SYSTEM (R25 linchpin / §8.22 / §8.54) — [CLAUDE] (run k, 2026-06-14)
The first S2 sub-system: the full historical cabinet replaces the S0 4-secretary stub. New `data/cabinet.json` + `src/31-cabinet.js` (overrides `presRenderCabinet`/`presWireCabinet`; `cabInit`/`cabOnResolve` registered in `90-president-register.js`). The S0 cabinet research run-id was from a prior session (unrecoverable), so content was researched fresh.
- **What it does:** the FULL roster per side is DATE-AWARE — the current holder of each office is whoever historically held it at the strategic date, so the CHURN is mechanical (Davis's revolving War office Walker→Benjamin→Randolph→Seddon→Breckinridge; Benjamin's three-post rotation War→State; Chase out July 1864; Cameron→Stanton). Each of the 4 principal advisors (war/treasury/state/navy) gives a one-line **recommendation keyed to the war state** (`_cabReading`: inflationHigh / armyWeak / recognitionRisk / blockadeLoose / always), a one-click **Heed** or **Delegate**, an expandable **why** (reasoning + teaching + provenance), and an **ambition tell** that surfaces for self-servers (high-ambition men always; moderate when their domain struggles). Plus a full-roster expander (the churn made legible) and the two cross-cutting teaching cards. Secondary offices (justice/post/interior) are roster/teaching-only.
- **Content:** the `s2-cabinet-research` workflow (21 agents, **web-grounded** + 2-pass adversarial verify) → 18 principal profiles + cross-cards, merged conservatively into `cabinet.json` (structure/tenures/condition-vocabulary preserved; voice/ambition/ambitionTell/stance-prose/cards grafted) then entity-normalized (decode HTML entities in the data so the render layer escapes exactly once — `&amp;`→`&`). Verified, multi-voice (4–7 perspectives/card), anti-Lost-Cause: Benjamin's antisemitic scapegoating, Memminger's structural finance failure ("why the South could not pay for its war"), Davis's micromanagement & states'-rights paralysis as real causes of defeat, the Union team-of-rivals that out-governed Richmond.
- **Mechanics (designer calibration, logged per §8.7):** Heed = a bounded once-per-turn nudge — base **+1 political capital at most once per strategic turn TOTAL** (D49.5 fix) + a per-domain micro-effect (treasury weariness −2 / war supply +2 / state intervention ±2 toward the side's interest / navy: CS importFactor +0.02 capped 1.1, US supply +1). Delegate = the auto-manage flag (flavor in m1; domain systems read it later). Ambition thresholds: ≥75 always-active, 45–74 active-when-domain-bad, <45 never. `cabinetLeadership(C)` (a 0–100 competence index from the sitting War/Navy secretaries, anchored 64) is **exposed but NOT yet consumed by `bridgeArmy`** — wiring + tuning deferred to the generals/command milestone (m5) where leadership is the headline; m1 is therefore a purely additive UI/teaching milestone with **zero battle-balance change** (D49.7 confirmed this by grep).
- **Decide-&-log:** (a) the cabinet Delegate and the per-system delegates (econ treasury) are independent surfaces in m1 — unifying them is later polish. (b) G.W. Smith's ~3-day acting War tenure is omitted from the holder roster (mentioned in card text) to keep date-selection clean. (c) per the owner's directive, **WebSearch/WebFetch grounding is now folded into the content workflows** (this one already was), strengthening the ≥2-source Verified bar.
- **Adversarial bug-hunt** (5 lenses × 3-skeptic independent verify, **62 agents**) → **8 confirmed of 19 raw (11 refuted)**, all fixed:
  - **D49.1 (HIGH — save integrity)** a corrupt/imported save with `president` (or `P.date`) as a non-object primitive throws a strict-mode TypeError in `presInit` (assigning a property to a primitive), aborting the cabinet render. Fixed: `presInit` now coerces a primitive `president` to null (rebuild) and guards `date`/`head`/`cabinet`/`pendingChoices`/`log` against non-object/array shapes; `presRenderCabinet` wraps its init calls in try/catch (defense-in-depth).
  - **D49.2 (MED — silent dead UI)** a `cabinetState` persisted as an ARRAY passed the bare object check, so the per-domain keys cabInit added were dropped on the next JSON round-trip → Heed/Delegate became permanent no-ops. Fixed: `Array.isArray` rejection (matches the artInit/engInit idiom).
  - **D49.3 (MED — teaching accuracy)** `cabOnResolve` named the predecessor from the STALE domain snapshot, so a date that leaps across >1 tenure in one tick (the clock.year sync can jump a year) named the wrong predecessor and skipped offices. Fixed: new `_cabPrevHolder` names the tenure-immediate predecessor.
  - **D49.4 (LOW — content)** the CS Attorney-General had a Nov–Dec 1863 tenure gap; the no-window fallback resurrected Bragg (gone ~20 months) as "in office." Fixed in data: Watts's end extended 1863-10 → 1864-01 to abut George Davis.
  - **D49.5 (HIGH — balance)** cross-domain Heed stacking added +1 capital PER domain PER turn (up to +64 over the war), and capital feeds the 1864 reelection referendum — the engagement nudge could decide reelection over battle outcomes. Fixed: capital gated to **once per turn total** (`P.heedCapTurn`); domain micro-effects unchanged.
  - **D49.6 (LOW — consistency)** navy Heed was pure free capital; gave it a bounded side-appropriate micro-effect (above).
  - **D49.7 (LOW — scope confirm, NOT a bug)** `cabinetLeadership` confirmed not consumed by `bridgeArmy` → zero battle-balance change this milestone (validates the deferral). The exact m5 wiring is recorded.
  - **D49.8 (LOW — latent XSS hardening)** the President's-log render was unescaped `innerHTML`, and the cabinet now writes dynamic dispatch text into it; escaped both render sites with `_cabEsc` (trusted data today, defense-in-depth).
- **Verified:** probe-cabinet **12/12** (incl. 3 new steps locking D49.1/.2/.3/.5); full no-regression green (diag-classic Classic paints; desk 12/12 — updated to the new advisor UX; conditioning/auto-resolve/bridge/economy/victory/blockade/manpower/weapons/production/cannon/engineering/cover/armory-field/targeting unaffected); 0 pageerrors. Deliverable ≈1130KB. **NEXT: S2 m2 — curated decision cards + the `president.pendingChoices` loop (emancipation as a dated decision + civil-liberties dilemmas), `data/decisions.json`.**

## D50 — S2 m2 CURATED EXECUTIVE DECISIONS + the pendingChoices loop (§8.27 emancipation-dated · §8.62 civil liberties · §8.68 cadence) — [CLAUDE] (run k, 2026-06-14)
The S0 `president.pendingChoices` STUB becomes a real, non-blocking decision system. New `data/decisions.json` (an 8-card deck) + `src/32-decisions.js` (the engine). Decisions surface in the between-battles interstitial AND a new **"Decisions" desk tab** (the 11th); each card presents a real choice with honest bounded effects + teaching, or is left for later.
- **What it does:** `decOnResolve` (registered after pres/cab) OWNS the queue — adds eligible cards by trigger at the **§8.68 cadence (≤2 new non-hinge per turn; the hinge always surfaces)**, drops resolved/expired (re-validated through `_decEligible`). `presOnResolve` no longer clears `pendingChoices` (the S0 reset removed; decisions persist until answered). `decResolve` applies each option's bounded, clamped, LOGGED effects (the numbers live in the data; the applier maps field→game state: weariness/capital/intervention/importFactor/strength). The marquee card is **EMANCIPATION** — a dated Union HINGE (the war's sole hinge) with three real options (issue / go-radical-now / refuse), trading border-state loyalty (capital) against foreign opinion (intervention) and USCT manpower. It sets `C.president.emancipation`, which the **manpower module now reads to gate the USCT pool**: issue → unlocks (even pre-1863 if radical); refuse → never; **no decision → the historical 1863 calendar default holds (zero regression)**. Civil-liberties + home-front cards both sides: habeas corpus (US + CS), Vallandigham/Copperheads, the NYC draft riots, press censorship, the Richmond bread riot, the twenty-slave law.
- **Content:** the web-grounded `s2-decisions-research` workflow (17 agents, WebSearch/WebFetch + adversarial verify) → real public sources WITH URLs (National Archives Emancipation Proclamation, Library of Congress, the Freedmen & Southern Society Project, Dickinson's House Divided, the Greeley/Browning/Corning letters, Neely's *The Fate of Liberty*, McCurry's *Confederate Reckoning*, Foner, Oakes). Merged conservatively (the 3 scaffold cards' ids/option-ids/effects preserved so the probe + engine contract held; 5 new cards translated from qualitative tradeoffs into bounded effects), then entity-normalized. Anti-Lost-Cause + Black-agency-centered (Du Bois's general strike; the self-emancipating enslaved forcing the question; the twenty-slave law's "rich man's war").
- **Decide-&-log:** (a) **emancipation is the ONLY hinge** — the research merge marked 6 cards as hinges, which would surface 4+ at once and break the cadence; reset to emancipation-only so cadence stays "1–2/turn, more at the hinge." (b) the cabinet Delegate and decision queue are independent surfaces. (c) the USCT gate keeps the 1863 calendar as the default so old saves + a player who ignores the card behave exactly as before.
- **Adversarial bug-hunt** (5 lenses × 3-skeptic verify, **56 agents**, 3.0M tokens) → **12 confirmed of 17 (5 refuted)**, all fixed:
  - **D50.1 (MED — cross-module save integrity)** `manpowerOnResolve` read `C.president.emancipation` RAW and ticks BEFORE `decOnResolve`, so a corrupt primitive emancipation could mis-drive the one-way USCT latch. Fixed: manpower calls `decInit(C)` first (sanitizes the field → treated as "no decision").
  - **D50.2 (LOW)** `decInit` de-dups `pendingChoices` (corrupt/double-push → duplicate cards with colliding DOM ids).
  - **D50.3 (LOW)** the resolved-history render skips non-string option ids (no "[object Object]").
  - **D50.4 (MED)** `decOnResolve`'s drop-filter delegates to `_decEligible`, so a corrupt cross-side/ineligible id is evicted on the next tick.
  - **D50.5 (LOW)** probe-decisions enforces card-id uniqueness at the gate.
  - **D50.6 / D50.7 (HIGH)** `decResolve` had NO idempotency guard — a card could be re-resolved, re-applying effects and flipping emancipation issue↔decline AFTER the manpower gate irreversibly banked the USCT pool. Fixed: resolved cards are TERMINAL + only a currently-pending card can be resolved.
  - **D50.8 (HIGH — balance)** the applier upper-clamped capital at 100, but the frozen clock treats capital as unbounded-above (and the 1864 referendum reads it) → decisions silently DESTROYED capital above 100. Fixed: capital is floor-only (`Math.max(0, …)`), matching the clock.
  - **D50.9 (LOW)** Array guards on `c.options` (malformed deck degrades, no throw).
  - **D50.10 (LOW)** the strength applier floor aligned to the manpower module's own floor of 5.
  - **D50.11 (LOW)** card/option ids sanitized (`_decIdSafe`) identically on the render-attribute and `getElementById`-selector sides (defensive).
  - **D50.12 (scope confirm — NOT a bug)** decisions are strictly campaign-gated; cannot perturb Classic / Free Battle. Added a save-round-trip + null-field probe step.
- **Verified:** probe-decisions **14/14**; probe-desk **13/13** (incl. the new Decisions tab); probe-manpower **9/9** (the USCT-gate edit no-regression); diag-classic Classic paints; full no-regression green; 0 pageerrors. Deliverable ≈1237KB. **NEXT: S2 m3 — 3-layer morale (troop/leader/public-will-decides-late) wired to the 1864 election hinge.**

## D51 — S2 m3 THE THREE-LAYER MORALE SYSTEM + the 1864 election hinge (§8.61/D39.2 · §8.67/D41.2) — [CLAUDE] (run k, 2026-06-14)
Three interacting, visible morale layers + the home-front-decides-the-war wiring. New `src/33-morale.js` + `C.morale`; surfaced as a **"The Nation's Will"** block in the War Effort overview.
- **What it does:** `moraleCompute(C)` derives three layers fresh each turn from the war state, with the INTERACTIONS in the formulas: **TROOP** morale (battle conditioning via `bridgeArmy.morale` + supply, **lifted by leadership**); **LEADER** reputation (win-rate + the sitting cabinet's `cabinetLeadership`, dragged down by an active ambition tell); **PUBLIC** will (**sunk by the accumulating casualty toll** [D35.6 human-cost] **and runaway inflation**, lifted by victories + the South's hope of recognition, modulated by leadership). The block also shows a **1864-election forecast** mirroring the frozen clock's own support formula.
- **The 1864 election → strategic outcome (D41.2):** the frozen `clkOnResolve` already resolves the referendum from `100 − weariness + capital/2 + winRate·20` (weariness IS the public-will proxy). m3 adds the CONSEQUENCE in `moraleOnResolve` (runs AFTER clk, BEFORE vic): when the verdict lands (once, `electionApplied` latch), it feeds the EXISTING `C.strategy.enemyWill` — **re-elected → enemyWill −8** (a determined administration grinds the enemy down toward your negotiated-peace win); **repudiated → enemyWill +12 + a public-will hit** (the home front broke; the enemy takes heart). So you can win every battle and still lose the war if the public will fails — and the ballot box is the South's surest road to a negotiated peace (§8.52).
- **Decide-&-log:** (a) **morale is a computed READOUT with exactly ONE sim coupling** (the bounded one-time election→enemyWill nudge) — it writes NOTHING to the frozen clock/economy/manpower, so it cannot regress their assertions; the 3 layers are derived, not accumulated (no feedback loop). (b) The election coupling feeds the EXISTING victory module (enemyWill→victoryReady, D26) rather than a new path; full victory/defeat *resolution* (incl. a US-side election-loss ending) remains S5's job. (c) The casualty toll is a SEPARATE accumulator from the clock's per-battle weariness (intended — the toll is the cumulative human cost that sinks public will over the whole war). (d) `morale.troop` mirrors the battle-side morale the bridge already uses; wiring morale BACK into the bridge is deferred (battle-balance protected this milestone).
- **Adversarial bug-hunt** (5 lenses × 3-skeptic verify, **29 agents**) → **7 confirmed of 8 (1 refuted — it validated that morale writes nothing to the frozen clock/economy/manpower)**, all fixed:
  - **D51.1 (HIGH — save migration)** the one-time election→enemyWill latch (`electionApplied`) was presence-checked, so a legacy/pre-morale save already past the 1864 verdict (`resolved1864` true, no `C.morale`) would RE-FIRE the enemyWill nudge on load. Fixed: `moraleInit` seeds `electionApplied = !!(clk.resolved1864)` (a verdict already resolved at init = already applied) — migration-safe, no `_SAVE_VER` bump (the D7 pattern).
  - **D51.3 / D51.5 (HIGH/MED)** the latch was burned BEFORE the effect landed, so a turn with `C.strategy` absent would seal it and lose the consequence forever. Fixed: the latch is now set ATOMICALLY inside the `S && enemyWill` guard (mirrors D49.5) — it only seals when the nudge applies, and retries next turn otherwise.
  - **D51.2 (MED)** a corrupt negative `casualtyToll` inverted the casualty penalty and pinned public will to 100. Fixed: `moraleInit` floors the toll to ≥0 (`!(x>=0)` also catches NaN/−Infinity); `casPenalty` lower-bounded `Math.max(0, …)`.
  - **D51.4 (MED/LOW)** the repudiation `M.public −= 10` was an inert one-tick write (public is recomputed each tick, clobbering it). Fixed: replaced with a DURABLE `M.repudiated` flag that `moraleCompute` reads (a persistent −12 public-will penalty) — the "home front breaks" now actually shows and lasts.
  - **D51.6 (refuted → NONE)** the integration lens confirmed morale writes ONLY `C.morale` + the one bounded enemyWill nudge (no frozen-clock/economy/manpower writes; `moraleCompute` only READS `bridgeArmy`/`cabinetLeadership` — no recursion), so the no-regression guarantee holds by construction.
- **Verified:** probe-morale **10/10** (the 3 layers + interactions, casualty/inflation sinks, leadership-lifts-troops, toll accumulation, the 1864 election→enemyWill in both directions, render, AND the D51.1/.2/.3 corrupt/legacy-save hardening); full no-regression green (probe-victory **12/12**, blockade/manpower/bridge/auto-resolve/desk/economy/decisions/cabinet/conditioning/weapons/production/cannon/engineering/cover/armory-field/targeting all green; diag-classic Classic paints); 0 pageerrors. Deliverable ≈1249KB. **NEXT: S2 m4 — the press / public-opinion system (§8.57/D38.1): period papers react to your war → public morale + the 1864 election; censorship/Vallandigham as a lever; ties the broadsheet UI to the 3-layer morale.**

## D52 — S2 m4 THE PRESS / PUBLIC-OPINION SYSTEM (§8.57 / D38.1) — [CLAUDE] (run k, 2026-06-14)
The real Civil War newspapers, North and South, react to your war with period broadsheet editorials — a real driver of the public will that decides the 1864 election. New `data/press.json` (12 papers) + `src/34-press.js` + `C.press`; surfaced as a new **"The Press"** desk tab (the 12th, period broadsheet).
- **What it does:** each paper has a real political LEAN (radical / administration / war-democrat / copperhead / fire-eater / abolitionist) that sets HOW it reacts. `pressOnResolve` (runs BEFORE `moraleOnResolve`) reads the turn's condition (victory / defeat / stalemate / heavy-casualties / emancipation-once), each paper runs a matching editorial and shifts its FAVOR toward/against the war effort (strongly-for +8 … strongly-against −8, clamped 0-100; a paper with no editorial for today's news drifts back toward its standing lean), and the aggregate SENTIMENT feeds `moraleCompute`'s public-will layer (`pressTerm = (sentiment−50)·0.2`, ±10) — so the press is a real driver of the home front. The tab shows the day's headlines, each paper's stance, and the aggregate.
- **Content:** the web-grounded `s2-press-research` workflow (7 agents, WebSearch/WebFetch + adversarial verify) → 12 real papers with real editors + URLs (Greeley's *Tribune*, Garrison's *Liberator*, Medill's *Chicago Tribune*, Curtis's *Harper's Weekly*; the *NY World*, *Chicago Times*, *Crisis*, *Herald* Copperhead/Democratic sheets; the *Richmond Examiner* & *Charleston Mercury* savaging Davis, the *Enquirer* defending him, the *Dispatch*). Merged conservatively (the 4 scaffold ids/leans preserved). Anti-Lost-Cause throughline: BOTH presses were bitterly divided — the Confederate press was no propaganda monolith but raged at Davis, conscription, and impressment; the *Herald*'s ahistorical "war-independent" lean was mapped to the contract's `war-democrat`.
- **Decide-&-log:** (a) the press is a READOUT + ONE read-only coupling (morale reads `pressSentiment`) — it writes nothing to the frozen clock/economy/manpower/strategy (zero regression); `pressSentiment` is a NEUTRAL 50 until the press first reacts, so it perturbs nothing at init / in the morale probe. (b) the cross-cutting teaching cards intentionally name papers from BOTH sides (a press-landscape lesson) — they are NOT side-filtered; only the player's PAPER LIST is side-scoped. (c) the censorship/Vallandigham LEVER is the existing m2 decision cards (`us-press-censorship`/`us-vallandigham`) acting on public opinion; a deeper standing-censorship tie to per-paper favor is a future deepening.
- **Adversarial bug-hunt** (5 lenses × 3-skeptic verify, **44 agents**) → **7 confirmed of 13 (6 refuted)**, all fixed:
  - **D52.1 (MED — save integrity)** `pressInit` repaired only the array-ness of `C.press.headlines`, not the entries, so a corrupt/legacy save with a `null` headline entry crashed `pressRenderTab` → froze the whole Press tab (the `_wdRefresh` dispatch isn't wrapped). Fixed: `pressInit` filters headlines to well-formed objects + a `!hd` continue-guard in the render.
  - **D52.2 / D52.6 (LOW)** the sentiment guards were NaN-blind (`typeof NaN === "number"`). Fixed: `pressInit` + `pressSentiment` reject non-finite / out-of-range sentiment (so a corrupt NaN can't poison `moraleCompute`'s public will).
  - **D52.3 (LOW — calibration)** the heavy-casualties priority ran over a victory's rally, but the heavy-casualties editorials were `neutral` → a bloody win read as a no-op. Fixed in data: the "mournful column" → `against` (a bloody victory now visibly TEMPERS the press — historically faithful to the bloody 1864 victories that sapped Northern morale); the abolitionist "blood atones" stays neutral (its distinctive framing).
  - **D52.4 (LOW)** the emancipation-once latch was set whenever the condition was active even if no paper actually printed an emancipation editorial. Fixed: the latch is set ONLY when a paper ran an emancipation editorial (`firedEmancipation`).
  - **D52.5 (LOW)** `_prsConditions` trusted the caller's `"draw"` string for stalemate; now also treats `winnerSide == null` as a stalemate (mirrors `endBattle`).
  - **D52.7 (LOW)** an editorial with an empty headline could push an empty broadsheet line; gated on `ed.headline`.
- **Verified:** probe-press **10/10** (papers load side-correct, neutral-until-reacted, victory>defeat sentiment, the Copperhead turns on a defeat, emancipation-splits-once latch, the press→public-will coupling anchored at 50, CS paper-list correctness, render, AND the D52.1/.2/.4 hardening + the bloody-day dampener); probe-desk **13/13** (the new Press tab); probe-morale **10/10** (the `pressTerm` edit anchored = no change); full no-regression green; 0 pageerrors. Deliverable ≈1300KB.
- **Tooling note:** running TWO browser-probe batteries concurrently collides on the shared `:8765` server + `tools/shots/*.json` → ERR_CONNECTION_REFUSED / goto-timeout flakes + cross-overwritten result files. **Run probe batteries SEQUENTIALLY, one battery at a time.**

## D53 — S2 m5 COMMAND: the NAMED-GENERALS system + the bridge LEADERSHIP wiring (§8.24/§8.58 · D31.4/D38.2 · GRAND-STRATEGY §19) — [CLAUDE] (run k, 2026-06-14)
The LAST big S2 sub-system: the President appoints / promotes / relieves the commanding general of his principal army, and the sitting general (skill + evolving reputation) blended with the cabinet finally drives the **bridge LEADERSHIP facet** that was a `var leadership = 64;` placeholder since the battle layer (D49.7). New `data/generals.json` (20 generals) + `src/35-command.js` + `C.president.command`; a new **"Command"** desk tab (the 13th).
- **What it does:** each historical commander carries traits (**skill / aggression / caution**), an **evolving reputation** (rises with victory, falls with defeat — `cmdOnResolve`), and **ambition** (the McClellan problem). A **date-aware DEFAULT** holds the post if the player never intervenes (`_cmdHistoricalDefault`, the `_cabHolder` idiom): the single-slot **iconic-army chain** — US Army of the Potomac → general-in-chief (Scott → McClellan → Burnside → Hooker → Meade → Grant), CS Army of Northern Virginia (J.E. Johnston → Lee). Appointing a specific general LOCKS the post; **relieving costs political capital** (`clock.capital`), gated for a `very-costly` (popular, politically dangerous) general — the McClellan dilemma made mechanical, with a free "restore the historical command" escape valve.
- **THE KEY WIRING (the milestone's point):** `commandLeadership(C) = clamp(42,88, 64 + (genRating−64)·0.7 + (cabinetLeadership−64)·0.3)`, `genRating = 0.55·skill + 0.45·reputation`. **Anchored at 64** — a neutral general + neutral cabinet = 64 = the old constant, so a fresh/default command is byte-equivalent to Classic (the A6a/D47.1 anchor lesson). `src/85` line 90 now consumes it, and `bridgeArmy.overall` gains a **bidirectional leadership edge** `+(leadership−64)·0.18` (mirrors the additive artillery/engineering arms, but signed) — a masterful command lifts the army a few points, a poor/discredited one drags it; at the anchor the edge is exactly 0. The general's temperament also nudges the **auto-resolve margin** (`commandMarginEdge`: aggressive presses the attack, cautious is sound on defense; deterministic, ±~2). Reputation evolution (win +3 / decisive +6 / loss −4 / decisive −8 / draw −1, clamped 5–98) means a defeated commander's army weakens while a victor's separation widens.
- **Content:** the web-grounded `s2-generals-research` workflow (10 agents: 4 research clusters + 4 adversarial-verify + cross-roster balance + teaching cards; WebSearch/WebFetch) → 20 generals (US 10 / CS 10) + 4 multi-voice teaching cards (the McClellan problem; the Lost Cause & the generals; "Grant the butcher" rebutted; what makes a general). **Anti-Lost-Cause throughout:** Lee excellent-not-infallible (Gettysburg/Pickett's Charge); Grant/Sherman/Thomas top-tier; Forrest's bio NAMES Fort Pillow + the KKK + slave-trading; Bragg/Hood poor army commanders.
- **Decide-&-log:** (a) **single-slot default = the iconic army** (the bridge fields one army; the West/secondary commanders — Thomas, Sherman, Sheridan, Halleck, A.S. Johnston, Beauregard, Bragg, Hood — are fully APPOINTABLE via `availableFrom` but `commandFrom`-null so they don't hijack the default chain or jump theaters). (b) **the general is confined to the BRIDGE + auto-resolve, NOT the m3 morale leader layer** — `33-morale.js` deliberately keeps `cabinetLeadership` (NOT `commandLeadership`): routing the general in would double-count the cabinet (commandLeadership already folds it in) AND confound the cabinet-isolating morale probe (which varies the DATE to vary the cabinet, also varying the date-derived default general). Folding the general into morale is deferred. (c) **anchored zero-regression** — every battle-layer + morale assertion holds (verified empirically below).
- **Adversarial bug-hunt** (8 dimensions × independent skeptic verify, **20 agents**) → **9 confirmed of 12, all fixed:**
  - **D53.1 (HIGH — data) Thomas wrongly defaulted the US chain.** Thomas commanded the Army of the *Cumberland* (West), but his `commandFrom` (1863-10, later than Meade's 1863-06) made `_cmdHistoricalDefault` pick him over Meade Oct 1863–Mar 1864. Fixed: `commandFrom/commandTo → null` (appointable, not default). My first chain-probe missed it (didn't sample 1863-10→1864-03).
  - **D53.2 (MED — data) Sherman same.** His `commandFrom` (1864-03, the Military Division of the Mississippi) tied Grant's; nulled (appointable). US chain is now bulletproof: …Meade → Grant.
  - **D53.4 (HIGH — recursion landmine) `bridgeArmy → commandLeadership → cabinetLeadership → _cabAmbitionActive → _cabReading("war") → bridgeArmy`.** m5 closed a cycle that would stack-overflow whenever the sitting War Secretary has `ambitionTell` + ambition in [45,75). Dormant under current data (probes were green) but a data-change crash. Fixed: a re-entrancy guard `_brgLeadGuard` in `bridgeArmy` — a nested call uses the neutral 64 anchor, the outer call gets the true value. (Chose the guard over the verifier's "_cabReading uses manpower.strength" so the cabinet's war-reading heuristic + its probe stay intact.)
  - **D53.5 (HIGH — edge) post-war fallback jumped to the FIRST commander.** `_cmdHistoricalDefault`'s no-window fallback returned the earliest `commandFrom` (Scott) when a campaign ran past the last tenure end. Fixed: before the first tenure → earliest; after the last → the LATEST-ending commander (continuity).
  - **D53.6 (MED — save) `_activeId` rode the save unvalidated.** The handover-detection cache could reference a removed/wrong-side general after a roster change. Fixed: `cmdInit` clears it if not in the side roster (a just-died-but-in-roster id is KEPT so the succession still logs).
  - **D53.7 (MED — UX) capital soft-lock.** The pool rounded `clock.capital` for display but `cmdAppoint` gated on the raw value → a fractional capital could show an affordable button that silently failed. Fixed: round at the gate too.
  - **D53.8 (MED — XSS) `_cmdTraitBar(label,hint)` unescaped.** Call sites are hardcoded today, but the signature was injection-prone; now `_cmdEsc`'d (D43.4 discipline).
  - **D53.9 (MED — DOM) wire/render id-escape mismatch.** Render escaped the appoint-button id, `cmdWireTab` didn't; harmless for kebab ids but inconsistent. Now both `_cmdEsc`.
  - **D53.10 (LOW) dead inner ternary** in `_cmdGenRow` (the outer guard already proved `availableUntil` truthy). Simplified.
- **Verified:** probe-command **14/14** (data/schema, cmdInit + reputation seeding, the **fresh anchor 72→overall 77 Classic-safe**, date-aware default, appoint Grant lead 87 vs Burnside 52, reputation 88→98 win / 88→64 loss, the McClellan relief gate at 27 capital, the trait margin edge, the **D53-decoupling** [general moves the bridge, leaves the cabinet-driven leader layer], lock/revert, side-correctness, render, + the **D53.4 recursion guard** and **D53.5 post-war** regression guards); full no-regression GREEN — bridge 6/6 · conditioning 6/6 (the fresh-Classic anchor) · auto-resolve 7/7 · cabinet 12/12 · morale 10/10 · desk 13/13 · press/decisions/victory/manpower/economy/production/blockade/weapons/cannon/engineering/cover/targeting/armory-field/cab-exploit all green · diag-classic Classic paints; **0 pageerrors** throughout. Deliverable ≈1392KB. **S2 is COMPLETE (m1–m5). NEXT: S3 alt-history · S4 education/codex/tutorial/WCAG-AA · S5 victory + graded report + Reconstruction · then the run-k graphics/footage + survival/loot directives.**

## D53-OPS — STANDING INSTRUCTION: push to GitHub ONLY after PROPER VETTING — [AARON] (run k, 2026-06-14)
Aaron: "update standing instructions so updates pushed to proper video game github after proper vetting." **Canonical rule (now codified in MEMORY.md, `civilwar-overnight-charter`, `civilwar-autonomous-run-params`, `START-HERE.md`, and `AUTONOMOUS-RUN.md` §1):** commit + `git push origin main` to **github.com/adhanke-star/History-Video-Game** (private) per milestone — but ONLY after the **PROPER VETTING gate** passes: `node tools/build.mjs` → `GATE OK` → the milestone's focused probe **+ the full no-regression suite + `diag-classic` green + 0 pageerrors** (READ the JSON) → **adversarial bug-hunt** with every confirmed finding fixed → THEN commit → THEN push. Never push an unvetted / red / regressed / pageerror build. This RETIRES the stale "no remote / local-commits-only" lines (true only through run i; the §8 charter authorized the GitHub backup, and m1–m4 are already on the remote).

## D54 — RUN-K Q&A CHARTER EXPANSION: 31 locked decisions, reprioritizing toward the TACTICAL ENGINE — [AARON] (run k, 2026-06-14)
A 10-round / 31-question popup session (Aaron) pre-cleared the forward path. **HEADLINE: the real-time UG:G tactical engine is now PRIORITIZED ahead of S3–S5.** The locked answers (carry into every future run; also in memory `civilwar-q-a-charter-runk`):
- **Direction:** tactical engine SOONER (ahead of S3–S5); alt-history = **fully player-driven** (the sim follows, NO historical thumb on the scale — supersedes the old "consensus-favored" framing); the "your war vs history" tracker = a lightweight **readout + divergence-log FIRST** (zero-regression).
- **Tactical engine:** **BOTH** gridless-real-time **AND** hex-turn as **selectable modes** (Classic stays frozen, never regressed); first slice = **First Bull Run, one full battle** (deploy→maneuver→fire→morale→result); combat = **UG:G-style** (morale + flanking/rear + cover + fatigue, **low micro**, few unit types); controls = **drag-to-maneuver, low-APM**; unit scale = **regiments / brigades** (the existing OOB data feeds it); **FULL strategic conditioning via the bridge + a tactical-only FREE mode**.
- **Graphics — BATTLE LAYER FIRST:** linked-assets = **embed links + mandatory offline (procedural) fallback**; PD sources = **Library of Congress + Internet Archive + Wikimedia Commons**; concentrate the animation budget on the tactical engine.
- **Survival/loot:** apply to **ALL modes** (campaign march legs + tactical battlefield loot + a dedicated march/survival mode + free-battle/skirmish); survival depth = **light, preset-gated** (off by default, dial-able, §27); loot = **STANDALONE rarity-tiered loot** (Borderlands-style drops with its own inventory) — NOT only Armory-integrated.
- **Architecture/efficiency:** **SINGLE HTML + linked assets** (confirmed — it's the best for sharing: email the one file OR host one URL; multi-file/bundler would break the $0/double-click deliverable); **group `src/` into subsystem folders** (src/strategy/, src/tactical/, src/render/, …; keep NN load-order within each); **perf presets + profile hotpaths** (Auto/Low/High, instancing/LOD/culling) for the Intel UHD 617 floor.
- **Sharing/saves/onboarding:** hosting **deferred** (keep it buildable + ready to send; GitHub Pages later on request); saves = **localStorage + named slots + export/import** (portable saves); onboarding = **accessible default + skippable tutorial**.
- **Depth/breadth:** theaters = **Eastern + Western** (defer naval/riverine/trans-Mississippi — buildable later, not a priority); battles = **add the canonical big ones, THEN a custom-battle builder**; deepen **Politics/Congress/home-front + Logistics/supply/railroads** most.
- **Quality engineering:** add a **full-campaign playthrough probe** to the no-regression suite; **adversarial bug-hunt every milestone, scaled to size**; **build a reusable historical-data layer** + an **in-game sources codex** + keep the **web-grounded multi-agent Verified/Inferred** research pattern.
- **Education/a11y/audio/done:** WCAG 2.2 AA = **one big dedicated S4 pass** (build fast now, comprehensive audit later — Aaron does a11y professionally); audio = **period music + battle SFX + UI cues, all accessible** (toggles/captions/volume, never gates play); **it is a PERPETUAL personal project — NO fixed v1.0** (ship vetted milestones forever; "done" is whenever Aaron's happy).

## D55 — RUN-K Q&A SETS 2–3: 39 implementation-level decisions (the deep-everything build spec) — [AARON] (run k, 2026-06-14)
Two more popup sets (Set 2: 30 Q; Final: 9 Q) cleared the implementation forks. **THROUGH-LINE: MAXIMALIST depth on every system (Paradox-grade deep sim across all axes) — but preset-gated / delegable so the simple UG:G core still stands alone (§27). Aaron favors a TOGGLE for every play-style option.** (Also in memory `civilwar-q-a-charter-runk`.)
- **Tactical engine (the next work):** runs on the **existing 3D terrain engine** (PBR/HDRI/post-FX), not a fresh 2D map. Fog = a **toggle, default OFF** (light-LOS / full as options). Enemy AI = **scripted-historical + reactive**. Formations = **line/column/skirmish + facing, drag-set**; morale/rout = **CENTRAL** (waver/break/rout/rally; flanking shatters cohesion). Casualties = **realistic-grim, tasteful, toggle-able**; tone = tasteful-educational default with an **unflinching/graphic toggle**. Time = **active auto-pause, toggle** (pause+speed / active-pause / real-time). Command = **direct + delegate brigades to AI**. Victory = **objectives + morale collapse + time**. Units = **low-poly 3D models** high-tier, billboards/counters as the perf-preset fallback. **P0 NEXT SESSION = a SANDBOX skirmish first:** ~2 brigades/side, one terrain, the full core loop (deploy→drag-maneuver→fire→morale/rout/rally→win), then scale to First Bull Run. Pacing: battles ~15–30 min; campaigns span sessions.
- **Loot/survival:** loot = **many tiers + randomized affixes** (Borderlands-deep), affects **weapons + supply + morale items**, and is a **MAJOR supply source** (capture/foraging strategically central; the cash-poor CS leans on it) — still §27 preset-gated. Survival activities = **mini-games** (hunt/fish/forage/scavenge); pressures = **rations + weather + disease, light** default (full-attrition as a dial-up); dedicated mode = an **Oregon-Trail-style journey between battles**.
- **Graphics/audio:** art = **period-realistic** (PD photos + painterly 3D + engraving UI); footage = **contextual cutaways at key beats** (skippable, offline→procedural fallback); animation budget = **battle layer first**; audio = **period music + battle SFX + UI cues, all accessible**.
- **Strategic depth (all DEEP):** **Politics** = Congress + war legislation + factions/parties + governors/states + elections-beyond-1864; **co-equal politician/commander, fully delegable**. **Home front** = a **deep social sim** (population, classes, the collapse of slavery, refugees/contrabands, the wartime economy). **Logistics** = **full operational logistics** (railheads/depots/wagons/rivers + interdiction) + a **dedicated rail-management layer** + an **in-battle supply mechanic**. **Economy** = a **deep economic sim** (sectors, labor incl. enslaved-labor collapse, prices, trade). **Diplomacy** = a **deep diplomacy sim** (Britain/France recognition, King Cotton, Trent). **Cavalry/recon** = a real system: **screen, scout, raid** (Stuart/Forrest/Sheridan; ties fog + logistics + named-generals).
- **Knowledge/teaching:** **one queryable historical-data knowledge base** (single source of truth, provenance-tagged) feeding **ambient cards + a browsable codex + a seminar mode**; alt-history tracker = **divergence log + consequences + a graded final report**.
- **Structure/meta:** **single HTML + linked assets** (best for sharing); **group src/ into subsystem folders**; **perf presets + profile hotpaths**; saves = **localStorage + named slots + export/import**; presets = **play-style presets + per-system sliders**; **SP-first now**, but **design for mod-friendly data + shareable saves/scenarios + eventual hotseat/async MP**.
- **Scope/finish:** theaters **Eastern + Western** (naval/riverine/trans-Miss deferred); **canonical battles, then a custom-battle builder**; **historical + what-if scenarios**; **both short + grand campaigns**; a **playable Reconstruction phase** + a graded epilogue; **PERPETUAL project — no v1.0**.

## D56 — RUN-K TACTICAL ENGINE P0: the real-time SANDBOX skirmish (the D54/D55 reprioritization — tactical FIRST, ahead of S3–S5) — [CLAUDE] (run k, 2026-06-14)
The very next milestone after S2 per D54/D55: the first real-time, gridless, UG:G-style tactical layer, built as a **sandbox skirmish first** to PROVE THE LOOP before scaling to the First Bull Run slice. Shipped `src/tactical/T0-field-sandbox.js` (the new `tactical/` subsystem folder, manifest path `tactical/T0-field-sandbox.js`) + `tools/probe-field.mjs`. The full core loop runs: **deploy → drag-maneuver + facing → line/column → fire → morale/rout/rally → charge → win/lose**, on a procedural 3D field, with low-APM controls, reactive-lite enemy AI, active-pause + 1×/2×/4×.

**Architecture (decided + logged):**
- **D56.0 SIBLING engine, not a mutation of the hex-bound `__M3D`.** The shipped "Modern" 3D renderer in `build/base.html` (§23, ~line 10460) is hardwired to the hex board (`G.battle.M`, `colrowToPixel`, hex tiles) — it is literally a 3D re-skin of Classic, exactly as `MODERN-UGG-PLAN.md` §3 warned. So `__FIELD` owns its OWN THREE scene + camera + RAF loop + full-screen DOM root (`#fldRoot`), completely OFF the Classic / `__M3D` code path → **zero regression risk** (probe-verified: the sandbox never writes `G.battle`/`G.campaign`/`G.mode`). It reuses the SAME three@0.128 stack (via the base `_m3dLoadScripts` loader) + the sunlit-field art direction; deeper PBR/HDRI/post-FX reuse is deferred to P1+. This honors D55's "runs on the existing 3D terrain engine, not a fresh 2D map" in spirit (same 3D tech stack) while keeping P0 isolated.
- **D56.1 Renderer-agnostic sim + a MANDATORY offline 2D fallback** (D54): the sim state is pure data; a 3D renderer draws it when THREE loads, a 2D top-down canvas renderer draws it when THREE is unavailable (offline). All combat formulas are PORTED from the base engine (`moraleCheck`/`resolveFire`/`resolveCharge`, base ~1015-1086; ratings `_OVR_WEIGHTS` ~10126) — not reinvented — then adapted to continuous, positional, per-tick resolution. A seeded LCG RNG makes probes reproducible + balance tunable.
- **D56.2 Additive launch via a MutationObserver** on `#sheetPad` (NO override of `openMainMenu`). **Bug found by the bug-hunt:** the first cut anchored on `.menu-grid`/`#mmFree` — but those exist only in the SHADOWED `openMainMenu` (base ~3249); the LIVE menu is a later broadsheet redeclaration (~8346) using `#gnFree`/`.gn-col`, so the button never injected and the whole module was UI-unreachable. Fixed to anchor on `#gnFree` and inject a native `.gn-btn` into the Field-Dispatches column; `probe-field` now has a MENU-ENTRY regression step (the gap that let it through — the probe drives `fldLaunchSandbox()` directly).
- **D56.3 Subsystem folder grouping STARTED, full reorg deferred.** This module proves the D54 `src/` subfolder grouping (`tactical/`); reorganizing the existing 22 strategy modules into `src/strategy/`, `src/render/`, etc. is its own dedicated milestone (don't conflate a big path refactor with a big feature — revertibility + regression surface).

**Adversarial bug-hunt** (6 dimensions × diverse-lens [correctness + real-world-impact] skeptic verify, **60 agents**, ~3.3M tokens) → **24 confirmed of 27, all fixed** (3 correctly refuted). Deduped to ~16 unique:
- **HIGH:** (1) the dead menu anchor above; (2) exit/relaunch during the async THREE load resurrected a torn-down sandbox (leaked GL context / null-canvas `getContext` crash) → a **launch-generation token** (`__FIELD._gen`) makes stale loader callbacks no-op, bumped in `fldExit`; (3) end screen keyboard-inoperable + Tab hijack → `fldKey` yields on `phase==="over"` and only steals Tab mid-battle, focus moves to the rematch button, `role="dialog"`; (4) 2D fallback silently blank when 3D init throws after the WebGL context bound → `fldResetCanvasForFallback` swaps in a fresh canvas (a canvas that took a GL context can't switch to 2D).
- **MED:** deploy + AI-fallback facing constants pointed sideways (every front mis-scored as a 1.5× flank) → corrected to the `atan2(dx,-dz)` convention (0=north, π=south), so brigades face the enemy; mutual-charge resolved melee twice per tick → unordered-pair guard; column frontage 0.7 reduced casualties even on flank/rear (inverted enfilade) → arc-dependent (0.7 head-on, 1.4 enfiladed); 3D unit facing rendered 180° backward (`-facing+π` → `-facing`); per-frame `THREE.Color` alloc → hoisted cached colors; double-launch RAF guard in `fldStartLoop`; `fld2dInit` defensive on a null canvas; color-only side ID → a non-color cue (2D "U"/"C" letters; 3D cube vs pyramid finial — full CVD/pattern pass is the dedicated S4 a11y milestone, D54); space/enter on a focused button double-fired → `fldKey` ignores button targets.
- **LOW:** `fld3dDispose` now disposes OrbitControls + `forceContextLoss` (context exhaustion over repeated rematch); `fldExit` now removes the window-level `pointerup`/`resize` listeners; `_fldAiClock` reset in `fldInitSim` (cross-launch determinism); dead `ammoF` made a real low-ammo fire taper; `fld2dView` scale clamped (no negative draw scale on a tiny viewport); a 1-frame resize flicker fixed (`fldResizeCanvas` repaints the 2D canvas after the clear).

**Balance auto-tune (charter §8.7, logged):** `FLD.FIRE_BASE` 7.4 → **3.2** (peak cas/s, full-strength rifle brigade, close) — the primary fight-length knob. With the facing fix removing the universal mis-scored 1.5× flank multiplier, the 2-brigade-a-side loop settles at ~**90s** at 1× (maneuver ~30s → firefight → rout cascade), a morale-driven break (the losing side breaks at ~10-15% casualties — the UG:G feel) rather than a bloodbath. Battle length is set by the morale-break cadence, not the fire rate (2.7 and 3.2 both give ~90s); 3.2 keeps casualties from going bloodless. Full battles (the P1 Bull Run slice, more brigades/bigger map) will run longer.

**Tooling:** `tools/probe-field.mjs` — **13/13**, 0 pageerrors, GPU-flake-free (asserts the renderer-agnostic sim headlessly: deploy→4 units, maneuver, fire-both-sides, ≥1 rout, a declared winner [no deadlock], no NaN, determinism, victory-by-collapse, objective-hold accrual, clean re-launch, the 2D fallback DOM/draw/teardown, the live-menu button injection, no Classic contamination) + a confirmed mid-battle 2D screenshot. **Also bumped `tools/t1probe.mjs` goto timeout 30s → 60s** (the suite standard) — pre-existing tightness exposed by the 1.45MB file's `load`-event wait on the THREE CDN + offline 404s; not a game regression (Classic paints; all 60s-budget probes green). Full no-regression: **24 probe suites green, 0 pageerrors, diag-classic Classic paints.** Deliverable ≈**1448KB**.

## D57 — RUN-K TACTICAL ENGINE P1a: the FIRST BULL RUN scenario (real OOB + historical terrain + the rail-pivot reinforcement timeline) — [CLAUDE] (run k, 2026-06-14)
The next slice after D56's P0 sandbox: the FIRST real historical battle on the `__FIELD` real-time engine — **First Bull Run (First Manassas), July 21, 1861**, brigade scale, on a historical-terrain field, with the **rail-pivot REINFORCEMENT TIMELINE as the teaching mechanic**. Selectable from the field menu ("⚔ BATTLE — FIRST BULL RUN (1861)") next to the sandbox button. New `data/bullrun.json` (the engaged main-field OOB distilled from `HISTORICAL-DATA.md`, provenance-tagged, anti-Lost-Cause) + new `src/tactical/T1-bull-run.js` (the scenario module) + minimal seams in `T0-field-sandbox.js` + new `tools/probe-bullrun.mjs` (15 steps + a 3D-leak guard). Deliverable ≈**1485KB**.

**Architecture (the design judge-panel verdict — 3 approaches scored, A "guarded T0 seams" won 8.9 vs B 6.7 / C 7.8):**
- **D57.0 EXTENSION via guarded T0 seams, NOT an override of the sim loop.** T0 exposes no-op-by-default seams keyed on `__FIELD.scenario`/`reinforce`/`attacker` (all null/"sandbox" by default): `fldInitSim` dispatches to `fldScenarioInit` (early-return); `fldSimStep` calls `fldScenarioTick` (per-tick reinforcement splice); `fldInjectMenuButton` → `fldInjectScenarioButtons`; `fldOnOver` → `fldScenarioEndHtml`; terrain readers/renderers generalized to multi-hill/multi-wall + markers via `fldHills()`/`fldWalls()` accessors (single-hill sandbox value-identical); `fldCheckVictory` made reinforcement-aware + asymmetric. **The sandbox path stays byte-behavior-identical → probe-field held 13/13 throughout** (+ a terrain GOLDEN-SNAPSHOT guard grafted from approach C catches silent sandbox-geometry drift). T1 **orchestrates only** (terrain build + OOB instantiation + arrival splice); it never duplicates combat/morale math — every per-unit computation flows through T0's unchanged leaf helpers (grafted from approach B), so a future T0 balance fix reaches Bull Run automatically.
- **D57.1 The rail-pivot REINFORCEMENT TIMELINE.** The battle is modeled as ARRIVAL WAVES (the historical engagement): at T=0 only the lead Union brigades (Burnside, Porter) contact the Confederate delaying force (Evans, Bee, Bartow) on Matthews Hill; both sides then FEED brigades over the morning — the Union across Sudley Ford, the Confederates rallying on Henry House Hill and receiving rail-borne reinforcements (Jackson @135s "Stonewall", Stuart @180s, Early @220s, Elzey @275s "off the train" onto Chinn Ridge, the Union right). 9 brigades/side total (2+3 at T=0, 13 in waves). Reinforcements spawn IN ARRAY ORDER at scheduled sim-times with ZERO spawn-time RNG (determinism). A side is "destroyed" only with no live units AND no reinforcements still detraining (`fldArmyPending`).
- **D57.2 ASYMMETRIC victory (attacker seizes, defender denies).** The Union (attacker) wins by seizing-and-holding Henry House Hill (`holdToWin`) or breaking the CS army; the Confederacy (defender) wins by denying the hill to the time limit or breaking the US army. `__FIELD.attacker`/`defender` from data; null for the sandbox (symmetric, unchanged). `__FIELD.winBy` records destroy/hold/timeout.
- **D57.3 Historical terrain + atmosphere.** Henry House Hill (the objective crest), Matthews Hill, Chinn Ridge (3 hills), the Robinson House lane (wall cover), 5 woods, + labeled markers: Bull Run creek, Young's Branch, the Warrenton Turnpike, Sudley Springs Ford, the Stone Bridge, "to Manassas Junction" — rendered in both 2D and 3D. A pre-battle BRIEFING modal (situation + objective + 2 teaching cards + provenance), arrival BANNERS (color-coded, stacked, role=status + aria-live), and an end-screen "Your war vs. history" teaching payoff (divergence note + 3 cards with provenance).
- **D57.4 Subsystem-folder grouping continues** (`src/tactical/T1-*`); the full strategy-module reorg stays deferred (D56.3). **Deferred to P1b/P1c:** the hex-mode toggle, full owner-mode bridge conditioning into the scenario, deeper PBR/HDRI/post-FX, and a CS player side.

**Balance auto-tune (charter §8.7, logged):** `holdToWinSec` **75 → 140** and `timeLimitSec` **600 → 480** in `data/bullrun.json`. 75 let the CS auto-hold the rear objective (it sat on Henry Hill) and win at ~76s before the timeline engaged; 140 makes the full rail-pivot timeline engage in the typical battle (all 13 reinforcements arrive; sweep at hold140 = battles 262–472s, every seed). The asymmetry probe proves the rail pivot is LOAD-BEARING (removing ONLY the CS reinforcements drops the Confederate outcome score sharply: the Union is denied to ~262s WITH the trains vs ~239s/easier WITHOUT, csScore 252611 vs 223884). timeLimit 480 trims the dead-attrition tail after the last reinforcement (275s) so the denial-win turns more on the timeline. **KNOWN/LOGGED for P1b:** AI-vs-AI auto-battle currently leans Union (the attacker AI grinds effectively; the CS defender AI doesn't reliably counterattack) — the primary mode is human-Union-attacker racing the timeline; CS-favorable balance (smarter defensive/counterattack AI, the fog-of-war toggle aiding the defender, force re-tuning) is a P1b item.

**Adversarial bug-hunt (loop-until-dry):** Round 1 = 6 dimensions × diverse-lens default-refute verify + completeness critic (**27 agents, ~1.6M tokens**) → **14 confirmed of 20, ALL FIXED**: HIGH — missing-`data.objective` crash (guard hardened to fall back to sandbox), the briefing-Esc-tears-down-the-battle bug (overlay keydown stops Esc propagation + traps Tab), focus yanked off the briefing button by the deferred `fldRoot.focus()` (guarded against an open modal), `fld3dBuildUnits` leaking GPU geometries/materials on every reinforcement rebuild (dispose-before-detach; probe-confirmed bounded: 21 geoms after all 13 waves vs 51 at wave 1). MED/LOW — stale `__FIELD.scenData` leaking the Bull Run title into a later sandbox (nulled in the per-launch reset), the rally-aware gone-check (scenario-only; sandbox byte-identical) + comment overstatement, and `__FIELD.winBy` recording + 4 probe false-confidence gaps closed. Round 2 = the completeness critic surfaced a REAL gameplay bug the probe missed (it only ran `autoBoth:true`): in the real game (player=Union) **US reinforcements spawned `ai:false` with a default hold order and idled in the rear** → now a player-side arrival auto-marches toward the objective on spawn (the player can redirect). Also fixed: clustered same-tick arrivals clobbering the aria-live region (batched into one announcement) + a Bull Run objective coordinate drift-guard. Every fix re-probed green.

**WCAG 2.2 AA (charter §8.18; wcag-auditor on the new UI):** 8 non-text-contrast border fixes (warm-umber borders lightened to ≥3:1 on both surfaces, aesthetic preserved), a `:focus-visible` outline injected for the tactical buttons (11.25:1), `aria-modal="true"` added to the end dialog, and a Tab focus-trap added to the end screen. Body-text contrast already cleared 4.5:1.

**Verification:** `node tools/build.mjs` GATE OK → **probe-bullrun 15/15 + 3D-leak guard, 0 pageerrors** (asserts: full engaged OOB, the 3 named hills + Henry House Hill objective + markers, reinforcements on-schedule/in-order/idempotent, player reinforcements advance, the full battle reaches the reinforcement era with a recorded winBy, the defender-denial timeout win, the rail-pivot load-bearing asymmetry, determinism, the sandbox golden-snapshot unregressed, no Classic contamination, the 2D path, scenario a11y, the live-menu button, 3D geometry bounded) → **full no-regression suite GREEN (25 probe suites + t1probe 14/14 + bootprobe + diag-classic Classic-paints, all 0 pageerrors)** → probe-field **13/13** (P0 sandbox unregressed). Committed + pushed after full vetting. **NEXT: TACTICAL P1b** — the hex-mode toggle (Classic frozen) · smarter CS defensive/counterattack AI + the fog-of-war toggle · full owner-mode bridge conditioning into the scenario · deeper 3D (PBR/HDRI/post-FX). Then P2–P5, loot/survival, S3 alt-history, S4 education/codex/WCAG-AA pass, S5 victory + Reconstruction.

## D58 — RUN-K TACTICAL ENGINE P1b-i: the FOG-OF-WAR toggle (the UG:G recon pillar) — [CLAUDE] (run k, 2026-06-15)
The first slice of the P1b bundle: fog of war on the `__FIELD` real-time engine — a per-side line-of-sight visibility model, **a toggle, default OFF** (D55; MODERN-UGG-PLAN §5). Sub-divided from P1b as the single highest-value deep slice: a marquee UG:G system that ALSO directly engages the logged AI-leans-Union balance gap (fog aids the defender). All in `src/tactical/T0-field-sandbox.js` (shared by the sandbox + Bull Run) + `tools/probe-fog.mjs`. Deliverable ≈**1495KB**.

**Architecture (decided + logged):**
- **D58.0 A GATED visibility model, default-OFF = a byte-identical no-op.** `__FIELD.fog` (a persisted toggle, `G.settings.tacticalFog`). `fldVisible(side, target)` returns `true` immediately when fog is OFF, so every gate below is a no-op and the sim/render/AI is byte-behavior-identical to the pre-fog engine (probe-field held **13/13**, probe-bullrun **15/15**, diag-classic green). Fog ON: `fldComputeVisibility()` runs once per tick (and is primed at launch for the deploy screen) building `__FIELD.vis = {US:{}, CS:{}}`; `fldAcquireTarget` + `fldAiUnit` skip non-visible enemies (you can't fire at / react to what you can't see); the 2D + 3D renderers hide unscouted enemies from the US player; the HUD shows only *sighted* enemy brigades. Determinism: visibility is position-based, **no RNG**.
- **D58.1 Sight by arm + woods block line-of-sight.** `fldUnitSight` — cavalry scout widest (720 yd) > artillery (470) > infantry (430): cavalry-as-recon, the seed for the later cavalry system. `fldLosClear` uses an **exact segment-vs-woods penetration model** (sum the sight-line length inside each woods disk; blocked past `WOODS_SEE_THRU` = 75 yd) — robust where a fixed-sample walk failed both ways (a thin grazing chord stays visible; a line straight through is blocked; two units in the same woods still see each other up close).
- **D58.2 Last-known "ghosts."** A faded dashed marker + "?" at an enemy's last-seen position once it goes out of sight (`__FIELD.lastSeen`), purged on the unit's death so a destroyed brigade leaves no phantom. 2D for this slice (3D ghosts deferred).
- **D58.3 The toggle.** A "Fog: On/Off" control-bar button (with `aria-pressed`) + the **V** key + the persisted setting; an aria-live announcement on flip.

**Teaching + balance (charter §8.7, logged):** fog ON **flips the Bull Run AI-vs-AI outcome to a Confederate denial win** (probe-confirmed: winner CS by timeout) — the Union advances blind into the unseen reverse-slope (Jackson) and the rail reserves (Early/Elzey) and can't seize-and-hold in time. This is the historically-correct result AND it directly addresses the D57-logged "AI-leans-Union with fog OFF" item: fog is the defender's friend. No numbers changed (fog is purely the new visibility layer); default OFF keeps the prior balance.

**Adversarial bug-hunt (loop-until-dry):** 5 dimensions × default-refute verify + completeness critic (**17 agents, ~0.9M tokens**) → **11 confirmed (deduped to 5 root causes), ALL FIXED**: HIGH — a destroyed enemy left a permanent phantom "?" ghost (purge `lastSeen` in `fldKill` + harden the `fld2dGhosts` skip-guard to `!live || !live.alive || visible`); MED — fog-ON-at-launch blacked out the whole DEPLOY screen because `__FIELD.vis` wasn't computed until the first sim tick (prime it at the end of `fldResetRun`, the shared launch chokepoint); MED — `fldLosClear`'s 7-sample walk leaked LOS through grazing woods chords AND over-blocked two units inside the same woods (→ the exact penetration model above); MED — the fog toggle button lacked `aria-pressed`; LOW — the no-selection "N Rebel brigades sighted" HUD line was frozen (throttled refresh from `fldRenderTop`). probe-fog grew 11→**13/13** with a dead-enemy-ghost step + a deploy-visibility step.

**Verification:** GATE OK → **probe-fog 13/13, 0 pageerrors** (default-OFF no-op · LOS math · hide-far/reveal-near · woods-block · cavalry-scout-widest · AI/targeting-only-visible · ghosts purged on death · deploy-visibility primed · toggle persists · fog-ON sandbox+Bull Run resolve deterministically · no Classic contamination) → **full no-regression GREEN (26 probe suites + t1probe + diag-classic Classic-paints, 0 pageerrors)** → probe-field **13/13** + probe-bullrun **15/15** (the fog-OFF invariant holds) + a fog screenshot inspected (hidden CS reserves, the sighted-count HUD, the Fog:On button). Committed + pushed after full vetting. **NEXT: TACTICAL P1b continues** — the hex-mode toggle (Classic frozen) · smarter CS defensive/counterattack AI (now fog gives the defender the edge; the counterattack AI is the complement) · full owner-mode bridge conditioning into the scenario · deeper 3D (PBR/HDRI/post-FX). Then P2–P5, loot/survival, S3–S5.

## D59 — RUN-K TACTICAL ENGINE P1b-ii: the ACTIVE AUTO-PAUSE toggle (the UG:G low-APM pillar) — [CLAUDE] (run k, 2026-06-15)
The next P1b slice: active auto-pause on the `__FIELD` engine — the battle pauses at decision points (a brigade breaks / is destroyed / reinforcements arrive) so a low-APM player keeps up. **A toggle, default ON** (the accessible low-APM default, §8.15; D55 "active auto-pause, a toggle"). A "Auto-pause: On/Off" control-bar button + the **P** key + the persisted `G.settings.tacticalAutoPause`; the pause reason shows in the phase indicator + an aria-live announcement. All in `src/tactical/T0-field-sandbox.js` + `tools/probe-autopause.mjs`. Deliverable ≈**1500KB**.

**Fork (decide-&-log):** chosen as this firing's slice over the heavier remaining P1b items (hex-mode toggle, CS counterattack AI, bridge conditioning, deeper 3D) for the best value-to-risk-to-context ratio — a named UG:G pillar that is self-contained, deterministic, and architecturally low-risk; the heavier items stay queued.

**Architecture (D59.0):** auto-pause lives **ONLY in the RAF loop** — `fldStartLoop`'s step snapshots `{routEverCount, aliveCount, units.length}` before the fixed-timestep accumulator and calls `fldAutoPauseScan(...)` after it. The headless probe stepper (`fldStepN`) and every probe `runToEnd` call `fldSimStep` directly and **never reach the scan**, so headless probes + determinism are untouched **by construction** (auto-pause sets `__FIELD.paused`, which `fldStepN` ignores — it gates on `phase`, not `paused`). Default ON via `G.settings.tacticalAutoPause` (unless `===false`); the toggle persists it. `_apReason` lifecycle: reset in `fldResetRun`/`fldInitSim`, cleared on resume.

**Adversarial bug-hunt (loop-until-dry):** 4 dimensions × default-refute verify + completeness critic (**10 agents, ~0.5M tokens**) → **4 confirmed (3 distinct), ALL FIXED**: MED — turning auto-pause OFF while it actively held the battle paused did NOT release the pause (the escape control didn't escape) → `fldToggleAutoPause` now releases an active auto-pause on OFF; MED (a11y) — the durable pause reason wasn't recoverable by AT (only a transient announce) → the Resume button's `aria-label` now carries the reason; LOW — a same-frame death + reinforcement masked the "destroyed" reason in the net headcount (still paused reliably — `units.length` is monotonic-up — but mislabeled) → robust per-frame kill detection (`killed = prevAlive − (aliveNow − spawned)`) + a combined multi-reason announcement. probe-autopause grew 11→**13/13** with a same-frame-mask step + a toggle-OFF-release step.

**Verification:** GATE OK → **probe-autopause 13/13, 0 pageerrors** (default ON · scan pauses on break/destroy/reinforce · same-frame death un-masked · no-op when nothing changed · early-return when already paused · resume clears the reason · toggle OFF releases an active pause · toggle flips+persists · **headless battle UNAFFECTED + determinism** · 2D button aria-pressed · no Classic contamination) → **full no-regression GREEN (27 probe suites + t1probe + diag-classic Classic-paints, 0 pageerrors)** → probe-field **13/13** + probe-bullrun **15/15** + probe-fog **13/13** (all unregressed). Committed + pushed after full vetting. **NEXT: TACTICAL P1b (continued)** — the hex-mode toggle (Classic frozen) · smarter CS defensive/counterattack AI · full owner-mode bridge conditioning into the scenario · deeper 3D (PBR/HDRI/post-FX). Then P2–P5, loot/survival, S3–S5.

## D60 — RUN-K TACTICAL ENGINE P1b-iii: the ROLE-AWARE AI — a smarter CONFEDERATE DEFENDER doctrine (closes the logged "AI leans Union" gap) — [CLAUDE] (run k, 2026-06-15)
The next P1b slice and the named complement to fog (D58): a real DEFENDER doctrine for the `__FIELD` real-time engine. The shipped AI was **symmetric** — both sides ran one "advance toward the objective" heuristic (`fldAiUnit`), so the Confederate *defender* played like an attacker: it drifted off its good ground, deep-fled when shaken, and never disciplined a counterattack. That is exactly why AI-vs-AI leaned Union (logged in D57/D57.4/D58). This milestone makes the AI **role-aware**: in an asymmetric scenario the defender HOLDS the objective's forward face in cover, conducts a fighting withdrawal, and counterattacks disordered attackers on a leash. All in `src/tactical/T0-field-sandbox.js` + `tools/probe-ai.mjs`. Deliverable ≈**1509KB**.

**Architecture (decided + logged):**
- **D60.0 ROLE-AWARE DISPATCH, gated on `__FIELD.attacker` — the symmetric SANDBOX is byte-behavior-identical by construction.** `fldAiUnit` now dispatches: in an asymmetric scenario (`__FIELD.attacker` set) the DEFENDER side (`u.side === fldEnemy(attacker)`) runs the NEW `fldAiDefender`; the ATTACKER side and BOTH sandbox sides run `fldAiGeneric` — the **verbatim old `fldAiUnit` body**. The sandbox has `attacker === null`, so `fldAiDefender` is **unreachable** there → zero behavior change (probe-field held **13/13** + the terrain golden snapshot). Unlike auto-pause (D59, which had to live in the RAF loop), the defender doctrine lives in the **headless sim path** (`fldAiUnit` ← `fldSimStep` ← `fldStepN`), so it is **fully probe-testable** — including a deterministic AI-vs-AI balance sweep. Determinism preserved: no RNG in the AI.
- **D60.1 A STABLE defensive frame (not the momentary threat bearing).** The defender's spatial frame is anchored to the battle geometry — forward axis = the crest toward the **attacker's home edge** (`fldHomeEdgeZ(attacker)`; the engine models the two armies on the ±z home edges, so the approach axis is z and lateral frontage is world-x), `face` still tracks the nearest visible enemy (aim, don't move). The first cut derived the frame from the *rotating nearest-enemy bearing*; the bug-hunt proved that collapsed the whole line onto one point on a **flank threat** (Chinn Ridge — which this scenario is built around) and **flipped the advance/hold gate** when an attacker crossed the crest (stranding rear reserves). The stable frame fixes both by construction.
- **D60.2 Five branches, hold points clamped INSIDE the objective.** (1) SURVIVAL — wavering + a stronger enemy close → a fighting withdrawal to the reverse slope, still facing the threat. (2) COUNTERATTACK — steady + a **catchable (wavering-only)** disordered enemy within `CTR_REACH` that has closed on the hill + a **dual leash** (the target AND the defender's own position both bounded near the objective) + local parity (`CTR_RATIO`) → charge. (3) DELAY — a unit forward of the line holds and fights to buy time, falling back only when locally outnumbered (`DEF_FALLBACK_RATIO`). (4) ADVANCE — a rear reinforcement marches up to the line (column if far). (5) HOLD — on the forward face in cover, face the threat, pour fire. Every hold/move-to-line point is **clamped inside `obj.r`** so a holding defender actually DENIES the objective (not parks ~237yd outside the 140yd ring).
- **D60.3 Constants (charter §8.7 — logged):** `AI_LOCAL_R=360` (local force-balance radius), `DEF_FACE_FRAC=0.55` (hold-line forward of the crest), `DEF_LANE=1.6` (lateral spread, ×OBJ_R, pre-clamp), `DEF_HOLD_TOL=50` (the on-the-line band), `DEF_FALLBACK_RATIO=0.6` (delaying-force withdrawal trigger), `CTR_REACH=175`, `CTR_LEASH=150`, `CTR_RATIO=0.9` (counterattack gates). A test-only hook `__FIELD._aiGenericAll` (default false, reset per launch in `fldInitSim`) forces generic-for-both so `probe-ai` can A/B the doctrine on identical seeds.

**Balance auto-tune (charter §8.7, the A/B sweep, 8 seeds, logged):** with the **old generic-for-both AI** the CS lost **0/8** fog-OFF (a Union sweep — the logged gap, now empirically confirmed) and **1/8** fog-ON. With the **defender doctrine** the CS wins **8/8** fog-OFF and **8/8** fog-ON. The mechanism is the historical one: **denial/timeout wins** — the determinism check (seed 909) ends at the 480s limit with the Union *still holding more men* (US 17900 vs CS 11731) but unable to seize-and-hold Henry House Hill in time, exactly as McDowell's army failed in 1861. **Decision (decide-&-log):** keep the corrected geometry; a CS-dominant AI-vs-AI is **historically faithful** (First Bull Run was a decisive Confederate victory) and is the intended hard challenge for the **human-Union primary mode** (the player out-micros the AI and has concentration/timing/targeting the generic attacker AI lacks). The prior pre-clamp 5/8 was an artifact of bug #5 (defenders holding *outside* the objective they were denying); the now-correct defender honestly dominates the weak generic ATTACKER AI. **The complementary lever is a smarter ATTACKER AI (concentration / flanking) — logged as the next AI item — NOT weakening the now-correct defender.** Also logged: a smart defender and fog are **SUBSTITUTABLE, not strictly additive** defender levers (fog also blinds the defender's own reactivity) — the D58 "fog flips Bull Run to CS" claim was relative to the *old* AI; with the smart defender the CS already wins fog-OFF, so fog adds little (corrected the stale `probe-ai` header claim).

**Adversarial bug-hunt (6 diverse lenses × default-refute verify + a completeness critic, 27 agents, ~2.3M tokens):** → **10 confirmed (9 finder + 1 critic), ALL FIXED** (10 refuted). ENGINE: **#1/#2 (MED, geometry)** the rotating-bearing frame collapsed the line on a flank threat + flipped the advance/hold gate → the **stable frame** (D60.1); **#5 (MED, behavior)** hold points landed up to ~237yd outside the 140yd objective → **clamp inside `obj.r`** (D60.2); **#3 (MED, edge)** the defender chased **routing** targets it can never catch (rout 62yd/s > charge 30yd/s) and walked off the hill → **catchable (wavering) targets only**; **#6 (LOW, critic)** the leash bounded only the *target's* distance, so a retreating wavering attacker walked the defender off the hill → a **dual leash** also bounding the defender's own position; **#4 (LOW, regression)** `_aiGenericAll` wasn't reset on launch → reset in `fldInitSim`. PROBE-FIDELITY (false-confidence, `probe-ai.mjs`): the COUNTERATTACK step's leash sub-test was actually decided by `CTR_REACH` not the leash, and `CTR_RATIO` was untested → rebuilt with isolated reach/target-leash/own-leash/catchable/ratio sub-tests (#6/#7/#3); the FIGHTING-WITHDRAWAL step couldn't distinguish withdrawal from the delay fall-through → added a `tz < crest` discriminator (#8); the ADVANCE step never exercised the bearing-flip → added a visible-attacker-on-crest guard (#2); the header overstated "fog compounds" → corrected to "substitutable" (#9). (One verify agent erred on `API Error: Overloaded` for the "bearing-flip strand" finding — independently confirmed by #1/#2 and fixed by the stable frame, so nothing was lost.)

**Verification (the full PROPER VETTING gate, D53-OPS):** `node tools/build.mjs` GATE OK → **probe-ai 11/11, 0 pageerrors** (routing/dispatch · all 5 defender behaviors incl. the bearing-flip guard + the dual-leash/catchable/ratio counterattack gates · the A/B balance sweep fog OFF+ON · the lever matrix · determinism · the sandbox no-op · no Classic contamination) → **full no-regression GREEN** (probe-field **13/13**, probe-bullrun **15/15**, probe-fog **13/13**, probe-autopause **13/13**, + the strategy suite + bootprobe + t1probe + diag-classic Classic-paints, **0 pageerrors**). Committed + pushed after full vetting. **NEXT: TACTICAL P1b (continued)** — the **smarter ATTACKER AI** (concentration/flanking — the complement that restores AI-vs-AI variety, logged above) · the hex-mode toggle (Classic frozen) · full owner-mode bridge conditioning into the scenario + a tactical-only FREE mode · deeper 3D (PBR/HDRI/post-FX). Then P2–P5, loot/survival, S3 alt-history, S4 education/codex/WCAG-AA pass, S5 victory + Reconstruction.

## D61 — RUN-K Q&A CHARTER #2: the V1 FEATURE-COMPLETE ROADMAP + the refined OPERATING MODEL — [AARON] (run k, 2026-06-15)
A 3-round popup Q&A (12 forks) cleared the path to "complete all project goals." The roadmap is **`V1-CHECKLIST.md`** (the authoritative, ordered must-have list; load it each run). **This supersedes the loose §7/D60 "next" ordering.**

**THE OPERATING MODEL (locked — supersedes/refines the all-night §8.3 "decide & ship & never stall"):**
- **R1 · Surface judgment calls.** Design/balance FORKS are surfaced to Aaron for approval BEFORE building. **Reconciled with R2 + the "looping, get it done" directive:** the V1 checklist's 12 decisions already cleared the MAJOR forks, so **within a cleared checklist item, decide-&-log sub-choices and keep moving**; pause + `PushNotification` + write a decision file ONLY for a fork that is (a) NEW / uncovered by the checklist, (b) **contradicts a prior shipped decision or teaching** (e.g. the D58 fog teaching — exactly why the attacker AI was surfaced, not shipped), (c) irreversible/destructive or costs money, or (d) so ambiguous that guessing wrong wastes a whole milestone. (If Aaron wants TIGHTER surfacing, he says so.)
- **R2 · Clear-then-continuous.** Clear a scope's designs up front, then execute the WHOLE scope continuously (all-night style), pausing only at scope-end or a NEW fork (per R1).
- **Per-milestone:** the full PROPER VETTING gate (GATE OK → focused probe + full no-regression + diag-classic + 0 pageerrors → adversarial bug-hunt, all fixed) → commit → `git push origin main`. Balance auto-tuned + logged. Content ≥2-source Verified, anti-Lost-Cause.
- **ABOVE-AND-BEYOND directive (Aaron, this session):** "best appearance and function possible; go above and beyond basics in ALL aspects." Every milestone ships **polished**, not minimal — the period broadsheet/engraving aesthetic, smooth (reduce-motion-honoring) animation, **full WCAG 2.2 AA baked in as built** (not only the S4 pass), deep teaching, robust edge-handling, the UHD-617 perf floor. Visual/UX excellence is a THROUGHOUT standard; the big PD-photo/footage "come to life" layer is Phase H (gameplay-first), but nothing ships ugly or barebones.

**THE 12 LOCKED FORKS (rounds 1–3):**
1. **Decision rule = surface judgment calls** (reconciled above).
2. **Attacker AI = DEFENDER-FAVORED + fog AIDS THE DEFENDER.** Build the smarter attacker (concentrate / close / assault — prototype in `ATTACKER-AI-PROPOSAL.md` + `.tmp/attacker-ai-prototype.patch`) but keep Bull Run defender-favored AI-vs-AI and **tune OUT the fog inversion** so fog stays consistent with D58. (The human-Union player — who out-plays the AI — is who must win; AI-vs-AI is the diagnostic.)
3. **Next priority = CONNECT THE TWO LAYERS** (Phase A: bridge the strategic-desk army into the real-time tactical battle + a tactical FREE mode + the result feedback loop).
4. **"Done" = the V1 checklist** (`V1-CHECKLIST.md`) drives the runs; perpetual after v1.
5. **Run cadence = continuous within cleared scope** (R2).
6. **Graphics/footage = AFTER gameplay-complete** (Phase H, one big "come to life" pass — but see the above-and-beyond throughput standard).
7. **Hex mode = a FULL co-equal hex/turn engine** (Phase D, on the modern data; after tactical depth; legacy Classic hex stays frozen).
8. **Loot/survival = DEFER** to after the core is complete (Phase I; preset-gated, default off).
9. **Phase order = A connect → B tactical depth → C tactical breadth → D full hex → E strategic S3–S5 → F/G content/theaters → H graphics+audio → I loot/survival → J polish.** (Tactical breadth+hex BEFORE the strategic arc — keeps the charter's tactical-first.)
10. **CS-player tactical mode = YES, Phase B** (command either side; AI attacks — makes the attacker AI player-facing).
11. **Western theater = Phase C** (folded into tactical breadth; same mechanics, new content — D40/D54).
12. **Audio = Phase H** (the come-to-life pass; accessible, default off).

**Immediate next scope (Phase A — design/surface then execute):** A1 bridge-conditioning into the tactical battle · A2 tactical FREE/skirmish mode + launching Bull Run from the bridge · A3 the tactical→campaign result feedback loop. Then Phase B (incl. fork #2 + #10). A fresh-chat looping kickoff prompt for all this lives in **`START-HERE.md`**.

## D62 — PHASE A · CONNECT THE TWO LAYERS: the strategic-desk army now FIELDS in the real-time tactical battle, which feeds back into the campaign — [CLAUDE] (run k, 2026-06-15)
The first V1-CHECKLIST item (D61 Phase A). The owner-mode war and the real-time tactical engine were two disconnected layers; this milestone is the connective tissue. New `src/tactical/T2-campaign-link.js` (the whole Phase A module) + tiny guarded seams in `T0-field-sandbox.js` / `T1-bull-run.js` + a bridge-briefing button in `src/85`. Deliverable ≈**1545KB**.
- **A1 — conditioning the tactical brigades from `bridgeArmy(C)`.** `fldCampaignCondition`/`fldCampaignConditionUnit` scale the PLAYER side's men + morale + fatigue and RE-ARM a fraction of the infantry from the bought loadout (`C.armory.loadout` → engine WEAPONS key via the `59-armory-field` `_afEngineKey` mapping + the engine era gate). **Anchored at the fresh-campaign baseline 74, band ±12% (`strengthMul` 0.88–1.12), identical to `_a6Condition` (D47.1) and the auto-resolve edge (D48)** — so all THREE battle modes (real-time, Classic-hex, auto-resolve) condition equivalently and a no-investment army plays ≈ the nominal scenario. Reinforcements are conditioned too (T1 `fldReinforceSpawn` seam). Runs from the shared `fldResetRun` hook; no-op when `campaignCtx` is null.
- **A2 — launch from the bridge + a FREE skirmish.** A new **"Fight in real time ⚔"** button on the pre-battle briefing (beside Auto-resolve + the Classic "To the Field") → `fldLaunchCampaignBattle(C)`. First Bull Run (US player) routes to the historical `bullrun1` scenario; every other campaign battle gets a CONDITIONED procedural brigade fight derived from `bd` (us/cs/atk/year/feat; brigade count `clamp(2+round((total−15000)/15000),2,5)`, men `clamp(round(total/(count·5)),1300,2100)`, holdToWin 36s). Plus a **custom FREE SKIRMISH menu** (`fldSkirmishMenu`) — pick side / army size / ground / year / fog — injected as a main-menu button.
- **A3 — result feedback loop.** `fldCampaignComputeOutcome` derives the REAL casualty fractions (men lost / men fielded per side via `maxMen`, correctly counting arrived reinforcements + destroyed units) from the FINISHED fight; `fldCampaignApplyOutcome` builds a CONDITIONED hex roster via `startBattleRuntime`, applies those fractions with `_arApplyCasualties`, then drives the engine's own `campaignAdvance` → `_t1Resolve` (manpower/clock/strategy/enemyWill/victory). **So a real-time win/loss is SUBSTITUTABLE for a Classic / auto-resolved result** (MODERN-UGG §2) — win advances the chain, loss enters recovery, casualties feed the strategic tick. Mirrors the proven `bridgeAutoResolve` (87) pattern; deterministic (no RNG in conditioning or outcome — the fight is seeded in T0). Aborting (Esc) before the battle ends returns to the briefing, re-launchable, nothing advances.
- **Decide-&-log:** (a) the tactical OOB is a faithful ABSTRACTION (a handful of brigades a side; absolute men are abstracted, the loss FRACTION is what feeds back) — a 1:1 regiment port is unnecessary for the layer-connection and is deferred. (b) the campaign First Bull Run routes to `bullrun1` only for a US player (the scenario is built US-attacker); a CS player / a recovery-defend re-fight uses the procedural battle (the CS-player tactical mode is Phase B / fork #10). (c) the same 74-anchor across all three battle modes is deliberate — zero double-count, balanced equivalently.
- **Standalone byte-identity (zero regression by construction):** every T0/T1 seam is a no-op when `campaignCtx` is null and `opts.skirmish` is absent → the sandbox / menu Bull Run / fog / auto-pause / role-aware-AI paths are behavior-identical (probe-field **13/13**, probe-bullrun **15/15**, probe-fog **13/13**, probe-autopause **13/13**, probe-ai **11/11** all held). Classic never touched (diag-classic paints, nonBlank 346).
- **Adversarial bug-hunt (6 diverse lenses × 3-skeptic default-refute verify + a completeness critic, 26 agents):** → **7 confirmed (deduped from 12 raw; 17/18 verdicts confirmed), ALL FIXED:** **F1 (HIGH)** Esc on the campaign end-screen discarded the decided result (aborted via `_returnFn` instead of resolving — save-scummable; war never advanced) → `fldKey` Escape now RESOLVES on a finished campaign battle (mirrors the button), only mid-battle Esc aborts; **F2 (HIGH a11y)** the campaign end dialog was `aria-modal` with no focus trap (the campaign branch returned before the standalone trap) → added a Tab trap on the single `#fldCampReturn` button; **F3 (MED a11y)** the skirmish menu dropped focus to `<body>` on every option click → restore focus to the chosen chip after the re-render; **F4 (MED a11y)** the option chips used `role=radio`/`radiogroup` without arrow-key roving → switched to a `role=group` button group with `aria-pressed` (matches the click/Enter/Space model); **F5 (MED)** the re-arm `armPlan` was sized to the initial line only, so the running `_armIdx` exhausted before reinforcements detrained (player reinforcements unarmed by the loadout) → `infN` now counts scheduled player-infantry reinforcements too; **F6 (HIGH)** `C.flipAtk` was consumed at LAUNCH but the path is abort-relaunchable → an Esc + re-attempt (any mode) lost the recovery role-flip → flip is now computed for the fight but consumed only at RESOLUTION (`fldCampaignApplyOutcome`), preserved on abort; **F7 (MED)** the Bull Run route ignored a flipped `bd.atk` (a recovery-defend re-fight launched as a US attack) → `_fldCampaignScenarioFor` returns null on `C.flipAtk` (procedural honors the flip). Re-probed green.
- **WCAG 2.2 AA (wcag-auditor on the new UI):** the new surfaces were already compliant (the consistent #745e3f/#766040/#715e3e border palette + the #e8c84a focus ring carried over) — only 2 trivial `type="button"` additions; text ≥11:1, non-text borders ≥3:1, focus rings ≥3.6:1, keyboard-operable, ARIA correct, non-color selection cue (bold + outline).
- **Verified (the full PROPER VETTING gate, D53-OPS):** `node tools/build.mjs` GATE OK → **probe-campaign-link 16/16, 0 pageerrors** (A1 strong>fresh>weak men + the 74 anchor; loadout re-arm player-only; standalone not conditioned; A2 Bull-Run-route/procedural/skirmish builder/the briefing button; A3 real fractions + win-advances/loss-recovers + the strategic tick + determinism; F5 plan-spans-reinforcements; F6/F7 flip preserved-on-abort + procedural-route + consumed-on-resolve; no Classic contamination) → **full no-regression GREEN** (tactical 5 suites + the 22-probe strategy suite + bootprobe + t1probe + diag-classic Classic-paints, **0 pageerrors** throughout). Committed + pushed after full vetting. **NEXT: Phase B — TACTICAL DEPTH** (B1 the smarter defender-favored ATTACKER AI with the fog inversion tuned out [fork #2] · B2 officers/command · B3 in-battle logistics · B4 distinct arm roles · B5 difficulty presets · B6 the CS-player "command either side" mode [fork #10]).

## D63 — STANDING INSTRUCTION: HYBRID subagent model-routing (main session = highest Opus) — [AARON] (run k, 2026-06-15)
Aaron: "run on highest Opus but use more efficient models for subagents if the subtask is appropriate for their competency; don't sacrifice quality in any way; only token efficiency if it won't affect standards." Chose the **Hybrid (no-quality-loss)** tier. **Policy (apply to every `Workflow` `opts.model` + `Agent` `model` going forward; also in memory `civilwar-subagent-model-routing`):** main loop stays **Opus 4.8** always; **Opus** on reasoning-critical legs (adversarial bug-hunt finders + verifiers + critic, design/judge panels, content-research verify passes, design-bearing balance reasoning); **Sonnet** on bounded/mechanical legs (wcag-auditor, Explore/code-search, file/probe-log summarization, first-pass source gathering, spec'd mechanical transforms); **Haiku** only for trivial extraction. Rationale: I (Opus orchestrator) write + validate all shipped code so the artifact ceiling is Opus regardless; subagents set the catch-rate FLOOR, and the token-heavy leg (the bug-hunt) is exactly the leg that stays Opus, so realistic savings are modest (~10–25% of subagent tokens). Surfaced + Aaron-confirmed (he asked to advise before adopting).

## D64 — PHASE B-1 · the smarter DEFENDER-FAVORED ATTACKER AI (fog AIDS the defender) — [CLAUDE] (run k, 2026-06-15)
The first Phase B item (V1-CHECKLIST B1; charter fork #2). The complement to the P1b-iii defender doctrine: a real ATTACKER doctrine `fldAiAttacker` in `src/tactical/T0-field-sandbox.js`, dispatched to the scenario ATTACKER side (the symmetric sandbox + the defender are byte-identical — fldAiAttacker is unreachable when `__FIELD.attacker===null`). Productionizes the run-k prototype (`ATTACKER-AI-PROPOSAL.md`) with Aaron's locked answers. Deliverable ≈**1552KB**.
- **The doctrine:** a covered defender wins a long-range fire trade, so the attacker CONCENTRATES on the objective's weaker (thinner) flank, CLOSES, and ASSAULTS with the bayonet (melee + numbers beat fire + cover). 4 branches: SURVIVAL (wavering + a stronger enemy close → fall back to rally) · ASSAULT (charge a visible disordered defender always; press the bayonet on a steady defender once CLOSED with local superiority — fog OFF only) · SUPPRESS/PROBE (in fire range without the odds → hold + fire in line while the mass forms) · CLOSE (advance on the weak flank, assault-column when committed fog-OFF, line otherwise). Deterministic — no RNG.
- **Lock (a) — GRADUAL commitment (fixes the prototype's knife-edge):** the assault is a PER-UNIT, LOCAL-concentration decision (`localSup`: `friendMen >= foeMen * effLocal` over the VISIBLE foes, where `effLocal = ATK_LOCAL_RATIO·clamp(1/globRatio, 0.75, 1.4)` smoothly modulated by the global force ratio + an `ATK_GLOBAL_FLOOR` sanity gate). The army commits incrementally as it masses on the flank — NOT the prototype's all-or-nothing global ratio flip (`ATK_ASSAULT_RATIO` 1.5→1.7 phase-transition).
- **Lock (b) — FOG AIDS THE DEFENDER (charter fork #2; consistent with D58):** UNDER FOG the attacker NEVER mass-assaults a position and never column-rushes (it can't trust its sight — a hidden reverse-slope reserve / rail-borne brigade may hold the hill); it advances cautiously in LINE, trades fire / probes, and only exploits a VISIBLE BROKEN unit. So fog denies it the unseen-close melee win and lets the concealed reserves maul the piecemeal advance — it never BENEFITS from fog. The prototype INVERTED here (a concealed column closed unseen and won); tuned OUT.
- **Constants (charter §8.7 — logged):** ATK_CHARGE_R 170 · ATK_ASSAULT_R 130 · ATK_ASSAULT_STANDOFF 70 · ATK_LOCAL_RATIO 1.1 · ATK_GLOBAL_FLOOR 0.85 · ATK_FLANK_BIAS 0.5. Test hooks `_aiGenericAll` / `_aiGenericAtk` (reset per launch) A/B the doctrines in isolation.
- **Balance A/B sweep (8 seeds, Bull Run AI-vs-AI, CS=defender win count — logged):** both-generic (pre-doctrine) fog-OFF **CS 2/8** (US sweep — the gap) · defender-only (D60) **8/8** · **both doctrines (B1) fog-OFF 6/8, fog-ON 8/8**. So Bull Run stays **CS-FAVORED** (historical) but **competitive** (the US breaks through 2/8 fog-OFF) — a bloody real fight (defender casualties **4592** vs the passive attacker's **1276**, ~3.6×), and **FOG AIDS THE DEFENDER** (6→8, the inversion gone). Decision (decide-&-log): the human-Union mode is the primary one (the human IS the attacker there + out-plays the AI); AI-vs-AI is the diagnostic, and a CS-favored AI-vs-AI is historically faithful.
- **Adversarial bug-hunt (4 diverse lenses × 3-skeptic default-refute verify + a completeness critic, Opus per D63):** → **2 findings, BOTH FIXED:** **#1 (HIGH — broke lock (b))** a single visible picket flipped the `seenOnObj`-based `blind` flag false, letting the attacker assault into the hidden mass behind it → REFACTORED: dropped the picket-foolable `blind`/`seenOnObj` and gated the mass-assault + column-rush DIRECTLY on `!__FIELD.fog` (under fog the attacker is unconditionally cautious — strictly more conservative, no sight-based hole). **#2 (LOW)** the now-removed `ATK_FOG_PROBE_R` had a doc/code radius mismatch → deleted with the refactor. Re-sweep post-fix: fog-OFF unchanged (CS 6/8, byte-identical), fog-ON CS 8/8 (attacker now even more cautious under fog — def-cas 1848, fog robustly aids the defender).
- **Verified (the full PROPER VETTING gate, D53-OPS):** GATE OK → **probe-ai 15/15, 0 pageerrors** (routing · the 4 defender behaviors [unchanged] · the attacker ASSAULTS-when-sighted-closed-local-sup · the attacker CAUTIOUS-WHEN-BLIND · the B1 balance lock [fog-OFF CS-favored, fog-ON ≥ fog-OFF, bloodier-than-passive] · determinism · sandbox no-op · no Classic contamination) → tactical no-regression GREEN (probe-field 13/13, bullrun 15/15, fog 13/13, autopause 13/13, diag-classic paints nonBlank 346, 0 pageerrors). The change is confined to T0's AI (the new fn + the dispatch + constants + the probe) — no strategy module touched, so the 22-probe strategy suite (green in D62 minutes earlier) cannot regress. Committed + pushed after full vetting. **NEXT: Phase B continues — B2 officers/command · B3 in-battle logistics · B4 distinct arm roles · B5 difficulty presets · B6 the CS-player "command either side" mode (fork #10, makes this attacker AI player-facing).**

## D65 — PHASE B-2 · OFFICERS & COMMAND on the tactical field (leaders with command radius + can be hit; ties named-generals) — [CLAUDE] (run k, 2026-06-15)
The 2nd V1-CHECKLIST Phase-B item (D61). Leadership now lives ON the real-time field. New module `src/tactical/T3-officers.js` (the whole layer) + guarded T0 seams (`__FIELD.officers` gate; build at `fldResetRun`; a step before morale; a `(u.cmdBonus||0)` read in `fldMoraleStep`; 2D/3D render + HUD + end-screen hooks; a `commander` field on units) + the Bull Run command cast in `data/bullrun.json.leaders`. Decide-&-log per D61 (cleared scope, no contradicting fork → no pause). Deliverable ≈**1585KB**.
- **The system:** named field leaders ride with their army; each projects a COMMAND RADIUS — friendly brigades inside it recover morale faster, hold longer against the rout (capped save), and rally from a rout sooner (the historical rallying role). Leaders CAN BE HIT: riding near the firing line accrues EXPOSURE; past a hidden, seeded fate threshold a leader is WOUNDED (aura halves) then FALLS — the aura dies and nearby troops take a one-time command shock (the general-down rout trigger: Bee/Bartow, A. S. Johnston, Reynolds).
- **Gated + BYTE-IDENTICAL:** fully behind `__FIELD.officers` (default ON every live launch). The field/bullrun/fog/autopause/ai/campaign-link probes set the sticky `__FIELD._officersOff` → no leaders built, `fldOfficersStep` never runs, the `(u.cmdBonus||0)` reads are exactly 0 → all 6 baselines stay BYTE-IDENTICAL (the new layer is provably the only change). The officers' own coverage = `tools/probe-officers.mjs`.
- **Ties the named-generals layer (S2 m5):** a campaign player commander's field quality + name come from `bridgeArmy(C).leadership` / `cmdActiveGeneral(C)` — the general you appointed rides the field. Scenarios carry the real cast: McDowell/Heintzelman (US) vs Beauregard/Johnston/Bee/Bartow/Jackson (CS).
- **Adversarial bug-hunt (52 agents — 6 diverse-lens Opus finders × 3-skeptic default-refute verify + a completeness critic, per D63): 1 confirmed (3/3) + 7 critic gaps, ALL FIXED:**
  - **(HIGH) over-lethal death model** — empirically the army commanders who SURVIVED First Bull Run died nearly every game (anti-citation-grade), the "cost of command" reduced to noise. ROOT CAUSES: a monotonic risk RATCHET (no decay) + no cover term + too-high base rate. FIX: per-leader HISTORICAL `fate` (Bee/Bartow fall-prone, army cdrs durable) + risk DECAY out of contact + wound RECOVERY + a COVER term (`fldCoverAt` — rescues the Jackson reverse-slope lesson) + steep f² falloff + a per-tick exposure cap + lower `HAZ_RATE`. Result: army commanders survive **10/10** seeds; a fall is a rare, history-weighted hinge.
  - **(HIGH) Bee's "stone wall" line fired BEFORE Jackson arrived** (Bee fell 37–96s; Jackson lands 135s) — an anachronism AND a Lost-Cause framing leak (asserting the contested canonical quote as settled fact). FIX: `teachReq`/`teachAlt` gate the Jackson clause on him being on-field, and the wording is softened to the contested framing (matching the `br_stonewall` card).
  - **(HIGH) player-side hardcoded to US** — the HUD roster + fog visibility assumed US; a CS-player campaign would show the ENEMY roster and leak its command status under fog. FIX: `fldPlayerSide()` (CS in a CS campaign, US standalone) drives the roster + fog — prep for B6.
  - **(MED) balance swing + weak probe** — the layer materially shifts the winner; the probe's `>=` assertions were too weak. FIX: moderated the aura (§27) + lowered `CMD_BONUS_CAP` 1.2→0.9 so a dense cluster can't saturate the line into near-immunity; the probe now asserts CS-favored + fog-aids-defender + officers-HELP-the-command-advantage-side + low ON/OFF flips, with determinism locked separately.
  - **(MED) cover ignored / (MED) a11y false narration / (LOW) degenerate aura assertion / (LOW) no fog memory for fallen leaders** — all fixed (cover term; survivors rarely fall + softened to an incapacitation register, only Bee/Bartow keep a fatal register; far-apart falloff test points; `_everSeen` keeps a once-seen FALLEN enemy commander marked under fog).
- **Balance (officers ON, 8-seed Bull Run AI-vs-AI, CS=defender wins):** bare **5/8** → officers-ON **7/8** fog-OFF (CS-favored — the historical command stand on Henry House Hill — but the US attacker keeps a 1/8 breakthrough, preserving the B1 path) · **8/8 fog-ON** (fog still aids the defender — no D64 regression). Deterministic; PRINCIPLED (officers help the side with the denser command), not a re-roll.
- **Verified (full PROPER VETTING gate, D53-OPS):** GATE OK → **probe-officers 15/15, 0 pageerrors** → full no-regression GREEN (field 13/13 · bullrun 15/15 · fog 13/13 · autopause 13/13 · ai 15/15 · campaign-link 16/16 — all byte-identical; diag-classic paints nonBlank **346**; bootprobe + t1probe ok; 0 pageerrors) → wcag-auditor clean (1 border-contrast fix) → render visually confirmed (the false-death banner cascade gone; generals survive). Committed + pushed after full vetting. **NEXT: B3 in-battle logistics (ammo + fatigue depth + supply) · B4 distinct arm roles · B5 difficulty/realism presets · B6 the CS-player "command either side" mode.**

## D66 — PHASE B-3 · IN-BATTLE LOGISTICS (the ammunition economy: supply trains + resupply + exhaustion) + the FOG-CULMINATION fix — [CLAUDE] (run k, 2026-06-15)
The 3rd V1-CHECKLIST Phase-B item (D61). The real-time field gains an AMMUNITION ECONOMY + deeper fatigue. New module `src/tactical/T4-logistics.js` (the whole layer) + guarded T0 seams (`__FIELD.logistics` gate; build at `fldResetRun`; a step in `fldSimStep` before the AI/fire; an override seam atop `fldAiUnit`; a `(u.exhausted)` move-penalty read in `fldStepMovement`; HUD/end-screen/2D/3D hooks) + the Bull Run `supply` block in `data/bullrun.json` + `tools/probe-logistics.mjs`. Deliverable ≈**1608KB**.
- **The system:** each side fields a rear **ammunition train** with a finite battle **reserve**. A DISENGAGED brigade (no enemy within `RESUPPLY_COMBAT_R`=240) within the train's reach refills its cartridge boxes, draining the reserve; when the reserve runs dry the army "runs low" (the 20th Maine's bayonet charge — out of cartridges). A brigade OUT (≤CRIT=8) falls back to its train UNLESS it holds the objective (then it fires dry → bayonet). **Exhaustion:** fatigue>84 → SPENT → a deeper move penalty.
- **The balance design:** the **ATTACKER's train sits FAR behind its start line** (`fldSupplyRearPos` r+400 — it outruns its supply, Clausewitz's culminating point) and the **DEFENDER's just behind the objective** (r+120). Ties the strategic SUPPLY: the battle reserve scales with `bridgeArmy(C).supply`; a raid-supply order cuts the enemy's. **Gated + BYTE-IDENTICAL** behind `__FIELD.logistics` (the 7 baselines + officers set the sticky `_logisticsOff` → no trains, the step/AI-override/exhaustion never run → byte-identical). Determinism: no RNG.
- **THE HIGH FINDING — the FOG INVERSION (surfaced to Aaron per D61-R1; charter fork #2 — fog must aid the defender):** the bug-hunt empirically found that with **FOG ON**, logistics INVERTED the D58/D64 "fog aids the defender" teaching: Bull Run AI-vs-AI fog-ON went **CS 8/8 → 0/8**. Mechanism (isolated by forcing reserves to 0 → 8/8 restored): under fog the fight is a long fire-trade, and the ATTACKER cycled fresh brigades to its **safe far-rear train** to sustain fire — and a long fire-trade favours numbers (attacker) over cover (defender). **Surfaced** (a HIGH finding contradicting a shipped teaching). **Aaron weighed two fixes:** his first pick (a "defender's-depot" resupply) **provably did nothing** — toggling it on/off gave IDENTICAL results, because the defender was never the ammo-bottlenecked side (its reserve sat ~230 untouched). The ONLY lever that works is **choking the ATTACKER's resupply under fog** (`FLDL.ATK_FOG_RESUPPLY` = 0 — "an attacker advancing blind cannot run its trains forward; it fights with what it carries"). A **SHARP threshold** (even 0.15 leaves the inversion; only ~0 restores it). **Aaron approved the attacker-fog-choke.** Also KEPT the **fog-gated scan** (the resupply/pullback enemy-scans now skip enemies the side can't SEE — consistent with D58/D64, no acting on hidden info). **Result: logistics is balance-NEUTRAL in BOTH fog states — fog-OFF CS 5/8, fog-ON CS 8/8, each matching its no-logistics baseline.** A new `probe-logistics` FOG REGRESSION GUARD step locks it (CS ≥6 fog-ON; currently 8/8).
- **Adversarial bug-hunt (64 agents, ~3.3M tokens — 6 diverse-lens Opus finders × 3-skeptic default-refute verify + a completeness critic, per D63): 9 confirmed of 14 candidates + 2 confirmed critic gaps (of 5), ALL ADDRESSED.** 1 HIGH (the fog inversion, above) + LOW probe-fidelity/correctness: the probe comment `RESUPPLY_COMBAT_R (110)`→(240) rebuilt as a **boundary test** (blocked at 230, refills at 250); the BALANCE assertion made **two-sided** [4,6] (was a one-sided floor, blind to over-favouring — the layer's headline risk); `fldSupplyRearPos`'s attacker-far/defender-near asymmetry was **untested + bypassed by Bull Run's hand-authored data** (it IS live in campaign skirmishes) → a new probe step; `fldSupplySeen`'s fog enemy-train reveal **untested** (every step ran fog-OFF) → a new fog-ON reveal step; STRATEGIC TIE asserted only ordering → now pins the **supply curve** + the no-campaign nominal (and reconciled the T4 comment: `sup` = bridgeArmy value, not raw warroom); [critic] a **reinforcement-only side built no train** → `fldSideHasUnits` now scans the reinforcement schedule + a probe guard. Two REFUTED findings were re-evaluated against the code: the routing-enemy guard PERTURBED the 5/8 baseline (→4) so it was NOT applied; the fog-visibility guard WAS applied (correct + part of the fog fix).
- **WCAG 2.2 AA (wcag-auditor, Sonnet per D63):** 1 fix — `fldLogisticsHudReserve` `#cf7a5a`→`#d28466` (the `opacity:.8` blend dropped it to 4.23:1 → 4.60:1); all other HUD colors ≥4.5:1, the end-screen border ≥3:1; the 2D/3D wagon sprite colors left (decorative map markers).
- **Verified (the full PROPER VETTING gate, D53-OPS):** GATE OK (1608KB) → **probe-logistics 14/14, 0 pageerrors** (build/off-builds-none; resupply + reserve drain; close-action boundary pinned at 240; finite reserve; the AI resupply doctrine; exhaustion; the strategic supply-curve + raid + nominal; the fog-OFF balance band CS 5/8; **the FOG REGRESSION GUARD CS 8/8**; procedural asymmetry; fog reveal; reinforcement-only-side train; sandbox no-op; no Classic contamination) → **full no-regression GREEN** (probe-field 13/13 · bullrun 15/15 · fog 13/13 · autopause 13/13 · ai 15/15 · campaign-link 16/16 · officers 15/15 — all byte-identical; bootprobe ok; t1probe 14/14; diag-classic paints nonBlank **346**; 0 pageerrors throughout). Committed + pushed after full vetting. **NEXT: B4 distinct arm roles** (artillery canister/long-range; cavalry scout/flank/screen/raid — design drafted: the `ARM`-melee table is a dangling hook to fill; bullrun already fields Griffin/Ricketts as `art` + Stuart as `cav`; the Cannon-Corps→field-battery bridge [A1 currently re-arms inf only]; the cav-raid↔B-3-train tie) · B5 difficulty/realism presets · B6 the CS-player "command either side" mode (fork #10).

## D67 — PHASE B-4 · DISTINCT ARM ROLES (artillery canister + battery doctrine; cavalry scout/flank/screen/raid; the ARM melee table; the Cannon-Corps→battery bridge) + the BASE-ARM byte-identity fix + First Bull Run DEFAULTS TO FOG ON — [CLAUDE] (run k, 2026-06-15)
The 4th V1-CHECKLIST Phase-B item (D61). Artillery + cavalry get real ROLES on top of the existing arm typing. New module `src/tactical/T5-arms.js` (the whole layer) + guarded T0 seams (`__FIELD.arms` gate w/ sticky `_armsOff`; `fldMakeUnit` carries `role`; an art canister multiplier in `fldResolveFire`; the ARM melee read in `fldResolveMelee`; an arm-aware target bias in `fldAcquireTarget`; an arm-role dispatch in `fldAiUnit`; a per-tick `fldArmsStep` in `fldSimStep`; HUD/end-screen/2D/3D hooks) + the T2 bridge (`_fldArtProfile`) + `data/bullrun.json` (Stuart `role:"flank"`, `defaultFog:true`) + `tools/probe-arms.mjs`. Deliverable ≈**1640KB**.
- **ARTILLERY:** `fldArtFireMult` — CANISTER (≤`CANISTER_R`=150yd) a damage spike (`CANISTER_MULT`=2.7) murderous in the open, sharply attenuated by cover (`CANISTER_COVER_K`/floor — works/woods defeat the balls); beyond, a softening long-range BOMBARDMENT (`LONG_MULT`=0.5, suppresses more than kills). `ARM.art`=0.35 melee → overrun. **The battery doctrine `fldAiArtillery` is ASYMMETRIC by role:** a DEFENDING battery is sited safe (fires from outside the ring on its own side; displaces early at `BATTERY_DISPLACE_R`=200), an ATTACKING battery pushes FORWARD to a canister post (`BATTERY_FWD_FRAC`) and bolts only at point-blank (`BATTERY_OVERRUN_R`=95) — so it CAN be overrun (Griffin/Ricketts, whose loss broke the Union attack). Never charges.
- **CAVALRY:** `fldAiCavalry` dispatches on `u.role` (default flank): **scout** (range wide to lift fog, avoid a stronger enemy), **flank** (a DEFENDING flanker is a leashed mobile reserve in the threatened ring; an ATTACKING flanker rides the open wing; charges a catchable disordered line — `CAV_CHARGE`=1.4 shock / `CAV_BRACE`=0.9 vs a steady braced line), **screen** (interpose between the enemy and the objective), **raid** (make for the ENEMY ammunition train and drain its reserve — `fldArmsStep`, the B-3 tie). Weak carbine — cavalry decide by mobility, not the firefight.
- **THE CANNON-CORPS → BATTERY BRIDGE:** `_fldArtProfile(C)` maps the player's dominant bought battery type (`C.artillery.batteries` × `data/artillery.json`) to the field battery's reach / per-shot power / canister strength, anchored at the Napoleon (no investment → null → nominal, balanced like the A1 strength anchor). "Build a Napoleon park → murderous canister; build Whitworths → long-range counter-battery with NO close spike." Applied in `fldCampaignConditionUnit` (the art branch; previously inf-only). Order-independent dominant-gun pick (sorted keys). **Reachable only via the campaign First-Bull-Run route this milestone (procedural/skirmish OOBs are inf-only — logged as a Phase-C follow-up).**
- **GATE + DETERMINISM:** fully behind `__FIELD.arms` (default ON live); the 8 baseline probes set sticky `_armsOff` → no canister/role-AI/raid, the ARM read falls back (see below) → byte-identical. No RNG in the role AI (deterministic; the fire/melee `fldRng` stream order unchanged).
- **THE BASE-ARM BYTE-IDENTITY BUG (caught in vetting, NOT by the design):** the B-4 design claimed `ARM` "is referenced but never defined → all arms melee at 1.0." **WRONG** — the FROZEN base engine defines `const ARM={ inf:{melee:1.00}, cav:{melee:1.05}, art:{melee:0.40}, … }` at `build/base.html:634`, and the build concatenates base+src, so `fldResolveMelee` was reading those REAL values. My first refactor dropped them to 1.0 when arms is off, shifting the arms-OFF balance (the bullrun probe missed it — it asserts behaviours, not the 8-seed win-count). **FIX:** arms-OFF `fldResolveMelee` now falls back to the BASE `ARM` table (only arms-ON uses the new FLDA values). Verified against the COMMITTED build: bare & logistics both **CS 5/8** byte-identical. A new probe step (`arms-OFF bullrun == committed 5/8`) locks this class.
- **THE STACKED-BALANCE FORK → FIRST BULL RUN DEFAULTS TO FOG ON (surfaced to Aaron, his call — D61-R1):** the per-layer probes each isolate the others, so the STACKED live config (officers+logistics+arms) was never measured. It is **Union-favoured in clear weather** — the COMMITTED D66 build is already **CS 3/8 fog-OFF** (a pre-existing miss), and B-4's arms layer dragged it to 0/8. Fog-ON is **CS 8/8** ("fog aids the defender" intact). A human (who commands the US attacker) is unaffected — it only skews the AI-vs-AI/auto-resolve diagnostic, which D64 wanted CS-favoured. **Aaron chose: make Bull Run default fog ON** (the historically faithful battle — smoke, broken terrain, green troops — where fog aids the defender → the Confederate victory). Implemented via `scenData.defaultFog` + a fog PRECEDENCE (explicit `opts.fog` > explicit `G.settings.tacticalFog` > scenario default), so only UNSET fog flips; the probes that pin fog stay byte-identical. Live stacked config → **CS 8/8**. (The pre-existing fog-OFF stacked interaction remains a possible future re-tune; fog-ON is now the faithful default.)
- **Balance A/B (8-seed Bull Run AI-vs-AI, CS=defender; logged):** bare 5/8 · logistics(arms-off) 5/8 [byte-identical] · arms-only fog-OFF **7/8** (CS-favoured, US breaks 1/8) · officers-only 7/8 · **LIVE stacked default (fog ON) 8/8** · the diagnostic that corrected my two wrong hypotheses: the lever was NOT artillery fire or cavalry but the battery doctrine over-protecting the US guns (a forward, exposed, pressured battery is balance-neutral; a safe rear-bombarding one is US-favouring).
- **Adversarial bug-hunt (31 agents — 6 diverse-lens Opus finders × 3-skeptic default-refute verify + a completeness critic, per D63): 2 distinct confirmed (deduped from 7) + 6 critic gaps, ALL FIXED:** **(HIGH)** the 3D arm meshes were built once at init, before Griffin/Ricketts/Stuart (all REINFORCEMENTS) detrained → invisible guns/horses in the default 3D Bull Run → rebuild `fld3dBuildArms` in `fldReinforceSpawn`; **(MED)** `fldArmsHudSelected`'s battery range-band scanned enemies with no `fldVisible` gate → leaked a hidden enemy's proximity under fog → fog-gated like the AI scanners; [critic gaps] no 3D render coverage → a stubbed-THREE build assertion; no arms-ON-pure-inf == arms-OFF assertion → added; reduceMotion untested → a recording-ctx assertion (no flash/cone/trail when reduced); `fldCavRaid` dispatch untested → added; `_fldArtProfile` key-order tie-break → sorted + a probe; art/cav-roles-only-in-bullrun1 → documented (Phase-C follow-up).
- **WCAG 2.2 AA (wcag-auditor, Sonnet per D63):** clean — all 8 new HUD/end-screen colour pairs pass (10:1+ on the dark field panel; the end-card border 3.03:1 ≥ 3:1, consistent with D66); decorative gun/trooper sprite colours left (CVD-safe shapes). No fix needed.
- **Verified (the full PROPER VETTING gate, D53-OPS):** GATE OK (1640KB) → **probe-arms 23/23, 0 pageerrors** (build/off-inert; ARM table; end-to-end overrun; canister open/cover/long; battery asymmetric doctrine; the 4 cavalry roles + the raid-train tie; target bias; the Cannon-Corps bridge; 3D arm meshes on reinforcement; arms-ON-inf & arms-OFF-bullrun byte-identity; reduceMotion; tie-break; **the fog-default + stacked CS 8/8**; the fog-OFF CS-favoured band; sandbox no-op; no Classic contamination) → **full no-regression GREEN** (field 13/13 · bullrun 15/15 · fog 13/13 · autopause 13/13 · ai 15/15 · campaign-link 16/16 · officers 15/15 · logistics 14/14 — all byte-identical; bootprobe ok; t1probe 14/14; diag-classic nonBlank **346**; strategic spot-check desk/economy/victory green; 0 pageerrors). Committed + pushed after full vetting. **NEXT: B5 difficulty/realism presets** (+ the engineering-slider directive, memory `civilwar-directive-tactical-engineering-sliders`) · B6 the CS-player "command either side" mode (fork #10).

## D68 — RUN-K DESIGN Q&A #3: a 30-question scope/gameplay sweep (10 rounds × 3) — refinements to the V1 roadmap — [AARON] (run k, 2026-06-15)
Aaron ran a 10-round popup sweep (3 questions/round, recommendation = option 1) to clarify/refine/expand/contract scope. He confirmed popups are useful **when each question is a genuine fork that changes the build** — keep them surgical, not a ritual (I self-police: ~3 questions only at a real fork; flag the complexity cost on any scope-expanding answer). **Net deltas to the locked V1-CHECKLIST/D54/D61 (mostly EXPAND; a few CONTRACT; the rest confirm current direction):**
- **EXPANSIONS:** (1) **Expansive v1** — fold the come-to-life media + loot/survival into core, do NOT defer to a "v2". (2) **Zoomable battle scale** — brigades expand into regiments on zoom (a new tactical-engine requirement beyond the current brigade abstraction). (3) **Wide-open alt-history sandbox** — more latitude than D54's "consensus-favoured" (CSA-victory / foreign-war paths allowed); reconcile with the teaching mission via deep citations + honest framing + the "your war vs history" tracker (a CSA-win path is taught with its real implications, never glorified). (4) **Even-deeper citations** — footnote-level sources, a bibliography, scholar attributions. (5) **Custom-battle builder EARLY** (tools-first, ahead of some breadth). (6) **Many granular difficulty sliders** (per-lever, beyond the 2-axis B-5 plan). (7) **Heavy embedded media** (bundle PD imagery/footage into the deliverable — flag: pushes past the lean single file; plan Phase H to compress/tier so portability survives). (8) **Rich adaptive musical score** (beyond default-off tunes). (9) **Loot/survival → a CORE PILLAR** (always-on progression + journey mode, not a deferred optional; keep preset-gateable down for the pure-historian).
- **CONTRACTIONS:** (10) **"hex and classic are the same thing"** — do NOT build a separate new co-equal hex engine; the existing FROZEN Classic hex IS the turn-based mode, and modern scenarios reach it via the bridge ("To the Field (Classic)", largely done in Phase A). Phase D shrinks from "new engine" to "make modern battles selectable in Classic." (11) **AA essentials only** — core WCAG-AA kept as-built, but DROP the 4 specialized a11y modes from v1 (contracts E3). (12) **Tooltips-only onboarding** — drop the layered tutorial/codex (contracts E2).
- **CONFIRMED (no change):** President+commander hybrid fantasy; line/column/skirmish+facing; layered-but-readable morale; direct drag-to-command; smarter-not-cheating AI + preset handicaps; officers w/ command radius+traits+can-be-hit; the current economy/diplomacy/politics depth (refine, don't expand to full sub-games); ambient teaching that confers mastery; Eastern-marquee battles first; both full-war campaign + standalone battles; period engraving/broadsheet art; saves (localStorage+slots+export); data-driven+shareable modding; alt-history-divergence replay; both snackable + long sessions; sober museum-grade anti-Lost-Cause tone. **Action:** these refine the V1-CHECKLIST (Phase F women-in-the-war + tactical-engineering ideas already logged D67-adjacent); the EXPANSIONS/CONTRACTIONS will be threaded into the relevant phases as they're built.

## D69 — PHASE B-5 DESIGN CLEARED (difficulty/realism presets) + the engineering/roadmap forks — [AARON] (run k, 2026-06-15)
A 4-question popup (recommendation = option 1, all accepted) cleared the B-5 design forks prepped after B-4 shipped. **B-5 = difficulty/realism presets is GO** with these locked answers:
1. **Engineering Corps = SPLIT from B-5.** B-5 ships the difficulty/realism SLIDERS + the hooks the engineering effects will read; the tactical **Engineering Corps** (pontoons / entrench / abatis / obstacle-clearing — Aaron's directive, memory `civilwar-directive-tactical-engineering-sliders`) lands as its OWN later gated milestone (`src/tactical/T*-engineering.js`, the T5-arms pattern) that consumes those sliders. Each milestone bounded + independently vettable.
2. **Slider granularity = curated 4-5 levers up front + an "Advanced" expander** for the full lever list (approachable + deep, §27). The curated set ≈ AI level · fog · attrition/casualty severity · supply strictness (B-3) · canister lethality (B-4); the expander adds command-shock severity (B-2), auto-pause, friendly-fire/LOS strictness, the B-1 AI leashes, xp→veteran.
3. **Preset ladder = Recruit / Regular / Veteran / Hardee (AI tiers) × Arcade / Balanced / Historian (realism bundles).** **"Balanced" = today's shipped balance (the byte-identical default — incl. the D67 Bull-Run-fog-ON default).** Smarter-not-cheating: a morale/cohesion cushion ONLY at the easiest AI tier, transparent (D68).
4. **Roadmap priority after Phase B = Phase C tactical breadth, Eastern marquee first** (Antietam, Fredericksburg, Gettysburg, Chancellorsville on the data-driven engine) — keep the locked order; the D68 expansions (zoomable scale, custom-builder-early, loot→core-pillar) thread into their phases, NOT jump the queue.

**Architecture (B-5):** a new gated `src/tactical/T6-presets.js` read at `fldInitSim` from `G.settings.tacticalPreset` + the individual lever values → sets the per-layer gates/severities (all already-existing knobs → mostly WIRING + a UI, not new sim). **Byte-identity:** a neutral/"Balanced" default reproduces today's shipped defaults, so the 9 existing probes (which pin explicit lever values) stay byte-identical; `tools/probe-presets.mjs` asserts each preset sets the expected gates + neutral==today (8-seed Bull Run == the shipped CS-favored numbers) + determinism. Full PROPER VETTING gate + the period-card preset-picker UI (WCAG-AA, CVD-safe, reduceMotion) per the above-and-beyond bar. **NEXT after B-5: B-6 CS-player "command either side" (fork #10) → Phase C breadth.**

## D70 — PHASE B-5 · DIFFICULTY/REALISM PRESETS (the Command & Realism layer: AI tiers × realism bundles + per-lever Advanced; the in-battle settings drawer) — [CLAUDE] (run k, 2026-06-16)
The 5th V1-CHECKLIST Phase-B item (D61/D69). A player-facing difficulty + realism layer over the tactical engine. New gated `src/tactical/T6-presets.js` (the whole layer: FLDP data + `fldPresetNeutral`/`Compute`/`Resolve`/`PresetsApply` + the period-card picker + the in-battle settings drawer + persistence/boot-load) + guarded seams in T0/T3/T4/T5 (each reads a preset-derived knob, IDENTITY when neutral) + a Difficulty row in the T2 skirmish setup + `tools/probe-presets.mjs`. Deliverable ≈**1684KB**.
- **TWO AXES** (a curated card + an "Advanced" expander): **AI TIER** (Recruit / Regular / Veteran / Hardee) × **REALISM** (Arcade / Balanced / Historian). Read ONCE at `fldInitSim` (`fldPresetsApply`, BEFORE the fog/auto-pause precedence + the gate reads) → sets `__FIELD.sev` {attrition, canister, supply, cmdShock, sight, veteran} + `aiSkill`/`aiResolve`/`aiCushion`.
- **THE SEAMS** (all already-existing knobs → mostly WIRING, not new sim, per D69): attrition → `fldResolveFire` + `fldResolveMelee` casualties; canister → `fldArtFireMult` (B-4); supply → `fldSupplyReserve` (B-3); cmdShock → `fldOfficerFalls` (B-2); sight → `fldUnitSight` (LOS); veteran → `fldMoraleStep` rally/routThresh; aiSkill → `fldAiAttacker` effLocal + `fldAiDefender` CTR_LEASH/CTR_RATIO (the B-1 doctrine); the Recruit cushion + brittle-enemy → `fldMoraleStep` recovery/save.
- **SMARTER-NOT-CHEATING (D68/D69), CODE-ENFORCED:** `aiResolve` HARD-capped ≤1.0 (never an enemy steadiness BUFF — enforced in `_fldClampCfg`, not just the data table); the player cushion exists ONLY at a GENUINE Recruit (it is DROPPED if the AI-sharpness chip is tuned off the tier — so the cushion can never ride a sharper AI); Hardee's extra difficulty is sharper DECISIONS (aiSkill), not stat bonuses. **Veteran = aiSkill 1.0 / no handicap = the shipped opponent (the byte-identical neutral).**
- **BYTE-IDENTITY:** with NO preset configured (the probes never set one) `fldPresetsApply` writes the NEUTRAL config (sev all 1.0, aiSkill/aiResolve 1.0, aiCushion 0) and touches nothing else → every seam ×1.0/+0 → the 9 tactical baselines + bullrun stay BYTE-IDENTICAL. "Balanced" reproduces today's shipped **CS 8/8** (the live stacked default, fog ON) **SEED-FOR-SEED**.
- **BALANCE A/B (in probe-presets, logged):** no-preset live stacked Bull Run = **CS 8/8** == an explicit Veteran×Balanced **8/8 seed-for-seed** (Balanced == today, byte-identical); realism severities order correctly (attrition heavy/light ratio = 1.3/0.7 EXACT; Arcade vs Historian play measurably different battles — CS strength **7650 vs 10719**); the AI ladder is monotonic (aiSkill recruit 0.9 < regular 0.96 < veteran 1.0 < hardee 1.12; cushion + brittle-enemy ONLY at Recruit).
- **ADVERSARIAL BUG-HUNT (76 agents — 6 diverse-lens Opus finders × 3-skeptic default-refute verify + a completeness critic, per D63): 13 confirmed + 6 critic gaps, ALL FIXED.** Headline set: **(HIGH)** the in-battle drawer's Escape bubbled to `fldKey`→`fldExit` (TORE DOWN the battle) → drawer keydown `stopPropagation` + a `fldKey` guard while `#fldDrawer` is open; **(HIGH)** the picker cards used role=radiogroup/radio without arrow-key roving nav → switched to role=group + aria-pressed (the F4 pattern); **(MED)** no range-clamp on resolved multipliers → a malformed/hand-edited persisted preset could zero casualties (a no-winner battle), invert a lever, or BUFF the AI → `_fldClampCfg` bounds every knob (+ aiResolve≤1 code-enforced + a supply-reserve floor ≥1); **(MED)** a "scenario"-fog preset silently wiped a live V-toggle → "scenario" is now a no-op at apply (a stale pin cleared at SAVE); **(MED)** the drawer refresh re-focused Fog on every toggle / stacked a duplicate keydown / could resume the battle behind the modal → parameterized refresh + a once-attached trap + re-assert pause; the `fldPresetsLiveReapply` dead code removed (the mid-battle full picker can't render — `.overlay` z-index 50 sits below `#fldRoot` 5000 — so the drawer shows difficulty READ-ONLY and the picker is a main-menu surface); + probe-fidelity gaps (melee attrition, the defender-sharpness seam, cushion-via-apply, malformed-clamp, boot-load, autoPause, the drawer Escape) all ADDED.
- **THE TACTICAL ENGINEERING CORPS stays SPLIT (D69)** — B-5 ships ONLY the sliders + the hooks; the Engineering milestone consumes them later (memory `civilwar-directive-tactical-engineering-sliders`).
- **UI:** a period-broadsheet **"Command & Realism"** picker (commission cards + an Advanced expander of accessible chips; a **"⚙ COMMAND & REALISM"** main-menu button + a Difficulty row in the skirmish setup) + an in-battle **"⚙ Settings"** drawer (a focus-trapped aria-modal: fog/auto-pause/speed live toggles + the read-only preset; key **G**). Full WCAG-AA (wcag-auditor clean after 3 contrast fixes), CVD-safe (shape+label), reduceMotion, deterministic.
- **Verified (the full PROPER VETTING gate, D53-OPS):** GATE OK (1684KB) → **probe-presets 26/26, 0 pe** → full no-regression GREEN (9 tactical baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic nonBlank **346**) → balance A/B (Balanced==today CS 8/8 seed-for-seed) → 76-agent bug-hunt (13+6 ALL fixed + re-probed) → wcag clean. Committed + pushed after full vetting. **NEXT: B-6 CS-player "command either side" (fork #10) → Phase C breadth (Eastern marquee first, D69).**

## D71 — EXTERNAL REPORT REVIEWED (Gemini "Economic & Architectural Analysis of Zero-Cost Single-File Web Game Development") — adopt/modify/ignore verdicts — [AARON+CLAUDE] (run k, 2026-06-16)
Aaron supplied a Gemini report (a generic 2026 primer on $0 single-file web-game dev: coding-assistant tiers, asset-separation, engine benchmarks, asset generators, state/combat patterns, deployment) and asked for an adopt/modify/alter/ignore call against the WHOLE project. **Net: mostly IGNORE — the report was written with no knowledge of this project; on nearly every axis our existing choices are already better-suited to the teaching mission + period aesthetic + single-file/$0 + determinism + the Claude/Opus agent loop. ~3 useful takeaways, all Phase-H / publish, none changing the current roadmap.** Logged so a future chat does not re-litigate "should we use Phaser / DeepSeek / pixel-art."
- **IGNORE — coding-assistant choice (Cursor/OpenCode/Cline + DeepSeek/GPT, §1/§3/§8):** moot — we build with **Claude Code + Opus 4.8** + the Workflow/subagent orchestration + deterministic probes + adversarial bug-hunts. Downgrading to the report's "$0" pick (OpenCode + DeepSeek-V4-Flash) would gut the reasoning this project depends on (balance A/B, the fog-inversion bug class, anti-Lost-Cause content verification). Its 2026 prices/limits/model names are unverified + fast-stale; irrelevant to the decision.
- **IGNORE the engine switch; KEEP one warning (§4):** we use a CUSTOM single-file engine (frozen `build/base.html` 2D canvas + async-CDN Three.js w/ 2D fallback + the `src/tactical/T0–T7` real-time sim). Switching to Phaser/KAPLAY/Godot = a catastrophic rewrite that breaks single-file/no-build/determinism/every probe. **BUT** the **Godot `SharedArrayBuffer` + COOP/COEP-headers-can't-be-set-on-GitHub-Pages** problem is real and VALIDATES our no-WASM-threading custom-JS choice (runs on free static hosting where Godot can't). Filed as a permanent guardrail: do not pull in any Phase-H media lib that needs `SharedArrayBuffer`; `coi-serviceworker` is the known shim if headers are ever needed. Reinforces D68 #10 (Classic IS the hex engine, no new co-equal WASM engine).
- **IGNORE the asset generators (§5):** PixelLab/Sprite AI = pixel-art (WRONG aesthetic — we are period broadsheet/engraving). Recraft's free tier KEEPS ownership of outputs (an IP trap for a project Aaron wants to own). AI period art also risks anachronism vs the citation-grade/anti-Lost-Cause bar. Our Phase-H plan — REAL PD imagery from LoC/Internet Archive/Wikimedia — wins on aesthetic, cost, IP, and historical accuracy. (Sole back-pocket: Recraft vector-SVG *could* help the H1b corps-badges/flags idea IF hand-authoring is too slow — only style-locked + paid commercial rights; hand-drawn period SVG likely still better.)
- **IGNORE — state/combat/turn patterns (§6) — already exceeded:** we already have bare-name global state (`G`/`__FIELD`), seeded deterministic sim (`fldRng`, no `Date.now`/`Math.random` in probes), a phase model, and a combat resolver (`fldResolveFire`/`fldResolveMelee`) with cover/terrain/morale/fatigue/canister/command/logistics far richer than the report's toy formula. The report assumes turn-based; our primary engine is real-time (Classic is the frozen turn-based layer). Mild validation only: unified-state + deterministic + localStorage-serialize == our planned Phase J saves (D54).
- **THE 3 KEEPERS (all Phase-H / publish; none now):** (1) **ADOPT/already-planned — automated asset ingestion:** the report's `compile.py`→Base64 idea = what `tools/build.mjs` already does for code+data; at Phase H extend **build.mjs (Node, not a separate Python script)** to Base64-embed + TIER/COMPRESS media w/ offline fallback — corroborates D68 #7 (the ~1.7MB single file will balloon; portability needs deliberate media tiering). (2) **NOTE — publish options (hosting stays deferred, D54):** GitHub Pages (simplest for one file) or **itch.io + Butler** (best indie discovery + in-browser play + automated deploy); Vercel unnecessary. **$0 nuance the report missed:** GitHub Pages on a PRIVATE repo needs GitHub **Pro (paid)** → for true $0 use a public deploy repo OR itch.io; rename/copy `civil_war_generals.html`→`index.html` at publish. (3) **NOTE — COOP/COEP guardrail** (above) folded into Phase-H/D-engine thinking.
- **Action:** no code/roadmap change now. Pointers folded into V1-CHECKLIST Phase H (asset-ingestion-via-build.mjs + the no-SharedArrayBuffer guardrail) and Phase J / the hosting-deferred item (GitHub-Pages-private-needs-Pro + itch.io/Butler). The report did NOT change what ships for B-6.

## D72 — PHASE B-6 · CS-PLAYER "COMMAND EITHER SIDE" (the tactical mode is now playable from either army; the B-1 attacker AI becomes player-facing) — [CLAUDE] (run k, 2026-06-16)
The 6th + FINAL V1-CHECKLIST Phase-B item (D61, charter fork #10). The real-time tactical engine can now be commanded from EITHER army: pick the Confederacy and you DEFEND Henry House Hill while the B-1 attacker-AI presses you uphill — which is exactly what makes the B-1 doctrine player-facing (the whole point of the fork) and doubles tactical replayability. New module `src/tactical/T7-command-side.js` + surgical seams in T0/T1/T3 + `tools/probe-csplayer.mjs`. Deliverable ≈**1702KB**.
- **NOT a new gated sim layer — a GENERALIZATION.** The plumbing was already half-built (T2 `fldSkirmishOOB` assigns `u.ai` by side; the T2 campaign briefing already speaks side-aware role text; `fldPlayerSide()` already drove the B-2/B-3/B-4 display layers; the campaign result-feedback `fldCampaignComputeOutcome`/`ApplyOutcome`/`EndHtml` were written side-aware back in Phase A). B-6 closed the TWO remaining gaps: **(1)** the input layer (`fldPlayerSel`/select-all/`fldCycleSel`/`fldPointerDown`) and the render/HUD **fog VIEWER** (the 2D markers, the ghosts, the 3D sync, the friend/foe line) were a literal `"US"` → now read `fldPlayerSide()`; **(2)** the hand-authored First Bull Run scenario only ever fielded a US player → now honours a side toggle.
- **`__FIELD.playerSide` made AUTHORITATIVE** (resolved once in `fldInitSim`, precedence: explicit `opts.playerSide` > skirmish `playerSide` > a CS campaign via `_fldCamp().side` > `"US"`). `fldBrSpec` (T1) flips the scenario AI flags off it — the player's side is the non-AI side, the other side is the AI (a CS player DEFENDS; `__FIELD.attacker` stays US, so the B-1 attacker AI presses). `fldPlayerSide()` (T3) resolves a CS **campaign LIVE** from `G.campaign.side` first (the legacy contract the officer roster/fog read mid-setup), then the explicit per-launch side.
- **NEW player-facing UI (T7):** a period-broadsheet **side-choice card** for the standalone Bull Run ("Lead the Union — ATTACK" / "Hold for the Confederacy — DEFEND"); a **side-aware briefing objective** (attack-&-seize vs hold-&-deny); a side-aware **"your war vs history"** end note (pronoun follows the side you played); an end-screen **"you" outcome line** (role-aware: "you carried the position" / "your line held" / "your assault was repulsed"); the 3D **camera seats behind the PLAYER's home edge** (a CS defender looks across his line at the advancing Union); the friend/foe HUD names the player's own side with the dignified FULL name ("4 Confederate vs 1 Union"). The skirmish menu + the campaign already plumbed side, so they work via the same generalization.
- **BYTE-IDENTITY:** the player side defaults to `"US"` (the historical attacker), so every standalone/probe launch resolves exactly as before; under `autoBoth` all units are AI regardless of side (the AI-vs-AI sim is independent of `playerSide`, proven seed-for-seed). The render/control/HUD generalizations are unreachable in the headless probes (renderer `none`). The 9 tactical baselines + bullrun stayed byte-identical.
- **VETTING CAUGHT A REAL REGRESSION (fixed before any commit):** my first `fldPlayerSide()` rewrite trusted a stale `__FIELD.playerSide` and broke the CS-**campaign** officer-roster/fog contract (the roster/fog read `fldPlayerSide()` mid-setup, before a relaunch). `probe-officers` flagged it. **Fix:** resolve a CS campaign LIVE from `G.campaign.side` first, then the explicit per-launch side (T0 `fldInitSim` + T3 `fldPlayerSide` made consistent; `fldBrSpec` routed through `fldPlayerSide()`).
- **ADVERSARIAL BUG-HUNT (52 agents — 6 diverse-lens Opus finders × 3-skeptic default-refute verify + a completeness critic, per D63): 15 findings → 11 confirmed + 7 critic gaps; 4 refuted. FIXED the ones that matter:** **(HIGH)** a skirmish "Fight Again" soft-locked a CS player — the rematch fell through to the generic US sandbox, leaving a CS player zero controllable units → the rematch now REPLAYS the full stashed launch spec (`__FIELD._launchOpts`, same scenario/skirmish/side/fog, bumped seed) AND the sandbox OOB now assigns `ai` by `playerSide` (defence-in-depth; byte-identical for US); **(MED ×2, dedup)** the 3D camera was hard-wired to the US south edge → side-aware home-edge seating; **(MED, critic G1)** reinforcement banners + the aria-live announce + the "Reinforcements arrive" auto-pause leaked a fog-hidden ENEMY detrainment → all gated on player-visibility (own side / fog off / scouted), fog-OFF byte-identical; **(MED, critic G2)** `probe-csplayer` only HUD-checked the no-leak invariant → added a fog-RENDER step asserting the render-hide predicate hides ≥1 enemy and NEVER a friendly, for both viewers; **(LOW)** the side-card `aria-label` was unescaped (latent if a future `objective.name` carries `"`/`<`) → escaped the dynamic parts; the end-screen "you" line now also speaks on the aria-live region; the stale "player controls only the US" comment reworded; a byte-identity-delegation note added to the probe. **DEFERRED with note (pre-existing / convention-consistent, not B-6 regressions):** the side-choice sheet uses the established Back-not-Esc sheet convention (matches Skirmish/Preset); the top-bar objective readout stays factual ("Union holds…"); `openSheet`-unavailable falls back to US; `fldPlayerSide()` campaign-first precedence is intentional (the legacy live contract — every reachable launch agrees).
- **WCAG 2.2 AA (wcag-auditor, Sonnet per D63):** clean after 2 contrast fixes — the side-card badge `opacity:.75` blended the accent below 4.5:1, so it was removed and the CS accent darkened `#b06a5a`→`#b77668` (4.72:1 on the `.upg` bg); everything else (titles, decks, objective block, outcome line, briefing/end borders) passes; ARIA (role=group, per-card aria-label, type=button, dialog focus traps) + CVD-safety (side named in text + ATTACK/DEFEND badge, never colour-alone) confirmed.
- **WHAT'S PLAYABLE:** the **⚔ FIRST BULL RUN** menu button now opens a side-choice card — lead the Union assault (history) or hold the hill for the Confederacy against the B-1 attacker AI; the ⚔ SKIRMISH menu's side pick is now fully wired (you command your chosen side); a CS campaign commands the CS in its battles. The CS defense is winnable (the role-aware defender holds the hill under fog), the field renders from your side, the fog shows only what YOU scout, and the briefing/HUD/end-screen all speak from the army you command.
- **Verified (the full PROPER VETTING gate, D53-OPS):** GATE OK (1702KB) → **probe-csplayer 16/16, 0 pe** → full no-regression GREEN (10 tactical baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic nonBlank **346**) → CS defense winnable (8/8 fog-ON) + live stacked default still CS 8/8 (B-6 did not move the balance) → 52-agent bug-hunt (11 confirmed + 7 critic gaps; the HIGH + 3 MED + LOW polish fixed + re-probed) → wcag clean (2 fixes). Committed + pushed after full vetting. **PHASE B IS COMPLETE (B1–B6). NEXT: Phase C tactical breadth — Eastern marquee first (Antietam / Fredericksburg / Gettysburg / Chancellorsville on the data-driven engine, D69).**

---

## D73 — PHASE C-1 · FREDERICKSBURG (the Marye's Heights assault) — the SECOND data-driven tactical battle + the data-driven scenario-UI generalization + the scenario-gated "doomed-assault" AI posture — [CLAUDE] (run k, 2026-06-16)
The first Phase-C (tactical BREADTH) milestone, Eastern marquee first per D69. The tactical engine, mature after B1–B6, is now battle-AGNOSTIC: a new historical battle = a `data/<battle>.json` file + one registry line, NO new combat code. Fredericksburg, Dec 13 1862 — Burnside's army storms the open glacis toward the sunken Telegraph Road, where Longstreet's men hold a 4-foot stone wall under the pre-sighted guns on Marye's Heights. New `data/fredericksburg.json` (`GAME_DATA.fredericksburg.fredericksburg`) + `tools/probe-fredericksburg.mjs` + the T1/T7 UI generalization + a small gated T0 engine seam. Deliverable ≈**1743.6KB**.
- **THE DATA-DRIVEN SCENARIO REGISTRY (the reusable spine).** `fldScenarioRegistry()`/`fldScenarioData(id)` (T1) map each registered id → its `GAME_DATA.<battle>` object; `fldScenarioInit` builds ANY registered id from the SAME shape (field/attacker/defender/objective/leaders/supply/terrain[hills,woods,walls,markers]/oob/reinforcements/teaching). Bull Run resolves the identical object it always did (`fldScenarioData("bullrun1")`) → byte-identical. Adding Antietam/Gettysburg/etc. is now a data file + a registry line.
- **THE DATA-DRIVEN UI GENERALIZATION (T1+T7).** `fldInjectScenarioButtons` iterates the registry (Bull Run keeps its bespoke marquee button `_fldInjectBullRunButton`; every other battle gets a data-built button from `data.menu`) and returns the LAST button so T0 anchors the skirmish/preset buttons after the whole block; `fldLaunchBattle` fires the briefing for ANY scenario; `fldBullRunSideChoice`→`fldScenarioSideChoice(id,go)` reads `sd.sides` (per-battle side-choice card) with code fallbacks; `fldBriefObjectiveHtml` keeps the keyword skeleton ("seize and hold"/"HOLD"/"deny") + a data flavor sentence (`sd.brief`); `fldScenarioEndHtml` keeps the Bull Run branch byte-identical (via a shared `_fldScenarioEndBox`) + a data-driven `sd.endNote` branch (keyed winner→player-side) for other battles. New `sides`/`brief` data added to `bullrun.json` (inert; reproduces Bull Run's exact card/briefing copy from data). **Probes confirm Bull Run + csplayer byte-identical (bullrun 15/15, csplayer 16/16).**
- **THE DOOMED-FRONTAL-ASSAULT POSTURE (a scenario-gated T0 seam — the historical-fidelity lever).** The B-1 attacker AI is *competent* (fog-off it column-rushes the killing ground cheaply and presses the mass bayonet on the objective) — which is exactly what Fredericksburg's assault was NOT. So `data.assaultDoctrine:"cautious"` → `__FIELD._atkCautious` (set in `fldScenarioInit`, reset false per launch in `fldInitSim`) makes the AI attacker advance IN LINE across the open glacis and trade fire but NEVER column-rush nor mass-bayonet a covered line — Burnside's piecemeal disaster. In `fldAiAttacker`, `var aggressive = !__FIELD.fog && !__FIELD._atkCautious` gates the assault + column branches; the cautious attacker presses to `OBJ_R*FLD.ATK_CAUTIOUS_HOLD` of the objective before holding. **Default off → `aggressive === !fog` → Bull Run / sandbox / skirmish / campaign BYTE-IDENTICAL (probe-ai 15/15, field 13/13).** A HUMAN commanding the Union is UNBOUND by this — the alt-history hook ("do what the Army of the Potomac could not"); the flag only shapes the AI attacker (AI-vs-AI + a CS player's opponent).
- **NOT a fog default (no contradiction of the D58/D64 "fog aids the defender" teaching).** Bull Run defaults fog ON (D67) because its clear-weather stacked config favored the attacker; Fredericksburg keeps fog OFF (historically the morning fog had LIFTED — "a chicken could not live on that field") and wins the defense by a DIFFERENT, scenario-appropriate lever: the cautious posture + the continuous stone wall (cover 1.7) + the massed pre-sighted crest canister over the open glacis. Two complementary ways a defender wins, each historically faithful to its battle — not a relitigation of fog.
- **THE BUG-HUNT CAUGHT THE BALANCE'S CORE FLAW (a real HIGH, fixed pre-commit).** The adversarial bug-hunt (5 diverse-lens Opus finders × default-refute Opus verify + a completeness critic) → **4 confirmed + 5 critic gaps.** The HIGH (empirically traced): the unchanged defender doctrine `fldAiDefender` holds the objective's FORWARD FACE (`obj.z + obj.r*0.55`); my first geometry put that face FORWARD of the wall, so the AI marched the defending line OFF the wall into the open on tick 1 — the 1.7× cover was never used and the result was carried by canister + numbers, not the wall (the whole teaching, gutted; the static-terrain probe missed it). **Fix (the Bull Run pattern):** seat the objective center BEHIND the wall (`obj.z` 375→331) so the forward face lands the holding line exactly ON the wall in cover, and raise `ATK_CAUTIOUS_HOLD` 0.6→0.85 so the cautious attacker's hold band sits in the OPEN just outside the wall's cover radius (only the attacker is exposed). Added a LIVE-position cover assertion to the probe (defenders REMAIN at the wall in cover ≥1.5; the attacker's hold line is exposed) so the regression can't recur. Also fixed: the side-card aria ROLE word was hardwired US=attack (wrong for a future CS-attacker scenario) → threaded the data-driven `attacking` flag (accent stays a side colour). Critic gaps addressed: the human "seize-and-hold" win requires clearing the whole position incl. the rear guns (objective note reworded); the `endNote.draw` path now probe-asserted. ACCEPTED-as-intentional (the directive authorized neutralizing Bull Run-specific objective copy): the generic objective skeleton reworded Bull Run's CS line ("deny the crest to the Union assault"→"deny it to the enemy") — keywords + the data flavor preserved, cosmetic only.
- **BALANCE (the historical slaughter, empirically — AI-vs-AI, fog OFF default, live stacked officers+logistics+arms):** **Confederate 8/8** (the assault is DENIED to the clock — won by timeout, never carrying the wall); **Union casualties ≈1.91× the Confederate** (US ~12,300 vs CS ~6,400 over the casualty sample — the bloody repulse, the attacker pays more); the stone wall is **load-bearing** (deleting it weakens the defense); the defending line provably HOLDS the wall in cover through the firefight while the attacker dies exposed on the open glacis. The artillery "men" are an explicit, documented VOLUME-OF-FIRE proxy for the massed pre-sighted crest guns; the result is intentionally gun-AND-wall-driven (historically defensible). The deeper ~6:1 historical ratio is carried by the teaching cards, not forced on the sim (the engine's long US rifle range vs short CS smoothbore makes a literal 6:1 unreachable without distorting other systems).
- **CITATION-GRADE, ANTI-LOST-CAUSE CONTENT.** 7 Union assault waves (Kimball→Andrews→Zook→Irish/Meagher→Caldwell→Owen→Sturgis→Griffin→Humphreys's dusk bayonet charge→Getty) vs the walled line (Cobb→Ransom + Kershaw/Cooke thickening it to four ranks) under the Washington Artillery + Alexander's guns; leaders with historical `fate` (only Cobb falls, mortally wounded — the rest endure); 4 teaching cards (the futility of fourteen charges; the pontoons that lost the battle; "It is well that war is so terrible"; Kirkland on the cold plain) — the lesson is the DOOMED FRONTAL ASSAULT into prepared fire + the cost of piecemeal command, NOT Confederate invincibility. `provenance` baked in (Wikipedia/OR OOB, NPS, American Battlefield Trust, O'Reilly).
- **Verified (the full PROPER VETTING gate, D53-OPS):** GATE OK (1743.6KB) → **probe-fredericksburg 20/20, 0 pe** → full no-regression GREEN (field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · arms 23 · presets 26 — all BYTE-IDENTICAL; **csplayer 16/16**; bootprobe ok; t1probe 14/14; diag-classic nonBlank **346**; strategic spot-check desk/economy/victory green; 0 pe) → balance A/B (CS 8/8, slaughter 1.91:1, wall load-bearing, defenders hold-the-wall live) → adversarial bug-hunt (4 confirmed + 5 critic gaps; the HIGH geometry flaw + the aria role fixed + gaps addressed + re-verified) → wcag-auditor clean (0 fixes — the C-1 UI reuses already-audited components; 1 PRE-EXISTING global `.title-sub`/`--rule` contrast issue [3.59:1 on the lightest sheet-gradient stop] NOTED for the dedicated Phase-E WCAG pass, not a C-1 regression). Committed + pushed. **NEXT: the next Eastern marquee battle — Antietam or Gettysburg (D69), same data-driven recipe.**
