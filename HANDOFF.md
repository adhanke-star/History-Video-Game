# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D527 next-lane=LANE-019 state=DRIVE owner=ChatGPT/Codex -->
> **Live status (D527, 2026-07-22):** LANE-019 is `DRIVE` / `ChatGPT/Codex` for one docs/research-only rail physical construction/existence-window adjudication covering the exact 27 rail services plus rail-facing evidence for four interchanges.
>
> **Boundary:** D527 changes only committed law/routing and one planning-probe boundary tooth. D525 remains the product head; transport data, runtime, state, save, UI, topology, movement, and generated output remain fixed, and no rail row is presumed to meet the source floor.
>
> **Authority:** `HANDOFF.md`'s first ⚡ block and LANE-019's D527 exact contract own the boundary. D528 must use the existing rail packet, enforce the two-family floor, retain every unresolved/disputed row and all four interchange limits, and stop before data/runtime behavior; LANE-020 remains independently blocked.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-22, D527: LANE-019 TAKES DOCS/RESEARCH DRIVE FOR THE EXACT 27-ROW RAIL PHYSICAL CONSTRUCTION/EXISTENCE ADJUDICATION; D525 REMAINS THE PRODUCT HEAD.** D526 is committed and pushed clean at `24c7a2b051fa1f4fafefe17ba5f3908b2081a221`. The full LANE-019 contract, Package A physical/control split, existing D497 packet, exact `CTS-R-01` through `CTS-R-27` mappings, four rail-facing CTI mappings, current transport substrate, and protected runtime owners were re-read. The packet currently supports dated corridor snapshots and isolated construction/rebuild facts, but no row proves a safe continuous physical window: even two dated observations can conceal an undocumented removal and rebuild. LANE-019 therefore takes `DRIVE` / ChatGPT/Codex for source adjudication only. D527 changes law/routing and the current-boundary planning tooth; it changes no product behavior and presumes no positive result.

**Sole artifact and preservation law:** D528 works only in the existing `docs/design/strategic-rail-conquest-research-packet.md`. It may append genuinely accessed and used `SR-*` source-register entries plus exactly one normative audit section/table. That table covers the exact ordered `CTS-R-01`..`CTS-R-27` / `RE-01`..`RE-27` services, followed by a separate ordered rail-face subpart for `CTI-01`..`CTI-04`. The packet's current node, candidate-edge, and case-study tables and every shipped JSON `historicalEligibility.dateText` value remain byte-identical. Its historic `READY_FOR_CONQUEST_LAW` verdict remains D497 topology/category-input readiness only; it does not supersede D526 or claim physical-window/runtime readiness. No new packet, cross-mode table, registry, schema, parser, data field, runtime owner, or generated product change is authorized.

Existing `SR-01` through `SR-26` source-register rows remain byte-identical and ordered. Any genuinely new unique source ID appends contiguously from `SR-27`, with no gap or insertion.

Each rail row must retain its exact service/edge IDs, corridor, and endpoints, then separately record start or dated-presence evidence, end or continued-existence evidence, continuity evidence, and nonphysical context. Every claim record carries an underlying-work `familyKey`, resolved `SR-*` ID, claim role, source type, precise locator, and the source's verbatim wording or bounded precision. Different hosts of one creator/work/report are one family. Every cited or used claim source and locator—including snapshot, unresolved, and disputed evidence—must actually be accessed and manually read back; an unavailable source cannot count. Direct physical evidence is limited to construction, engineering, property/inspection, or company records, or an official account explicitly describing completion, rebuilding, survival, removal, or destruction. Period maps and photographs prove only a dated snapshot. Military use, control, capture, damage, condition, service, and operation cannot establish a physical boundary or continuity.

The four closed rail dispositions use deterministic precedence: explicit material conflict → `PHYSICAL_EXISTENCE_DISPUTED`; qualifying continuous proof → `PHYSICAL_WINDOW_ESTABLISHED`; one or more dated physical observations without the complete floor → `PHYSICAL_PRESENCE_SNAPSHOTS_ONLY`; otherwise → `PHYSICAL_EXISTENCE_UNRESOLVED`. An established window requires two independent non-tertiary families for the exact corridor and each asserted bound, at least one direct physical family supporting presence and continuity, and evidence that actually proves continuous existence across the asserted interval. Two snapshots are insufficient. Exact precision remains verbatim; missing dates and campaign boundaries cannot be invented. `CTS-R-27` retains the removal dispute and no settled end. Zero established windows is a valid result. All labels are docs-only findings, never runtime/data enums, legal-now values, operating/control/condition claims, gauge/terminal/through-service findings, adjacency, topology, route, or movement authority.

**Interchange and negative binds:** the only admitted rail faces are `CTI-01` / `RE-06`, `CTI-02` / `RE-22`, `CTI-03` / `RE-11`, and `CTI-04` / `RE-11` plus `RE-12`. Each may record only `RAIL_FACE_EVIDENCE_ONLY`; every full interchange remains `INTERCHANGE_WINDOW_UNADJUDICATED`. No `SW-*`, water/service date, rebuild date, co-location, landing, or operational event can establish a handling connection. RE-05's August 1861 direct connection proves neither an earlier seamless transfer nor a later physical end; RE-06's rebuild is not original construction or a City Point interchange window; RE-07 degradation is not physical absence; RE-04 cannot create Strasburg-Harpers Ferry rail; RE-17+18 and RE-25 cannot become through service; RE-27 remains disputed; and D526's Cape Fear control/operation parser counterexample remains binding. Any material contradiction with an existing edge, endpoint, explicit non-link, or qualitative date HALTs rather than rewriting the shipped substrate.

**D528 proof and distribution-safe bind:** add one suite-excluded filesystem guard, `tools/probe-conquest-rail-window-research.mjs`. It must prove a singular audit; byte-identical node/edge/case tables; byte-identical ordered `SR-01`..`SR-26` rows plus contiguous unique appended source IDs; exact ordered 27 service/edge/endpoint mappings; exact ordered four CTI rail faces; required claim metadata; source/family/locator resolution and independence; verbatim precision and role separation; the direct-source/continuity floor; every named negative; no runtime/data enum or prohibited authority; and the final CONTRACT/unowned routing state. If an established row exists, Bind R removes one qualifying family from the first such row. If none exists, it promotes the first ordered non-disputed row lacking the floor without curing its evidence deficit. One named `DISPOSITION / POSITIVE FLOOR / CONTINUITY` tooth owns both deterministic precedence and the complete positive floor; exactly that tooth must red, all other teeth remain green, and packet/probe bytes restore exactly before the clean rerun.

**Exact allowlists and gates:** D527 changes only `AUTONOMOUS-RUN.md`, `COORDINATION.md`, `DECISIONS.md`, `HANDOFF.md`, `RUN-LOG.md`, `START-HERE.md`, `V1-CHECKLIST.md`, `WAKE-UP.md`, Package A §8, `legacy/HANDOFF-ARCHIVE.md`, and only the current-boundary tooth in `tools/probe-conquest-transport-plan.mjs`. D528 may additionally change only the existing rail packet and add the dedicated research probe; it must update the transport-plan boundary tooth when releasing the lane. Data, every `src/` and runtime probe, manifest/build/suite/save owner, water/road packets, generated HTML, frozen base, and every unlisted surface are barred; normal build output must remain byte-identical.

**D527 routing gates:** syntax for the existing transport-plan probe; doc coherence; transport plan 12/12; conquest, Mayhem, and War Career plans; normal build and `--check`; exact byte-verbatim D525 archive comparison at 4,023 bytes and SHA-256 `02314fbd9a5b9432e546b023147e05b74d5bd69234f2ead624596c28753376d0`; exact 11-file allowlist; protected no-diff audit; fresh JSON with zero failed/errors/pageerrors/realErrors; and `git diff --check`. **D528 research gates:** syntax for both touched probes; the dedicated research proof; manual readback of every cited/used claim source and locator including snapshot, unresolved, and disputed evidence; Bind R exact-red and byte restore; the transport-plan terminal tooth; byte-verbatim archive of D526's exact 4,723-byte block at SHA-256 `c156b0bbfbbddd6448a36a9fcedff3621def8819f13e4d18886a7a0a3391c044`; then the same doc/static, build, artifact, allowlist, protected, and diff gates. No browser or full 140-row battery is owed unless a protected shared surface moves.

The D527 routing proof is green: doc coherence 5/5, transport plan 12/12, conquest plan 8/8, Mayhem plan 13/13, and War Career plan 24/24; normal build/check is `GATE OK` at data 65, territories 36, transport 27/15/2/4/18, manifest 112 with 116 last, and suite 140. Syntax, the exact 11-file allowlist, protected no-diff audit, `git diff --check`, fresh artifact readback, and the exact D525 archive comparison pass. No product/generated diff exists. Final independent Sol-high re-audit reports PASS. No browser or full-suite gate is owed for this routing-only contract.

LANE-019 is now `DRIVE` / ChatGPT/Codex; D525 remains the product head, and no simultaneous edits are permitted. **EXACT NEXT:** after D527 is committed, pushed, fetched, and proven clean, execute only this D528 research pass. Record the honest 27+4 findings and every unresolved/disputed quarantine, run the complete proof/source-readback/bind/restore/gate contract, synchronize D528 delivery evidence, commit, push, fetch, and return LANE-019 to `CONTRACT` / `none`. Then reload D514 and continue the next independent item. Do not change data/runtime/current-turn/state/receipt/topology/movement/live-save behavior or make a lane-level `SHIPPED` claim.

**⚡ AMENDMENT — 2026-07-22, D526: QUALITATIVE TRANSPORT DATES CANNOT BECOME HISTORICAL LEGALITY; THE PHYSICAL-WINDOW PREREQUISITE IS SOURCE-BLOCKED.** D525 is committed and pushed clean at `9b94e3f9723f4e1bfd8f90e7c7a924730f141b61`. Package A, all 44 shipped physical-service descriptors, the four separate interchanges, the D497/D499 source packets, and the detached calendar/state/transport owners were re-audited. The service surface divides exactly into ten current `eligible`, 21 `conditional`, and 13 `operation-specific` qualitative records. Their verbatim `historicalEligibility.dateText` preserves source evidence; it is not a machine-readable physical-availability interval, legal-now decision, opening condition, or player-state transition. No service or interchange is runtime-ready from the current export.

Package A §8.30 records the exact row partition. The ten `eligible` plus 21 `conditional` rows are docs-only `QUALITATIVE_OR_MIXED_EVIDENCE_ONLY`: their prose can combine physical existence with control, use, damage, rebuilding, endpoint loss, fragmentation, or condition. The thirteen water/sea operation rows are `OPERATION_OBSERVATION_ONLY`: they preserve a bounded passage or approach but cannot become routine standing service. `CTI-01` through `CTI-04` are `INTERCHANGE_WINDOW_UNADJUDICATED`; a service date cannot be inherited by a distinct handling connection. These labels are audit dispositions only, not future JSON/runtime enum values.

`CTS-S-02` / `WE-26` is the binding parser counterexample. Its “through 15 Jan. 1865; closed thereafter” phrase describes the Cape Fear approach closing to blockade runners after Fort Fisher; the same row separately records the joint operation's actual Union landing and fort capture. Treating it as a generic physical-service end date reverses the source meaning. No parser or future law may substitute control, capture, military use, cut/damage, service condition, blockade state, or a named operation for physical existence. Exact source dates stay separate from the two-month campaign calendar and may not be rounded to a turn boundary. Unresolved or disputed claims remain unavailable.

**Architecture/dependency verdict:** create no cross-mode packet, parallel table, second registry, schema, cursor, parser, or runtime owner. New source claims belong in the existing D497 rail or D499 water packet; only a later separately contracted and source-proved pass may extend the existing evidence JSON and transport owner. A current-turn cursor lacks a state holder; opening territory/node/service values are unsourced; receipt payload/precondition/transition law is unchosen; and a route query would require forbidden topology or prose interpretation. Those candidates are rejected as premature, not implemented as empty mechanical slices.

D526 changes only Package A law, the existing transport planning proof, routing/history/checklist documents, and the byte-verbatim HANDOFF archive. No product source, data, manifest, build source, generated game, save shape/version, suite enrollment, UI, state, receipt, topology, movement, or existing runtime owner changes. Normal build and `--check` are `GATE OK` at data 65, territories 36, transport 27/15/2/4/18, manifest 112 with 116 last, and suite 140. Final static proof is doc coherence 5/5, transport plan 12/12, conquest plan 8/8, Mayhem plan 13/13, and War Career plan 24/24; syntax, `git diff --check`, exact 11-file allowlist, protected no-diff audit, and the 5,870-byte D524 archive comparison pass. Independent Sol-high re-audit reports PASS with no remaining blocker. No browser or full 140-row battery is owed for this docs/planning-only finding; the first playable-loop release checkpoint remains deferred.

LANE-019 remains `CONTRACT` / `none`; D525 is still the product head, and D526 is a bounded prerequisite finding rather than playable conquest or a lane-level `SHIPPED` claim. **EXACT NEXT:** under D514, D527 must make a separate committed docs/research-only DRIVE take for the first mode-bounded source adjudication, beginning with the 27 rail services. It must use the existing rail packet; distinguish construction/physical existence from canonical control, operation, and service condition; preserve exact dates and the two-family source floor; retain every unresolved row; examine but not prematurely authorize the rail-facing evidence for `CTI-01` through `CTI-04`; define a row-by-row audit/probe and exact allowlist; and stop before data/runtime/current-turn/state/receipt/topology/movement/live-save work. If rail evidence remains insufficient, quarantine it and continue the next independent D514 item. LANE-020 remains separately blocked; no simultaneous edits.
