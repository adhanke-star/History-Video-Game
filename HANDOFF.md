# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D522 next-lane=LANE-019 state=DRIVE owner=ChatGPT/Codex -->
> **Live status (D522, 2026-07-22):** LANE-019 is under ChatGPT/Codex DRIVE for one detached fresh-start conquest identity/state serialization foundation in a new sole state owner after the shipped board and transport owners.
>
> **Boundary:** D522 changes only routing/law plus the transport plan's live-boundary tooth; D521 remains the runtime head. The contracted factory is detached and may establish only exact immutable `C.campaignKind`, existing immutable `C.ruleset`, and one empty `C.conquest` namespace—no live start/load/UI, migration, topology, control, service condition, army, order, movement, or operational default.
>
> **Authority:** `HANDOFF.md`'s first ⚡ block and LANE-019's D522 exact contract own the slice. D514's work-conserving loop remains binding; LANE-020 stays `CONTRACT` / `none` at its independent-turn dependency.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-22, D522: LANE-019 TAKES DRIVE FOR ONE DETACHED FRESH-START CONQUEST IDENTITY/STATE SERIALIZATION FOUNDATION; OPERATIONAL CONQUEST AND LIVE ENTRY REMAIN CLOSED.** D521 is committed and pushed clean at `ef85725b2e7ea55fba96b4c7996a0d70abb6a56f`. The live campaign constructor, Mayhem ruleset wrapper, save/apply pipeline, named slots/import/undo/Ironman surfaces, and turn-register owners were re-inventoried before this take. The legacy ladder constructor immediately launches a battle and is not a lawful conquest owner; the generic save envelope already serializes the whole campaign object but its apply path and adjacent authorities are broad protected surfaces. The smallest dependency-ready foundation is therefore detached and fresh-start-only: it proves an exact serializable campaign identity/state shape without creating a public start or continue path and without changing save application.

**Architecture/representation verdict:** create `src/116-conquest-state.js` after the shipped territory and transport owners as the sole conquest-state foundation owner. Preserve existing immutable `C.ruleset` as ruleset identity and add a separate immutable versioned discriminator, exact `C.campaignKind = {id:"conquest",version:1}`. Add exactly one empty plain `C.conquest` namespace whose root property cannot be replaced but whose object remains extensible for later separately contracted state. This separates campaign kind from Historical/Mayhem ruleset, supplies versioned save discrimination without moving global `_SAVE_VER`, and refuses to invent operational defaults. Rejected alternatives are overloading `C.ruleset`; a scalar kind plus an invented nested schema; attaching conquest to the battle ladder; placing the owner in Mayhem wrapper `src/107-mayhem-rules.js` or transport owner `src/115-conquest-transport.js`; and broadening `applySave`, slots, import, undo, bookmarks, or Ironman before a lawful public conquest entry path exists.

The implementation exposes exactly two globals: `conquestCampaignFoundation(startView)` and `conquestCampaignFoundationView(candidate)`. Factory `startView` and nested `ruleset` may have only `Object.prototype` or `null` prototype and exact own-data keys `{side,ruleset}` / `{id,version}`; every custom prototype, inherited enumerable authority, hidden or symbol extra, accessor, extra/missing key, wrong scalar type, or unsupported value fails `null`. Validation first inspects prototypes, all own names/symbols, and own descriptors, then reads only descriptor `.value`; it never invokes an accessor. `side` is exactly `US` or `CS`; ruleset is exactly `{id:"historical"|"mayhem",version:1}`. To retain existing ruleset ownership without polluting the final root, call `mayhemInit(carrier,id,"new")` only on a disposable local plain carrier, verify its exact frozen non-writable/non-configurable enumerable `ruleset` descriptor/value, transfer only that same descriptor/value to the final root, and discard the carrier plus every incidental field. Never call `mayhemInit` on the returned root. A valid result has exact keys `side`, `campaignKind`, `ruleset`, and `conquest`; `campaignKind` and `ruleset` are frozen, and root `campaignKind`, `ruleset`, and `conquest` are non-writable/non-configurable. Factory `conquest` is an exact empty `Object.prototype` plain object and intentionally not frozen. The view accepts only `Object.prototype` candidates in one of two internally consistent forms: the locked factory descriptors, or the ordinary enumerable own-data descriptors produced by JSON round-trip. It rejects null/custom prototypes, mixed descriptor modes, symbols, hidden/extra fields, and accessors before reading values. Success returns a new deeply frozen exact four-field snapshot with locked root descriptors; its empty `conquest` snapshot is frozen while the factory namespace remains extensible. Malformed, mixed-legacy, or unsupported-version candidates fail without attachment or migration.

This foundation reads or writes no `G`, settings, localStorage, DOM, board, transport, or alternate authority and creates no UI. Existing `serializeSave` may naturally carry a controlled detached campaign object because it already serializes the whole campaign, but D522/D523 make no change to `_SAVE_VER`, `tools/save-shape.json`, `serializeSave`, `applySave`, named slots, import/export, undo, bookmarks, Ironman, War Career `runId`, or any old-save behavior. No live start/load/continue action, migration, topology, adjacency, control, node operation, service condition, Historical date availability, army, turn/date, order, movement, capacity, cut/repair, economy, AI, battle, resolver, Chronicle, career, or tactical field is authorized.

The implementation allowlist is the new `src/116-conquest-state.js`; `src/00-manifest.json`; new focused suite-excluded `tools/probe-conquest-state.mjs`; the existing conquest-layer and transport planning probes only for exact module/count/current-contract pins; generated `civil_war_generals.html` only through normal build; mechanical generated/source pins in the Mayhem and War Career plans only when byte evidence proves movement; and milestone law/routing/archive documents. Data, build source, `src/91-save-slots.js`, `src/105-save-guard.js`, `src/107-mayhem-rules.js`, `src/114-conquest-board.js`, `src/115-conquest-transport.js`, `tools/save-shape.json`, `build/base.html`, tactical/runtime UI, and every unlisted surface are barred. Data remains 65, suite remains 140, save shape and `_SAVE_VER=1` remain unchanged; the implementation moves manifest 111→112 with module 116 last.

Focused proof is exactly ten steps: API/manifest ownership; exact strict factory input; exact root and both sides; immutable campaign-kind values/descriptors; reuse of existing ruleset owner and immutability; empty namespace plus JSON round-trip/view; legacy/malformed/mixed rejection with no mutation; controlled proof that existing serialization carries only the detached object followed by byte/state restoration; repeated-call purity across `G`/storage/DOM/board/transport; and absence of every prohibited operational field, action, and UI trace. Bind A removes only the `campaignKind.version === 1` validation in the view; exactly the invalid-discriminator tooth must red while all normal teeth stay green, then source and generated output restore byte-for-byte before a clean rerun. Required gates are syntax, normal build `GATE OK`, doc coherence, focused state proof and fresh artifact readback, conquest layer/board/transport plan plus existing 18/18 transport proof, Mayhem mode/plan, save-slot adjacency, War Career plan, isolated bind/restore, exact allowlist, and `git diff --check`. The 140-row full battery remains deferred to the first playable conquest loop unless a shared gate exposes a real regression.

This D522 commit changes routing/law and the transport plan's current-boundary tooth only; D521 remains the product/runtime head. LANE-019 is `DRIVE` / ChatGPT/Codex from clean D521, and no simultaneous edits are permitted. **EXACT NEXT:** after D522 is committed, pushed, fetched, and proven clean, implement only the detached factory/view under the exact allowlist as D523; grow and read back the ten-step focused proof; execute Bind A and byte restore; rerun every declared adjacent gate; synchronize D523 evidence; commit, push, fetch, and prove clean parity. Then reload the ledger and continue the next dependency-ready item under D514—never infer operational conquest state or broaden a live campaign/save path inside this foundation.

**⚡ AMENDMENT — 2026-07-22, D521: ARC 7 TRANSPORT MOVEMENT SLICE 2B SHIPS A PURE PHYSICAL-SERVICE EVIDENCE QUERY; LANE-019 RELEASES BEFORE STATEFUL TRANSPORT.** D520's contract/DRIVE take is pushed at `685c1f6ee5630a818dfc3eb8b6d60790331bf991`. Existing `src/115-conquest-transport.js` remains the sole immutable transport owner and now exposes exactly one additional global, `conquestTransportPhysicalServices(rulesetView)`. The input is a strict plain object with exactly `id` and `version`; only Historical or Mayhem version 1 qualifies. The helper reads no `G`, `C`, setting, localStorage, DOM, or alternate authority and returns `null` unless the existing complete territory/transport normalizer and its exact 27/15/2/4/18/road-status envelope remain lawful.

The only non-null result is a deeply frozen exact `{rulesetId,services}` object. Source order remains 27 rail, 15 inland-water, then two bounded-sea rows. Historical returns all 44 source descriptors with their qualitative status and verbatim `dateText` unchanged; current Mayhem also returns 44 because every retained row is source-backed and physically eligible, while a synthetic otherwise-valid false flag is correctly filtered to 43. Rows, array, and root are cloned/frozen. Roads, interchanges, and non-links never become services, and the output contains no topology, adjacency, endpoint projection, path, current availability, legal-now, control, state, save, order, movement, capacity, cut/repair, economy, AI, or battle field.

The first candidate reader only checked a truthy normalized value before concatenating arrays. The focused probe's malformed-substrate tooth exposed that a test override returning `{}` would throw instead of fail `null`. Final code now requires the exact root keys, 27/15/2 service counts, 4/18 interchange/non-link counts, arrays, and exact road status before deriving output. Independent adversarial review then found that the generic enumerable-key guard accepted inherited/custom-prototype input and hidden or symbol extras. The final input-only validator checks plain/null prototype, all own string names, zero symbols, and own data descriptors before reading either value; accessors and every extra channel fail `null`. No invalid or partial substrate can cross the reader boundary.

Focused proof grows exactly 11→18 and is green 18/18 with `ok:true`, zero failed/page/real errors, and no UI trace. The seven new teeth prove strict input; exact 44-row order/deep freeze; Historical evidence preservation without a legal-now claim; Mayhem false-flag filtering; road/interchange/non-link and runtime-field exclusion; malformed normalized failure; and repeated-call state/storage/DOM/board purity. Bind 2B removes only the Mayhem physical filter and returns the exact expected red: 17/18 with only `MAYHEM EXCLUDES SYNTHETIC FALSE PHYSICAL ELIGIBILITY` failing, exit 1, and zero page/real errors. Source and generated HTML restore byte-for-byte before the final clean rerun.

Restored SHA-256 is `fece1658553a214ce5600e824e6a93ce3332de421343706caa918a7897be707e` for `src/115-conquest-transport.js` and `ffabe02523a96bf502031be0caa9297a81a73a53f1279dd2923f376e25d1b7d0` for the generated game; MD5 is `4a00eee8ffce00acdb9463ea34f8adaf` and `345772de75bb0f59f9b24947796398bb`. The transport plan, conquest layer plan, conquest board, Mayhem plan, and War Career plan pass 11/11, 8/8, 13/13, 13/13, and 24/24 with fresh clean artifacts. Mechanical pins move generated game `2249daa3`→`345772de` and source tree `6950a862`→`797c266b`. Normal build is `GATE OK`; data 65, territories 36, transport 27/15/2/4/18, manifest 111 with 115 last, suite 140, frozen base, save shape, `_SAVE_VER=1`, and all existing owners hold. No full battery is owed before the first playable transport loop.

LANE-019 returns to `CONTRACT` / `none`; D521 is a foundation shipment, not playable conquest or a lane-level `SHIPPED` claim. The next dependency must be separately adjudicated and contracted. Package A requires a fresh-start immutable conquest campaign identity, one `C.conquest` namespace, and versioned save discrimination before any authoritative movement mutation; the current ladder constructor/save/bookmark/undo/import/Ironman paths cannot be incidentally broadened.

**EXACT NEXT:** inventory the live campaign-creation, ruleset, save/apply, slot/import/undo/Ironman, and turn-register owners; write the smallest fresh-start conquest identity/state/save contract inside LANE-019 with exact initial fields, old-save isolation, discriminator/version law, probes, binds, allowlist, and do-not-touch list; commit and push that routing take before runtime. Do not infer opening territory control, node operation, service condition, armies, Historical availability windows, or migration behavior—if the existing Package A/source law does not determine one, keep it out of the foundation or record the bounded prerequisite. No simultaneous edits.
