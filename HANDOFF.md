# HANDOFF — "The Civil War"

## ⚡ HANDOFF — 2026-07-18 **D458: LANE-012 IS ALL-SLICES-GREEN — SLICE 3 SHIPS THE RE-TOOTHING SWEEP AND THE LANE MOVES DRIVE → VERIFY (battery-only).** The stripJsComments family is ROOT-FIXED TREE-WIDE: all ELEVEN non-suite battle plan probes were red on clean HEAD from one shared cause (the naive two-pass stripper ate from the `data/*` glob in validate-data-schemas.mjs line 2's line comment to the first real closer, deleting BATTLE_FILES → every integration tooth misread its SHIPPED battle as never-enrolled), and the SAME idiom rode FOUR suite probes (learn-battle/chief-of-staff/concept-links/memory-chain) where over-stripping can HIDE forbidden tokens (false green). One idiom now in all 15 files: a single-pass scanner honoring line comments, block comments, string literals AND regex literals (v1's string-only pass was itself caught by probe-learn-battle — the quote inside src/100's `.replace(/"/g,…)` desynced it and a block-comment `learnMeta` mention survived as a phantom reader; v2 tracks regex state and is verified on the real inputs). No tooth weakened — the scans now cover MORE text. All 11 plan probes 0-exit; all four suite probes green, zero pageerrors; INVERSE BIND md5-proven on cedar-creek (old stripper → exactly the family red; v2 restore byte-identical f1a70fe3 → green); tree-wide grep proves zero remaining broken-idiom instances. The consequence-only-absence audit is CLOSED: the probe-mayhem-mode family head was split-and-chained in D457; probe-afteraction carries the AAR-side pin; every other noQuarter/infamy reference in tools/ is a D457 chain comment or an unrelated committed-spec anchor; E41 hashes functions only (additive fields never trip it; save-slots 16/16). No src/data/generated change this slice (game a6cbfd2d holds); counts 26/59/1,614/137; `_SAVE_VER=1`; frozen base untouched. **LANE-012: Slices 1-3 = D456/D457/D458, DRIVE → VERIFY (unowned at session close; the next release checkpoint's full battery gates SHIPPED per D160/D176). EXACT NEXT: ARC 1 is functionally complete — charter ARC 2 (content unlocks: Fort Pillow + the Crater + Olustee as ONE research family completing C3 entirely, + Leetown OOB + Front Royal scoring, per the D455 packet §4b/§7 — Fort Pillow rides the SHIPPED no-quarter machinery) in a FRESH session, lane ledger-only first; OR run the release battery to flip LANE-012 SHIPPED. Standing candidates behind the arcs: R-7 triggers, LANE-002 5b batches, J-polish.** *[AMENDED in-flight, ARC 2 overnight 2026-07-18 — LANE-013 · content-unlocks is at DRIVE (charter `3506716`): P1 the massacre-treatment research family packet SHIPPED as D459 (+ the incidental D457 schema-gate root-fix, 59/59). EXACT NEXT: P2 the Leetown Cherokee OOB (the D455 §3 row 7 unlock; Watie's 2nd CMR fields on the sourced March 8 station; the full ladder contract lives in COORDINATION.md LANE-013).]*

## ⚡ HANDOFF — 2026-07-18 (superseded by D458) **D457: LANE-012 SLICE 2 IS SHIPPED — THE HISTORICAL SURRENDER/NO-QUARTER UNLOCK, JUDGED NEVER REWARDED, BEHIND THE LOAD-BEARING MASSACRE-BLOCK (the D455 §3 row-2 unlock through the SHIPPED Mayhem effect machinery — EXTENDED, never bypassed).** Data: `no-quarter-historical` (rulesetId "historical") carries CONSEQUENCES ONLY — morale −6 · press −8 · diplomacy −6 (recognition moved AGAINST the actor) · notoriety +25 (the NEW capped `C.infamy` ledger); the SIGNS are law, the magnitudes are a balance call with the logged deterministic A/B in the probe artifact (public will 33→25 · press 50→42 · recognition 20→26 · reprisal pressure 17→24 · exchange function 45→39 · returned prisoners 450→225 at cap). Engine (src/107): `_mhResolve` legalizes an action iff its rulesetId EXACTLY matches the campaign ruleset; the `ruleset.is` predicate matches the campaign ruleset; the MASSACRE-BLOCK (`_MH_HISTORICAL_OPS`) refuses EVERY reward family before mutation with the sign law on the four consequence ops — and the t1 tooth surfaced + root-fixed an ordering bug: ALL validation now runs BEFORE `_mhSanitizeReceipts`, so a refused action leaves a receiptless campaign byte-identical. Adapters through EXISTING owners (all D74 inputs, all bounded, all additive, NO `_SAVE_VER` bump): `M.infamyShock` [−25,0] read by moraleCompute; `C.press.infamyShock` read by pressSentiment; `C.blockade.recognition` clamped, no reader change; the `C.infamy` {total ≤100, events ≤16} ledger with present-only sanitation read by the prisoner-exchange REPRISAL CYCLE (pressure up, exchange rate down, exact no-op when absent). Offer: the campaignAdvance wrapper stamps `C.mayhemNoQuarterOffer` for BOTH rulesets from the shipped T25/T2 captured chain; the judged panel (`mhJudgedNoQuarterPanel`, GEA-14 idiom inside aarRenderReport) states ALL consequences before confirmation, renders the applied receipt with committed condemnation (tcChronicleLine Fort Pillow/cartel + the committed GO 252 policy-timeline line with sources) and the infamy ledger, and returns "" at no-offer/no-infamy so the Historical AAR is BYTE-IDENTICAL; the one `[data-mh-no-quarter]` click path routes by ruleset; AAR grading/endings moral voice UNTOUCHED. TEETH: probe-mayhem-mode 24/24 (the new t1-t6 step + the actions-2→3 re-pin + the "Historical refusal/bytes failed" family-split chains at both sites) · probe-afteraction 19 (the AAR-side byte-equivalence pin) · probe-save-slots 16/16 · probe-campaign-link 19 — zero pageerrors, all artifacts read. BINDS (md5-proven restores): A1 family-refusal disabled → EXACTLY t1 red; A2 sign law removed → EXACTLY t2 red; B data tampered with battle.score.add → EXACTLY t3 red with the bytes-unchanged proof; plus the standing no-captures/no-offer byte-identity A/B in-step. Plan probes re-pinned with the AD-7 chain idiom (game a6cbfd2d · dataTree 8647e586 · srcTree 41ee94b1); counts hold 26/59/1,614/137; `_SAVE_VER=1`; frozen base untouched. **EXACT NEXT: LANE-012 SLICE 3 — the re-toothing sweep: grep ALL of tools/ for teeth assuming consequence-only ABSENCE in Historical (family head handled: the probe-mayhem-mode "Historical refusal/bytes failed" split is chained at both sites — audit the REST of the tree the D454 way, one family, never one-red-per-row; also the E41 save-shape pins if offer/infamy fields surface in serialized saves) AND root-fix the stripJsComments family — eleven non-suite battle plan probes red on clean HEAD because the naive `replace(/\/\*[\s\S]*?\*\//g,"")` stripper eats from the `data/*` glob in validate-data-schemas.mjs line 2 to the first real `*/`, deleting BATTLE_FILES before the scan (probe-side reading bugs, the legitimate D453 class; fix the stripper/read idiom at the root, never weaken a tooth).**


**THE LIVE-HEAD SINGLE-SOURCE RULE (D412):** the top ⚡ block below is THE one canonical live boundary for the whole project. Every other canonical doc (START-HERE, WAKE-UP, AUTONOMOUS-RUN §2, V1-CHECKLIST's live gate) carries at most a 3-5 line summary plus a pointer here; RUN-LOG stays the chronological log. This file keeps the latest ⚡ block plus exactly ONE prior; older superseded blocks move byte-verbatim to `legacy/HANDOFF-ARCHIVE.md` at each release (newest at top), gated by the probe-anchor inventory. Full rules: `START-HERE.md` §Naming convention.

**Older handoff blocks (D456 and earlier, plus the run-h scaffold sections):** moved byte-verbatim to [`legacy/HANDOFF-ARCHIVE.md`](legacy/HANDOFF-ARCHIVE.md) — newest at top (D456 moved at D458; D454 moved at D457; D423 moved at D454; D422 moved at D453; D421 moved at D423; D418 and D417 moved at D420; D413 moved at D418; D412 at D417; D411 at D413; D410 → run-h in D412). Nothing was deleted or paraphrased beyond the standing "(superseded by …)" label flip.
