# HANDOFF — "The Civil War"

## ⚡ HANDOFF — 2026-07-18 **D454: THE AD-0 BATTERY IS COMPLETE — 137/137 GREEN WITH EVERY ARTIFACT READ; LANE-009 + LANE-010 + LANE-011 ARE SHIPPED; THE D423-D452 ARC IS FULLY RELEASED AT AN AUDITED HEAD.** The resumed serialized battery (rows 73-137, alone on the machine) ended `VET NO-REGRESSION OK`: the 72-row D453 prefix + seven segments (23+5+7+8+3+1+18; logs `.tmp/ad0-battery-resume3..10.log`; each segment resumed `--from=` its fixed row at the fix commit's head; final segment at `1a43027`). ALL 136 expected artifact JSONs read — ok=true, ZERO pageerrors, ZERO realErrors; 232 screenshots. SIX battery reds root-fixed at their exact labels, every one a stale VETTING-DEFERRED pin or never-run tooth from the D436-D452 coding-first slices (the D453 adjudication held exactly), none weakening a tooth: 'weather' cold-harbor provenance → exact enum (`bbffcb4`) · 'tactical roster' atlanta PHASE_COUNTS 2 pinned vs the ||3 default (`2e2fcde`) · 'gettysburg' green-process exit hang fixed per the D398 owning-process law + 600s slow-Mac budget (`7916d14`) · 'wilderness' the D442-missed Overland family, six sites in three probes at once — cold-harbor leaves the forbidden scans the documented D397/D393 way, adjacency chains re-pinned to the shipped coldHarbor-68.5 slot, meta-teeth proven by regex first (`9b7f58d`) · 'kennesaw' the missed DOM-variant adjacency (`b5be203`) · 'cedar creek' + probe-franklin the D436-missed Atlanta-insert adjacency, family CLOSED by a tree-wide audit of every adjacency assertion in tools/ — zero stale remain (`1a43027`). Environmental (no git change): orphaned headless Chromes from killed harness runs held memory — cleaned; one cold-boot page timeout green on retry. Counts hold: 26 scenarios / schema 59 / Army Register 1,614 / suite 137 / `_SAVE_VER=1` / frozen base untouched / D74 clean / no new historical claims. **AD-0 → SETTLED IN D454 — the AUDIT-DEBT ledger is fully discharged; LANE-009 + LANE-010 + LANE-011 → SHIPPED (unowned) with battery head `1a43027`.** Docs synced per the D412 live-head law (D423 block archived verbatim under the probe-anchor inventory; the overdue 56-block V1-CHECKLIST gate-state stack archived verbatim, inventory-proven unpinned); the two doc-grepping plan probes re-pinned at this head with the AD-7 chain idiom. **D455 IS COMMITTED in the same session: `docs/design/unlocked-but-judged-design.md` (Aaron's eleven popup-locked rounds recorded verbatim — UNLOCKED-BUT-JUDGED, the toggle kept as a plausibility split, Historical mechanics unlocked playable-judged-never-rewarded, the always-visible teaching companion in both modes, Fort Pillow/Crater/Olustee + Leetown unlocks, the loot/badge/GM expansions, the Risk-style CONQUEST layer with DEM+period-map terrain, the graphics uplift promoted to ARC 3, and the integrated ARC 0-9 master roadmap absorbing Phase D/E/H/I + C3) + DECISIONS D455. EXACT NEXT: charter ARC 1 (the unlock-and-teach spine) from the packet §7 in a FRESH session — lane ledger-only first, per-slice gates, the packet is THE law; the standing candidates (R-7 triggers, further 5b batches, J-polish) queue behind the arcs.** THE FINAL AUDIT SETTLED EVERY AD-11..AD-19 DEBT AND RAN THE AD-0 BATTERY TO 72/137 GREEN WITH ZERO UNRESOLVED REDS; AARON CALLED THE SAFE STOP MID-BATTERY.** All nine LANE-011 debt rows are settled exactly as written (every named probe green with zero pageerrors: mayhem 22/22 · afteraction 18/18 · field 27/27 ×4 · audio-ambience 34/34 · memory-chain 10/10 · concept-links 10/10 · chief-of-staff 10/10 · learn-battle 10/10 · decisions 19/19 · divergence 14/14 · desk 13/13 · h0-main-menu 5/5; all EIGHTEEN predeclared binds bit exactly their teeth with md5-proven byte-identical restores; all three --diagnostic-invalid fixtures re-proven). FOUR dead-code-under-h0-override runtime bugs root-fixed on the LIVE owners (the D444 learn card → src/100 sheet; the D445 morning brief → src/99 desk shell + wiring; the D446 desk concept spans → REAL live copy on the treasury/diplomacy tabs; the D452 Mayhem free-battle launch → the design-§8.2 persistent "· MAYHEM RULESET" HUD chip in fldRenderTop) — each guarded-off path proven byte-identical to its pre-slice build. SIX never-run probe teeth re-toothed STRONGER (comment-stripped forbidden-token scans ×4; the audio panel factored-helper pin; the keymap dispatch tooth); the GEA-14 packet gained the battle-level source register ("every cited source list verbatim" for ancestor-covered battles); DECISIONS.md's two literal NUL bytes restored to text. THE AD-0 BATTERY: 72/137 GREEN serialized and alone (rows 1-37 + 38-49 + 50-72), with TWO battery reds root-fixed and re-run green — row 38 war-career (three stale frozen-wall md5 pins from VETTING-DEFERRED slices, re-pinned with documented chains → 45/45, e9bc7de) and row 50 h0-main-menu (the tooth pre-dated the D420 public ruleset-picker flow, re-toothed STRONGER to the shipped flow → 5/5, 266f6b0). Counts hold: 26 scenarios / schema 59 / Army Register 1,614 / suite 137 / `_SAVE_VER=1` / frozen base untouched / D74 clean. LANE-009 + LANE-010 + LANE-011 rest at VERIFY (battery-only; AD-0 alone gates their SHIPPED flip). **EXACT NEXT: resume the AD-0 battery at THIS head — `export TMPDIR="$PWD/.tmp"` then `node tools/vet-no-regression.mjs --from='field sandbox'` (rows 73-137, ~65 rows), serialized and ALONE on the machine, war-career budget 900s (already passed), reading EVERY artifact JSON (ok + pageerrors; a green artifact is not a green harness until the owning process exits — the D398 law); root-fix any red at its exact label (never weaken a tooth; probe-side reading bugs and stale VETTING-DEFERRED pins are the legitimate classes — the D453 precedent); then write D454 (the release entry), flip the AD-0 ledger row settled, flip LANE-009 + LANE-010 + LANE-011 SHIPPED with the final battery SHA, sync HANDOFF/START-HERE/WAKE-UP/RUN-LOG per the D412 live-head law, commit + push.**


**THE LIVE-HEAD SINGLE-SOURCE RULE (D412):** the top ⚡ block below is THE one canonical live boundary for the whole project. Every other canonical doc (START-HERE, WAKE-UP, AUTONOMOUS-RUN §2, V1-CHECKLIST's live gate) carries at most a 3-5 line summary plus a pointer here; RUN-LOG stays the chronological log. This file keeps the latest ⚡ block plus exactly ONE prior; older superseded blocks move byte-verbatim to `legacy/HANDOFF-ARCHIVE.md` at each release (newest at top), gated by the probe-anchor inventory. Full rules: `START-HERE.md` §Naming convention.

**Older handoff blocks (D422 and earlier, plus the run-h scaffold sections):** moved byte-verbatim to [`legacy/HANDOFF-ARCHIVE.md`](legacy/HANDOFF-ARCHIVE.md) — newest at top (D423 moved at D454; D422 moved at D453; D421 moved at D423; D418 and D417 moved at D420; D413 moved at D418; D412 at D417; D411 at D413; D410 → run-h in D412). Nothing was deleted or paraphrased beyond the standing "(superseded by …)" label flip.
