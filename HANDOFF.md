# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D542 next-lane=LANE-022 state=DRIVE owner=Claude -->
> **Live status (D542, 2026-07-23):** `LANE-022` **takes Claude Code DRIVE for Slice 3** — repair plus finite engineering capacity, the slice where Aaron's logged Engineering Corps directive lands **load-bearing** rather than decorative. This is the contract-before-teeth commit the Contract Relay's hard rule requires: the lane carries the complete Slice-3 acceptance contract as committed prose and **NO runtime byte moved**. Game `4764b1fccb40c473edd871621497f62b`, srcTree `5f6d33325fa5feb04f6d5b11e3f5a3b7`, suite **142**, manifest 112, data 65, `_SAVE_VER` 1 and the frozen base `c9db83fa99230ffb95bdfdfe059f3fb9` all HOLD and are NOT re-pinned. **D541 remains the ARC 7 product head until Slice 3 ships.**
>
> **What Slice 3 contracts.** Clearing a cut costs finite engineering capacity: the Construction Corps rebuilds a cut rail line, the Pontoon Train restores a cut water crossing (roads stay absent until Slice 5, so "clearing" acts on a Slice-2 cut rail/water service condition, never on a road). Capacity is derived from the shipped `C.engineering.levels` (Construction + Pontoon branches), and the **B-5 effectiveness/realism slider** (`fldPresetResolve().attrition`, clamped `[0.6, 1.4]` exactly as `T13`'s `fldEngRealism`) governs magnitude. Repair EXTENDS the shipped `conquestSupplySetCondition` clear-path plus a capacity ledger `C.conquest.supply.repair` — no fourth mutator, no second owner. Multiple simultaneous cuts exceed one pass's capacity, forcing the standing decision "which artery do you restore, or accept the longer water route?" (design law §5). Save stays **purely additive** (`_SAVE_VER` 1, the D447/GEA-12 precedent); containment fails CLOSED both directions at the same `_lgTraceRuleset` seam; non-conquest play stays byte-identical.
>
> **Authority:** design-law §4 Slice 3 + Aaron's logged Engineering Corps + B-5-slider directive. Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-23, D542: LANE-022 TAKES CLAUDE CODE DRIVE FOR SLICE 3 — REPAIR PLUS FINITE ENGINEERING CAPACITY, WHERE THE LOGGED ENGINEERING CORPS DIRECTIVE LANDS LOAD-BEARING (CONTRACT ONLY; NO RUNTIME BYTE MOVED).** D541 is committed and pushed clean at `5ecc00bdb6f5fc65d9e26c32d51fb49371e81d2b`. This is the contract-before-teeth commit the Contract Relay's hard rule requires: LANE-022 carries the complete Slice 3 acceptance contract as committed prose, and the acceptance teeth land in the SAME commit as the code that greens them. Nothing in `src/`, `data/`, `build/`, the manifest, the suite, the save shape or the generated deliverable moved here, so game `4764b1fccb40c473edd871621497f62b`, srcTree `5f6d33325fa5feb04f6d5b11e3f5a3b7`, suite 142, manifest 112, data 65, `_SAVE_VER` 1 and the frozen base `c9db83fa99230ffb95bdfdfe059f3fb9` do NOT move and are NOT re-pinned. **D541 remains the ARC 7 product head until Slice 3 ships.**

**What Slice 3 contracts.** Clearing a cut is no longer free: it costs FINITE engineering capacity, and multiple simultaneous cuts exceed one pass's capacity, forcing the standing decision the ladder is built to teach — *which artery do you restore, or accept the longer water route?* (design law §5). The Construction Corps rebuilds a cut RAIL line; the Pontoon Train restores a cut WATER crossing. Roads stay absent until Slice 5, so "clearing restores a road" (design §4) is implemented here as clearing a Slice-2 cut rail/water service condition, NEVER as authoring a road. Repair EXTENDS the shipped `conquestSupplySetCondition` clear-path plus a capacity ledger `C.conquest.supply.repair` — invariant F holds: NO fourth public mutator, NO second logistics/engineering owner, NO second save owner, NO new clock. Capacity is a pure reader `conquestRepairCapacity(C)` over the shipped `C.engineering.levels` (Construction + Pontoon branches — the Engineering Corps made load-bearing) scaled by the B-5 slider; a pure reader `conquestSupplyRepairReport(C)` surfaces the standing decision.

**The B-5 slider governs magnitude (invariant I — the live symbol confirmed from src, not invented).** Repair magnitude reads the shipped effectiveness/realism lever `fldPresetResolve().attrition` — the exact lever `T13`'s `fldEngRealism` already couples to engineering (Arcade 0.7 fast / Balanced 1.0 / Historian 1.3 slow), clamped to `[0.6, 1.4]` identically. Higher realism ⇒ lower repair capacity ⇒ harder engineering, the same polarity as the tactical dig-speed coupling. Guarded (`typeof fldPresetResolve === "function"`, neutral 1.0 when no preset); no `sev.`, `G.` or `window.` in the seam region.

**Bounded channel, containment and save, all unchanged in law.** `logisticsBridgeBonus` keeps its caps (`supply ≤ 7`, `fatigueRelief ≤ 5`, `overall ≤ 2`) and `wr.supply` keeps its 0.15 troop-morale weight; supply stays a bounded CONDITIONING INPUT — repair NEVER writes casualties, morale, victory, score or RNG, and Slice 3 creates NO new combat channel. Containment reuses the same `_lgTraceRuleset` allowlist gate: `conquestRepairCapacity` / `conquestSupplyRepairReport` gate through `_lgSupplyView`, so authored repair/capacity state fails CLOSED on the evidence-gated ruleset before any substrate read, and `CONTAINMENT-B` stays the ONE tooth owning the gated-ruleset query and the allowlist source assertion. The capacity/repair state is control/condition-class and must NEVER enter the CF-2 memo. Save stays **purely additive**: `C.conquest.supply.repair` rides the existing envelope, none of the seven `tools/save-shape.json` functions is touched, and `_SAVE_VER` stays 1 as a CONSCIOUS decision (the D447/GEA-12 precedent). A pre-Slice-3 save loads, round-trips and re-serializes BYTE-IDENTICALLY, and the sanitizer fails CLOSED on a malformed repair payload.

**Gates owed at the shipping commit:** `node --check` on every touched file; `node tools/build.mjs` GATE OK; artifact readback of the new repair/capacity symbols inside the built 61 module region; the two LANE-022 probes; the adjacent logistics/bridge/conditioning/engineering/board/state/transport/campaign-link/auto-resolve/save-slots/command/presets probes; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest 8/8, doc coherence 5/5; **three** A/B legs — leg 1 the 24-scenario × 8-seed direction battery byte-identical to the `5ecc00b` baseline `18f609d07b1190904ec0c11e4ca64675`, leg 2 `probe-full-campaign` byte-identical to `a38185fd371a7f181250eff3a6cbf76a`, and leg 3 the conquest-ON leg which WILL move as repairs restore friction, with both columns logged, the direction stated, and the movement adjudicated under D92 as an accurate-inputs consequence (a restored line lowers friction toward its TRACED value — never tuned, never an output gate); ONE declared negative bind on the containment allowlist redding ONLY `CONTAINMENT-B` with an md5-proven byte-identical restore; `git diff --check`. Five pin sites re-anchor (three generated-game, two srcTree); suite/focused/manifest/base pins hold and no probe is added, so the suite stays 142. **EXACT NEXT:** implement D542 exactly as contracted, ship it under its own D###, then release LANE-022 and contract Slice 4 (blockade/sea edge) or issue the capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.

**⚡ AMENDMENT — 2026-07-23, D541: LANE-022 SLICE 2 SHIPS — CONTROL AND SERVICE CONDITION BECOME REAL STATE AND SUPPLY BITES INSIDE A CONQUEST CAMPAIGN, WHILE NON-CONQUEST PLAY STAYS BYTE-IDENTICAL; THE LANE RELEASES TO CONTRACT/NONE.** D540 is committed and pushed clean at `7e48ca813be39a9db9df565e5af4976547b55a80`. Slice 2 landed exactly as contracted, with two acceptance teeth tightened mid-slice (over-broad, never the code). **D541 replaces D539 as the ARC 7 product head, and LANE-022 releases to `CONTRACT` / `none`.**

**What shipped.** Territory control and per-segment service condition are real state in ONE namespace, `C.conquest.supply`, read by one pure fail-closed reader (`_lgSupplyView`) and written by exactly two guarded mutators (`conquestSupplySetControl` / `conquestSupplySetCondition`) through one guarded store accessor (`_lgSupplyStore`). `conquestSupplyTrace` runs a BASE walk over the full open projection and a LIVE walk filtered by cuts and control, resolving to three bounded states. `TRACED` and `SEVERED` flip `applied` true and `_lgRoute` adopts the clamped `tracedFriction` into `friction` — the single sim-affecting line of the whole slice; `SUBSTRATE_GAP` does not. A segment carries for a side only if it is not cut AND neither endpoint is enemy-held, with the depot friendly, so a cut degrades `depotReach` for every army downstream (64 → 52 on the traced US line). `C.logistics` stays the sole logistics store; `presLogisticsBlock` gains one guarded conquest supply-line block and the other logistics owners are textually unchanged. No module added (manifest 112), no data change (65), `build/base.html` frozen.

**The eleven-component gap is a first-class taught case, not a penalty.** `SUBSTRATE_GAP` leaves `applied:false` and the shipped friction untouched: the CS/E default carrier reads friction 12 == plain 12, depotReach 46 == plain 46 in the built deliverable — the evidence gap costs the player exactly zero. That gap is ours (five passes returned zero established windows; the road layer waits for Slice 5) and roads were universal 1861–65, so charging the player for it would invert the history and trip the D92 output-gate bar.

**A/B — two zero-diff legs, one honest leg.** Leg 1 (conquest OFF) the 24-scenario × 8-seed direction battery `18f609d07b1190904ec0c11e4ca64675` BYTE-IDENTICAL to the re-captured `a613bac` baseline (0 diffs); leg 2 `probe-full-campaign` `a38185fd371a7f181250eff3a6cbf76a` BYTE-IDENTICAL (0 diffs). Leg 3 (conquest ON, in-page, both columns, 0 pageerrors): US TRACED friction 8→7; US SEVERED (cut / enemy-target / depot-lost) friction 8→40 at the authored ceiling, depotReach 64→52; CS SUBSTRATE_GAP 12→12 unmoved — adjudicated under D92 as accurate-inputs consequences, every number derived from the graph walk plus the control/cut state, never tuned, every state inside the shipped caps.

**Bind, save, and the two carry-forwards.** Bind D540-B1 mutated the containment allowlist and ran the focused probe WITHOUT rebuilding: ONLY `CONTAINMENT-B` redded (15/16), restore byte-identical (`src/61` md5 `fa03e6a65e76a3e6c994aa026a179f63` pre == post), rebuild returned the identical game md5 at 16/16. Save is **purely additive** — `C.conquest.supply` rides the existing envelope, none of the seven `tools/save-shape.json` functions moved, `_SAVE_VER` stays 1; a legacy campaign round-trips byte-identically and nine malformed payloads each fail closed. **CF-2** was resolved by measurement — the memo (`_lgTraceBase` / `_lgTraceMemo`) caches only the control/condition-independent base projection (the real cost was `conquestTransportNormalized()` at 12.10 ms/call, not the 0.14 ms adjacency build), proven to deep-equal a cold recompute and to invalidate on a swapped pack. **CF-1** was resolved by git adjudication — three of that probe's four reds were shown to predate D539 at `0829d8d`, only the suite pin was D539's, so option (a) re-pinned the two 140s to 142 (5/9 → 6/9) and left the three structural reds named as LANE-020's.

**Gates, all green with every artifact read:** build `GATE OK`; focused 16/16 (0 pageerrors, 0 realErrors); plan 10/10; adjacent logistics-rail 8, logistics 15, bridge 6, conditioning 9, conquest-board 13/13, conquest-state 15/15, conquest-transport 18/18, campaign-link 19, auto-resolve 10, save-slots 17/17, command 100, presets 27 — all exit 0, 0 pageerrors; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest layer 8/8, doc coherence 5/5; `git diff --check` clean. Game `45278110cb73ea4719fa41ffef7682f9` → `4764b1fccb40c473edd871621497f62b`, srcTree `08f95d9e9311e90313cc5b7a930f9380` → `5f6d33325fa5feb04f6d5b11e3f5a3b7`, re-anchored at FIVE disk-verified sites (three generated-game, two srcTree); suite md5, focused md5, manifest and frozen base HOLD. **LANE-019 is deliberately unrewritten** (transport plan 12/12). **EXACT NEXT:** under D514, reload the ledger and contract LANE-022 Slice 3 (repair + finite engineering capacity — the Engineering Corps directive load-bearing) in a committed DRIVE take, or issue the capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
