# HANDOFF — "The Civil War"

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM R-4 — the X-FACTORS (the dramatic "in the zone" surge, inside the no-fudge wall)** (overnight run, D98)

> **The Madden layer now SWINGS a fight — provably without ever touching the scoreboard.** An X-Factor badge enters "in the zone" when its situational trigger fires and surges on ONE channel, hard-capped. Demonstrated: a single `foot_cavalry` badge on one CS sandbox brigade **flipped a deterministic US win into a CS win** (same seed), yet the total-casualty shift was only **17.2%** and the command aura peaked at **0.47 < the 0.9 wall** — the rating reaches the result EMERGENTLY through bounded inputs, never by writing `cas`/`victory`/`men`/`sev`.
>
> **`fldXFactorStep(dt)` (`src/tactical/T14-ratings.js`):** gated on `__FIELD.badges`; for each unit carrying an `rung:"xfactor"` badge (`_fldUnitXFactors`), `_fldXFactorInZone` tests activation (march_vigor=moving · his_offensive=moving/charging · ammo_low_defend=ammo<12 & not charging · last_stand_defend=holding & (low morale or a friendly routing within 220yd) · usct_assault/mass_assault=charging · flanking=cav & charging). In the zone it surges **ONE channel = the declared lever**: a NON-speed X-Factor sets `u._xfActive = fldRatingRealismCap(tier,"xfactor")` (arcade 1.45 / balanced 1.20 / historian 1.08); a SPEED X-Factor (Foot Cavalry / The Slows) sets `u._spdMul` only (capped [0.85,1.15]) and does NOT also surge the aura. One-shot ⚡ `fldAnnounce`+`fldScenarioBanner` (`_xfOn` rising edge); `_xfGlow` marker pulse; deterministic `_fldXfDecay`→1 out of zone. **NO RNG.** A unit with no X-Factor is wholly untouched → byte-identical.
>
> **`fldXFactorApplyCmd(u)`:** `u.cmdBonus = min(FLDO.CMD_BONUS_CAP, (cmdBonus||0)*_xfActive)` — pushes the summed aura toward the **0.9 wall, never beyond**; strict no-op unless active. Called in `fldSimStep` AFTER `fldOfficersStep` (aura summed first), gated on `badges` (NOT officers) so both surge channels share one gate.
>
> **Seams (`src/tactical/T0-field-sandbox.js`):** `fldSimStep` runs the surge + apply around `fldOfficersStep`; **`fldMoveFactor` clamps the COMBINED speed factor** (the `_spdMul` surge × the R-3 static speed-badge factor) to ONE documented band **[0.75,1.30]** (exactly 1 → skipped → byte-identical); `fld2dDraw` draws a guarded `_xfGlow` — a CVD-safe amber burst RING (the shape carries meaning) + a **vector-drawn** lightning bolt (no emoji-font tofu), `reduceMotion`-honored. **Data:** added `first_with_the_most` (Forrest, cav) → 7 X-Factors; none assigned to a shipped roster yet (R-6) → byte-identical.
>
> **Vet:** GATE OK (no-fudge ✓); `probe-ratings` **17/17** (the R-4 surge step + the NO-FUDGE REPLAY GATE: the surge bites + bounded + capped); the **`domcontentloaded` byte-identity harness IDENTICAL for all 10** (sandbox + 9 battles, R-4 build vs committed R-3 HEAD — the authoritative X-Factor-off no-regression proof); `tactical-visuals` 10/10 scenes 0 pe; `diag-classic` `nonBlank:346`; the 9 battle probes + field + logistics + bootprobe passed via `load` (when momentarily healthy). **⚠ ENV (still intermittent):** the standard `load` suite stalled on the THREE CDN this session — the systems probes (officers/command/presets/…) were `load`-blocked, but the byte-identity harness exercises the full `fldSimStep`→`fldXFactorStep`→`fldOfficersStep` path (officers ON) for all 10 and proves it byte-identical, covering their core concern. **Re-run them next session once `load` is healthy** (serve via ThreadingHTTPServer; if `load` still stalls, the durable fix = domcontentloaded+settle in the probe runner OR vendor THREE locally — a recommended small vetted milestone). **Bug-hunt** (23 Opus agents, 16 raw → 4 confirmed + 3 critic, 0 HIGH, all LATENT@R-6): all 7 fixed (combined-speed clamp; cmdBonus surge gated to non-speed [one channel/badge]; apply decoupled from officers; vector bolt; grand_charge prov→Inferred + dual-downside deferred-note); catalog-wide badge-provenance sweep logged for R-6.
>
> **NEXT = Q7 — the GM franchise layer** (RATING-SYSTEM-DESIGN §12–§14): the depth-chart roster (Army→Corps→Division→Brigade billets, each showing OVR+grade+badges + roll-up OVR) · roster moves (appoint/relieve/promote/transfer/commission) spending **political capital** (`C.clock.capital`, extends `cmdAppoint`/`_cmdReliefCost`) · the **dual Attack/Defend OVR** (`fldDualOVR` already exists — surface it) · the **pre-battle matchup screen** (corps-vs-corps OVR board, a labeled predicted-edge bar; pure display over numbers the sim already reads). Then Q8 camp loop → R-5 prosopography scale-out → R-6 negative-badge sweep + the badge-provenance sweep + live dev-traits → the E strategic arc → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §12 (the depth chart) + §13 (dual OVR + matchup screen) + §14.3 (the political-capital economy numbers) + §15 the GM-layer answers → DECISIONS D98/D97/D94 → `src/35-command.js` (`cmdAppoint`/`cmdRevert`/`_cmdReliefCost`/`_cmdGenRow` — the GM spine to generalize) + `src/tactical/T14-ratings.js` (`fldDualOVR`/`fldUnitRatingOVR`/`fldRatingGrade`) + `data/ratings.json` (`generalPersonas` + `dualOvrTilt`). Build R-4's siblings (Q7→Q8) on the now-complete badge+X-Factor engine.

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM R-3 — the badge engine (the first rating→combat seam) + the no-fudge build-gate** (overnight run, D97)

> **The first place a rating TOUCHES combat is shipped.** A guarded one-token factor seeds the levers the engine already reads; **byte-identical when off** (proven: sandbox + all 9 battles produce IDENTICAL deterministic results on the R-3 build vs the committed R-2 build).
>
> **`fldBadgeFactor(u, lever)` (`src/tactical/T14-ratings.js`):** guarded MULTIPLICATIVE one-token factor — identity 1.0 when `__FIELD.badges` off / no matching badge. Sums the unit's badges' signed `mag`s for a lever, gates by trigger (`_fldBadgeTrig`), CLAMPS the sum to the realism-scaled per-lever cap (`fldRatingRealismCap(tier,"badgeLever")` — arcade .16 / balanced .10 / historian .06). Negatives DAMP (≥1−cap), never enemy buffs. Plus `fldUnitCohesion` (rally term, 0 when unauthored).
>
> **Four guarded seams (`src/tactical/T0-field-sandbox.js`):** fire (`fldResolveFire`), rally+cohesion (`fldMoraleStep`), melee (`fldResolveMelee`), speed (`fldMoveFactor` — `_spdMul` reserved for the R-4 X-Factor surge). `__FIELD.badges` gate (default ON, `_badgesOff` hook) + `__FIELD.realismTier` (`T6 fldPresetsApply`).
>
> **Badge data (`data/ratings.json`):** 25 badgeDefs gain `fldLever` + signed `mag`. Ships Star/Superstar positives + the Verified negatives with real bite (McClellan Slows / Bragg Piecemeal / Burnside Rigid / Green Levies / Powder-Shy). **NOT yet assigned to shipped rosters** (that's the R-6 sweep) → battles byte-identical.
>
> **No-fudge OUTPUT-WALL build-gate (`tools/build.mjs` gate 4d):** scans the rating module for any `cas`/`aCas`/`bCas`/`.victory`/`.men`/`sev.*` write — **dot AND bracket notation** — build FAILS (exit 5) if found. Verified firing on planted `u.men=0`, `u["men"]=0`, `F.sev["attrition"]=2`; clean build prints `GATE OK · … · no-fudge ✓`.
>
> **⚠ VETTING — environmental deviation (READ before re-vetting):** the standard `load`-based probe suite was **blocked** this session — headless Chrome's `load` event never fires for the game page (the THREE-CDN `<script>` stalls in headless; it breaks the committed-green R-2 build identically; a trivial page fires `load` in 73ms; `domcontentloaded` loads everything incl. THREE in ~1.5s). Contributors: the single-threaded `python -m http.server` chokes on the page's ~170 local asset requests (**use a ThreadingHTTPServer**), + the THREE-CDN `load` stall (documented gotcha). R-3 was vetted via a **`domcontentloaded` byte-identity harness** (`.tmp/r3-verify.mjs`): sandbox + all 9 battles deterministic, R-3 build vs committed R-2 → **IDENTICAL** for all 10, + the full badge-logic assertions, 0 pageerrors. Bug-hunt (4 Opus lenses × verify + critic): 2 confirmed (gate-4d bracket bypass → fixed; phantom `sharpshooters` key → fixed). **NEXT SESSION: re-run the standard `load` suite (serve with `python3 -c "from http.server import SimpleHTTPRequestHandler,ThreadingHTTPServer; ThreadingHTTPServer(('',8765),SimpleHTTPRequestHandler).serve_forever()"`); if `load` still stalls, the fix is a domcontentloaded+settle wait in the probe runner or vendoring THREE locally.**
>
> **NEXT = R-4** (the X-FACTORS — the dramatic Madden surge): `u._xfActive` activation ("in the zone") that scales the **already-summed** `cmdBonus` aura, hard-capped by `fldRatingRealismCap(tier,"xfactor")` (never breaching `CMD_BONUS_CAP`); `abZap`/`fldAnnounce` one-shot + HUD glow; ship Foot Cavalry, Rock of Chickamauga, Bayonet!, First with the Most, Earned in Blood, The Slows. **The no-fudge replay gate:** deterministic seed-replay shows badges-on final casualties differ from badges-off only within the bounded lever delta; X-Factor never exceeds `CMD_BONUS_CAP`. Resume map: START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §4 (the X-Factor rows) + §6 (the `u._xfActive` seam: "scales the already-summed cmdBonus, clamped at CMD_BONUS_CAP") + §9 (R-4 asserts) → DECISIONS D97/D96/D95/D94* → `src/tactical/T14-ratings.js` (fldBadgeFactor + the realism caps) + `src/tactical/T3-officers.js` (`fldOfficersStep` — the cmdBonus aura sum, where `_xfActive` scales) + `src/tactical/T0-field-sandbox.js` (`fldAnnounce`/HUD glow) + `tools/probe-ratings.mjs`.

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM R-2 — the OVR read-out UI (Command-desk A–F grades + tactical HUD brigade OVR)** (overnight run, D96)

> **The Madden layer is now VISIBLE.** R-2 surfaces the rating numbers the R-1 spine already produces, graded **A–F**, across two surfaces — **PURE DISPLAY, no sim change** (full no-regression confirms all 9 battles byte-identical).
>
> **Command desk (`src/35-command.js`):** the active-general card headline shows the **officer OVR** (`round(_cmdGenRating)` — A+ ≥90 reachable, unlike the 42–88-clamped `commandLeadership`) + the **A–F grade letter** + the word, band colour as a decorative left-border accent, with the bridge value kept as a secondary "Fields command at NN" line; the appoint-pool rows show a colour dot + bold A–F letter + word + "(NN OVR)".
>
> **Tactical HUD (`src/tactical/T14-ratings.js` `fldRatingHudSelected` → `T0` `fldRenderHud`):** the selected brigade's computed OVR (`fldUnitRatingOVR`, pure read) + A–F grade, as a new guarded HUD line (byte-identical when ratings data absent).
>
> **WCAG 2.2 AA + CVD-safe:** triple-encoded everywhere — **number + letter-grade + word in high-contrast `--parch` text; colour DECORATIVE only** (aria-hidden stripes/dots). `wcag-auditor` PASSED all three new surfaces; two pre-existing sub-AA secondary labels lifted `--rule #8a7350 → #b3925e` (5.0:1 measured against the **real** `.sheet` gradient, not pure black — the bug-hunt caught the auditor's first #8d7551 being computed against #000 and still failing).
>
> **Vet:** GATE OK; `probe-ratings` 14/14 + `probe-command` 16/16 (new R-2 UI steps); FULL no-regression GREEN (28 probes byte-identical); diag-classic `nonBlank:346`; 0 pageerrors. Bug-hunt (3 Opus lenses × refute-verify + critic): 2 confirmed (false-AA contrast → fixed/honestly-measured) + 2 critic (a `/* */` audit comment leaking into the rendered DOM → moved out of the JS string, DOM-leak signature now 0; `gr.word`/`g.word` unescaped → escaped, defense-in-depth). All fixed + re-vetted.
>
> **NEXT = R-3** (the BADGE ENGINE — the first rating→combat seam): a guarded `fldBadgeFactor(u, lever)` (identity 1.0/0 when `__FIELD.badges` absent, mirroring `(u.cmdBonus||0)`) + the `u._spdMul` march seam + the `cohesion` rally extension in `fldMoraleStep`; the **global per-unit per-lever stacking cap** (realism-scaled via `fldRatingRealismCap`); the **no-fudge build-gate assertion** (no badge handler may write `cas`/`aCas`/`bCas`/victory/`tgt.men` or out-of-band `sev.*`); ship Star/Superstar + the Verified negatives (Bragg Piecemeal / McClellan Slows / Burnside Rigid / Green Levies / Powder-Shy). **Badges-off byte-identical; badges-on bounded + reversed by removing data.** Resume map: START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §4 (the badge catalog) + §6 (engine wiring, the exact seam table) + §9 (R-3 probe asserts) → DECISIONS D96/D95/D94* → `src/tactical/T14-ratings.js` (`fldBadgeDef`/`fldRatingRealismCap` already defined) + `data/ratings.json` (`badgeDefs`/`badgeRungs`/`realismCaps`) + `src/tactical/T0-field-sandbox.js` (`fldResolveFire`/`fldMoraleStep`/`fldMoveFactor` — the `xpF`/rally/move lines to add the one-token factor) + `src/tactical/T3-officers.js` (`cmdBonus` aura) + `tools/probe-ratings.mjs`.

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM R-1 — officer derivation wired into the command + field pipes (the persona is now the source of truth)** (overnight run, D95)

> **The first WIRING increment of the D94 Madden layer is SHIPPED + vetted + pushed** (R-0 shipped the pure-function data spine on 2026-06-21; this is R-1). A documented persona now SEEDS the two pipes the engine already reads — strategic `gen.skill` and tactical officer `quality`/`radius`/`fate` — with the **no-fudge keystone intact** (the persona only sets INPUTS at build/make time; nothing is ever read at casualty/winner/rout time) and **every unrated entity byte-identical**.
>
> **Strategic** (`src/35-command.js`): `_cmdGenRating` now derives a general's `skill` from a linked persona via new `_cmdGenPersona`/`_cmdEffectiveSkill`; `cmdActiveTraits.skill` + the Command-desk Skill bar route through the same fn (ONE source of truth). The **9 strategic personas** (`generalPersonas` in `data/ratings.json`, keyed by the `generals.json` id — **CS** Lee/Jackson/A.S. Johnston/Bragg/Longstreet · **US** McClellan/Grant/Burnside/Meade, the principal commanders of the 9 D92-hardened battles) are calibrated so `round(0.45*command+0.35*tactical+0.20*discipline) === gen.skill EXACTLY` → commandLeadership/auto-resolve/conditioning/appoint-pool words stay **byte-identical** while the persona is the source.
>
> **Tactical** (`src/tactical/T3-officers.js`): `fldMakeOfficer` derives a field leader's `quality` (calibrated, aura shifts ≤0.005), `radius` (authored echelon + charisma nudge, clamped 160–290), `fate` (persona durability) — but ONLY via an **explicit `pid` opt-in (no `id` fallback)**, the fix for a real cross-scenario id collision (`ld_jackson` is reused at Bull Run / Antietam / Chancellorsville). The 7 Bull Run leaders are opted in (`data/bullrun.json`); every other scenario stays byte-identical.
>
> **Vet:** GATE OK; `probe-ratings` 13/13 (+3 R-1 steps incl. a collision guard), `probe-command` 15/15 (+source-of-truth guard); FULL no-regression GREEN (all 9 battles byte-identical incl. **chancellorsville 24/24** = collision fix proven, + roster/custom/fog/ai/autopause/campaign-link/logistics/arms/presets/csplayer/officers/field/engineering-corps/tactical-visuals); diag-classic `nonBlank:346`; 0 pageerrors. Adversarial bug-hunt (5 Opus lenses × refute-verify + critic): 0 confirmed finder findings, 1 LOW critic finding (single-source-of-truth) FIXED.
>
> **NEXT = R-2** (the OVR read-out UI: OVR + word + **A–F report-card grade** on the Command desk + a unit-inspect line; pure display, WCAG/CVD triple-encode — number+word+glyph, color decorative). Then R-3 badge engine → R-4 X-Factors → Q7 GM layer → Q8 camp loop → the E arc → Phase F/I/H/J pillars → **Chattanooga + USCT battles LAST** (Aaron's queue order, §2 of `OVERNIGHT-RUN-PROMPT.md`). Resume map: START-HERE → this block → `RATING-SYSTEM-DESIGN.md` (§3 bands + §10 the WCAG/CVD badge+tier UI for R-2) → DECISIONS D95/D94* → `src/35-command.js` (`_cmdActiveCard`/`_cmdGenRow` to surface OVR+grade) + `src/tactical/T14-ratings.js` (`fldRatingGrade`/`fldDualOVR`/`fldPersonaOVR`) + `data/ratings.json` (`generalPersonas`) + `tools/probe-ratings.mjs`.

---

## ⚡ ARCHIVED — 2026-06-21 **ROSTER HISTORY-ACCURACY HARDENING — all 9 shipped battles audited; 15 HIGH wrong-fact corrections** (Aaron-directed; the EA-Sports-roster groundwork) — D92

> **Aaron asked whether the full Chattanooga-grade rigor had been applied to the already-built battles. It had NOT for the 7 pre-Opus battles** (the Codex/early-built C1 Eastern marquee + Shiloh + Bull Run); only Vicksburg + Chickamauga had the Opus adversarial history pass. An adversarial audit (9 Opus historians + critic + the new consolidated `tools/sweep-all-battles.mjs` casualty-direction diagnostic) found **15 HIGH wrong facts shipped under "Verified" stamps** — mostly the **rank-at-the-battle** class — ALL FIXED + annotated in-file: **Malvern** (Hunt→Col., Jackson→Maj.Gen., Porter→Brig.Gen., Hooker→Maj.Gen.); **Chancellorsville** (Rodes→Brig.Gen., Berry→III Corps, Stonewall Bde→Colston's div, **dead officer Hazzard→Best**); **Shiloh** (Breckinridge→Brig.Gen., Bragg→RIGHT wing, Wallace died Apr 10); **Gettysburg** (Doubleday & Early→Maj.Gen., Stannard→I Corps); **Vicksburg** (M.L. Smith→Brig.Gen.). Plus ~12 medium/low. **Surfaced, NOT auto-fixed (genuine source conflict):** Bull Run Heintzelman + Antietam Mansfield.
>
> **LOCKED PRINCIPLE (Aaron, the input/output distinction — obey it):** outcomes emerge from **skill + weapon type + terrain + tactics** via the D74 universal model; history enters ONLY as the **default equipment** (accurate counts/guns/weapons/terrain/doctrine) so the **probable** result lands near history *because the inputs are true* — **NEVER an output gate forcing a casualty direction/winner.** DROPPED the planned Antietam balance refight + hard historical-direction probe gates; `sweep-all-battles.mjs` is a DIAGNOSTIC only; kept guards are **system validations** (cover↓casualties, rifled out-ranges smoothbore, more guns=more fire) that keep the game legible. (Memory `civilwar-roster-accuracy-principle`; DECISIONS **D92**.)
>
> **NEW PILLAR captured + scheduled (D93, Aaron): "The Soldier's Story"** — an EA-Sports-style prosopography DB (bio/stats/history/portrait/team for EVERY identifiable individual) + a **play-as-anyone whole-war journey mode** (pick any person, choose when+where to start, proceed from their **real rank/position**). **Build AFTER the C+E roadmap**, unified with the D91 loot/survival journey mode + interpersonal/romance. The D92 rank/position hardening is its first roster data (hard prerequisite for "start from real rank"). Granularity = exhaustive ("every named individual found").
>
> **Verification:** GATE OK; all 9 battle probes green (each battle's balance assertions still green → outcomes byte-preserved), roster 8/8, custom-builder 9/9, broad tactical-systems suite + boot/t1 green, diag-classic `nonBlank:346`, 0 pageerrors. Edits are labels/notes only except Antietam Benjamin 6→4 guns (accurate) + Shiloh Wood→Chalmers (stats kept).
>
> **⚡ COMMITTED 2026-06-21 (resumed session — the prior one died on a 529 API error mid-D92 with the work uncommitted).** Before committing, ran an **independent adversarial re-verification** (Opus historian per battle + critic) to catch any D92 "fix" that was itself wrong → found + fixed **4 more** (see DECISIONS *D92-verify*): Bull Run Elzey gloss (his own regiment = the 1st Maryland, not the Virginia regts); **Shiloh Ammen → Col.** (was wrongly Brig. Gen.; BG dated Jul 16 1862, after the battle); **Antietam Battery B `rifled→smooth`** (six 12-pdr Napoleons — its note already said "double canister"; antietam probe stayed 16/16, outcome-neutral); **Malvern Hill Hooker reverted Maj.Gen.→Brig.Gen.** (D92 over-promoted him by date-of-rank, but his MG commission *issued* Jul 26 1862 after the battle — the Seven Days OOB lists him BG, like Porter/Couch/Kearny). Re-vet GREEN, pushed to `adhanke-star/History-Video-Game` main.
>
> **NEXT (unchanged): CHATTANOOGA** — the US-offensive reversal (Orchard Knob → Lookout Mountain → Missionary Ridge; brief `.tmp/chattanooga-design.md`, verified fact sheet `.tmp/chattanooga-factsheet.json`), then **C3 USCT battles**, then the **E strategic arc**, then the Soldier's Story pillar.

---

## ⚡ CONTINUE HERE — 2026-06-21 **CHICKAMAUGA — the 3-phase Western epic shipped + adversarially hardened** (the woods → Longstreet's breakthrough → Thomas at Snodgrass Hill; probe 18/18) — D90

> **Chickamauga is playable and verified — the bloodiest battle of the Western theater, the fourth multi-phase epic, built on the T8 engine.** A new **⚔ BATTLE — CHICKAMAUGA (1863)** main-menu button (rank 60, after Vicksburg) opens the side-choice card; command Bragg/Longstreet's army or Thomas's defense across three sectors in sequence: **P0 The Battle in the Woods (Sep 19)** — a confused meeting engagement in blind timber; the Union line holds the road (historical). **P1 Longstreet's Breakthrough (Sep 20)** — the Wood gap order pulls a US division out of line at the moment Longstreet's column strikes the Brotherton farm; the CS pours through and routs the right (the decisive **scoreWeight-3** phase). **P2 The Rock of Chickamauga** — Thomas holds Snodgrass Hill / Horseshoe Ridge until dusk, reinforced by Granger/Steedman, saving the army. Anti-Lost-Cause: **contingency over genius** (a staff blunder + a timing coincidence, not martial superiority) and **the squandered victory** (Bragg fails to pursue → the siege of Chattanooga → the November reversal).
>
> **Framing (decide-&-log, no fork):** the C2 "Chickamauga/Chattanooga" item = a matched PAIR of separate 3-phase epics (the Antietam/Gettysburg precedent), each its own milestone. **Top-level `attacker: CS` / `defender: US` across all 3 phases** (the Army of the Cumberland fought on the defensive) — the CS player is the offensive, the US player holds. This sets **Chattanooga up as the US-offensive MIRROR** (the next milestone; design brief at `.tmp/chattanooga-design.md`, which also banks the engine-grain lessons from this build). Universal gun model only; **no per-battle damage fudge** (D74).
>
> **Balance (AI-vs-AI, 32 seeds, deterministic):** P0 US-holds 32/32 (mutual bloodbath ~2,400 each); P1 **CS breakthrough 32/32** (US routed cheaply, US:CS ~5.1); P2 **US-holds 32/32**, CS attacker pays **CS:US ~3.0** (the Fredericksburg failed-assault lesson); **aggregate CS 32/32** (CS 3 / US 2). A human US player who closes the Wood gap in P1 **overturns** the battle (the alt-history hinge). The CS won the field yet **bled MORE in aggregate** (~70k vs ~63k / 8 seeds) — the costly-victory teaching, probe-guarded.
>
> **Research+verify (Workflow) corrected before authoring:** Hood Sep 20 / Maj. Gen. (not Sep 19 / Lt. Gen.); Helm under Breckinridge (not Cleburne); Cheatham restored; Granger & W.H.T. Walker = Maj. Gen.; Loring removed; gap-reporter flagged (Kellogg vs Bond); "River of Death" shipped only as disputed legend.
>
> **Bug-hunt (10 Opus agents): 2 confirmed + 3 critic, all fixed.** HIGH (the Vicksburg wrong-rank-under-Verified class): **Charles Harker was a COLONEL at Chickamauga, not "Brig. Gen."** (like Hans Heg; the BG dated to the battle but came after) → corrected to **Col. Harker** + a fact-check guard. Plus 4 probe-rigor guards (P1 casualty-direction US:CS ≥2.5, P0 winner ≥7/8, P1 win-by-SEIZE "hold", aggregate winner-bled-more).
>
> **Verification:** GATE OK; `probe-chickamauga` **18/18**, 0 pe; full no-regression GREEN (all 8 battle baselines + roster 8 + field/officers/logistics/arms/presets/csplayer/campaign-link/fog/ai/autopause + **custom-builder 9** [EXPECTED baseline updated for `chickamauga` — the D86/D88 gotcha] + engineering-corps 22 + **tactical-visuals 10 scenes incl. live Chickamauga 2D/3D, texWarn 0**; diag-classic nonBlank 346; boot/t1 ok; 0 pageerrors); WCAG inherited-AA (no new color CSS). Pushed to `adhanke-star/History-Video-Game` main (D90).
>
> **Next:** **CHATTANOOGA** (the US-offensive reversal — Orchard Knob → Lookout Mountain "Battle Above the Clouds" → Missionary Ridge; build it from `.tmp/chattanooga-design.md`, which has the framing + the engine-grain lessons), then **C3 USCT battles** (the Crater · New Market Heights · Olustee · Nashville). **Resume map:** START-HERE.md → this block → WAKE-UP.md top block → newest RUN-LOG.md entry → DECISIONS.md D90 → `.tmp/chattanooga-design.md` → `data/chickamauga.json` (the template) + `src/tactical/T1-bull-run.js` (registry/rank) + `src/tactical/T8-phases.js` (multi-phase engine) + `tools/probe-chickamauga.mjs` / `tools/sweep-chickamauga.mjs` (probe/sweep templates).

---

## ⚡ CONTINUE HERE — 2026-06-21 **ENGINEERING CORPS — increment 3: PONTOON BRIDGING shipped + hardened → THE CORPS IS COMPLETE** (the pontoon train; player-only/byte-identical; probe 22/22) — D89

> **The Tactical Engineering Corps is DONE — all 3 of Aaron's locked "all of the above" (entrench + abatis + pontoons).** A NEW **opt-in river** (`__FIELD.terrain.rivers[]`) actually gates movement: a river is impassable except at a **crossing** — a **shallow ford** (passable, slow + fatiguing), a **deep ford** (fordable only at Arcade; needs a bridge at Balanced/Historian), or a player-laid **pontoon bridge** (key **N** / "Pontoon" button): an infantry pioneer detail on the near bank lays a bridge over realism-scaled time, opening a fast crossing. Surfaced via the **Skirmish → Ground → "River crossing"** option (+ an `opts.river` sandbox flag for the probe). The **B-5 realism slider** scales sapper speed, ford slow, AND fordable-vs-requires-pontoon; the strategic **Engineer Works Corps** (`bridgeArmy.engineering`) lays bridges faster on a campaign launch (the A1 anchor, via `__FIELD.engCorpsSpeed`). 2D slate-water band + boat-deck pontoons + ford stepping-stones + labels (CVD-safe, reduceMotion-safe); 3D water plane + pontoon boats/deck; a citation-grade Fredericksburg/Rappahannock + James-River-1864 teaching card.
>
> **Architecture (byte-identical):** the active gate REUSES the two T0 seams reserved for it — `fldEngMoveFactor` (ford slow) + `fldEngMoveGate` (returns a clamped bank point for an illegal water step). New T13 fns: `fldEngInstallRiver`, `fldEngCrossAt`, `_fldRiverNearest/_fldRiverBankPoint/_fldEngBridgeSite`, `fldSelPontoon`, the pontoon loop in `fldEngStep`, `fldEngDrawWater2d`, `fld3dBuildWater` + pontoon sync in `fld3dSyncEng`. **No shipped scenario declares `terrain.rivers` and the AI never lays a pontoon** → every AI-vs-AI baseline byte-for-byte unchanged (every read gated by `rv && rv.length` / `engPontoons`; reset per-launch + per-phase). One T0 seam edit: `fldStepMovement` now settles a no-progress river clamp to HOLD (the bug-hunt's soft-deadlock fix) — `eg`-gated, so riverless baselines are identical.
>
> **DECISIONS (decide-&-log, no fork surfaced):** Bull Run's creek/Stone Bridge stay COSMETIC (making them live would regress a shipped, probed battle + break byte-identity); the active river is the opt-in feature, and the teaching card cites the Rappahannock as the historical anchor. AI river-pathing / AI-built works is the named T13 extension point (player-tool this increment, like D87/D88), so a river skirmish frames the PLAYER as the attacker forcing the crossing (river installed between the player's home edge and the objective, either side) while the AI defends the far bank.
>
> **Bug-hunt (19 agents, Opus): 8 confirmed + 6 critic, all fixed.** HIGH: the gate's origin test let a unit on an open ford **sidestep into deep water and wade the band** → block entering blocked water from anywhere except already-blocked water + a midpoint sample (tangential bends). HIGH: the **3D water plane was buried under the opaque terrain** → raised to a thin on-ground sheet (+ fords lifted, no z-fight). HIGH: a unit ordered across impassable water **marched in place forever, fatigue→100** → settle to HOLD at the bank. HIGH: the river skirmish was degenerate for a CS player → river placed by player side. MED: silent crossing refusal → aria-live halt announce + HUD line. LOW: dupe-span message; pontoon `side` + per-side cap.
>
> **Verification:** build GATE OK; `probe-engineering-corps` **22/22**, 0 pe; full no-regression **GREEN (21 probes byte-identical: field 13 · bullrun 15 · shiloh 29 · malvern 24 · antietam 16 · fredericksburg 22 · chancellorsville 24 · gettysburg 16 · vicksburg 18 · roster 8 · visuals ok [texWarn 0] · custom 9 · fog 13 · ai 15 · autopause 13 · campaign-link 16 · officers 15 · logistics 14 · arms 23 · presets 26 · csplayer 16; diag-classic `nonBlank:346`; boot/t1 ok; 0 pageerrors)**; WCAG-AA on all new UI (≥4.5:1, CVD-safe). Also added the long-missing B/X/N engineering keys to the help overlay.
>
> **Next (the Engineering Corps is complete):** Phase C tactical BREADTH — **Chickamauga / Chattanooga** (the Western theater continues after Shiloh + Vicksburg), then **C3 USCT battles** (the Crater · New Market Heights · Olustee · Nashville). Then D hex engine → E strategic S3–S5. **Resume map:** START-HERE.md → this block → WAKE-UP.md top block → newest RUN-LOG.md entry → DECISIONS.md D89 → `src/tactical/T13-engineering.js` (the INCREMENT 3 section: rivers + pontoons) + the T0 seams (grep `fldEng`/`rivers`/`engPontoons`) → `tools/probe-engineering-corps.mjs`.

---

## ⚡ CONTINUE HERE — 2026-06-21 **ENGINEERING CORPS — increment 2: ABATIS / OBSTACLES shipped + hardened** (the axe; player-only/byte-identical; probe 15/15) — D88

> **The Engineering Corps is 2 of 3 increments done.** A selected player infantry brigade now builds an **abatis** — a capped timber belt across its front (key **B** / "Abatis" button); enemies forcing it are slowed, disordered once per crossing (morale + fatigue), and fire raggedly until they recover; a nearby brigade **clears** a lane (key **X**). Build/clear time, slow, and disorder all read the **B-5 realism slider** (`fldEngRealism()`). 2D stake-line + 3D crossed-timber (fog-gated), HUD lines, and a Mahan-sourced teaching end-card. **Player-only → every AI-vs-AI baseline is byte-identical.**
>
> **Provenance:** the abatis code was authored 2026-06-20 but left uncommitted/unvetted when the prior **Codex** session ran out of usage (its only landed work was the browser-probe Seatbelt guard — committed separately as `679ee7d`). This session vetted it through the full gate + bug-hunt and pushed it (`02b2dd8`).
>
> **Architecture:** the abatis half of `src/tactical/T13-engineering.js` + guarded T0 seams (`fldMoveFactor` gains `u`, `fldResolveFire` fire factor, `fldStepMovement` move-gate, `fldInitSim` reset, `fldOnOver` end-card, DOM buttons + keys B/X) + a **T8 `_fldBuildPhase` per-phase reset** (`fldEngPhaseReset`). Every seam is an exact identity with no live belt.
>
> **Bug-hunt (16 agents, Opus): 8 confirmed + 4 critic, all fixed.** HIGH: belts leaked across phases in multi-phase battles (Vicksburg/Antietam/Gettysburg) → `fldEngPhaseReset` (keeps `_engUsed` for the end-card). MED: abandoned belts never decayed → fade like abandoned entrenchments. MED: render ignored fog → fog-gated 2D+3D. LOW: same-tick id collision → monotonic `_engObsSeq`. LOW: routing-unit morale dock → routing guard. **Kept (deliberate):** entrench+abatis on one brigade (historically correct — Fredericksburg/Cold Harbor apron) and one disorder shock per crossing.
>
> **Verification:** build GATE OK; `probe-engineering-corps` **15/15** (added: multi-phase reset, abandoned decay, unique ids, routing immunity); full no-regression **GREEN (50 probes, 0 pageerrors, diag-classic `nonBlank:346`)**. Also fixed a D86 leftover: `probe-custom-battle-builder` was red since Vicksburg shipped (EXPECTED registry omitted `vicksburg`) → 9/9.
>
> **Next (the LAST engineering increment):** **PONTOON bridging** — a NEW river/water terrain feature + a bridge-laying engineer step + a crossing gate (fordable vs requires-pontoon, realism-gated); the Bull Run terrain already has Young's Branch / the Stone Bridge to build against. Then C tactical breadth (Chickamauga/Chattanooga, C3 USCT). **Resume map:** START-HERE.md → this block → WAKE-UP.md top block → newest RUN-LOG.md entry → DECISIONS.md D88 → `src/tactical/T13-engineering.js` (header + the abatis fns) + the T0/T8 seams (grep `T13`/`fldEng`) → `tools/probe-engineering-corps.mjs`.

---

## ⚡ CONTINUE HERE — 2026-06-20 **ENGINEERING CORPS — increment 1: FIELD ENTRENCHMENTS shipped** (the spade; byte-identical; probe 8/8) — D87

> **The Tactical Engineering Corps has begun.** Aaron locked the full corps ("all of the above" — entrench + abatis + pontoons), so it ships as one module `src/tactical/T13-engineering.js` in vetted increments. Increment 1 = FIELD ENTRENCHMENTS: a player brigade ordered to Entrench (key **E** + control-bar button + help copy) digs in over ~70 sim-seconds for earned, facing-aware parapet cover; marching off abandons the works. Reads the B-5 realism slider (Arcade fast/×1.43, Balanced ×1.62, Historian slow/×1.81). 2D dirt parapet + 3D dirt berm (low-tier aware, reduceMotion-safe).
>
> **Architecture:** 9 guarded byte-identical T0 seams (fldResolveFire/fldResolveMelee cover, fldSimStep step, fldKey E, control-bar button, fldRenderHud line, fld2dDraw + 3D build/sync hooks, fld3dDispose ref-nulling) + the T13 module + `tools/probe-engineering-corps.mjs` (8/8). Entrenchment is **player-only** this increment (no scenario/AI digs), and `fldEngCover` returns EXACTLY 1 for any non-entrenched unit — so every AI-vs-AI baseline is byte-for-byte unchanged.
>
> **Bug-hunt (17 agents):** 1 confirmed — cover was omnidirectional; fixed facing-aware (front full / flank half / rear none) so flanking counters works. Critics triaged: melee charger-exposed is the correct base convention; the multi-phase 3D works self-heal via fld3dSyncEng stale-disposal; the fog-ghost-works case is deferred to the AI/scenario-entrench increment (noted in the T13 header).
>
> **Verification:** build GATE OK; probe-engineering-corps 8/8; full no-regression suite byte-identical (field/bullrun/shiloh/fog/ai/autopause + all battle probes + officers/logistics/arms/presets/csplayer/campaign-link); tactical-visuals 8/8 (textureWarnings:0); diag-classic nonBlank 346; boot/t1 ok; 0 pageerrors.
>
> **Next (the rest of "all of the above"):** ABATIS / obstacles (a fldEngMoveFactor slow seam + cross-belt disorder step + engineer placement), then PONTOON bridging (a NEW river/water terrain feature + bridge-laying + crossing gate) — each its own vetted increment extending T13. The T13 module header lists the EXTENSION POINTS. **Resume map:** START-HERE.md → this block → WAKE-UP.md top block → newest RUN-LOG.md entry → DECISIONS.md D87 → src/tactical/T13-engineering.js (header) + the T0 seams (grep "T13"/"fldEng") → tools/probe-engineering-corps.mjs.

---

## ⚡ CONTINUE HERE — 2026-06-20 **VICKSBURG: the 3-phase river-fortress SIEGE shipped + adversarially hardened** (the last Codex project, finished) — D86

> **Vicksburg is the first SIEGE in the game and the last open Codex project, now finished and vetted.** Built as a three-phase battle on the T8 multi-phase engine — **Stockade Redan (May 19) → Forlorn Hope / Great Redoubt (May 22) → the Saps and the Mine vs the 3rd Louisiana Redan (June 25)**. New `data/vicksburg.json` + registry line in `src/tactical/T1-bull-run.js` (menu rank 55) + `tools/probe-vicksburg.mjs` (18 steps) + the new `tools/sweep-vicksburg.mjs` balance harness. Universal gun-count model only; no per-battle damage fudge. Anti-Lost-Cause: dug out, not starved out.
>
> **Finish work over the Codex draft:** (1) the readability probe's WebGL screenshot was switched from the hanging `page.screenshot()` to the proven render→`canvas.toDataURL()` capture; (2) `tools/probe-tactical-roster.mjs` was updated to guard `vicksburg` (the Codex left it out, so the menu-order step had been failing).
>
> **Adversarial bug-hunt (25 agents) confirmed + fixed:** the **Hebert→M.L. Smith** historical misattribution at the Stockade Redan (Hebert relocated to his real 3rd-Louisiana-Redan sector); the **inverted May-assault casualties** (the dug-in defender was bleeding more than the exposed attacker — rebalanced toward fire parity + the fortress's historical gun edge so the assaults now cost the attacker US:CS 1.93 / 2.64, the redans still hold 8/8, the siege wins 8/8, the city falls 8/8 with a clear US 3 / CS 2 margin); a new **casualty-direction probe gate** so it can't re-invert; the **T12 GPU leak** (per-sync materials/geometries now disposed via a `__M3D._t12Disp` registry), a **low-tier halo gate**, and a **WCAG dark-pill** behind the strength count.
>
> **Verification:** build GATE OK; `probe-vicksburg` 18/18, 0 pe (assaultCostUStoCS 1.96 / 2.71, totalGuns 56); `probe-tactical-roster` 8/8; bullrun 15/15; malvern-hill 24/24; antietam 16/16; fredericksburg 22/22; chancellorsville 24/24; gettysburg 16/16; shiloh 29/29; field 13/13; officers 15/15; logistics 14/14; arms 23/23; presets 26/26; csplayer 16/16; campaign-link 16/16; `probe-tactical-visuals` 8/8, 0 pe, `textureWarnings:0`; bootprobe ok (10 tolerated 404s); t1probe ok; diag-classic `nonBlank:346`; `git diff --check` clean. Sim engine T0–T8 untouched.
>
> **Known limitation (LOW, deferred):** passive (player-side, non-`autoBoth`) headless resolution is side-asymmetric — a demo-only artifact (real play has a human commanding). Noted for a future passive-AI pass.
>
> **Next Codex-owned task (non-battle):** the **Tactical Engineering Corps** on the field (pontoons / entrenchments / abatis / obstacle-clearing) interacting with the B-5 difficulty/realism sliders — Aaron's run-k directive (V1-CHECKLIST Phase F + B-5 pointer). Other open items: C3 USCT battles, S3 alt-history, S4 codex/glossary, the comprehensive WCAG pass, the full-campaign playthrough probe.
>
> **Resume map:** `START-HERE.md` → this block → `WAKE-UP.md` top block → newest `RUN-LOG.md` entry → `DECISIONS.md` D86 → `V1-CHECKLIST.md` Phase C/F → `data/vicksburg.json`, `src/tactical/T8-phases.js`, `src/tactical/T1-bull-run.js`, `src/tactical/T12-3d-readability.js`, `tools/probe-vicksburg.mjs`, `tools/sweep-vicksburg.mjs`.

---

## CONTINUE HERE — 2026-06-19 leftover gate closed + 3D objective beacon complete — C4/Malvern/Chancellorsville/custom-builder vetted, visual probe 8/8

> **Critical first job is complete.** Codex inspected the dirty working tree before starting new work. The uncommitted D77-D84-era C4/Malvern/Chancellorsville/custom-builder/roster/visual/help/docs state matched prior live docs, so it was treated as lower-model/session leftovers and brought to a vetted state rather than reverted. No unrelated user/project changes were reset.
>
> **One issue was found and fixed:** `tools/probe-gettysburg.mjs` had a probe-output bug where `sandboxPhases` was read after launching Antietam, so the printed summary misleadingly showed Antietam phase data for a single-objective Gettysburg assertion. The probe now captures Gettysburg's single-objective phase state before the Antietam control launch and reports `antietamPhases` separately. No gameplay behavior changed.
>
> **New Codex-owned graphics/readability pass:** `src/tactical/T0-field-sandbox.js` now builds a small procedural 3D `objectiveBeacon` at the scenario objective in real-time 3D. It uses Three.js primitives/materials only; no external assets, textures, browsing, downloads, accounts, or Blender. It does not change simulation, balance, AI, input, 2D rules, or Classic.
>
> **Visual probe expanded:** `tools/probe-tactical-visuals.mjs` now captures Shiloh, Malvern Hill, Gettysburg, and Chancellorsville in 2D and 3D (8 captures). Every 3D scene asserts the `objectiveBeacon` exists at the objective coordinates, while retaining the blank-canvas/pageerror/texture-warning gates. Current result: **8/8 ok**, 0 pageerrors, `textureWarnings:0`.
>
> **Verification:** `node tools/build.mjs` → GATE OK; `node tools/probe-custom-battle-builder.mjs` → 9/9; `node tools/probe-tactical-roster.mjs` → 8/8; `node tools/probe-chancellorsville.mjs` → 24/24; `node tools/probe-malvern-hill.mjs` → 24/24; `node tools/probe-shiloh.mjs` → 29/29; `node tools/probe-gettysburg.mjs` → 16/16; `node tools/probe-antietam.mjs` → 16/16; `node tools/probe-fredericksburg.mjs` → 22/22; `node tools/probe-bullrun.mjs` → 15/15; `node tools/probe-field.mjs` → 13/13; officers/logistics/arms/presets/csplayer/campaign-link all green; `node tools/probe-tactical-visuals.mjs` → 8/8; broad legacy strategy/tactical sweep (`ai`, `autopause`, `fog`, `auto-resolve`, `armory-field`, `blockade`, `bridge`, `cab-exploit`, `cabinet`, `cannon`, `command`, `conditioning`, `cover`, `decisions`, `desk`, `economy`, `engineering`, `manpower`, `morale`, `motion`, `portraits`, `press`, `production`, `targeting`, `victory`, `weapons`) all green; `node tools/bootprobe.mjs` → ok with known tolerated resource 404 console noise; `node tools/t1probe.mjs` → ok; `node tools/diag-classic.mjs` → Classic paints, `nonBlank:346`; `git diff --check` → clean; `node tools/build.mjs --check` → GATE OK.
>
> **Known residuals:** benign Chromium readback/performance warnings may appear in screenshot paths, and pre-existing resource/favIcon 404 console messages remain tolerated in boot/Classic artifacts. Blender is closed and should stay closed unless an explicit future Phase-H asset job truly requires it.
>
> **Resume map:** `START-HERE.md` → this block → `WAKE-UP.md` top block → newest `RUN-LOG.md` entry → `DECISIONS.md` D85-D82 → `V1-CHECKLIST.md` Phase C/H notes → `src/tactical/T0-field-sandbox.js`, `src/tactical/T1-bull-run.js`, `src/tactical/T11-custom-battle.js`, `tools/probe-tactical-visuals.mjs`, and the focused battle/custom probes. **Next Codex-owned task:** Phase C Western breadth, especially Vicksburg/siege architecture and probes. Secondary graphics follow-up: improve 3D unit label/selection readability now that objective location is clear.

---

## ⚡ CONTINUE HERE — 2026-06-18 Custom Battle Builder C4 complete ✅ — single-phase authoring, import/export, slots, launch gate green

> **Custom Battle Builder C4 is built and verified.** New `src/tactical/T11-custom-battle.js` adds a first-class main-menu builder flow for player-authored real-time tactical scenarios. It covers scenario name/date/place, sides/briefs, objective, field size, terrain hills/woods/walls/markers, OOB units, artillery/gun counts, reinforcement/deployment timing, optional leaders, supply trains, teaching cards, provenance, JSON import/export, shareable JSON, and six local persistence slots.
>
> **Architecture decision:** C4 is intentionally **single-phase V1**. Phase authoring remains deferred until a UI for `phases[]` can be proven as safe as authored historical phase data. Custom scenarios do **not** enter `fldScenarioRegistry()`; `fldScenarioData(id)` only falls back to `fldCustomScenarioData(id)` for explicit `custom_...` IDs. That preserves the exact historical battle roster while still launching valid custom scenarios through the same single-phase `fldScenarioInit` data path. Field size is reset every launch via `FLD.BASE_FIELD_W/H`, then a scenario may apply `field.w/h` safely.
>
> **Validation guardrails:** required metadata, safe unique IDs, malformed/unsafe JSON keys, duplicate IDs, bounds and numeric clamps, objective placement, reinforcement timing, side ownership, leader attach ownership, realistic artillery crew counts (12-40 per gun), forbidden combat/damage keys, and `phases[]` are all blocked. Custom artillery uses the universal `guns` model only; no per-custom-battle damage fudge was added.
>
> **Focused probe:** `tools/probe-custom-battle-builder.mjs` verifies builder render, validation failures, valid scenario creation, export/import round trip, UI malformed-import rejection, slot save/list/load/delete, explicit custom-ID launch contract, universal gun model, field-size reset, no phase leak, no Classic `G.battle`, and no historical registry contamination. Current result: **9/9 steps ok, 0 fail**, with `tools/shots/probe-custom-battle-builder.json/png`.
>
> **Verification:** `node tools/build.mjs` → GATE OK; `node tools/probe-custom-battle-builder.mjs` → 9/9; `node tools/probe-tactical-roster.mjs` → 8/8; `node tools/probe-field.mjs` → 13/13; `node tools/probe-bullrun.mjs` → 15/15; `node tools/probe-malvern-hill.mjs` → 24/24; `node tools/probe-antietam.mjs` → 16/16; `node tools/probe-fredericksburg.mjs` → 22/22; `node tools/probe-chancellorsville.mjs` → 24/24; `node tools/probe-gettysburg.mjs` → 16/16; `node tools/probe-shiloh.mjs` → 29/29; `node tools/probe-officers.mjs` → 15/15; `node tools/probe-logistics.mjs` → 14/14; `node tools/probe-arms.mjs` → 23/23; `node tools/probe-presets.mjs` → 26/26; `node tools/probe-csplayer.mjs` → 16/16; `node tools/probe-campaign-link.mjs` → 16/16; `node tools/probe-tactical-visuals.mjs` → 6/6; `node tools/bootprobe.mjs` → ok=true with known/tolerated 404 resource console entries; `node tools/t1probe.mjs` → ok=true; `node tools/diag-classic.mjs` → Classic paints, nonBlank 346; `git diff --check` → clean.
>
> **Docs/checklist status:** `RUN-LOG.md`, `HANDOFF.md`, `WAKE-UP.md`, `MASTER-TASK-LIST.md`, `V1-CHECKLIST.md`, and `DECISIONS.md` now mark C4 complete. Eastern marquee C1 remains complete; C2 Western still has Vicksburg, Chickamauga/Chattanooga, Atlanta/March, Franklin/Nashville; C3 USCT battles remain open.
>
> **Priority picker/model routing remains saved:** `START-HERE.md` and `MASTER-TASK-LIST.md` include the Codex-vs-DeepSeek/Cline priority list. Codex owns architecture, balance, probes, regression-heavy integration, WCAG judgment, and cross-module work. DeepSeek/Cline only receive Codex-packeted bounded tasks. Loot status remains explicit: Armory/Cannon have loot-style rarity tiers, but standalone Phase I loot/survival is not built.
>
> **Resume map:** `START-HERE.md` → this top block → `WAKE-UP.md` top block → newest `RUN-LOG.md` entry → `V1-CHECKLIST.md` Phase C → `DECISIONS.md` D84-D82 → `src/tactical/T11-custom-battle.js`, `src/tactical/T0-field-sandbox.js`, `src/tactical/T1-bull-run.js`, `tools/probe-custom-battle-builder.mjs`, `tools/probe-tactical-roster.mjs`, and the scenario probes. **Next Codex-owned task:** continue Phase C breadth with Western theater/Vicksburg architecture, C3 USCT scenario design, or another priority-picker item.

---

## ARCHIVED — 2026-06-18 Chancellorsville completion gate ✅ — superseded by the Malvern Hill block above

> **Tactical cohesion mini-pass complete.** The stale first-launch/help/tactical controls copy now matches the live real-time engine: current battle roster (First Bull Run, Antietam, Fredericksburg, Chancellorsville, Gettysburg, Shiloh, Skirmish), click/Shift-click/A selection, drag-to-place/facing, L/C/H/F/Enter orders, 1/2/3 = 1x/2x/4x, V/P/G/Tab/Esc. The in-battle tactical help dialog is now aria-modal, focusable, Tab-trapped to its close control, and Escape closes the dialog without also exiting the battle.
>
> **HUD declutter shipped:** the giant pause overlay is now a small top-right status chip; the tactical top bar is split into compact title/clock/phase/objective/status chips; multi-phase sector state moved into a dedicated `#fldSector` chip; objective text no longer repeats the huge phase label; reinforcement banners are narrower, shorter-lived, and ellipsized with a tooltip.
>
> **New visual probe + warning gate:** `tools/probe-tactical-visuals.mjs` launches the live `__FIELD` engine and captures Shiloh + Gettysburg in **2D and real-time 3D**: `tools/shots/visual-shiloh-2d.png`, `visual-shiloh-3d.png`, `visual-gettysburg-2d.png`, `visual-gettysburg-3d.png`, plus `tools/shots/probe-tactical-visuals.json`. Pixel probes confirm all four scenes render nonblank/readable with 0 pageerrors. The probe now FAILS if the known Three.js warning `THREE.WebGLRenderer: Texture marked for update but image is undefined` appears.
>
> **3D flag texture fix:** `src/tactical/T10-flags.js` replaced the immediate SVG `TextureLoader().load(svg)` + `tex.needsUpdate` flow with procedural cached canvas-backed `CanvasTexture` maps. Replaced flag mesh texture maps are disposed, and wrapper hooks dispose flag maps before unit rebuild and scene teardown. The generated `civil_war_generals.html` T10 block was aligned so browser probes exercise the fix. No gameplay simulation code changed.
>
> **Roster integration probe:** Codex added `tools/probe-tactical-roster.mjs` and made `fldScenarioMenuOrder()` explicit: First Bull Run → Antietam → Fredericksburg → Chancellorsville → Gettysburg → Shiloh, then future scenarios by full date. The probe verifies exact registry ids, data/menu/side-card contracts, single-vs-multi-phase shapes, gun-count artillery for post-Bull-Run battles, DOM button order/idempotency, both side-choice callbacks, and renderer-none launches for all six historical battles. No gameplay simulation code changed.
>
> **Chancellorsville finished after the interrupted run:** `data/chancellorsville.json` is registered in `src/tactical/T1-bull-run.js` and guarded by `tools/probe-chancellorsville.mjs`. The battle is a single-phase Jackson flank-march scenario with CS attacker / US defender, Chancellor crossroads objective, Wilderness/Hazel Grove/Fairview/breastworks terrain, gun-model artillery, leaders/fates, reinforcements, side cards, teaching/codex, deterministic AI-vs-AI resolution, CS-favored-but-not-scripted balance, and no Classic contamination.
>
> **Verification:** `node tools/build.mjs` → GATE OK; `node tools/probe-tactical-roster.mjs` → 8/8, 0 pageerrors; `node tools/probe-chancellorsville.mjs` → 24/24, 0 fail; `node tools/probe-bullrun.mjs` → 15/15, 0 pageerrors; `node tools/probe-shiloh.mjs` → 29/29, 0 pageerrors; `node tools/probe-gettysburg.mjs` → 16/16, 0 pageerrors; `node tools/probe-tactical-visuals.mjs` → 4/4 captures ok with clean per-scene pageerrors/console arrays; `node tools/diag-classic.mjs` → Classic paints, nonBlank 346. Current repo history already includes Gettysburg, Shiloh, T10 flags, T9 audio, portraits, and save/help weekend polish; docs below this block may still contain older milestone prose. Known residual console noise: benign Chromium `ReadPixels` warnings from the roster screenshot path and the pre-existing favicon 404 in roster/diag-classic.
>
> **Blender status:** Blender is closed and should stay closed for current code/browser/Three.js work. Reopen only for an explicit future Phase-H asset job. No current probe, flag fix, or tactical implementation depends on Blender.
>
> **Universal AI implementer standards:** any VS Code agent implementing this project (Codex, Claude, Cline, DeepSeek, or a lesser model) must follow `START-HERE.md`'s Universal AI implementer standards and `AUTONOMOUS-RUN.md` §1A. Mandatory loop: repo-ground first, make small reversible edits, self-audit the diff and outputs, run the proper gates, learn from failures by updating repo-visible docs/probes, and report files/commands/results honestly. Cline/DeepSeek/lower models may only run exact bounded packets and must stop on ambiguity or any red gate.
>
> **Archived resume map:** this Chancellorsville-era block is preserved for history. It is superseded by the Malvern Hill completion block above and DECISIONS D82.
>
> **Archived prompt:** completed by the Malvern Hill/D82 pass above; do not reuse it as the current next task.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE C-2 · ANTIETAM — the FIRST MULTI-PHASE EPIC ✅ SHIPPED + PUSHED (bug-hunt to finish)** → NEXT = finish the Antietam bug-hunt, then DEMO-POLISH / graphics (Aaron's weekend demo)

> **Antietam — the bloodiest day, as a 3-phase epic — is built, vetted (except the bug-hunt), and pushed** (DECISIONS **D76**). A NEW gated engine capability (sequential phases in one scenario) + the citation-grade 3-phase scenario: **the Cornfield → the Sunken Road "Bloody Lane" → Burnside's Bridge**. New `src/tactical/T8-phases.js` + surgical T0/T1 seams + `data/antietam.json` + a registry line + `tools/probe-antietam.mjs`. Deliverable ≈**1814.7KB**.
>
> **✅ THE MULTI-PHASE ENGINE (T8, gated on `__FIELD.phases`):** a `data.phases[]` scenario routes (T1) to `_fldScenarioInitPhased`/`_fldBuildPhase`; the `fldCheckVictory` intercept (`_fldPhaseResolved`) records each phase + carries casualties/score forward + advances (headless auto-advances so `fldStepN` keeps stepping; UI shows a period-broadsheet inter-phase card); the last phase ends with the aggregate winner (`_fldBattleWinner`, weighted score + draw band). A per-phase `scenData` VIEW makes the officers (T3) + logistics (T4) layers build the phase cast/trains with ZERO change to those modules. **Byte-identical: every seam is `if (__FIELD.phases)` (null for non-phased)** — the full no-regression suite is byte-identical fresh (field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · presets 26 · csplayer 16 · arms 23 · fredericksburg 22 · boot ok · t1 14 · diag-classic 346).
>
> **✅ BALANCE (deterministic, 8 seeds):** historically faithful per-phase outcomes — Cornfield CS-held 8/8, Sunken Road US-carried 8/8, Burnside's Bridge CS-held 8/8 → aggregate **CS 2-1**; the end-screen teaches the tactical draw / strategic Union check / **the Emancipation Proclamation**. A human can rewrite any sector. probe-antietam **16/16, 0 pe**; wcag clean (4 fixes).
>
> **⚠ THE ONE OPEN GATE:** the **adversarial bug-hunt (7 lenses) was LAUNCHED but did not finish before commit** (committed under a time constraint to preserve the playable milestone). **FIRST NEXT-SESSION TASK: re-run the Antietam bug-hunt** (`.tmp/bughunt-antietam.json` if it landed, else re-launch the 7-lens workflow on T8 + the seams + `data/antietam.json`), fix any confirmed finding, re-probe (`probe-antietam` + the no-regression suite), and amend the commit / push a follow-up. Part A's identical hunt found 0 real bugs, so risk is low, but the gate is not formally closed.
>
> **What's playable:** a new main-menu **⚔ BATTLE — ANTIETAM (1862)** button opens a side-choice card, then plays the bloodiest day as three sequential battles — fight the Cornfield, then (after a period transition card showing the running tally + the next sector's history) the Sunken Road, then Burnside's Bridge; the cost carries forward and the end-screen scores the day phase-by-phase with the emancipation turning point. Command either army across all three. (Bull Run, Fredericksburg, Skirmish, Command & Realism unchanged.)
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` Phase C → `DECISIONS.md` **D76** (Antietam multi-phase) + **D75** (the gun model) + **D74** (the Phase-C clearing decisions) + **D73** (the data-driven scenario template) → the multi-phase engine in `src/tactical/T8-phases.js` (`_fldScenarioInitPhased`/`_fldBuildPhase`/`_fldPhaseResolved`/`_fldBattleWinner`/`_fldInterphaseCard`/`_fldPhasesEndHtml`) + the T0 seams (grep `__FIELD.phases`) + the T1 route/registry + `data/antietam.json` (the 3-phase template) + `tools/probe-antietam.mjs` (16/16) → the research dossier `.tmp/antietam-dossier.md` + the design `.tmp/B-antietam-multiphase-design.md`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE C-1.5 · THE UNIVERSAL ARTILLERY GUN MODEL ✅ SHIPPED + VETTED + PUSHED** → IMMEDIATE NEXT = ANTIETAM as the MULTI-PHASE epic (D74 #1/#2)

> **The D74 "universal-combat" artillery cleanup — the FIRST Antietam sub-milestone (do FIRST) — is complete** (DECISIONS **D75**). Fredericksburg's per-battle **"volume-of-fire proxy men"** fudge (the C-1/D73 batteries inflated `men` to 500/440/500 to fake the massed crest fire) is REPLACED by a CONSISTENT gun-count model used identically in every battle. Deliverable ≈**1749.6KB**.
>
> **✅ THE GUN MODEL (T5):** `FLDA.GUN_FIRE_WEIGHT` = 27 (men-equivalent fire-strength per gun, UNIVERSAL). `fldArtFireStr(u)` returns the fire-strength numerator (`fldResolveFire` divides by 1500): a battery with a GUN COUNT → `guns * 27 * clamp(men/maxMen)` (rated fire from the gun count, crew-scaled so a battery fades smoothly as cannoneers fall); infantry / arms-off / a LEGACY no-`guns` battery → `u.men` EXACTLY as before. `guns` carried data→unit via `fldMakeUnit` (T0) + `fldBrSpec` (T1); wired at `fldResolveFire`'s `strF`. CALIBRATED so a 6-gun battery → 162 ≈ Bull Run's realistic 160-men/6-gun Griffin & Ricketts → the model REPRODUCES the existing batteries (consistent, not merely gated). The gun count is army-size-invariant "hardware" (strategic strength flows through the Cannon-Corps `_canisterScale`, not raw volume — documented).
>
> **✅ BYTE-IDENTITY:** the gun path is reached ONLY by a battery declaring `guns` (Fredericksburg's 3 crest batteries); every baseline has none → `fldArtFireStr` returns `u.men` → byte-identical. **field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · presets 26 · csplayer 16 · arms 23** — all byte-identical, 0 pe; bootprobe ok; t1probe 14; diag-classic **346**.
>
> **✅ FREDERICKSBURG RE-TUNED VIA HONEST DESIGN (not fudge):** the 3 batteries now carry real gun counts (Washington Artillery 9 · Marye's grand battery 16 · Alexander 9 = **34 historical guns**) + realistic crew (200/350/200). The CS edge EMERGES from the wall's cover + the massed gun count + the cautious-attacker-dies-exposed geometry. RE-VET: **CS 8/8**, casualty ratio **2.06:1** (better than the proxy's 1.91), the wall measurably **load-bearing** (cuts CS loss 18-23%, 5/5 CS wins), the crest guns **survive 8/8** + fire — **probe-fredericksburg 22/22, 0 pe**. Bug-hunt: **0 confirmed finder bugs + 5 critic gaps ALL FIXED** (the `u.men0` probe typo → maxMen; a battery-survival guard; a crew-scaling assertion; the campaign-semantics + legacy-HUD notes). WCAG clean.
>
> **IMMEDIATE NEXT = ANTIETAM (Sep 17, 1862) as the MULTI-PHASE epic (D74 #1/#2).** Build the NEW gated `phases[]` engine capability (sequential phase objectives; default single-objective path = Fredericksburg/Bull Run byte-identical) → use it for the 3 phases: **the Cornfield → the Sunken Road "Bloody Lane" → Burnside's Bridge**. The citation-grade research **dossier is drafted** (`.tmp/antietam-dossier.md`, 8-agent workflow, anti-Lost-Cause, corrections applied) and the **engine design is drafted** (`.tmp/B-antietam-multiphase-design.md`: discrete sequential sub-battles, casualty+score carry-over, weighted-aggregate scoring with a draw band, the inter-phase broadsheet transition card). Author `data/antietam.json` (gun-model artillery) + one `fldScenarioRegistry` line + `tools/probe-antietam.mjs`. Standalone menu battle (D74 #4). **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` Phase C → `DECISIONS.md` **D75** (the gun model) + **D74** (the Phase-C clearing decisions) + **D73** (C-1 Fredericksburg, the data-driven template) → the gun model in `src/tactical/T5-arms.js` (`FLDA.GUN_FIRE_WEIGHT`/`fldArtFireStr`) + the `fldResolveFire` seam (T0) + the `guns` plumbing (`fldMakeUnit`/`fldBrSpec`) + `data/fredericksburg.json` (the de-fudged batteries) + `tools/probe-fredericksburg.mjs` (22/22) → the Part-B drafts in `.tmp/`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE C-1 · FREDERICKSBURG (Marye's Heights) ✅ SHIPPED + VETTED + PUSHED** → NEXT = the next Eastern marquee battle (Antietam or Gettysburg, D69)

> **C-1 — Fredericksburg, the SECOND data-driven tactical battle — is complete** (DECISIONS **D73**). The tactical engine (mature after B1–B6) is now BATTLE-AGNOSTIC: a new historical battle = a `data/<battle>.json` file + one registry line, NO new combat code. New `data/fredericksburg.json` + `tools/probe-fredericksburg.mjs` + the T1/T7 data-driven scenario-UI generalization + a small gated T0 engine seam. Deliverable ≈**1743.6KB**.
>
> **✅ C-1 — the data-driven scenario spine + Fredericksburg:** `fldScenarioRegistry()`/`fldScenarioData(id)` (T1) map any id → `GAME_DATA.<battle>`; `fldScenarioInit` builds ANY registered id from the same shape (Bull Run byte-identical). The UI is generalized: `fldInjectScenarioButtons` iterates the registry (Bull Run keeps its bespoke marquee button; others build from `data.menu`) and returns the last button so the skirmish/preset buttons anchor after the block; `fldLaunchBattle` fires the briefing for ANY scenario; `fldScenarioSideChoice(id,go)` reads `sd.sides` (a per-battle side-choice card; `fldBullRunSideChoice` is now an alias); `fldBriefObjectiveHtml` keeps the keyword skeleton + a data flavor (`sd.brief`); `fldScenarioEndHtml` keeps the Bull Run branch byte-identical (shared `_fldScenarioEndBox`) + a data `sd.endNote` branch. `bullrun.json` gained inert `sides`/`brief`.
>
> **✅ THE DOOMED-FRONTAL-ASSAULT POSTURE (the headline mechanic — a scenario-gated T0 seam):** the B-1 attacker AI is *competent* (fog-off it column-rushes + presses the mass bayonet), which Fredericksburg's assault was NOT. `data.assaultDoctrine:"cautious"` → `__FIELD._atkCautious` makes the AI attacker advance IN LINE across the open glacis and trade but never column-rush nor carry the wall (Burnside's piecemeal disaster). `var aggressive = !__FIELD.fog && !__FIELD._atkCautious` gates the assault/column branches → **default off → Bull Run/sandbox/skirmish/campaign BYTE-IDENTICAL.** A HUMAN Union player is unbound (the alt-history hook). Fredericksburg keeps **fog OFF** (the morning fog had LIFTED — clear fields of fire) and wins the defense by the cautious posture + the stone wall (cover 1.7) + massed crest canister — a DIFFERENT, battle-faithful lever from Bull Run's fog (no contradiction of "fog aids the defender").
>
> **VETTING CAUGHT A REAL HIGH (fixed pre-commit):** the bug-hunt (5 diverse-lens Opus finders × default-refute verify + a completeness critic → 4 confirmed + 5 critic gaps) empirically traced that `fldAiDefender` holds the objective's FORWARD FACE (`obj.z+obj.r*0.55`) — my first geometry put that forward of the wall, so the AI marched the defending line OFF the wall into the open on tick 1 (the 1.7 cover unused; the static-terrain probe missed it). **Fixed (the Bull Run pattern):** `obj.z` 375→331 (the forward face now lands ON the wall in cover) + `ATK_CAUTIOUS_HOLD` 0.6→0.85 (the cautious attacker holds in the OPEN just outside the wall's cover band — only the attacker exposed). Added a LIVE cover assertion to the probe. Also fixed: the side-card aria role hardwired US=attack → threaded the data `attacking` flag.
>
> **✅ FULLY VETTED:** GATE OK (1743.6KB) → **probe-fredericksburg 20/20, 0 pe** → full no-regression GREEN (field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · arms 23 · presets 26 — all BYTE-IDENTICAL; **csplayer 16/16**; bootprobe ok; t1probe 14/14; diag-classic nonBlank **346**; strategic spot-check green) → balance A/B (**CS 8/8**; the slaughter **≈1.91:1** Union casualties; the wall load-bearing; the defending line holds the wall in cover live while the attacker dies exposed on the open glacis) → bug-hunt fixed + re-verified → wcag clean (0 fixes; 1 pre-existing global `.title-sub`/`--rule` contrast issue noted for the Phase-E WCAG pass). Committed + pushed.
>
> **What's playable:** a new main-menu **⚔ BATTLE — FREDERICKSBURG (1862)** button opens a side-choice card — lead the Union assault across the open glacis (and try to do what the Army of the Potomac could not), or hold the stone wall for the Confederacy as charge after charge breaks before it. The AI assault is the doomed frontal attack; the wall + the pre-sighted crest guns repel it. (The ⚔ FIRST BULL RUN button + the ⚔ SKIRMISH + ⚙ COMMAND & REALISM menus are unchanged.)
>
> **IMMEDIATE NEXT = ANTIETAM (Sep 17, 1862) as a MULTI-PHASE epic (DECISIONS D74).** Four Aaron-locked decisions clear the next runs: **(1)** next battle = **Antietam** (chronological; the Cornfield → the Sunken Road "Bloody Lane" → Burnside's Bridge); **(2)** scope = **multi-phase epics** — build a NEW gated engine capability (sequential phases/objectives in one scenario, a `phases[]` data block; default = the single-objective path → Fredericksburg/Bull Run byte-identical) and use it for Antietam's three phases; **(3)** the **UNIVERSAL-COMBAT BALANCE PRINCIPLE** — ONE shared, consistent per-unit damage model across EVERY battle (never tuned per-battle); outcomes EMERGE from scenario DESIGN (real OOB/terrain/cover/tactics); casualty ratios follow the player's/AI's tactics, not a forced historical number, but historical tactics → ~the historical result. **DO FIRST:** replace Fredericksburg's inflated artillery "volume-of-fire proxy men" (a per-battle fudge that violates D74) with a CONSISTENT gun-count/battery-weight artillery model, re-tune FB via geometry/OOB (not fudge), and RE-VET; **(4)** new battles stay **standalone menu battles** (no new campaign wiring). Then the rest of C1 → **C2 Western · C3 USCT · C4 the custom-battle builder**. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63): set model+effort EXPLICITLY — Sonnet low/med on mechanical legs (Explore/wcag/summarize), Opus high ONLY on bug-hunt finders/verifiers/critic.**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` Phase C (C1 partially ticked — Fredericksburg ✅) → `DECISIONS.md` **D73** (C-1) + **D69** (Phase-C order) + **D72** (B-6) + **D67** (fog-ON precedent) → the data-driven spine in `src/tactical/T1-bull-run.js` (`fldScenarioRegistry`/`fldScenarioData`/`fldScenarioInit`/`fldInjectScenarioButtons`/`fldScenarioEndHtml`) + `src/tactical/T7-command-side.js` (`fldScenarioSideChoice`/`fldBriefObjectiveHtml`) + the cautious-attacker seam in `src/tactical/T0-field-sandbox.js` (`fldAiAttacker` + `__FIELD._atkCautious` + `FLD.ATK_CAUTIOUS_HOLD`) + `data/fredericksburg.json` (the authored template) + `data/bullrun.json` (the reference) + `tools/probe-fredericksburg.mjs`. Everything is on disk and pushed. The B-6 CONTINUE-HERE block follows below.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE B-6 · CS-PLAYER "COMMAND EITHER SIDE" ✅ SHIPPED + VETTED + PUSHED — PHASE B COMPLETE** → NEXT = Phase C tactical breadth (Eastern marquee first)

> **PHASE B-6 — command EITHER army — is complete** (DECISIONS **D72**), and with it **Phase B (tactical depth) is DONE (B1–B6).** New module `src/tactical/T7-command-side.js` + surgical seams in T0/T1/T3 + `tools/probe-csplayer.mjs`. Deliverable ≈**1702KB**.
>
> **✅ B-6 — the CS-player tactical mode:** you can now command EITHER side in a real-time battle. Pick the Confederacy and you **DEFEND** Henry House Hill while the **B-1 attacker AI** presses you uphill — which is the whole point of fork #10 (it makes the B-1 doctrine player-facing). B-6 is a GENERALIZATION, not a new sim layer: `__FIELD.playerSide` is made authoritative in `fldInitSim` (precedence: explicit `opts.playerSide` > skirmish `playerSide` > a CS campaign via `_fldCamp().side` > `"US"`); the input layer (`fldPlayerSel`/select-all/cycle/drag) + the render/HUD **fog VIEWER** (2D markers, ghosts, 3D sync, friend/foe line) were a literal `"US"` → now read `fldPlayerSide()`; `fldBrSpec` (T1) flips the scenario AI flags off the player side; `fldPlayerSide()` (T3) resolves a CS **campaign LIVE** from `G.campaign.side` first, then the explicit per-launch side. New T7 UI: a period **side-choice card** (Lead the Union–ATTACK / Hold for the Confederacy–DEFEND), a side-aware briefing/objective + "your war vs history" + end-screen "you" line, a player-home-edge 3D camera, a dignified full-name friend/foe HUD ("4 Confederate vs 1 Union").
>
> **VETTING (the headline):** vetting caught a **real CS-campaign regression** I introduced — the first `fldPlayerSide()` rewrite trusted a stale `__FIELD.playerSide` and broke the officer-roster/fog contract (the roster/fog read it mid-setup, before a relaunch); `probe-officers` flagged it → fixed by resolving a CS campaign LIVE from `G.campaign.side`. Then the **52-agent bug-hunt → 11 confirmed + 7 critic gaps**: fixed **(HIGH)** a skirmish "Fight Again" soft-lock for a CS player (rematch now REPLAYS the stashed launch spec + the sandbox OOB assigns `ai` by playerSide), **(MED)** a US-hardwired 3D camera, **(MED)** a fog-leak via reinforcement banners/announce/auto-pause (now gated on player-visibility, fog-OFF byte-identical), **(MED)** a render-fog probe gap (added a deterministic step), + LOW polish; deferred-with-note the pre-existing/convention items.
>
> **✅ FULLY VETTED:** GATE OK (1702.3KB) → **probe-csplayer 16/16, 0 pe** → full no-regression GREEN (10 tactical baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346) → CS defense winnable 8/8 fog-ON + **live stacked still CS 8/8 (B-6 did not move the balance)** → wcag clean (2 contrast fixes). Committed + pushed.
>
> **What's playable:** the **⚔ FIRST BULL RUN** menu button now opens a side-choice card — lead the Union assault (history) or **hold the hill for the Confederacy** against the B-1 attacker AI; the ⚔ SKIRMISH side pick is fully wired; a CS campaign commands the CS. The CS defense is winnable, the field renders from your side, fog shows only what YOU scout, and the briefing/HUD/end-screen all speak from the army you command.
>
> **IMMEDIATE NEXT = PHASE C · TACTICAL BREADTH (D69):** more real-time battles on the data-driven engine, **Eastern marquee FIRST** — Antietam, Fredericksburg, Gettysburg, Chancellorsville (then Western C2, USCT C3, the custom-battle builder C4). The engine is mature (B1–B6); C is mostly new scenario DATA (OOBs + terrain + reinforcement timelines + teaching cards) on the proven T0–T7 stack, the bullrun1 pattern. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).** Also this session: the Gemini-report review (D71) — keep our engine/assistant/art choices; 3 Phase-H/publish notes folded in.
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1–B6 all ticked; Phase C next) → `DECISIONS.md` **D72** (B-6) + **D71** (the Gemini-report review) + **D70/D69/D67/D66/D65/D64/D62/D61** → `src/tactical/T7-command-side.js` (`fldBullRunSideChoice`/`fldBriefObjectiveHtml`/`fldPlayerOutcomeLine`/`_fldSideName`) + the seams (grep `fldPlayerSide`/`__FIELD.playerSide` across T0/T1/T3) + `tools/probe-csplayer.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE B-5 · DIFFICULTY/REALISM PRESETS ✅ SHIPPED + VETTED + PUSHED** → NEXT = B-6 CS-player "command either side"

> **PHASE B-5 — the Command & Realism layer — is complete** (DECISIONS **D70**). New gated `src/tactical/T6-presets.js` + guarded seams in T0/T3/T4/T5 + a Difficulty row in the T2 skirmish setup + `tools/probe-presets.mjs`. Deliverable ≈**1684KB**.
>
> **✅ B-5 — difficulty + realism presets:** **TWO AXES** — an **AI tier** (Recruit / Regular / Veteran / Hardee) × a **realism bundle** (Arcade / Balanced / Historian), plus an **"Advanced"** per-lever expander. Read ONCE at `fldInitSim` (`fldPresetsApply`, before the fog/auto-pause precedence) → sets `__FIELD.sev`{attrition·canister·supply·cmdShock·sight·veteran} + aiSkill/aiResolve/aiCushion → **WIRES the already-existing B-1..B-4 knobs** (attrition→fire/melee cas · canister→`fldArtFireMult` · supply→`fldSupplyReserve` · cmdShock→`fldOfficerFalls` · sight→`fldUnitSight` · veteran→`fldMoraleStep` · aiSkill→`fldAiAttacker`/`fldAiDefender` thresholds · the Recruit cushion+brittle-enemy→`fldMoraleStep`). Mostly WIRING + a UI, not new sim (D69).
>
> **SMARTER-NOT-CHEATING, CODE-ENFORCED:** `aiResolve` HARD-capped ≤1.0 (never an enemy steadiness buff — in `_fldClampCfg`, not just the data table); the player cushion exists ONLY at a GENUINE Recruit (dropped if the AI-sharpness chip is tuned off the tier); **Hardee = sharper DECISIONS (aiSkill), not stat bonuses**; **Veteran = aiSkill 1.0 / no handicap = the shipped opponent.** **BYTE-IDENTITY:** no preset → NEUTRAL config (all 1.0 / 0), every seam ×1.0/+0 → the 9 tactical baselines stay byte-identical; **"Balanced" == today's shipped CS 8/8 (live stacked, fog ON) SEED-FOR-SEED.** The tactical **Engineering Corps stays SPLIT** (D69) — B-5 ships the sliders + hooks, not the corps.
>
> **✅ FULLY VETTED:** adversarial bug-hunt **76 agents → 13 confirmed + 6 critic gaps, ALL FIXED** (HIGH: drawer-Escape-tore-down-the-battle → `stopPropagation`+`fldKey` guard; HIGH: picker ARIA radiogroup→group+aria-pressed; MED: a malformed-preset clamp `_fldClampCfg`; MED: the fog-"scenario" V-toggle wipe → no-op at apply; MED: drawer focus-jump/stacked-listener/resume-behind-modal; + 7 probe gaps). **probe-presets 26/26, 0 pe** · full no-regression GREEN (9 tactical baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346) · wcag clean (3 contrast fixes). Committed + pushed.
>
> **What's playable:** a new main-menu **"⚙ COMMAND & REALISM"** button + a Difficulty row in the ⚔ SKIRMISH setup open a period-broadsheet picker (pick an AI tier × realism bundle, or open Advanced for per-lever chips); the choice persists and governs EVERY real-time battle (campaign "Fight in real time", skirmish, the standalone Sandbox + First Bull Run). In any battle, the control bar's **"⚙ Settings"** (key **G**) opens a focus-trapped drawer with live fog/auto-pause/speed toggles + the current difficulty. Pick **Recruit × Arcade** for the gentle on-ramp, **Hardee × Historian** for the unforgiving real thing — the AI never cheats.
>
> **IMMEDIATE NEXT = PHASE B-6 · CS-player "command either side"** (fork #10 — `fldPlayerSide` already honored from B-2): command EITHER side, so you DEFEND as the CS while the B-1 attacker AI presses you. Makes the attacker doctrine player-facing; doubles tactical replayability. Then **Phase C** breadth (Eastern marquee first — Antietam/Fredericksburg/Gettysburg/Chancellorsville on the data-driven engine, D69). **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1–B5 ticked; B6 next) → `DECISIONS.md` **D70** (B-5) + **D69** (the B-5 design clear) + **D68** (the Q&A) + **D67/D66/D65/D64/D62/D63/D61** → `src/tactical/T6-presets.js` (`FLDP`; `fldPresetCompute`/`fldPresetsApply`/`_fldClampCfg`; the picker `fldPresetMenu` + the drawer `fldOpenSettingsDrawer`) + the per-layer seams (grep `__FIELD.sev`/`aiSkill`/`aiResolve`/`aiCushion` across T0/T3/T4/T5) + `tools/probe-presets.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE B-4 · DISTINCT ARM ROLES ✅ SHIPPED + VETTED + PUSHED** → NEXT = B-5 difficulty/realism presets

> **PHASE B-4 — artillery + cavalry get real ROLES — is complete** (DECISIONS **D67**). New `src/tactical/T5-arms.js` + guarded T0/T1/T2 seams + `data/bullrun.json` (Stuart `role:flank`, `defaultFog:true`) + `tools/probe-arms.mjs`. Deliverable ≈**1646KB**.
>
> **✅ B-4 — the arms of the service:** **Artillery** fires CANISTER up close (a shotgun — murderous in the open, defeated by works/woods) and a long-range BOMBARDMENT beyond; `ARM.art`=0.35 → overrun in melee; the **battery doctrine is ASYMMETRIC** — a DEFENDING battery sits sited-safe + displaces early, an ATTACKING battery pushes FORWARD to a canister post and is catchable (Griffin/Ricketts, whose loss broke the Union attack). **Cavalry** runs scout / flank / screen / **raid** (a raider drains the enemy B-3 ammunition train); mounted shock (1.4) shatters a disordered line but breaks on a braced one (0.9). The **Cannon Corps you BUY** conditions the field battery (Napoleon→canister, Whitworth→long-range, via `_fldArtProfile`/the A1 bridge). Gated behind `__FIELD.arms` (sticky `_armsOff` → the 8 baselines, incl. bullrun1's art+cav, byte-identical).
>
> **TWO THINGS VETTING CAUGHT (the headline):** **(1) a real BYTE-IDENTITY bug the design missed** — the FROZEN base engine defines `const ARM={…melee…}` at `build/base.html:634` (art 0.40 / cav 1.05), so the old melee was reading REAL values; my first refactor dropped them to 1.0 when arms-off → shifted the balance. **Fixed:** arms-OFF now falls back to the base `ARM` table (verified vs the committed build — bare & logistics both CS 5/8, byte-identical). **(2) a stacked-balance FORK (surfaced to Aaron):** the per-layer probes each isolate the others, so the STACKED live config (officers+logistics+arms) was never measured — it's **Union-favoured in clear weather** (committed D66 already CS 3/8 fog-OFF; B-4 → 0/8). Fog-ON is CS 8/8 ("fog aids the defender" intact). **Aaron's call: First Bull Run now DEFAULTS to FOG ON** (the historically faithful battle; live stacked config → CS 8/8). Fog stays a toggle; an explicit setting still wins.
>
> **✅ FULLY VETTED:** adversarial bug-hunt **31 agents → 2 distinct confirmed (3D-mesh-on-reinforcement, fog HUD leak) + 6 critic gaps, ALL FIXED** (incl. 6 new probe steps). **probe-arms 23/23, 0 pe** · full no-regression GREEN (8 baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346 · strategic spot-check green) · wcag clean. Balance: arms-only fog-OFF CS 7/8 · **LIVE stacked default (fog ON) CS 8/8**. Committed + pushed.
>
> **What's playable:** unchanged entry points (campaign "Fight in real time ⚔" · the ⚔ SKIRMISH menu · the standalone Sandbox + First Bull Run). The difference: **batteries and cavalry fight as themselves** — a battery flays a line in the open with canister but is overrun if you let infantry reach it; the forward Union guns at Henry House Hill are exposed (history); cavalry scouts, screens, flanks, charges a broken line, and can raid the enemy trains. **First Bull Run now opens under fog** (press **V** to clear it) — smoke and broken ground, where the defender holds the hill.
>
> **IMMEDIATE NEXT = PHASE B-5 · difficulty/realism presets — DESIGN CLEARED (DECISIONS D69):** new gated `src/tactical/T6-presets.js` read at `fldInitSim` from `G.settings.tacticalPreset` + per-lever values → sets the per-layer gates/severities (existing knobs → WIRING + a UI). **Curated 4-5 sliders + an "Advanced" expander** (AI level · fog · attrition · supply strictness · canister lethality; expander = command-shock/auto-pause/LOS/AI-leashes/xp). **Ladder = Recruit/Regular/Veteran/Hardee × Arcade/Balanced/Historian; "Balanced" = today's shipped balance (the byte-identical default, incl. Bull-Run-fog-ON).** Smarter-not-cheating (an easy-tier cushion only). `probe-presets.mjs` asserts neutral==today + each preset's gates. **The tactical ENGINEERING CORPS is SPLIT to its OWN later milestone** (Aaron's directive; consumes these sliders; memory `civilwar-directive-tactical-engineering-sliders`). Then **B6** CS-player "command either side" (fork #10) → **Phase C** breadth (Eastern marquee first — D69). **D68 = a 30-question design Q&A**: expansive v1, zoomable scale, wide-open alt-history + deeper citations, custom-builder-early, loot→core-pillar; **hex=Classic (no new engine)**, AA-essentials, tooltips-onboarding. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1–B4 ticked; B5 next) → `DECISIONS.md` **D67** (B-4) + **D68** (the Q&A) + **D66/D65/D64/D62/D63/D61** → `src/tactical/T5-arms.js` (`FLDA`; `fldArmMelee`/`fldArtFireMult`/`fldAiArtillery`/`fldAiCavalry`/`fldArmsStep`) + the base-ARM fallback in T0 `fldResolveMelee` + the `defaultFog` precedence (T0 `fldInitSim` / T1 `fldScenarioInit`) + `tools/probe-arms.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE B-3 · IN-BATTLE LOGISTICS ✅ SHIPPED + VETTED + PUSHED** → NEXT = B-4 distinct arm roles

> **PHASE B-3 — the ammunition economy on the real-time field — is complete** (DECISIONS **D66**). New `src/tactical/T4-logistics.js` + guarded T0 seams + the Bull Run `supply` block + `tools/probe-logistics.mjs`. Deliverable ≈**1608KB**.
>
> **✅ B-3 — supply trains + resupply + exhaustion:** each side fields a rear **ammunition train** with a finite **reserve**; a DISENGAGED brigade within reach refills (draining the reserve); a brigade OUT of cartridges holding the objective fires dry → bayonet (the 20th Maine); a SPENT brigade (fatigue>84) drags. The **attacker's train sits far behind its start line** (it outruns its supply) and the **defender's just behind the objective** → balance-neutral. **Ties strategic supply:** the reserve scales with `bridgeArmy().supply`; a raid-supply order cuts the enemy's. Gated + BYTE-IDENTICAL behind `__FIELD.logistics` (the 7 baselines + officers set sticky `_logisticsOff`).
>
> **THE FOG FIX (the headline — a HIGH bug-hunt finding, surfaced to Aaron):** with **fog ON**, logistics first INVERTED "fog aids the defender" (D58/D64) — Bull Run AI-vs-AI fog-ON **CS 8/8 → 0/8** (under fog the attacker cycled fresh brigades to its safe far-rear train and out-sustained the long fire-trade). Aaron's first pick (a defender's-depot rule) provably did nothing (the defender wasn't the ammo-constrained side); the fix that works (Aaron-approved) is **choking the ATTACKER's resupply under fog** (`ATK_FOG_RESUPPLY`=0 — an attacker advancing blind can't run its trains forward). **Result: logistics is balance-NEUTRAL in BOTH fog states — fog-OFF CS 5/8, fog-ON CS 8/8** (each = the no-logistics baseline; fog-aids-defender preserved). A FOG REGRESSION GUARD probe step locks it.
>
> **✅ FULLY VETTED:** adversarial bug-hunt **64 agents → 9 confirmed + 2 critic gaps, ALL ADDRESSED** (1 HIGH fog + 6 LOW probe-fidelity/correctness: probe boundary test, two-sided balance band, asymmetry + fog-reveal + supply-curve probe steps, reinforcement-only-side train). **probe-logistics 14/14, 0 pe** · full no-regression GREEN (7 baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346) · wcag clean (1 HUD-contrast fix). Committed + pushed.
>
> **What's playable:** unchanged entry points (campaign "Fight in real time ⚔" · the ⚔ SKIRMISH menu · the standalone Sandbox + First Bull Run). The difference: each side now has a rear **ammunition train** (a ❈ wagon marker + a faint resupply ring); a brigade out of close action near its train refills, the reserve is finite, and a brigade out of cartridges on the objective fires dry then bayonets; the HUD shows the reserve ("ample / running low / …") + a selected brigade's ammo/resupply/spent status. Turn on Fog (V) and the defender still holds the hill — the attacker outruns its supply.
>
> **IMMEDIATE NEXT = PHASE B (continued):** **B4** distinct arm roles (artillery canister/long-range; cavalry scout/flank/screen/raid) — **design drafted** (`.tmp/B4-arm-roles-design.md`: the `ARM`-melee dangling hook to fill; Bull Run already fields Griffin/Ricketts as `art` + Stuart as `cav`; the Cannon-Corps→field-battery bridge; the cav-raid↔B-3-train tie) · **B5** difficulty/realism presets · **B6** the CS-player "command either side" mode (fork #10). **B5+B6 also sketched** (`.tmp/B5-B6-design-sketch.md`). **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1 + B2 + B3 ticked; B4 next) → `DECISIONS.md` **D66** (B-3) + **D65/D64/D62/D63/D61** → `src/tactical/T4-logistics.js` (`FLDL` incl. `ATK_FOG_RESUPPLY`; `fldBuildSupply`/`fldSupplyRearPos`/`fldLogisticsStep`/`fldUnitInCloseAction`/`fldLogisticsAiUnit`) + `tools/probe-logistics.mjs`. Everything is on disk and pushed.
>
> **Session-env note:** global CC permissions = **bypassPermissions** (`~/.claude/settings.json`); MJI data/tooling/transcripts **purged** from this machine (personal-projects-only now); `~/.claude/CLAUDE.md` rewritten accordingly. None of that affects the game build.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE B-2 · OFFICERS & COMMAND ✅ SHIPPED + VETTED + PUSHED** → NEXT = Phase B continues (B3 logistics · B4 arm roles · B5 presets · B6 CS-player)

> **PHASE B-2 — leadership ON the field — is complete** (DECISIONS **D65**; charter fork: officers/command). New `src/tactical/T3-officers.js` + guarded T0 seams + the real Bull Run command cast in `data/bullrun.json.leaders`.
>
> **✅ B-2 — `fldOfficersStep` + the officer layer:** named leaders ride with their army and project a **COMMAND RADIUS** — brigades inside it recover morale faster, hold longer against the rout (capped save), and rally sooner — and **CAN BE HIT**: exposure near the firing line → wound (aura halves) → fall (aura dies + a one-time **command shock**, the general-down rout trigger). **Ties the named-generals layer (S2 m5):** a campaign commander's quality + name come from `bridgeArmy().leadership` / `cmdActiveGeneral()`; scenarios carry the cast (McDowell/Heintzelman vs Beauregard/Johnston/Bee/Bartow/Jackson). **Gated + BYTE-IDENTICAL:** behind `__FIELD.officers`; the 6 existing probes set sticky `_officersOff` → `(u.cmdBonus||0)`=0 → all baselines unchanged. **Bug-hunt: 52 agents → 1 confirmed + 7 critic gaps, ALL FIXED** — the big one was an over-lethal death model (army commanders who *survived* First Bull Run died every game); fixed via per-leader **historical `fate`** + risk **decay** + wound recovery + a **cover** term (the Jackson reverse-slope lesson) → army cdrs now survive **10/10**; also the Bee/Jackson anachronism (gated + contested framing), the CS-player hardcoding (`fldPlayerSide`, prep for B6), a balance moderation, and fog memory for fallen leaders. **Balance (officers ON, Bull Run AI-vs-AI):** CS **7/8** fog-OFF (the Henry House Hill command stand; US still breaks 1/8) · **8/8 fog-ON** (fog still aids the defender). **probe-officers 15/15**; full no-regression GREEN (6 baselines byte-identical, diag-classic 346, boot/t1, 0 pageerrors). Deliverable ≈**1585KB**.
>
> **What's playable:** unchanged entry points (campaign "Fight in real time ⚔" · the ⚔ SKIRMISH menu · the standalone Sandbox + First Bull Run). The difference: **named generals now ride the field** — a faint command ring + a mounted-officer marker; brigades under command steady and rally; a general riding the line too long can be **wounded or fall**, and his loss shocks the troops near him. At First Bull Run you see Beauregard, Johnston, Bee, Bartow, and (at 135s) Jackson — and the cost of leading from the saddle. The HUD shows your field officers' status; a selected brigade shows its commander + whether it's in command range.
>
> **IMMEDIATE NEXT = PHASE B (continued):** **B3** in-battle logistics (ammo + fatigue depth + supply) · **B4** distinct arm roles (artillery canister/long-range; cavalry scout/flank/screen/raid) · **B5** difficulty/realism presets · **B6** the **CS-player "command either side" mode** (fork #10 — now that `fldPlayerSide` is honored, this is the natural next). Then **Phase C** breadth → **D** full hex → **E** strategic S3–S5 → **H** graphics/footage/audio → **I** loot → **J** polish. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1 + B2 ticked; B3 next) → `DECISIONS.md` **D65** (B-2) + **D64/D62/D63** + D54/D55/D61 → `src/tactical/T3-officers.js` (`fldBuildOfficers`/`fldOfficersStep`/`fldOfficerHazard`/`fldOfficerFalls`/`fldPlayerSide`) + `data/bullrun.json.leaders` + `tools/probe-officers.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE B-1 · the smarter ATTACKER AI ✅ SHIPPED + VETTED + PUSHED** → NEXT = Phase B continues (B2 officers · B3 logistics · B4 arm roles · B5 presets · B6 CS-player)

> **PHASE B (tactical depth) is underway. B1 — the smarter DEFENDER-FAVORED ATTACKER AI — is complete** (DECISIONS **D64**; charter fork #2). The complement to the P1b-iii defender: a real attacker doctrine `fldAiAttacker` in `src/tactical/T0-field-sandbox.js`, dispatched to the scenario attacker (the symmetric sandbox + the defender stay byte-identical — unreachable when `__FIELD.attacker===null`).
>
> **✅ B1 — `fldAiAttacker`:** a covered defender wins the long-range fire trade, so the attacker CONCENTRATES on the weaker flank, CLOSES, and ASSAULTS (melee + numbers beat fire + cover). **Two locks (fork #2):** (a) **GRADUAL** per-unit local-concentration commit (no knife-edge global flip — fixed the prototype) and (b) **FOG AIDS THE DEFENDER** — under fog the attacker NEVER mass-assaults / column-rushes (it can't trust its sight of the hidden reserves), only exploits visibly-broken units + probes in line. **Balance A/B (8 seeds, Bull Run AI-vs-AI, CS=defender wins):** both-generic fog-OFF **2/8** (the logged gap) → both-doctrines fog-OFF **6/8** (CS-favored but competitive — US breaks 2/8) / fog-ON **8/8** (fog aids the defender — the prototype's inversion tuned out); defender casualties **4592** vs the passive attacker's 1276 (~3.6×, a real bloody assault). **Bug-hunt 4 finders → 2 findings, BOTH FIXED** (a picket-unblind hole that broke the fog lock → gated the assault directly on `!fog`; a dead-constant doc nit → deleted). **probe-ai 15/15**; tactical no-regression GREEN (field 13/13, bullrun 15/15, fog 13/13, autopause 13/13, diag-classic paints); 0 pageerrors. Deliverable ≈**1552KB**.
>
> **What's playable:** unchanged entry points (campaign "Fight in real time ⚔" · the ⚔ SKIRMISH menu · the standalone Sandbox + First Bull Run). The difference: in any asymmetric battle the **ATTACKER AI now fights for real** — it masses on a flank and presses the bayonet (fog OFF) instead of trading fire it loses; under fog it advances cautiously, so the defender's hidden reserves matter. AI-vs-AI Bull Run is now a bloody, historically-faithful Confederate-favored fight rather than a Union walkover.
>
> **IMMEDIATE NEXT = PHASE B (continued):** **B2** officers/command (leaders: command radius + morale bonus, can be hit; ties named-generals) · **B3** in-battle logistics (ammo + fatigue depth + supply) · **B4** distinct arm roles (artillery canister/long-range; cavalry scout/flank/screen/raid) · **B5** difficulty/realism presets · **B6** the **CS-player "command either side" mode** (fork #10 — makes this attacker AI player-facing: you defend, the AI attacks). Then **Phase C** breadth → **D** full hex → **E** strategic S3–S5 → **H** graphics/footage/audio → **I** loot → **J** polish. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1 ticked; B2 next) → `DECISIONS.md` **D64** (B1) + **D62/D63** + D54/D55/D61 → `ATTACKER-AI-PROPOSAL.md` (the prototype + the locked answers) → `src/tactical/T0-field-sandbox.js` (`fldAiAttacker`/`fldAiDefender`/`fldAiUnit`) + `tools/probe-ai.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE A · CONNECT THE TWO LAYERS ✅ SHIPPED + VETTED + PUSHED** → NEXT = Phase B (tactical depth)

> **The V1-CHECKLIST drives the run (D61).** Phase A — the connective tissue between the owner-mode GRAND STRATEGY desk and the real-time TACTICAL engine — is **complete** (DECISIONS **D62**). The two layers were disconnected; now the army your war built FIELDS in the real-time fight, and the fight's result flows back into the campaign.
>
> **✅ PHASE A — A1 conditioning · A2 fight-from-bridge + FREE skirmish · A3 result feedback — SHIPPED.** New `src/tactical/T2-campaign-link.js` + guarded T0/T1 seams + a bridge-briefing button (`src/85`). **A1:** `bridgeArmy(C)` scales the player's tactical brigades (men/morale/fatigue) + re-arms from the bought loadout, **anchored 74 / ±12% — identical to Classic-hex (`_a6Condition`) and auto-resolve**, so all three modes balance equivalently; reinforcements conditioned too. **A2:** a **"Fight in real time ⚔"** button on the pre-battle briefing (beside Auto-resolve + the Classic "To the Field") — First Bull Run (US) routes to the `bullrun1` scenario, every other battle to a CONDITIONED procedural brigade fight; plus a **"⚔ SKIRMISH (custom)"** main-menu setup (side / size / ground / year / fog). **A3:** the fight's **REAL casualty fractions** (men lost / fielded, counting reinforcements) drive `startBattleRuntime`+`_arApplyCasualties`+`campaignAdvance`→`_t1Resolve` — a real-time win/loss is **substitutable** for a Classic/auto-resolved result (win advances, loss recovers, casualties feed the strategic tick); deterministic; abort (Esc mid-battle) returns to the briefing, re-launchable. **Adversarial bug-hunt: 46 agents → 7 root-cause findings (incl. 3 HIGH: Esc-discards-the-result, no-focus-trap, flipAtk-lost-on-abort), ALL FIXED + re-probed.** WCAG-AA clean (2 trivial fixes). **probe-campaign-link 16/16**; full no-regression GREEN (tactical 5 suites + 22-probe strategy suite + bootprobe + t1probe + diag-classic paints, 0 pageerrors); deliverable ≈**1545KB**.
>
> **What's playable:** run the President's Desk → at any campaign battle's pre-battle briefing, choose **Auto-resolve**, **Fight in real time ⚔** (the new tactical engine, conditioned by your war, result feeds back), OR **To the Field (Classic)** (the hex battle). The main menu also has **⚔ SKIRMISH (custom)** for a free real-time engagement (pick side/size/ground/year/fog) + the standalone Tactical Sandbox + First Bull Run. The strategic desk (13 tabs, S1–S2) + Classic hex battles are unchanged.
>
> **IMMEDIATE NEXT = PHASE B (TACTICAL DEPTH, P2–P5):** **B1** the smarter **defender-favored ATTACKER AI** (concentrate/close/assault — prototype in `ATTACKER-AI-PROPOSAL.md`/`.tmp/attacker-ai-prototype.patch`; keep Bull Run defender-favored + **tune OUT the fog inversion** so fog stays consistent with the D58 teaching — fork #2) · **B2** officers/command (command radius + morale bonus, can be hit) · **B3** in-battle logistics (ammo+fatigue depth + supply) · **B4** distinct arm roles (artillery canister/long-range, cavalry scout/flank/screen/raid) · **B5** difficulty/realism presets · **B6** the **CS-player "command either side" mode** (you defend, AI attacks — fork #10). **PUSH RULE: per-milestone after PROPER VETTING (D53-OPS).** **Subagent models: HYBRID routing (D63)** — Opus on reasoning legs, Sonnet on mechanical legs.
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A ticked; Phase B next) → `DECISIONS.md` **D62** (Phase A) + **D63** (model routing) + D54/D55/D61 → `MODERN-UGG-PLAN.md` → `src/tactical/` (T0 sandbox+P1b · T1 bull-run · **T2 campaign-link**) + `src/85/86/87` (the bridge) + `tools/probe-campaign-link.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): TACTICAL ENGINE P0 ✅ · P1a Bull Run ✅ · P1b-i FOG ✅ · P1b-ii AUTO-PAUSE ✅ · **P1b-iii ROLE-AWARE AI (smarter CS DEFENDER) ✅ SHIPPED** → NEXT = the smarter ATTACKER AI, then the rest of P1b

> **The run-k charter reprioritized the real-time TACTICAL ENGINE ahead of S3–S5 (D54/D55).** S2 is COMPLETE (m1–m5; the block below). The tactical engine is now being built in gated, probed, adversarially-bug-hunted, pushed slices: **P0 sandbox (D56) → P1a First Bull Run (D57) → P1b-i fog (D58) → P1b-ii active auto-pause (D59) → P1b-iii role-aware AI (D60, just shipped).**
>
> **✅ TACTICAL P1b-iii — ROLE-AWARE AI: a smarter CONFEDERATE DEFENDER — SHIPPED + VERIFIED + PUSHED** (DECISIONS **D60**). The named complement to fog: the shipped AI was symmetric (both sides "advance toward the objective"), so the CS *defender* played like an attacker — the root of the logged "AI-vs-AI leans Union" gap (D57). Now `fldAiUnit` **dispatches** on `__FIELD.attacker`: the scenario DEFENDER runs the new `fldAiDefender` (hold the objective's forward face in cover via a **stable defensive frame**; fighting withdrawal to the reverse slope; counterattack a *catchable* disordered attacker on a **dual leash**; hold points **clamped inside `obj.r`**); the ATTACKER + BOTH sandbox sides run the verbatim-unchanged `fldAiGeneric`. The sandbox has `attacker===null` → `fldAiDefender` unreachable → **byte-behavior-identical** (probe-field 13/13). Deterministic + in the headless sim path → **fully probe-testable**. **Balance A/B (8 seeds):** old AI CS 0/8 fog-OFF, 1/8 fog-ON → doctrine 8/8 / 8/8, via historical **denial/timeout** wins. Adversarial bug-hunt **27 agents → 10 confirmed (6 engine + 4 probe-fidelity), ALL FIXED**. probe-ai **11/11**; full no-regression GREEN (probe-field 13/13, bullrun 15/15, fog 13/13, autopause 13/13 + strategy suite + bootprobe + t1probe + diag-classic, 0 pageerrors); deliverable ≈**1509KB**. In `src/tactical/T0-field-sandbox.js` + `tools/probe-ai.mjs`.
>
> **What's playable:** open `civil_war_generals.html` → main menu Field Dispatches has **"⚔ Tactical Sandbox (Beta)"** + **"⚔ BATTLE — FIRST BULL RUN (1861)"**. Bull Run: briefing → take command of the Union → drive for Henry House Hill before Jackson/Early/Elzey arrive by rail. Toggles: **V** fog (default OFF), **P** auto-pause (default ON). The CS now defends the hill intelligently (holds in cover, fighting-withdraws, counterattacks) — a real opponent. Speed 1×/2×/4×, active pause, a 2D offline fallback. The strategic owner-mode desk (13 tabs, S1–S2) + Classic hex battles are unchanged.
>
> **IMMEDIATE NEXT = TACTICAL P1b (continued):** the **smarter ATTACKER AI** (concentration/flanking — the complement that restores AI-vs-AI variety now that the defender is strong; the logged lever, NOT weakening the defender) · the **hex-mode toggle** (Classic frozen) · **full owner-mode bridge conditioning** into the scenario + a **tactical-only FREE mode** · **deeper 3D** (reuse run-h PBR/HDRI/post-FX). Then P2–P5, the loot/survival texture, then S3 alt-history · S4 education/codex/WCAG-AA · S5 victory + Reconstruction · the graphics/footage layer. **PUSH RULE: commit + `git push origin main` per milestone ONLY after PROPER VETTING** (GATE OK + full no-regression + diag-classic + 0 pageerrors + bug-hunt-fixed) — DECISIONS **D53-OPS**.
>
> **Resume map:** `START-HERE.md` → `AUTONOMOUS-RUN.md` §2 (the tactical m0–m4 block) + §8 charter → `DECISIONS.md` D54/D55 + **D56–D60** → `MODERN-UGG-PLAN.md` → `src/tactical/` (T0 sandbox+all P1b systems · T1 bull-run) + `tools/probe-{field,bullrun,fog,autopause,ai}.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (overnight, 2026-06-14): S2 COMPLETE — m1 CABINET ✅ + m2 DECISIONS ✅ + m3 MORALE ✅ + m4 PRESS ✅ + m5 COMMAND ✅ SHIPPED → NEXT = S3 alt-history

> **The §8 charter governs the run** (`AUTONOMOUS-RUN.md` §8 + `DECISIONS.md` D30–D41). Battle layer A1–A6 done (run j). S2 is built INCREMENTALLY — one sub-system per gated + empirically-probed + adversarially-bug-hunted + committed + **pushed** milestone. **Owner directive (this session): fold web-search/fetch grounding into the content-research workflows** (real public sources → adversarially verified). Blender stays CLOSED — art is code-only by charter (§8.8); nothing's lost.
>
> **✅ S2 m1 — THE CABINET & ADVISOR SYSTEM (R25 linchpin) SHIPPED + VERIFIED + PUSHED** (commit on `main`; DECISIONS **D49**). The S0 stub → the FULL date-aware historical cabinet: `data/cabinet.json` (US 13 / CS 15; the churn is mechanical) + `src/31-cabinet.js` (overrides `presRenderCabinet`/`presWireCabinet`; `cabInit`/`cabOnResolve` registered in `90`). Per principal advisor (war/treasury/state/navy): a war-state-keyed **one-line rec** + one-click **Heed** (bounded once-per-turn nudge) or **Delegate** + expandable **why** + an **ambition tell** for the self-servers (Chase/Toombs/Blair). Date-aware holders make the churn legible (Davis's revolving War office, Benjamin's 3-post rotation). Content = the web-grounded `s2-cabinet-research` workflow (21 agents, 2× adversarial verify), anti-Lost-Cause. Bug-hunt 62 agents → 8/19 confirmed, all fixed (D49.1–.8). `cabinetLeadership(C)` exposed but **not yet consumed by the bridge** — that wiring lands with the generals milestone. probe-cabinet **12/12**; 16 probe suites + diag-classic green; 0 pageerrors. Deliverable ≈1130KB.
>
> **✅ S2 m2 — EXECUTIVE DECISIONS + the pendingChoices loop SHIPPED + VERIFIED + PUSHED** (DECISIONS **D50**). `data/decisions.json` (8 web-grounded cards) + `src/32-decisions.js`: a non-blocking decision deck surfaced in the between-battles interstitial AND a new **"Decisions" desk tab** (the 11th). `decOnResolve` owns the queue at the §8.68 cadence (≤2 new non-hinge/turn; the hinge always surfaces); `decResolve` applies bounded/clamped/logged effects. **EMANCIPATION = a dated Union hinge** (issue / go-radical-now / refuse) wired to the **manpower USCT gate** (`C.president.emancipation`: issue → unlocks even pre-1863; refuse → never; no-decision → the historical 1863 default = zero regression). Civil-liberties + home-front cards both sides (habeas US+CS, Vallandigham, NYC draft riots, press censorship, Richmond bread riot, twenty-slave law). Bug-hunt 56 agents → 12/17 fixed (2 HIGH: decResolve idempotency, capital floor-only). probe-decisions **14/14**, desk **13/13**, manpower **9/9**; 0 pageerrors.
>
> **✅ S2 m3 — THE THREE-LAYER MORALE SYSTEM + the 1864 election hinge SHIPPED + VERIFIED + PUSHED** (DECISIONS **D51**). `src/33-morale.js` + `C.morale`: troop / leader / public-will, computed fresh each turn (`moraleCompute`) with the interactions baked in (leadership lifts troops; the accumulating casualty toll [D35.6] + inflation sink the public; victories/recognition lift it). Surfaced as **"The Nation's Will"** in the War Effort overview with an 1864-election forecast. **The election → the war's end (D41.2):** `moraleOnResolve` (after clk, before vic) feeds the verdict into the existing `C.strategy.enemyWill` — re-elected → −8 (grind the enemy toward your negotiated-peace win); **repudiated → +12 + a durable public-will hit** (you can win battles and lose the war). A computed READOUT with ONE bounded sim coupling — writes nothing to the frozen clock/economy/manpower (zero regression). Bug-hunt 29 agents → 7/8 fixed (2 HIGH: migration-safe + atomic election latch). probe-morale **10/10**; probe-victory **12/12**; 0 pageerrors.
>
> **✅ S2 m4 — THE PRESS / PUBLIC-OPINION SYSTEM SHIPPED + VERIFIED + PUSHED** (DECISIONS **D52**). `data/press.json` (12 real web-grounded papers) + `src/34-press.js` + a new **"The Press"** desk tab (the 12th, period broadsheet). Each paper's LEAN (radical/administration/copperhead/fire-eater/abolitionist/war-democrat) sets how it reacts to the war's turns; `pressOnResolve` (before morale) shifts each paper's FAVOR; the aggregate SENTIMENT feeds `moraleCompute`'s public-will layer (`pressTerm`, anchored at 50 = neutral until reacted, so zero perturbation at init). Greeley/Garrison/Medill/Curtis + the Copperhead sheets + the Richmond Examiner savaging Davis; anti-Lost-Cause (BOTH presses divided). Bug-hunt 44 agents → 7/13 fixed (MED: a corrupt-`null`-headline crash → `pressInit` filters entries). probe-press **10/10**, desk **13/13**, morale **10/10**; full no-regression green; 0 pageerrors.
>
> **✅ S2 m5 — COMMAND / NAMED-GENERALS SHIPPED + VERIFIED + PUSHED** (DECISIONS **D53**). `data/generals.json` (20 generals, US 10 / CS 10) + `src/35-command.js` + `C.president.command` + a new **"Command"** desk tab (the 13th). Appoint / promote / relieve the principal-army commander; each carries skill/aggression/caution traits, an EVOLVING reputation (win ↑ / loss ↓), and ambition (the McClellan relief-cost gate on `clock.capital`, with a free "restore the historical command" escape). **THE KEY WIRING (done):** `commandLeadership(C)` = `0.55·skill + 0.45·reputation` blended with `cabinetLeadership`, **anchored at 64**, now drives the bridge LEADERSHIP facet (the `src/85` line-90 `leadership=64` placeholder, live since D49.7) + a bidirectional `bridgeArmy.overall` edge (0 at the anchor → fresh/default command = byte-equivalent Classic) + a `commandMarginEdge` auto-resolve nudge. **The m3 leader-morale layer deliberately STAYS on `cabinetLeadership`** (D53 decoupling — folding the general in would double-count the cabinet AND confound the cabinet-isolating morale probe). Date-aware iconic-army DEFAULT (US Scott→McClellan→Burnside→Hooker→Meade→Grant; CS J.E. Johnston→Lee; the West/Halleck/Sheridan/Thomas/Sherman appointable but null-default). Content = web-grounded `s2-generals-research` (10 agents), anti-Lost-Cause (Forrest's bio names Fort Pillow + KKK + slave-trading; Lee excellent-not-infallible; Grant top-tier). Bug-hunt 20 agents → **9/12 fixed** incl. 2 HIGH (a `bridgeArmy↔cabinet` recursion landmine → `_brgLeadGuard` re-entrancy guard; Thomas/Sherman Western-theater defaults nulled; post-war fallback continuity). probe-command **14/14**; full no-regression GREEN (bridge 6/6 · conditioning 6/6 · auto-resolve 7/7 · cabinet 12/12 · morale 10/10 · desk 13/13 · all 16+ suites + diag-classic); 0 pageerrors. Deliverable ≈1392KB.
>
> **IMMEDIATE NEXT = S3 (alt-history engine):** tiered divergence + hinge-points + the emergent toggle + curated scenarios, deterministic-by-performance with consensus framing + the "your war vs history" tracker (GRAND-STRATEGY §3 / §S3). Then **S4** education/codex/tutorial/WCAG-AA · **S5** victory + graded report + Reconstruction · then the **run-k owner directives**: the graphics/footage "make everything come to life" linked-assets layer (offline fallback, memory `civilwar-graphics-footage-directive`) + the hunting/fishing/town-looting "Borderlands × Oregon Trail" survival texture (memory `civilwar-gameplay-survival-loot`). **PUSH RULE (Aaron, this session): commit + `git push origin main` to `adhanke-star/History-Video-Game` per milestone ONLY after PROPER VETTING** (GATE OK + full no-regression + diag-classic + 0 pageerrors + bug-hunt-fixed) — see DECISIONS **D53-OPS** + `START-HERE.md` + `AUTONOMOUS-RUN.md` §1.
>
> **The desk has Cabinet (10th) + Decisions (11th) + The Press (12th) + Command (13th) tabs; the overview shows War Production · The Ranks · The Nation's Will.** Modules `src/10/20/30/31/32/33/34/35/40/50/55/56/57/58/59/60/70/80/85/86/87/90`. Tick order in `_t1Resolve`: clk → econ → wr → blockade → prod → eng → manpower → mr → pres → **cab → cmd → dec → press → morale** → vic → bridge. Data: `data/{cabinet,decisions,press,generals,economy,diplomacy,manpower-teaching,artillery,engineering,terrain-cover,weapons}.json` → `GAME_DATA`. **Cross-system wiring patterns now in place** (each is the same `_t1Resolve`-ordered, bounded, readout+one-coupling shape): decision → `C.president.emancipation` → manpower USCT gate (m2); morale's 1864 verdict → `C.strategy.enemyWill` (m3); press sentiment → `C.morale.public` (m4); **the sitting general → `commandLeadership` → the bridge LEADERSHIP facet (m5, anchored at 64)**. S2 is COMPLETE.

---

## ⚡ CONTINUE HERE — run j (overnight, 2026-06-14): ⚑ BATTLE LAYER A1–A6 ✅ COMPLETE (war playable end-to-end) → NEXT = the STRATEGIC ARC S2

> **The §8 charter governs the run** (68 Aaron-locked decisions; `AUTONOMOUS-RUN.md` §8 + `DECISIONS.md` D30–D41 — read §8 FIRST). Unblockers: gated frozen-engine overrides AUTHORIZED · halt only for irreversible/money/external-account · **GitHub backup LIVE (`adhanke-star/History-Video-Game`, private — push every milestone)** · adversarial bug-hunt before every commit · auto-tune balance+log · max systems shipped.
>
> **⚑ THE BATTLE LAYER IS COMPLETE — the owner-mode war is PLAYABLE END-TO-END.** All shipped + pushed; each gated + empirically probed + adversarially bug-hunted; full no-regression green; 0 pageerrors; Classic never regressed:
> **A1 Cannon Corps** `da74ad9` (D42) · **A2 Engineering Works Corps** `efad4c6` (D43, 4 Verified cards) · **A3 Terrain cover** `b21b5d7` (D44, 1st frozen-engine override) · **A4 Armory→battlefield** `47ae217` (D45, `genForce` override) · **A5 Targeting** `3959b65` (D46, verified) · **A6a Battle-day conditioning** `88f878c` (D47, `startBattleRuntime` override) · **A6b Bridge auto-resolve** `149d07e` (D48, the playable-end-to-end keystone). 16 probe suites green.
>
> **The playable loop:** pick a side → run the President's Desk (economy/production/blockade/diplomacy/manpower + the three buildable corps: small arms, Cannon Corps, Engineering Works) → field a CONDITIONED army (the bridge) → at each battle **Auto-resolve OR Fight** (weapons bought + terrain cover + conditioning all real on the day) → the result drives manpower/clock/strategy/enemyWill/victory → win via the chain or a path-to-victory.
>
> **IMMEDIATE NEXT = the STRATEGIC ARC, charter-expanded (§7 item 2 / AUTONOMOUS-RUN §2).** **S2** = the FULL historical cabinet (§8.22) + one-line-rec/delegate/why advisor UX (§8.54) + 3-layer morale (§8.61; public-will-decides-late §8.67) + the 1864 election + the press/public-opinion system (§8.57) + command: appoint/promote/relieve generals (§8.58) + emancipation as a dated decision (§8.27) + civil-liberties dilemmas (§8.62) + named generals (§8.24) + the curated home-front event deck (§8.25). **Content may be recoverable via the cached s2-content-research workflow — see `AUTONOMOUS-RUN.md` §7 item 7** (else re-run a fresh advisor+decision-card research workflow). Then S3 alt-history · S4 education/codex/tutorial/WCAG-AA · S5 victory + graded report + Reconstruction · content systems · theaters · tactical P0–P5.
>
> **Full morning summary: `WAKE-UP.md` (root).** To run it: fresh chat → paste the `START-HERE.md` kickoff prompt → it loads `AUTONOMOUS-RUN.md` §2 (A1–A6 ✅, S2 next) + D42–D48 → continue the §3 loop at S2. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run i progress (S0 + S1 COMPLETE: a/b/c/d SHIPPED) → next: S2 (updated 2026-06-13)

> **S1 is COMPLETE — the full asymmetric economy.** S1a finance/inflation · S1b production/logistics · S1c cotton/blockade/diplomacy · **S1d manpower/conscription (just shipped).** `src/70-manpower.js` + `G.campaign.manpower`: the replacement-ratio collapse (US 1.0 holds; CS 0.9→0.1 by 1865 → armies melt), Union draft-as-stimulus, +180k USCT pool at 1863, immigrant inflow, CS desertion; **`C.manpower.strength` is the army-strength index the S5 battle bridge reads.** Surfaced as "The Ranks" in the War Effort overview. Content-complete via an 11-agent workflow → `data/manpower-teaching.json` (5 multi-voice cards + 9-claim audit; no sim correction needed). **VERIFIED:** probe-manpower 9/9 (20-turn arc to 1865: US strength 100 / CS 53-melting / ratio 0.1 / pool 0); full no-regression suite green (desk 12/12, economy 8/8, production 4/4, blockade 11/11, diag-classic paints, 0 pageerrors).
>
> **NEXT = S2** (`GRAND-STRATEGY-PLAN` §3/§4/§16/§25, R25 the linchpin): the **advisor/cabinet system** — generalize the Treasury/Diplomacy/War per-domain Delegate toggle + the ambient "Why it mattered" teaching into a full system where each secretary auto-manages + recommends + TEACHES; wire `president.pendingChoices` curated non-blocking decision cards (the S0 stub); 3-layer morale (troop/leader-reputation+ambition/public); wire the 1864 election (`clk.resolved1864`) to consequences. The 10 economy/diplomacy/manpower card sets + the desk are the foundation to build on. Then S3 alt-history · S4 education/codex · S5 victory + the battle bridge.
>
> Eight desk tabs now: The War Effort (overview: treasury/home-front meters + War Production + The Ranks blocks) · The Treasury · Diplomacy · War Room · 1864 Clock · Muster Roll · Cabinet · Theater Map. Modules `src/10/20/30/40/50/60/70/90`. Tick order in `_t1Resolve`: clk → econ → wr → blockade → prod → manpower → mr → pres. Data: `data/{economy,diplomacy,manpower-teaching}.json` → `GAME_DATA`.

---

### (prior) ⚡ CONTINUE HERE — S0 + S1a + S1b + S1c SHIPPED → S1d

> **S1c cotton/blockade/foreign just shipped + gated + committed.** `src/60-blockade.js` + `G.campaign.blockade`: the 1861 King-Cotton self-embargo trap (rev≈0), blockade strangulation (capture 0.10→0.33, ports fall, a dynamic `importFactor` that WIRED the S1b prod placeholder — off>full arms), cotton revenue→funds, foreign recognition (1862 famine window → foreclosed 1863) → `clock.intervention`; blockade-depth toggle (full/flat/off, R14); new 8th **Diplomacy** desk tab. Content-complete via an 11-agent research+verify+audit workflow → `data/diplomacy.json` (5 multi-voice cards + a 9-claim numbers audit that corrected Erlanger 72%→45% in `economy.json`) + `HISTORICAL-DATA-DIPLOMACY.md`. **VERIFIED:** probe-blockade 11/11 (16-turn arc), full no-regression suite green (desk 12/12, economy 8/8, production 4/4, diag-classic Classic paints, 0 pageerrors). **NEXT = S1d manpower/conscription** (`data/economy.json` → `manpower`): recruitment pools, the draft (US 1863 / CS 1862), the late-war **CS replacement-ratio collapse 1.0→0.1** (the war-ender), immigrant pool, enslaved-labor base (ties to S2 emancipation/contraband); feeds the pre-battle strength bridge (S5). New `src/70-manpower.js`, register a `manpowerOnResolve` tick. Then S2.

---

### (prior) ⚡ CONTINUE HERE — S0 + S1a + S1b SHIPPED → S1c

> **For a long unsupervised run, read `AUTONOMOUS-RUN.md`** — the operating manual for the self-perpetuating 12-hour build loop (prime directive, loop steps, phase roadmap, guardrails, recovery).

**Strategic-first build underway. Three milestones shipped + gated + committed locally this run (local commits only — Aaron: no GitHub push):**
- **S0 — President's-Desk owner shell** (commit `673894f`): zero-dep **build system** (`build/base.html` frozen run-h monolith + ordered `src/*.js` modules + `tools/build.mjs` → concat → splice before the engine-end anchor → in-memory parse/hex/collision gate → write `civil_war_generals.html`). `G.campaign.president` state (sibling of clock/muster/warroom; rides the save, no `_SAVE_VER` bump). War Department **expanded in place** into a 7-tab desk; between-battles **auto-surface/one-click-skip interstitial**. Cabinet = 4 engraved advisors (stub).
- **S1a — finance core** (commit `3a6aad6`): build is now **data-driven** (`tools/build.mjs` injects `data/*.json` as `GAME_DATA`). `data/economy.json` + `HISTORICAL-DATA-ECONOMY.md` from a citation-grade 8-agent research workflow. `src/40-economy.js` = 3 funding levers (bonds/taxes/press) with an **emergent inflation asymmetry** (US anchored ×1.13 / CS spiral ×87.5 over 12 turns ≈ historical ~90×; inflation→`clock.weariness`). "The Treasury" tab; Secretary delegated by default; teaching card.
- **S1b — war production** (commit `b2179d0`): `src/50-production.js` extends the War Room nodes → asymmetric matériel (US well-found; CS iron-ceiling + import + irreversible rail decay → ragged/hungry; hunger→weariness). Probe-verified 12-turn US rail100/equip100 vs CS rail42/equip24/arms4/food19%. War Production block in the War Effort overview.

**OPERATING PARAMETERS (Aaron-locked, run i — govern the whole run):** run the whole arc (S0→S5→tactical; stop only at unresolvable fork/blocker/done) · **always decide & log** (forks → `DECISIONS.md`, never pause mid-run) · **auto-commit AND push** at gated milestones (no remote yet → local only) · **content-complete each phase** (real data + PD images per system).

**TOOLCHAIN (proven this run):** `node tools/build.mjs` (then bootprobe/t1probe/probe-desk/probe-economy/diag-classic — START a `python3 -m http.server 8765` yourself + confirm 200 first; probes' auto-spawn races; bootprobe goto=60s). **READ the JSON in `tools/shots/` + the PNGs.** Gotchas: no literal comment-closer in JS block comments (bit the build script + a module — scan `src/` before building); bare-name globals `G`/`GAME_DATA` (never `window.G`); `portraitFor` → `window.portraitFor` from modules. Decisions: `DECISIONS.md`. Run detail: `RUN-LOG.md` §"run i".

**NEXT — finish S1, then S2 (all data already in `data/economy.json`; full plan in `AUTONOMOUS-RUN.md` §4):**
- **S1c cotton/blockade/foreign** — King Cotton self-embargo trap + blockade strangulation schedule + runner economics + Erlanger loan; foreign-recognition pressure inputs (feeds S2 diplomacy). Blockade-depth = toggle (R14).
- **S1d manpower/conscription** — recruitment pools, draft, the late-war CS replacement-ratio collapse (1.0→0.1) that ends the war; immigrant pool; enslaved-labor base (ties to S2 emancipation/contraband).
- Wire economy consequences into the pre-battle conditioning bridge (§3/§11) and the cabinet advisors (each secretary surfaces its domain's choices + teaches).
- **Then S2** — executive decisions + 3-layer morale + the 1864 election + the **advisor/cabinet system** (the linchpin R25: auto-manage + recommend + TEACH, per-domain delegation — the Treasury delegate toggle is the seed pattern).

**To continue:** read this CONTINUE-HERE block, then `DECISIONS.md`, `RUN-LOG.md` §"run i", `data/economy.json` + `HISTORICAL-DATA-ECONOMY.md`, and the design law `GRAND-STRATEGY-PLAN.md` (esp. §2 economy, §27 balance). Build S1b next with the same module+gate+probe+commit discipline. The full design orientation is below.

---

# HANDOFF — "The Civil War" — MASTER new-chat handoff (run h → next build)

**Written 2026-06-13 (run h, Opus 4.8). THE single entry point for the next session.** Personal project — NOT MJI. Full autonomy, Ultracode on, $0/free-CC0, build bar 200/100. **To start the next chat: paste the §PASTE block at the bottom.** Everything else here orients you.

---

## 1 · THE PROJECT IN ONE PARAGRAPH
"The Civil War" is becoming **the definitive teaching wargame: Ultimate General: Gettysburg's combat FEEL + Paradox-grade strategic DEPTH + a museum's historical RIGOR** — all three, no compromise. You are the **President (Lincoln or Davis)** running the entire war (economy, politics, diplomacy, morale, generals, the home front) AND commanding the battles in a real-time, gridless, drag-to-maneuver tactical engine — wrapped in a PhD-level history + alternate-history teaching layer where scholarly consensus is the *favored-but-not-guaranteed* path. **The governing design law (Aaron's central tension, resolved): the simple low-micro UG:G battle is the always-fun CORE that stands alone; ALL depth is optional — auto-managed by characterful advisors, delegable per-domain, preset-gated, fluidly dial-able; education is ambient, never blocks play, and confers real in-game mastery (so learning and fun pull the SAME direction).** One engine must serve the pure-battle player, the grand-strategist, and the PhD-historian simultaneously.

## 2 · WHERE THINGS STAND (run h, verified)
- **Classic (hex, turn-based) is FROZEN/finished** — the complete hex lineage + its 3D-skin "Modern." NEVER regress it (`tools/diag-classic.mjs` every splice).
- **Shipped + GPU-verified this run (all reusable):** procedural unit motion; **131 real PD-photo leader portraits** (`assets/portraits/`, license-gated, side-split for Anderson/Gregg) with a closure refactor + upgraded engraving fallback; a fuller period UI pass (drawn order-icons, engraved frames, brass portrait mat). Plus the inherited cinematic 3D foundation (PBR terrain, HDRI, ACES/bloom post-FX, weather, props, camera) and the adaptive period audio score.
- **The shipped "Modern" is only a 3D re-skin of the hex-turn engine** — it does NOT play like UG:G. The real-time tactical engine + the grand-strategy owner-mode are **NOT built yet** — that's the next work.
- **DESIGN: 63 decisions LOCKED** across a 35-round popup design session (Aaron). Fully captured in `GRAND-STRATEGY-PLAN.md` (Parts I–III). **No new-engine code written yet** — the next chat builds from the spec.

## 3 · THE DOCS (read in this order — you have zero prior context)
1. **`GRAND-STRATEGY-PLAN.md`** — THE DESIGN LAW. All 63 locked decisions: Part I (rounds 1–10: player/economy/executive/morale/alt-history/upgrades/education/victory/modes/first-build), Part II (rounds 11–20: diplomacy/slavery/politics/naval/map/generals/soldiers/intel/events/replay), Part III (rounds 21–33: **the depth↔simplicity balance** + presentation), §27 **the balance principle**, §13/§24/§28 decision indexes.
2. **`HISTORICAL-DATA.md`** — citation-grade content (211KB, 18-agent research): First Bull Run full OOB (Union + CSA), weapons/ballistics with ranges, USCT (structure/battles/criticism), the historiography framework, PD image sources. Feeds OOBs, weapon stats, debate cards, the codex.
3. **`MODERN-UGG-PLAN.md`** — the real-time tactical battle engine spec (built SECOND, after the strategic layer).
4. **`RUN-LOG.md` §"run h"** — what shipped + the pivot + this design session; skim §"run g"/§"run f" for the 3D foundation.
5. **`REVIEW-QUEUE.md` top** + **`GRAPHICS-RUN-CONTINUE.md` §TOOLCHAIN/GOTCHAS** — the splice/gate/real-GPU-shot toolchain + the bare-name-globals / async-THREE / override-by-redeclaration lessons.

## 4 · ARCHITECTURE DECISION (run h — important)
**Modularize via a zero-dependency build step.** The scope (50–100k+ lines) outgrows the single 14k-line file. Author the new engine in **source modules**; a tiny **node concat script** assembles the one shippable `civil_war_generals.html` (keep the $0/portable/open-and-play deliverable + `play.command`). Set this up FIRST in S0. Classic + the existing engine stay intact through the transition (move them into the build later, carefully, or keep the current HTML as the base the build appends to). Gate the BUILT output (parse/hex/Classic-regression). This replaces the append-only-single-file splice model for new work — but the gate discipline carries over.

## 5 · BUILD ORDER & PHASES (Aaron: STRATEGIC-FIRST, confirmed twice)
**Phase group S (grand-strategy owner-mode) FIRST, then phase group P (real-time tactical).** Battles use the EXISTING engine via a clean bridge until the UG:G engine swaps in.
- **S0 — scaffold:** set up the module/build system; expand the War Department (`G.campaign.{clock,muster,warroom}`) into the President's-desk shell (period war-room UI); strategic-turn loop; save. Layered scenario onboarding (start ~1861, limited toolset).
- **S1 — economy** (full asymmetric model + finance/inflation/blockade).
- **S2 — executive decisions + 3-layer morale + the 1864 election; the advisor/cabinet system** (the linchpin: auto-manage + recommend + TEACH, per-domain delegation).
- **S3 — alt-history engine** (tiered divergence + hinge-points + emergent toggle + curated scenarios; deterministic-by-performance with consensus framing; the "your war vs history" tracker).
- **S4 — education layer** (multi-voice debate cards + deep-dives + codex from HISTORICAL-DATA.md; ambient teaching; difficulty/realism sliders; play-style presets; full a11y).
- **S5 — victory/defeat** (asymmetric paths incl. **negotiated peace for both sides**, military-only hard-loss) + all campaign scopes; wire existing battles through the bridge → **playable full owner-mode war.**
- **THEN P0–P5** (`MODERN-UGG-PLAN.md`): the real-time UG:G tactical engine (First Bull Run vertical slice first), swapping in behind the bridge.
Each phase: gate + real-GPU self-verify (probes you READ) + Classic no-regression + left in a working, polished state (ship-quality iteratively).

## 6 · THE BALANCE PRINCIPLE (design law — apply to EVERY system)
The simple, low-micro UG:G battle is the always-fun core that works on its own. Every deep system is optional — auto-managed by characterful advisors, delegable per-domain, preset-gated, fluidly dial-able mid-campaign. Education is ambient, never blocks play, and confers real mastery (learning is rewarded by winning, not a chore-score). Presets + sliders let each player set their own balance. **If a feature forces depth on a battle-first player, it's wrong; if it denies depth to a strategist/historian, it's wrong.** (Full statement: GRAND-STRATEGY-PLAN §27.)

## 7 · TOOLCHAIN, GATES, GOTCHAS (proven — reuse)
- **Gates every change:** `.bak` → parse (`node --check` the built/extracted `<script>`) → invalid-hex grep `0x[0-9a-fA-F]*[g-z]` = 0 → collision-grep new fns = 0. With the new build step, gate the assembled output.
- **Real-GPU self-verify (READ the PNGs/probes):** `tools/shot-postfx.mjs`, `tools/shot-gpu.mjs`, `tools/shot-ui.mjs`, `tools/probe-motion.mjs`, `tools/probe-portraits.mjs`, `tools/bootprobe.mjs`, `tools/diag-classic.mjs`. Real GPU = **Intel UHD 617** via headed Chrome `--use-angle=metal`. A real-time sim/strategic loop needs an EMPIRICAL probe (sample state over frames), not just screenshots. Stale server: `lsof -ti tcp:8765 | xargs kill`.
- **Perf target:** smooth on the Intel UHD 617 at Auto tier; instancing/LOD/perf-tiers mandatory.
- **CRITICAL gotchas:** `G`/`HEX`/`PALETTE`/`THREE_BASE` are bare-name lexical globals (never `window.G`); THREE loads async (no top-level `new THREE.*`); override-by-redeclaration (last def wins); never a literal `*/` in a block comment; honor `reduceMotion` + `gfxQuality`; **NEVER regress Classic**; $0/free-CC0; Verified/Inferred provenance on all history.
- **Asset pipeline (historical run-H note; current top block supersedes live setup):** PD images via the run-h portrait pattern (Wikipedia REST summary → Commons license API PD-gate → download → `sips` resize). Extend to weapons/flags/USCT. Blender MCP remains available for future explicit 3D asset jobs (PolyHaven CC0 / Sketchfab), but Blender does not need to stay open unless a Phase-H asset task starts; free Hyper3D trial is drained.

## 8 · WORKING NORMS (Aaron)
- Full autonomy; build continuously; surface **questions only at a genuine design fork** — and Aaron LOVES **popup rounds of 3 questions, Recommended = option 1** (use AskUserQuestion). Aaron tests/feels much later — self-verify everything.
- Terse, direct; recommendation + reason on suggestions; push back on gaps; no emojis unless he uses one.
- The **modes & toggles, all-play-styles** spine governs everything; **protect everything (no cuts)** is the scope ambition (build iteratively, but aim to deliver the full vision).

## 9 · §PASTE (paste as the FIRST message of the new session)
> Build the next phase of "The Civil War" (personal project, NOT MJI; full autonomy, Ultracode on, $0/free-CC0, build bar 200/100). Read `HANDOFF.md` fully, then `GRAND-STRATEGY-PLAN.md` (THE design law — 63 locked decisions across Parts I–III + §27 the balance principle), `HISTORICAL-DATA.md` (citation-grade content), `MODERN-UGG-PLAN.md` (the tactical engine, built second), `RUN-LOG.md` §"run h", `REVIEW-QUEUE.md` top, and `GRAPHICS-RUN-CONTINUE.md` §TOOLCHAIN/GOTCHAS. The game is THREE-in-one: UG:G real-time combat feel + Paradox-depth owner-mode grand strategy (you are President Lincoln/Davis) + PhD-level history & alternate-history teaching. THE design law: the simple low-micro UG:G battle is the always-fun core that stands alone; ALL depth is optional (auto-managed by characterful cabinet advisors who also TEACH, delegable per-domain, preset-gated, fluidly dial-able); education is ambient, never blocks, and confers real in-game mastery. BUILD ORDER = STRATEGIC-FIRST: build the full grand-strategy Owner-Mode war + alt-history + teaching layer (GRAND-STRATEGY-PLAN §12 phases S0→S5) by EXTENDING the War Department (`G.campaign.*`), wrapping the existing battle engine via a clean bridge; the real-time UG:G tactical engine (MODERN-UGG-PLAN P0→P5, First Bull Run slice first) swaps in afterward. ARCHITECTURE: modularize now via a tiny zero-dependency node concat build step that assembles the single shippable civil_war_generals.html (set this up in S0); keep the $0/portable one-file deliverable; Classic (hex/turn) is FROZEN — never regress it (`tools/diag-classic.mjs` every change). Gate every change (`.bak` + parse + hex + collision on the built output); self-verify on the real GPU (Intel UHD 617, headed Chrome `--use-angle=metal`) with probes you READ; Aaron tests much later. Honor the bare-name-globals / async-THREE / no-`*/`-in-comments gotchas, the modes-&-toggles spine, Verified/Inferred provenance, and "protect everything (no cuts)." Confirm you've read HANDOFF.md + GRAND-STRATEGY-PLAN.md, give a 3-line plan for S0 (module/build scaffold + President's-desk shell), then build continuously; surface questions only at a genuine design fork (Aaron likes popup rounds of 3 with Rec first).
