# HANDOFF — "The Civil War"

## ⚡ HANDOFF — 2026-07-13 **D392: THE WILDERNESS IS CONTRACTED — THE AXIS-SCOPE LAW, THE THICKET LAW, THE JUNCTION-HOLD + CASUALTY-DIRECTION LAW; NO RUNTIME YET.** `docs/design/wilderness-battle-build-spec.md` (md5 `996508a3325b675fb163fbc11ab3f677`) + `tools/probe-wilderness-plan.mjs` (md5 `aa657d017b6bee143c52eed66cda60b7`; 12/12, dual-mode, filesystem-first, fail-closed, bind-tested) ship the second D382 Overland/attrition planning boundary under Claude/Fable's LANE-003 DRIVE (taken at clean `6840e9b` in ledger commit `fe740c2`; the one commit beyond D391's `6c23082` was a docs-only CLAUDE.md trim, adjudicated as moving no task or decision number). The game is unchanged: 22 scenarios · schema 52 · Army Register 1380 · coverage 22 · suite 127 · sweep 22 · HTML byte-identical `91b9979144731ae3299af4ebaca4628a` (rebuild-verified GATE OK) · base `c9db83fa99230ffb95bdfdfe059f3fb9`. **Selection (from the packet verdicts + the D382 order):** D382's ratified 3.5 names "Wilderness · Spotsylvania's Mule Shoe · a Petersburg trench treatment" — Spotsylvania is discharged (D390→D391), the Wilderness (packet Med-High, no dignity fork) outranks every Petersburg treatment (Crater Med/dignity · initial assaults Med "redundant" · Apr-2 breakthrough Med-Low un-researched · siege Low), so the battle ladder continues; **SURFACED for Aaron: Cold Harbor ranks High in the packet but is NOT in the D382 3.5 lock** — say so at a boundary if it should slot after the Wilderness runtime. Research: a 7-agent workflow (4 Sonnet/med gather → 2 Opus/high default-refute, 82 verdicts → 1 Opus/high critic, 0 errors; the packet carries the §13 addendum). **Rank law (two-family):** Kershaw BRIGADIER commanding McLaws's old division (MG date of rank June 2, 1864 — the exact bind anchor); Gibbon BRIGADIER (MG June 7 — the civilwarintheeast "MG" documented as the backdated-grade class); Gordon BRIGADIER commanding a BRIGADE of Early's division (division May 8, MG May 14); **A. P. Hill Lt. Gen. PRESENT and commanding Third Corps May 5-6** (never Early here — the reverse of the Spotsylvania trap); **Sedgwick ALIVE throughout** (killed May 9 AT SPOTSYLVANIA — the reverse dead-officer guard; no card may date his death inside May 5-7); Stevenson (not Crittenden) at IX 1st Division; Wadsworth m.w. May 6/died May 8 in CS hands; Hays k. May 5; Getty w. May 6; Longstreet wounded ~noon May 6 by his own troops near the Jackson-Chancellorsville ground, Jenkins killed, "Press the enemy" to Field; Burnside independent (incorporation Disputed May 24 vs D390's May 25, never load-bearing). **Design law:** standalone SINGLE PHASE May 5-7, 1864 (id `wilderness`, title "The Wilderness", menu rank 67 between Chattanooga 65 and Spotsylvania 68 — 66 reserved for a possible Mine Run); **THE AXIS-SCOPE LAW** (the ORANGE PLANK ROAD AXIS is fielded — Getty→Hancock→Wadsworth vs Hill→Longstreet→the Sorrel flank grouping; the Turnpike/Saunders Field axis and Gordon's dusk attack are taught, never fielded); attacker CS / defender US (logged deviation from the packet's CAMPAIGN recipe; the standalone models the sourced defensive invariant); objective = the Brock Road / Orange Plank Road junction; **THE JUNCTION GUARD** (US holds ≥5/8 — every family confirms the junction never fell; the May 6 breach retaken within the hour) + **THE AGGREGATE CASUALTY-DIRECTION TOOTH** (US>CS ≥5/8, direction only, never magnitude — the honest split, unlike Spotsylvania's neutral law: US ~17,666-18,000 Verified vs CS Disputed ~8,000-13,000; both Opus refuters recommended it; the anti-winner-bleeds-less class); **THE THICKET LAW** (the blindness was vegetation + powder/brush-fire smoke — a confirmed NEGATIVE on weather fog — and it hurt the numerically superior side, the opposite of the engine's fog grain: encode as symmetric dense-woods cover + honestly LOW deployed-gun counts + fog OFF, never a fog/visibility buff); **THE BURNING-WOODS DIGNITY LAW** (McParlin's ~200 burned/suffocated wounded anchor the card; the fires are never a mechanic, spread sim, spectacle, or scoring lever); envelopes US 15,000-30,000 / CS 12,000-26,000 all Inferred (no source pins axis totals; present-vs-engaged scopes ship Disputed); ten teaching cards + codex Eastern/Overland Campaign/Inconclusive with BOTH result framings taught; USCT accuracy-as-dignity (Ferrero's division guarded trains — no invented combat); D74 wall + woods/blindness/visibility/smoke/brushFire/fireDamage/flank/rollUp/friendlyFire/confusion families; Classic `wilderness` row + strategic rail route + strategic-probe fixtures separate byte-identical layers. **THE SIX NAMED RESHAPE OBLIGATIONS for the runtime slice (spec §11, the D391 insertion class):** probe-kennesaw adjacency ×2 and probe-spotsylvania adjacency ×2 reshaped to the four-battle chronology, probe-spotsylvania's SCOPE forbidden-id regex and forbiddenData filename scan drop `wilderness` while keeping Cold Harbor/Petersburg/Crater — all same-commit with documented history — plus the 1380 pin-bump grep across tools/ (twelve-plus sites). Citation-integrity catches shipped per-fact: ABT "around midday" + "250 of 800" not on the fetched page; the Bearss quote dropped; IX independence re-cited; the EV slug corrected; Wikipedia/EV casualty tables = ONE root. Bind: the one-token Kershaw Brig.→Maj. tamper bit EXACTLY `RANKS + COMMAND TRAPS` (11/12, exit 1) → md5-identical restore to `996508a3…` → 12/12, artifact ok=true. Gate green, every artifact read: GATE OK HTML byte-identical · schema 52/52 (timestamp churn restored) · research 15/15 · Wilderness plan 12/12 · ALL TEN prior plan probes exact-count green · suite 127 · diff-check exit 0 · runtime-scope diff EMPTY. No A/B (no sim input exists in a planning slice). `npm run vet:noreg` deliberately not run (D389 discharged the checkpoint; owed at the 2-3-battle boundary — AT or immediately after the Wilderness runtime, alone on the machine). **LANE-003 is CONTRACT/unowned at this pushed boundary. NEXT: playable Wilderness runtime from this committed contract (spec §11/§14 are the law: data + T1 rank 67 + schema 53 + pins 1380+U×3 + the six reshapes + focused probe with the junction-hold AND casualty-direction batteries + both binds + honest A/B), in a fresh session under a new committed DRIVE lock — then the full release battery, then the D382 ladder continues (Petersburg question / war-career loop, with the Cold Harbor gap awaiting Aaron).**

## ⚡ HANDOFF — 2026-07-13 **D391: SPOTSYLVANIA IS PLAYABLE — SCENARIO 22; THE BREAK-IN EMERGES AND THE DEFENDER HOLDS.** The D390 contract is live as a single-phase May 12, 1864 fight: US attacker (Hancock's II Corps column near full weight — Miles/Brooke/Birney/Mott + Gibbon's disclosed supporting division + 24 guns; Wright's VI Corps as timed reinforcements, Neill @35 with Wright's timed BRIGADIER aura, Russell @100 with the Upton grade nuance) vs CS defender (Johnson's gun-stripped 5,500 on the tip — **ZERO opening guns, THE ARTILLERY-WITHDRAWAL INPUT LAW probe-enforced, captured batteries never re-enter** — plus the sourced piecemeal counterattack: Gordon 3,500 @12 · Ramseur 1,650 @25 · Daniel 1,650 @35 · Harris 1,550 @50 · McGowan 1,550 @65 · the re-formed-line 12-gun/460 grouping @110; Lee and Ramseur field ordinary sourced auras). Committed totals **US 18,300 / CS 15,860** inside the D390 envelopes, all `Verified identity; Inferred strength`. Objective = the salient INTERIOR (McCoull ground, r165, hold 200s, limit 410s — inside the sourced 17-24h day spread); the interior traverse field + rain-mud swales ship as the spec-§4 universal works/cover vocabulary. Menu rank 68 (Chattanooga → Spotsylvania → Kennesaw; probe-kennesaw's TWO stale adjacency teeth reshaped to the true chronology — the D376 class). The §6 rank wall ships verbatim (Wright/Gordon BRIGADIERS, paperwork dates disclosure-only; no Sedgwick/Longstreet/Anderson; the dead Stafford/Jones never in a command seat; Miles/Brooke Inferred-grade). Nine cards + Eastern/Overland Campaign/Inconclusive codex under the Rhea-root two-source rule; prisoners are outputs and teaching only. **THE HONEST A/B (5 logged iterations, every value in D391, enumerated eligible inputs only, never an output key):** 0/8 → 0/8 → 1/8 → 3/8 → 8/8-with-one-gate-red (art crew 500/12 breached the universal ≤40/gun ceiling) → crew-460 root-fix → **FINAL 7/8 defender-holds** (seed 19 US by hold — the spec's own honest near-run spread); CASUALTY-DIRECTION-NEUTRAL holds absolutely (no per-side casualty tooth exists). Integration: **22 scenarios · schema 52 · Army Register 1380 (18 ids ×3; the documented D391 fragment at ELEVEN whole-registry pin sites incl. probe-five-forks' registry-COUNT 21→22 and the women-arc plan's triple pin) · flags/weather/Intel/media 22 · suite 127 · sweep 22 · T10 `E/true/anv`** · Kennesaw keeps the 17-unit largest-scene crown (Spotsylvania opens at 10) · Classic `spotsylvania` row + strategic rail route byte-identical (probe-asserted). Binds on the final candidate: registry removal → plan FUTURE-INTEGRATION only + EXACTLY four registration-dependent focused teeth (direction battery green by design); the one-token Wright Brig.→Maj. tamper → plan RANKS only + EXACTLY the focused RANK WALL tooth; both restores md5-identical (T1 `a44f9915bbfbcfcb25e548dfca8a0fdf` · data `152a62767b4dbc68483096723a8fc8b0` · HTML `91b9979144731ae3299af4ebaca4628a`). Gate green, every fresh artifact read: GATE OK · schema 52/52 · research 15/15 · plan 12/12 · focused 16/16 (0 pe) · roster 8/8 · builder 15/15 · loot@1380 · flags 48/48 · weather 30/30 · Intel 26/26 · media 13/13 (standing soft warning) · vet --list 127 · chattanooga 16/16 + kennesaw 11/11 · ALL TEN plan probes · diff clean · base `c9db83fa99230ffb95bdfdfe059f3fb9` unchanged. **Full `npm run vet:noreg` deliberately deferred: D389 discharged the release checkpoint one day ago and Spotsylvania is the first battle since — the next serialized battery is owed at the 2-3-battle checkpoint, alone on the machine. LANE-003 is CONTRACT/unowned at this pushed boundary. NEXT: the D382 ladder continues — the next Overland/attrition slice (Wilderness/Cold Harbor/Petersburg remain packet lanes needing spec-first contracts) or the war-career loop, per the D382 order, under a fresh committed DRIVE lock.**

## ⚡ HANDOFF — 2026-07-13 **D390: SPOTSYLVANIA "BLOODY ANGLE" IS CONTRACTED — THE SAME-DAY PROMOTION SEAM, THE GUN-STRIPPED TIP, THE CASUALTY-NEUTRAL LAW; NO RUNTIME YET.** `docs/design/spotsylvania-battle-build-spec.md` (md5 `84f458f3494001f37886161001827764`) + `tools/probe-spotsylvania-plan.mjs` (md5 `8cc219b748dbb23a2797a37afaf29cba`; 12/12, dual-mode, filesystem-first, fail-closed, bind-tested) ship the first D382 Overland/attrition planning boundary under Claude/Fable's LANE-003 DRIVE (taken at clean `3ba2c93` in ledger commit `a76dcd4`). The game is unchanged: 21 scenarios · schema 51 · Army Register 1326 · coverage 21 · suite 126 · sweep 21 · HTML byte-identical `21544e26c8871bc47e26ff117cce1f32` · base `c9db83fa99230ffb95bdfdfe059f3fb9`. Research: a 6-agent workflow (4 Sonnet/med gather → 2 Opus/high default-refute; 86 claims, ~40 verdicts, 0 errors; the attrition packet carries the §12 addendum) resolved every §9 spec-time gap. **Rank law:** Anderson MAJOR GENERAL for all of May 8-21 (temp Lt. Gen. only May 31, never confirmed by the CS Congress — the exact bind anchor); Wright Brig. Gen. with his MG dated **May 12 itself** (same-day trap, disclosure-only); Gordon Brig. Gen. (MG May 14); Sedgwick dead May 9 (the dead-officer guard — VI Corps to Wright only); Longstreet ABSENT (wounded May 6); Early commanding Third Corps for the sick A. P. Hill; Grant Lt. Gen./General-in-Chief-as-role anchored to the FETCHED NARA page (Feb 29 signing/nomination, Mar 2 confirmation; LOC mcc.017 stays bot-403, dropped as load-bearing; ceremony date Disputed Mar 9 vs 10); Johnson (Maj. Gen.) and Steuart (Brig. Gen.) captured; the dead Stafford/J. M. Jones never alive on May 12. **Design law:** standalone SINGLE PHASE, May 12 1864, id `spotsylvania`, menu rank 68 between Chattanooga 65 and Kennesaw 70, fog OFF (the sourced pre-dawn fog aided the ATTACKER; the engine's fog model aids the DEFENDER — rain/fog ship as cards); attacker US (Hancock II + Wright VI as timed reinforcements) vs defender CS (Ewell + the sourced piecemeal counterattack — the D90 recipe); objective anchored on the salient's INTERIOR ground so break-in-without-breakthrough reads honestly; **THE ARTILLERY-WITHDRAWAL INPUT LAW** (22 of 30 guns pulled May 11, recalled too late, ~20 captured — encoded ONLY as low initial CS gun counts, never a surprise bonus); envelopes US 14,000-25,000 / CS 8,000-16,000 all Inferred (no source pins axis totals; the 12-regiment/4,500 figure is UPTON'S MAY 10 and is an explicit conflation trap); **CASUALTY-DIRECTION-NEUTRAL** (US ~9,000 vs CS ~8,000-incl-~3,000-prisoners is not robust to prisoner counting — the Opus refute recommendation adopted verbatim); the single 8-seed guard is DEFENDER ULTIMATELY HOLDS ≥5/8; D74 wall extended with melee/hand-to-hand/prisoner/capture families; the Rhea single-root disclosure controls citation tiering; Classic `spotsylvania` row + strategic rail route are separate byte-identical layers (the shiloh/franklin same-name convention); the Crater and Cold Harbor stay outside. Bind: the one-token Anderson Major→Lieutenant tamper bit EXACTLY `RANKS + COMMAND TRAPS` (11/12, exit 1) → md5-identical restore → 12/12, artifact clean. Gate green, every artifact read: GATE OK HTML byte-identical · schema 51/51 · research 15/15 · Spotsylvania plan 12/12 · all nine prior plans exact-count green · suite 126 · diff-check exit 0 · runtime-scope diff EMPTY · all five D389 hashes held. No A/B (no sim input exists in a planning slice). **LANE-003 is CONTRACT/unowned at this pushed boundary. NEXT: playable Spotsylvania runtime from this committed contract (spec §11/§14 are the law: data + T1 rank 68 + schema 52 + pins 1326+U×3 + focused probe with the defender-holds battery and the casualty-neutral anchor + both binds + honest A/B), in a fresh session under a new committed DRIVE lock; then the D382 ladder continues.**

## ⚡ HANDOFF — 2026-07-13 **D389: THE ELKHORN RELEASE IS VERIFIED — 126/126; EVERY ARTIFACT READ; LANE-003 RELEASED.** D388's playable commit is `67f9672a6ff8c734c7f0ec6fa385fb7d5ad700e8`. The complete manifest passed in four exact-label segments, 77 + 14 + 11 + 24 = 126 eventual green commands, serialized and alone. Three honest release catches shipped without touching runtime/data/simulation inputs: Arms keeps the required full PNG with a 240-second screenshot window, bounded page/browser cleanup, and a 600-second suite allowance (23/23); Tripo's strengthened tooth now measures the shared formation-instance slot and proves every duplicate base inactive/absent while GLB/flag/ring remain (15/15); NMH now guards the real Gettysburg → NMH → Fort Donelson → Elkhorn → Shiloh registry and DOM order (14/14). Audit: 125/125 expected JSONs fresh by owning segment and parseable, `ok:false` 0, pageerrors 0, realErrors 0; schema 51/51 with Elkhorn `2 phases, CS vs US` PASS; sweep 21×8=168 rows with `failures:[]`; 149 PNG + 4 JPEG artifacts decode and all 50 JSON-referenced images are fresh/present/decodable; Army Register 1,326; suite 126. Standing notices only: media 2.418 MB soft tier, eight disabled Tripo slots pending optimized local files/license proof, headless AudioContext/multiple-Three.js/WebGL ReadPixels messages, and the frozen optional-resource 404 class. Tactical visuals records one post-green Shiloh 3D browser-close cleanup timeout; its scene/page/lifecycle/image and all later scenes are green. Elkhorn remains 8/8 on all four direction guards, with all five release hashes unchanged. **LANE-003 is CONTRACT/unowned. No D382 work has started. Exact next: take a fresh DRIVE lock and complete the planning-only Spotsylvania / Bloody Angle contract from `docs/design/battle-build-research/1864-65-attrition-battle-build-research.md`: re-verify its spec-time OOB/rank/source gaps, write the standalone single-phase spec plus a dual-mode bind-tested plan probe, gate/commit/push, and stop before runtime.**

## ⚡ HANDOFF — 2026-07-12 **D388: ELKHORN TAVERN IS PLAYABLE — SCENARIO 21; BOTH DAYS TURN ON GEOGRAPHY, TIME, AND SUPPLY.** The D387 contract is live as a two-phase T8 role reversal: `Elkhorn Tavern - March 7` (w1, CS attacker, US defender, disclosed operational-surprise fog) → `Curtis's Counterattack - March 8` (w3 DECISIVE, US attacker, CS defender, fog off). Home edges expose Van Dorn's move into the Union rear; the Camp Stephens mistake exists only as per-phase T4 supply-train positions; the transition teaches Greer's remnant and Curtis's four-division re-formation without scoring either. P1 totals are US 4,030 / CS 5,250; P2 US 7,950 / CS 5,050; March 8 guns US 21 / CS 12. Fifteen unique unit ids add 45 Army Register rows, 1,281→**1,326**. Every strength remains Inferred, every battle-date rank lock holds (Curtis Brig. Gen. twice), and the Leetown/Native absence guard stays executable while ten teaching cards preserve the omitted Native history. Integration is **21 scenarios · schema 51 · coverage 21 · suite 126 · sweep 21 · menu rank 49 · T10 `TM/false/first-national`**; Classic `peariver` and rail remain untouched. First authored inputs passed all four direction guards **8/8** with zero tuning. Bind A produced only plan FUTURE + exactly eight registration-dependent runtime reds; Bind B produced only plan FUTURE + RANK + NAME LOCKS red; exact restores returned plan **14/14** and runtime **16/16**, 0 pageerrors. Final hashes: T1 `0ea7ea9e87bc417da4a3b5b6e2ad29ed`; data `6798671e0e3d46a3d9c30e655198fdd0`; focused probe `c6654e9298c23317698d706b60407f21`; generated HTML `21544e26c8871bc47e26ff117cce1f32`; base `c9db83fa99230ffb95bdfdfe059f3fb9`. Focused/adjacent gate green: node-check 18/18 · GATE OK · schema 51/51 · research 15/15 · roster 8/8 · builder 15/15 · loot 12/12@1326 · flags 48/48 · weather 30/30 · Intel 26/26 · media 13/13 standing soft warning only · Fort Donelson 15/15 · Shiloh 31/31 · suite 126; every artifact read. **LANE-003 is ChatGPT/Codex-owned VERIFY. Exact next: run all 126 `npm run vet:noreg` commands serialized and alone, audit every fresh artifact, record the release, and return the lane to CONTRACT/unowned. Do not begin the D382 Overland/attrition extension before that release.**

## ⚡ HANDOFF — 2026-07-12 **D387: ELKHORN TAVERN IS CONTRACTED — THE HONEST AXIS, THE AMMUNITION LAW, THE LEETOWN ABSENCE GUARD; NO RUNTIME YET.** `docs/design/elkhorn-tavern-battle-build-spec.md` (md5 `075a6c7c755697d0ac36959c4d1ea67f`) + `tools/probe-elkhorn-tavern-plan.mjs` (14/14, dual-mode, fail-closed, bind-tested) ship the M4 planning boundary under Claude/Fable's LANE-003 DRIVE (taken at clean `1753f51` in ledger commit `377c456`). The game is unchanged: 20 scenarios · schema 50 · Army Register 1281 · coverage 20 · suite 125 · HTML byte-identical `a9b42b69c1c735b81fff7c9c878c1bc0`. Research finished ACROSS Aaron's pause: 11/12 agent results recovered from the stopped `wf_1a2d6b8d-36e` journal per the junk-stub law, the resume's cache-churn re-runs stopped, and a minimal continuation workflow (`wf_448b7f8c-07c`) ran the missing native-carveout refute + completeness critic — ~66 verdicts, 0 errors, Fable adjudicating all. Locked shape: two-phase T8 ROLE REVERSAL — `Elkhorn Tavern - March 7` (w1, CS attacker, per-phase fog ON as the DISCLOSED operational-surprise abstraction: no literal weather fog is sourced) → `Curtis's Counterattack - March 8` (w3 DECISIVE, US attacker, fog OFF); weights [1,3] sum 4 — **the D92 phase-weight audit was written down first and the packet's weighting PASSES it** (unlike Fort Donelson's). Home edges inverted (CS NORTH from Cross Timber Hollow, US SOUTH toward Little Sugar Creek). Menu rank `elkhornTavern:49` between fortDonelson:48 and shiloh:50. **THE AMMUNITION LAW:** the deciding fact — the reserve train mistakenly ordered back to Camp Stephens, ~12 miles/six hours away — encodes ONLY as T4 per-phase `supply` train POSITIONS (the shipped FD pattern) + readiness inputs; the D74 wall adds the ammoPenalty/supplyMult/exhaustionMult/surpriseBonus/envelopmentMult families. **The rank wall is the war's densest colonel trap:** Curtis Brig. Gen. (MG date-of-rank Mar 21 FOR the victory); Osterhaus/Jefferson C. Davis/Carr/Dodge/Vandever ALL COLONELS (Carr's BG backdated to Mar 7, appointed April; MoH Jan 16 1894); Price CSA Maj. Gen. dated **March 6, 1862 — the eve of battle** (two-source; the packet's MSG string is Wilson's-Creek-only); **Col. William Y. Slack** (2nd Missouri Brigade — the POSTHUMOUS brigadier trap: died Mar 21, commissioned BG Apr 17 to date Apr 12); **Frost the REVERSE trap** (already CSA BG Mar 3 commanding MSG divisions); Little/Gates/Greer colonels; Watie a COLONEL (1864 star never backdated). Direction law: 8 seeds ≥5/8 — P1 CS seizes · P2 US seizes · aggregate US · aggregate casualty DIRECTION only CS>US (1,384 firm vs the honest ~2,000-2,500 range; **no per-phase casualty tooth — no per-day split exists**). **THE LEETOWN ABSENCE GUARD is executable** (Fort Pillow pattern): no `data/leetown*`, no Leetown/Wilson's-Creek/Glorieta registry line, a Native-formation name-scan over every future OOB unit row — while teaching cards 8-10 (Leetown, Native agency, the scalping + its weaponization) are MANDATORY: dignity-by-design, not erasure. Critic carve-out verdict: HIGHLY FEASIBLE (the Native role was at Leetown, two miles off-axis; the Mar 8 Big Mountain skirmish was peripheral; the judgment ships Inferred as an argument from silence). Refute catches this pass: the scalping/Pike cluster's EOA misattribution (ABT-only ← Shea, ships Inferred), Gates's tactical details REFUTED, Welfley's Knoll un-refuted (the named 21-gun ridge vs 12 CS guns — sourced day-2 counts), and the Shea single-scholar disclosure now controls the register's two-FAMILY rule. Packet §12 addendum committed (research 15/15). Bind: Curtis lock tamper → EXACTLY `RANKS + TRAPS` red (13/14) → md5-identical restore → 14/14. Gate green, every artifact read: GATE OK (HTML byte-identical) · schema 50/50 · ALL NINE plan probes green (14/13/12/11/11/11/10/8/10) · women focused 13/13 @11-lock 0 pageerrors · suite 125 · diff clean. **LANE-003 is CONTRACT/unowned at this pushed boundary. NEXT: D388 playable Elkhorn Tavern runtime from this committed contract (data + T1 rank 49 + schema 51 + pins 1281+U×3 + focused probe + both binds + honest A/B), then the D382 ladder (Overland/attrition extension). Full `npm run vet:noreg` owed at the next release checkpoint (D384 residual + D386 pin edits + this slice), alone on the machine.**

## ⚡ HANDOFF — 2026-07-12 **D386: THE WOMEN-IN-WAR ARC IS PLAYABLE — FOUR DOCUMENTED LIVES, CHAPTER BY CHAPTER, UNDER THE REGISTER LAW.** The Campaign Kit's Women in the War lane now plays: Edmonds (8 chapters), Cashier (7), Clayton (5), Barton (8) — a WCAG-AA chapter stepper (T29 disclosure pattern, aria-current steps, aria-live body, ephemeral state — nothing rides the save) with per-chapter provenance chips, dispute notes, chapter sources, documented/claimed tie banners at bullrun1/malvernHill/antietam/fredericksburg/fortDonelson/stonesRiver/vicksburg, and a read-only played-ground reflection from `C.completed`/journey career rows. Every D385 §4 adjudication ships in the copy: Cashier he/him with the historiographical split surfaced (+ the unsupported "Irene" scrubbed); Clayton the documented mystery (generic negative finding, the wound-location self-report conflict, no husband name, no death scene); the Edmonds Antietam chapter TEACHES the memoir-vs-regimental-record contradiction; Barton's Andersonville chapter carries the NPS myth-correction + Atwater credit, and her sleeve-bullet is "by her own account" (shipped-record correction, flagged). Data: `data/women-in-war.json` 9→**11** (9V/2D); importer arc law (register law: documented ⇒ Verified stage; Clayton ties claimed forever) negative-proven on four violation classes; NEW `src/39-women-war-arc.js` + typeof-guarded 38 seam (byte-identical without module/arc data, probe-proven). Pins with documented history at all three sites: focused probe 11/9/2 (+ Clayton joins the contested/Disputed filter locks), under-told `women-d153-lane` text + tooth same-commit. Baselines HELD: 20 scenarios · schema 50 · Army Register 1281 (live) · coverage 20 · suite 125; HTML `a9b42b69c1c735b81fff7c9c878c1bc0`. Runtime bind: `wiwArcSectionHTML` stub → EXACTLY the five declared arc-DOM teeth red → md5-identical restores → **13/13**. Serialized gate green, every artifact read: GATE OK · importer 11/9/2 · schema 50/50 · arc plan probe 10/10 INTEGRATION (fail-closed) · focused 13/13 (0 pe, 0 leaks) · under-told 8/8 · loot 12/12@1281 · roster 8/8@20 · seven battle plan probes ALL OK · diff clean. No A/B — no sim input exists in this arc. **LANE-003 is CONTRACT/unowned at this pushed boundary. NEXT: the M4 ELKHORN TAVERN SPEC — non-Leetown axis ONLY (D359 dignity carve-out: no Leetown Native OOB; Native participation taught, never fielded) — from `docs/design/battle-build-research/trans-mississippi-battle-build-research.md`, spec-first with a bind-tested plan probe. Full `npm run vet:noreg` owed at the next release checkpoint (D384 residual + the D386 pin edits).**

## ⚡ HANDOFF — 2026-07-12 **D385: THE WOMEN-IN-WAR ARC IS CONTRACTED — FOUR DOCUMENTED LIVES, THE REGISTER LAW, NO RUNTIME YET.** `docs/design/women-in-war-arc-spec.md` (md5 `6348e1f9a592118b4f26a007e75561c7`) + `tools/probe-women-in-war-arc-plan.mjs` (10/10, dual-mode, fail-closed, bind-tested) ship the M3 planning boundary under Claude/Fable's LANE-003 DRIVE (taken at clean `007cbc0` in ledger commit `c33d92a`; Phase-0 adjudicated 17 stat-stale paths blob-identical to HEAD, nothing discarded). The game is unchanged: 20 scenarios · schema 50 · Army Register 1281 · coverage 20 · suite 125 · women lane 9 records (8V/1D) · generated HTML byte-identical `22e3ca1360a7260070b69301acea1348`. Locked shape: an interactive documented-chapter walk-through per figure INSIDE the D153 lane (Campaign Kit) — chapter stepper + read-only played-ground reflection from `C.completed`/journey career log; per-record `arc` blocks in `data/women-in-war.json` (top-level schema untouched); NEW guarded `src/39-women-war-arc.js` + typeof-guarded 38 seam; ephemeral chapter state (D360 nothing-rides-the-save). THE REGISTER LAW is the honesty device: battle-tied chapters carry `documented|claimed` registers; `documented` legal ONLY on Verified stages; Clayton's fortDonelson/stonesRiver ties claimed FOREVER; shared nodes carry per-figure registers (Antietam: Barton documented beside Edmonds claimed — the contrast IS the teaching). Research: 9-agent gather→default-refute→critic (`wf_527a8c0d-3ed`, 0 errors) — the refute caught a misattributed-quote fabricated-citation defect (NARA→NPS), REFUTED Edmonds-at-Antietam against the regimental record (NPS Battle Units UMI0002RI/Dyer, main-loop verified), REFUTED the Clayton prop-uniform forensics, and downgraded every memoir-only beat. TOP-LOOP adjudications binding on D386 (spec §4): Cashier he/him AFFIRMED with the historiographical split surfaced to the player (+ the unsupported "Irene" scrub); Clayton = DOCUMENTED MYSTERY (photographs Verified, service an unverifiable claim, generic Blanton & Cook negative finding only, wound location itself a self-report conflict); the Edmonds memoir wall (Verified spine outranks memoir everywhere); the Barton corrections (Dunn/Antietam nickname, sleeve-bullet as her own account, MANDATORY Andersonville myth-correction + Atwater credit, Red Cross 1881). Bind: §4.1 pronoun tamper → EXACTLY the ADJUDICATIONS tooth red (9/10) → md5-identical restore → 10/10. Gate green, artifacts read: GATE OK (HTML byte-identical); plan 10/10; all seven battle plan probes ALL OK; focused women probe 8/8 @9-lock, 0 pageerrors, staticScan 0 leaks; diff clean. **NEXT (same Fable DRIVE): D386 playable arc — 2 new records (Edmonds Verified, Clayton Disputed) moving 9→11 / 8V/1D→9V/2D across ALL THREE pin sites (focused probe, under-told data text, under-told tooth — same commit, documented-history comments), arc blocks ×4 per spec §5, importer arc law, the 39-module + 38 seam, WCAG AA per §7, both runtime binds, full §9 gate. Then M4 Elkhorn Tavern non-Leetown spec.**

## ⚡ HANDOFF — 2026-07-12 **D384: FORT DONELSON IS PLAYABLE — SCENARIO 20; THE RIVER WAR ENTERS AS INPUTS AND TEACHING.** The D383 contract is live: three-phase T8 — `The Investment - Feb 13` (w1, US attacker under cautious doctrine, CS holds) → `The Breakout - Feb 15` (w1, CS ATTACKER, CS seizes the Forge Road Exit) → `The Recapture - Smith's Assault` (w3 DECISIVE, US carries the vacated 30th-Tennessee-held pits); weights [1,1,3]; fog off; menu rank 48 between New Market Heights and Shiloh; the Feb 14 naval repulse is the phase-2 transition-card interstitial (four ironclads beaten at close range; the transports' fresh division as the river's real gift) and the recall teaching rides phase 3's card. 27 unique ids field committed slices inside the corrected US 24,531 / CS 16,171 anchors (P1 12,850/11,420 · P2 13,800 US def / 12,900 CS atk · P3 7,450/4,850) and add 81 Army Register rows, 1200→**1281** — all seven whole-registry pins bumped same-commit, including the gate-caught undocumented five-forks registry-COUNT pin (19→20). Schema 50; flags/weather/Intel/media 20; suite 125; T10 `W/false/first-national`. Honest A/B converged in three logged iterations (P2 CS 0/8 → 1/8 → 3/8 → 7/8 via objective geometry, the sourced piecemeal-defense timeline, the pre-dawn approach line, and holdToWin 120→90 — never a multiplier); final battery **P1 CS 8/8 · P2 CS 7/8 · P3 US 8/8 · aggregate US 8/8** with NO casualty-direction tooth anywhere. Grant's phase-2 absence is a timed leader arrival (inactive at dawn, active at 170s). Bind A (registry removal) → plan FUTURE + exactly 8 dependent focused teeth red, SAME-SEED reconciled green; Bind B (Grant-grade tamper) → plan FUTURE + exactly the RANK tooth red; restores md5-identical (T1 `ec051a8b2126d3f0e83853d58b3e224a`, data `4d88587ede4394d09506eabfadd41309`, HTML `22e3ca1360a7260070b69301acea1348`). Serialized gate green, every artifact read: GATE OK; schema 50/50; research 15/15; FD plan 13/13; FD focused 15/15 (0 pe); roster 8/8@20; builder 15/15; loot 12/12@1281; flags 48/48@20; weather 30/30; Intel; media 13/13 (standing soft warning); suite 125; adjacents five-forks 16/16 · stones-river 13/13 · shiloh 31/31; diff clean. Residual: the cedar/cross-keys/gaines/NMH pin edits are assert-verified + syntax-green and run at the next release checkpoint. **NEXT (same Fable DRIVE, D382 ladder): M3 Women-in-War playable arc (research → spec-first with plan probe → playable slice; D153 lane law — women NEVER collapse into ss: replacements), then M4 Elkhorn Tavern non-Leetown spec. Full `npm run vet:noreg` at the next 2-3-battle checkpoint.**

## ⚡ HANDOFF — 2026-07-12 **D383: FORT DONELSON IS CONTRACTED — LAND+RIVER AS INPUTS, THREE PHASES, A REWEIGHTED DECISIVE; NO RUNTIME YET.** `docs/design/fort-donelson-battle-build-spec.md` (md5 `4a8c4e226e4224e1486383f806782118`) and `tools/probe-fort-donelson-plan.mjs` (13/13, dual-mode, bind-tested) ship the planning-only D383 boundary under Claude/Fable's LANE-003 DRIVE (taken at `257bea8` in ledger commit `87a82a8`; Aaron's chartered mega run). The game remains at 19 scenarios, schema 49, Army Register 1200, coverage 19, suite 124, generated HTML `10a64a20394521efdc94b7edb1646686`. Locked shape: three-phase T8 — `The Investment - Feb 13` (w1, US probes repulsed, CS holds) → `The Breakout - Feb 15` (w1, CS attacker opens the Forge Road / Wynn's Ferry exit, CS seizes) → `The Recapture - Smith's Assault` (w3 DECISIVE, US carries the vacated 30th-Tennessee-held right, US seizes); weights `[1,1,3]` deviate from the packet's `[1,3,1]` because the sourced leans under the packet weighting would flip the aggregate against the sourced Union victory (the D92 catch of this spec). The Feb 14 naval repulse is a Phase-2 transition-card TEACHING INTERSTITIAL (Stones River Jan-1 pattern); gunboats enter ONLY as inputs (transports → Lew Wallace's division as sourced reinforcement timing) and the water batteries are terrain/teaching, never land OOB — no ship-vs-ship engine. Strength anchors CORRECTED from the packet: US 24,531 / CS 16,171 engaged (ABT's 40,702 is the combined total — the packet misread it; §12 addendum recorded). Rank wall: Grant Brig. Gen. (MG ranked FROM Feb 16, no bare date ships), Floyd/Pillow/Buckner/Bushrod Johnson Brig. Gens., Forrest Lt. Col., Foote Flag Officer, colonels stay colonels; Buckner's OR letter reads "post," not "fort." Direction law: 8 seeds, ≥5/8 on four sourced guards, NO casualty-direction tooth anywhere (US bled more in k/w AND won — the accounting conflict is the teaching). Research: 13-agent workflow (6 Sonnet gather → 6 Opus default-refute → 1 Opus critic), ~57 verdicts, ZERO REFUTED, all AMENDs applied. Bind: first tamper exposed the §13-quotation weak tooth → hardened to section-6 scope → exactly `RANKS + TRAPS` red (12/13) → md5-identical restore → 13/13. Gate: GATE OK (HTML byte-identical); schema 49/49; research 15/15; all seven plan probes green; Five Forks runtime 16/16; roster 8/8 (one load-11 fonts-stall re-run green solo); builder 15/15; suite 124; diff clean; 0 pageerrors. **NEXT: D384 playable Fort Donelson runtime from this committed contract — same Fable DRIVE session if usage allows (the mega-run charter relaxes D171), else the continuation prompt's zero-context packet. Then the D382 ladder: Women-in-War arc → Elkhorn Tavern spec (non-Leetown) → Overland extension.**

## ⚡ HANDOFF — 2026-07-12 **D381: THE RELEASE CHECKPOINT IS VERIFIED — 124/124, EVERY ARTIFACT READ; LANE-003 RELEASED.** Claude/Fable took the Codex VERIFY transfer at clean `2f3da4a`, resumed the serialized `npm run vet:noreg` battery exactly at command 38, and completed 124/124 across evidenced `.tmp` segments with nothing running beside it. The one red — `tactical visuals` at its 600s budget with 9/10 scenes already green — was root-caused to slow-Mac battery load (kill artifact on scene 10; historical greens 445-507s), re-ran standalone 10/10 with fresh PNGs, and the battery resumed at command 99; no probe weakened. Full artifact audit: 123/123 suite JSONs clean; sweep **19 scenarios × 8 seeds = 152 rows, all finished, `failures:[]`**, direction summaries matching every shipped law; schema 49/49 with the tracked HTML at exactly 49 body rows; 147 fresh PNGs decode; representative visuals inspected. The eighth bootprobe 404 (`ford_albedo.png`) was investigated and confirmed as the frozen engine's documented absent-optional-asset class (base.html per-terrain-key texture composition with explicit 404 fallback; contract reads "~7"). Three transfer-era plan-probe reds were greened in the D381 commit itself per the relay hard rule: the five-forks and gaines LANE teeth were reshaped off current-lock-holder anchors onto the durable serialization phrase + role-roster owner checks, cedar-creek's dropped durable history sentence was restored in the lane text, and the loot probe's dropped `D362: 912 -> 957` pin-history fragment was restored (comment-only). Bind A → exactly five-forks LANE red; Bind B → exactly the two reshaped teeth red; restores md5-identical `ae574598c2e0a1fd2d88d8b203260052`; finals 12/12 · 8/8. Final gates: build **GATE OK**, HTML md5-identical `10a64a20394521efdc94b7edb1646686`; all six plan probes green; Five Forks runtime 16/16; diff clean. Pins held: **19/49/1200/19/124**; zero simulation inputs changed; Classic/rail/T8/Custom Builder/Appomattox boundaries untouched. **NEXT: LANE-003 is CONTRACT/unowned — Fort Donelson SPEC ONLY from `docs/design/battle-build-research/naval-river-battle-build-research.md` (land+river gunboat support, no ship-vs-ship engine), runtime in a later bounded slice. Aaron's 2026-07-12 popup Q&A ratified the D382 forward slate (career loop, sandbox, surrender/no-quarter consequence-only, four system-integrated teaching threads, naval engine, Overland extension, onboarding, media/audio session) — read D382 before choosing any post-ladder work.**

## ⚡ HANDOFF — 2026-07-12 **D380: FIVE FORKS IS PLAYABLE; SCENARIO 19 AND GENERIC ATOMIC RELIEF ARE GREEN.** The D379 single-field contract is live as a US attacker-seize fight for Five Forks Crossroads, fog off, menu rank 85 after Nashville. Ten unique ids field 21,000 US / 9,200 CS and add 30 Army Register rows, 1170→1200; schema 49; flags/weather/Intel/media cover 19; suite 124. Shared T3 validates own-`replaces` rows before fate RNG, validates a whole due batch before aura accumulation, and atomically leaves Warren alive+relieved+inactive while Griffin becomes the sole active aura. Invalid raw or event-time states reject terminally, all-or-none, without throw, announcement, mutation, or extra RNG; legacy leaders remain byte-identical. Relief has distinct HUD/selected/2D/3D/AAR presentation and writes no result. White Oak Road, the Angle, the road-axis variant, Gravelly Run, and Hatcher's Run shape the field; Dinwiddie and shad-bake blame stay teaching; Appomattox stays nonplayable. Griffin is Brig. Gen. (brevet Maj. Gen.); Pickett/Fitz Lee/Rosser remain absent auras. First authored inputs passed both source guards 8/8 with zero simulation-input changes; one teaching-only Dinwiddie term was added. Bind A made only three generic relief teeth plus focused COMMAND EVENT red; Bind B made only the plan future-integration tooth plus four registry/menu/launch-dependent focused teeth red. Both restores returned T3 `7c3b0924c94e6f450e2fd491726a022f`, T1 `85c12c00440499a7bddc67060e9913fc`, data `380150cee52d99f7e10cbe7b45321f1a`, generic probe `da4934bbfc3bdeecca1150aff66045ed`, focused probe `e4da87df501b2ba50bdab4cf6857df27`, and generated HTML `10a64a20394521efdc94b7edb1646686`. Serialized focused gate: GATE OK; schema 49/49; research 15/15; plan 12/12; officers 20/20; Five Forks 16/16; roster 8/8; builder 15/15; loot 12/12 at 1200; flags 48/48; weather 30/30; Intel 26/26; media 13/13 with only the known 2.418 MB soft warning; Cross Keys plan/runtime 11/11·15/15; Bull Run 15/15; Nashville 12/12; every artifact read, no failed steps/pageerrors/realErrors, diff clean. **NEXT: LANE-003 remains ChatGPT/Codex-owned VERIFY. Push the D380 playable commit, require clean parity, stop every helper/browser/research process, run `npm run vet:noreg` alone for all 124 commands, read every fresh JSON plus sweep/diag artifacts, then record D381 and release the lane to CONTRACT/unowned. Do not start Fort Donelson.**

## ⚡ HANDOFF — 2026-07-12 **D379: FIVE FORKS IS CONTRACTED; ONE FIELD, GENERIC WARREN-TO-GRIFFIN RELIEF, NO RUNTIME YET.** `docs/design/five-forks-battle-build-spec.md` and `tools/probe-five-forks-plan.mjs` ship the planning-only D379 boundary. The game remains at 18 scenarios, schema 48, Army Register 1170, flags/weather/Intel/media 18, suite 123, and generated HTML md5 `097eabeea06387e47bd819d125950f0d`. Locked shape: one April 1, 1865 single-phase US-attacker / CS-defender fight, fog off, menu rank 85 after Nashville, about 21,000 engaged US against 9,200 CS, and Five Forks Crossroads as the scored objective. White Oak Road, the return / the Angle, road-axis naming variant, Gravelly Run, and Hatcher's Run shape the field; Dinwiddie and the shad bake are teaching-only. The future shared T3 `replaces` seam validates raw cast data before RNG, resolves all due events before aura accumulation, atomically relieves living Warren and activates Griffin with no overlap/gap, distinguishes relieved from fallen across presentation, and rejects every malformed/missing/cross-side/duplicate/chain/repeat case without throw, mutation, announcement, or extra RNG. Missing `replaces` is byte-identical; no battle-name branch, output writer, T8 change, or Custom Builder expansion. Griffin is Brig. Gen. (brevet Maj. Gen.) on April 1. Pickett, Fitz Lee, and Rosser remain absent aura sources. Future direction law: exactly eight seeds; at least 5/8 US seizures and 5/8 CS-total-losses-greater-than-US, with no magnitude or prisoner-count tooth. The Griffin-grade bind made exactly `RANKS + ABSENCES` red (11/12 green), then restored to spec md5 `0caa5bf0bf9777a3a778090cc6030864`; final plan 12/12. Focused gate: GATE OK; schema 48/48; research 15/15; Cross Keys plan 11/11 and runtime 15/15; roster 8/8; builder 15/15; suite 123; every artifact read, 0 failed steps/pageerrors, diff clean. Full `npm run vet:noreg` was not run. **NEXT: LANE-003 is CONTRACT/unowned. Take D380 DRIVE in a ledger-only commit; implement playable Five Forks and the generic shared T3 relief seam from this contract; complete 19/49/`1170+3U`/19/124 integration, both runtime binds, and the full serialized release battery with nothing concurrent.**

## ⚡ HANDOFF — 2026-07-11 **D378: CROSS KEYS / PORT REPUBLIC IS PLAYABLE — 18 SCENARIOS, TWO FIELDS, ZERO OUTCOME TUNING.** The D377 contract is live as Cross Keys w1 (US attacks Ewell's ridge; CS holds; cautious doctrine) → Port Republic w3 decisive (Jackson attacks Tyler and Carroll at The Coaling; CS wins; standard doctrine), fog off with field-specific home edges. Fifteen unique ids add 45 Army Register rows, 1125→1170; schema 48; flags/weather/Intel/media cover 18; suite 123; menu rank 12 sits between Bull Run and Gaines' Mill. Fremont's 11,500 remains army-present, lower splits stay Inferred, the Coaling battery is Unpinned, Jackson is Maj. Gen., Ashby is absent, and no regiment/battery identity or D74 result shortcut was invented. The first authored inputs passed all four direction guards 8/8, so there were zero outcome-tuning A/B iterations. The roster gate caught two excess artillery crew abstractions; crew was transferred into same-phase Inferred infantry groupings without changing totals or gun counts, and the corrected candidate remained 8/8. Registry removal made plan REGISTRY plus exactly eight focused dependent teeth red; the Jackson-grade tamper made plan REGISTRY plus only the focused rank/name/absence tooth red; both restored byte-identically to T1 `468e234a742255811e8f3cf3e5a2920a`, data `143c89fb819f826bb90bdaf7d865905c`, and generated HTML `097eabeea06387e47bd819d125950f0d`. Final serialized gate: GATE OK; schema 48/48; research 15/15; plan 11/11; runtime 15/15; roster 8/8; builder 15/15; loot 12/12; flags 47/47; weather 30/30; Intel 26/26; media 13/13; suite 123; Bull Run 15/15; Gaines 13/13; Malvern 27/27; NMH/Stones/Cedar plans and runtimes green; every required artifact read, no failed steps, 0 pageerrors, diff clean. Full `npm run vet:noreg` was not run. **NEXT: LANE-003 is CONTRACT/unowned; take Five Forks SPEC-ONLY DRIVE from the Appomattox-campaign packet, commit its bind-tested plan probe, then build runtime in a later bounded slice. The full release battery is due after playable Five Forks.**

## ⚡ HANDOFF — 2026-07-11 **D377: CROSS KEYS / PORT REPUBLIC IS CONTRACTED; TWO FIELDS, ONE HONEST VALLEY FINALE.** `docs/design/cross-keys-port-republic-battle-build-spec.md` + `tools/probe-cross-keys-port-republic-plan.mjs` ship the spec-only D377 boundary. No runtime data, registry/menu entry, schema row, Army Register row, generated behavior, or combat input changed. Locked T8 shape: `Cross Keys - Ewell Holds the Ridge` (w1, US attacks/CS defends, cautious doctrine) → `Port Republic - The Coaling` (w3 decisive, CS attacks/US defends, standard doctrine); fog is off and weights sum 4. Source honesty is the load-bearing decision: Fremont's 11,500 is army present, not a Verified committed subtotal, so the modeled 6,000-9,500 commitment and every lower split stay Inferred; the Coaling battery remains a generic Unpinned emplacement and no 7th/9th Louisiana or battery name may be invented. Jackson is Maj. Gen. in this Valley-1862 payload only; Ewell/Fremont are Maj. Gens., Tyler/Taylor/Winder Brig. Gens., Carroll Col., and Ashby is absent after his June 6 death. Future direction teeth are P1 CS hold, P2 CS seize, aggregate CS, and P1 US>CS losses only; no phase-2/aggregate casualty direction or magnitude tooth. Terrain/teaching pin Mill Creek, Ewell's ridge, The Coaling, Lewiston Lane, the South Fork, count-free foot-cavalry history, and the operational-layer limit without Lost-Cause sainthood. D74 bars hesitation/genius/flank/winner multipliers. The dual-mode probe keeps planned-only baselines at 17 scenarios, schema 47, Army Register 1125, flags/weather/Intel/media 17, and suite 122, then fails closed unless future D378 integration reaches 18/48/`1125+units×3`/18/123 completely. The Jackson-grade negative bind made exactly HISTORY red (10/11, exit 1), restored md5-identically to `bbe53c90c2cbb39045d3bc90f7d52518`, then reran 11/11. Focused gate: node checks green; GATE OK with generated HTML byte-identical; schema 47/47; research 15/15; Cross Keys plan 11/11; Gaines 8/8; New Market Heights 10/10; Stones River 11/11; Cedar Creek 11/11; roster 8/8; builder 15/15; every produced JSON read; 0 pageerrors; diff clean. Full `vet:noreg` was not run. **NEXT: LANE-003 is CONTRACT/unowned; take D378 DRIVE in a committed ledger edit, then build playable Cross Keys/Port Republic from this contract.**

## ⚡ HANDOFF — 2026-07-11 **D376: CEDAR CREEK IS PLAYABLE — 17 SCENARIOS, THE FIRST ROLE REVERSAL.** Scenario 17 implements the D375 contract: `Gordon's Dawn Assault` (w1, CS attacker under real river fog) → `Sheridan's Counterattack` (w3 decisive, US attacker in the clear), with side-keyed home edges and no Cedar-specific combat/result fudge. The direction-neutral law is executable: the final 8-seed battery passes P1 CS, P2 US, aggregate US, P1 US>CS phase losses, and P2 CS>US phase losses **8/8 each**. Honest A/B took three documented corrections after the first battery: geometry/hold `0/8`, staged Union re-formation `4/8`, then only Dwight `105→115` + Grover `125→135` for final `8/8`; all values live in D376. Nineteen unique units move the Army Register 1068→1125; schema 47/47; menu order is Kennesaw → Cedar Creek → Franklin; T10 is E/false/anv; flags/weather/Intel/media cover 17; suite is 122; Kennesaw keeps the 17-unit largest-scene crown. Both binds bit correctly and restored md5-identically: registry removal → exactly 8 dependent focused teeth red; Emory grade tamper → exactly 1 focused tooth red. Final serialized gate and every required JSON are green: GATE OK; plan 11/11; Cedar 15/15; roster 8/8; builder 15/15; loot 12; flags 46; weather 30; Intel 26; media 13; Stones 13; Kennesaw 11; Franklin 10; 0 pageerrors; diff clean. Relay sync corrected two stale pins without weakening their substance: Franklin now requires the new three-battle menu order, and Gaines' plan now requires CONTRACT/unowned while retaining the D362 handoff history; all four relay-sensitive plan probes are green. The D375→D376 relay needed zero ambiguity questions, ratifying S-01. **NEXT: LANE-003 is CONTRACT/unowned at this pushed boundary; take a new DRIVE lock and ship Cross Keys/Port Republic SPEC ONLY from `docs/design/battle-build-research/shenandoah-1862-battle-build-research.md`, with a bind-tested plan probe before runtime.** This session crossed compaction during the serialized gate, so S-04's defensible ≥40% threshold was not met; no next spec was started. Full `vet:noreg` remains due after 2-3 stretch battles, not now.

## ⚡ HANDOFF — 2026-07-11 **D375: CEDAR CREEK IS CONTRACTED — THE ROLE-REVERSAL BATTLE; SPEC + 11/11 PLAN GATE.** Cedar Creek is not playable yet; the roster stays 16 through Stones River. D375 adds `docs/design/cedar-creek-battle-build-spec.md` + `tools/probe-cedar-creek-plan.mjs` (**11/11**, bind-tested: the Emory rank-lock tamper went exactly one step red, md5-identical restore). Written entirely from the committed packet §12 (D374) plus one 3-agent spec-time workflow that CONFIRMED **Col. J. Howard Kitching** (NPS + CCBF + NY State Military Museum) and documented his posthumous-brevet trap (backdated to Aug 1 1864 — predates the battle, never encode; died of the wound Jan 11 1865, never KIA). Locked shape: two-phase T8, the game's first ATTACKER/DEFENDER ROLE REVERSAL — `Gordon's Dawn Assault` (w1, CS attacker, per-phase fog ON — the real river fog as a D92 input) → `Sheridan's Counterattack` (w3 DECISIVE, US attacker, fog OFF); **weights sum 4, never 5**; menu rank `cedarCreek:72` (Kennesaw → Cedar Creek → Franklin, by date). THE DIRECTION-NEUTRAL LAW is this lane's headline: **US bled MORE and WON** (all three accountings agree on direction) — NO aggregate casualty tooth in either direction; only phase-scoped D92 guards (P1 US>CS, P2 CS>US, direction-only) beside the three outcome guards. CMH-anchored bounds: P1 CS 12,500-14,500 (Lomax NOT fielded) vs US 27,000-31,610; P2 US 22,000-28,500 vs CS 13,000-19,000. Rank teeth: Emory **Brig. Gen. (Bvt. Maj. Gen.)** (the §12 correction), Early Lt. Gen. (temporary grade), Rosser Brig. Gen. (the NPS OOB label is an ERROR), five Colonels (Thoburn/Hayes/Powell/Keifer/Lowell) + Kitching, format law: brevets follow the name. Teaching: the DISPUTED fatal halt with both primaries ("glory enough for one day" ONLY via Gordon's Reminiscences — probe-enforced attribution), the ride/poem split, "The Burning" teaching-only dignity line, count-free pursuit. D376 obligations pinned: schema 47, loot `1068 + units×3` (+ bump every other whole-registry pin same-commit), flags/Intel/media 17, suite 122, T10 `E/false/anv`, largest-scene check vs Kennesaw's 17, Classic-layer collision grep. Gate: GATE OK (HTML byte-identical); schema ok; research 15/15; cedar plan 11/11; stones plan 11/11; NMH plan 10/10; 0 pageerrors; diff clean. **NEXT: D376 playable Cedar Creek — ROUTED BY AARON (2026-07-11, in-chat) to the chartered ChatGPT/Codex 5.6 Sol Ultra session.** LANE-003 is released to CONTRACT at this boundary (the relay commit also updates the three plan-probe LANE teeth to the new true contract and opens **COORDINATION.md §6, the Joint Strategy Board** — Fable's S-01..S-04 strategy items await Sol's response in the D376 session). Sol takes DRIVE in a committed ledger edit at session start; the kickoff prompt was delivered in Aaron's chat; the full battery is NOT owed again until the next release boundary.

## ⚡ HANDOFF — 2026-07-11 **D373: LANE-003 RELEASE CHECKPOINT GREEN — 121/121, EVERY ARTIFACT READ.** The full serialized `npm run vet:noreg` battery owed since D366 is green: segment 1 (1-18) + `--from='hard war'` (103/103), final `VET NO-REGRESSION OK`. All 120 fresh `tools/shots/*.json` artifacts read: ok:true, 0 pageerrors, 0 realErrors, no failed steps; `sweep-all-battles` proves all **16 scenarios × 8 seeds, failures=[]**; diag-classic green; bootprobe's 7 filtered 404s are the documented absent-optional-assets probe. The ONE red was root-caused, never blind-retried, no probe weakened: hard-war finished all 8 steps green at 84s (artifact mtimes prove it) then hung in `browser.close()` under a research workflow Fable had left running concurrently — **lesson: nothing runs concurrently with the release battery on the 8 GB Mac**; idle rerun 92s green, in-battery rerun 108.1s green. LANE-003's release obligation is DISCHARGED; the lane stays Fable-owned DRIVE for its ratified stretch order: **Cedar Creek → Cross Keys/Port Republic → Five Forks → Fort Donelson → Elkhorn Tavern** (2-3 full-depth beat 6 shallow). Cedar Creek research is DONE (two workflows, 19 agents, 0 errors — CMH staff-ride division strengths incl. Kershaw 3,071, DISPUTED fatal-halt with both primaries, the Emory substantive-Brig.-Gen. correction) and is COMMITTED as the packet's §12 addendum (D374, incl. the Emory Brig.-Gen. §4 correction); next: `docs/design/cedar-creek-battle-build-spec.md` + plan probe, then runtime — the D363-D366 pattern. Sol's queue (E71 → C72; GEA-01 + S44 fallback; LANE-002 5b at 918 rows) waits for a fresh lock at the next clean lane boundary per D372.

## ⚡ HANDOFF — 2026-07-10 **D372: SOL CLOSES CLEAN; FABLE'S RELEASE BATTERY IS NEXT.** D369 shipped the web-sourced 22-feature genre-elite design law and GEA ladder; D370 ran the standing full-spectrum audit and filed 12 confirmed findings (3 HIGH / 8 MED / 1 LOW), with E71 Ironman, C72 Shiloh, and E72 probe integrity at the top; D371 fixed E72 so `menuprobe` now fails honestly (diagnostic exactly one red) and finishes 4/4 green with H0 main menu 5/5, 0 pageerrors. Aaron surfaced 10% remaining immediately afterward, so no second Phase C slice and no prosopography record started. LANE-004 is SHIPPED/released; LANE-002 5b is CONTRACT/unowned at **918** remaining rows; LANE-003 is untouched and still Fable-owned DRIVE. **FABLE NEXT:** `git fetch origin`, require clean HEAD==origin/main at D372 or later, read `START-HERE.md` → all relevant `COORDINATION.md` lanes → this block → D372/D371/D370 → `REVIEW-QUEUE.md`; run the deferred serialized `npm run vet:noreg` **121-command** LANE-003 release battery first, inspect every JSON/pageerror summary, fix root causes, update that owned lane, commit and push. If context remains, honor LANE-003's existing stretch-battle order at full depth. At its next clean bundle boundary, take a new lock before Sol-queue work: E71 first if a full bounded milestone fits; otherwise GEA-01 then S44 as separate small commits. C72 needs its own two-source historical rewrite with no combat/OOB change. Phase D starts only after a batch is sized from 918 and logged before record one.

## ⚡ HANDOFF — 2026-07-10 **D366: PLAYABLE STONES RIVER SHIPPED — 16 SCENARIOS; D367 CHARTERS A FULL CHATGPT 5.6 SOL SESSION; THE RELEASE BATTERY DEFERS ONE SESSION.** Scenario 16 (menu rank 52, Shiloh → Stones River → Vicksburg) implements the D365 contract exactly: two-phase T8 CS-attacker/US-defender — `December 31 - The Dawn Attack and the Round Forest` (w3, 16 opening units: 10 US incl. Hazen's anchor brigade + the 38-gun pike line vs 6 CS + Breckinridge's two crossing groups; 39,600 vs 32,300) → `January 2 - Breckinridge at McFadden's Ford` (w1; 4,500 exactly in the OR-report two-line pairing vs 9,300 with Mendenhall's 57 guns; 0 CS guns). Jan 1 is the Emancipation interstitial on the transition card, never a phase. THE NEAR-PARITY LAW IS EXECUTABLE AND GREEN: first battery 8/8 on ALL FIVE guards — p1 US holds, p2 US holds, p2 CS>US (direction only), aggregate US, and max/min ≤ 1.6 at observed 1.08-1.15 (historical 1.10) — **no input moved, zero A/B iterations**; no tooth anywhere asserts US < CS. All 26 unit notes carry `Verified identity; Inferred strength`. Pre-authoring citation workflow: Wheeler card dropped (single-source), Garesché ships the à-Kempis correction (Wikipedia's "personal Bible" is the rejected flattening), WHN scoped to repulse/guns/45-min with Breckinridge's OR controlling the line pairing, quod.lib retained as canonical primary (bot-403; D365-fetched) beside Wikipedia's quote. Integration: schema 46/46, loot pin 990→1068 (+ gaines/NMH whole-registry pins same-commit), flags 45 with a Stones River W/hardee semantic tooth, Intel/media 16 with **Kennesaw keeping the largest-scene crown (17 vs 16)**, suite 121, roster `stonesRiver:2`, builder; logistics-rail untouched (probe-asserted). Binds: registry-line removal → exactly the 7 registry-dependent teeth red; Polk Lt.→Maj. → exactly ONE tooth red; both md5-identical restores; final rerun plan 11/11 + focused 13/13, 0 pageerrors everywhere; roster's one battery-row flake solo-reran 8/8 green; the focused screenshot adopted the kennesaw best-effort heavy-scene pattern (reproducible fonts-stall; teeth + pageerrors remain the gate). **NEXT: per D367 the full serialized `npm run vet:noreg` (121) runs in the NEXT Claude/Fable session to close LANE-003 (still Fable-owned DRIVE — ChatGPT must not drive it). Meanwhile the chartered ChatGPT 5.6 Sol session runs LANE-004 (genre-elite audit → design law, AUDIT-PROMPT run, cleared elite-basics quick wins) + LANE-002 5b batches, from this pushed HEAD; kickoff prompt in OPUS-PLAYBOOK.md + delivered in-chat.**

## ⚡ HANDOFF — 2026-07-10 **D365: STONES RIVER SPEC + PLAN PROBE SHIPPED; FABLE HOLDS LANE-003 DRIVE.** Stones River is not playable yet; the roster stays 15 through New Market Heights. D365 adds `docs/design/stones-river-battle-build-spec.md` + `tools/probe-stones-river-plan.mjs` (**11/11**, bind-tested — the bind's first pass exposed a substring-tooth weakness and the HISTORY tooth was anchored to the exact trap lines before the tamper bit exactly). An 11-agent research pass (5 Sonnet gather → 5 Opus default-refute → 1 Opus critic, 0 errors) confirmed all 23 battle-date ranks (Polk/Hardee LIEUTENANT generals; Sheridan BRIGADIER with the backdated-MG trap; Hazen COLONEL) and REFUTED a claimed OR brigade-strength table — only Breckinridge's 7,053/~4,500 is OR-sourced; everything else ships Inferred inside the Verified ABT anchors (US 41,400 / CS 35,000, casualties 12,906 / 11,739). Locked shape: two-phase T8, CS attacker / US defender — `December 31 - The Dawn Attack and the Round Forest` (w3 DECISIVE, US holds) → `January 2 - Breckinridge at McFadden's Ford` (w1, US holds sharply; Mendenhall's 45-58 guns as an OOB range tooth); Jan 1 is a TEACHING INTERSTITIAL (Emancipation Proclamation takes effect mid-battle), never a scored phase. THE NEAR-PARITY LAW: US wins while bleeding MORE — aggregate guard max/min ≤ 1.6, FORBIDDEN to force US < CS; the only lopsided guard is phase-2 direction-only. Menu rank `stonesRiver:52` (Shiloh → Stones River → Vicksburg); the Classic `stonesriver` id/rail route is a guarded separate layer. D366 obligations pinned: schema 46, loot `990 + units×3`, flags/Intel/media 16, suite 121, weather rain/dawn (never snow), largest-scene check vs Kennesaw's 17. Gate: GATE OK (HTML md5-identical); schema 45/45; research 15/15; stones plan 11/11; NMH plan 10/10; roster 8/8; builder 15/15; nashville 12/12; 0 pageerrors; diff clean. **NEXT: D366 playable Stones River from this committed boundary, then the full serialized `npm run vet:noreg` release battery before LANE-003 closes.**

## ⚡ HANDOFF — 2026-07-10 **D364: PLAYABLE NEW MARKET HEIGHTS SHIPPED — THE GAME'S FIRST USCT-LED BATTLE; 15 SCENARIOS LIVE.** Fable closed out the green runtime WIP recorded in COORDINATION's LANE-003 pointer (Aaron had stopped the prior session mid-vet). Shipped: `data/new-market-heights.json` (two-phase T8 over the SAME ground — Duncan w1 CS-holds → Draper w3 US-carries vs the `Verified withdrawal order; Inferred residual strength` reduced line), registry/menu rank 45 between Gettysburg and Shiloh, the guarded T13 pre-placed abatis seam (`fldEngSeedScenarioObstacles`, byte-identical no-op without the data key), T10 `E/false/anv` with the AoJ badge limitation documented, schema 45th row, loot pin 957→990 (11 units × 3, documented history), flags/Intel/media 15, suite 120, weather fog/dawn presentation-only. Spec amendment logged: the sourced 22nd USCT skirmish detachment joins phase 1 (≤250; opening OOB 630-770 held, total ≤1,000). Honest A/B in DECISIONS (attempt 0 P2 2/8 FAIL → attempt 1 PASS, all inputs in-bounds). Bind 1 (registry) + bind 2 (T13 stub → EXACTLY the OBSTACLE tooth red → md5-identical restore) both proven. A 3-packet Opus default-refute pass on the new teaching prose applied one AMEND (MoH superlative scoped to Black soldiers) and strengthened the Butler-card sources; Fort Pillow and Butler Medal cards confirmed. Stale 957 pin in probe-gaines-mill bumped to 990 (the documented pin-bump idiom). Gate all green, every JSON read: GATE OK; schema 45/45; plan 10/10; runtime 14/14 (direction 8/8 · 7/8 · 7/8 · 8/8; US wins AND bleeds more); roster 8/8; builder 15/15; loot 12; flags 44; weather 30; Intel 26; media 13 (known soft warning); vet --list 120; gaines 13; nashville 12; field 23; 0 pageerrors; diff clean. **NEXT: Stones River spec-first (near-parity casualty guards — never assume US < CS; Polk/Hardee are LIEUTENANT generals here), then playable; full vet:noreg at the LANE-003 release boundary.**

## ⚡ HANDOFF — 2026-07-10 **D363: NEW MARKET HEIGHTS SPEC + PLAN PROBE SHIPPED; FABLE HOLDS LANE-003 DRIVE.** New Market Heights is not playable yet; the roster stays 14 through Gaines' Mill. D363 adds `docs/design/new-market-heights-battle-build-spec.md` + `tools/probe-new-market-heights-plan.mjs` after a 13-agent research pass (6 Sonnet gather → 6 Opus default-refute → 1 Opus critic, 0 errors) resolved every USCT-packet §9 unknown. Locked shape: two-phase T8 over the same ground — Phase 1 `Duncan's Assault - the Abatis` (w1, CS holds; 4th+6th USCT ~700 vs the full ~1,800-2,000 line with Hardaway's 8 guns) → Phase 2 `Draper Carries the Heights` (w3, US carries; 5th/36th/38th ~1,300 + Holman support vs a `Verified withdrawal order; Inferred residual strength` reduced line). Fort Harrison is TEACHING-ONLY (2,800-vs-200 below brigade grain; garrison commander OR-contradicted); the withdrawal ships as a phase-boundary OOB input — no clock-timed thinning, causation taught as the sourced controversy. Cost guard INVERTS winner-bleeds-less (US wins, US bleeds more). D364 obligations pinned: menu rank 45, T13 pre-placed abatis seam (guarded), T10 E/false/anv, schema 45th row, loot `957 + units×3`, flags/Intel/media 15, suite 120, weather fog/dawn, executable Fort Pillow absence guard, `valorMult`/`heroism` on the forbidden wall. Negative bind: Holman-artifact line → exactly 1 red (9/10, exit 1) → checksum restore → 10/10. Gate: GATE OK (HTML unchanged); schema 44/44; research 15/15; Gaines plan 8/8; NMH plan 10/10; roster 8/8; builder 15/15; Nashville 12/12; 0 pageerrors; diff clean. **NEXT: D364 playable New Market Heights, then Stones River spec-first. Full vet at lane release.**

## ⚡ HANDOFF — 2026-07-10 **D362: PLAYABLE GAINES' MILL SHIPPED; LANE-003 → CLAUDE FABLE 5.** Fourteen scenarios now run through Nashville. Gaines: 15 units (8 opening), 27,000 US / 32,000 CS, 72 / 32 guns, exact ranks, sourced teaching, no D74 output key; first final-input battery CS hold 8/8 and CS killed/wounded ≥ US 8/8; same-seed exact; passive sides terminate; Army Register 957. Impossible 9/8 bind failed exactly then restored. Gate: GATE OK; schema 44/44; plan 8/8; runtime 13/13; loot 12/12; flags 43/43; weather 30/30; Intel 26/26; media 13/13; Malvern 27/27; Nashville 12/12; roster/builder green; zero pageerrors. Full vet remains owed after the final lane battle. **NEXT: Fable reads `COORDINATION.md` LANE-003 and commits D363 New Market Heights spec + plan probe before runtime.**

## ⚡ HANDOFF — 2026-07-10 **D361: GAINES' MILL SPEC + PLAN PROBE SHIPPED; LANE-003 IS DRIVE-LOCKED TO CHATGPT/CODEX.** Gaines' Mill is not playable yet; the registered roster remains 13 through Nashville. D361 adds `docs/design/gaines-mill-battle-build-spec.md` + `tools/probe-gaines-mill-plan.mjs` and locks D362: single-phase CS attacker / US defender, fog off, standard doctrine, Watt House plateau behind Boatswain's Creek, menu rank 15 between Bull Run and Malvern Hill. The source audit records the real strength conflict: ABT's evening map says about 32,000 CS / 34,000 US, CMH says 50,000 Confederates amassed, and NPS gives Porter's 27,000 core rising toward 34,000. The game target 27,000 US / 31,500-32,500 CS is a disclosed mixed-source abstraction (core line vs ABT's 16-brigade wave), not an exact same-time OOB; the CS aggregate is Disputed and modeled strength Inferred. Rank locks have two-source support: Porter Brig. Gen.; A. P. Hill Maj. Gen.; Hood Brig. Gen./Texas Brigade; Turkey Hill excluded. Direction teeth split CS objective wins ≥5/8 from CS killed/wounded ≥US ≥5/8 after captured/missing subtraction. D362 must update data/T1/schema, both baselines, exact `912 + unique units×3` loot pins, T10 flags, weather, 14-scene Intel/media count, suite 118→119, generated HTML, focused/adjacent gates. Aaron also clarified helper routing: on surfaces without per-helper model/effort controls, inheriting helpers may do bounded output-insensitive evidence work; the Ultra top loop keeps all quality-bearing judgment and the commit. Gate: build GATE OK; schema 43/43; research 15/15; plan 8/8; roster 8/8; custom builder 15/15; Bull Run 15/15; Malvern Hill 27/27; JSON all `ok:true`, 0 failed/pageerrors; negative DRIVE bind exactly red then byte-identical restore; diff clean. **NEXT: D362 playable Gaines' Mill from the committed spec.**

## ⚡ HANDOFF — 2026-07-10 **D360: PHASE I 5c START-ANYWHERE CAREER TRAJECTORY SHIPPED — LANE-002's PLANNED SLICES ARE COMPLETE.** The journey promotion hook grew from two transitions (Private→Sergeant→Captain — every other rank was a dead career) to an 11-rung lattice from enlisted specialists through General: legacy D151 thresholds verbatim, decisive fast-track kept, escalating win thresholds as Inferred balance, next-ranks constrained to `rankBase` keys so `fldPromotePerson`'s unknown-rank guard stays the hard floor. A "Career Trajectory" block (current → next rank, victories banked vs needed, decisive note, arc-so-far) renders in the Campaign-Kit journey panel and the full journey report; compact/AAR stays lean; off-ladder ranks labeled honestly; General is the summit. NOTHING new rides the save (computed from career[] + person snapshot; no `_SAVE_VER`, no new accept lanes, E50 posture untouched); D74 holds (journey-inactive paths untouched). Teeth: probe-loot-survival **12/12** with the new D360 step driving the D358 Sherman row Col.→Brig. Gen. over four victories; **surgical bind test** — colonel rung alone removed → exactly D360 red, D151 GREEN → restored byte-identical. Adjacents ratings **22/22** (one documented goto-flake rerun) + camp **8/8**; schema **43/43**; GATE OK; 0 pageerrors; diff clean. **D359 (same session, Aaron popup): all phase lanes unlocked for the next Codex session — trans-Miss Tier 1, M8, USCT battles, Phase H media, cavalry, naval-river — with TWO dignity carve-outs held (Leetown Native OOB per D178/D183; playable Fort Pillow) and no ship-vs-ship engine. NEXT: the Codex 5.6 Sol battle ladder runs under COORDINATION.md LANE-003 (Gaines' Mill → New Market Heights → Stones River); Claude keeps LANE-002 (further 5b batches, 840 rows remain).**

## ⚡ HANDOFF — 2026-07-10 **D358: PHASE I 5b FIRST PROSOPOGRAPHY BATCH SHIPPED — EIGHT BULL RUN COMMAND ROWS.** The batch was sized (8) and logged in DECISIONS BEFORE the first record, per the LANE-002 contract. Col. W. T. Sherman, Col. Andrew Porter, Col. O. O. Howard, and Capt. Charles Griffin (US) plus Col. N. G. Evans, Brig. Gen. Barnard Bee, Col. Francis Bartow, and Col. Wade Hampton (CS) now replace the `bullrun1` generated `:cmd` rows — each of which previously carried the generated hardcoded 'Captain' rank, so the batch is also a rank-accuracy fix the D357 muster-roll UI displays in play. Pipeline held the Contract Relay roles: Sonnet/medium gathers → Opus/high default-refute (16 agents, 0 errors) → Fable-5 adjudication owning all player-facing text. All 8 verdicts were AMEND and were applied: no brigade-casualty numbers anywhere (banked ~605/~464 failed verification — division returns only), Howard's attackers corrected to Kirby Smith's brigade under Elzey + Early, Griffin's overrun credits the chief of artillery's overrule, Evans ships the NPS temporary-colonel nuance, Bee's stone-wall cry stays contested (never verbatim) with posthumous confirmation noted, Bartow's monument logged as a memorial-rank artifact, Hampton scoped to the Legion's ~600 engaged infantry with no casualty-breakdown/wound-mechanism claim. Gate: importer **39/39 Verified**; build **GATE OK**; probe-loot-survival **11/11** with the registry pin **912 HELD** (replacements preserve length — the kickoff's "both pins move" was wrong and is corrected in D358) and applied=39; **two negative bind tests** (record removal → exactly the count tooth; rank tamper → exactly the deep tooth); byte-identical restore (checksum-proven); adjacents ratings **22/22** + camp **8/8**; schema **43/43**; 0 pageerrors; diff clean. **LANE-002: 848 → 840 remaining, lane OPEN. NEXT: 5c start-anywhere career trajectory (save/journey/report contracts unbroken; E41 gate law; E50 — keep new journey state clear of own `hasOwnProperty` keys). Full battery not owed until the next release boundary (D355 was green 2026-07-10).**

## ⚡ HANDOFF — 2026-07-10 **D357: PHASE I 5a SHIPPED — THE MUSTER ROLL IS PLAYER-REACHABLE.** The probe-vetted T14 `fldMusterRollHtml(u)` panel (men-mean OVR + materialized person rows) finally has its UI: `src/tactical/T29-muster-roll-ui.js` puts a native, WCAG-AA toggle button in the selected-unit HUD (aria-expanded/controls, always-in-DOM `hidden` panel per the canonical disclosure pattern, 11.05:1 text contrast, wcag-auditor target-size fix applied, focus survives HUD rebuilds via a reassignment wrapper). Presentation-only and byte-identical when ratings data is absent; the open flag never rides the save. Teeth: probe-ratings **22/22** with the seam bind-tested (removed → exactly the T29 tooth red → restored); adjacents probe-field **23/23**, probe-order-feel **22/22**; 0 pageerrors; build **GATE OK**; diff clean. **NEXT (LANE-002, per COORDINATION.md): 5b — size the prosopography batch FROM THE LANE'S 848-row inventory (TOP LOOP logs the number in DECISIONS before the first record), then 5c career trajectory. Full battery not owed again until the next release boundary (D355 was green today).**

## ⚡ HANDOFF — 2026-07-10 **D355: FULL RELEASE CHECKPOINT GREEN — 118/118, THREE FINDINGS FIXED IN-RUN, NOTHING DEFERRED.** The `npm run vet:noreg` battery owed since the run-2 fix stack ran locally (full-access session, serialized, artifacts read) in four fail-fast segments with `--from=` resume: 1-25 · 26-36 · 37-93 · 94-118, final `VET NO-REGRESSION OK`. Every red was root-caused and re-run green, never blind-retried: (1) probe-western-theater's time-bound "future battles must not exist" leg re-toothed to a registry-membership stowaway guard, bind-tested; (2) probe-loot-survival's pre-D353 poison fixture converted to an E50 door-rejection tooth (sanitize teeth keep their non-poison tampers) and the stale 645 Army-Register pin bumped to the real **912** (+89 units × 3, cross-checked vs the LANE-002 inventory); (3) **product-data fix** — chattanooga/franklin/nashville weather hints shipped illegal `Verified/Inferred` provenance (+ Franklin `late afternoon`) and are now honest `Inferred`/`afternoon` per the shipped convention, presentation-only, with the three battle guards re-run green after the change. diag-classic nonBlank **346**; sweep/full-campaign/tactical-visuals all `ok=true`; 0 pageerrors everywhere; build **GATE OK**; schema **43/43**; diff clean. **NEXT: M4 — commit COORDINATION.md (the Contract Relay: lane states, the never-carry-red-teeth rule, the E50 retro lane, the opened Phase I lane with the 848-row inventory), then Phase I "Named Army" 5a muster-roll UI.**

## ⚡ HANDOFF — 2026-07-10 **D354: E70 TRANSFER READINESS CONSUMER BUILT — THE RUN-2 LEDGER IS FULLY DISPOSITIONED.** Per Aaron's 2026-07-10 disposition (build the consumer, keep the cost), Transfer readiness now has EXACTLY ONE bounded consumer: `_cmdTransferReadinessLift` pays a data-configured −3 command-friction on `commandLeadership` (the Q10/Q12 input facet the bridge already reads — never the scoreboard, D74 intact) ONLY when the player explicitly appoints a cross-theater general without a Transfer order. Natural fit, completed Transfer, and the history-following default read exactly 0 — untouched campaigns byte-identical. Command-desk disclosure copy and its pinned tooth updated together (the pre-E70 "records readiness only" phrasing became false). Negative bind test: consumer unwired → exactly the E70 tooth red (83→83) → restored. Gate: build **GATE OK**; schema **43/43**; probe-command **90/90** (purity-snapshotted E70 tooth); adjacents ratings **21/21** + bridge **6/6** + oob **17/17**; all 0 pageerrors; diff clean. **NEXT: the full `npm run vet:noreg` release checkpoint (118 commands) — owed since the run-2 fix stack began. Then COORDINATION.md (the Contract Relay) and Phase I "The Soldier's Story: Named Army" (5a muster-roll UI · 5b prosopography batch sized from a helper inventory · 5c career trajectory).**

## ⚡ HANDOFF — 2026-07-10 **D353: E50 CAMPAIGN-ENVELOPE DEEP SAVE GUARD SHIPPED — THE RUN-2 FIX-NOW QUEUE IS EMPTY.** The campaign save envelope is now deep-guarded at every accept lane: new `_slCampaignPoisoned` (91-save-slots) rejects an own `hasOwnProperty` at ANY depth; `loadLocal` treats a poisoned autosave as corrupt (clean menu boot, `hasSave()` false), `applySave` rejects ATOMICALLY before any mutation (no partial settings, no campaign replacement), and the slot-read/import/undo lanes reject via `_slValidSave`. The D323 `raw.ids` Transfer sink also iterates tamper-proof (`Object.prototype.hasOwnProperty.call`) and sanitizes directly poisoned live state — poison dropped, legit siblings kept. The prior Codex session's uncommitted red teeth were adopted unchanged as the acceptance contract and committed WITH the fix. Negative-tested: guard stubbed → exactly the 5 E50 teeth red (verbatim sink crash) → restored. Gate: build **GATE OK** (E41 save-shape hashes consciously updated, NO `_SAVE_VER` bump — purely rejective, legit saves byte-identical); probe-save-slots **16/16**; probe-command **89/89**; schema **43/43**; adjacents ratings **21/21** + bridge **6/6**; all 0 pageerrors; diff clean. Full suite deferred under D176 — and it is now the imminent checkpoint. **NEXT: E70 — give Transfer readiness ONE bounded, source-honest consumer (Aaron 2026-07-10 disposition: build the consumer, do not suspend the cost), then the full `npm run vet:noreg` release checkpoint (118 commands).**

## ⚡ HANDOFF — 2026-07-10 **D352: E69 REPORT HTML ENTITY-ESCAPING CONTRACT SHIPPED.** Five audited HTML generators now share one ampersand-first escaper for all five reserved characters, including artifact fields formerly trusted as numeric. A new filesystem gate binds every import/use, rejects identity helpers, and parses seven hostile payloads in text plus quoted-attribute contexts. Identity mode failed the exact two intended rows (**2/4**, exit 1); clean mode **4/4** with zero forbidden nodes/event attrs. All five real reports parse clean; schema **43/43**; orphan assets **199/199**; probe summary **117/117**; media **13/13**; Group 6 **9/9**; build GATE OK, generated game unchanged; direct Parse5 dependency resolves; suite list **118**; diff clean. Full suite deferred under D176. **E50 is the sole remaining run-2 FIX-NOW item. NEXT: E50 — deep-guard the campaign save envelope and the raw.ids command sink with tampered-save probes.** E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-10 **D351: E66 LATE-WESTERN BATTLE-FLAG METADATA SHIPPED.** Chattanooga, Kennesaw, Franklin, and Nashville now resolve explicitly as Western/AoT fields with no AotP corps badges and a representative Hardee-pattern CS default, instead of inheriting the Eastern/ANV sandbox fallback. Tennessee State Museum/NPS evidence grounds the family and Army-of-Tennessee identity; the code explicitly avoids claiming that every regiment carried one identical flag. `probe-flags` now requires explicit metadata for all 13 registered scenarios and pins all four new records/patterns. Deleting Nashville metadata in-browser made exactly 3 new teeth red (**39/42**, exit 1), with all 39 legacy rows green; final flags **42/42**, schema **43/43**, roster **8/8**, visual fidelity **27/27**, 0 pageerrors/texture warnings; screenshots/artifacts read; syntax/build/diff green. Full suite deferred under D176. **2 run-2 FIX-NOW items remain: E69 and E50. NEXT: E69 — real HTML entity escaping in the five audited report tools plus hostile-string fixtures.** E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-10 **D350: S43 CANONICAL ROUTING COHERENCE SHIPPED.** The active startup/picker/roadmap/audit instructions now agree with live git: 13 registered historical scenarios through Nashville; Antietam's sole built-battle revision closed by D341; broad phase locks removed by D324; D336 5.6 Sol + Ultra routing; and **E66 → E69 → E50** as the remaining run-2 FIX-NOW order. Old D322/D333 snapshots are explicitly historical and do not render as current routing. Phase D/H/naval/M8/custom-authoring lanes are eligible under normal gates; the trans-Mississippi packet alone retains its narrower D183 two-tier source/OOB go/no-go. Gate: four-doc visible-text/comment-balance assertion green; build **GATE OK**, generated HTML unchanged; schema **43/43**; battle research **15/15**; roster **8/8**, 13 exact scenarios, 0 pageerrors; suite list **117**; diff clean. Full suite deferred under D176. **3 run-2 PENDING FIX-NOW items remain including E50. NEXT: E66 — add and guard explicit Western/AoT flag metadata for Chattanooga, Kennesaw, Franklin, and Nashville.** E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-10 **D349: E68 LARGEST-SCENE INTEL UHD-617 PROFILE COVERAGE SHIPPED.** The profile now derives every shipped opening OOB and proves Kennesaw is uniquely largest among 13 scenarios at **17 initial units** (Franklin 16; representative Chickamauga 8). Existing Chickamauga high/low legs remain; a third serialized Kennesaw `largest-low` leg verifies the full roster, low-tier formation/marker/smoke fallbacks, nonblank render, pageerrors, texture warnings, and unchanged caps. The old **360 render-call / 1,400 scene-object** hard limits are pinned; Kennesaw reads **117 / 169**, 17 units, 0 pageerrors/texture warnings. Timing remains warning-only; the sole warning is the known frozen raw tier at 2.418 MB over soft. A stale Chickamauga-largest bind test failed exactly the inventory tooth (25/26, exit 1); final Intel profile **26/26**, schema **43/43**, media budget **13/13**, Kennesaw **11/11**, build **GATE OK**, diff clean. Full suite deferred under D176. **4 run-2 PENDING FIX-NOW items remain including E50. NEXT: S43 — reconcile stale canonical routing text against live D324-D349 state.** E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-10 **D348: S42 DRAFT-HONEST CONNECTED-AI LIVE STATUS SHIPPED.** Connector readiness is now one pure value predicate: runtime launch/dispatch applies it to saved `_llmConn`, while the panel's polite live status applies it to current unsaved `_llmUi`. Both mismatch directions are pinned through real controls: invalid saved + completed valid draft announces ready while saved state remains invalid until Save; valid saved + emptied draft model announces incomplete while saved state remains valid. Both preserve the same live node, save nothing, and send nothing. Restoring the old mixed-state predicate made exactly the S42 step red. Final gate: syntax clean; build **GATE OK**; LLM commander **26/26**, 0 pageerrors, 0 network; accessibility **25/25**; H0 main menu **5/5**, both 0 pageerrors; diff clean. Full suite deferred under D176. **5 run-2 PENDING FIX-NOW items remain including E50. NEXT: E68 — profile a largest shipped tactical scene on the Intel UHD-617 floor without relaxing caps.** E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-10 **D347: S41 DETERMINISTIC TACTICAL-LABEL DECLUTTER SHIPPED.** Every 2D terrain/route, objective, officer, train, and engineering label now enters one deterministic priority/collision pass that reserves visible units, marker bodies, and tactical UI. Labels paint last as high-contrast chips; displaced labels receive map-anchor leader lines; objective and visible officer names are mandatory, while only lower terrain/route tiers may hide. Refreshed screenshots are materially legible: Shiloh **19/19** labels; Malvern **16/19**, hiding only 3 lower map labels and preserving all 9 officers plus the objective. Across all five 2D scenes: 0 label-label overlaps, 0 label-versus-reserved overlaps, 0 mandatory hidden, stable identical-frame signatures; all five 3D pairs green. Hostile Shiloh mode exposed **9** collisions and exited 1; final tactical visuals **10/10**, 0 pageerrors/lifecycle/cleanup errors. Adjacent field **23/23**, officers **15/15**, visual fidelity **27/27**; syntax clean; build **GATE OK**; diff clean. Full suite deferred under D176. **6 run-2 PENDING FIX-NOW items remain including E50. NEXT: S42 — validate the current Connected-AI draft for its aria-live status.** E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-10 **D346: E64 FAIL-CLOSED MEANINGFUL DATA-SCHEMA GATE SHIPPED.** `validate-data-schemas` now covers all 43 data documents through four explicit nonempty families (13 battle / 20 schema / 9 meta / 1 ratings), rejects unregistered files, binds real battle/OOB/phase and per-document structures, writes an authoritative JSON artifact, and exits nonzero on any failed row. A four-family in-memory diagnostic failed exactly Antietam objective, artillery guns, cabinet sides, and ratings attributes, wrote red, and exited 1; clean mode restored **43/43**, 0 failures/pageerrors, and 0 zero-key rules. `vet-no-regression` enrolls the validator after both canonical importers; suite **117 commands**. Final gate: syntax clean; soldier imports **31/31**, women imports **9/9**, battle-build research **15/15**; build **GATE OK**, generated HTML unchanged; schema JSON/report green; `git diff --check` clean. Full suite deferred under D176. The run-2 ledger now has **7 PENDING FIX-NOW items including E50**. **NEXT:** **S41** — deterministic tactical-label priority/collision treatment plus screenshot/readability teeth. E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-10 **D345: E63 BATTLE-GUARD ENROLLMENT + HONEST CONSOLIDATED SWEEP SHIPPED.** `vet-no-regression` now serially enrolls Chattanooga, Kennesaw, Franklin, and Nashville after Chickamauga (suite **116 commands**), so their focused historical-direction guards bind future batch gates. `sweep-all-battles` now writes/obeys `ok`, registry/order parity, exact seed counts, finished phases, valid winner/`winBy`/roles, finite casualties, bounded steps, multi-phase logs, pageerrors, and failures; any defect exits 1 after bounded Chrome/server cleanup. A controlled pageerror bind-test completed all 13 one-seed rows but wrote red and exited 1; clean mode then covered **13 scenarios / 104 serialized runs**, registry/order parity true, 0 unfinished/invalid rows, 0 failures, 0 pageerrors. Final gate: both tools syntax-clean; build **GATE OK**, generated HTML unchanged; Chattanooga **16/16**, Kennesaw **11/11**, Franklin **10/10**, Nashville **12/12**, all green/0 pageerrors; `git diff --check` clean. The sweep timeout is 15 minutes for the expanded serialized roster on the 8 GB Mac. Full suite deferred under D176. The run-2 ledger now has **8 PENDING FIX-NOW items including E50**. **NEXT:** **E64** — make `validate-data-schemas.mjs` fail closed, require meaningful invariants for every family/branch (no zero-key schema), add hostile/invalid fixtures, and enroll it in `vet-no-regression`. E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-10 **D344: C70 ALFRED J. VAUGHAN IDENTITY/SPELLING CORRECTION SHIPPED.** VMI Archives identifies Alfred Jefferson Vaughan and records his Confederate brigadier-general service, so Kennesaw now uses Vaughan consistently in the leaders note, brigade/commander labels, explanatory note, codex axis, and durable spec; the NPS OOB's "Vaughn" rendering is retained only as a documented source variant. Stable id `cs_vaughn` and every simulation input remain unchanged. The plan/runtime guards now pin the VMI source, correct spelling, unchanged id, exact player-facing labels, and misspelling rejection. Bind tests proved both layers bite (plan **5/6** on a reverted spec; runtime red on rank+identity teeth on reverted data), then final bytes were restored: build **GATE OK**; schema **43/43**; plan **6/6**; Kennesaw **11/11** with CS holds **8/8**, US higher loss **8/8**, 0 pageerrors; adjacent Franklin **10/10**, 0 pageerrors; `git diff --check` clean. The existing post-side-choice Kennesaw PNG is a black/non-semantic capture and was not used as identity evidence. Full suite deferred under D176. The run-2 ledger now has **9 PENDING FIX-NOW items including E50**. **NEXT:** **E63** — enroll Chattanooga/Kennesaw/Franklin/Nashville focused guards in `vet-no-regression`, then make `sweep-all-battles` collect pageerrors and exit nonzero on any failed battle while keeping Chrome serialized. E70 remains proposal-only.

## ⚡ HANDOFF — 2026-07-09 **D343: PART 3 M6 SHIPPED — E65 LLM-CONNECTOR BROWSER-CALLS CONSENT ENFORCEMENT.** `fldLlmConnConfigured()` is now the one consent seam: adapter B (Anthropic) is unconfigured without the explicit "Allow direct browser calls" opt-in, so no arm and no dispatch — and no `anthropic-dangerous-direct-browser-access` header — can precede the player's consent; adapter A providers unaffected; pre-E65 stored configs deliberately not grandfathered. The panel note and Status line now state the enforced behavior and name the toggle when it is the only blocker. The probe's wrong assertion (anthropic configured without opt-in) is corrected, and a new E65 CONSENT step proves refusal-without-consent (sync `cb(null)`, zero fetch), the exact consented request shape (locked endpoint + opt-in header + key + `anthropic-version` + body), and arm-seam gating both directions against a recording mock fetch. A 3-lens adversarial panel REFUTED the consent-bypass hunt at high confidence; a bind-test (predicate removed → probe red on exactly the two consent teeth, zero real network even then → restored → green) proves the teeth bite. Final-bytes gate: build **GATE OK**; `probe-llm-commander` **25/25**, 0 pageerrors, 0 network; adjacents `probe-field` **23/23** + `probe-accessibility` **25/25**; `git diff --check` clean; full suite deferred under D176. The run-2 ledger now has **10 PENDING FIX-NOW items including E50**. **NEXT:** **C70** (Kennesaw Vaughan spelling) is the queued mechanical Codex slice; the next reasoning-tier slice is **E64** (give the schema validator a failing exit path + real per-family invariants) or **S41** (deterministic label declutter).

## ⚡ HANDOFF — 2026-07-09 **D342: PART 3 M5 SHIPPED — E67 TACTICAL-VISUALS PER-SCENE BROWSER ISOLATION (Codex authored · Fable completed).** Codex rewrote `tools/probe-tactical-visuals.mjs` so every heavy scene owns a fresh browser+context+page with bounded teardown, crash-aware verdicts, `failureStage` labels, honest partials, and a `--diagnostic-close-scene` negative-test mode — then ran out of usage before the negative test, docs, and commit. This session panel-reviewed the uncommitted diff (3 lenses, no blocking finding), hardened it (240 s evaluate hang bound + bounded teardown evaluate, SIGKILL on close-timeout, `in-page-assert`/`texture-warning` stages), and re-gated the final bytes: the diagnostic leg deliberately kills Malvern Hill 3D (the audited killer scene) and proves `recoveryProven=true later=6/6` at exit 1; the clean leg is **10/10, exit 0**, 0 pageerrors/lifecycleErrors/cleanupErrors, Malvern 3D PNG read fully painted; build **GATE OK**, generated HTML byte-identical, `git diff --check` clean. Full suite deferred under D176. The run-2 ledger now has **11 PENDING FIX-NOW items including E50**. **NEXT:** Part 3 M6 = **E65**, make the T28 LLM-connector "Allow direct browser calls" opt-in actually gate configured-state and dispatch, with positive/negative dispatch teeth (this session, reasoning tier); **C70** Vaughan spelling remains the next mechanical Codex slice.

## ⚡ HANDOFF — 2026-07-09 **D341: PART 3 M4 SHIPPED — C69 ANTIETAM RICHARDSON BATTLE-DATE RANK CONSISTENCY.** `data/antietam.json` now gives the Sunken Road reinforcement `us_richardson.commander` the exact `Maj. Gen. Israel B. Richardson` form already used by `ld_richardson.name`; no other data field changed, Mansfield remains untouched, and `cs_ridge_guns` still names the different Washington Artillery captain in `Capts. Squires, Richardson, Brown`. `probe-antietam` adds one focused stable-id truth tooth for the phase, both Richardson records, the obsolete-rank rejection, and the separate captain. Final gate: data parse + probe syntax clean; build **GATE OK**; schema **43/43, 0 failed** with timestamp churn removed; battle-build research **15/15** with Antietam now `SOLID_AS_IS`; Antietam **17/17**, `ok=true`, 0 failed steps, 0 pageerrors; all 16 baseline step objects exact by name; final screenshot read after rejecting and rerunning one incomplete-paint PNG; `git diff --check` clean. Full suite remains deferred under D176. The run-2 ledger now has **12 PENDING FIX-NOW items including E50**. **NEXT:** Part 3 M5 = **C70**, correct player-facing Alfred J. Vaughan spelling in Kennesaw and its spec/probe guards without changing stable ids or simulation inputs; do not start E65 or another finding in that slice.

## ⚡ HANDOFF — 2026-07-09 **D340: PART 3 M3 SHIPPED — S40 KEYBOARD TACTICAL ORDERING EQUIVALENCE; THE RUN-2 HIGH QUEUE IS CLOSED.** Selected player brigades now have a full keyboard endpoint path: `M` opens the order cursor, arrows move it in 30-yard steps, `[` / `]` turn facing by 15 degrees, Enter commits, Shift+Enter appends through the existing waypoint queue, and Escape cancels without exiting battle. T20 remains the one order contract: the cursor synthesizes a movement-only `fldResolveOrderGesture`, which uses `fldApplyOrder` / `fldEnqueueOrder`; no parallel movement/sim logic and no AI/combat/casualty/score/winner change. A static dual-stroke crosshair + facing arrow, high-contrast instruction chip, HUD/help copy, and concise `fldLive` status cover visible and screen-reader feedback; no animation/transition means reduceMotion is honored. Editable focus, visible aria-modal dialogs, inactive tactical state, and pointer takeover suppress/cancel only the pending cursor. Gate: build **GATE OK**; order-feel **22/22**, with the original 17 rows byte-equal to the D339 baseline and new real-key/pointer teeth green; field **23/23**; accessibility **25/25**; tactical HUD **3/3** desktop/tablet/phone; help overlay **10/10**; every final JSON `ok=true`, 0 failed steps, 0 pageerrors; screenshots read; `git diff --check` clean. REVIEW-QUEUE S40 → FIXED (D340). **NEXT:** Part 3 M4 = **C69**, the outcome-neutral Antietam Richardson reinforcement-rank label (`Brig. Gen.` → `Maj. Gen.`) plus a focused probe tooth; do not start C70 in the same slice.

## ⚡ HANDOFF — 2026-07-09 **D339: PART 3 M2 SHIPPED — C71 KENNESAW PIGEON HILL OVERFIELDING CORRECTED WITH AN HONEST A/B.** NPS National Register text and the NPS Pigeon Hill site bulletin confirm that about **5,500** men in three brigades formed the complete Pigeon Hill commitment: Giles A. Smith and Lightburn from Brig. Gen. Morgan L. Smith's division plus Walcutt from Harrow's. The unsupported 1,200-man `us_morgan_smith_support` is removed, so Pigeon Hill is **6,700→5,500** and all-fielded US strength is **16,000→14,800**; CS remains **11,925**. Morgan L. Smith stays in the teaching as the division commander, not a fourth fielded echelon. Every fielded US infantry brigade now has an `assaultSector`; `probe-kennesaw` sums OOB plus reinforcements by sector, rejects unclassified infantry/the removed id, and derives casualty bases from all fielded units. No terrain/timing/doctrine/weapon/xp/gun/combat/score/winner/output gate changed. Honest A/B at `f67cec5` vs D339 input: CS holds **8/8→8/8**, US-higher-loss **8/8→8/8**; the recorded casualty deltas are non-monotonic and were not tuned. Gate: build **GATE OK** (known soft warning), schema **43/43 0 failed** (report read per E64), Kennesaw plan **6/6**, Kennesaw **10/10** 0 pageerrors, Franklin **10/10** 0 pageerrors after one initial no-step readiness-timeout retry, Chattanooga **16/16** 0 pageerrors, all final JSON `ok=true` with 0 failed steps, `git diff --check` clean. REVIEW-QUEUE C71 → FIXED (D339). **NEXT:** Part 3 M3 = **S40** keyboard movement destination/facing/waypoint equivalence, the last run-2 HIGH; preserve pointer play and T20 endpoint semantics, add the bounded keyboard path + probe, and do not start C69/C70 or any MED/LOW finding.

## ⚡ HANDOFF — 2026-07-09 **D338: PART 3 M1 SHIPPED — GETTYSBURG TRUTH BUNDLE C66+C67+C68 (TEXT/HISTORY ONLY).** `data/gettysburg.json` no longer teaches the Lost Cause sole-blame Longstreet story its own codex debunks: the Day-2 leader note/teach, header comment, provenance, and `gb_command` afterword now carry the documented contested context (vigorous objection, Lee overruling, late-afternoon step-off after the countermarch, real-but-partial delay, Lee's hands-off piecemeal management, the Jubal Early scapegoating "largely overturned" — matching the codex hedge). The unsourced "General, I will take command" Meade quote is removed (the cited NPS page contains no such quote) and Hancock's July 1 field-command assignment is taught separately from his July 3 wound; Kemper is seriously wounded and captured (Garnett killed, Armistead mortally wounded) per NPS Fort McHenry. A new data-only truth-teeth step in `tools/probe-gettysburg.mjs` fails on each exact regression (negative-tested: clean file passes, all four regression classes caught). Verification: 4 Sonnet source-fetch agents on the mandated URLs + a 3-lens Opus adversarial panel (framing/citations/consistency); all accepted panel edits shipped. Gate: build **GATE OK** (known soft warning), schema report **43/43 0 failed** (read per E64), `probe-gettysburg` **19/19** 0 pageerrors with the direction battery unchanged (Day 1 CS 7/8, Day 2 US 8/8, Day 3 US 8/8, aggregate 8/8 — no sim change), adjacent `probe-antietam` **16/16** 0 pageerrors, `git diff --check` clean. REVIEW-QUEUE C66/C67/C68 → FIXED (D338). **NEXT:** Part 3 M2 = **C71** Kennesaw Pigeon Hill overfielding (`us_morgan_smith_support` makes the runtime commit ~6,700 vs the sourced ~5,500 contract, and the probe masks it) — a SIM-AFFECTING input correction requiring an honest A/B; correct the input/abstraction, never the output gate.

## ⚡ AUDIT HANDOFF — 2026-07-09 **D337: AUDIT RUN 2 COMPLETE; PART 3 HAS NOT STARTED.** At `c77ebca` (D336 docs; live game D335), the full-spectrum pass confirmed **17 new FIX-NOW defects** (4 HIGH / 11 MED / 2 LOW), re-confirmed/re-triaged existing E50, and filed one MED proposal. The ranked evidence lives at `REVIEW-QUEUE.md` → `## AUDIT — 2026-07-09 (run 2)`. Aaron's hard boundary was honored: no finding, V1 item, or V2/V3 item was implemented. The current tactical-visual artifact remains honestly red after three green scenes, one Malvern Hill 3D page teardown, and a closed-page cascade; no rerun began, artifacts were preserved, and no server remains. Generated reports: keep `source-file-inventory.csv` and `orphan-assets-report.html`; discard the schema report's timestamp-only churn. Docs/report gate: build GATE OK, source inventory 4/4, ledger integrity green, `git diff --check` clean. **NEXT:** fresh Part 3 M1 = C66/C67/C68, one text-only Gettysburg truth bundle with source-honest wording + focused probe teeth and no OOB/combat-number change; C71 follows separately with an A/B because it changes a live strength input.

## ⚡ MODEL/OPERATING UPDATE — 2026-07-09 **CHATGPT 5.6 SOL IS THE CURRENT MAIN LOOP (D336).** Select **5.6 Sol** at **Ultra** (the highest effort available in that UI) for architecture, important code, historical judgment, UX/accessibility, adversarial review, final verification, and integration. Use any explicitly selectable lower tier only for a bounded mechanical packet with exact files, constraints, acceptance criteria, a stated reason, and main-loop verification. Do not delegate combat balance, history, UX/accessibility judgment, architecture, or final integration. Aaron authorized autonomous completion of all unlocked work; HALT only for irreversible/destructive changes, spending, account creation, unclear licenses/provenance, publishing, or a true contradiction of shipped design/history. Visual/media work is $0 and public-domain/permissive-license only by default. Canonical prompt/routing docs: `OPUS-PLAYBOOK.md` (filename retained) and `AUDIT-PROMPT.md`.

## ⚡ MODEL/OPERATING UPDATE — 2026-07-06 **CLAUDE OPUS 4.8 IS THE PERMANENT MAIN LOOP (D286 — the D223 Fable-5 migration is reverted; Fable left Aaron's subscription).** The Claude Code main loop runs **Claude Opus 4.8** (`claude-opus-4-8` / `opus[1m]`) + xhigh; the D145 routing law stands with Opus 4.8 as the top tier — helpers set model+effort explicitly, **Sonnet/Haiku on mechanical legs and Opus (high/xhigh) only on genuine reasoning legs**, on every Agent/Workflow call (the old "never Fable on a helper" clause is retired — Fable no longer exists). Canonical docs: **`OPUS-PLAYBOOK.md`** (Opus 4.8 behavior + the standard run-prompt snippets every long/autonomous kickoff embeds) and **`AUDIT-PROMPT.md`** (the re-runnable full-spectrum audit+improve kickoff; its findings ledger lives in `REVIEW-QUEUE.md`). The Fable playbook/audit files are retired to `legacy/`; `~/.claude/settings.json` already carries `"model":"opus[1m]"` + `"effortLevel":"xhigh"`. No src/data/roadmap change; the deliverable is byte-identical. Its old phase-lock clause is superseded by D324.

## ⚡ OPERATING GATE UPDATE — 2026-06-30 **QUEUE-LOOP FULL AUDIT DEFERRAL (D176, supersedes ambiguous D160 trigger language).** The gate is not gone: every commit still needs source/doc read order, source-only edits, `node tools/build.mjs` GATE OK, relevant importer/schema checks, focused probe(s), JSON/pageerror readback, `git diff --check`, and clean commit/push. Use `node --check` plus 1-3 adjacent probes for JS/runtime changes. During Aaron's explicit same-chat queue loop, do **not** run full `npm run vet:noreg` after each queued item, even when a slice touches manifest/bridge/render/lifecycle/suite enrollment. Keep `vet:noreg` intact and run it at the end-of-queue planned-work batch/release checkpoint or when Aaron explicitly asks. Never weaken probes, skip relevant probes, edit `build/base.html`, hand-edit `civil_war_generals.html`, add per-battle combat fudge, or push red.

## ⚡ GATE BATCHING UPDATE — 2026-06-29 **FULL SUITE IS A BATCH/RELEASE GATE, NOT A PER-SLICE TAX.** Aaron stopped the D161 full no-regression battery and directed that the big suite stay deferred until the planned work is built out. Keep the probes intact; do not delete checks, weaken assertions, or mark red green. For current slices, use source/doc read order, syntax checks, importer/schema gates, `node tools/build.mjs`, focused probes, adjacent probes, JSON/pageerror readback, and `git diff --check`. Run the full `npm run vet:noreg` battery for the planned-work batch/release gate or when Aaron explicitly asks for it.

## ⚡ ROUTING UPDATE — 2026-07-08 **D324: ALL PREVIOUSLY LOCKED PHASE LANES ARE UNLOCKED.** Aaron explicitly directed: "unlock all locked phases now." Treat this as the current routing law over stale lock language below. M8 battle-build, Chattanooga, Atlanta/March, Franklin/Nashville, USCT playable battles, Phase H media additions, H2 footage, HDRI/model media, Tripo/model/asset work, remaining Soldier's Story rows, Phase D full-hex tactical mode, custom-battle phase authoring/editor work, and optional Group 6 report polish are now eligible for normal planning and execution. D74 no-fudge, citation-grade history, accessibility/performance standards, source/build discipline, focused gates, JSON/pageerror readback, `git diff --check`, clean commit, and push still bind every slice.

## ⚡ SAME-CHAT BUNDLES + FRESH-CHAT BOUNDARY — 2026-07-08 **D307 RELAXES THE TOO-FREQUENT ONE-SLICE STOP PATTERN INSIDE SAFE HOMOGENEOUS QUEUES.** Auto-condense is still only a safety net, not the queue runner. For cleared low-risk queues such as Group 6 tooling/reporting, run a same-chat execution bundle: ship 2-4 small slices, with focused gate + docs sync + commit/push after EACH slice, then continue to the next safe slice. Stop and return a paste-ready next-chat prompt when the bundle is exhausted, before a new execution group/phase, on a design fork/risk, before a browser-heavy/full-suite gate, or near ~70-80% context. Current grouped order is in `V1-CHECKLIST.md` §"Fresh-Chat Execution Groups"; after D324, formerly locked lanes are eligible too, so choose the highest-leverage unlocked lane rather than defaulting to optional Group 6 polish.

## ⚡ CONTINUE HERE — 2026-07-08 **D335: NASHVILLE PLAYABLE BATTLE SHIPPED.** D335 implements the D334 packet as a playable two-phase T8 Franklin/Nashville-lane battle. Runtime/data changes: `data/nashville.json`; registry/menu rank after Franklin in `src/tactical/T1-bull-run.js`; focused guard `tools/probe-nashville.mjs`; both historical baselines updated (`probe-tactical-roster`, `probe-custom-battle-builder`); `validate-data-schemas` now treats Nashville as a battle file; generated `civil_war_generals.html` and schema artifact rebuilt. Scenario shape: US attacker / CS defender, fog off, Phase 1 Dec. 15 Redoubts/Montgomery Hill scoreWeight 1, Phase 2 Dec. 16 Shy's Hill/Peach Orchard Hill scoreWeight 3; Franklin stays separate and Spring Hill stays teaching-only. Source honesty held: compact phase strengths, not broad 85,000/55,000 totals; phase 1 **16,300 US / 7,575 CS**, phase 2 **19,550 US / 11,150 CS**, with **20** Inferred-strength labels. Rank/sector/USCT traps held: Thomas/A. J. Smith/Schofield/Wilson/Steedman Maj. Gens.; McArthur/Wood Brig. Gens.; Hood temporary General/permanent Lt. Gen.; S. D. Lee and Stewart Lt. Gens.; Cheatham/Bate Maj. Gens.; Shy not a general; Forrest absent; S. D. Lee at Peach Orchard/Overton, Cheatham/Bate/Shy at Shy's Hill, Stewart center; 12th/13th/100th USCT plus 18th support context at Peach Orchard/Overton only. D74 held: no battle-specific damage/firepower/casualty/winner/score/force-win keys; artillery uses gun counts. Probe readback: `probe-nashville` **12/12**, US aggregate **8/8**, Redoubts **8/8**, Shy's Hill **8/8**, CS higher loss **8/8**, 0 pageerrors; plan **7/7**; schema **43/43**; roster **8/8**; custom-builder **15/15**; Franklin **10/10**; Kennesaw **10/10**; all required JSON artifacts `ok=true`, 0 failed steps, 0 pageerrors; build GATE OK with known raw-embed soft warning; `git diff --check` clean. **NEXT:** stop at this Group 7 battle-build boundary. Remaining natural candidates include USCT/New Market Heights or another researched lane; D328's Antietam rank-label fix remains a small independent cleanup.

## ⚡ CONTINUE HERE — 2026-07-08 **D334: NASHVILLE SPEC + PLAN PROBE SHIPPED (DOCS/PROBE ONLY).** D334 turns Nashville into the follow-on Franklin/Nashville implementation packet before runtime work: `docs/design/nashville-battle-build-spec.md` and `tools/probe-nashville-plan.mjs`. No `data/nashville.json`, no registry/menu entry, no combat/runtime change, and no generated-HTML behavior change. The spec locks a **two-phase T8** battle: US attacker / CS defender, `defaultFog:false`, Phase 1 Dec. 15 Redoubts/Montgomery Hill scoreWeight 1, Phase 2 Dec. 16 Shy's Hill/Peach Orchard Hill scoreWeight 3. Franklin stays separate; Spring Hill stays teaching-only. Reverified source readback: ABT Nashville materials, Battle of Nashville Trust Peach Orchard/Shy's Hill pages, and Nashville Metro markers support the T8 shape, USCT Peach Orchard role, Shy's Hill decisive break, and sector separation. D335 must not use 85,000/55,000 as literal active map totals; exact unit strengths stay `Verified identity; Inferred strength` unless pinned. Rank/sector traps: Thomas/A. J. Smith/Schofield/Wilson/Steedman Maj. Gens.; McArthur Brig. Gen.; Wood not overpromoted without source note; Hood temporary General/permanent Lt. Gen.; S. D. Lee and Stewart Lt. Gens.; Cheatham/Bate Maj. Gens.; Shy not a general; Forrest absent from the main Nashville field. D74 held: no battle-specific damage/firepower/casualty/winner/score/force-win keys. Gate green: `node --check tools/probe-nashville-plan.mjs`; build GATE OK (known raw-embed soft warning); `probe-nashville-plan` **7/7**; roster **8/8**; custom-builder **15/15**; Franklin **10/10**; Kennesaw **10/10**; all required JSON artifacts `ok=true`, 0 failed steps, 0 pageerrors; `git diff --check` clean. **NEXT:** D335 playable Nashville implementation: `data/nashville.json`, registry/menu after Franklin, focused runtime probe, both historical baselines, schema/build/browser readback, docs sync, commit, push.

## ⚡ CONTINUE HERE — 2026-07-08 **D333: FRANKLIN PLAYABLE BATTLE SHIPPED.** D333 implements the D332 packet as a playable single-phase Franklin/Nashville-lane battle. Runtime/data changes: `data/franklin.json`; registry/menu rank after Kennesaw in `src/tactical/T1-bull-run.js`; focused guard `tools/probe-franklin.mjs`; both historical baselines updated (`probe-tactical-roster`, `probe-custom-battle-builder`); `validate-data-schemas` now treats Franklin as a battle file; generated `civil_war_generals.html` and schema artifact rebuilt. Scenario shape: CS attacker / US defender, fog off, objective = Carter House / Carter cotton gin / Columbia Pike main line; Nashville remains queued as a later T8 battle. Source honesty held: active CS assaulting force **19,000** (inside D332's **18,000-20,000** target), no broader army-present figure, and game-unit strengths carry **Verified identity; Inferred strength** handling. Rank traps held: Hood temporary General / permanent Lt. Gen. nuance; Schofield/Stanley Maj. Gens.; Cox/Wagner Brig. Gens.; Opdycke Col.; Cheatham/Cleburne/Brown Maj. Gens.; Stewart Lt. Gen.; John C. Carter mortally wounded Nov. 30 and died Dec. 10. D74 held: no battle-specific damage/firepower/casualty/winner/score/force-win keys; artillery uses gun counts. Probe readback: `probe-franklin` **10/10**, US holds **8/8**, CS higher loss **8/8**, 0 pageerrors; `probe-franklin-plan` **7/7**; schema **42/42**; roster **8/8**; custom-builder **15/15**; adjacent Kennesaw **10/10**; all required JSON artifacts `ok=true`, **0** failed steps, **0** pageerrors; build **GATE OK** with known raw-embed soft warning; `git diff --check` clean. **NEXT:** stop at this Group 7 battle-build boundary. Remaining natural Group 7 candidates include Nashville, USCT/New Market Heights, or another researched lane; D328's Antietam rank-label fix remains a small independent cleanup.

## ⚡ CONTINUE HERE — 2026-07-08 **D332: FRANKLIN SPEC + PLAN PROBE SHIPPED (DOCS/PROBE ONLY).** D332 turns the Franklin/Nashville research packet into a durable Franklin implementation packet before runtime work: `docs/design/franklin-battle-build-spec.md` and `tools/probe-franklin-plan.mjs`. No `data/franklin.json`, no registry/menu entry, no combat/runtime change, and no generated-HTML behavior change. The spec locks a **single-phase** Franklin battle: CS attacker, US defender, `defaultFog:false`, objective = the Federal main line around the Carter House / Carter cotton gin / Columbia Pike. Nashville remains queued as a later T8 two-phase battle, not merged into this slice. Reverified source readback: roughly **18,000-20,000 Confederate assaulting troops**, not the broader 30,000+ / 33,000 Army of Tennessee present figure; exact brigade strengths require **Verified identity; Inferred strength** treatment unless pinned at runtime. Rank traps: Hood temporary General / permanent Lt. Gen. nuance; Schofield and Stanley Maj. Gens.; Cox/Wagner Brig. Gens.; Opdycke Colonel; Cheatham/Cleburne/Brown Maj. Gens.; Stewart/S. D. Lee Lt. Gens. if included; "six generals killed" teaching must keep John C. Carter as mortally wounded Nov. 30 / died Dec. 10. D74 held: no battle-specific damage/firepower/casualty/winner/score/force-win keys. Gate green: `node --check tools/probe-franklin-plan.mjs`; build **GATE OK** (known raw-embed soft warning); `probe-franklin-plan` **7/7**; roster **8/8**; custom-builder **15/15**; adjacent Kennesaw **10/10**; all required JSON artifacts `ok=true`, **0** failed steps, **0** pageerrors; `git diff --check` clean. **NEXT:** D333 playable Franklin implementation: author `data/franklin.json`, register/menu-rank after Kennesaw, add `tools/probe-franklin.mjs`, update BOTH historical baselines, update schema tooling if needed, rebuild generated HTML from source, run schema/build/focused/adjacent gates, read JSON/pageerrors, docs sync, commit, push.

## ⚡ CONTINUE HERE — 2026-07-08 **D331: KENNESAW MOUNTAIN PLAYABLE BATTLE SHIPPED.** D331 implements the D330 packet as a playable single-phase Atlanta Campaign battle. Runtime/data changes: `data/kennesaw.json`; registry/menu rank after Chattanooga in `src/tactical/T1-bull-run.js`; focused guard `tools/probe-kennesaw.mjs`; both historical baselines updated (`probe-tactical-roster`, `probe-custom-battle-builder`); `validate-data-schemas` now treats Chattanooga and Kennesaw as battle files; generated `civil_war_generals.html` rebuilt. Scenario shape: US attacker / CS defender, fog off, objective = Kennesaw ridge breastworks and the Dead Angle across Pigeon Hill / Little Kennesaw and Cheatham Hill. Source honesty held: Pigeon Hill assault sums to **5,500** Federals, Cheatham Hill to **9,000** Federals, no campaign-scale 150,000/100,000 totals, and unsourced exact brigade/Confederate strengths are marked **Verified identity; Inferred strength**. Rank traps held: Johnston is full General CSA and Hood is not army commander yet; Hardee Lt. Gen.; Cheatham/Cleburne Maj. Gens.; Maney/Vaughn Brig. Gens.; Sherman/Thomas/Schofield/McPherson/Logan Maj. Gens.; McCook/Mitchell Colonels. D74 held: no battle-specific damage/firepower/casualty/winner/score/force-win keys; artillery uses gun counts. Probe readback: `probe-kennesaw` **10/10**, CS holds **8/8**, US higher loss **8/8**, 0 pageerrors; `probe-kennesaw-plan` **6/6**; schema **41/41**; roster **8/8**; custom-builder **15/15**; adjacent Chattanooga **16/16**; all required JSON artifacts `ok=true`, **0** failed steps, **0** pageerrors; build **GATE OK** with known raw-embed soft warning; `git diff --check` clean. **NEXT:** stop at this Group 7 battle-build boundary. If continuing battle-build, choose Franklin/Nashville or USCT unless Aaron reorders; D328's Antietam rank-label fix remains a small independent cleanup.

## ⚡ CONTINUE HERE — 2026-07-08 **D330: KENNESAW MOUNTAIN SPEC + PLAN PROBE SHIPPED (DOCS/PROBE ONLY).** D330 turns the D327 Atlanta/March lead candidate into a durable implementation packet before runtime work: `docs/design/kennesaw-battle-build-spec.md` and `tools/probe-kennesaw-plan.mjs`. No `data/kennesaw.json`, no registry/menu entry, no combat/runtime change, and no generated-HTML behavior change. The spec locks a **single-phase** Kennesaw Mountain battle: US attacker, CS defender, `defaultFog:false`, objective = the Confederate ridge/breastwork line across Pigeon Hill / Little Kennesaw and Cheatham Hill / Dead Angle. The attacker historically loses because of true inputs — terrain, fieldworks, abatis, timing, OOB, doctrine — not D74 output fudge. Sector OOB was reverified before spec: **~5,500 Federals at Pigeon Hill** via Giles A. Smith, A. J. Lightburn, and Walcutt; **~9,000 Federals at Cheatham Hill** via Newton/Davis, with Harker/Wagner/Kimball and McCook/Mitchell named. Rank traps: Sherman/Thomas/Schofield/McPherson Maj. Gen.; Morgan L. Smith Brig. Gen.; Johnston full General CSA and still army commander; Hood not army commander yet; Hardee Lt. Gen.; Cheatham/Cleburne Maj. Gen.; Maney/Vaughn Brig. Gen. D331 must not use Atlanta-campaign totals (150,000/100,000) as battle strengths and must mark unsourced exact brigade strengths as **Verified identity; Inferred strength**. Gate green: `node --check tools/probe-kennesaw-plan.mjs`; build **GATE OK** (known raw-embed soft warning); `probe-kennesaw-plan` **6/6**, JSON `ok=true`, `failedSteps:0`; `git diff --check` clean. **NEXT:** D331 playable Kennesaw implementation: author `data/kennesaw.json`, register/menu-rank after Chattanooga, add `tools/probe-kennesaw.mjs`, update BOTH `probe-tactical-roster` and `probe-custom-battle-builder`, run schema/build/focused/adjacent gates, read JSON/pageerrors, docs sync, commit, push.

## ⚡ CONTINUE HERE — 2026-07-08 **D329: BATTLE-RESEARCH COVERAGE IS NOW COMPLETE (RESEARCH/DOCS ONLY).** D329 closes the gap between D327 (five forward lanes) and D328 (the ten shipped battles): every buildable Civil War battle/campaign that is neither shipped nor already covered now has a durable, citation-grade packet. Six new packets in `docs/design/battle-build-research/` — **`eastern-1862`** (Second Bull Run + the Seven Days beyond the built Malvern Hill), **`shenandoah-1862`** (Jackson's Valley), **`shenandoah-1864`** (Sheridan's Valley), **`western-gaps`** (Perryville + Stones River), **`trans-mississippi`** (Wilson's Creek, Pea Ridge, Glorieta, Red River), **`appomattox-campaign`** (Five Forks, the Apr 2 breakthrough, Sailor's Creek, the surrender) — plus a README rewrite and an extended guard (**15/15**, +1 HYGIENE step catching leaked JSON-escape artifacts across all 21 packets). No data/runtime/registry change; `civil_war_generals.html` byte-unchanged (SHA-256 identical pre/post build, not staged). Built via 6 × [Opus researcher → Opus adversarial verifier] + Opus 4.8 final verify; 12 agents, 0 errors. **All six lanes are READY_FOR_SPEC** — leads: **Gaines' Mill** (single-phase defender-hold, the exact inverse of the built Malvern Hill), **Cross Keys + Port Republic**, **Cedar Creek**, **Stones River**, **Pea Ridge**, **Five Forks**. **⚠ ONE LANE IS BLOCKED ON AARON: `trans-mississippi` sits behind D183's verbatim "M8 battle-build still needs Aaron go/no-go"** — a two-tier gate (Tier 1 = the whole Trans-Mississippi build, even the Native-free Elkhorn Tavern axis; Tier 2 = any Leetown Native OOB, which D183/D178 expressly barred). Its READY_FOR_SPEC means "writable once Aaron clears the gate," NOT "start now." **Do not self-clear it.** The adversarial layer again earned its keep: it **refuted the run prompt's own false claim** that Jackson was never a lieutenant general (he was, from Oct 10 1862 — a blanket "any year" probe guard would have falsely failed a correct future Chancellorsville build), and the main-loop verify then caught Jackson's wounding (May 2 1863) vs death (May 10 1863) conflation. Other fixes: ABT Second Manassas shows Union **125,000 *total*** not engaged and prints an anachronistic "Lt. Gen. Longstreet"; Cedar Creek's 7,682 total is **NPS**'s, not ABT's 8,824 or Wikipedia's 8,575. **Rank flip resolved:** Polk and Hardee were **Maj. Gens. at Perryville (Oct 8 1862)** but **Lt. Gens. at Stones River (Dec 31 1862)** — appointed Oct 10-11, two days after Perryville; Stand Watie was a **Colonel** at Pea Ridge (BG only in 1864); Sterling Price was a **Missouri State Guard** major general, not Confederate. **D74/D92 discovery:** Stones River, Perryville, Wilson's Creek and McDowell **invert** the "winner bleeds less" assumption, so a Stones River spec must guard **near-parity, explicitly NOT US < CS**. **Teaching-only (dignity/engine):** "The Burning," the Leetown scalpings, Sand Creek, the Appomattox surrender, Glorieta Pass (a wagon-train raid decided it), Brandy Station and all cavalry raids, the Valley Campaign's operational maneuver, the Red River river-war. Gate green: `probe-battle-build-research` **15/15**, JSON `ok=true`, hygiene `checked:21 clean:true`; build **GATE OK** (no-fudge ✓ · citations ✓; known raw-embed soft warning); `git diff --check` clean. **NEXT:** the library is complete, so a battle-build slice now starts from assembled evidence. The most-ready implementation slice is still **Kennesaw Mountain (Atlanta/March)** per D327; **Gaines' Mill** and **Stones River** are the strongest new additions. Codex may still apply the single Antietam `us_richardson.commander` label fix from D328 at any time.

## ⚡ PREVIOUS — 2026-07-08 **D328: BUILT-BATTLE RESEARCH/AUDIT LAYER SHIPPED (all 10 shipped battles), FOR CODEX REVISION (RESEARCH/DOCS ONLY).** In response to Aaron's mid-run request to confirm the already-built battles are researched/supported to the D327 standard, D328 adds `docs/design/battle-build-research/built-battles/` — a README index + one durable audit packet per shipped battle (Bull Run, Antietam, Fredericksburg, Chancellorsville, Malvern Hill, Gettysburg, Shiloh, Vicksburg, Chickamauga, Chattanooga) — and extends `tools/probe-battle-build-research.mjs` to guard it (now **14/14**). No data/runtime/registry/generated-HTML change; `civil_war_generals.html` is byte-unchanged. Each audit carries a Source Register, Confirmed-Solid list, a **Revision Checklist For Codex**, OOB/rank re-verification, terrain+teaching/anti-Lost-Cause check, D74/D92 no-fudge check, and a SOLID_AS_IS/MINOR_REVISIONS/NEEDS_REVISION verdict. Built via Opus auditor → adversarial Opus verifier → Opus 4.8 final verify against the live data. **Finding: 9 of 10 are SOLID_AS_IS** (the D92/D86/D90/D325 hardening held under independent re-verification). The two-layer design paid off twice: **Antietam = MINOR_REVISIONS** — one confirmed, outcome-neutral fix for Codex: `us_richardson.commander` `Brig. Gen.` → **`Maj. Gen. Israel B. Richardson`** (MG since Jul 4 1862; the file's own `ld_richardson` leader entry already says Maj. Gen., so it's an internal self-contradiction) — and **Fredericksburg** had one auditor **false flag refuted** (Owen was genuinely a Brig. Gen. via a Nov 1862 recess appointment, unlike Zook; DO-NOT-APPLY). Gate green: `probe-battle-build-research` **14/14**; build **GATE OK** (known raw-embed soft warning only); `git diff --check` clean. **NEXT:** Codex may apply the single Antietam `us_richardson.commander` label fix at any time (then re-gate: build GATE OK + `probe-antietam` 16/16 + `git diff --check`). The next battle-build implementation slice is **Kennesaw Mountain (Atlanta/March)** per D327 unless Aaron reorders.

## ⚡ PREVIOUS — 2026-07-08 **D327: DURABLE BATTLE-BUILD RESEARCH LIBRARY SHIPPED FOR EVERY REMAINING LANE (RESEARCH/DOCS ONLY).** D327 adds `docs/design/battle-build-research/` — a `README.md` index plus five citation-grade research packets (`atlanta-march`, `franklin-nashville`, `usct`, `naval-river`, `1864-65-attrition`) — and the guard `tools/probe-battle-build-research.mjs`. No `data/*.json`, no registry line, no runtime, no generated-HTML change; `civil_war_generals.html` is byte-unchanged. Each packet ranks candidate battles by buildability, recommends a playable shape (single-phase vs T8, roles, phases, scoreWeight logic), and carries a Source Register (URLs + confidence), OOB/rank traps, terrain landmarks, teaching cards + anti-Lost-Cause framing, D74 no-fudge risks, probe teeth, a verdict, and the exact next slice. Built via an Opus research→adversarial-verify workflow + Opus 4.8 final verify (fixed: Forrest=Lt.Col. at Donelson; 54th Mass ~40-45% at Fort Wagner; Jonesborough day-by-day casualties; two-phase scoreWeights sum to 4; Cold Harbor "7,000 in 30 min" = disputed myth). **All five lanes are READY_FOR_SPEC for their lead candidate** — Kennesaw Mountain (Atlanta), Franklin, New Market Heights (USCT), Fort Donelson (naval/river), Spotsylvania/Overland (attrition). **DO_NOT_BUILD_NOW:** all ship-vs-ship naval actions (no naval engine) and **Fort Pillow** (massacre = teaching only). D74/D92 no-fudge and the dignity line held. Gate green: `probe-battle-build-research` **8/8**; build **GATE OK** (known raw-embed soft warning only); JSON readback `ok=true`; `git diff --check` clean. **NEXT (this session, per Aaron's mid-run request):** a research/audit pass over the already-built battles (D328) so Codex can revise them alongside building new battles; then the first battle-build spec slice — **Kennesaw Mountain (Atlanta/March)** — unless Aaron reorders to Franklin/Nashville, USCT, or Fort Donelson.

## ⚡ PREVIOUS — 2026-07-08 **D326: CHATTANOOGA PLAYABLE BATTLE SHIPPED; NEXT = ATLANTA/MARCH PLANNING SPEC UNLESS AARON REORDERS.** D326 implements the D325 Chattanooga packet as a playable three-phase T8 battle. New runtime data: `data/chattanooga.json`; registry/menu order: Chattanooga after Chickamauga; new focused guard: `tools/probe-chattanooga.mjs`; both historical baselines now include `chattanooga`. Scenario shape: Orchard Knob / Indian Hill (US seizes), Lookout Mountain / Cravens-house bench below the palisade (US seizes with lighter-casualty/fog teaching), and Missionary Ridge (US seizes, scoreWeight-3 decisive phase), with top-level US attacker / CS defender. Cleburne/Tunnel Hill/Ringgold Gap are included as teaching/end-note content rather than an unsupported fourth playable phase. D74 held: no per-battle damage/firepower/casualty/winner fudge. Focused gate green: build GATE OK with known raw-embed soft warning; syntax clean for touched JS/probes; `probe-chattanooga-plan` **6/6**; `probe-chattanooga` **16/16**; `probe-tactical-roster` **8/8**; `probe-custom-battle-builder` **15/15**; `probe-vicksburg` **18/18**; `probe-chickamauga` **18/18**; all required JSON artifacts `ok=true`, **0** failed steps, **0** pageerrors; `git diff --check` clean. **NEXT:** stop at this battle-build boundary. The next queued battle-build item is an Atlanta/March durable planning/spec slice unless Aaron reorders to Franklin/Nashville, USCT, Phase H, Soldier's Story, Phase D, or custom-battle phase authoring.

## ⚡ PREVIOUS — 2026-07-08 **D325: CHATTANOOGA BATTLE-BUILD SPEC + PLAN PROBE SHIPPED; SUPERSEDED BY D326 PLAYABLE CHATTANOOGA.** D325 began the highest-priority unlocked battle-build lane without guessing at OOB/source details. It shipped `docs/design/chattanooga-battle-build-spec.md` and `tools/probe-chattanooga-plan.mjs`, with no runtime implementation. D326 then authored the playable scenario and runtime probe.

## ⚡ PREVIOUS — 2026-07-08 **D324: ALL PREVIOUSLY LOCKED PHASE LANES UNLOCKED; SUPERSEDED BY D325 CHATTANOOGA PLANNING.** This is a docs/routing milestone on top of D323. The old "do not start M8/Q5/Q6/Phase H media/Tripo/Soldier's Story/Phase D" language is superseded. All those lanes are now eligible under normal gates. If Aaron gives no narrower target, start with a short fresh-chat planning pass over the unlocked lanes and recommend the next concrete slice; then execute that slice with the normal source/doc read order, focused gate, JSON/pageerror readback, docs sync, commit, and push. D323 remains the latest code/content change: player-visible, gated cross-theater Transfer shipped green at `probe-command` **88/88**, with ratings/OOB/bridge adjacents green and **0** pageerrors.

## ⚡ PREVIOUS — 2026-07-08 **D323: GROUP 2 EXPLICIT TRANSFER MOVE SHIPPED; SUPERSEDED BY D324 ROUTING UNLOCK.** D323 builds the player-visible, gated cross-theater Transfer move over D322's theater substrate. The Command desk now has a `Cross-theater transfer` section; `cmdTransfer(C,id)` spends configured political capital and stores only current-battle readiness under `C.president.command.transfer`; `cmdTransferReadiness(C,id)` reports natural fit vs transferred readiness. `cmdInit` drops malformed, stale, native-fit, uncommissioned, bogus, and wrong-theater Transfer saves. The output wall remains intact: no hidden enemy Transfer, no enemy command save state, no hidden commissions, and no scoreboard/battle-result/casualty/OOB-total/combat-output writes. Focused gate green: `node --check src/35-command.js`; `node --check tools/probe-command.mjs`; `node tools/build.mjs` GATE OK with known raw-embed soft warning; `node tools/validate-data-schemas.mjs` **39/39**; `node tools/probe-command.mjs` **88/88**, **0** pageerrors; adjacents `node tools/probe-ratings.mjs` **21/21**, `node tools/probe-oob.mjs` **17/17**, and `node tools/probe-bridge.mjs` **6/6**, all **0** pageerrors. **NEXT = superseded by D324 unlock.**

## ⚡ PREVIOUS — 2026-07-08 **D322: GROUP 2 GM/TRANSFER THEATER SUBSTRATE SHIPPED; SUPERSEDED BY D323 TRANSFER MOVE.** D322 closes the D173 blocker by adding broad `theater` / `theaters` classification to all **26** command-pool records (starting rosters + commission pools) and pure command helpers that resolve next-battle theater and `cmdTransferReadiness(C,id)`. The AI-GM enemy shadow now carries commander theater and battle-theater readout, but it still writes no enemy command save state, performs no hidden Transfer, commissions no hidden officers, and touches no scoreboard/output path. Focused gate green: `node --check src/35-command.js`; `node --check tools/probe-command.mjs`; `node tools/build.mjs` GATE OK with known raw-embed soft warning; `node tools/validate-data-schemas.mjs` **39/39**; `node tools/probe-command.mjs` **81/81**, **0** pageerrors. **NEXT = superseded by D323.**

## ⚡ PREVIOUS — 2026-07-08 **D321: GROUP 6 SOURCE-FILE INVENTORY CLOSEOUT SHIPPED; MOVE TO GROUP 2 GM/TRANSFER SUBSTRATE NEXT.** D320 added read-only diagnostic/reporting tools (`validate-data-schemas`, `check-orphan-assets`, `summarize-probe-logs`, `inventory-source-files`, report/dashboard exporters) and D320b refreshed tracked report outputs. D321 closes the loose end in that batch: `tools/inventory-source-files.mjs` now walks `src/**/*.js` instead of only top-level `src/*.js`, and new `tools/probe-source-file-inventory.mjs` pins the report against the live source tree. Focused gate green: source inventory **91** JS files / **29** tactical modules / **34,207** lines / **1,846** functions / **0** exports; source-file inventory probe **4/4**; build GATE OK with known raw-embed soft warning; adjacents `probe-group6-readback` **9/9**, `probe-media-budget` **13/13**, and `probe-historical-source-domains` **6/6**; `git diff --check` clean. **NEXT = superseded by D322.**

## ⚡ PREVIOUS — 2026-07-08 **D319: GROUP 6 HISTORICAL SOURCE-DOMAIN POLICY-READBACK CONSISTENCY GUARD SHIPPED; PREPARE TO STOP AT NEXT SAFE BUNDLE BOUNDARY.** D319 is tooling-only: `tools/probe-historical-source-domains.mjs` now enforces that policy readback `current*` values match computed stats (`invalidUrlItems`, `concentrationTop20Pct`, `uniqueDomains`) in the same artifact. Focused gate green: `node --check probe-historical-source-domains.mjs`; `node build.mjs` GATE OK with known raw-embed soft warning; `node probe-historical-source-domains.mjs` **6/6**; adjacents `node probe-group6-readback.mjs` **9/9** and `node probe-media-budget.mjs` **13/13**; readback confirms `policyConsistencyStep=true`, URL items **152**, unique domains **33**; `git diff --check` clean. **NEXT = superseded by D320/D321.**

## ⚡ PREVIOUS — 2026-07-08 **D310: GROUP 6 CONSOLIDATED READBACK GUARD SHIPPED.** D298 remains the latest playable-content milestone: John Bell Hood is the thirty-first `Verified` Soldier's Story replacement, lane count **31**, side balance **15US/16CS**, no engine/combat change, D74 intact. D299 deferred remaining generated/unresearched Soldier's Story replacement rows until all other queued tasks/phases are done. D300 froze the self-contained core while raw embeds are above soft warning, froze PD still/surviving-colours assets, and kept H2 footage / HDRI / model media disabled. D301-D306 made the media-budget probe enforce/read back category ceilings, source mirror, informative metadata, policy state, source organization, and source inventory. D307 updated the operating rule for same-chat bundles. D308 shipped read-only historical-data/source inventory tooling. D309 shipped read-only hotpath profile tooling. **D310 shipped read-only consolidated Group 6 readback tooling:** new `tools/probe-group6-readback.mjs` re-runs and cross-checks media-budget, historical-data inventory, and hotpath profile artifacts, then writes `tools/shots/probe-group6-readback.json` and `tools/shots/group6-readback.json`. Current consolidated readback: media raw tier **soft-warning** at **2.418 MB** with D300-D303 guards/source parity/metadata green; historical inventory **1,559 source items / 675 source notes**; hotpath profile **381 functions / 228 loop sites / one fetch isolated to T28**. Focused gate green: syntax, build GATE OK with known raw-embed soft warning, consolidated readback probe **6/6**, adjacent media-budget **12/12**, JSON readback, and `git diff --check`. **NEXT = superseded by D311.**

## ⚡ PREVIOUS — 2026-07-08 **D309: GROUP 6 HOTPATH PROFILE READBACK SHIPPED.** D309 shipped `tools/profile-hotpaths.mjs` and `tools/probe-hotpath-profile.mjs`, emitting/gating a static source profile over core-field, battle-step, render-3d, orders, AI, and opt-in connector surfaces. Focused gate green: syntax, build GATE OK with known raw-embed soft warning, hotpath profile **6/6**, adjacent field **23 steps / 0 pageerrors**, JSON readback, and `git diff --check`. **NEXT = superseded by D310.**

## ⚡ PREVIOUS — 2026-07-08 **D308: GROUP 6 HISTORICAL-DATA SOURCE INVENTORY SHIPPED.** D308 shipped `tools/historical-data-inventory.mjs` and `tools/probe-historical-data-inventory.mjs`, emitting probeable artifacts over the canonical `HISTORICAL-DATA*.md` docs and all `data/*.json` source fields. Focused gate green: syntax, build GATE OK with known raw-embed soft warning, historical-data inventory **5/5**, adjacent primary-sources **14 steps / 0 pageerrors**, JSON readback, and `git diff --check`. **NEXT = superseded by D309.**

## ⚡ PREVIOUS — 2026-07-08 **D307: SAME-CHAT EXECUTION BUNDLES ARE NOW THE DEFAULT FOR SAFE HOMOGENEOUS QUEUES; D306 REMAINS THE LATEST MEDIA-BUDGET TOOLING SLICE.** D307 updated the operating rule: for Group 6-style safe tooling/reporting queues, ship a 2-4-slice same-chat bundle, committing and pushing each green slice before continuing. Stop only when the bundle is exhausted, before a new group/phase, on a fork/risk/lock, before a browser-heavy/full-suite gate, or near context risk. **NEXT = superseded by D308.**

## ⚡ PREVIOUS — 2026-07-08 **D306: GROUP 6 MEDIA-BUDGET SOURCE-INVENTORY READBACK SHIPPED; FUTURE GATES CAN SEE EXACT CORE SOURCE/EMBED PARITY DIRECTLY.** D306 shipped `data/media-budget.json` schemaVersion 1.6 and `tools/probe-media-budget.mjs` `metrics.sourceInventory`; focused gate green: `node --check tools/probe-media-budget.mjs`, build GATE OK with known raw-embed soft warning, media-budget probe **12/12**, exact source/embed parity green for portraits **156/156**, weapons **8/8**, artillery **2/2**, USCT **7/7**, leaders **20/20**, scenes **6/6**, no source-only files, embed-only files, or duplicate stems; JSON readback and `git diff --check` clean. **NEXT = superseded by D307's bundle operating rule.**

## ⚡ PREVIOUS — 2026-07-08 **D305: GROUP 6 MEDIA-BUDGET SOURCE-ORGANIZATION READBACK SHIPPED; FUTURE GATES CAN SEE THE SOURCE-TIER/METADATA POSTURE DIRECTLY.** D298 remains the latest playable-content milestone: John Bell Hood is the thirty-first `Verified` Soldier's Story replacement, lane count **31**, side balance **15US/16CS**, no engine/combat change, D74 intact. D299 deferred remaining generated/unresearched Soldier's Story replacement rows until all other queued tasks/phases are done. D300 froze the self-contained core while raw embeds are above soft warning, froze PD still/surviving-colours assets, and kept H2 footage / HDRI / model media disabled. D301 made the D300 per-category freeze ceilings executable. D302 made the source-tier mirror executable. D303 made informative metadata coverage executable. D304 made policy-state readback executable. **D305 shipped the fifth Priority-1 Group 6 tooling slice:** `data/media-budget.json` schemaVersion 1.5 now records `requireSourceOrganizationReadback`, and `tools/probe-media-budget.mjs` emits/enforces `metrics.sourceOrganization` with declared core categories, informative categories, source/embed counts, mirror status, and metadata coverage. Focused gate green: `node --check tools/probe-media-budget.mjs`; build GATE OK with known raw-embed soft warning; media-budget probe **11/11**, **199 files / 2.418 MB raw**, source mirror green for portraits **156/156**, weapons **8/8**, artillery **2/2**, USCT **7/7**, leaders **20/20**, scenes **6/6**, informative metadata green for USCT **7**, leaders **20**, scenes **6**, no missing source categories, no metadata issue categories, warnings=1; JSON readback and `git diff --check` clean. **NEXT = superseded by D306.** Do not add media assets, fetch assets, spend credits, use Tripo, enable H2 footage, reopen zero-byte Phase H polish, pick another Soldier's Story slice, or start M8/Q5/Q6. Phase D deferred.

## ⚡ PREVIOUS — 2026-07-08 **D304: GROUP 6 MEDIA-BUDGET POLICY-STATE READBACK SHIPPED; FUTURE GATES CAN SEE THE ACTIVE D300-D303 POSTURE DIRECTLY.** D298 remains the latest playable-content milestone: John Bell Hood is the thirty-first `Verified` Soldier's Story replacement, lane count **31**, side balance **15US/16CS**, no engine/combat change, D74 intact. D299 deferred remaining generated/unresearched Soldier's Story replacement rows until all other queued tasks/phases are done. D300 froze the self-contained core while raw embeds are above soft warning, froze PD still/surviving-colours assets, and kept H2 footage / HDRI / model media disabled. D301 made the D300 per-category freeze ceilings executable. D302 made the source-tier mirror executable. D303 made informative metadata coverage executable. **D304 shipped the fourth Priority-1 Group 6 tooling slice:** `data/media-budget.json` schemaVersion 1.4 now records `requirePolicyStateReadback`, and `tools/probe-media-budget.mjs` emits/enforces `metrics.policyState` with raw tier, soft/review/hard/file headroom, active D300-D303 guards, H2/heavy-media lock state, and zero/negative frozen-category headroom. Focused gate green: `node --check tools/probe-media-budget.mjs`; build GATE OK with known raw-embed soft warning; media-budget probe **10/10**, **199 files / 2.418 MB raw**, rawTier **soft-warning**, review headroom **348,121 bytes**, hard headroom **610,265 bytes**, file headroom **61**, all D300-D303 guards active, all six core categories at zero file/raw-byte headroom, no negative headroom, warnings=1; JSON readback and `git diff --check` clean. **NEXT = superseded by D305.** Do not add media assets, fetch assets, spend credits, use Tripo, enable H2 footage, reopen zero-byte Phase H polish, pick another Soldier's Story slice, or start M8/Q5/Q6. Phase D deferred.

## ⚡ PREVIOUS — 2026-07-08 **D303: GROUP 6 MEDIA-BUDGET INFORMATIVE-METADATA GUARD SHIPPED; INFORMATIVE EMBEDS CANNOT LOSE ALT/CAPTION/CREDIT COVERAGE.** D298 remains the latest playable-content milestone: John Bell Hood is the thirty-first `Verified` Soldier's Story replacement, lane count **31**, side balance **15US/16CS**, no engine/combat change, D74 intact. D299 deferred remaining generated/unresearched Soldier's Story replacement rows until all other queued tasks/phases are done. D300 froze the self-contained core while raw embeds are above soft warning, froze PD still/surviving-colours assets, and kept H2 footage / HDRI / model media disabled. D301 made the D300 per-category freeze ceilings executable. D302 made the source-tier mirror executable. **D303 shipped the third Priority-1 Group 6 tooling slice:** `data/media-budget.json` schemaVersion 1.3 now records `requireInformativeEmbedMetadata`, and `tools/probe-media-budget.mjs` enforces exact metadata coverage plus nonempty `alt`/`caption`/`credit` for declared informative embedded categories. Focused gate green: `node --check tools/probe-media-budget.mjs`; build GATE OK with known raw-embed soft warning; media-budget probe **9/9**, **199 files / 2.418 MB raw**, D300 freeze active, all category headroom **0**, source mirror green, informative metadata green for USCT **7/7**, leaders **20/20**, scenes **6/6**, warnings=1; JSON readback and `git diff --check` clean. **NEXT = superseded by D304.** Do not add media assets, fetch assets, spend credits, use Tripo, enable H2 footage, reopen zero-byte Phase H polish, pick another Soldier's Story slice, or start M8/Q5/Q6. Phase D deferred.

## ⚡ PREVIOUS — 2026-07-08 **D302: GROUP 6 MEDIA-BUDGET SOURCE-MIRROR GUARD SHIPPED; EMBEDS CANNOT ORPHAN FROM SOURCE TIER.** D298 remains the latest playable-content milestone: John Bell Hood is the thirty-first `Verified` Soldier's Story replacement, lane count **31**, side balance **15US/16CS**, no engine/combat change, D74 intact. D299 deferred remaining generated/unresearched Soldier's Story replacement rows until all other queued tasks/phases are done. D300 froze the self-contained core while raw embeds are above soft warning, froze PD still/surviving-colours assets, and kept H2 footage / HDRI / model media disabled. D301 made the D300 per-category freeze ceilings executable. **D302 shipped the second Priority-1 Group 6 tooling slice:** `data/media-budget.json` schemaVersion 1.2 now records `requireEmbedSourceMirror`, and `tools/probe-media-budget.mjs` enforces that every declared core `assets/embed/<category>/` file has a same-stem source file under that category's `sourceDir`. Focused gate green: `node --check tools/probe-media-budget.mjs`; build GATE OK with known raw-embed soft warning; media-budget probe **8/8**, **199 files / 2.418 MB raw**, D300 freeze active, all category headroom **0**, source mirror green for portraits **156/156**, weapons **8/8**, artillery **2/2**, USCT **7/7**, leaders **20/20**, scenes **6/6**, warnings=1; JSON readback and `git diff --check` clean. **NEXT = superseded by D303.** Do not add media assets, fetch assets, spend credits, use Tripo, enable H2 footage, reopen zero-byte Phase H polish, pick another Soldier's Story slice, or start M8/Q5/Q6. Phase D deferred.

## ⚡ PREVIOUS — 2026-07-08 **D301: GROUP 6 MEDIA-BUDGET TOOLING STARTED; D300 FROZEN-CORE CATEGORY CEILINGS ARE EXECUTABLE.** D298 remains the latest playable-content milestone: John Bell Hood is the thirty-first `Verified` Soldier's Story replacement, lane count **31**, side balance **15US/16CS**, no engine/combat change, D74 intact. D299 deferred remaining generated/unresearched Soldier's Story replacement rows until all other queued tasks/phases are done. D300 froze the self-contained core while raw embeds are above soft warning, froze PD still/surviving-colours assets, and kept H2 footage / HDRI / model media disabled. **D301 shipped the first Priority-1 Group 6 tooling slice:** `data/media-budget.json` schemaVersion 1.1 now records per-category D300 freeze ceilings, and `tools/probe-media-budget.mjs` enforces/reports them while raw embeds remain above soft warning. Focused gate green: `node --check tools/probe-media-budget.mjs`; build GATE OK with known raw-embed soft warning; media-budget probe **7/7**, **199 files / 2.418 MB raw**, D300 freeze active, all category headroom **0**, warnings=1; JSON readback and `git diff --check` clean. **NEXT = superseded by D302.** Do not add media assets, fetch assets, spend credits, use Tripo, enable H2 footage, reopen zero-byte Phase H polish, pick another Soldier's Story slice, or start M8/Q5/Q6. Phase D deferred.

## ⚡ PREVIOUS — 2026-07-08 **D300: MEDIA-BUDGET/PROFILE DECISION RESOLVED CONSERVATIVELY; GROUP 6 TOOLING ACTIVE.** D298 remains the latest playable-content milestone: John Bell Hood is the thirty-first `Verified` Soldier's Story replacement, lane count **31**, side balance **15US/16CS**, no engine/combat change, D74 intact. D299 deferred remaining generated/unresearched Soldier's Story replacement rows until all other queued tasks/phases are done. **Aaron adopted all three D300 media recommendations:** keep the self-contained core frozen while the raw embed tier is above soft warning, keep PD still/surviving-colours asset work frozen, and keep H2 footage / HDRI / model media disabled. Focused gate green: build GATE OK with known raw-embed soft warning; media budget **6/6**, **199 files / 2.418 MB raw**; Intel UHD-617 profile **19/19**, high **10.74ms / 75 calls / 116 objects**, low **1.98ms / 63 calls / 97 objects**, **0 pageerrors**, **0 texture warnings**; JSON readback and `git diff --check` clean. **NEXT = superseded by D301.** Do not add media assets, fetch assets, spend credits, use Tripo, enable H2 footage, reopen zero-byte Phase H polish, pick another Soldier's Story slice, or start M8/Q5/Q6. Phase D deferred.

## ⚡ PREVIOUS — 2026-07-08 **D299: SOLDIER'S STORY REMAINING GENERATED/UNRESEARCHED ROWS DEFERRED; MEDIA-BUDGET/PROFILE CHECKPOINT ACTIVE.** D298 remains the latest playable-content milestone: John Bell Hood is the thirty-first `Verified` Soldier's Story replacement, lane count **31**, side balance **15US/16CS**, no engine/combat change, D74 intact. **Aaron directive:** the currently researched Soldier's Story rows are complete and shipped through D298; defer the remaining generated/unresearched Soldier's Story replacement rows until all other queued tasks/phases are done. **NEXT = superseded by D300.** Before any real surviving-colours/PD asset work, H2 moving footage, HDRI/model media, optional HD pack, or other media-heavy work, run/read `data/media-budget.json`, `tools/probe-media-budget.mjs`, `tools/probe-intel-uhd617-profile.mjs`, and the latest JSON artifacts, then present exactly three popup-style decisions: core single-file vs optional HD pack path; PD still-image lane vs assets frozen; H2/HDRI/model media disabled vs bounded provenance/perf pilot. D300 resolved those choices conservatively. M8/Q5/Q6 locked; Phase D deferred.

## ⚡ PREVIOUS — 2026-07-08 **D298: JOHN BELL HOOD SHIPPED AS THE THIRTY-FIRST SOLDIER'S STORY REPLACEMENT.** Bounded Priority-1 Chickamauga/CS command-row slice: Hood replaces `ss:chickamauga:CS:cs_hood_arrives:cmd`, the open Hood's Arriving Brigades command row. Lane now has **31 `Verified` records**; side balance is **15US/16CS**. Data + probe only; no engine/combat change, D74 intact. **Honesty:** ships at Maj. Gen., Hood's Division, Longstreet's Left Wing / Hood's Corps, Army of Tennessee; no lieutenant-general-at-Chickamauga claim, no company-command claim, no portrait, no later Atlanta/Franklin command-rating claim, no sourced ratings. Sources: NPS John Bell Hood page, U.S. Army Staff Ride Handbook for Chickamauga, NPS History Chickamauga narrative, American Battlefield Trust Chickamauga facts, and American Battlefield Trust Hood biography. **Gate:** importer OK records=31 verified=31 disputed=0 · importer `--check` OK records=31 verified=31 disputed=0 · `node tools/build.mjs` GATE OK (known raw embed soft warning only) · `node --check tools/probe-loot-survival.mjs` · **probe-loot-survival ok=true 11/11, applied=31, 0 pageerrors** · adjacents **probe-tactical-roster 8/8** + **probe-women-in-war 8/8, 0 pageerrors** (`replacementRecords:31`, lane still separate) · JSON readback clean · `git diff --check` clean. Full `vet:noreg` was not rerun because D292 completed the recent batch/release checkpoint. **NEXT = superseded by D299.**

## ⚡ PREVIOUS — 2026-07-08 **D297: BUSHROD R. JOHNSON SHIPPED AS THE THIRTIETH SOLDIER'S STORY REPLACEMENT.** Bounded Priority-1 Western/CS command-row slice: Johnson replaces `ss:chickamauga:CS:cs_johnson_gap:cmd`, the open Chickamauga / Johnson's Provisional Division breakthrough command row. Lane now has **30 `Verified` records**; side balance is **15US/15CS**. Data + probe only; no engine/combat change, D74 intact. **Honesty:** ships at Brig. Gen., Johnson's Provisional Division, Longstreet's Left Wing / Hood's Corps, Army of Tennessee; no major-general-at-Chickamauga claim, no company-command claim, no portrait, no sourced ratings. Sources: NPS Bushrod Johnson page, U.S. Army Staff Ride Handbook for Chickamauga, NPS History Chickamauga narrative, American Battlefield Trust Chickamauga facts, and American History Central. **Gate:** importer OK records=30 verified=30 disputed=0 · importer `--check` OK records=30 verified=30 disputed=0 · `node tools/build.mjs` GATE OK (known raw embed soft warning only) · `node --check tools/probe-loot-survival.mjs` · **probe-loot-survival ok=true 11/11, applied=30, 0 pageerrors** · adjacents **probe-tactical-roster 8/8** + **probe-women-in-war 8/8, 0 pageerrors** (`replacementRecords:30`, lane still separate) · JSON readback clean · `git diff --check` clean. Full `vet:noreg` was not rerun because D292 just completed the batch/release checkpoint. **NEXT = superseded by D298. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-08 **D296: JOHN W. HALEY SHIPPED AS THE TWENTY-NINTH SOLDIER'S STORY REPLACEMENT.** Bounded Priority-1 Gettysburg/US slice: Haley replaces `ss:gettysburg:US:us_birney_iii:pvt`, the open Gettysburg / Birney's Division / III Corps private row. Lane now has **29 `Verified` records**; side balance is **15US/14CS**. Data + probe only; no engine/combat change, D74 intact. **Honesty:** ships at Private, Company I, 17th Maine Infantry, de Trobriand's Brigade, Birney's First Division, III Corps / Army of the Potomac; no Gettysburg-corporal claim, no portrait, no sourced ratings. Sources: Haley journal/bibliography trail, ECW John Haley/17th Maine article, NPS Gettysburg Army of the Potomac OOB, Maine at War Gettysburg march article, UNE/Saco Museum exhibit page, and Civil War Monitor Trobriand/Wheatfield article. **Gate:** importer OK records=29 verified=29 disputed=0 · importer `--check` OK records=29 verified=29 disputed=0 · `node tools/build.mjs` GATE OK (known raw embed soft warning only) · `node --check tools/probe-loot-survival.mjs` · **probe-loot-survival ok=true 11/11, applied=29, 0 pageerrors** · adjacents **probe-tactical-roster 8/8** + **probe-women-in-war 8/8, 0 pageerrors** (`replacementRecords:29`, lane still separate) · JSON readback clean · `git diff --check` clean. Full `vet:noreg` was not rerun because D292 just completed the batch/release checkpoint. **NEXT = superseded by D297. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-08 **D295: TALIAFERRO N. "TALLY" SIMPSON SHIPPED AS THE TWENTY-EIGHTH SOLDIER'S STORY REPLACEMENT.** Bounded Priority-1 Western/CS slice: Simpson replaces `ss:chickamauga:CS:cs_kershaw_rock:nco`, the open Chickamauga / Kershaw's Brigade NCO row. Lane now has **28 `Verified` records**; side balance is **14US/14CS**. Data + probe only; no engine/combat change, D74 intact. **Honesty:** ships at Corporal, Companies A/B, 3rd South Carolina Infantry, Kershaw's Brigade, McLaws' Division, Longstreet's Corps / Army of Tennessee; no portrait, no promotion, no company command, no sourced ratings. Sources: Oxford/PagePlace preview of *Far, Far from Home*, Clemson Simpson bio, Old Pendleton January 2013 newsletter, ACWScots Chickamauga Confederate OOB, and NPS History Chickamauga narrative. The bio keeps the Confederate enlisted letters tied to slavery by noting enslaved Zion rather than romanticizing the voice. **Gate:** importer OK records=28 verified=28 disputed=0 · importer `--check` OK records=28 verified=28 disputed=0 · `node tools/build.mjs` GATE OK (known raw embed soft warning only) · `node --check tools/probe-loot-survival.mjs` · **probe-loot-survival ok=true 11/11, applied=28, 0 pageerrors** · adjacents **probe-tactical-roster 8/8** + **probe-women-in-war 8/8, 0 pageerrors** (`replacementRecords:28`, lane still separate) · JSON readback clean · `git diff --check` clean. Full `vet:noreg` was not rerun because D292 just completed the batch/release checkpoint. **NEXT = superseded by D296. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-07 **D294: JOHN CAMDEN WEST SHIPPED AS THE TWENTY-SEVENTH SOLDIER'S STORY REPLACEMENT.** Bounded Priority-1 Western/CS slice: West replaces `ss:chickamauga:CS:cs_law_gap:pvt`, the still-open Chickamauga / Hood's-Law's Division private slot beside D291's Val C. Giles NCO row. Lane now has **27 `Verified` records**; side balance improves 14US/12CS → **14US/13CS**. Data + probe only; no engine/combat change, D74 intact. **Honesty:** ships at Private, Company E, 4th Texas Infantry, Robertson's Texas Brigade, Hood's Division (Law commanding), Longstreet's Corps / Army of Tennessee; no portrait, no promotion, no company command, no sourced ratings. Sources: TSHA John Camden West entry, Online Books Page/HathiTrust listing for *A Texan in Search of a Fight*, Daily Observations transcription of West's 24 Sep 1863 Chickamauga letter, NPS 4th Texas battle-unit page, and Chickamauga OOB placing the 4th Texas in Robertson/Hood/Law. **Gate:** importer OK records=27 verified=27 disputed=0 · `node tools/build.mjs` GATE OK (known raw embed soft warning only) · `node --check tools/probe-loot-survival.mjs` · **probe-loot-survival ok=true 11/11, applied=27, hostileRejected=true, 0 pageerrors** · adjacents **probe-tactical-roster 8/8** + **probe-women-in-war 8/8, 0 pageerrors** (`replacementRecords:27`, lane still separate) · JSON readback clean · `git diff --check` clean. Full `vet:noreg` was not rerun because D292 just completed the batch/release checkpoint. **NEXT = superseded by D295. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-07 **D292: BATCH/RELEASE NO-REGRESSION CHECKPOINT COMPLETE AFTER D284-D291.** Verification-only run at HEAD `d0556d4` covered the LLM-opponent arc, E59, and Soldier's Story records 17-25. No `src/` or `data/` edits. The full suite was completed in segmented local-Mac coverage: `strategy bridge` and `disease medical` printed/wrote clean artifacts before teardown hangs; `probe-weather` was rerun standalone green **30/30** after an early interruption; `probe-tactical-visuals` was rerun standalone green **10/10** after the wrapper's 600s timeout; the final resume from `tactical roster` ended **`VET NO-REGRESSION OK — 16 commands`**. Every suite entry is green or covered by a green standalone rerun; JSON/pageerror artifacts were read back. **Probe-presets verdict:** green **27/27, 0 pageerrors** — D288's parenthetical claiming an expected pre-existing E60 red was stale copy; D278 already resolved E60 and this checkpoint confirms it remains resolved. Known media warning remains only the raw embed soft warning (`rawMB=2.418`). **Queue returned to bounded Priority-1 Soldier's Story slices, preferably Western/CS. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-07 **D291: PHASE I SOLDIER'S STORY SCALE-OUT — THREE NEW CITATION-GRADE CS WESTERN REPLACEMENTS (23-25), A SECOND THEATER-BALANCING SLICE. The lane grows 22→25 `Verified` records; side balance 14US/8CS → 14US/11CS; all three Confederate AND Western, deepening Chickamauga (→3 CS), Vicksburg (→2 CS), and Shiloh (→2 CS).** New records in `data/soldier-replacements.json`: **Val C. Giles** (`ss:chickamauga:CS:cs_law_gap:nco` — Sgt, 4th Sergeant from May 1863, Co. B "Tom Green Rifles", 4th Texas, Robertson's Texas Bde / Hood's Division (Law commanding 20 Sep) / Longstreet's Corps on loan, Chickamauga 19-20 Sep 1863; memoir *Rags and Hope*; captured Wauhatchie 29 Oct 1863) · **William Pitt Chambers** (`ss:vicksburg:CS:cs_smith_stockade:nco` — Sgt, rose private→first sergeant, Co. B, 46th Mississippi, Baldwin's Bde / M.L. Smith's Division, Vicksburg garrison; a Covington County schoolteacher; journal *Blood and Sacrifice* — the **2nd Vicksburg-CS** record) · **Sam Houston Jr.** (`ss:shiloh:CS:cs_bragg_corps:pvt` — Pvt, Co. C "Bayland Guards", 2nd Texas, Jackson's Bde / Withers' Div / Bragg's Corps, Shiloh; wounded + left for dead on the second day (7 Apr 1862), a Bible from his mother stopped a ball, POW Camp Douglas; the eldest son of the Unionist governor Sam Houston, removed for refusing the Confederate oath — echoes the fluid-loyalties theme beside Stanley). **Honesty (D92):** Giles ships his documented Sergeant grade (NCO in an NCO slot; "Sergeant Val Giles" is quoted on the field), no portrait; Chambers does NOT claim the 46th held the Stockade Redan salient itself (its parent Smith's Division held that northern sector — the salient proper was Shoup's Louisianans), ships his first-sergeant grade as generic "Sergeant" (transparently disclosed, a within-NCO generalization not a cross-line deflation), no portrait; Houston ships his enlisted Private rank, the Bible/left-for-dead details attributed to sources not asserted, the 7-April wound-day pinned with a bio hedge that some accounts place it on the first day, placed for his own service not his father, no portrait. **Every load-bearing URL was independently verified IN THE MAIN LOOP** (inline `WebSearch`/`WebFetch` — the D289 workflow-hang lesson applied; Giles's rank was raised `:pvt`→`:nco` and Houston's wound-day pinned DURING that verification). Data + probe only — **no engine/combat change, D74 intact, standalone/sandbox byte-identical**. Probe `tools/probe-loot-survival.mjs` retitled D290→D291, counts bumped 22→25, three new per-record verification blocks added (probe-women-in-war reads the count dynamically → no edit). **Focused gate (D176):** importer validate 25/25 Verified · `node tools/build.mjs` GATE OK (citations ✓, women-in-war ✓) · `node --check` probe clean · **probe-loot-survival ok=true 11/11, applied=25, hostileRejected=true, 0 pageerrors** · adjacents probe-tactical-roster 8/8 + probe-women-in-war 8/8 lane-separation green (replacementRecords:25) · `git diff --check` clean. **Adversarial fact-check (read-only default-refute Opus ×3 workflow `wf_a1ac5e1b-4c9`, the D281 read-only-lens lesson — completed cleanly, no hang/args-misfeed because each lens embedded the record text): 9/9 SHIP, 0 FIX, 0 BLOCK.** Two minor advisories: Chambers's "first sergeant"→"Sergeant" (all lenses non-inflating + already disclosed → no change) and Houston's contested 7-April wound-day → **advisory adopted** (a bio hedge added), re-gated green. Full detail: DECISIONS D291 · RUN-LOG D291 · SOLDIER-REPLACEMENT-FORMAT. **NEXT = more Priority-1 Soldier's Story slices (theater balance still leans US) OR a batch/release `vet:noreg` checkpoint (Aaron's call). Held for a future slice: Haley (US Gettysburg — worsens the side balance this lane corrects). M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-07 **D290: PHASE I SOLDIER'S STORY SCALE-OUT — THREE NEW CITATION-GRADE CS REPLACEMENTS (20-22), A THEATER-BALANCING SLICE. The lane grows 19→22 `Verified` records; side balance 14US/5CS → 14US/8CS; all three Confederate, spanning Eastern + two Western theaters (Vicksburg had 0 CS — now filled).** New records in `data/soldier-replacements.json`: **Berry Benson** (`ss:chancellorsville:CS:cs_ap_hill_div:nco` — Cpl, Co. H, 1st South Carolina, McGowan's Bde, A. P. Hill's Light Division, Chancellorsville; leg-wounded, missed Gettysburg; the scout who tunneled out of Elmira — the prompt's held candidate, shipped with the 2nd rank source confirmed) · **John W. "Johnny" Green** (`ss:chickamauga:CS:cs_breck_rock:nco` — an enlisted man of the 9th Kentucky, Helm's "Orphan Brigade", Breckinridge's Division, Polk's Right Wing, Chickamauga 20 Sep 1863; entered service Oct 1861, rose to regimental Sergeant-Major) · **William H. Tunnard** (`ss:vicksburg:CS:cs_3rd_louisiana:nco` — Sgt, Co. K, 3rd Louisiana, Hebert's Bde/Forney's Div; his regiment held the 3rd Louisiana Redan, captured at the 4 July surrender; author of *A Southern Record* — the **first Vicksburg Confederate** record). **Honesty (D92):** Benson shipped at Corporal, bracketed (corporal at enlistment per NGE; sergeant only after his late-1864 capture per American Rifleman); Green's exact Chickamauga grade is undocumented → placed at "Sergeant" in the NCO slot because he demonstrably rose to Sergeant-Major and secondary accounts date a Shiloh-1862 promotion before the battle (a `:pvt` slot would risk DEFLATING a documented NCO — the D92/Dooley principle), Sergeant-Major-at-the-battle NOT asserted; Tunnard not placed personally in a mine crater, *A Southern Record* read as a period/participant account; no portraits. **Every load-bearing URL was independently re-fetched IN THE MAIN LOOP** (the D289 workflow-hang lesson applied — fetches owned inline, not parked on a subagent). Data + probe only — **no engine/combat change, D74 intact, standalone/sandbox byte-identical**. Probe `tools/probe-loot-survival.mjs` retitled D289→D290, counts bumped 19→22, three new per-record verification blocks added. **Focused gate (D176):** import 22/22 Verified · `node tools/build.mjs` GATE OK (citations ✓) · `node --check` probe clean · **probe-loot-survival ok=true 11/11, applied=22, hostileRejected=true, 0 pageerrors** · adjacents probe-tactical-roster 8/8 + probe-women-in-war lane-separation green (replacementRecords:22) · `git diff --check` clean. **Adversarial fact-check (read-only default-refute Opus ×3 workflow `wf_e9626494-e91` + a clean single-agent accuracy re-run):** honesty lens all 3 SHIP, slot lens Benson/Tunnard SHIP + Green FIX (the tier question, addressed by the Shiloh-promotion evidence + a strengthened caveat), accuracy re-run on the final text; panel advisories adopted before commit (Benson caveat margin, Green tier justification, Tunnard period-source hedge). Full detail: DECISIONS D290 · RUN-LOG D290 · SOLDIER-REPLACEMENT-FORMAT. **NEXT = more Priority-1 Soldier's Story slices OR a batch/release `vet:noreg` checkpoint (Aaron's call — probe-presets green since D278). Held for a future slice: Haley (US Gettysburg — worsens the side balance this slice corrects). M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-07 **D289: PHASE I SOLDIER'S STORY SCALE-OUT — THREE NEW CITATION-GRADE REPLACEMENTS (17-19). The lane grows 16→19 `Verified` records; side balance 13US/3CS → 14US/5CS; the two biggest gaps filled.** New records in `data/soldier-replacements.json`: **Henry M. Stanley** (`ss:shiloh:CS:cs_hardee_corps:pvt` — Pvt, Co. E "Dixie Grays", 6th Arkansas, Hindman's/Shaver's Bde, Hardee's Corps, Shiloh; captured→galvanized Yankee; the future explorer, born John Rowlands — the **first Western-CS** record) · **John Dooley** (`ss:gettysburg:CS:cs_pickett_div:cmd` — 1st Lt, Co. C "Montgomery Guard", 1st Virginia, Kemper's Bde, Pickett's Div; wounded both thighs + captured in Pickett's Charge — the **first Confederate Gettysburg** record) · **Joseph H. De Castro** (`ss:gettysburg:US:us_gibbon_bdes:nco` — Cpl, Co. I, 19th Massachusetts, Hall's Bde/Gibbon/II Corps; seized a Confederate color at the Angle repulsing Pickett's Charge, MOH 1 Dec 1864, widely-credited first Hispanic-American recipient). Dooley (charging) + De Castro (repulsing) = the two sides of the Angle. **Honesty (D92/D152-D239):** Stanley's autobiography flagged self-mythologizing + soldier-life-source-only, no portrait, no claim on his contested later career; Dooley ships the firm 1st-lieutenant rank + discloses the backdated 17 May 1863 captaincy (mapped to the officer `:cmd` slot — an officer in a `:pvt` slot would DEFLATE rank, forbidden like inflation), journal read critically not a Lost Cause frame, no portrait; De Castro battle-day corporal (not the later sergeancy), the 19th-Virginia flag ID hedged as later-secondary (Devereux's primary report names his capture but lists 14th/19th/57th Virginia without matching officer to flag), "first Hispanic-American" hedged as "widely credited", no portrait. **Every load-bearing URL was independently re-fetched and live-confirmed.** Data + probe only — **no engine/combat change, D74 intact, standalone/sandbox byte-identical**. Probe `tools/probe-loot-survival.mjs` retitled D239→D289, counts bumped 16→19, three new per-record verification blocks added. **A real content bug was caught + fixed in-flight:** the runtime clips `sourceNote` to 360 chars, so long notes lose their trailing honesty caveats from the *displayed* text (the lane probe caught Stanley's "contested later career" clipped) → fixed by front-loading the caveats, NOT weakening the probe. **Focused gate (D176):** import gate 19/19 Verified · `node tools/build.mjs` GATE OK (citations ✓) · `node --check` probe clean · **probe-loot-survival ok=true 11/11, applied=19, hostileRejected=true, 0 pageerrors** · adjacents probe-tactical-roster 8/8 + probe-women-in-war (lane-separation) green · `git diff --check` clean. **Adversarial fact-check (read-only Opus single agent — ultracode was toggled off mid-session, so not a workflow; the research workflow `wf_b50adca5-ea2` HUNG overnight on a stalled `WebFetch` and was killed, then finished from its four completed verify packets + independent Stanley verification): all three SHIP, zero errors.** Held for a future slice (vetted, not shipped): Benson (CS Chancellorsville, corporal — rank single-sourced) + Haley (US Gettysburg — worsens the side balance). Full detail: DECISIONS D289 · RUN-LOG D289 · SOLDIER-REPLACEMENT-FORMAT. **NEXT = Priority-1 Soldier's Story scale-out (more bounded named-person slices) OR a batch/release `vet:noreg` checkpoint (Aaron's call — probe-presets green since D278). M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-06 **D288: E59 FIXED — THE PROCEDURAL SKIRMISH-FORCES CLAMP NO LONGER COMPRESSES AUTHORED ODDS TO A COIN FLIP; AN ODDS-PRESERVING JOINT BRIGADE SCALE (SYMMETRIC CAP 1.8), SHARED BY THE FOUGHT PROCEDURAL PATH AND THE PM3 DELEGATED RESOLVE.** E59 (filed at D277) is closed. `_fldCampaignSkirmishParams` (`src/tactical/T2-campaign-link.js`) used to clamp brigade count [2,5] and men [1300,2100] INDEPENDENTLY per side, destroying the authored `bd.us:bd.cs` ratio — Sumter 6:1 → 2600-vs-2600 (a coin flip a fresh delegate needed up to 9 tries to win); Fredericksburg 1.58:1 → 10500-vs-10500 (both saturated, 1:1). Now the two sides are derived JOINTLY (`_skForces`/`_skDecomp`) so the ratio SURVIVES — proportionally, up to a symmetric cap `SK_MAX_ODDS=1.8`: `effBig=clamp(3400+big*0.13,3400,10500)`, `ratio=min(1.8,big/small)`, `effSmall=effBig/ratio` (floored 2600, lifting effBig), decompose to `{count=clamp(2,5,ceil(eff/2100)), men=clamp(1300,2100,round(eff/count))}`. Pure deterministic; no RNG; `_arSimSeed` untouched; INPUT-fidelity ceiling, NOT a D74 output gate; `_skCnt`/`_skMen` deleted. **The cap is MEASURED (cap-sweep A/B `.tmp/ab-e59-cap.json`):** full ~4:1 preservation strands the delegated US at Sumter and the CS at Kernstown (the campaign advances only on a WIN); **1.8 is the UNIQUE cap** keeping the delegated US completing (mcl 1) and the CS honestly stalling at Spotsylvania (idx 17, inward from the old Nashville/Bentonville). Mean odds distortion 0.391→0.218 (−44%); the coin-flip class is gone; standalone/sandbox path byte-identical (probe-field 23/23, explicit skirmish objects). Honest probe re-baselines: probe-auto-resolve PL-3 flip re-homed Wilderness→Fort Donelson (10/10); probe-full-campaign CS wall 20→16 (4/4). Focused gate GREEN: campaign-link 19/19 · auto-resolve 10/10 · full-campaign 4/4 · bridge 6/6 · field 23/23 · **0 pageerrors** · diff-check clean. **Panel `wf_a0945f25-a3c` (read-only default-refute Opus ×4, D281 lesson): 4× SAFE_TO_COMMIT, 0 BLOCK** (every D74/D69 refutation failed first-hand — pure symmetric input ceiling, seed untouched, shipped `_skForces` byte-for-byte == the A/B harness; STANDS notes all advisory/adopted).** Decide-&-log (not surfaced): the cap preserves every D277 invariant, contradicts no teaching, is symmetric (no D69 cushion), reversible. Full detail: DECISIONS D288 · RUN-LOG D288 · REVIEW-QUEUE E59→fixed-in-D288. **NEXT = Priority-1 Soldier's Story scale-out (another bounded citation-grade named-person slice). M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-06 **D287: LLM-OPPONENT SLICE 3 SHIPPED — VOICE + PERSONA IS LIVE; THE LLM-OPPONENT ARC (D283→D287) IS COMPLETE.** The D283 law's §5 (voice & gravity) + §7.4 slice 3 executed. The enemy commander can now speak: an optional ≤160-char in-character **dispatch** rides the plan through the full §5 containment stack, plus optional per-general persona seasoning. Surgical edits — `src/tactical/T27-llm-commander.js` (CAPTURE: `fldLlmCleanDispatch` + the §5.5 `fldLlmIsMetaDispatch` filter; the wall/install carry `st.dispatch`, never touching orders/planN/applied/malformed) + `src/tactical/T28-llm-connector.js` (schema `dispatch` maxLength 160 with NO fact fields · `LLM_SYSTEM` grim-professional + anti-Lost-Cause + one-way-facts charter · `fldLlmSomberNow`/`fldLlmSceneIsSomber` · persona `fldLlmPersonaLine`/`_fldLlmSurname`/`fldLlmPersonaFor` from `scenData.leaders`→`generals.json` · the top-left "Dramatization" render surface `fldLlmRenderDispatch`, HTML-escaped · the aria-live in-place panel-polish) + ONE guarded `fldRenderTop` line in T0. **No new module/manifest; byte-identical OFF.** §5 honored: one-way facts (schema+digest bands) · "Dramatization" caption never Verified/Inferred · somber suppression on antietam/gettysburg/chancellorsville (text hidden, orders execute) · failure=silence incl. AI-meta/refusal → silence (a legit grim "I cannot hold" survives) · grounding reads only `__FIELD._t27`. Persona = INPUT texture only, never an outcome (D74); verified live — Bull Run CS→Beauregard (bold), US→Burnside (measured). **Panel `wf_370ea214-aef` (read-only default-refute Opus ×4, D281 lesson): 0 BLOCK** — d74-persona + robustness SAFE_TO_COMMIT high; containment + probe-adequacy ADVISORY with **4 findings FIXED before commit** (§5.5 meta-filter · persona was inert vs real data → rewired to `scenData.leaders` · somber broadened to `scenData.id/name` · the D285 aria-live polish pinned); residuals recorded (Adapter-A free-text = disclosed §5 residual · stale-line = §3.4 continuity · somber hand-mirror = fail-safe). Gates: GATE OK · `node --check` clean · **probe-llm-commander 24/24 ×2, 0 pe, 0 net** (18 slice-1/2 + 6 voice teeth) · adjacents ai 15/15 · attacker-parity 13/13 · presets 27/27 (OFF-state byte-identity guard) · field 23/23 · spots bullrun 15/15 · fredericksburg 22/22 · PM3 lock auto-resolve 10/10 + campaign-link 19/19 · diff-check clean. **Screenshot readback caught + fixed a real top-right `#fldHud` collision** (D282 lesson) → top-left, re-verified. **WCAG 4/4 AA** (weakest 9.68:1). **Manual-only, never a gate:** a live OpenRouter `:free` / Anthropic smoke by hand confirms slices 2+3 end-to-end (not run this session). **NEXT = E59 (procedural clamp compression) → Priority-1 Soldier's Story scale-out. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-06 **D285: LLM-OPPONENT SLICE 2 SHIPPED — THE UNIVERSAL BYOK CONNECTOR IS LIVE.** The D283 law's §2/§6/§7.4 slice 2 executed. New `src/tactical/T28-llm-connector.js` — the network + settings + arm-hook half of the LLM field commander. **Adapters (§2.1):** A = OpenAI-compatible `POST {baseUrl}/chat/completions` + `Authorization: Bearer` + `response_format:json_object` (OpenRouter/Groq/Ollama/LM Studio/custom); B = Anthropic Messages `POST /v1/messages` + `x-api-key` + `anthropic-version:2023-06-01` + `anthropic-dangerous-direct-browser-access:true` + `output_config.format` json_schema, **default `claude-haiku-4-5`, never an Opus/Fable auto-select**. **Presets (§2.2) RE-VERIFIED 2026-07-06** (web workflow `wf_101741db-65d`): OpenRouter `:free` (Mistral `:free` GONE → dropped), Groq (**CORS wide-open by live preflight**), Ollama/LM Studio (local, CORS/Local-Network-Access caveats in the UI), Anthropic (paid; "Max is NOT an API key" note), Custom; Gemini/OpenAI-direct EXCLUDED (still no browser CORS). **Keys (§2.3):** device-only `localStorage cw_llm_conn` + `cw_llm_key` (secret split for a surgical Clear-key), "🔒 stored on this device only" note + a Forget button — NEVER in `G.settings`/`gor_save`/C4/exports/repo (probe-proven canary). **Async (§3.3):** T27's `fldLlmRequestPlan` is callback-based — sync mock (probes byte-identical) else T28's `fldLlmDispatchAsync` (the ONLY fetch; T27 network-free); `fldLlmCycle` = one in-flight max + 12s timeout/abort + triple staleness drop; ANY failure → `cb(null)` → last good plan, engine commands on. **Arm hook (§6 + the two D284 traps, now built + pinned):** `fldLlmArmOnLaunch` runs at T0 launch AFTER `fldInitSim` stamps `playerSide` (**TRAP 2** — LLM aims at the AI army, proven US & CS), arms only from live enabled config, refuses headless/autoBoth (PM3 engine-only), and NEVER writes `llmCommander` into `_launchOpts` (**TRAP 1** — relaunch can't re-arm, proven by disable-then-replay). Gates: GATE OK · `node --check` clean · probe-llm-commander **18/18 ×2, 0 pe, 0 net** (13 slice-1 + 5 slice-2: T28-contract/config-state/key-leak/arm-traps/panel-a11y+zero-net-UI) · adjacents ai 15/15 · attacker-parity 13/13 · presets 27/27 · field 23/23 · spots bullrun 15/15 · fredericksburg 22/22 · PM3 lock auto-resolve 10/10 + campaign-link 19/19 · diff-check clean · **screenshot readback both variants clean · WCAG 11/11 AA (weakest 8.17:1)**. **Panel `wf_fc1308d1-912` (read-only default-refute Opus ×3): 3× SAFE_TO_COMMIT high, zero blocking** (security lens zero refutations; 4 advisories all resolved without a code change — the Anthropic schema's partial `required` is correct since hold omits tx/tz; the aria-live echo is a slice-3 panel-polish, mitigated by the focused `role=switch` announcing its `aria-checked`). **Manual-only, never a gate:** a live OpenRouter `:free` / Anthropic smoke by hand (not run this session). **NEXT = SLICE 3 — voice + persona (law §5): the ≤160-char dispatch render surface under the containment stack [one-way facts, "Dramatization" caption, anti-Lost-Cause charter, somber-set suppression, failure=silence], persona/difficulty prompt presets as INPUT texture only, optional per-general seasoning. Then E59 → Priority-1 Soldier's Story. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.** The paste-ready slice-3 kickoff is at the bottom of this session's report.

## ⚡ PREVIOUS — 2026-07-06 **D284: LLM-OPPONENT SLICE 1 SHIPPED — THE T27 ENGINE SEAM IS LIVE, ZERO NETWORK.** The D283 law's §7.4 slice 1 executed verbatim: `src/tactical/T27-llm-commander.js` (per-launch `_gen`/`phaseIdx` state · the PURE fog-respecting band-quantized digest [250-men/25-yd/5-s/compass; `fldVisible`-filtered contacts — the LLM can be feinted] · the deterministic validation wall [malformed → whole-cycle fallback keeping the last good plan; illegal order → that brigade falls to the real D64+T26 engine field-for-field] · order application writing ONLY `u.order`/`u.formation`+`_t27`, vocabulary `move|hold|charge`+`line|column`, no fldRng, never playerCharge) + the T0 §3.2 dispatch line above fldAiDefender + manifest + `tools/probe-llm-commander.mjs` (13 teeth) + vet:noreg enrollment (113 entries). **Four refusal gates**: default-off `llmCommander` (NOTHING in the shipped game sets it — `fldLlmConfigured()` honestly false until the slice-2 connector; the `_t27MockPlan` test hook is the only plan source) · sticky `_llmOff` · the PM3 double lock (`autoBoth` OR renderer-none — the D277 replay path structurally unreachable) · no-plan-source; plus the `_gen` self-heal disarm (consent never crosses launches; **the slice-2 arm contract**: `llmCommander=true` + `_t27=null` at launch from explicit opts + live config). The D278-owed T0:351 badges-comment fix rode along (the honest 5/8 → 7/8 pre-D273 → 8/8 chain). Gates: GATE OK · probe-llm-commander **13/13 ×2, 0 pe, 0 net** (in-page fetch/XHR/WS/beacon spy hard-0; playwright listener tolerates only the pre-existing base THREE-CDN boot loader) · adjacents ai 15/15 / attacker-parity 13/13 / presets 27/27 / field 23/23 · spots bullrun 15/15 / fredericksburg 22/22 · PM3 lock auto-resolve 10/10 + campaign-link 19/19 · diff-check clean. **Panel `wf_89fe1c05-993` (read-only default-refute Opus ×3): 3× SAFE_TO_COMMIT high, zero refutations**; advisories adopted — the disarm-only `llmCommander=false` write recorded as a seam-owned control flag (D284 write-set amendment), and **two slice-2 arming traps recorded as contract law: (1) NEVER persist `llmCommander` into `__FIELD._launchOpts` (the relaunch/new-seed button Object.assigns it into a fresh launch → silent re-arm without consent, violating §6); (2) stamp `__FIELD.playerSide` BEFORE arming (fldLlmSide's US-fallback could otherwise aim the LLM at the human's own side).** **NEXT = SLICE 2 — the connector (law §7.4/§2): adapters A+B (OpenAI-compat + Anthropic), provider presets, the settings panel + per-battle enable (WCAG AA), `cw_llm_*` device-only key handling + honesty notes, the zero-network tooth extended to the UI paths; live smoke vs an OpenRouter `:free` model runs MANUALLY, never a gate. Then slice 3 voice → E59 → Priority-1 Soldier's Story. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-06 **D283: THE LLM-OPPONENT DESIGN LAW IS AARON-LOCKED (Q-D270-5 planning slice, docs-only — BUILD NOT STARTED).** The milestone is the **live LLM field commander**: a player-configured LLM commands the AI-led army at operational cadence through a new T27 order-writing seam (T26 idiom — `u.order`/`u.formation`/`_t27` only, no fldRng, never playerCharge), with the shipped D64+T26+doctrine engine as the per-brigade and whole-cycle fallback. **Aaron's four locks (popups):** (1) live commander over the judged hybrid (the planning workflow `wf_888c5559-785` scored doctrine-engine 22/30 · narrator 21/30 · live-commander 14/30 — Aaron chose with the kill shots disclosed; the judged containment stack is adopted wholesale); (2) the UNIVERSAL CONNECTOR — one OpenAI-compatible adapter (OpenRouter `:free` models · Groq free tier · localhost Ollama/LM Studio · custom) + one Anthropic adapter (browser opt-in header; haiku-class default; "Claude Max is NOT an API key" honesty note) — $0 browser-CORS rungs web-verified 2026-07-06, refuting the judges' reach kill shot; Gemini-direct/OpenAI-direct EXCLUDED (no reliable browser CORS; a proxy breaks $0/no-backend); (3) GUARDED LIVE VOICE — the ≤160-char dispatch may be live-generated under the §5 containment stack (one-way facts, no figure/date/citation fields, anti-Lost-Cause charter, "Dramatization" caption, somber-set suppression, failure=silence); (4) OPT-IN/OFF-DEFAULT + PM3 STRICTLY NEUTRAL (T27 hard-refuses autoBoth/headless — the D277 replay-×2 path is untouchable; sticky `_llmOff` + no-config + autoBoth each independently gate → the whole vet:noreg battery runs with the seam unreachable, ZERO re-pins; probe-llm-commander adds digest-purity/fog-mask/validation-wall-via-`_t27MockPlan`/triple-inertness/zero-network-fetch-spy teeth; network never in a probe). Keys device-only localStorage `cw_llm_*`, never in saves/shares/repo. **LAW: `docs/design/llm-opponent-design.md`** (§9 = the paste-ready slice-1 build prompt). Three slices: S1 engine seam ZERO network (RIDES the D278-owed T0:351 stale-comment fix) → S2 connector+UI → S3 voice/persona. **NEXT = the T27 slice-1 build (run law §9 verbatim), then E59, then Priority-1 Soldier's Story. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-06 **D282: THE D280 MAIN-MENU BUILD SHIPPED — the Aaron-locked hybrid command screen is LIVE.** One module rewrite (`src/98-h0-main-menu.js`) + the probe re-anchor (`tools/probe-h0-main-menu.mjs`) + docs; presentation-only (D74), zero new embedded bytes, base.html untouched. All three Aaron locks honored and probe-pinned: (1) HYBRID — full-bleed PD-scene backdrop (aria-hidden, cold static grade, vignette+scrim, high-contrast force-hides it) under ≥.90 dark-glass panels (19/19 WCAG pairs AA at worst-case white-pixel bleed; weakest 7.53:1); (2) the CAMPAIGN-CHAIN TRACKER rail — theater-tinted per `BATTLES.th`, fought/current-static-brass-ring/ahead-dimmed, SR summary + aria-hidden decoration, overflow-x:auto, suppressed no-save, placed OUTSIDE `.h0-top` so the `.gn-col:last-child` classifieds contract survives by construction; (3) SOMBER-STATIC — `H0_SOMBER_SCENES` {antietam, gettysburg, chancellorsville} (verified = the SCENE_IMG dead-in-frame set) kills the 340ms one-shot entrance AND the hover lift; nothing loops (probe-pinned). `#gnFree` anchors a labeled scroll-capped **Field Operations** group — the T0→T1→T2→T6→T11 sibling chain clusters inside it, mechanics unchanged (panel-verified at T0:2387/T11:812). **The E61 nudge-card half SHIPPED** (read-only `recoveryMode && recoveryLossCount>=2` → take-command card naming the walled battle; REVIEW-QUEUE flipped; the collapse-terminal half stays OPEN). Dual D74 tripwires: static (the generated module region carries NO `C.<any-field>=` / `G.campaign=` / `saveLocal(`) + runtime (double menu-open leaves `gor_save` byte-identical). **Two real defects caught by the mandated screenshot readback, both fixed + probe-pinned:** the scroll-capped grid froze Chrome button rows at min-height (decks painted over the next button → flex column + fit-content tooth) and the tracker sat below the desktop fold post-injection (vertical trims + a fold tooth measured against the SHEET box after injection settles). **Panel wf_a20efea1-d4c (read-only default-refute Opus ×3, the D281 lesson honored): 3× SAFE_TO_COMMIT, zero refutations; both advisories adopted** (somber hover stilled; tripwire broadened) and re-gated green ×2. Gates: GATE OK · probe-h0-main-menu ×2 green 0 pe · adjacents vs the final CSS all green (menuprobe · help-overlay 10 · tutorial 15 · save-slots 14/14 · custom-battle-builder 15/15 · presets 27/27 · bootprobe realErrors 0) · 8 screenshots read visually · diff-check clean. Deferred per the law's §4 scope: D278's T0 stale-comment incidental (next T0-touching slice). **NEXT = LLM-opponent planning (Q-D270-5, its own planning slice — design law before any build), then E59, then Priority-1 Soldier's Story. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-06 **D281: E62 RESOLVED — the cautious×logistics fog-OFF attacker-resupply inversion is CLOSED at the T4 seam (`ATK_STANDOFF_RESUPPLY`, the `ATK_FOG_RESUPPLY` analog, AI-LED BRIGADES ONLY — the human player stays unbound, orders and resupply both). Neutrality measured restored: delta −5 → 0 (cautious ON 8/8 == OFF 8/8; aggressive delta 0 — the hole was cautious-specific); five surgical-identity gates full-row (logistics-OFF/aggressive/fog byte-identical between builds); live cautious scenarios (Fred CS 8/8 · MH US 8/8 · BR byte-identical fog-ON) + the 8-battle battery + parity/ai/auto-resolve/campaign-link + the vicksburg/chickamauga multi-phase ON-vs-OFF spot-check ALL GREEN 0 pe. probe-logistics BALANCE v2 = the neutrality invariant itself (live ON+OFF legs, |delta|≤1) + exact ON pins + winBy/CS≥7000 sharpenings + a dedicated choke tooth (with an ai:false human positive control) — bind-proven by the panel's adversarial revert. Panels: wf_eef00e48-e93 (probe-integrity SAFE_TO_COMMIT high; mechanism sharpenings ALL adopted incl. the decisive u.ai AI-scope; one REFUTED verdict adjudicated as a PANEL-RACE ARTIFACT — the lens read the tree during the other lens's revert+rebuild bind-test; lesson recorded: tree-mutating lenses run isolated/serialized) + read-only confirm wf_59e68d5d-c6e 2× SAFE_TO_COMMIT (high, zero refutations). The false T4:152 + stale T0-badges comments fixed in-slice; the 3 `_assaultDoctrineNote`s rewritten Inferred-grade. probe-logistics' vet:noreg entry is GREEN again — every entry expected green at this head. Evidence: D281 · `.tmp/ab-e62-{fix,live,phases}*`. NEXT = the D280 MAIN-MENU BUILD (Aaron kickoff orders same-session continuation): run `docs/design/main-menu-redesign-design.md` §7 verbatim — the three Aaron locks are non-negotiable (hybrid synthesis · chain tracker in scope · casualty photos somber-static, NO motion over the dead); one module (`src/98-h0-main-menu.js`) + one probe + docs; presentation-only D74; no new embedded bytes; §2 contracts survive by construction; §6 gates. Then: LLM-opponent planning (Q-D270-5) → E59 → Priority-1 Soldier's Story. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.**

## ⚡ PREVIOUS — 2026-07-06 **D279+D280: THE BATCH CHECKPOINT RAN (Aaron-approved) — 111/112 GREEN; NEW E62 FILED (cautious×logistics fog-OFF attacker-resupply inversion, the D66 class — the default-refute panel caught it after the first re-anchor adjudication was refuted and reverted). THE MAIN-MENU DESIGN LAW IS AARON-LOCKED (D280). NEXT = the E62 owner-slice (Aaron popup lock), then the main-menu build.**
**Prompt A (the E62 owner-slice):** read `REVIEW-QUEUE.md` E62 (the full spec) + `DECISIONS.md` D279 + `.tmp/ab-e62-logistics.mjs`/`ab-e62-results.json` (if still present; re-derivable from the D279 recipe) + `src/tactical/T4-logistics.js` (the `ATK_FOG_RESUPPLY` seam :53-57/:147-159 and the :152 neutrality invariant) + `src/tactical/T0-field-sandbox.js:856-895` (the cautious branch). Design + A/B a fog-off stand-off resupply gate at the T4 seam (an `ATK_FOG_RESUPPLY` analog; targets the engine's own neutrality invariant, not a probe number). Gates: the full 2×2 (cautious × logistics, OFF-aggressive leg included) · live cautious scenarios (Fredericksburg/Malvern Hill/Bull Run) layers-ON before/after · the 8-battle direction battery · fix the false `T4:152` comment + the stale T0 badges 5/8 comment (E32-class) in-slice · re-adjudicate the probe-logistics BALANCE tooth against the fixed engine (band valid again, or fresh re-anchor adopting the D279-recorded `winBy` + CS-floor sharpenings; consider the panel's ON-vs-OFF-delta tooth shape as the acceptance criterion) · Opus default-refute panel before ship · full probe-logistics ×2 + adjacents (arms/presets/field/bullrun) · commit/push + docs trail. **probe-logistics is expected-red until this lands (D266/E56 posture).** The main-menu BUILD then runs from `docs/design/main-menu-redesign-design.md` §7.

## ⚡ PREVIOUS — 2026-07-05 **D278: E60 RESOLVED — probe-presets' two stale bullrun1 pins deliberately re-anchored (tools+docs only; the game is byte-identical). EVERY vet:noreg entry was then expected green (the D279 checkpoint refuted that at one entry — see above). Original block: NEXT = the batch/release full-suite checkpoint decision (Aaron's call), then Aaron's queue (main-menu redesign · LLM-opponent · E59/E61 · Priority-1 Soldier's Story).** The E60 owner-slice ran the E56/D268 recipe attribution-first. **The A/B (`.tmp/ab-e60.mjs`, 7 fresh-page legs vs the git-extracted pre-D273 deliverable `629f6fa`, full-row {w,us,cs,steps}, 0 pe, ×2 deterministic) REFUTED the filing's T26 hypothesis:** `_parityOff` legs are full-row identical to base — T26 is exactly zero at bullrun1 (the cautious posture rejects the whole seam at `fldParityAiUnit`'s first gate). The movers: the D273 `assaultDoctrine:"cautious"` row + cautious-v2, STACKED on the E49b drift — the old CS 5/8 pin was DOUBLY stale (pre-D273 reads 7/8 with exactly the D268 per-seed vector, US on 55 only; cautious flips 55 back → 8/8, all full-clock timeout holds; lifting `_atkCautious`+`_parityOff` reproduces the pre-D273 build full-row byte-identically on all 8 seeds — the D273 arc is 100% of its window). Re-anchor: `_parityOff` joins the layers-OFF isolation set; the parity-OFF row is pinned to the EXACT per-seed {winner,us,cs,steps} vector AND parity-ON ≡ parity-OFF full-row (the T26-inertness tripwire); the aiSkill tooth lifts the posture in-fixture only and pins BOTH commit owners (the original D64 effLocal assertions verbatim + the explicit T26 wave-commit arm `_e53.armed` false@0.9/true@1.12). 2× RE_ANCHOR_SOUND default-refute Opus lenses (wf_f410fa01-736, zero refutations); sharpenings adopted; the T0 stale badges-comment example filed as an E32-class incidental for the next src-touching slice. Gates: probe-presets 27/27 ×2 byte-identical · attacker-parity 13/13 · ai 15/15 · 0 pe · diff-check clean · deliverable byte-identical by cmp. M8/Q5/Q6 locked; Phase H parked; Phase D deferred.

## ⚡ PREVIOUS — 2026-07-05 **D277: PARITY-M3 SIM-BACKED AUTO-RESOLVE SHIPPED — THE BATTLE-MODE-PARITY ARC (E42/E43/E45) IS COMPLETE. NEXT = the E60 owner re-anchor (small), then Aaron's queue (batch checkpoint candidate · main-menu redesign · LLM-opponent).** The delegated auto-resolve now runs the SAME conditioned battle headless (renderer none, both sides AI, neutral preset PL-10, war-state-pure seed PL-6) through the SHARED T2 compute/apply path; the rating-margin model, `_arEnemyRating`, the 0.42/0.72 floors, the ±5 draw band, and the `commandMarginEdge` nudge are DELETED (D48 rider added). PL-3 measured: Spencer@Gettysburg shifts eFrac .417→.510; Henry@Wilderness FLIPS a delegated loss into a hold win. Bull Run = FAITHFUL scenario (delegation repeats history; recovery converges in 1). Pacing A/B vs the old model: **5/6 cells strictly improve** (US weak now COMPLETES vs 0W/90L; CS fresh reaches idx 22 vs idx-0 stall); the pure-delegation dominant-CS chain honestly stalls at Nashville/Bentonville — outs: take command, or the political victoryReady path (not guaranteed by delegation alone). 3× SAFE_TO_COMMIT Opus panel (wf_178fdb5b-883), zero blocking; both sharpenings applied. Probes: auto-resolve 10/10 · campaign-link 19/19 · full-campaign 4/4 · bridge 6/6, all 0 pe. **NEW ledgers: E59 (procedural clamp compression) · E60 (probe-presets 2 pre-existing E53-v2 stale pins — vet:noreg reds on that entry until its owner re-anchors) · E61 (no strategic-defeat terminal).** M8/Q5/Q6 remain locked; Phase H parked; full vet:noreg deferred — the completed arc is a natural batch-checkpoint candidate (Aaron's call).

## ⚡ PREVIOUS — 2026-07-05 **D276: E46 PIPER GUNS RE-ATTEMPT RED-HALTED AND PARKED. NEXT = PM3 SIM-BACKED RESOLVE.** The documented Piper-farm gun row was restored exactly after D275's E53-v2+E54 engine, then reverted because the unchanged D264 gate still red-halted: P1 Sunken Road US **5/8 default + 5/8 probe** vs required US >=6/8. All non-Antietam rows plus sandbox stayed byte-identical, pageerrors were 0, captured stayed 0 on every Antietam row, and the Antietam `_e46Note` now records the D276 evidence. E47 is shipped, E46 is honestly parked as a documented divergence, and PM3 can start from this boundary. M8/Q5/Q6 battle-build remains locked unless Aaron explicitly unlocks.

## ⚡ PREVIOUS — 2026-07-05 **D275: E54 POCKET-COLLAPSE CAPTURE SEMANTICS + E47 SHILOH/GETTYSBURG FLIPS SHIPPED. NEXT = E46 PIPER GUNS RE-ATTEMPT, THEN PM3.** E54 adds a universal, role-aware captured/pocket seam: only an attacker HOLD win with explicit `homeEdge`, a 2:1 field-collapse ratio, and attacker local objective control captures live routing/wavering defenders before the T8 ledger freezes. It is inert at sandbox/no-homeEdge and near-parity fights, preserving Chancellorsville and the D273 E53 false-capture guards. E47 then lands the remaining Shiloh/Gettysburg role-aware home-edge data: Shiloh flips to correct CS direction and records nonzero captured US on CS-hold seeds; Gettysburg keeps aggregate US while Day 1 CS holds on 7/8 default+probe seeds with nonzero captured US on the CS-hold rows. Gates green: build, node --check, field 23/23, Shiloh 31/31, Gettysburg 18/18, attacker-parity 13/13, Chickamauga 18/18, Chancellorsville 27/27, Malvern Hill 27/27, Antietam 16/16, JSON readback all ok/pageerrors 0, plus preserved E47 smokes `pe=0`. M8/Q5/Q6 battle-build remains locked unless Aaron explicitly unlocks.

## ⚡ PREVIOUS — 2026-07-05 **D274: E47 REATTEMPT RED-HALTED. D273 A+B MAKES SHILOH/GETTYSBURG DIRECTION GREEN, BUT CAPTURED STAYS ZERO; DATA REVERTED. NEXT = E54 CAPTURED/POCKET SEMANTICS BEFORE E47 CAN SHIP.** The D262 E47 flip edits were reapplied only for acceptance smoke on top of D273 E53-v2, then reverted before ship. Shiloh flip: CS 7/8 default + 8/8 probe, flipNoShed 8/8 both sets, but `captured` 0 on every seed. Gettysburg flip: aggregate US 8/8 both sets, Day 1 CS 7/8 default + 7/8 probe, but `captured` 0 on every seed; flipNoShed Day 1 7/8 default + 6/8 probe, still captured 0. Both runs had 0 pageerrors. **Conclusion:** E53-v2 fixed attacker direction enough for the flips, but correct-direction geometry still lacks the capture/pocket semantics the gates require. **NEXT:** run a bounded E54 session for captured/pocket semantics, then re-attempt E47 behind the same unweakened gates; E46 Piper stays honestly hard/open after that; PM3 remains hard-gated on E47.

## ⚡ CONTINUE HERE — 2026-07-05 **D273: E53-v2 BUILD SHIPPED AND PUSHES THE QUEUE TO E47 FLIPS NEXT.** The live game now has attacker tactical parity v2: A+B envelopment wing + wave commit are live in `src/tactical/T26-attacker-parity.js`; C/abandonment is absent; T25 has the E53-scoped sidestep capture valve; Bull Run has the accurate-input `assaultDoctrine:"cautious"` row and cautious-v2 holds at existing `ATK_ASSAULT_R` from the nearest visible defender. Full battery `.tmp/ab-e53-v2.mjs` ran twice green and byte-identical (`pe=0`; Bull Run CS/timeout 11/11; Chancellorsville CS 8/8 default + 7/8 probe; Chickamauga capUS 0; Gettysburg capCS 3,531). Focused/adjacent probes green: attacker-parity 13/13, ai 15/15, chickamauga 18/18, field 22/22, bullrun 15/15, all 0 pageerrors. **E58 update:** the rail-pivot tooth is now green in `probe-bullrun` (with rail = CS timeout, without rail = US hold), so it is no longer an expected-red `vet:noreg` blocker from this head. **NEXT = E47 Shiloh/Gettysburg flip re-attempts behind the same unweakened gates, then E46 Piper honestly, then PM3.**

## ⚡ PREVIOUS — 2026-07-05 **D272: E53-v2 DESIGN IS LOCKED DOCS-ONLY. NEXT = E53-v2 BUILD/RE-LAND FROM `.tmp/e53-build-d269/`, APPLY THE §0 DELTAS, RUN THE FULL BATTERY, COMMIT/PUSH ONLY IF GREEN.** D272 ran fresh measurements after temporarily re-landing the D269 T26 build and then rebuilding it away: anatomy/variants/cautious/final artifacts all 0 pageerrors. The law amendment is now `docs/design/e53-attacker-parity-design.md` §0. **Doctrine locks:** A+B retained; C/abandonment dropped; the mass-capture governor is E53-active only (existing T25 lane must fail at real x and at `x±FLD.RALLY_R`); cautious-v2 holds at existing `ATK_ASSAULT_R` from the nearest visible defender and is fog-guarded; `bullrun1` gets `assaultDoctrine:"cautious"` as an accurate-input posture. **Why:** global valve rejected to preserve OFF-state identity; C harms Chancellorsville and does not fix captures; wave-only/no-wing breaks required gates; Bull Run's old 0.85×ring cautious remedy was refuted. No sim/data/code shipped in D272; `DECISION-NEEDED-e53-build.md` is resolved/removed. **E58 rail-pivot remains expected red owner-slice until resolved; do not treat it as this build's regression.**

## ⚡ PREVIOUS — 2026-07-05 **D269: THE E53 BUILD RAN THE D267 LAW VERBATIM AND WENT HONESTLY RED ON ITS OWN A-PRIORI §4 BATTERY — REVERT-FILE-HALTED (deliverable cmp byte-identical to HEAD `576bf8a`; docs-only commit; the game is unchanged). THE FORK IS AARON'S: `DECISION-NEEDED-e53-build.md` (PushNotification sent). NEXT = Aaron's answer → Prompt A below executes it.** T26 was built to law §3 verbatim (seam/trigger/wing/wave/abandonment; constants a-priori: WING_DEPTH 180, WING_X 120=RALLY_R/2, ABANDON_X 2; GATE OK; smoke green) and the §4 battery ran with EVERY gate frozen before any parity row was seen (`.tmp/ab-e53.mjs` — base/off/par × 10 configs × the 11-seed union + the probe-arms tooth legs + fog smoke + ±60 depth legs; ~380 rows, 0 pe). **GREEN:** off==base row-for-row EVERYWHERE (the strongest OFF-state proof any tactical layer has shipped); par==base at every inert config (sandbox · cautious Fredericksburg/MH · fog-on Bull Run/Antietam); **Shiloh CS/hold 11/11 with the artifact-direction capUS 5,872→0** · **Gettysburg agg US 8/8 with capCS 10,700→3,531 ≈ the documented D1 ~3,600** · **Antietam P1 US 8/8 def + 6/8 prb, P0/P2/agg held** · all E49 protective teeth green wing-active. **RED (three independent, design-level — no implementation bug found):** (1) **Bull Run flips CS 7/8 → US 8/8 in the D268-re-anchored tooth config** (Aaron lock-3 halt class; attribution airtight — offT==baseT) AND the prepared McDowell-piecemeal remedy is **refuted as-prepared** by a temp-build measurement (US-by-HOLD 6/8: `ATK_CAUTIOUS_HOLD 0.85` parks the cautious attacker at 0.85×obj.r — INSIDE Bull Run's ring; the band was tuned for Fredericksburg's wall geometry — and fog-on shipping Bull Run degrades CS 11/11→5/8); (2) **Chancellorsville §6 collapses 8/8+7/8 → 2/8+2/8** — C fired on 9/11 seeds and stood Lee's army off the battle he won by feeding (REFUTED per the law's own clause), the no-C isolation leg still reds §6 at depth 180 (CS 5/8 both sets — the emulation's 8/8 did not survive the engine-native differences), and C is byte-identical-INEFFECTIVE on the captured channel it was justified by; (3) **the wing mass-captures at defender-collapse geometries** — Chickamauga ~9.2k US captured/battle at 180 (documented total ~4.8k) and **11/11 five-figure lock-4 rows at the law's RALLY_R depth anchor 240** (which also regresses Gettysburg's captured off the band). Depth 180 reds §6; 240 reds lock-4 — no sanctioned config. **Everything preserved for a ~5-minute re-land: `.tmp/e53-build-d269/` (EVIDENCE.md = the full record; T26 + T0/manifest/vet diffs + the probe + all harnesses/JSONs).** THE FORK: (a) RECOMMENDED — the E53-v2 design session (the D255/D259/D263/D267 pattern) solving the three measured blockers (mass-capture governor, likely joint with E54's pocket semantics · a defender-line-anchored Bull-Run-safe cautious band and/or E57 first · a C-v2 trigger shape distinguishing "broken twice with zero progress" from "the slow grind that historically converted") · (b) E57 clocks first · (c) park E53 (NOT recommended — E47/E46/PM3 all gated). Evidence: **D269**.

## ⚡ CONTINUE HERE — 2026-07-05 **D271: D270 carry-forward is executed. The 3-lens rerun against preserved D269 evidence converged (E53-v2 required; no legal retune path under SL-7), and user-unavailable guidance authorized autonomous continuation, so the recommended doctrine path is LOCKED: option (a) = run the E53-v2 design session next. Docs trail is synced; no sim surface shipped. NEXT = execute Prompt A directly (option-a lane), then amend law/ledger/docs and ship docs-only at that boundary; main-menu redesign + LLM-opponent remain queued milestones.**

## ⚡ PREVIOUS — 2026-07-05 **D268: E56 RESOLVED — THE PROBE-ARMS BULLRUN1 BASELINE DRIFT IS 100% E49b STRAGGLER-SHEDDING (E48/E49a EACH PROVEN EXACTLY ZERO); DELIBERATE RE-ANCHOR CS 5/8→7/8 WITH THE PER-SEED VECTOR PINNED; 2× RE_ANCHOR_SOUND OPUS; NEW E58 FILED (probe-bullrun rail-pivot red at HEAD — same drift class, pre-existing). NEXT = THE E53 BUILD (Prompt A below).** The attribution A/B (`.tmp/ab-e56.mjs`, 7 legs × the 11-seed union vs the git-extracted D251 deliverable `084fa40`, exact tooth config, fresh page per leg, 0 pe, exit 0): **headNoShed (`_e49NoShed`) FULL-ROW byte-identical to the d251 leg on all 11 seeds — E49b is the SOLE mover**; headNoE48 (fldCheckVictory suppression wrapper forcing holdLive=false, demonstrably live at 95,842 calls) and headNoSur (`_e49NoSurrender`) each FULL-ROW identical to head — **E48 and E49a exactly zero** (surrender/captured 0 on every row; the SL-2 corridor geometry structurally unreachable at bullrun1 — the D262-D267 finding again); headNoE49/headNoAll close the attribution. Per-seed: 5 US→CS flips (fast ~4,100-5,300-step US holds starved into full-clock CS timeouts by the attacker's larger shed — US missing ~4.6% of fielded vs CS ~4-5%, both inside the D261 2.8-4.8% band) + 2 CS→US flips (49, 55); tooth = 55-out/21-42-303-in (+2), union +3 — the arithmetic reconciles exactly. **Adjudication (D74/SL-7): the honest baseline of a better engine → DELIBERATE re-anchor, never silent.** 2 independent default-refute Opus lenses both RE_ANCHOR_SOUND high-confidence (the first mechanism-lens agent died on a StructuredOutput retry cap — an agent failure, honestly re-run standalone); sharpenings ADOPTED: the tooth now pins the PER-SEED WINNER VECTOR (CS on 1,7,21,42,101,303,909; US on 55 — compensating opposite-direction flips can never net back to 7 and false-green), exact-equality kept, and the comment records provenance + the dual meaning (a future drop toward 5/8 = an E49b regression signal, NOT the melee-table bug; any D74-approved E49b retune legitimately reds the tooth and needs a fresh attribution A/B). **probe-arms 23/23 green ×2** (exit 0 under pipefail; artifact readback clean) — **its vet:noreg entry is GREEN again**; adjacent probe-field 22/22 0 pe. **INCIDENTAL (NEW E58):** the adjacent-probe gate surfaced probe-bullrun's HISTORICAL-ASYMMETRY rail-pivot tooth (`tools/probe-bullrun.mjs:145`, seed 55) RED at HEAD — PRE-EXISTING (this slice is tools-only, deliverable bit-identical to HEAD; deterministic ×2), drift window pinned first-hand by `.tmp/ab-e58-railpivot.mjs` (at D251 the pivot is dramatically load-bearing: with-rail = CS timeout win at 480s, without = US hold at 263s; at HEAD BOTH runs are US wins, 266s vs 291s — inverted, shedding ledgers active); seed 55 is the same seed that flipped CS→US in this A/B — presumptively the same E49b composition, but **its owner runs its own attribution + adjudication** (single-seed TEACHING-DIRECTION tooth; multi-seed re-anchor is in the remedy space — never a silent weakening); **`vet:noreg` will red on the probe-bullrun entry at the next batch checkpoint until E58 lands — expected and recorded (the D266/E56 posture).** Tools+docs only; ZERO sim surface; the game is byte-identical. Evidence: **D268** · `.tmp/ab-e56-results.json` · `.tmp/ab-e58-results.json` (gitignored).

## ⚡ PREVIOUS — 2026-07-05 **D267: THE E53 ATTACKER-PARITY DESIGN SESSION RAN AND LOCKED (docs-only; deliverable byte-identical through the session by cmp). E56 = the owner-slice it queued FIRST (RESOLVED in D268 above), THEN THE E53 BUILD.** The session ran the D255/D259/D263 pattern end to end: engine facts first-hand → the repo's largest attacker-behavior measurement program (5 harnesses `.tmp/measure-attacker-d267{,b,c,d,e}.mjs`, all 0 pe; the preserved E46 Piper row + E47 Shiloh flip applied as TEMPORARY builds, measured, reverted, cmp-proven each cycle; harness cross-checks reproduced the D262/D264 reds verbatim) → a ZERO-REFUTATION 3-lens default-refute Opus panel (wf_bd1e6180-aaf) → the seam-faithful A+B emulation the panel demanded → SIX Aaron popups. **THE MEASURED STORY:** the aggressive D64 doctrine (cautious stripped by hook) STILL loses Fredericksburg 0/8 at +124% of a naive player's cost — the per-unit localSup gate + hold-at-fire-range is the grinder; ENVELOPMENT is the lever — ½-deep + ½-press wins flipped Shiloh 8/8 (all else 0-3/8) because deep presence within RALLY_R denies rally by existing, and E49a captures fire for the FIRST time in correct-direction geometry (9,582 at Antietam P1); the mechanics are JOINTLY required (wing-alone 1/8 · press-alone 0/8 · composite 8/8); the trigger is load-bearing (unconditional wing at MH = 19,231 of the attacker's OWN men captured; with the trigger the wing stays home, 0 deploys); the seam-faithful form holds EVERY standing direction (Chancellorsville §6 CS 8/8 full-set · Antietam P0/P1/P2 · MH · Shiloh unflipped · Gettysburg D1 toward history) EXCEPT Bull Run (flips US 8/8 in emulation) · Piper P1 4/8 vs ≥6/8 · Antietam P0/P2 resist every plan inside their 220s clocks (~48 rows). **AARON'S SIX LOCKS (D267): A+B+C ship as ONE doctrine** (envelopment wing + wave-commit + abandonment/recall — LAW `docs/design/e53-attacker-parity-design.md` §3–§4) · the D64 lock-(a) supersession RECORDED · Bull Run goes to the build's real A/B (a red = revert-file-halt + the prepared McDowell-piecemeal assaultDoctrine remedy as DECISION-NEEDED — never a weakened gate/tuned trigger) · captured-honesty = a DECISION-RULE never a numeric gate (five-figure captured at a no-documented-mass-surrender battle = refute-and-revert class, Aaron adjudicates) · **NEW E57** (phase-clock accurate-inputs audit — flat 220s = ~49×–90× inconsistent compression; clocks stand meanwhile; lengthen-to-flip stays rejected) · **QUEUE: E56 owner-slice FIRST → the E53 BUILD → the E47 flip re-attempt (direction projects GREEN, seam 8/8 vs ≥4/8; a captured>0 red goes to an E54-scoped session) → the E46 Piper re-attempt HONESTLY EXPECTED RED (seam 4/8 vs ≥6/8; stays open pending E57/E54) → PM3 (hard-gated on E47).** Panel sign-correction banked: the "7,040 CS captured" Fredericksburg row was 7,040 US — the attacker's OWN cut-off wing (T25:134 keys the ledger by surrendering side). Watch: MH s7 reads CS/hold at HEAD (probe tooth US ≥6/8 reads 7/8, still green — E56-class drift note). Evidence: **D267** · the law · `.tmp/e53-evidence-digest-d267.md` (+ the harnesses/JSONs, gitignored).

## ⚡ PREVIOUS — 2026-07-05 **D266: PARITY-M2 SHIPPED — THE D250 CAPPED ENEMY-STRENGTH CONDITIONING CHANNEL IS LIVE (E43 CLOSED); THE D249/D251 WIN-CHANNEL INVERSION IS MEASURED DEAD. NEXT = THE E53-CLASS DESIGN SESSION (Prompt A below).** The rebuild ran the D251 spec verbatim (no new constants; the only changed variable = the E48/E49a/E49b engine underneath): `bridgeEnemyWillStrengthMul` in 85 (exact-1.0 at 72/70, debuff-only, 0.0015/pt, floor 0.90, NaN-safe) + the T2 men/maxMen enemy leg (initial line + T1-seam reinforcements) + the `_a6Condition` strength/maxStr leg (enemy morale untouched; 87's rating read = recorded PM3 debt). **Unit teeth = the D251 record row-for-row** (conditioning 9/9 — Classic enemy 4,577→4,304 at will 30, every unit exactly round(fresh×0.94); campaign-link 18/18 — fresh enemy byte-identical to standalone, reinforcement seam 1000/1200→900/1080 exact, no player-line leak). **The A/B (2×10 seeds × fresh/eroded vs HEAD): fresh 0/20 · base-build erosion inert 20/20 · playerWins 13→17 FOR the player** (six US seeds convert 9,600-step timeout losses into ~2,000-step hold wins; s5 = the lone D249-class rout-cycling residual, s7 = an honest contested near-miss at 33.8/36 — both trace-classified) · like-for-like casualty channel UP 0.3881→0.4199 (the raw dip is early-win Simpson composition — panel-re-derived; **gate provenance recorded in D266:** the raw-aggregate tooth was session-added, never law; D250's recorded red trigger is the WIN channel; the final harness gates strictly MORE than the D249 original) · ×2 byte-identical, ok=true · **3× SAFE_TO_COMMIT Opus panel (wf_d73ba771-5f1)**. Watch rows: `captured` 0 on all 40 eroded rows (the E53 envelopment gap at a FOURTH config); E51's raid −6 confirmed applied-at-launch/combat-dominated-by-60s (stays PROPOSED). **NEW E56 filed:** probe-arms' bullrun1 arms-OFF baseline (committed CS 5/8) reads 7/8 at HEAD — PRE-EXISTING E48/E49-era drift (last green D251; proven against the HEAD deliverable; structurally unreachable by this slice), needs its owner's attribution A/B + deliberate re-anchor; `vet:noreg` will red on that entry at the next batch checkpoint until resolved. Evidence: **D266**. E54 stays filed-not-scheduled (its PM2-red trigger did NOT fire). Queue: **the E53 session → the E47 flips + E46 Piper re-land behind the SAME unweakened gates → PM3 (hard-gated on E47).**

## ⚡ PREVIOUS — 2026-07-05 **D265: AARON'S PLAYER-AGENCY AUDIT — VERDICT NO-MANDATE (the measured risk runs the OTHER way: opponent quality below historical makes alt-history too cheap); AARON LOCKED E53's SCOPE TO FULL ATTACKER TACTICAL PARITY ("concentrate, envelop, and/or exploit") AND SHIPPED THE PLAYER-ORDER HARNESS AS A STANDING NON-BLOCKING DIAGNOSTIC (`tools/diag-player-agency.mjs`, vet:noreg 109→110); NEW LEDGER E55. THE QUEUE IS UNCHANGED — PM2 REBUILD NEXT (Prompt A below).** The audit (4 read legs + Opus default-refute judge wf_ac91406a-8aa + the repo's first player-ORDER measurements): one universal side-agnostic combat model; **every direction gate is dev-time AI-vs-AI** — no probe asserts a player-game outcome; `fate` an exposure die a player can beat (risk decays on withdrawal, T3:236); `atSec` a fixed historical timetable (input); B-5 cushions player-favoring only; alt-history endNotes in all 9 battles. **Empirical baselines (in the tool header):** Fredericksburg US player idle 0/8 → **naive frontal 8/8** → flank 8/8 cheaper, vs the D64 AI 0/8 at double cost; Antietam frontal **P1 8/8 inside the 220s clock**, P0/P2 hold. The diagnostic is NON-BLOCKING by design (reachability logged never asserted — SL-7/D74; structural teeth only). The clock question + the opponent-quality fix ride the E53 session; honesty note: the audit's battle-data read leg was a degenerate stub (D254 class), caught by the judge who re-verified data first-hand. Evidence: **D265** · E55/E53 ledger lines · `tools/shots/diag-player-agency.json`.

## ⚡ PREVIOUS — 2026-07-05 **D264: THE PIPER GUNS RESTORE (E46 COMPLETION) RAN WITH E48+E49a+E49b ALL LIVE AND WENT HONESTLY RED ON THE UNCHANGED D243 GATE — REVERTED; THE THIRD E53 DATA POINT, MEASURED IN THE SHIPPING CONFIG; THE QUEUE MOVES TO THE PM2 REBUILD (Prompt A below).** The build was exact to the D243 record (`cs_piper_guns`: Miller's WA 3rd Co. + Hardaway + Boyce + Carter, 8 guns/136 crew at the D75 17-per-gun convention, the sourced farm seat z 212 OUTSIDE the objective ring, citation-stamped Longstreet staff-manning note) behind its own both-seed-set A/B with the §5.4 shedding ON/OFF guard (`.tmp/ab-e46-piper.mjs`, 0 pe). **RED — the D243 red signature verbatim: P1 Sunken Road US 8/8 → 1/8 (default) / 2/8 (probe) vs the ≥6/8 gate, every other seed a CS timeout hold**; P0/P2/aggregate CS 8/8 hold; **all 8 non-Antietam battles + sandbox byte-identical on every seed** (data-only proof); guard clean both ways (piperNoShed P1 US 1/8 — shedding assists one seed, breaks nothing). **The mechanism is measured in the shipping config: `captured` 0 on all 33 Antietam leg-rows** — the lane's defenders rout toward their OWN rear, so the ~300-prisoner Barlow-wheel pocket never forms (the D263 geometry finding, third battle) — **and E48-live timeouts are genuine stalls** (the US never mid-hold at the buzzer): the D64 attacker grinds frontally and cannot convert ~2:1 weight inside 220s against the documented gun line. Honest ratio rows (logged never gated — D74/SL-7): **with the guns the battle ratio lands 1.25/1.22 vs the historical 1.20** and P1 mean US loss rises 597 → 1,791 (toward the sector's ~3,000) — every COST moves toward history while the RESULT flips: the wrong-mechanism fingerprint; HEAD's own baseline ratio is now 0.98/1.02 under E49b accounting (was 0.81/0.82 at D243). **Revert-file-halt executed:** unit row reverted; `_e46Note` rewritten honestly (the divergence is STILL OPEN; the completion target is re-conditioned from "when E48/E49 land" — superseded by measurement — to "when the E53-class attacker-side mechanics land"); sim-inert proof of the note-only diff (all 11 Antietam rows byte-identical to the HEAD base leg); probe-antietam 16/16 + probe-gettysburg 18/18 on the reverted tree; re-land spec preserved **`.tmp/e46-piper-evidence-d264.md`** (exact row + numbers + recipe; registry re-pin 645→648 and the probe completion tooth ship with the re-land). No commit panel due (comment-only data + docs — the D242/D256/D260/D262 precedent; the A/B was the arbiter). **The E53 design session now carries THREE data points: D254's MH 3.59 · D262's full-clock grinds · D264's Piper lane. NEXT: the PM2 rebuild + re-A/B (Prompt A below) → the E53-class session → the flips + the Piper re-land behind the SAME unweakened gates → PM3.** Evidence: **D264**.

## ⚡ PREVIOUS — 2026-07-05 **D263: AARON ANSWERED THE D262 FORK "a" SAME-SESSION AND THE DESIGN SESSION HONESTLY REFUTED ITS OWN PREMISE — THE FLIPS ARE PARKED FOR E53-CLASS DESIGN; THE QUEUE CONTINUES AT PIPER GUNS → PM2; NEW LEDGER E54 FILED (law §7).** The session measured before designing: **pocket-at-break fires 2/195 (flipped-Shiloh US) and 0/140 (flipped-Gbg US)** — insufficient exactly at the targets — while wrongly arming **MH CS 8 units/~19,249 men · Antietam US 4/~11,630 · both Chancellorsville sides** (false positives at battles with no documented mass surrender); the frozen-break-point variant is marginal-at-targets/catastrophic-elsewhere (6/6·4/5·4/4 completions on five-figure men at Bull Run/Antietam/Fredericksburg). The rally measurement found the real physics: **routers rally OFF-FIELD (median 18-72yd) and massively FRIENDLESS** (flipped Shiloh US 61%/CS **99%** · Antietam US 98% · Fredericksburg 91% · Bull Run 70% — vs anchored Chancellorsville US 30%, nearest-friendly median 26yd). The surfaced candidate V1 rally-anchor-or-exit is calibrate-free (all back-solve refutations FAILED) but the **3-lens default-refute Opus panel (wf_10bb78af-d6c: SHARPEN_FIRST + 2× PREFER_PARK) refuted it as a flip-enabler**: the ATTACKER dissolves symmetrically → captured stays 0; the flagship events are formal SURRENDERS that must land in CAPTURED (not scatter-to-missing); predicted reds at zero-headroom §6 + Antietam (a 10-18× player-facing missing overshoot = a teaching defect); a steady-count→0 cascade amplifier (the per-event-shedding class); and the bluff crowd historically STOPPED at the Landing (cuts against exit-and-vanish). **AARON'S 3 POPUPS (all recommended): (1) flips PARKED for an E53-class attacker-side design session (assault-abandonment + envelopment/flanking — the D64-adjacent gap every measurement points at); re-attempt behind the SAME unweakened D242 gates after it lands. (2) Locked queue FIRST: Piper guns (E46 completion, Prompt A below) → PM2 rebuild + re-A/B (its post-E49 result is direct E53 evidence) → the E53 session; PM3 stays hard-gated on E47. (3) NEW ledger E54 (rally-consequence) FILED-not-scheduled with the panel's mandatory sharpenings; first customer = the PM2 re-A/B if it reds again.** Docs-only; deliverable byte-identical through the session by cmp; `DECISION-NEEDED-e47-flips.md` deleted (resolved). Evidence: **D263** · law §7 · `.tmp/measure-pocket-d263.json` + `.tmp/measure-rally-d263.json`.

## ⚡ PREVIOUS — 2026-07-04 **D262: THE SHILOH FLIP RAN WITH E49 FULLY LIVE AND WENT HONESTLY RED ON BOTH HALVES OF ITS UNCHANGED D242 GATE — REVERTED; A GETTYSBURG EVIDENCE SMOKE SHOWS THE IDENTICAL CLASS; THE FORK IS AARON'S (`DECISION-NEEDED-e47-flips.md`).** The mandated data-only flip (`homeEdge {US:low, CS:high}`, the D240/D241 idiom) behind its own both-seed-set A/B with the §5.4 shedding ON/OFF guard: **flip (shipping config) CS 3/8 BOTH sets vs the ≥4/8 gate + `captured` 0 on ALL 11 seeds** (the Prentiss half never fires) · **flipNoShed CS 2/8 both sets = D242's pre-E49 number exactly** (the D257 guard clean both ways: shedding assists one seed, breaks nothing). **The root cause is now MEASURED** (`.tmp/diag-shiloh-flip.json`): max `surrenderT` ever accrued by any US router post-flip = **0.8s of the 6s grace** (2 of 4 seeds never blocked at all) · CS steady penetration min z 275-380 vs the Landing at 120 · US units break **123-134×/battle and return every time** · `fldSurrenderBlocked` verified direction-correct (no sign bug) — **the SL-2 corridor predicate only fires when routers flee INTO the enemy: every E49a capture at these two battles (capUS 6,349 · capCS 27,073) rode the artifact direction the flips remove** (the real mass captures were POCKETS closing around a position, not a router blocked mid-flight 6s). The Gettysburg smoke (top-level `homeEdge`, reverted by design): **D1 CS 6/8-def/8/8-prb → 1/8/0/8, captured 0 everywhere**; agg US 8/8 + D2/D3 hold. Revert-file-halt executed: **deliverable byte-identical to HEAD 43a0c19 by cmp ×2 · GATE OK ×4 · probe-shiloh 31/31 0 pe on the reverted tree · diff-check clean**; no Opus panel due (docs-only — the D242/D256/D260 precedent). Re-land preserved: **`.tmp/e47-flip-evidence-d262.md`** (exact edits incl. the citation-stamped ~2,200 Prentiss SL-8 narration; ~2 min + the battery per battle). Also logged as E53 evidence: the CS grinds the full 480s at ~93% strength on every post-flip US-win seed. **THE FORK: (a) RECOMMENDED — a bounded Aaron-driven design session (the D255/D259 pattern) on an envelopment-predicate v2 for the flipped geometry (the pocket-at-break class, candidates prepared-not-decided; SL-7 binds; the flips re-attempt behind the SAME unweakened gates) with the queue continuing flip-independent meanwhile (Piper guns E46 completion → PM2 rebuild + re-A/B per D250/D251) · (b) E53-first (honestly noted: unlikely to rescue the flips alone) · (c) gate re-anchor (NOT recommended — the mechanism half fails absolutely, 0 captures across all 22 flip-leg rows; unlike D261's knife-edge headroom) · (d) close E47 partial (rejects D237). NEXT: Aaron's answer → Prompt A below executes it.** Evidence: **D262**.

## ⚡ PREVIOUS — 2026-07-04 **D261: E49b SHIPPED — STRAGGLER-SHEDDING IS LIVE GAME-WIDE (asymmetric battles). Aaron resolved the D260 fork same-day ("a"): the Chancellorsville gate RE-ANCHORED BY ITS OWNER (law §6 NEW — CS 8/8 default + ≥7/8 probe + the honest-contested rider: any non-CS seed must be `by=timeout` with BOTH sides' menObj > 0 at the buzzer; the D241 false-containment class stays a HARD RED; every other gate verbatim; SL-7 untouched; no probe assertion weakened — probe-chancellorsville's own ≥4/8 tooth reads 7/8).** The preserved D260 build re-landed byte-identical: **probe-field 22/22, 0 pe** (first-break EXACT 600/1000→570/30 · missing EXACTLY 50 through 5 breaks · captured banks the POST-shed 950 · both sides shed · `_e49NoShed` honored · Antietam phased bound) · **the §5.4 battery ×2 ALL GATES GREEN, run 2 byte-identical by cmp** (surr≡base all 110 rows · the ≤f bound per side/battle/seed AND per-phase · Gettysburg US 8/8 + Shiloh CS-majority + MH US ≥6/8 both sets · **s42 verified honest-contested: US/timeout@480, menObj US 3,252/CS 3,406** · shed fractions 2.8–4.8% · MH ratio 3.59→2.86 TOWARD the 1.8–2.7 band · MH US mean kw 2,547 ≈ the in-band ~2,561) · **all 8 standing probes green 0 pe** (MH 27/27 · Ch 27/27 · Ant 16/16 · Shi 31/31 · Gbg 18/18 · field 22/22 · link 17/17 · cond 7/7) · 3-lens Opus panel at commit (verdicts in RUN-LOG). Player-visible: the after-action MISSING column lights up (~3–5% side-level first-break straggling, silent ledger, OR convention, the "first broke" sentence). The s42 mechanism (a thinned assault grinding past the clock) is logged as **E53 evidence**. **NEXT: the SHILOH FLIP (Prompt A below — surrender + straggling both now exist, the mechanisms the D242 flip REDs were missing) → the Gettysburg flip → Piper guns → PM2 rebuild + re-A/B → PARITY-M3.** Evidence: **D261**.

## ⚡ PREVIOUS — 2026-07-04 **D260: THE E49b BUILD RAN EXACTLY TO THE §5 LAW AND WENT HONESTLY RED ON THE ONE CARRIED GATE — REVERTED; THE FORK WAS AARON'S (`DECISION-NEEDED-e49b-chancellorsville.md`, resolved "a" same-day → D261).** SL-1v2 built verbatim (one function + one T0:563 hook + `shedDone` spawn-init + the authorized "first broke" sentence): **probe-field 22/22, 0 pe** — first-break EXACT 600/1000 → 570/30 (men-present anchoring load-bearing) · missing EXACTLY 50 through 5 breaks (the dead form read 227) · shed→rally/kill/surrender identities EXACT (captured banks the POST-shed 950) · both sides shed · `_e49NoShed` honored · Antietam phased bound green. **The §5.4 battery (4 legs × [9 battles + sandbox] × 11 seeds, 0 pe) green on EVERY structural + named gate** — sandbox byte-identical ×4 · **surr≡base all 110 rows (the flip is PURE SL-1v2)** · missing==0 base+surr / >0 at the 4 heavies · the `≤ f×fielded + 0.5×unitsShed` bound per side/battle/seed AND per-phase (the invariant that would have caught D256) · **Gettysburg US 8/8 + Shiloh CS-majority + MH US ≥6/8 both sets** · shed fractions 2.8–4.8% (the §5.1 prediction) · **MH ratio 3.59→2.86 TOWARD the 1.8–2.7 band · MH US mean kw 2,547 ≈ the in-band ~2,561** — **EXCEPT Chancellorsville: probe set CS 7/8, SEED 42 AGAIN** (CS/hold@222 → US/timeout@480 at a near-symmetric CS 830/US 807 shed — **5.3× smaller than the D256 form's 4,378** — an HONEST contested E48 timeout: buzzer menObj CS 3,406 vs US 3,252, both armies ON the ring; not the D241 false-containment class; also logged: Ch kw UP on the shed legs — thinned assaults fight longer, the E53 class surfacing). Two forms × the same seed ⇒ **the zero-headroom SEED is the finding, not the form** (base already clears in E48 overtime on 4/8 probe seeds). SL-7 forbids every builder-side fix ⇒ revert-file-halt: **deliverable byte-identical to HEAD 0cf6842 by cmp, probe-field 19/19 originals, GATE OK, diff-check clean**; build preserved (`.tmp/e49b-*`, ~20-min re-land recipe `.tmp/e49b-reland-spec.md`, battery `.tmp/ab-e49b.mjs`+run1). **THE FORK: (a) RECOMMENDED — Aaron re-anchors the Chancellorsville gate for attrition-class changes (honest-contested rider; false-containment stays a hard red) → re-land · (b) hold for a joint E53 landing · (c) third form session (not recommended — a form built to spare s42 is back-solving). NEXT after resolution: the Shiloh/Gettysburg flips + Piper guns (shedding ON/OFF guard; the D258 rider stands) → PM2 rebuild + re-A/B → PARITY-M3.** Evidence: **D260**.

## ⚡ PREVIOUS — 2026-07-04 **D259: THE E49b DESIGN SESSION RAN AND LOCKED — straggler-shedding is redesigned as FIRST-BREAK-ONLY `round(men×0.05)` (all 4 popups = the recommended options); the BUILD is the next milestone (Prompt A below).** The session did the D255/D246 pattern on the shipped E49a HEAD: engine facts re-verified first-hand (**rally never restores men** — the hypothesized shed→rally identity hole refuted against live code; T8:98 fields fresh rosters per phase; the missing ledger confirmed wired-but-zero end-to-end), a NEW direct measurement (per-unit break instrumentation, 4 battles × 3 seeds, 0 pe: **0.45–16 breaks/unit per side** — any per-event rate is uncontrolled; the dead form's **2.2× feedback amplifier** measured at the D256 red seed; **first-break-only stable at ~4–5% side-level, bounded ≤ f×fielded by construction**), then a 3-lens default-refute Opus panel (**all SHARPEN_FIRST, unanimous for first-break-only**; maxMen-anchored refuted as dominated; kw-flat demoted to watch; the ≤f gate given rounding/phase headroom). **Aaron locked: the form · f=0.05 re-anchored as the ONE-TIME collapse fraction (explicit SL-7 extension: never raised toward any band/gate) · PER-PHASE re-arm · SILENT ledger (sentence sharpens to "first broke" — the one authorized E49a-surface word-change).** LAW: `docs/design/e49-surrender-straggler-design.md` **§5** (SL-1v2 + the §5.4 battery additions: the two shipped protective teeth re-target in the SAME commit — never deleted; the structural `≤ f×battleFielded + 0.5×unitsBroke` bound; named pre-flip **Gettysburg US 8/8 + Shiloh CS-majority** rows on the both leg; the shedding-ON/OFF flip-A/B guard). **NEXT: the E49b BUILD (fresh session, Prompt A — one function + one T0:563 hook + fixture re-targets; red → revert-file-halt) → the Shiloh/Gettysburg flips + Piper guns (E47/E46 completion) → PM2 rebuild + re-A/B → PARITY-M3.** Evidence: **D259**.

## ⚡ PREVIOUS — 2026-07-04 **D258: E49a SHIPPED — ENVELOPMENT-SURRENDER IS LIVE GAME-WIDE (asymmetric battles); every §3 gate green on the first run; 3× SAFE_TO_COMMIT.** The preserved D256 implementation re-landed surrender-only: probe-field 19/19 (the NEW SL-1-ABSENT tooth binds protectively — it FAILS if shedding returns); the 4-leg battery ×2 ALL GATES GREEN + byte-identical determinism — **Chancellorsville CS 8/8 both sets with rows identical to base** (the D256 red was pure shedding, now absent), **MH US 8/8 both sets** (ratio 3.59 unchanged), shed leg ≡ base EVERYWHERE (the removed half provably gone), surr ≡ both, 7/9 battles byte-identical; captures only at unflipped Gettysburg (capCS 27,073/7 seeds — the artifact-direction class the flip milestone replaces, honestly logged) and Shiloh (capUS 6,349 — **the Hornets' Nest mass-capture previewing**); missing = 0 everywhere (wired-but-zero awaiting E49b). Standing probes all green (MH 27/27 · Ch 27/27 · Ant 16/16 · Shi 31/31 · Gbg 18/18 · field 19/19 · link 17/17 · cond 7/7). **3-lens Opus panel: 3× SAFE_TO_COMMIT** — no-fudge ZERO findings; probe-integrity confirms the fixture re-targets are protective re-scopes; history/teaching's firm rider: **the Shiloh/Gettysburg flips MUST NOT SLIP** (while Gettysburg sits unflipped its captures point the wrong way — backwards vs the ~3,600 Union prisoners of the Day-1 retreat). **NEXT: the E49b straggler-shedding design session (Aaron drives, Prompt A below) → the Shiloh/Gettysburg flips + Piper guns (E47/E46 completion) → PM2 rebuild + re-A/B → PARITY-M3.** Evidence: **D258**.

## ⚡ PREVIOUS — 2026-07-04 **D257: AARON RESOLVED THE E49 FORK SAME-DAY ("a") — SPLIT. E49a (surrender-only) RE-LANDS NEXT SESSION from the preserved implementation behind the FULL §3 battery; E49b (shedding) gets a bounded Aaron-driven design session after, armed with the D256 measurements (engine rout frequency 3–8 breaks/unit in normal battles → 15–30% side shed at any per-event rate; candidate forms: first-break-only · once-per-battle round(maxMen×f) — prepared, not decided). Gates verbatim: Chancellorsville CS 8/8 + MH US ≥6/8 both sets MUST HOLD on E49a's landing; SL-7 no-back-solve; red → revert-file-halt. Queue after E49a: E49b design session → the Shiloh/Gettysburg flips + Piper guns (E47/E46 completion, riding E49a's removal mechanism — a flip needing the attrition partner is E49b EVIDENCE, never a tuning license) → PM2 rebuild + re-A/B → PARITY-M3. Evidence: D257/D256.** `DECISION-NEEDED-e49-shedding.md` deleted (resolved). Docs-only.

## ⚡ PREVIOUS — 2026-07-04 **D256: THE E49 BUILD RAN AND WENT HONESTLY RED ON THE LAW'S OWN GATE — REVERTED; THE FORK IS AARON'S (`DECISION-NEEDED-e49-shedding.md`).** Built verbatim to the D255 law; every unit tooth green (probe-field 19/19, 0 pe — exact shed compounding, blocked-corridor surrender with rallyT provably suppressed, the T2 identity EXACT, geometry controls, T8 subsets, sandbox inert). The §3.3 isolated A/B ({base, surrender-only, shedding-only, both} × [9 battles + sandbox] × 11-seed union, 0 pe): sandbox byte-identical on all legs, every direction holds EXCEPT **Chancellorsville CS 7/8 on the PROBE set (BOTH leg, seed 42)** — the CS assault sheds 4,378 stragglers and cannot clear the crossroads (an honest contested E48 timeout, US 4,979 men physically in the ring). Isolation attribution: **the surrender-only leg is FULLY GREEN on every gate** (touches only unflipped Gettysburg + Shiloh, where capUS 6,349 previews the Hornets' Nest); the flip is **pure SL-1 shedding**. Material finding: normal battles shed 15–30% side-level (the rationale assumed ~5–10% — engine rout frequency is 3–8 breaks/unit), total-loss columns inflate 2–3.3× documented bands with kw flat, MH ratio 3.59→4.04 away from the band (E53 evidence). SL-7 forbids tuning → revert-file-halt executed: deliverable byte-identical to HEAD (cmp), probe-field 14/14 originals green, GATE OK, diff-check clean. Everything preserved: `.tmp/e49-T25-surrender.js` · `.tmp/e49-tracked.diff` · `.tmp/e49-reland-spec.md` · `.tmp/ab-e49.mjs` + `ab-e49-run1.json` (the battery re-runs in ~6 min, gates + watch metrics built in). **Options (recommendation a): (a) split — re-land surrender-only NOW as E49a + a bounded E49b shedding design session with the measurements · (b) relax the Chancellorsville gate to ≥6/8 (NOT recommended) · (c) full halt until a combined redesign.** Queue after resolution: E49a/E49b → the Shiloh/Gettysburg flips + Piper guns (E47/E46 completion) → PM2 rebuild + re-A/B (D249 gate verbatim; E51 same battery) → PARITY-M3. Evidence: **D256**.

## ⚡ PREVIOUS — 2026-07-04 **D255: THE E49 DESIGN SESSION RAN AND LOCKED — envelopment-surrender + straggler-shedding have their Aaron-locked design law (`docs/design/e49-surrender-straggler-design.md`, SL-1..SL-10 + the §3 acceptance battery; all 8 popup decisions = the recommended options; evidence packet now TRACKED with its §8 panel-corrections addendum). NEW ledger E53 (assault-abandonment — filed, designed AFTER E49 + the flips; E49 must not touch the D64 commit logic). The next session is the E49 BUILD (Prompt A below): both mechanics as universal rules behind the locked battery — isolated A/B legs {base, surrender-only, shedding-only, both} × 9 battles × 11-seed union, Chancellorsville CS 8/8 + MH US ≥6/8 both sets MUST HOLD, all standing direction probes re-run at landing, determinism re-run, 3-lens Opus panel; red → revert-file-halt. Then: the Shiloh/Gettysburg flips + Piper guns complete E47/E46 (E49's true acceptance tests) → PM2 rebuild + re-A/B (D249 gate verbatim; E51 same battery) → PARITY-M3.** Key panel sharpenings baked into the law: the DIRECTIONAL-blocker clause + blocked-suppresses-rally (mechanic-blocking spec gaps); per-side compounding math corrected (~17–33% side shed at f=0.05); surrender = the removal mechanism, shedding = the attrition partner. Evidence: **D255**.

## ⚡ PREVIOUS — 2026-07-04 **D254: E52+E48 SHIPPED AS ONE MILESTONE — Malvern Hill's documented repulse now EMERGES from documented inputs (US 8/8 both sets, a physical hold of the Crew-house ring) and Chancellorsville's false containments stay closed (CS 8/8 both sets HELD); the E48 buzzer rule is LIVE game-wide. The E49 design session follows (it ran — see D255 above). Then: the Shiloh/Gettysburg flips + Piper guns complete E47/E46 → PM2 rebuild + re-A/B (blocked pending E49, D249/D251) → PARITY-M3.** One honest overshoot logged never tuned (D74): MH atk/def 1.34→3.59 past the historical 1.8-2.7 band — the no-assault-abandonment gap, now ledger **E53** (filed in D255). Evidence: **D254**; research trail `docs/design/e52-malvern-research.md` (tracked).

## ⚡ PREVIOUS — 2026-07-04 **D253: AARON APPROVED E52 SAME-DAY ("all recommendations approved") — the next session builds E52 (Malvern Hill accurate-inputs defense correction) + the E48 re-land as ONE milestone (Prompt A below, now single-branch). The D252 gate stands verbatim: Chancellorsville CS 8/8 HOLDS and MH US ≥6/8 EMERGES FROM INPUTS — never tuned; failure → revert-file-halt. Then: E49 (design session FIRST) → the E47 flips + Piper guns → PM2 rebuild (blocked pending E49, D249/D251) → PARITY-M3.**

## ⚡ PREVIOUS — 2026-07-04 **D252: E48 BUILT + PROVEN AT ITS TARGET, THEN HONESTLY REVERTED — the A/B closed Chancellorsville's false containments (CS 8/8 both sets) but exposed MALVERN HILL's documented repulse as 100% clock-dependent; ONE Aaron decision pending: approve E52 (`DECISION-NEEDED-e48-malvern.md`) → RESOLVED same-day, D253.** The complete-the-hold overtime form shipped its whole gate battery pre-revert: probe-field fixture 14/14 (mid-hold buzzer completes the fall in bounded overtime; contested/unattended objectives time out AT the clock), 9-battle × 11-seed-union A/B (198 headless battles, 0 pe): 5 battles byte-identical · Antietam/Gettysburg all directions hold · **Chancellorsville 4/4 → CS 8/8 BOTH sets** (every D241 false containment plays out to the historical hold, 487-532s, ratio in band) · **Malvern Hill US 8/8 → 4/4** — base wins ALL `by=timeout`, and the buzzer diagnostic shows the CS mid-hold with 5,286-13,814 men, US ZERO in radius, 4-6 US units routing, on EVERY seed (the D240 panel's deferred under-modeled-defense gap, now load-bearing; the contested/draw sub-form is strictly worse — all 9 seeds draw). Its documented `usWins ≥ 6` gate is never weakened (D74) → REVERTED per never-push-red (deliverable byte-identical by `cmp`; probe-field 13/13; the D232 screenshot repair ships). **NEW ledger E52** (MH accurate-inputs defense correction — candidates: Morell/Couch engaged weight, Sykes, Meagher/Sickles late brigades, gun rotation/siege guns/gunboats, CS assault mass; ALL to verify citation-grade, D92). Exact re-land: **`.tmp/e48-reland-spec.md`** (~30 min + the surviving A/B battery). Evidence: **D252**. **NEXT: Aaron answers `DECISION-NEEDED-e48-malvern.md` → (a) RECOMMENDED E52+E48 one session; or (b) the E49 design session first (independent, approved). Then: E49 → Shiloh/Gettysburg flips + Piper guns (E47/E46 completion) → PM2 rebuild + re-A/B (blocked pending E49, D251) → PARITY-M3.** All locks stand.

## ⚡ PREVIOUS — 2026-07-04 **D251: THE PM2-STRENGTH REBUILD WENT HONESTLY RED ON THE SAME DIRECTION A/B AND WAS REVERTED — the inversion is an ENGINE class, not a channel; PM2 (either channel) is BLOCKED pending E49; the same session proceeds to E48 (docs+harness commit only).** The D250 contract was built exactly to the amended law (85 `bridgeEnemyWillStrengthMul`: exact-1.0 at 72/70, debuff-only, 0.0015/pt of erosion, floor 0.90 pinned at will 0 both baselines, NaN-safe; T2 men/maxMen + `_a6Condition` strength/maxStr legs) and every unit tooth passed (conditioning 9/9 · campaign-link 18/18: fresh enemy byte-identical to standalone, will 30 → exact ×0.94 per unit). The D249-gate A/B: fresh **0/20 diffs**, casualty channel correct and STRONGER than the morale form (mean enemy loss .3559→.3987; survivors 5,854→5,132) — **but playerWins 15→11 (attacker rows 5→1), all 4 flips the exact D249 fingerprint** (base `w=US by=hold` ~2k steps → eroded `w=CS by=timeout` 9,600). Trace: the thinner defender routs 3-4× more and always returns (CS in the objective radius ~8,000/9,600 steps); the ATTACKER's own routs explode 10-18 → 77-89 (D64 ratio-gated commits grinding a never-dissolving defense); **at the buzzer the US holds NOTHING (holdSecs 0, zero men in radius, 4/4) — NOT the E48 buzzer class, by measurement.** The E49 class end to end → **no defender-debuff channel ships pre-E49.** Reverted per never-push-red (deliverable byte-identical to HEAD by `cmp`; 7/7 + 17/17 + arms 23/23 green on the reverted tree; the probe-arms D232/D233 harness repairs ship). Evidence + the ~1-hour rebuild spec: **D251** (A/B + trace scripts survive gitignored in `.tmp/`). **NEXT (this session): E48 → then E49 (design session FIRST) → the Shiloh/Gettysburg flips + Piper guns → PM2 rebuild + re-A/B → PM3.** All locks stand.

## ⚡ PREVIOUS — 2026-07-04 **D250: AARON RESOLVED THE D249 FORK SAME-DAY — PARITY-M2 switches to the capped enemy STRENGTH channel and builds NOW (Prompt A below: PM2-strength → then E48 in the same session if the window allows).** His words: "switching the enemy-conditioning channel to a capped strength debuff now." Everything in the D249 spec stands except the channel: the ONE shared contract now outputs an enemy **strength multiplier** (will-erosion = desertion = fewer men — monotone in both engines, structurally immune to the D249 rout-cycling inversion), exact-**1.0** at the 72/70 baseline, debuff-only, hard-floored, NaN-safe, consumed by the same two legs (T2 men/maxMen + `_a6Condition` strength/maxStr). The design law carries the §6.1 amendment; E43 ledger + V1-CHECKLIST flipped; `DECISION-NEEDED-parity-m2.md` deleted (resolved in D250). **The D249 direction gate stands verbatim for the rebuild: fresh-state byte-identity 0/20 + eroded direction on BOTH the casualty AND win channels — invert again → revert, file, HALT.** PM3 remains hard-gated on E47 completion (E48 → E49 → the flips). All locks stand.

## ⚡ PREVIOUS — 2026-07-04 **D249: PARITY-M2 ATTEMPTED → HONESTLY RED → REVERTED; the parity arc HALTS behind E48/E49 (docs+harness commit only).** The E43 shared enemy-conditioning contract was built to the design law and its unit teeth all passed (85 `bridgeEnemyWillDebuff` exact-zero at 72/70, cap −6, NaN-safe; T2 + `_a6Condition` legs; campaign-link 18/18, conditioning 9/9 pre-revert) — then the mandated eroded-will direction A/B went RED: fresh state byte-identical **0/20 diffs**, casualty channel correct in aggregate (mean enemy loss fraction .3559→.3675; survivors 5854→5774), **but the attacking player's wins fell 15→12 — every flip the same signature (base `hold`-win ~2k steps → eroded `timeout`-loss at 9,600)**. Trace-diagnosed (US#3): the debuffed defender routs 4× more (127 vs 31) and bleeds faster, but consequence-free rout-rally cycling re-contests the objective forever (US holdSecs pinned 0) — **the E49 surrender/straggler gap + the E48 buzzer class convert brittleness into a defense; will-erosion would teach backwards.** Slice REVERTED per never-push-red (deliverable byte-identical to the D246-D248 HEAD; probe-conditioning 7/7 original assertions; only its D233-class harness repair ships — it was harness-red at HEAD, steps=0). Full evidence + the ~1-hour rebuild spec: **D249**; ledger E43 → BLOCKED pending E48→E49→the E47 flips; NEW **E51** filed (raidSupply's real-time morale half is transient — the T0 MOR_RECOVER washout, measured 10/10 outcome-identical); the strength-channel fallback is Aaron's call (`DECISION-NEEDED-parity-m2.md`). **NEXT: E48 (universal buzzer-resolution rule, APPROVED — Prompt A below) → E49 (design session FIRST, APPROVED) → the Shiloh/Gettysburg flips complete E47 + the Piper guns complete E46 → PARITY-M2 rebuild + re-A/B → PARITY-M3.** All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-04 **D246-D248: PARITY-M1 SHIPPED — the battle-mode-parity arc is OPEN: design law committed, E45 cross-phase seam safe, ATTACKER-AI-PROPOSAL retired.** The arc's first milestone landed exactly per the design doc's §3 PM1: (1) `docs/design/battle-mode-parity-design.md` is now TRACKED (D246 registers the four Aaron-locked decisions; no untracked docs/design files remain); (2) **E45 built (D247):** T8 `battleFielded` cumulative tally beside `battleCas` (one `_fldSidePhaseTally` arithmetic, `_fldSidePhaseCas` a thin wrapper) + phase-aware `fldCampaignComputeOutcome` (guarded: `__FIELD.phases` set AND tallies accumulated → aggregate across ALL phases; else the unchanged single-phase read) — byte-identity PROVEN by probe-phased-ab **20/20 identical vs the D245 HEAD** (4 phased battles × 5 seeds, full outcome dicts), probe tooth in probe-campaign-link **17/17** (synthetic phased+campaign Antietam: honest all-phase pFrac 0.094 where the final-sector read would say ~0.43; fielded US 36,282/CS 20,975 vs last-phase 7,907/5,270); (3) **retirement (D248):** ATTACKER-AI-PROPOSAL.md → `legacy/` with the superseded-by-D64 header, START-HERE canonical line corrected. Adjacents antietam 16/16 + gettysburg 18/18; five D232/D233-class slow-Mac harness repairs assertions-untouched (campaign-link + antietam were harness-RED at HEAD, steps=0). Known latent recorded in D247 for the epics day: phased campaign conditioning touches phase-0 initial units only. **NEXT: PARITY-M2 (shared enemy conditioning — same session if context allows) → then the arc HALTS at the M3 boundary: PM3 is HARD-GATED on E47 fully landed (it is HALF-landed, D242) → E48 → E49 (design session first) → the Shiloh/Gettysburg flips complete E47 + the Piper guns complete E46 → THEN PARITY-M3.** All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-04 **D245: S25 PALETTE UNIFICATION SHIPPED — the three pre-battle config surfaces (T2 skirmish · T6 presets · T11 builder) now consume the shared H0 `--h0d-*` token set; the queue moves to the battle-mode-parity arc.** Presentation-only by PROOF: per-surface token definitions (the D232 six-shell idiom) with every value pinned to the `99-h0-president-desk.js` canon by a NEW node-side tooth in probe-custom-battle-builder (drift/unknown token = red before the browser launches); probe-presets' standing byte-identity guards green on the new skin (layers-off CS 5/8 unchanged, stacked run seed-for-seed identical); all recolored pairs ≥AA (worst text 7.48:1, worst non-text 3.82:1; the skirmish labels' latent `var(--rule)` 3.59:1 failure fixed as a side effect). Focused gate: node --check ×6 · GATE OK · **builder 15/15** (canon + computed-border teeth) · **presets 27/27** (S25 tooth) · adjacents field + h0-main-menu · 0 pe · three-surface screenshot readback · diff-check clean. Two documented slow-Mac harness repairs rode along assertions-untouched (presets+field off the stalling `'load'` wait D233-class; presets screenshot 120s D232-class). Opus lens default-refute: **SAFE_TO_COMMIT**, 4 LOW — the find: **the 95-playstyle picker is a FOURTH config surface on the same invented idiom → NEW ledger S39** (PROPOSED, with the deliberately-out-of-scope in-battle drawer + T2 briefing accents), not scope-crept. **NEXT: the battle-mode-parity arc (Prompt A below — its M1 commits the untracked `docs/design/battle-mode-parity-design.md`; its pacing A/B assumed E47 fully landed, which it is NOT — adjust or wait for E48/E49) → E48 → E49 (design session first) → the Shiloh/Gettysburg flips complete E47 + the Piper guns complete E46.** After the parity-arc M1 commits its doc, NO untracked docs/design files should remain. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-04 **D244: E13+E41 SAVE HARDENING SHIPPED — the tampered-autosave boot brick is closed and the save-shape law has structural build teeth; the queue moves to S25 palette.** One code-health milestone, two halves. **(E13)** NEW `src/105-save-guard.js` redeclares the frozen base's loadLocal + applySave (manifest `overrides` += both; the D44-class pattern) — the decisive design fact is HOISTING: the boot lane at base:3983 runs at script-eval time, so the 91-save-slots reassignment-wrapper idiom could never protect it, but duplicate top-level function declarations are instantiated last-in-source-wins BEFORE any statement runs. loadLocal now REJECTS an own-`hasOwnProperty` settings payload (reads as no-save — the D234 `_slValidSave` posture extended to the autosave lane); applySave iterates via `Object.prototype.hasOwnProperty.call` and skips the shadow key (sanitize, never throw). Byte-identical on every legit save. **Negative control:** the pre-fix HEAD deliverable + the tamper = boot death (`src.hasOwnProperty is not a function` + 2 cascade pageerrors, module layer dead); the fixed build = 0 pe, tamper rejected. **(E41)** NEW build gate **4h** + tracked **`tools/save-shape.json`**: sha256/16 pins on the 7 save-envelope-owning fns + `_SAVE_VER`↔saveVer mirror + required-core floor + same-file duplicate-declaration die + assignment-form rebind scan (importSave exempt — the D234 wrapper); **proven on 5 violation fixtures, live tree green** (`GATE OK … save-shape ✓`); the tracked-manifest diff IS the conscious bump-or-additive audit trail; honest coverage limits documented in-gate. **probe-save-slots 10/10 → 14/14** (tamper-boot reload + sanitize + round-trip + hygiene scan); two D232/D233-class harness repairs assertions-untouched (save-slots screenshot 120s budget; probe-full-campaign off the stalling `'load'` wait, was harness-red at HEAD today, 4 ok after). **3-lens Opus panel (default-refute): 3× SAFE_TO_COMMIT** — 1 MED fixed pre-verdict, 2 MEDs hardened in-slice, 1 pre-existing residual filed as **NEW ledger E50** (tampered `campaign.*` sub-objects still crash navigation-gated command surfaces — the callable-hasOwnProperty walk class in 35-command.js et al.; a future save-hardening slice, deliberately not scope-crept). **NEXT: S25 palette unification (Prompt A below) → the battle-mode-parity arc (untracked design doc; its pacing A/B assumed E47 fully landed — it is HALF-landed) → E48 → E49 (design session first) → the Shiloh/Gettysburg flips complete E47 and the Piper guns complete E46.** The one remaining untracked docs/design file (battle-mode-parity-design.md) is EXPECTED until its arc's M1. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-04 **D243: E46 ANTIETAM ACCURATE-INPUTS SHIPPED — the erased ~2:1 Union weight restored; the queue moves to E13+E41 save hardening.** The approved E46 milestone landed as one data-only commit (+ the dossier `docs/design/e46-antietam-research.md` now tracked): Meade's missing division added, XII Corps/French raised to engaged strength (Williams 4,600/Greene 2,500 per the in-session citation workflow), the Sunken Road lane de-double-counted (Rodes relabel, id stable), **Toombs's self-admitted 1,100 bottleneck proxy REMOVED** (real 450; the bluff cover + hold timer carry the bottleneck — A/B-proven, bridge still holds CS 8/8), A.P. Hill cut to his OR's "not over 2,000" (1,650/350), ANV majority-rifled (75.8%, all weapon tags Inferred via `_weaponsNote`), and two documented CS batteries added (Pelham's 15 Nicodemus guns — **Maj.** Pelham, panel-caught rank — and Jones's 10 ridge guns). **A/B both seed sets: agg CS 8/8 unchanged (the historical field-hold the endNote narrates as the tactical draw), atk/def 0.57/0.58 → 0.81/0.82 toward the historical 1.20, fielded 1.32:1 → 1.67:1 US, all phase directions historical, 8 other battles byte-identical, probe-antietam 16/16 with NO assertion touched, registry re-pinned 630→639.** ONE honest divergence (D242 pattern, recorded in the data's `_e46Note`): the documented Piper-farm guns invert the lane's documented fall (diagnosed as the E49 mass-capture gap — genuine timeout stall, NOT the E48 buzzer class) — **completion target for the approved E48/E49 arc** (with them the ratio hits 1.06). Structural answer to the E46 ledger question: the T8 draw band CANNOT fire on three decisive phases (header annotated); E48 is what could make phase draws reachable — re-A/B Antietam when it lands. **NEXT: E13+E41 save hardening (one milestone: base applySave hasOwnProperty guard via a guarded override module + a structural _SAVE_VER/additive-shape gate in build.mjs) → S25 palette → the parity arc → E48 → E49 → the Shiloh/Gettysburg flips + the Piper guns complete E47/E46.** The one remaining untracked docs/design file (battle-mode-parity-design.md) is EXPECTED until its arc's M1. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-04 **D240-D242: the E47 rout-direction arc is HALF-LANDED by design.** The guarded per-scenario `homeEdge` seam shipped (byte-identical when undeclared — no-flip sweeps proved it on both seed sets). **Malvern Hill (D240, 9b9b6c3):** US 8/8, atk/def 2.90/3.38→1.45/1.34 (artifact-inflated ratio corrected; 3-lens Opus panel SAFE_TO_COMMIT). **Chancellorsville (D241, 6ef914d):** CS 7/8→4/4 honest split, atk/def 0.32→0.84-0.86 vs the historical 0.77; endNote honesty fix; **NEW proposal E48** — the unanchored 480s house clock truncates the CS victory mid-fall (CS ON the crossroads with 12-13k men at the buzzer on every US-win seed). **Shiloh + Gettysburg (D242, docs-only):** flips built, RED on their historical direction gates (Shiloh CS 2/8 — the CS stalls at ~6% loss while the US holds the Hornets' Nest corridor that historically FELL; Gettysburg Day 1 inverts to US 8/8), root-caused — the artifact had been simulating the missing mass-surrender (Prentiss ~2,200; Gettysburg Day-1 ~3,600 prisoners) + straggler-collapse mechanisms — and REVERTED (never push red / never weaken a direction gate). **NEW proposal E49** (universal envelopment-surrender + straggler-shedding; recommendation: design session, then finish the arc). **NEXT: E48 + E49 are now BOTH APPROVED (Aaron, 2026-07-04 popup: buzzer-resolution rule + both surrender/straggler mechanics via design session) — they enter the queue as gated milestones (ordering Aaron's; default after the D237 queue) and then the Shiloh/Gettysburg flips complete the E47 arc. The approved queue continues at E46 Antietam draw (dossier pre-staged, untracked `docs/design/e46-antietam-research.md`) → E13+E41 save hardening → S25 palette → the battle-mode-parity arc (untracked `docs/design/battle-mode-parity-design.md`; NOTE: its pacing A/B assumed E47 fully landed — it is half-landed, Shiloh/Gettysburg still carry the artifact).** Two untracked docs/design files are EXPECTED in `git status` until their milestones commit them. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-04 **D238: FABLE AUDIT RUN 1 CLOSED — the full `npm run vet:noreg` batch checkpoint is GREEN (109/109 entries, two contiguous segments, E15-fresh artifacts, 0 pe/0 realErrors).** The night's one red stop was the harness SIGTERM-ing a GREEN probe-atmospherics (20/20, ok=true) 2.4s over its 360s budget — fixed with the 600s slow-Mac budget (D232 class, no assertion touched), re-run green. Segment logs: `.tmp/vet-no-regression-2026-07-03T22-57-57-256Z.log` (1-92) + `.tmp/vet-no-regression-2026-07-04T01-07-02-101Z.log` (93-109, "VET NO-REGRESSION OK — 17 commands"). **In progress THIS session: D239 = Soldier's Story #16 (John O. Casler, `ss:chancellorsville:CS:cs_colston_div:pvt`) + C64 Stannard split (650/1,140 per tablet strengths) + C65 (new C33-class ld_berry→us_berry_div attach fix), with both-seed-set A/Bs + probe re-vets + scaled Opus review.** Queue after D239 (Aaron-locked, D237/D238): **E47 rout-fix → E46 Antietam draw (dossier pre-staged, untracked `docs/design/e46-antietam-research.md`) → E13+E41 save hardening → S25 palette → battle-mode-parity arc** (design law + queue patches in untracked `docs/design/battle-mode-parity-design.md`; its M1 commits that doc; its pacing A/B REQUIRES E47 landed; pull-forward = Aaron-only, M1 alone safe). All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-03 **D237 (partial close-out): batch-gate harness repaired · Aaron's four popup decisions locked · THE GAME IS LIVE at https://adhanke-star.github.io/History-Video-Game/.** The run-ending full `npm run vet:noreg` battery was started; its first-ever exercise of the D230 E15 freshness law caught and root-caused three latent harness defects (five artifact-less gates now write ok-mirroring artifacts — 3 import gates + probe-portraits with new teeth + diag-classic contract-mirroring; probe-real-diplomacy made decoration-tolerant after C18 with the same required strings; probe-loot-survival's registry count corrected 603→627 for M10's nine documented formations). Aaron called the D171 boundary mid-battery: **the ONE open item is a clean end-to-end `npm run vet:noreg` — next session's FIRST task (Prompt A below).** Aaron's decisions: E47 APPROVED all 4 battles · next work = Soldier's Story + C64 · E46/E13+E41/S25 approved (E33 parked; E42/E43/E45 design-session) · X02 deleted · publish EXECUTED. HEAD after D237 commit. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M11 SHIPPED (D236): docs coherence + the two bounded tail fixes (S16/S35 · S17 · S18 · S19 · S20 · S36 · S38 · E01 · E02 · X01 + orphan E35).** REVIEW-QUEUE.md is now purely the audit ledger (June history → `legacy/REVIEW-QUEUE-2026-06-runs-efgh.md`); START-HERE canonical/legacy lists + head labels + read-order reconciled (D222 = playable-content head, the audit run = active work); DEPLOY.md reframed to the live PRIVATE remote with public-web publishing as the open Aaron decision; X01 research packet at `docs/design/ui-redesign-research.md`; `.tmp` scratch references annotated (S18); V1-CHECKLIST items 3/5 stamped (S20); AUTONOMOUS-RUN §2 header fixed (S38); entrench-cover doc drift annotated to the shipped ×1.53/×1.76/×1.99 (E01); the attrition lever label surfaces the T13 realism coupling (E02, presentation-only); prisoner-exchange's returned-POW strength channel cumulatively capped ≤6.0/campaign with a non-vacuous probe tooth (E35 — the item no milestone had claimed). **Audit run 1: 143 of 144 FIX-NOW ledger lines closed (D225-D236); only C64 remains (verify-first, next audit slice); 10 proposals await Aaron. NEXT: the run's full `npm run vet:noreg` batch checkpoint (D176), then the run ends at a clean D171 boundary.** Ledger: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". HEAD after D236 commit. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M10 SHIPPED (D235): sim-affecting accurate-inputs corrections (C28 HIGH + C05-scale/C11/C30/C33/C43/C44/C47 + C37 ledger flip + new proposal E47).** The Gettysburg attacker/defender seating inversion is fixed across all three phases (US defender on the objective ridges at low z, CS attacker at high z), and the corrected seating exposed the under-modeled Union defense — the documented repulse formations (Hazard 24 guns, McGilvery 39, Hays, Hall/Harrow; Birney + Bigelow on Day 2, with CS Benning/Anderson at the same altitude) were added per D92 rather than tuning anything. Shiloh gains Ruggles's 53-gun grand battery (atSec 100, before the dusk assault), both armies go honestly smoothbore, Wallace's attach binds, and the Shiloh badge displays "Hold the Line" (D104 lever untouched). Chancellorsville CS infantry rifled. Full seeded A/B (both seed sets) logged in D235: Gettysburg agg US 8/8, **atk/def 1.04 → 1.22 (exactly the historical ratio, emergent)**, Day 1 an honest CS 6-7/8, Day-3 charge pays ~2.6:1; Shiloh CS 7/8; Chancellorsville CS 6-7/8; six untouched battles byte-identical. Full 5-lens Opus panel pre-commit: 6 findings all fixed/filed (code/probe lenses 0); pre-existing Stannard double-count filed as new **C64**. New teeth in all three battle probes; five probes green, 0 pe. NEW proposal **E47** (side-keyed fldHomeEdgeZ rout asymmetry + its anti-Lost-Cause teaching liability) filed for Aaron. **Audit run 1 stands at 131 of 144 FIX-NOW ledger items closed (D225-D235 = M1-M10; C64 newly filed); M11 + orphan E35 + 10 proposals remain. NEXT: M11 — docs coherence (S16/S35 REVIEW-QUEUE restructure · S17 S18 S19 S20 S36 S38 · E01 E02 · X01) + the never-slotted E35, then the run's full `npm run vet:noreg` batch checkpoint (D176).** Ledger + full M-plan: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". HEAD after D235 commit. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M9 SHIPPED (D234): save-manager guards (S31/S32/S33/S34).** Overwrite/load/import/delete all confirm (S32 covers EVERY lane — the base menu's Load-from-File routed through the hardened importSave wrapper and had bypassed the guard until the pre-commit Opus review caught it); raw-present-but-unreadable slots render a distinct "Incompatible save" state (Save disabled against clobber, Delete kept for a deliberate confirmed clear — a future _SAVE_VER bump explains itself instead of silently "emptying" every slot). Focused gate: node --check ×2 · build GATE OK · probe-save-slots 10/10 (new S31-S34 step drives decline AND accept paths + the menu-import guard) · adjacent probe-full-campaign ok · 0 pe · diff-check clean. **Audit run 1 stands at 122 of 143 FIX-NOW ledger items closed (D225-D234 = M1-M9); M10-M11 + 9 proposals remain. NEXT: M10 — sim-affecting accurate-inputs corrections (C05-scale C11 C28 C30 C33 C43 C44 C47), each with A/B seeds + battle-probe re-vet + logged numbers (D92/D74); then M11 docs coherence; then the run's full `npm run vet:noreg` batch checkpoint.** Ledger + full M-plan: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". HEAD after D234 commit. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M8 SHIPPED (D233): a11y + discoverability + teaching-UI staleness (18 ledger items).** S21(HIGH) builder table controls named · S22 focus restore · S23 announced validation · S24 dirty-draft confirms · S26 numeric/decimal keypads · S27 clamp warning · S12/S14/S15 H2 cutaway trap/reduceMotion/dyslexia · S07/S08 gesture+R discoverability in BOTH control references · C19 honest quick-start · S13/S28/S30 small a11y · C18 glossary widened to 16 teaching tabs · C20 two Verified codex systems entries (rail, medicine) · C21 honest tour tabs. Review workflow: 2 LOW confirmed (delete-slot disabled-focus target; numeric-vs-decimal keypad), both FIXED pre-commit. Harness root-causes: probe-codex/tutorial/help-overlay off the stalling 'load' wait; builder screenshot on the 120s slow-Mac budget (its earlier "2 step fails" were a STALE artifact from a screenshot-timeout crash); probe-glossary's hook step updated to the C18-corrected scoping. Focused gate: node --check ×17 · build GATE OK · 9 probes ok=true 0 pe with JSON readback (custom-battle-builder 14/14 · help-overlay 10/10 · h2-cutaways 10/10 · codex 24/24 · tutorial 15/15 · presets 26/26 · h0-president-desk 3/3 · h0-battle-briefing 3/3 · glossary 12/12) · diff-check clean. **Audit run 1 stands at 118 of 143 FIX-NOW ledger items closed (D225-D233 = M1-M8); M9-M11 + 9 proposals remain. NEXT: M9 — save-manager guards (S31 S32 S33 S34).** Ledger + full M-plan: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". HEAD after D233 commit. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M7 SHIPPED (D232): H0/UI visual polish (9 ledger items).** S00 briefing "&amp;" (plain data + escape-at-sink) · S01 grade-letter CSS-specificity fix · S02 army panel spans the interstitial grid · S03+S11 menu accent tokens aligned to the shared six-shell palette (+3 rgba stragglers, one review-caught) · S04 portrait 2D field band centered between top bar and bottom chrome (phone void gone) · S05 menu action grid absorbs the no-save gap · S09 phone-only Unit-panel minimize toggle (aria-pressed, mirrors the legend collapse) · S29 desk year shown once. Plus root-cause harness fix: probe-h0-main-menu was RED at D231 HEAD (pre-existing slow-Mac 75s-budget flake, different scenes run-to-run) — ported the desk probe's SLOW_MAC profile, assertions unchanged. Focused gate: node --check ×14 · build GATE OK · probes h0-main-menu 4/4 · h0-tactical-hud 3/3 · h0-battle-briefing 3/3 · h0-after-action 3/3 · h0-between-battle 3/3 · h0-president-desk 3/3 · field · order-feel 17/17, all ok=true 0 pe via JSON readback with unpiped exit codes · phone screenshot verified · diff-check clean. Each fix carries a new probe tooth. **Audit run 1 stands at 100 of 143 FIX-NOW ledger items closed (D225-D232 = M1-M7); M8-M11 + 9 proposals remain. NEXT: M8 — a11y + discoverability + teaching-UI staleness (S07 S08 S12 S13 S14 S15 S21-S28 S30 + C18 C19 C20 C21).** Ledger + full M-plan: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". HEAD after D232 commit. All locks stand (Phase H parked D214; M8-battle-build/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M6 SHIPPED (D231): tactical control / perf / small-code (14 ledger items).** The H5-i4 charge lock gained its 3 missing releases (E03 river stall · E23 interposer contact · E34 permanent through rout→rally); S10 holding lines pivot in place (drag-only handle; tap still nudges — the review-workflow-confirmed dead-zone was FIXED via tap-fallthrough); E05/E06 conditioning honesty (collapsed-war 0 keeps the 0.88 floor; catch warns); E19/E20/E21/E18 render perf (figure-layer reclaim on rebuild · gated instanceColor uploads · ~24 Hz sway · hoisted fade closure); E44 '?' editable-target guard; S06 single pause indicator (sr-only aria-live lane kept); E04/E32 honest comments. All player-only-gated — AI-vs-AI byte-identity proven (presets committed-CS-5/8 guard · phased-ab · order-feel invariants). Focused gate: node --check ×10 · build GATE OK · 10 probes ok=true 0 pe (order-feel 17/17 · conditioning 7/7 · formation-figures 19/19 · render-richness · help-overlay 8/8 · field · presets · h0-tactical-hud 3/3 · phased-ab) · diff-check clean. **Audit run 1 stands at 91 of 143 FIX-NOW ledger items closed (D225-D231 = M1-M6); M7-M11 + 9 proposals remain. NEXT: M7 — H0/UI visual polish (S00 S01 S02 S03+S11 S04 S05 S09 S29).** Ledger + full M-plan: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". HEAD after D231 commit. All locks stand (Phase H parked D214; M8/Q5/Q6 locked; Phase D deferred).

### ▶ MODEL — Claude Opus 4.8 is the permanent main loop (D286, 2026-07-06)
The Fable-5 experiment (D223) ended: **Claude Fable 5 left Aaron's subscription, so the main loop is Claude Opus 4.8 (`claude-opus-4-8` / `opus[1m]`) + xhigh again — permanently.** No model-trigger check is needed anymore (`/model opus[1m]` is set + persisted). Operating law: **`OPUS-PLAYBOOK.md`** (behavior deltas + the standard run-prompt snippets). The live paste-ready continuation prompt is the **⚡ CONTINUE HERE** block at the top of this file (the newest milestone's report ends with the next-chat prompt). *(The former Fable-vs-Opus "MODEL TRIGGER / Prompt A / Prompt C" scaffolding that lived here is retired — its trigger condition fired, and the D278-era Prompt A it held was already stale by D285.)*

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M5 SHIPPED (D230): probe teeth (E37 + 5) · run paused clean, resumed with M6.** 43 exit-less enrolled probes gained a `beforeExit` teeth epilogue (nonzero exit unless a fresh ok/0-fail/0-pe artifact); `vet-no-regression` enforces artifact existence + freshness (E15); `probe-cab-exploit` asserts its exploit invariants (E08 — caught a stale navy-heed claim, corrected honestly); `probe-conditioning` enrolled + exit code (E09); `probe-motion` writes an artifact + exits nonzero (E10); new `probe-help-overlay` enrolled (E24). Focused gate: node --check, build GATE OK, probes cab-exploit 8/8 / conditioning / help-overlay / tutorial ok=true 0 pe, diff-check clean.

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M4 SHIPPED (D229): 10 build-gate blind spots closed.** 4d wall extended (both engines' strength/morale/ammo + alias-resistant lever names; T15 aggregate renamed menTotal); 4e source dedup; manifest-completeness gate; script-terminator gate; async/generator collision regex; hex-case/tmp/mask fixes; manifest comment honest. Verified: wall fires on 6 violation forms, passes 5 legit forms; GATE OK on live tree; probe-oob ok. **NEXT:** ledger M5 — probe teeth: E37 add failing exit codes to the 43 enrolled probes that lack them · E15 vet-no-regression requires fresh (post-run-start) ok=true artifacts · E08 probe-cab-exploit converts recorded metrics to thrown assertions · E09 enroll probe-conditioning in the SUITE · E10 probe-motion writes an artifact + exits nonzero · E24 new probe-help-overlay + enroll. Ledger: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". Locks stand.

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M3 SHIPPED (D228): stored-XSS HIGH closed + 8 tamper hardenings.** E38 fixed at both layers (T11 [<>] strip + T0 htmlEsc sink); E11/E12/E22/E25/E26/E27/E36/E39 all landed. Focused gate: node --check ×7 · build GATE OK · probes custom-battle-builder/save-slots/presets(byte-identity)/loot-survival/camp/field/tripo ok=true 0 pe · diff-check clean. **NEXT:** ledger M4 — build-gate hardening in tools/build.mjs (+manifest comment E07): E00 add .strength/.maxStr/.morale/.maxMor/.ammo to the 4d no-fudge wall · E14 4e dedup+content-validate sources · E16 hex-bomb case · E17 unique tmp path · E28 collision regex async/generator · E29 manifest-completeness gate vs src/ on disk · E30 sev alias guard · E31 data-file data:-URL mask parity · E40 script-terminator scan on injected content. Then M5 probe teeth (E37 43 probes + E08/E09/E10/E15/E24). Ledger: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". Locks stand.

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M2b SHIPPED (D227): 10 src caption/teaching corrections.** Imagery captions (Jackson Apr 26 · Sherman 1864-65 · Tubman 700+ · Sudley Church · honest Antietam credit · Chancellorsville plate honesty · Fredericksburg provenance), T1 Early-not-rail-borne, 83-endings McClellan-repudiated-the-plank. Focused gate: node --check ×5 · build GATE OK · 5 probes ok=true 0 pe · diff-check clean. **NEXT:** ledger M3 — tamper/XSS hardening: E38 HIGH (T11 custom-battle name/commander/teaching fields must reject or escape HTML before the T0:1487 innerHTML sink), plus E11 _slEsc quotes, E12 T11 hasOwnProperty key, E22 preset cushion clamp on the apply path, E25 unit-count caps, E26 marker-path clamp, E27 _lootAttr, E36 camp fatigue clamp, E39 tripo BAD_KEYS. Ledger: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". Locks stand.

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M2a SHIPPED (D226): 37 content text corrections across 15 data files.** All D74-inert; cabinet AG timeline made continuous (Benjamin/Keyes/Watts); two dishonest Verified stamps downgraded to Inferred; probe-loot-survival caught and forced the caveat-formula fix on the Howe note (probe kept, data fixed). Focused gate: build GATE OK · importer 15/15 · 8 probes ok=true 0 pe · diff-check clean. **NEXT:** ledger M2b — src-side caption/teaching strings (C16 C17 C22 C38 C39 C49 C54 C55 C56 C57), then M3 tamper/XSS hardening (E38 HIGH, T11+T0 sink). Sim-affecting items parked in M10 (C05-scale, C11, C28, C30, C33, C43, C44, C47). Ledger: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". Locks stand.

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT M1 SHIPPED (D225): 5 HIGH historical corrections.** C32/C41/C42/C46/C48 fixed text-only (D74-inert; ids stable; probe-vicksburg assertions track the corrected names at identical strength). C28 verified positional → M10 (sim-affecting, A/B + baseline re-vet). Focused gate: build GATE OK · vicksburg 18/18 · chancellorsville 24/24 · gettysburg/chickamauga/cabinet ok=true · 0 pageerrors · diff-check clean; full vet:noreg deferred to the run's batch checkpoint (D176). **NEXT:** ledger M2 — battle-data/codex/strategic-data text corrections (~50 MED/LOW items), then M3 tamper/XSS hardening (E38 HIGH). Ledger + plan: `REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)". All locks stand (Phase H parked D214; M8/Q5/Q6 locked; Phase D deferred).

## ⚡ PREVIOUS — 2026-07-03 **FABLE AUDIT RUN 1 LEDGER SHIPPED (D224, docs-only).** The first `FABLE-AUDIT-PROMPT.md` execution audited every quality dimension at HEAD `6d8b098` (3 workflows, 304 agents, default-refute verify): **150 confirmed findings (10 HIGH / 71 MED / 69 LOW), 43 refuted.** The standing ledger + ranked FIX-NOW milestone plan (M1–M11) live in **`REVIEW-QUEUE.md` §"FABLE AUDIT — 2026-07-03 (run 1)"** — that section is the memory between audit runs; every fix updates its line to fixed-in-D###. **NEXT:** work the plan in order — M1 HIGH historical corrections under Verified stamps (D225: C41/C42/C46/C48/C32, C28 verify-first) → M2 content text corrections → M3 tamper/XSS hardening (E38 HIGH) → M4 build-gate hardening → M5 probe teeth → M6–M11. PROPOSALS in the ledger await Aaron (none built). D222 Webb remains the latest playable change; Priority 1 Soldier's Story scale-out resumes after the audit queue or on Aaron's redirect. Phase H stays parked (D214); M8/Q5/Q6 battle-builds stay locked; Phase D deferred.

## ⚡ PREVIOUS — 2026-07-03 **PHASE I SOLDIER'S STORY ALEXANDER S. WEBB SLICE SHIPPED (D222).** D222 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. Alexander S. Webb now replaces the generated Gettysburg / Philadelphia Brigade command slot (`ss:gettysburg:US:us_phila_bde:cmd`) as the fifteenth `Verified` Soldier's Story replacement. His record keeps Brig. Gen. rank, Philadelphia Brigade / Second Division / II Corps / Army of the Potomac context, and the Angle / Pickett's Charge command story source-honest; it asserts no company command, no major-general-at-Gettysburg rank, no portrait asset, and neutral inferred ratings. Phase H remains parked: do not restart the automatic zero-byte polish loop, and do not reopen real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. **NEXT:** start Priority 1, another bounded citation-grade Phase I Soldier's Story named-person/story slice. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D222):** `data/soldier-replacements.json` now has fifteen Verified replacement records; Webb carries no portrait claim, no company-command claim, no major-general-at-Gettysburg rank claim, neutral inferred ratings, and a source trail using the NPS Union commander profile, the NPS Army of the Potomac order of battle, and the CMOHS Webb profile. `tools/probe-loot-survival.mjs` now locks the fifteen-record overlay, alias lookup from the old generated `ss:` row, Webb's rank/unit/team/source-note caveats, the UI detail/source rendering, no unsupported portrait, and the hostile-pack rejection path.
>
> **VET (focused, D176):** `node tools/import-soldier-replacements.mjs --check data/soldier-replacements.json` passed **records=15 verified=15 disputed=0**; `node --check` passed for `src/37-loot-survival.js`, `tools/import-soldier-replacements.mjs`, and `tools/probe-loot-survival.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning and rebuilt `civil_war_generals.html` from source; focused `probe-loot-survival` wrote an `ok=true` artifact with **11/11, 0 pageerrors**; adjacent `probe-women-in-war` **8/8**, `probe-save-slots` **9/9**, and `probe-bridge` **6/6**, all with zero pageerrors. JSON readback found all four artifacts `ok=true` with no failed steps, realErrors, texture warnings, or page errors. Full `npm run vet:noreg` was not run under D176 focused-slice batching. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-03 **PHASE I SOLDIER'S STORY SULLIVAN BALLOU SLICE SHIPPED (D221).** D221 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. Sullivan Ballou now replaces the generated First Bull Run / Burnside's Brigade command slot (`ss:bullrun1:US:us_burnside:cmd`) as the fourteenth `Verified` Soldier's Story replacement. His record keeps Major rank, 2nd Rhode Island Infantry, Burnside's Brigade / Hunter's Second Division / Army of Northeastern Virginia context, and the Sarah-letter / Matthews Hill wounding story source-honest; it asserts no company command, no autograph-original claim, no portrait asset, and neutral inferred ratings. Phase H remains parked: do not restart the automatic zero-byte polish loop, and do not reopen real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. **NEXT:** superseded by D222; continue Priority 1 Soldier's Story scale-out. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D221):** `data/soldier-replacements.json` reached fourteen Verified replacement records; Ballou carries no portrait claim, no company-command claim, no autograph-original letter claim, neutral inferred ratings, and a source trail using the NPS Ballou article, Rhode Island Historical Society Ballou Papers, and the NPS Union order of battle.
>
> **VET (focused, D176):** importer/schema, `node --check`, build, focused `probe-loot-survival`, adjacent `probe-women-in-war`, `probe-save-slots`, `probe-bridge`, JSON readback, and `git diff --check` passed; full `npm run vet:noreg` was deferred under D176 focused-slice batching.

## ⚡ PREVIOUS — 2026-07-03 **PHASE I SOLDIER'S STORY JOHN H. WORSHAM SLICE SHIPPED (D220).** D220 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. John H. Worsham now replaces the generated Antietam / Jackson's Division private slot (`ss:antietam:CS:cs_jr_jones:pvt`) as the thirteenth `Verified` Soldier's Story replacement. His record keeps Private rank, Company F / 21st Virginia Infantry, Jones' Brigade / Jackson's Division / Jackson's Command context, and a Maryland Campaign soldier-life story source-honest; it asserts no later sergeant/adjutant rank, no First Manassas claim, no portrait asset, and neutral inferred ratings. Phase H remains parked: do not restart the automatic zero-byte polish loop, and do not reopen real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. **NEXT:** superseded by D221; continue Priority 1 Soldier's Story scale-out. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D220):** `data/soldier-replacements.json` now has thirteen Verified replacement records; Worsham carries no portrait claim, no later sergeant/adjutant rank claim, no First Manassas claim, neutral inferred ratings, and a source trail using NPS Worsham, Antietam on the Web Worsham/Jones' Brigade pages, Worsham's DocSouth memoir, and the U.S. Army CMH Antietam order of battle. `tools/probe-loot-survival.mjs` now locks the thirteen-record overlay, alias lookup from the old generated `ss:` row, Worsham's rank/unit/team/source-note caveats, the UI detail/source rendering, no unsupported portrait, and the hostile-pack rejection path.
>
> **VET (focused, D176):** `node tools/import-soldier-replacements.mjs --check data/soldier-replacements.json` passed **records=13 verified=13 disputed=0**; `node --check` passed for `src/37-loot-survival.js`, `tools/import-soldier-replacements.mjs`, and `tools/probe-loot-survival.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning and rebuilt `civil_war_generals.html` from source; focused `probe-loot-survival` wrote an `ok=true` artifact with **11/11, 0 pageerrors**; adjacent `probe-women-in-war` **8/8**, `probe-save-slots` **9/9**, and `probe-bridge` **6/6**, all with zero pageerrors. JSON readback found all four artifacts `ok=true` with no failed steps, realErrors, or page errors. Full `npm run vet:noreg` was not run under D176 focused-slice batching. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-03 **PHASE I SOLDIER'S STORY FRANCIS C. BARLOW SLICE SHIPPED (D219).** D219 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. Francis C. Barlow now replaces the generated Antietam / Caldwell's Brigade command slot (`ss:antietam:US:us_barlow:cmd`) as the twelfth `Verified` Soldier's Story replacement. His record keeps Colonel rank, 61st and 64th New York Infantry, Caldwell's Brigade / First Division / II Corps context, and the Sunken Road enfilading-fire story source-honest; it asserts no brigadier-rank-at-Antietam, no single-company claim, no portrait asset, and neutral inferred ratings. Phase H remains parked: do not restart the automatic zero-byte polish loop, and do not reopen real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. **NEXT:** superseded by D220; continue Priority 1 Soldier's Story scale-out. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D219):** `data/soldier-replacements.json` now has twelve Verified replacement records; Barlow carries no portrait claim, no brigadier-rank-at-Antietam claim, no single-company claim, neutral inferred ratings, and a source trail using NPS Barlow/OOB pages, Barlow's transcribed Official Records report, and the Antietam on the Web officer profile. `tools/probe-loot-survival.mjs` now locks the twelve-record overlay, alias lookup from the old generated `ss:` row, Barlow's rank/unit/team/source-note caveats, the UI detail/source rendering, no unsupported portrait, and the hostile-pack rejection path.
>
> **VET (focused, D176):** `node tools/import-soldier-replacements.mjs --check data/soldier-replacements.json` passed **records=12 verified=12 disputed=0**; `node --check` passed for `src/37-loot-survival.js`, `tools/import-soldier-replacements.mjs`, and `tools/probe-loot-survival.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning and rebuilt `civil_war_generals.html` from source; focused `probe-loot-survival` wrote an `ok=true` artifact with **11/11, 0 pageerrors**; adjacent `probe-women-in-war` **8/8**, `probe-save-slots` **9/9**, and `probe-bridge` **6/6**, all with zero pageerrors. JSON readback found all four artifacts `ok=true` with no failed steps, realErrors, or page errors. Full `npm run vet:noreg` was not run under D176 focused-slice batching. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-02 **PHASE I SOLDIER'S STORY SAMUEL N. BENJAMIN SLICE SHIPPED (D218).** D218 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. Samuel N. Benjamin now replaces the generated Antietam / IX Corps artillery command slot (`ss:antietam:US:us_benjamin:cmd`) as the eleventh `Verified` Soldier's Story replacement. His record keeps First Lieutenant rank, Battery E / 2nd U.S. Artillery, IX Corps / First Division artillery context, and the Stone Bridge / Sharpsburg battery-report story source-honest; it asserts no captain-rank-at-Antietam, no portrait asset, and neutral inferred ratings. Phase H remains parked: do not restart the automatic zero-byte polish loop, and do not reopen real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. **NEXT:** superseded by D219; continue Priority 1 Soldier's Story scale-out. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D218):** `data/soldier-replacements.json` now has eleven Verified replacement records; Benjamin carries no portrait claim, no captain-rank-at-Antietam claim, neutral inferred ratings, and a source trail using Army.mil and CMOHS Medal of Honor sources, NPS Antietam IX Corps OOB, and Benjamin's transcribed Antietam report. `tools/probe-loot-survival.mjs` now locks the eleven-record overlay, alias lookup from the old generated `ss:` row, Benjamin's rank/unit/team/source-note caveats, the UI detail/source rendering, no unsupported portrait, and the hostile-pack rejection path.
>
> **VET (focused, D176):** `node tools/import-soldier-replacements.mjs --check data/soldier-replacements.json` passed **records=11 verified=11 disputed=0**; `node --check` passed for `src/37-loot-survival.js`, `tools/import-soldier-replacements.mjs`, and `tools/probe-loot-survival.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning and rebuilt `civil_war_generals.html` from source; focused `probe-loot-survival` wrote an `ok=true` artifact with **11/11, 0 pageerrors**; adjacent `probe-women-in-war` **8/8**, `probe-save-slots` **9/9**, and `probe-bridge` **6/6**, all with zero pageerrors. JSON readback found all four artifacts `ok=true` with no failed steps, realErrors, or page errors. Full `npm run vet:noreg` was not run under D176 focused-slice batching. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-02 **PHASE I SOLDIER'S STORY FRANCIS A. WALLER SLICE SHIPPED (D217).** D217 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. Francis A. Waller, spelled Wallar in the NPS Gettysburg article, now replaces the generated Gettysburg / Iron Brigade NCO slot (`ss:gettysburg:US:us_iron_bde:nco`) as the tenth `Verified` Soldier's Story replacement. His record keeps Corporal rank, Company I / 6th Wisconsin Infantry, First Brigade (Iron Brigade), First Division, I Corps context, and the Railroad Cut / 2nd Mississippi flag-capture story source-honest; it asserts no higher rank at Gettysburg, no portrait asset, and neutral inferred ratings while noting the later memory caveat. Phase H remains parked: do not restart the automatic zero-byte polish loop, and do not reopen real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. **NEXT:** superseded by D218; continue Priority 1 Soldier's Story scale-out. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D217):** `data/soldier-replacements.json` now has ten Verified replacement records; Waller carries no portrait claim, no higher-rank claim, neutral inferred ratings, and a source trail using Army.mil Medal of Honor citations, an NPS Gettysburg article, and the NPS Gettysburg Army of the Potomac order of battle. `tools/probe-loot-survival.mjs` now locks the ten-record overlay, alias lookup from the old generated `ss:` row, Waller's rank/unit/team/source-note caveats, the UI detail/source rendering, no unsupported portrait, and the hostile-pack rejection path.
>
> **VET (focused, D176):** `node tools/import-soldier-replacements.mjs --check data/soldier-replacements.json` passed **records=10 verified=10 disputed=0**; `node --check` passed for `src/37-loot-survival.js`, `tools/import-soldier-replacements.mjs`, and `tools/probe-loot-survival.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning and rebuilt `civil_war_generals.html` from source; focused `probe-loot-survival` wrote an `ok=true` artifact with **11/11, 0 pageerrors**; adjacent `probe-women-in-war` **8/8**, `probe-save-slots` **9/9**, and `probe-bridge` **6/6**, all with zero pageerrors. JSON readback found all four artifacts `ok=true` with no failed steps or page errors. Full `npm run vet:noreg` was not run under D176 focused-slice batching. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-02 **PHASE I SOLDIER'S STORY ORION P. HOWE SLICE SHIPPED (D216).** D216 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. Orion P. Howe now replaces the generated Vicksburg / Blair's Division Stockade Redan private slot (`ss:vicksburg:US:us_blair_stockade:pvt`) as the ninth `Verified` Soldier's Story replacement. His record keeps Musician rank, Company C / 55th Illinois Infantry, XV Corps / Blair's Second Division context, and the wounded drummer/cartridge-message story source-honest; it asserts no higher rank at Vicksburg, no portrait asset, and neutral inferred ratings. Phase H remains parked: do not restart the automatic zero-byte polish loop, and do not reopen real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. **NEXT:** superseded by D217; continue Priority 1 Soldier's Story scale-out. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D216):** `data/soldier-replacements.json` now has nine Verified replacement records; Howe carries no portrait claim, no higher-rank claim, neutral inferred ratings, and a source trail using the Congressional Medal of Honor Society, Illinois State Archives, NPS Vicksburg, NPS Civil War Soldiers and Sailors, and NPS History. `tools/probe-loot-survival.mjs` now locks the nine-record overlay, alias lookup from the old generated `ss:` row, Howe's rank/unit/team/source-note caveats, the UI detail/source rendering, no unsupported portrait, and the hostile-pack rejection path.
>
> **VET (focused, D176):** `node tools/import-soldier-replacements.mjs` passed **records=9 verified=9 disputed=0**; `node --check tools/probe-loot-survival.mjs` passed; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning and rebuilt `civil_war_generals.html` from source; focused `probe-loot-survival` passed **11/11, 0 pageerrors**; adjacent `probe-women-in-war` **8/8**, `probe-save-slots` **9/9**, and `probe-bridge` **6/6**, all with zero pageerrors. JSON readback found all four artifacts `ok=true` with no failed steps or page errors. Full `npm run vet:noreg` was not run under D176 focused-slice batching. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-02 **PHASE I SOLDIER'S STORY JOHN COOK SLICE SHIPPED (D215).** D215 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. John Cook now replaces the generated Antietam / Battery B, 4th U.S. Artillery NCO slot (`ss:antietam:US:us_battery_b:nco`) as the eighth `Verified` Soldier's Story replacement. His record keeps Bugler rank, Battery B / 4th U.S. Artillery, I Corps / First Division context, and Antietam action source-honest; it asserts no higher rank, no portrait asset, and neutral inferred ratings. Phase H remains parked: do not restart the automatic zero-byte polish loop, and do not reopen real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. **NEXT:** superseded by D216; continue Priority 1 Soldier's Story scale-out. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D215):** `data/soldier-replacements.json` now has eight Verified replacement records; Cook carries no portrait claim, no higher-rank claim, neutral inferred ratings, and a source trail using the Congressional Medal of Honor Society, Department of War/DOD News, the NPS Antietam artillery brochure, and the U.S. Army Center of Military History Antietam order of battle. `tools/probe-loot-survival.mjs` now locks the eight-record overlay, alias lookup from the old generated `ss:` row, Cook's rank/unit/team/source-note caveats, the UI detail/source rendering, no unsupported portrait, and the hostile-pack rejection path.
>
> **VET (focused, D176):** `node tools/import-soldier-replacements.mjs` passed **records=8 verified=8 disputed=0**; `node --check tools/probe-loot-survival.mjs` passed; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning and rebuilt `civil_war_generals.html` from source; focused `probe-loot-survival` passed **11/11, 0 pageerrors** after the probe caught a source-note case/caveat mismatch; adjacent `probe-women-in-war` **8/8**, `probe-save-slots` **9/9**, and `probe-bridge` **6/6**, all with zero pageerrors. JSON readback found all four artifacts `ok=true` with no failed steps or page errors. Full `npm run vet:noreg` was not run under D176 focused-slice batching. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-02 **PHASE H PARKED; PHASE I SOLDIER'S STORY STILLWELL SLICE SHIPPED (D214).** D214 closes out Group 5 Phase H as complete-enough after the D193-D213 guard stack and resumes Group 1 Phase I Soldier's Story scale-out. The short closeout audit says Phase H can park now: media-budget and optional-pack locks are executable, the Intel UHD-617 profile is green, H0/H2/audio/flags/readability seams are probed, and the terrain/marker/atmospheric 3D resource polish stack has covered the obvious zero-byte wins. Remaining visible Phase H work mostly needs an explicit media-budget/profile decision before real PD/surviving-colours assets, H2 footage, HDRI/model media, or optional-pack work. D214 adds Leander Stillwell as the seventh sourced replacement, replacing `ss:shiloh:US:us_prentiss:nco` with a Verified 61st Illinois / Company D enlisted-man story from Shiloh. **NEXT:** superseded by D215; continue Priority 1 Soldier's Story scale-out. Do not restart the automatic Phase H zero-byte loop. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D214):** `data/soldier-replacements.json` now has seven Verified replacement records; Stillwell carries no portrait claim, no higher-rank claim, neutral inferred ratings, and a source trail using Stillwell's memoir, NPS 61st Illinois battle-unit participation, ILGenWeb Company D roster lines, and a Kansas biographical sketch. `tools/probe-loot-survival.mjs` now locks the seven-record overlay, alias lookup from the old generated `ss:` row, Stillwell's rank/unit/team/source-note caveats, and the hostile-pack rejection path.
>
> **VET (focused, D176):** `node tools/import-soldier-replacements.mjs` passed **records=7 verified=7 disputed=0**; `node --check tools/probe-loot-survival.mjs` passed; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning and rebuilt `civil_war_generals.html` from source; focused `probe-loot-survival` passed **11/11, 0 pageerrors** after tightening the Stillwell no-portrait caveat; adjacent `probe-women-in-war` **8/8**, `probe-save-slots` **9/9**, and `probe-bridge` **6/6**, all with zero pageerrors. Full `npm run vet:noreg` was not run under D176 focused-slice batching. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-02 **GROUP 5 · PHASE H LOW-TIER MARKER BODY LAYER PERF POLISH SHIPPED (D213).** Group 5 continued without granting M8 battle-build approval. D213 was another zero-new-media render/resource slice under the D193-D213 media-budget/profile/readability/perf guard stack: low-quality 3D markers still keep slab/front/flag/pole readability, but the slab/front body now renders through shared scene-level `markerBodySlabLayer` and `markerBodyFrontLayer` `InstancedMesh` layers instead of one slab mesh plus one front-strip mesh per unit. T23 clears shared body slots when local GLB fixtures hide the base marker; T24 no longer revives stale child slab/front meshes on low-tier fallback restore; T18 casualty fade still materializes a temporary fallback body only when opacity needs it. `probe-intel-uhd617-profile` records/asserts the shared low-tier body layer; post-D213 profile reads high 6.30ms / 75 calls / 116 objects and low 1.62ms / 63 calls / 97 objects. **NEXT:** superseded by D214; Phase H is now parked unless Aaron explicitly reopens media/profile work. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D213):** `src/tactical/T0-field-sandbox.js` adds the shared low-tier marker-body layer, body slots, inactive parking, and rebuild/dispose cleanup. `src/tactical/T23-tripo-unit-assets.js` coordinates local GLB base-marker hiding/restoration with the body layer. `src/tactical/T24-formation-figures.js` keeps low-tier fallback restore from showing stale child slab/front meshes. `tools/probe-intel-uhd617-profile.mjs`, `tools/probe-formation-figures.mjs`, `tools/probe-tripo-unit-assets.mjs`, and `tools/probe-visual-fidelity.mjs` lock the shared-body contract.
>
> **VET (focused, D176):** `node --check` passed for touched source/probe files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-intel-uhd617-profile` **19/19** (high 6.30ms / 75 calls / 116 objects; low 1.62ms / 63 calls / 97 objects); adjacent `probe-formation-figures` **17/17**, `probe-tripo-unit-assets` **15/15**, `probe-visual-fidelity` **27/27**, `probe-render-richness` **31/31**, and `probe-media-budget` **6/6**, all with zero pageerrors and no texture warnings where applicable. Key-aware JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `git diff --check` passed.

## ⚡ PREVIOUS — 2026-07-02 **GROUP 5 · PHASE H HIGH-TIER MARKER BODY RESOURCE POLISH SHIPPED (D212).** Group 5 continues without granting M8 battle-build approval. D212 is another zero-new-media render/resource slice under the D193-D212 media-budget/profile/readability/perf guard stack: default high-tier infantry formation figures no longer keep hidden slab/front marker-body meshes/materials resident after T24 owns the visible marker read. T0 lazy-creates slab/front bodies for off/low/GLB/fade fallback paths; T21 reapplies rank maps to restored fallback slabs; T18 materializes a fallback body only for active casualty fade. `probe-intel-uhd617-profile` now records/asserts high-tier body residency; post-D212 profile reads high 8.58ms / 75 calls / 116 objects and low 1.98ms / 77 calls / 111 objects. **NEXT:** superseded by D213.

> **WHAT SHIPPED (D212):** `src/tactical/T0-field-sandbox.js` adds `fld3dAddMarkerBody()` / `fld3dNeedsMarkerBody()`; `src/tactical/T21-visual-fidelity.js` reapplies rank maps to lazily restored slabs; `src/tactical/T18-render-richness.js` ensures casualty fade has a temporary body cue without returning hidden default marker residents. `tools/probe-formation-figures.mjs`, `tools/probe-intel-uhd617-profile.mjs`, and `tools/probe-visual-fidelity.mjs` lock no-hidden-body default plus fallback restoration.
>
> **VET (focused, D176):** `node --check` passed for touched source/probe files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-formation-figures` **17/17**; `probe-intel-uhd617-profile` **19/19** (high 8.58ms / 75 calls / 116 objects; low 1.98ms / 77 calls / 111 objects); adjacent `probe-render-richness` **31/31** after catching and fixing the fade-body regression, `probe-visual-fidelity` **27/27**, `probe-tripo-unit-assets` **15/15**, and `probe-media-budget` **6/6**, all with zero pageerrors and no texture warnings where applicable. Key-aware JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings; relevant screenshots were spot-checked. `git diff --check` passed.

## ⚡ CONTINUE HERE — 2026-07-02 **GROUP 5 · PHASE H LOW-TIER MARKER POLE LAYER PERF POLISH SHIPPED (D211).** Group 5 continues without granting M8 battle-build approval. D211 is another zero-new-media render/resource slice under the D193-D211 media-budget/profile/readability/perf guard stack: low-quality 3D unit markers keep slab/front/flag/pole readability, but the pole cue now renders through one shared scene-level `markerPoleLayer` `InstancedMesh` instead of one pole mesh per unit. High-tier formation-figure infantry still omits hidden poles/toppers, and high/off fallback markers still restore normal per-unit pole/topper cues. `probe-intel-uhd617-profile` now records/asserts the shared low-tier pole layer; post-D211 profile reads high 8.83ms / 75 calls / 128 objects and low 2.65ms / 77 calls / 111 objects. **NEXT:** continue Group 5 Phase H with another bounded zero-byte render/readability/perf slice while the raw embed tier remains above the soft warning, or surface the explicit media-budget/profile decision before any real PD/surviving-colours/H2/HDRI/model media work. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D211):** `src/tactical/T0-field-sandbox.js` adds the shared low-tier `markerPoleLayer`, parks stale instances on quality changes, and keeps high/off fallback per-unit pole creation. `tools/probe-formation-figures.mjs` locks the low-tier shared pole cue. `tools/probe-intel-uhd617-profile.mjs` records `poleLayer` while preserving no-idle-ring/no-hidden-topper/no-hidden-high-pole contracts.
>
> **VET (focused, D176):** `node --check` passed for touched source/probe files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-formation-figures` **17/17**; `probe-intel-uhd617-profile` **19/19** (high 8.83ms / 75 calls / 128 objects; low 2.65ms / 77 calls / 111 objects); adjacent `probe-render-richness` **31/31**, `probe-visual-fidelity` **27/27**, `probe-tripo-unit-assets` **15/15**, and `probe-media-budget` **6/6**, all with zero pageerrors and no texture warnings where applicable. Key-aware JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings; the low-tier screenshot was spot-checked. `git diff --check` passed.

## ⚡ CONTINUE HERE — 2026-07-02 **GROUP 5 · PHASE H HIGH-TIER MARKER POLE RESOURCE POLISH SHIPPED (D210).** Group 5 continues without granting M8 battle-build approval. D210 is another zero-new-media resource slice under the D193-D210 media-budget/profile/readability/perf guard stack: default high-tier infantry formation figures no longer allocate hidden base-marker pole meshes/materials after T24 has replaced the slab/front/pole/topper read. Fallback marker paths still restore pole/topper readability, low tier still keeps slab/front/flag/pole, and non-infantry fallback markers keep their pole. `probe-intel-uhd617-profile` now records/asserts pole residency; post-D210 profile reads high 8.88ms / 75 calls / 128 objects and low 2.41ms / 84 calls / 118 objects. **NEXT:** continue Group 5 Phase H with another bounded zero-byte render/readability/perf slice while the raw embed tier remains above the soft warning, or surface the explicit media-budget/profile decision before any real PD/surviving-colours/H2/HDRI/model media work. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D210):** `src/tactical/T0-field-sandbox.js` adds `fld3dAddMarkerPole()` and `fld3dNeedsMarkerPole()` so default high-tier infantry skips hidden pole allocation while off/low fallback restores the pole. `tools/probe-formation-figures.mjs` locks high no-pole plus off/low restoration. `tools/probe-intel-uhd617-profile.mjs` records `poleCount` and preserves the no-idle-ring/no-hidden-topper contracts.
>
> **VET (focused, D176):** `node --check` passed for touched source/probe files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-formation-figures` **17/17**; `probe-intel-uhd617-profile` **19/19** (high 8.88ms / 75 calls / 128 objects; low 2.41ms / 84 calls / 118 objects); adjacent `probe-tripo-unit-assets` **15/15**, `probe-render-richness` **31/31**, `probe-visual-fidelity` **27/27**, and `probe-media-budget` **6/6**, all with zero pageerrors and no texture warnings where applicable. Key-aware JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings; formation and GLB fixture screenshots were spot-checked. `git diff --check` passed.

## ⚡ CONTINUE HERE — 2026-07-02 **GROUP 5 · PHASE H IDLE SELECTION-RING RESOURCE POLISH SHIPPED (D209).** Group 5 continues without granting M8 battle-build approval. D209 is another zero-new-media resource/performance slice guided by the D193-D208 media-budget/profile/readability/perf guards: default high/low 3D unit markers no longer allocate an idle selection-ring mesh/material until a unit is actually selected. T18 still restores the selected pulse and casualty-fade cue, and T23 still preserves the optional local-GLB ring cue when the base marker is hidden. `probe-intel-uhd617-profile` now records/asserts `ringCount:0` in default profile scenes; post-D209 profile reads high 8.37ms / 75 calls / 134 objects and low 2.03ms / 84 calls / 118 objects. **NEXT:** continue Group 5 Phase H with another bounded zero-byte render/readability/perf slice while the raw embed tier remains above the soft warning, or surface the explicit media-budget/profile decision before any real PD/surviving-colours/H2/HDRI/model media work. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D209):** `src/tactical/T0-field-sandbox.js` adds `fld3dEnsureSelectionRing()` and removes default idle ring allocation; `src/tactical/T18-render-richness.js` reacquires lazily-created rings so selected/fade behavior stays intact; `src/tactical/T23-tripo-unit-assets.js` asks for a ring when a local GLB hides the base marker. `tools/probe-render-richness.mjs` locks no-resident-or-hidden idle rings plus selected-ring visibility, and `tools/probe-intel-uhd617-profile.mjs` records the profile counts.
>
> **VET (focused, D176):** `node --check` passed for touched source/probe files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-render-richness` **31/31, 0 pageerrors, 0 texture warnings**; `probe-intel-uhd617-profile` **19/19** (high 8.37ms / 75 calls / 134 objects; low 2.03ms / 84 calls / 118 objects); adjacent `probe-tripo-unit-assets` **15/15**, `probe-visual-fidelity` **27/27**, `probe-formation-figures` **17/17**, `probe-media-budget` **6/6**, and `probe-presets` **26/26**, all with zero pageerrors and no texture warnings where applicable. Key-aware JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings; relevant screenshots were spot-checked. `git diff --check` passed.

## ⚡ CONTINUE HERE — 2026-07-02 **GROUP 5 · PHASE H HIGH-TIER MARKER TOPPER RESOURCE POLISH SHIPPED (D208).** Group 5 continues without granting M8 battle-build approval. D208 is another zero-new-media resource/performance slice guided by the D193-D207 media-budget/profile/readability/perf guards: default high-tier infantry formation figures no longer allocate hidden cube/cone marker toppers after T24 has replaced the slab/front/topper with the shared formation-figure layer. The fallback marker path still restores the non-color topper cue when formation figures are off, and low tier still omits the decorative topper while preserving slab/front/flag/pole readability. `probe-intel-uhd617-profile` now records/asserts the default high-tier no-hidden-infantry-topper contract; post-D208 profile reads high 8.33ms / 75 calls / 142 objects and low 2.03ms / 84 calls / 126 objects. **NEXT:** continue Group 5 Phase H with another bounded zero-byte render/readability/perf slice while the raw embed tier remains above the soft warning, or surface the explicit media-budget/profile decision before any real PD/surviving-colours/H2/HDRI/model media work. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D208):** `src/tactical/T0-field-sandbox.js` adds `fld3dNeedsMarkerTopper()` so default high-tier infantry markers skip hidden fallback topper allocation whenever T24 formation figures own the visible marker read. `tools/probe-formation-figures.mjs` locks the high-tier no-topper default plus `renderRich="off"` fallback-topper restoration. `tools/probe-intel-uhd617-profile.mjs` records the default-high no-hidden-topper contract and the new profile counts.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T0-field-sandbox.js`, `tools/probe-formation-figures.mjs`, and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-formation-figures` **17/17, 0 pageerrors**; `probe-intel-uhd617-profile` **19/19** (high 8.33ms / 75 calls / 142 objects; low 2.03ms / 84 calls / 126 objects); adjacent `probe-visual-fidelity` **27/27, 0 pageerrors, 0 texture warnings** and `probe-media-budget` **6/6** with the known soft warning only. Key-aware JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `git diff --check` passed.

## ⚡ CONTINUE HERE — 2026-07-02 **D207 PRIORITY LADDER REFRESHED; START PRIORITY 1 NEXT.** D207 is a docs/memory routing update on top of D206, not a gameplay milestone. No code, data, generated HTML, media, battle roster, combat model, save schema, or probe contract changed. The remaining queue is now prioritized explicitly: **Priority 1** continue Group 5 Phase H with one more bounded zero-byte render/readability/perf polish slice guided by `tools/shots/probe-intel-uhd617-profile.json`; **Priority 2** surface an explicit media-budget/profile decision before any real surviving-colours/PD asset, H2 footage, HDRI/model, or optional-pack work; **Priority 3** Group 6 meta/deferred tooling; **Priority 4** later Phase I Soldier's Story scale-out; **Priority 5** narrow GM/Transfer leftovers only after honest `theater` fields; **Priority 6** battle-build work last and only after Aaron go/no-go.

> **NEXT RUN:** pick a tightly bounded zero-byte Group 5 Phase H render/readability/perf polish slice from `tools/shots/probe-intel-uhd617-profile.json`. Set `TMPDIR="$PWD/.tmp"`, confirm full-access/non-seatbelt, run focused gates for the selected slice, inspect stdout and `tools/shots/*.json` for `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings, fix any real red at the root, run `git diff --check`, sync docs, commit, push, then stop with the next prompt.
>
> **LOCKS:** no heavy footage/HDRI/model media in core, no surviving-colours/PD asset work without the explicit media-budget/profile decision, no Tripo/actions/accounts/download dependencies without approval, no M8 battle-build without Aaron go/no-go, and Q5 Chattanooga/Atlanta-March/Franklin-Nashville/Q6 USCT playable battles remain LAST.

## ⚡ CONTINUE HERE — 2026-07-02 **GROUP 5 · PHASE H LOW-TIER MARKER TOPPER RESOURCE POLISH SHIPPED (D206).** Group 5 continues without granting M8 battle-build approval. D206 is another zero-new-media resource/performance slice guided by the D193-D205 media-budget/profile/readability/perf guards: low-quality 3D unit markers now skip allocation of the decorative white cube/cone topper that D205 already hid, while preserving slab/front/flag/pole readability. High tier still lazy-creates the non-color topper cue for the fallback marker path. `probe-intel-uhd617-profile` now records/asserts low-tier `topperCount:0` plus flag/pole preservation; post-D206 profile reads high 7.92ms / 75 calls / 148 objects and low 2.46ms / 84 calls / 126 objects. **NEXT:** continue Group 5 Phase H with another bounded polish/media/perf slice, preferably zero-byte render/readability/perf polish while the raw embed tier remains above the soft warning. Tightly budgeted PD/surviving-colours asset work should happen only after an explicit media-budget/profile decision. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D206):** `src/tactical/T0-field-sandbox.js` adds `fld3dAddMarkerTopper()` and lazy-creates topper geometry only when high-quality markers need the fallback cue; low-quality rebuilds create no `topper` child. `tools/probe-intel-uhd617-profile.mjs` records `topperCount` and asserts high tier keeps shared topper fallback resources while low tier has no resident topper allocation.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T0-field-sandbox.js` and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-intel-uhd617-profile` **19/19** (high 7.92ms / 75 calls / 148 objects; low 2.46ms / 84 calls / 126 objects); adjacent `probe-formation-figures` **17/17, 0 pageerrors**, `probe-visual-fidelity` **27/27, 0 pageerrors, 0 texture warnings**, and `probe-media-budget` **6/6** with the known soft warning only. Key-aware JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings; high/visual-fidelity screenshots were visually checked. `git diff --check` and `node tools/build.mjs --check` passed.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H LOW-TIER MARKER TOPPER PERF POLISH SHIPPED (D205).** Group 5 continued without granting M8 battle-build approval. D205 was another zero-new-media performance slice guided by the D193-D204 media-budget/profile/readability/perf guards: low-quality 3D unit markers hide the decorative white cube/cone topper while preserving slab/front/flag/pole side readability. High tier still uses shared instanced formation figures unchanged, and the T24 fallback wrapper reapplies the low-tier trim after restoring marker visibility. `probe-intel-uhd617-profile` records/asserts the topper trim plus flag/pole preservation; post-D205 profile read high 9.98ms / 75 calls / 148 objects and low 1.94ms / 84 calls / 134 objects. **THEN-NEXT, now shipped as D206:** low-tier topper resource polish. Tightly budgeted PD/surviving-colours asset work should happen only after an explicit media-budget/profile decision. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D205):** `src/tactical/T0-field-sandbox.js` trims the low-tier base marker topper, and `src/tactical/T24-formation-figures.js` reapplies that trim in the low/off fallback path after marker visibility is restored. `tools/probe-intel-uhd617-profile.mjs` records flag/pole/topper state and asserts low tier keeps the flag/pole side read while hiding only the decorative topper.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T0-field-sandbox.js`, `src/tactical/T24-formation-figures.js`, and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-intel-uhd617-profile` **19/19** (high 9.98ms / 75 calls / 148 objects; low 1.94ms / 84 calls / 134 objects); adjacent `probe-formation-figures` **17/17, 0 pageerrors**, `probe-visual-fidelity` **27/27, 0 pageerrors, 0 texture warnings**, and `probe-media-budget` **6/6** with the known soft warning only. JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `git diff --check` and `node tools/build.mjs --check` passed.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H ATMOSPHERIC SMOKE DRAW-RANGE PERF POLISH SHIPPED (D204).** Group 5 continued without granting M8 battle-build approval. D204 is another zero-new-media performance slice guided by the D193-D203 media-budget/profile/readability/perf guards: the T16 3D gunsmoke point cloud now draws only active smoke particles instead of the full 200-slot buffer. Active smoke, fog secrecy, reduce-motion/off suppression, and seed stability remain locked. `probe-intel-uhd617-profile` records/asserts high/low smoke draw-range state; post-D204 profile reads high 8.93ms / 75 calls / 148 objects / 111 smoke points and low 1.34ms / 92 calls / 134 objects / 43 smoke points. **THEN-NEXT, now shipped as D205/D206:** low-tier marker-topper perf/resource polish. Tightly budgeted PD/surviving-colours asset work should happen only after an explicit media-budget/profile decision. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D204):** `src/tactical/T16-atmospherics.js` initializes `atmoSmoke` hidden with draw range `0`, then updates `geometry.drawRange` and `visible` from the live active particle count each 3D frame. `tools/probe-atmospherics.mjs` records/asserts active particle draw range while preserving smoke spawn, fog-leak, reduce-motion/off, seed-stability, and texture-warning gates. `tools/probe-intel-uhd617-profile.mjs` records/asserts the high/low draw-range contract.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T16-atmospherics.js`, `tools/probe-atmospherics.mjs`, and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-atmospherics` **20/20, 0 pageerrors, 0 texture warnings**; `probe-intel-uhd617-profile` **18/18** (high 8.93ms / 75 calls / 148 objects / 111 smoke points; low 1.34ms / 92 calls / 134 objects / 43 smoke points); adjacent `probe-weather` **30/30, 0 pageerrors, 0 texture warnings**, `probe-media-budget` **6/6** with the known soft warning only, and `probe-visual-fidelity` **27/27, 0 pageerrors, 0 texture warnings**. JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H HIDDEN PEG-FALLBACK RESOURCE POLISH SHIPPED (D203).** Group 5 continued without granting M8 battle-build approval. D203 is another zero-new-media resource/performance slice guided by the D193-D202 media-budget/profile/readability/perf guards: when T24 formation figures replace the slab in high tier, T21 no longer keeps the older peg-rank fallback resident as hidden `vfPegs` meshes. The fallback is still available when `formationFigures="off"`, and low tier still gates peg ranks out. `probe-intel-uhd617-profile` now records/asserts the high-tier no-hidden-peg residency contract. **THEN-NEXT, now shipped as D204:** atmospheric smoke draw-range perf polish. Tightly budgeted PD/surviving-colours asset work should happen only after an explicit media-budget/profile decision. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D203):** `src/tactical/T21-visual-fidelity.js` adds `fldVfFormationFiguresWillReplace()` / `fldVfShouldBuildPegs()` and only builds `vfPegs` when the slab fallback is actually needed. `tools/probe-visual-fidelity.mjs` verifies default-high no resident pegs plus the visible peg fallback when formation figures are off. `tools/probe-formation-figures.mjs` asserts `pegsResident:false` when high-tier figures replace the slab and keeps its static scan focused on combat/sim leakage. `tools/probe-intel-uhd617-profile.mjs` records/asserts high-tier no-hidden-peg residency.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T21-visual-fidelity.js`, `tools/probe-visual-fidelity.mjs`, `tools/probe-formation-figures.mjs`, and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-visual-fidelity` **27/27, 0 pageerrors, 0 texture warnings** with screenshots visually checked; adjacent `probe-formation-figures` **17/17, 0 pageerrors**, `probe-intel-uhd617-profile` **16/16** (high 9.39ms / 75 calls / 148 objects; low 1.19ms / 92 calls / 134 objects; high-tier no hidden peg ranks), `probe-tripo-unit-assets` **15/15, 0 pageerrors**, and `probe-media-budget` **6/6** with the known soft warning only. JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H BASE 3D MARKER GEOMETRY PERF POLISH SHIPPED (D202).** Group 5 continued without granting M8 battle-build approval. D202 is another zero-new-media performance/resource slice guided by the D193-D201 media-budget/profile/readability/perf guards: the base T0 3D unit marker now shares immutable slab/front/ring/pole/topper geometries across unit groups for each rebuild, while keeping per-unit materials independent so casualty fades, routing opacity, flags, and selected/fading ring states cannot bleed across brigades. `probe-intel-uhd617-profile` now records/asserts the high/low geometry-sharing contract and the per-unit material guard. **THEN-NEXT, now shipped as D203:** hidden peg-fallback resource polish. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D202):** `src/tactical/T0-field-sandbox.js` adds a per-rebuild `fld3dUnitMarkerResources()` cache for immutable base marker geometries and reuses those geometries for every T0 unit group; per-unit materials still allocate separately. `tools/probe-intel-uhd617-profile.mjs` now inspects high and low scenes for shared marker geometry plus non-shared per-unit slab/front/pole/topper materials. `tools/probe-render-richness.mjs` keeps its assertions intact but extends the local per-scene wrapper timeout to 240s after the 120s wrapper cut off the rich 3D scene on this 8 GB Mac.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T0-field-sandbox.js`, `tools/probe-intel-uhd617-profile.mjs`, and `tools/probe-render-richness.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-intel-uhd617-profile` **15/15** (high 8.45ms / 75 calls / 154 objects; low 3.81ms / 92 calls / 134 objects; marker geometries shared, per-unit materials not shared); adjacent `probe-render-richness` **31/31, 0 pageerrors, 0 texture warnings**, `probe-visual-fidelity` **26/26, 0 pageerrors, 0 texture warnings**, `probe-formation-figures` **17/17, 0 pageerrors**, `probe-presets` **26/26, 0 pageerrors**, and `probe-media-budget` **6/6** with the known soft warning only. JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H RENDER-RICHNESS SELECTION-RING PERF POLISH SHIPPED (D201).** Group 5 continued without granting M8 battle-build approval. D201 is a zero-new-media performance slice guided by the D193-D200 media-budget/profile/readability/perf guards: the T18 render-richness layer now visibility-culls idle/dead/final-faded 3D selection rings instead of leaving opacity-zero rings visible to the renderer, while selected rings and active casualty-fade rings still render. **THEN-NEXT, now shipped as D202:** base 3D marker geometry perf polish. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D201):** `src/tactical/T18-render-richness.js` now hides idle/dead/final-faded selection rings with `visible=false` and opacity reset, restores selected rings for the existing pulse/static cue, and keeps casualty-fade rings visible while the fallen unit fades. `tools/probe-render-richness.mjs` locks idle-ring culling, selected-ring visibility, fade behavior, sim invariance, and bounded scene progress/timeout behavior. `tools/probe-intel-uhd617-profile.mjs` records/asserts high/low idle selection-ring culling.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T18-render-richness.js`, `tools/probe-render-richness.mjs`, and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-render-richness` **31/31, 0 pageerrors, 0 texture warnings** and the fresh 3D screenshot was visually checked; adjacent `probe-media-budget` **6/6** with the known soft warning only, `probe-intel-uhd617-profile` **14/14** (high 10.14ms / 75 calls / 154 objects; low 1.74ms / 92 calls / 134 objects), `probe-visual-fidelity` **26/26, 0 pageerrors, 0 texture warnings**, `probe-formation-figures` **17/17, 0 pageerrors**, and `probe-presets` **26/26, 0 pageerrors**. JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H FORMATION-FIGURES PERF POLISH SHIPPED (D200).** Group 5 continued without granting M8 battle-build approval. D200 is a zero-new-media performance slice guided by the D193-D199 media-budget/profile/readability/perf guards: the T24 procedural infantry formation figures now use one shared scene-level `ffFormationLayer` with five instanced layers instead of five instanced meshes per brigade, preserving high-tier formation readability while reducing resident scene objects and render calls. **THEN-NEXT, now shipped as D201:** render-richness selection-ring perf polish. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D200):** `src/tactical/T24-formation-figures.js` now owns one scene-level `ffFormationLayer` for bodies, heads, kepis, rifles, and bayonets. Unit groups keep a lightweight `ffFormation` marker and slot metadata; live infantry update their assigned instance block, while off/low/GLB-hidden units clear their slots and restore the correct slab/GLB fallback. `tools/probe-formation-figures.mjs` locks the shared-layer contract, pose/readability behavior, off/low fallback, screenshot, and zero pageerrors. `tools/probe-intel-uhd617-profile.mjs` records and asserts shared formation-figure mode.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T24-formation-figures.js`, `tools/probe-formation-figures.mjs`, and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-formation-figures` **17/17, 0 pageerrors** and the high-tier screenshot was visually checked; adjacent `probe-media-budget` **6/6** with the known soft warning, `probe-intel-uhd617-profile` **13/13** (high 7.36ms / 83 calls / 154 objects; low 2.32ms / 100 calls / 134 objects), `probe-tripo-unit-assets` **15/15, 0 pageerrors**, `probe-visual-fidelity` **26/26, 0 pageerrors, 0 texture warnings**, and `probe-presets` **26/26, 0 pageerrors**. JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H VISUAL-FIDELITY CONTACT-SHADOW PERF POLISH SHIPPED (D199).** Group 5 continued without granting M8 battle-build approval. D199 is a zero-new-media performance slice guided by the D193-D198 media-budget/profile/readability guards: the T21 visual-fidelity contact-shadow cue now uses one shared `vfShadowLayer` instanced mesh instead of one child mesh per brigade, preserving unit grounding while reducing resident scene objects and render calls. **THEN-NEXT, now shipped as D200:** formation-figures perf polish. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D199):** `src/tactical/T21-visual-fidelity.js` now owns one scene-level `vfShadowLayer` `InstancedMesh`; unit groups store a shadow instance index, live visible units update their shadow matrix, and dead/fog-hidden/off instances are parked at zero scale. `tools/probe-visual-fidelity.mjs` now verifies the shared instanced shadow layer, ground gap, positive width/depth scale, low-tier peg gating, off-switch, screenshot health, and byte-identity burst. `tools/probe-intel-uhd617-profile.mjs` records and asserts the instanced shadow mode in high and low profile scenes.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T21-visual-fidelity.js`, `tools/probe-visual-fidelity.mjs`, and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-visual-fidelity` **26/26, 0 pageerrors, 0 texture warnings** and the 3D screenshot was visually checked; adjacent `probe-media-budget` **6/6** with the known soft warning, `probe-intel-uhd617-profile` **12/12** (high 9.57ms / 108 calls / 178 objects; low 2.38ms / 100 calls / 134 objects), and `probe-h0-tactical-hud` **3/3, 0 pageerrors**. JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `probe-formation-figures` and `probe-render-richness` were attempted as extra adjacent checks but both hung before fresh artifacts and were stopped with exit 130, so neither is counted. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H TERRAIN OVERLAY PERF POLISH SHIPPED (D198).** Group 5 continued without granting M8 battle-build approval. D198 is a zero-new-media performance/readability slice guided by the D193-D197 media-budget/profile guards: default 3D Hillshade in `src/tactical/T22-terrain-readability.js` no longer prebuilds hidden contour and color-by-height overlay geometry, while selected Contours/Color-by-height modes still lazy-build and render on demand. **THEN-NEXT, now shipped as D199/D200:** visual-fidelity contact-shadow perf polish, then formation-figures perf polish. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D198):** `src/tactical/T22-terrain-readability.js` now lazy-builds optional 3D terrain overlays via `fldTrEnsure3dMode()` / `fldTrBuildHyp3d()`: default Hillshade keeps no resident optional contour/hypsometric geometry, mode toggles build the requested overlay once, and `fldExit` disposal still nulls all owned terrain overlay refs. `tools/probe-terrain-readability.mjs` now verifies the no-resident-default plus existing mode, legend, hover, lifecycle, low-tier, and sim-stability gates. `tools/probe-intel-uhd617-profile.mjs` records terrain-overlay residency in the profile artifact and asserts default high/low profile scenes do not prebuild optional terrain overlays.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T22-terrain-readability.js`, `tools/probe-terrain-readability.mjs`, and `tools/probe-intel-uhd617-profile.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-terrain-readability` **29/29, 0 pageerrors**; adjacent `probe-media-budget` **6/6** with only the known soft warning, `probe-intel-uhd617-profile` **11/11** (high 10.16ms / 115 calls / 185 objects; low 2.77ms / 107 calls / 141 objects), `probe-visual-fidelity` **26/26, 0 pageerrors, 0 texture warnings**, and `probe-h0-tactical-hud` **3/3, 0 pageerrors**. JSON scan of counted artifacts found no `ok=false`, failed steps, `FATAL`, pageerrors, realErrors, or texture warnings. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H BATTLE COLOURS HUD READABILITY POLISH SHIPPED (D197).** Group 5 continued without granting M8 battle-build approval. D197 is a zero-new-media polish slice guided by the D193-D196 media-budget/profile guards: the selected-unit battle-colours HUD now has a bounded labelled card, larger 58x37 flag image, escaped/wrapping readable title/caption/corps text, clearer corps-badge row, and readable pre-Mar-1863 badge note. **THEN-NEXT, now shipped as D198:** terrain overlay perf polish. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D197):** `src/tactical/T10-flags.js` now renders `fldFlagHudSelected()` as a bounded `role="group"` battle-colours card with a larger flag thumbnail, stronger card contrast, escaped HTML text, wrapping-safe readable labels, and a clearer corps-badge/note treatment. `tools/probe-flags.mjs` now verifies the HUD structure/ARIA label/image size/text sizing/wrapping plus the existing historical flag/corps gates and seed/sim invariance across a 45-frame render burst.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T10-flags.js` and `tools/probe-flags.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-flags` **39/39, 0 pageerrors**, no texture warnings; adjacent `probe-media-budget` **6/6** with only the known soft warning, `probe-intel-uhd617-profile` **10/10** (high 9.66ms / 115 calls / 195 objects; low 1.94ms / 107 calls / 144 objects), and `probe-h0-tactical-hud` **3/3, 0 pageerrors**. JSON scan of counted artifacts found no `ok=false`, failed steps, pageerrors, realErrors, or texture warnings. `probe-formation-figures` was attempted but hung before a fresh artifact and is not counted. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H H2 CUTAWAY READABILITY POLISH SHIPPED (D196).** Group 5 continued without granting M8 battle-build approval. D196 is another zero-new-media polish slice guided by the D194/D195 media and Intel UHD-617 guards: the skippable H2 field-cutaway shell is now phone-safe with viewport-clamped card bounds, contained scrolling, readable captions/notes, 42px controls, ARIA title/subtitle labelling, focus return, and key-handler cleanup. **THEN-NEXT, now shipped as D197:** battle-colours HUD readability polish. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D196):** `src/104-h2-cutaways.js` now bounds the H2 cutaway overlay/card on desktop and phone, keeps the existing still/procedural fallback readable with clamp-based frame heights and 12px captions, gives close/return controls 42px targets, adds `aria-labelledby`/`aria-describedby`, and removes the Escape listener on close. `tools/probe-h2-cutaways.mjs` now verifies desktop and 390px phone card containment, caption size/line-height/box sizing, touch targets, horizontal-overflow absence, focus return, disabled-video/offline-fallback policy, and zero pageerrors.
>
> **VET (focused, D176):** `node --check` passed for `src/104-h2-cutaways.js` and `tools/probe-h2-cutaways.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-h2-cutaways` **9/9, 0 pageerrors** with JSON readback clean; adjacent `probe-media-budget` **6/6** with only the known soft warning, `probe-intel-uhd617-profile` **10/10** (high 12.24ms / 115 calls / 195 objects; low 2.00ms / 107 calls / 144 objects), and `probe-h0-battle-briefing` **3/3, 0 pageerrors**. Key-aware JSON scan found no `ok=false`, no actual `FATAL`, no pageerrors, no realErrors, and no texture warnings. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-02 **GROUP 5 · PHASE H AUDIO/READABILITY POLISH SHIPPED (D195).** Group 5 continued without granting M8 battle-build approval. D195 is a zero-new-media polish slice guided by the D194 Intel UHD-617 profile: the battlefield Audio dialog is now phone-safe with a bounded scrollable card, 32px+ segmented controls including the opt-in Battle ambience row, reliable focus return, and a more readable caption box. **THEN-NEXT, now shipped as D196:** H2 cutaway readability polish. Heavy footage/HDRI/model media remains optional-pack or explicit-budget only. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D195):** `src/tactical/T9-audio.js` now gives the Audio dialog an `#fldAudioPanelCard` with max-height/overflow guards, larger segmented controls and Done button, and a caption style with stronger border/contrast, fixed line-height, viewport-bounded width, and border-box wrapping. `src/tactical/T19-battle-ambience.js` matches the ambience On/Off row to the larger touch target. `tools/probe-audio-ambience.mjs` now verifies desktop and phone dialog containment, row/button count, min button height, focus handoff/return, caption viewport fit/readable sizing, and zero pageerrors.
>
> **VET (focused, D176):** `node --check` passed for `src/tactical/T9-audio.js`, `src/tactical/T19-battle-ambience.js`, and `tools/probe-audio-ambience.mjs`; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-audio-ambience` **27/27, 0 pageerrors** with JSON readback clean; adjacent `probe-media-budget` **6/6** with only the known soft warning, `probe-intel-uhd617-profile` **10/10** (high 9.37ms / 115 calls / 195 objects; low 2.09ms / 107 calls / 144 objects), `probe-h0-tactical-hud` **3/3, 0 pageerrors**, `probe-accessibility` **25 steps, 0 pageerrors**, and `probe-presets` **26 steps, 0 pageerrors**. JSON readback found no `ok=false`, no `FATAL`, no pageerrors, no realErrors, and no texture warnings. `git diff --check` clean.

## ⚡ EARLIER — 2026-07-01 **GROUP 5 · PHASE H INTEL UHD-617 PROFILE SHIPPED (D194).** Group 5 continues without granting M8 battle-build approval. D194 adds an executable Intel UHD-617 performance/readout profile after D193's media-budget guard: the one-file core remains self-contained, heavy footage/HDRI/model media stays optional-pack or explicit-budget only, and H2 video remains disabled pending per-asset provenance. **THEN-NEXT, now shipped as D195:** audio/readability polish. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D194):** `data/media-budget.json` now records a `performanceProfile` policy for the Intel UHD Graphics 617 / 8 GB RAM floor. New `tools/probe-intel-uhd617-profile.mjs` launches the built single-file game in Chrome, records embedded asset footprint, WebGL renderer info, high/low 3D launch and render-burst timings, render calls, scene object counts, pixel nonblank checks, low-tier formation/peg gating, pageerrors, and texture warnings. `tools/vet-no-regression.mjs` now includes `intel uhd617 profile`. `civil_war_generals.html` rebuilt from source because `GAME_DATA["media-budget"]` changed.
>
> **PROFILE EVIDENCE:** local Chrome used the actual `ANGLE (Intel, ANGLE Metal Renderer: Intel(R) UHD Graphics 617, Unspecified Version)` path, not SwiftShader. High tier on Chickamauga: launch 3407.9ms, proxy frame burst 9.92ms avg, 115 render calls, 195 scene objects. Low tier: launch 343ms, 2.48ms avg, 107 render calls, 144 objects, `fldLow:true`, formation figures gated out. Embedded core remains 199 files / 2.418 MB raw and generated HTML is about 7.023 MB.
>
> **VET (focused, D176):** JSON parse for media-budget; `node --check` for the new profile probe and suite file; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-intel-uhd617-profile` **10/10** with the expected raw-embed soft warning only; adjacent `probe-media-budget` **6/6**, `probe-h2-cutaways` **5 steps, 0 pageerrors**, and `probe-formation-figures` **16/16, 0 pageerrors**; JSON artifact readback clean with no `ok=false`, `FATAL`, pageerrors, realErrors, texture warnings, or failed steps; `git diff --check` clean.

## ⚡ EARLIER — 2026-07-01 **GROUP 5 · PHASE H MEDIA BUDGET GUARD SHIPPED (D193).** Group 5 started without granting M8 battle-build approval. D193 adds an executable media-budget/optional-HD-pack guard for Phase H: the one-file core remains self-contained, heavy footage/HDRI/model media stays optional-pack or explicit-budget only, and H2 video remains disabled pending per-asset provenance. **THEN-NEXT, now shipped as D194:** Intel UHD-617 profiling/readout. M8 battle-build still requires Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D193):** `data/media-budget.json` records the core/HD-pack policy, raw embed caps, H2-video lock, Tripo lock, and battle-build lock. `tools/probe-media-budget.mjs` measures `assets/embed/**`, checks budget drift against `tools/build.mjs`, rejects undeclared/video embeds, verifies H2 video slots are still disabled, and writes `tools/shots/probe-media-budget.json`. `tools/vet-no-regression.mjs` now includes `media budget`. `civil_war_generals.html` rebuilt from source because the new data file is injected as `GAME_DATA["media-budget"]`.
>
> **VET (focused, D176):** JSON parse for media-budget/footage-cutaways/manifest; `node --check` for the new probe and suite file; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the known raw-embed soft warning; focused `probe-media-budget` **6/6** with one expected soft warning; adjacent `probe-h2-cutaways` **5 steps, 0 pageerrors** and `probe-scenes-imagery` **16/16, 0 pageerrors**; JSON artifact readback clean with no `ok=false`, `FATAL`, pageerrors, realErrors, or failed steps; `git diff --check` clean.

## ⚡ EARLIER — 2026-07-01 **GROUP 4 · BATCH/RELEASE GATE PASSED (D192).** The planned Group 4 no-regression checkpoint is complete. D192 is a verification/harness milestone only: it changed the logistics probe harness and live-status docs, not game source/data, generated HTML, combat, saves, historical content, or battle rosters. **THEN-NEXT, now shipped as D193:** Group 5 Phase H media budget guard. Do not start M8 battle-build without Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D192):** `tools/probe-logistics-rail.mjs` now uses a 120s screenshot timeout, bounded browser-close cleanup, and explicit nonzero exit when its JSON is red. This fixes the slow-Mac screenshot-timeout failure seen during the release gate without weakening assertions.
>
> **VET (release, D176):** initial `npm run vet:noreg` segment passed build/import/boot/strategic probes through production, then stopped only because `probe-logistics-rail` wrote `ok=false` after Playwright's default screenshot timeout. `node --check tools/probe-logistics-rail.mjs` passed; focused `probe-logistics-rail` passed **8 steps, 0 pageerrors**; resumed `npm run vet:noreg -- --from="logistics rail"` completed **VET NO-REGRESSION OK - 92 commands**. Combined suite coverage was 104 commands. `tools/shots/*.json` scan found **104 files, bad=0**: no `ok=false`, no `FATAL`, no pageerrors, no realErrors. Classic diag ended `nonBlank:346`, `m3dActive:false`, with only known filtered 404 lines.

## ⚡ EARLIER — 2026-07-01 **GROUP 4 · WESTERN-THEATER STRATEGIC READOUTS SHIPPED (D191).** Western-theater strategic readouts are complete as bounded Group 4 non-battle readouts. They add a sourced War Effort/H0 Desk and Theater Map ledger for the live Western arc: Shiloh, Vicksburg, Chickamauga, river/rail leverage, Chickamauga-to-Chattanooga reversal pressure, Atlanta/election pressure, and Franklin/Nashville Army-of-Tennessee strain. **THEN-NEXT, now completed as D192:** planned Group 4 batch/release gate. M8 battle-build track still needs an Aaron go/no-go first. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D191):** `data/western-theater.json` carries the readout source pack; `src/73-western-theater.js` adds readout-only state sanitation, snapshot, exact-zero bridge sentinel, resolve log, War Effort/H0 Desk block, and Theater Map block; manifest, lifecycle, H0 President's Desk, focused probe, and later-batch suite enrollment are wired; `civil_war_generals.html` rebuilt from source.
>
> **CONTRACT:** readout-only. `westernTheaterBridgeBonus()` returns exact zero and is not consumed by the bridge. Resolve writes only `C.westernTheater.lastTurn` and the capped `C.westernTheater.log`; it adds no priority lever, playable battle, battle roster, tactical OOB, casualty/winner/scoreboard, battle-output write, per-battle combat tuning, or direct `overall` effect. No `data/chattanooga.json`, `data/atlanta.json`, `data/franklin.json`, or `data/nashville.json` was created.
>
> **VET (focused, D176):** JSON parse for western-theater/manifest; `node --check` passed for touched source/probe/suite files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the existing raw-embed soft-budget warning; focused `probe-western-theater` **8 steps, 0 pageerrors**; adjacent `probe-human-cost` **7 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` then became the next planned batch/release gate under D176, completed as D192.

## ⚡ EARLIER — 2026-07-01 **GROUP 4 · HUMAN COST WITH GRAVITY TREATMENT SHIPPED (D190).** The human-cost treatment is complete as a bounded Group 4 non-battle readout. It adds a sourced War Effort/H0 Desk ledger for the war's death scale, the old 620,000 count, Hacker's roughly 750,000 revision, disease deaths, mourning, pensions, cemeteries, names, and Faust work-of-death framing. **THEN-NEXT, now shipped as D191:** Western-theater strategic readouts. M8 battle-build track still needs an Aaron go/no-go first. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D190):** `data/human-cost.json` carries `humanCost`; `src/72-human-cost.js` adds readout-only state sanitation, snapshot, exact-zero bridge sentinel, resolve log, and H0/War Effort blocks; manifest, lifecycle, H0 President's Desk, focused probe, and later-batch suite enrollment are wired; `civil_war_generals.html` rebuilt from source.
>
> **CONTRACT:** readout-only. `humanCostBridgeBonus()` returns exact zero and is not consumed by the bridge. Resolve writes only `C.humanCost.lastTurn` and the capped `C.humanCost.log`; it adds no priority lever, playable battle, battle roster, tactical OOB, casualty/winner/scoreboard, battle-output write, per-battle combat tuning, or direct `overall` effect.
>
> **VET (focused, D176):** JSON parse for human-cost/manifest; `node --check` passed for touched source/probe/suite files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓** with the existing raw-embed soft-budget warning; focused `probe-human-cost` **7 steps, 0 pageerrors**; adjacent `probe-disease-medical` green artifact **8 steps, 0 pageerrors** despite a post-artifact wrapper hang that was manually stopped, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` deferred to the planned-work batch/release gate under D176.

## ⚡ EARLIER — 2026-07-01 **GROUP 4 · REAL DIPLOMACY SYSTEM SHIPPED (D189).** The real diplomacy system is complete as a bounded Group 4 non-battle system. It adds sourced War Effort and Diplomacy-tab levers for **Mason/Slidell recognition work**, the **Trent Affair**, **1862 mediation pressure**, **Laird rams**, the **Russian fleet**, and **King Wheat vs cotton diplomacy**. **THEN-NEXT, now shipped as D190/D191:** human-cost-with-gravity treatment and Western-theater strategic readouts. **CURRENT ORDER:** planned Group 4 batch/release gate unless Aaron redirects. M8 battle-build track still needs an Aaron go/no-go first. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D189):** `data/diplomacy.json` now carries `realDiplomacy`; `src/71-real-diplomacy.js` adds side-aware state, snapshot, priority, resolve, bridge, War Effort, and Diplomacy-tab blocks; manifest, War Effort render, H0 President's Desk, diplomacy-tab wiring, lifecycle, and battle-bridge seams are wired; `tools/probe-real-diplomacy.mjs` locks the contract and `tools/vet-no-regression.mjs` enrolls the focused probe. `civil_war_generals.html` rebuilt from source.
>
> **CONTRACT:** bounded non-battle diplomacy; inactive default bridge effect is exact zero; active priorities are side-filtered, capped, and add no direct `overall` bonus. Resolve touches only diplomacy ledgers, blockade recognition, `clock.intervention`, `clock.capital`, neutral goodwill, commerce pressure, and crisis pressure. It adds no playable battle, battle roster, tactical OOB, casualty/winner/scoreboard, battle-output write, or per-battle combat tuning.
>
> **VET (focused, D176):** JSON parse for diplomacy/manifest; `node --check` passed for touched source/probe/suite files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓**; focused `probe-real-diplomacy` **8 steps, 0 pageerrors**; adjacent `probe-blockade` **11 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` deferred to the planned-work batch/release gate under D176.

## ⚡ EARLIER — 2026-07-01 **GROUP 4 · CS FINANCE TOOLKIT SHIPPED (D188).** The Confederate finance toolkit is complete as a bounded Group 4 non-battle system. It adds sourced War Effort choices for the **Erlanger loan**, **cotton bonds**, **produce loan**, **impressment**, and the **printing spiral**. **THEN-NEXT, now shipped as D189/D190/D191:** real diplomacy, human-cost, and Western-theater strategic readouts. **CURRENT ORDER:** planned Group 4 batch/release gate unless Aaron redirects. M8 battle-build track still needs an Aaron go/no-go first. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D188):** `data/cs-finance.json` adds the citation-guarded finance toolkit; `src/69-cs-finance.js` adds CS-only state, render, bridge-bonus, priority, and resolve hooks; manifest, War Effort render, H0 President's Desk, economy-tab wiring, lifecycle, and battle-bridge seams are wired; `tools/probe-cs-finance.mjs` locks the contract and `tools/vet-no-regression.mjs` enrolls the focused probe. `civil_war_generals.html` rebuilt from source. `HISTORICAL-DATA-ECONOMY.md` now carries the corrected Erlanger net-proceeds guardrail (~45% of face), not the stale 72% shortcut.
>
> **CONTRACT:** Confederate-side-only; inactive default bridge effect is exact zero; active priorities are capped and costly; no direct `overall` bonus; no playable battle, battle roster, tactical OOB, casualty/winner/scoreboard, or battle-output path changed. Strategic resolve touches only CS finance ledgers, funds/debt/cotton/impressment/civilian pressure, economy inflation, and capital pressure.
>
> **VET (focused, D176):** JSON parse for CS finance/economy/manifest; `node --check` passed for touched source/probe/suite files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓**; focused `probe-cs-finance` **8 steps, 0 pageerrors**; adjacent `probe-economy` **8 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` deferred to the planned-work batch/release gate under D176.

## ⚡ CONTINUE HERE — 2026-07-01 **GRADUATE-HISTORY PASS · M7 LIVED-SLAVERY + BLACK COMBAT AGENCY SHIPPED (D187).** M7 of the authorized graduate-history build-out is complete as a pure teaching/readout milestone. The Documents deck now has a **Lived slavery & resistance** lane with exact-excerpt `Verified` records for Harriet Jacobs and Fountain Hughes, including edition/interview/transcription source critiques. The codex now has `slavery-as-lived-institution` and `black-combat-agency`, plus sharper USCT, secession, contraband, emancipation-economics, 54th Massachusetts, and USCT manpower cross-links. **NEXT: CS finance toolkit** (Erlanger loan / cotton bonds / impressment / produce loan / printing spiral) as the next Group 4 non-battle system. **POST-CS-FINANCE ORDER:** real diplomacy system -> human-cost-with-gravity -> Western-theater strategic readouts -> planned batch/release gate. M8 battle-build track still needs an Aaron go/no-go first. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D187):** `data/primary-sources.json` gains the lived-slavery category and 2 records; `src/68-primary-sources.js` fallback order knows the new lane; `data/codex.json` gains 2 entries and updated USCT/secession/emancipation/manpower cross-links; `tools/probe-primary-sources.mjs` and `tools/probe-codex.mjs` lock the M7 source/readout claims. `civil_war_generals.html` rebuilt from source.
>
> **CONTRACT:** pure teaching/readout, **D74 byte-identical by construction**. No tactical, bridge, resolve, victory, save, OOB, battle-output, or economy-math path changed. The Black combat-agency card is explicitly a teaching/readout lock now; playable USCT battles remain later and still require the battle-build queue/go-no-go path.
>
> **VET (focused, D176):** JSON parse for primary sources/codex; `node --check` passed for touched source/probe files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓**; focused `probe-primary-sources` **14 steps, 0 pageerrors** and `probe-codex` **23 steps, 0 pageerrors**; adjacent `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` deferred to the planned-work batch/release gate under D176.

## ⚡ CONTINUE HERE — 2026-07-01 **GRADUATE-HISTORY PASS · M6 HOME-FRONT POLITICS/ECONOMY SHIPPED (D185).** M6 of the authorized graduate-history build-out is complete as a pure teaching/readout milestone. The Documents deck now has a **Home front, politics & economy** lane with exact-excerpt `Verified` records for Lincoln's August 23, 1864 blind memorandum and the February 25, 1862 Legal Tender Act. The codex now has `war-finance-civics`, `womens-home-front-labor`, `emancipation-economic-revolution`, and `ex-parte-milligan`, plus sharpened `election-of-1864`, `greenbacks-and-wartime-inflation`, `suspension-of-habeas-corpus`, and `emancipation-proclamation` links/body text. The Treasury why-it-mattered readout now teaches bonds/taxes/printing as civics under fire. **THEN-NEXT, now shipped as D187:** M7 — lived-slavery + Black combat-agency from `.tmp/civil-war-phd-execution-brief.md` / `.tmp/civil-war-history-audit-and-phd-plan.md`. **POST-D187 ORDER:** CS finance toolkit -> real diplomacy system -> human-cost-with-gravity -> Western-theater strategic readouts -> planned batch/release gate. M8 battle-build track still needs an Aaron go/no-go first. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D185):** `data/primary-sources.json` gains the home-front/politics/economy category and 2 records; `src/68-primary-sources.js` fallback order knows the new lane; `data/codex.json` gains 4 entries and updated election/finance/emancipation/Milligan cross-links; `src/40-economy.js` sharpens the Treasury teaching readout; `tools/probe-primary-sources.mjs`, `tools/probe-codex.mjs`, `tools/probe-economy.mjs`, and `tools/probe-h0-president-desk.mjs` lock the visible readouts. `civil_war_generals.html` rebuilt from source.
>
> **CONTRACT:** pure teaching/readout, **D74 byte-identical by construction**. No tactical, bridge, resolve, victory, save, OOB, battle-output, or economy-math path changed. The Legal Tender card names the coin exceptions for import duties and bond/note interest; Milligan is framed as a post-crisis constitutional boundary, not a one-line partisan verdict.
>
> **VET (focused, D176):** JSON parse/word-cap/related-link smoke passed for primary sources/codex; `node --check` passed for touched source/probe files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓**; focused `probe-primary-sources` **13 steps, 0 pageerrors**, `probe-codex` **22 steps, 0 pageerrors**, and `probe-economy` **8 steps, 0 pageerrors**; adjacent `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` deferred to the planned-work batch/release gate under D176.

## ⚡ CONTINUE HERE — 2026-07-01 **GRADUATE-HISTORY PASS · M5 KEYSTONE CARDS SHIPPED (D184).** M5 of the authorized graduate-history build-out is complete as a pure teaching/readout milestone. The codex now has `second-founding` and `age-of-emancipation`: McPherson/Foner Second Founding framing tied to greenbacks, conscription, contraband, emancipation, Reconstruction, and Douglass's July 6, 1863 enlistment speech; and a comparative emancipation card contrasting Britain 1830s compensated emancipation, Russia 1861 / roughly 23 million privately owned serfs, and U.S. wartime uncompensated emancipation driven by policy, self-emancipation, USCT service, and Black agency. `data/disease-medical.json` adds the Faust/Hacker work-of-death debate card, and `data/irregular-war.json` adds the Fellman/Sutherland irregular-war historiography card. The existing medical and irregular-war details render all debate cards so these are visible, not dark data. **THEN-NEXT, now shipped as D185:** M6 home-front/politics/economy from `.tmp/civil-war-phd-execution-brief.md` / `.tmp/civil-war-history-audit-and-phd-plan.md`. M8 battle-build track still needs an Aaron go/no-go first. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D184):** `data/codex.json` gains two `Verified` keystone entries; `data/disease-medical.json` gains `faust-work-of-death`; `data/irregular-war.json` gains `irregular-war-historiography`; `src/63-disease-medical.js` and `src/65-irregular-war.js` render all debate cards; `tools/probe-codex.mjs`, `tools/probe-disease-medical.mjs`, and `tools/probe-irregular-war.mjs` lock the M5 claims; `civil_war_generals.html` rebuilt from source.
>
> **CONTRACT:** pure teaching read-out, **D74 byte-identical by construction**. Display-only rendering changed for War Effort details; default inactive bridge effects, active caps, save shape, resolve hooks, tactical/OOB paths, scoreboards, and victory paths are unchanged. The Douglass "brass letters U.S." phrase is attributed to the July 6, 1863 Philadelphia enlistment speech, not the March 1863 `Men of Color, To Arms!` broadside. The Russian emancipation comparison keeps the corrected roughly 23 million privately owned serfs guardrail.
>
> **VET (focused, D176):** JSON parse for codex/disease-medical/irregular-war/manifest; `node --check` for touched source/probe files; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓**; focused `probe-codex` **21 steps, 0 pageerrors**, `probe-disease-medical` **8 steps, 0 pageerrors**, and `probe-irregular-war` **8 steps, 0 pageerrors**; adjacent `probe-primary-sources` **12 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` deferred to the planned-work batch/release gate under D176.

## ⚡ CONTINUE HERE — 2026-07-01 **GRADUATE-HISTORY PASS · M4 SOURCE-CRITICISM / UNDER-TOLD VOICES / NATIVE REWRITE SHIPPED (D183).** M4 of the authorized graduate-history build-out is complete. The pure-readout Documents deck now has a **Source criticism & under-told voices** lane with five `Verified` records: Gettysburg Address Bliss copy, Susie King Taylor's postwar critique, Dolly Lunt Burge's hard-war eyewitness/enslaver-language record, William McCarter's Irish Brigade memoir, and William Dwyer's Irish Brigade letter. The codex now has `source-criticism`, `east-tennessee-unionism`, and `west-virginia-statehood`; `irish-brigade` names both McCarter's retrospective memoir and Dwyer's contemporary immigrant-soldier letter. `women-in-war` now has Mary Boykin Chesnut and Susie King Taylor cards, both `canMap:false`. The Native-nations under-told thread is upgraded to `Verified` using the M4 dossier, but its source note still blocks playable Trans-Mississippi battle/OOB work and Confederate-diversity laundering. **NEXT: M5 — keystone cards** from `.tmp/civil-war-phd-execution-brief.md` / `.tmp/civil-war-history-audit-and-phd-plan.md`. M8 battle-build track still needs an Aaron go/no-go first. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D183):** `data/primary-sources.json` gains the source-criticism category and 5 records; `data/codex.json` gains Source Criticism, East Tennessee Unionism, and West Virginia Statehood entries plus Irish Brigade McCarter/Dwyer source locks; `data/women-in-war.json`, `src/38-women-in-war.js`, `tools/import-women-in-war.mjs`, and `tools/probe-women-in-war.mjs` support 9 cards / 8 Verified / 1 Disputed; `data/under-told-perspectives.json` gets the Native rewrite; `src/66-under-told-perspectives.js` updates only teaching/readout copy and the women-lane count; `tools/probe-primary-sources.mjs`, `tools/probe-codex.mjs`, and `tools/probe-under-told-perspectives.mjs` lock the M4 claims. `civil_war_generals.html` rebuilt from source.
>
> **CONTRACT:** pure teaching read-out, **D74 byte-identical by construction**. No tactical, resolve, victory, save, OOB, or battle-output path changed. The pre-existing under-told perspective-liaison bridge remains exact-zero when inactive, capped/costly when active, and still adds no direct overall bonus. M8 battle-build remains blocked pending Aaron go/no-go.
>
> **VET (focused, D176):** JSON parse passed for primary sources/codex/women/under-told; `node tools/import-women-in-war.mjs` passed **records=9 verified=8 disputed=1**; `node --check` passed for touched JS/probes; build **GATE OK · no-fudge ✓ · citations ✓ · women-in-war ✓**; focused `probe-primary-sources` **12 steps, 0 pageerrors** and `probe-under-told-perspectives` **8 steps, 0 pageerrors**; adjacent `probe-codex` **20 steps, 0 pageerrors**, `probe-women-in-war` **8 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` deferred to the planned-work batch/release gate under D176.

## ⚡ CONTINUE HERE — 2026-07-01 **GRADUATE-HISTORY PASS · M2 READ THE DOCUMENT APPARATUS SHIPPED (D181).** M2 of the authorized graduate-history build-out is complete. It adds a pure-readout **Documents / Read the Document** desk surface from new `data/primary-sources.json` (`cw_primary_sources_v1`) and `src/68-primary-sources.js`, registered in the manifest and wired into both the base and H0 President's Desk tab lists. The first source deck has 8 `Verified` records with ≥2 sources each: Confederate self-justifications (Stephens Cornerstone, Mississippi declaration, Confederate Constitution, Hale to Magoffin, Cobb to Seddon) plus Union/Black counter-documents (Emancipation Proclamation service clause, Douglass "Men of Color, To Arms!", Gooding equal-pay letter). Confederate cards render **verbatim excerpt → catalyst frame → attribution**, with source critique and source locators visible. The dark `manpower-teaching.json` scholarship is now surfaced in `src/70-manpower.js` inside **The Ranks** as a compact consensus / scholarly dissent / Lost Cause counter / primary documents block; manpower math and replacement ratios were not changed. **NEXT: M3 — Reconstruction/memory** from `.tmp/civil-war-phd-execution-brief.md` / `.tmp/civil-war-history-audit-and-phd-plan.md`. Then M4 source-criticism + under-told voices + Native rewrite (with `tools/probe-under-told-perspectives.mjs` updated in the same commit), M5 keystone cards, M6 home-front, M7 lived-slavery. **M8 battle-build track still needs an Aaron go/no-go first.** Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D181):** new `data/primary-sources.json`, new `src/68-primary-sources.js`, manifest registration, `Documents` tab dispatch/button wiring in `src/30-president-shell.js` and `src/99-h0-president-desk.js`, richer manpower debate-card rendering in `src/70-manpower.js`, focused `tools/probe-primary-sources.mjs`, `probe-manpower` teaching assertion + local-Mac navigation/screenshot budget hardening, and future-batch suite enrollment in `tools/vet-no-regression.mjs`. `civil_war_generals.html` rebuilt from source.
>
> **CONTRACT:** pure teaching read-out, **D74 byte-identical by construction**. No tactical, bridge, resolve, victory, save, or combat path reads `GAME_DATA["primary-sources"]` / `_ps*`; the focused probe statically scans that wall. Every `Verified` record carries ≥2 sources and every excerpt is ≤60 words. Source notes identify transcription/normalization caveats instead of laundering them.
>
> **VET (focused, D176):** JSON parse for `data/primary-sources.json` + manifest; `node --check` passed for touched JS/probe/suite files; build **GATE OK · no-fudge ✓ · citations ✓**; focused `probe-primary-sources` **10 steps, 0 pageerrors**; adjacent `probe-manpower` **9 steps, 0 pageerrors**, `probe-codex` **18 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**; JSON readback clean; `git diff --check` clean. Full `npm run vet:noreg` deferred to the planned-work batch/release gate under D176.

## ⚡ CONTINUE HERE — 2026-06-30 **GRADUATE-HISTORY PASS · M1 CORRECTIONS & FABRICATION FIX SHIPPED (D180).** A new authorized multi-milestone content build-out (brief `.tmp/civil-war-phd-execution-brief.md`; audit source-of-truth `.tmp/civil-war-history-audit-and-phd-plan.md`; Native rewrite `.tmp/native-nations-under-told-dossier.md`) raises the game's history toward a graduate-seminar bar. **M1 (this commit)** ships the audit's Phase-1 corrections on a clean base: fixed the one integrity defect — the twenty-slave-law teaching card in `data/decisions.json` shipped untagged composite/padded/invented "perspectives" under `provenance:"Verified"`; now composites are tagged `[composite illustration — not a verbatim quotation]`, the Sam Watkins voice is trimmed to the verified 1882 *Co. Aytch* clause, and the invented first-person "enslaved" voice is replaced with a verbatim ≥2-source Cpl. James Henry Gooding 1863 excerpt (the first real Black voice in verified words on a player-facing card) — plus five metadata/internal-doc fixes (Erlanger 72→45% of face, Vicksburg date standardized to May 18, manpower CS-replacement-ratio fallback reframed as a model parameter, `confederate-greenbacks-inflation` codex id → `greenbacks-and-wartime-inflation` ×4 lockstep, Mississippi Declaration date → Jan. 9 1861) and the Cornerstone "corner-stone" spelling. Pure teaching read-out, **D74 byte-identical**, anti-Lost-Cause. **NEXT: M2 — the "Read the Document" primary-source apparatus** (new `data/primary-sources.json` + `src/NN-primary-sources.js` + `tools/probe-primary-sources.mjs`; seed verified Confederate self-justifications + Union/enslaved counter-docs, each Confederate doc = verbatim self-justification → one-line catalyst frame → attribution; AND wire the DARK `manpower-teaching.json` consensus/dissent/fringe/scholars cards through `src/70-manpower.js` via the `src/60-blockade.js` pattern). Then M3 Reconstruction/memory, M4 source-criticism + under-told voices + Native rewrite (updating `tools/probe-under-told-perspectives.mjs` in the same commit), M5 keystone cards, M6 home-front, M7 lived-slavery. **M8 battle-build track needs an Aaron go/no-go first.** Prior commit `52fb716` = D179 flagship named units (Codex). Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D180):** edits to `data/decisions.json`, `data/economy.json`, `data/vicksburg.json`, `data/codex.json`, `src/70-manpower.js`, `HISTORICAL-DATA.md`; `civil_war_generals.html` rebuilt from source. **LOAD-BEARING RULE:** every quotation is transcribed character-for-character from ≥2 reputable transcriptions with a repository+locator, ≤~60 words — never paraphrased-into-quotes or padded.
>
> **VET (focused, D176):** JSON parse decisions/economy/vicksburg/codex; `node --check` manpower; build **GATE OK · no-fudge ✓ · citations ✓**; focused `probe-decisions` **19/0**, `probe-economy` **8/0**, `probe-codex` **18/0** (greenbacks id cross-links resolve); adjacent `probe-manpower` **9/0**, `probe-vicksburg` **18/0**; all 0 pageerrors; `git diff --check` clean. Full `npm run vet:noreg` deferred to the end-of-queue batch/release gate.

## ⚡ CONTINUE HERE — 2026-06-30 **GROUP 4 · FLAGSHIP NAMED UNITS FOCUSED-GATED (D179).** Group 4's first four non-battle content items are complete: hard war / civilian protection (D175), irregular war / civilian security (D177), under-told perspectives (D178), and flagship named units (D179). Flagship units now exist as a War Effort teaching/system surface with exact-zero default bridge behavior and a capped explicit unit-stewardship priority; they do not create playable battles, rewrite tactical OOB, create super-units, or collapse Soldier's Story / D153 Women in the War lanes. Full `npm run vet:noreg` remains deferred to the end-of-queue planned-work release gate under D176. **Historical then-next marker:** Group 4 CS finance/toolkit was superseded by the D180-D185 graduate-history run; use the D185/D186 top block for the live next item and order. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D179):** new `data/flagship-units.json` (`cw_flagship_units_v1`), `src/67-flagship-units.js`, manifest registration, War Effort/H0 President's Desk rendering and wiring, bridge/lifecycle hooks, focused `tools/probe-flagship-units.mjs`, and future-batch suite enrollment. `civil_war_generals.html` was rebuilt from source.
>
> **CONTRACT:** default inactive flagship-unit bridge input returns exact zero. The active unit-stewardship priority is explicit and capped at morale +1, supply cost 1, fatigue cost 1, no direct overall bonus. The system records readiness, colors/support, and casualty-memory readouts for named units, but never writes winners, casualties, OOB totals, tactical state, scoreboards, or replacement registries. The 54th Massachusetts is visible now; Battery Wagner / USCT playable battles remain in the later Q6 lane.
>
> **VET:** JSON smoke passed for `data/flagship-units.json`; `node --check` passed for touched source/probe/suite files; build **GATE OK** with idempotent HTML rebuild from source; focused `probe-flagship-units` **8 steps, 0 pageerrors** with clean JSON readback and screenshot. Adjacent probes passed: `probe-under-told-perspectives` **8 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**. Focused probing caught and fixed a recursive readout bug: flagship snapshots now read stored morale instead of calling `moraleCompute()` from the bridge path. `git diff --check` clean. Full `npm run vet:noreg` remains deferred under D176.

## ⚡ EARLIER — 2026-06-30 **GROUP 4 · IRREGULAR WAR / CIVILIAN SECURITY FOCUSED-GATED (D177) + QUEUE-LOOP GATE CLARIFIED (D176).** Group 4's first two non-battle content items are complete: hard war / civilian protection (D175) and irregular war / civilian security (D177). Irregular war now exists as a War Effort teaching/system surface with exact-zero default bridge behavior and a capped explicit civilian-security priority. Aaron clarified that full `npm run vet:noreg` must wait for the end-of-queue planned-work release gate, not run after every queued item. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

> **WHAT SHIPPED (D177):** new `data/irregular-war.json`, `src/65-irregular-war.js`, manifest registration, War Effort/H0 President's Desk rendering and wiring, bridge/lifecycle hooks, focused `tools/probe-irregular-war.mjs`, and future-batch suite enrollment. `civil_war_generals.html` was rebuilt from source.
>
> **CONTRACT:** default inactive irregular-war bridge input returns exact zero. Active civilian security is explicit and capped at morale +1, supply cost 2, fatigue cost 2, no direct overall bonus. The ledger names partisan pressure, reprisals, civilian danger, protected civilians, and local intelligence while distinguishing Mosby's commissioned partisan rangers from Quantrill/Lawrence massacre violence, General Order No. 11, the Lieber Code, and Lost Cause romance.
>
> **VET:** JSON parse passed for `data/irregular-war.json`; `node --check` passed for touched source/probe/suite files; build **GATE OK**; focused `probe-irregular-war` **8 steps, 0 pageerrors** with clean JSON readback. Adjacent probes passed: `probe-hard-war` **8 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**. Full `npm run vet:noreg` remains deferred under D176.

> **WHAT SHIPPED (D175):** new `data/hard-war.json`, `src/64-hard-war.js`, manifest registration, War Effort/H0 President's Desk rendering and wiring, bridge/lifecycle hooks, focused `tools/probe-hard-war.mjs`, and future-batch suite enrollment. `civil_war_generals.html` was rebuilt from source.
>
> **CONTRACT:** default inactive hard-war protection returns exact zero. Active protection is explicit and capped at morale +1, supply cost 2, fatigue cost 1, no direct overall bonus. The ledger names property destruction, displacement, and freedpeople/refugee protection without making hard war clean or using civilian suffering as Lost Cause inversion.
>
> **VET:** JSON parse passed for `data/hard-war.json`; `node --check` passed for touched source/probe/suite files; build **GATE OK**; focused `probe-hard-war` **8 steps, 0 pageerrors** after catching/fixing missing H0 shell insertion. Adjacent probes passed: `probe-disease-medical` **8 steps, 0 pageerrors**, `probe-logistics-rail` **8 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-h0-president-desk` **3/3, 0 pageerrors**. A partial `vet:noreg` run was interrupted after Aaron clarified D176; it had no red output through render-richness but is not the D175 gate.

## ⚡ EARLIER — 2026-06-30 **PHASE H0 · BATCH/RELEASE GATE PASSED (D174).** Group 3 is complete. The completed H0 surface set is now included in `npm run vet:noreg`, the release checkpoint passed, and the only code changes were browser-probe/suite harness hardening. Group 4 is now active.

> **WHAT SHIPPED (D174):** `tools/vet-no-regression.mjs` now runs all six H0 probes after accessibility and passes Playwright's no-font-wait screenshot setting to child probes. `tools/probe-h0-president-desk.mjs` gets longer local-Mac screenshot/viewport grace. `tools/probe-production.mjs` now writes green JSON before bounded page/browser cleanup so the suite cannot hang after successful production assertions.
>
> **VET:** build **GATE OK**; importer/schema checks passed for soldier replacements, women in war, and Tripo slots; `node --check` passed for touched JS/probe files. Focused H0 probes passed: main menu **4/4**, President's Desk **3/3**, battle briefing **3/3**, between-battle **3/3**, tactical HUD **3/3**, after-action **3/3**, all 0 pageerrors. Adjacent `probe-desk`, `probe-accessibility`, `bootprobe`, and `diag-classic` passed.
>
> **FULL BATTERY:** first `npm run vet:noreg` segment passed through `probe-economy`; `probe-production` produced `ok=true` / 0 pageerrors but timed out during cleanup. After the cleanup fix, standalone `probe-production` passed, and `npm run vet:noreg -- --from=production` finished **VET NO-REGRESSION OK - 84 commands**. JSON scan: **95 files, bad=0**, no `ok=false`, no `FATAL`, no pageerrors, no realErrors; only known filtered 404 console noise in boot/classic artifacts.

## ⚡ EARLIER — 2026-06-30 **RATING SYSTEM / GM FOLLOW-UP · SYMMETRIC AI-GM SHADOW READOUT FOCUSED-GATED (D173).** Group 2's first bounded leftover is complete. The opponent now has a deterministic AI-GM command shadow over the current enemy roster, with Command/OOB readouts and bounded helper values. D174 later completed the Group 3 H0 batch/release gate.

> **WHAT SHIPPED (D173):** `data/ratings.json` adds `aiGm` tier calibration; `src/35-command.js` adds pure enemy-side command helpers and the **Enemy command shadow** readout; `src/tactical/T15-oob.js` uses the same shadow to name enemy army/corps commanders on the existing OOB board when derived. `civil_war_generals.html` was rebuilt from source.
>
> **LEFTOVER TRIAGE / GUARDS:** live code shows D113 already shipped the political-general/election-support relief bind, so no new election path was needed. `data/generals.json` still has no honest `theater` fields, so cross-theater Transfer remains blocked. D173 writes no enemy command save state, commissions no hidden officers, does not wire into `87-auto-resolve.js`, and never writes winners, casualties, OOB totals, or scoreboard outputs. The predicted force edge remains force-only.
>
> **VET:** JSON parse passed for ratings/generals; build **GATE OK** with `no-fudge ✓`; `node --check` passed for touched JS/probes. Focused `probe-command` passed **79 steps, 0 pageerrors** and `probe-oob` passed **17 steps, 0 pageerrors** after one cold-load timeout before evaluation. Adjacent probes passed: `probe-ratings` **21 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, and `probe-desk` **13 steps, 0 pageerrors**. JSON artifact inspection found no failed steps, no FATAL errors, and no page errors. Full battery remains deferred to the H0 batch/release gate.

## ⚡ EARLIER — 2026-06-30 **PHASE I · SIXTH CITATION-GRADE SOLDIER REPLACEMENT SLICE FOCUSED-GATED (D172).** Soldier's Story now has six sourced named-person replacements. Strong Vincent replaces generated Gettysburg brigade-command slot `ss:gettysburg:US:us_vincent_bde:cmd`, adding brigade-level Little Round Top context above the existing Chamberlain 20th Maine row. D173 later completed the first GM follow-up leftover.

## ⚡ EARLIER — 2026-06-30 **PHASE H2 · SKIPPABLE FIELD CUTAWAY FALLBACK SHELL FOCUSED-GATED (D170).** H2 has a provenance-gated pre-battle cutaway shell that uses existing embedded PD scene stills or local procedural map art; it adds no new battle, no licensed footage, no external fetch, and no runtime web dependency. D172 later completed the sixth Soldier's Story replacement slice.

> **WHAT SHIPPED (D170):** new `data/footage-cutaways.json` (`cw_h2_cutaways_v1`), new `src/104-h2-cutaways.js`, manifest registration after `103-h0-after-action.js`, focused `tools/probe-h2-cutaways.mjs`, and future-batch inclusion in `tools/vet-no-regression.mjs`. `civil_war_generals.html` was rebuilt from source.
>
> **ASSET / PROVENANCE GUARDS:** all video slots are disabled with `sourcePath:null`, `externalUrl:null`, and pending PD provenance. Existing PD-vetted scene stills back Bull Run, Antietam, Fredericksburg, Chancellorsville, Vicksburg, and Gettysburg; Shiloh, Malvern Hill, and Chickamauga use an explicitly Inferred procedural-map fallback. No external asset fetch, account/trial/download action, upload, stream, or unclear-license asset was used.
>
> **RUNTIME / CONTRACT:** the cutaway opens from the H0 battle briefing action row, is optional and skippable, and never launches `brgRealTime`/`brgToField`, mutates `battlePrep`, writes battle winners/casualties/OOB, changes save version, or touches the combat model. The focused probe statically guards no `<video>`, no `<iframe>`, no `fetch`, no external URLs, no combat/output/save contamination, and no `window.*` global export.
>
> **VET:** syntax checks passed for `src/104-h2-cutaways.js`, `tools/probe-h2-cutaways.mjs`, and `tools/vet-no-regression.mjs`; schema smoke printed `cw_h2_cutaways_v1 9`; build **GATE OK**; focused `probe-h2-cutaways` **5 steps, 0 pageerrors** with clean JSON readback and visible overlay/card/frame metrics. Adjacent probes passed: `probe-scenes-imagery` **16/16 OK**, `probe-bridge` **6 steps, 0 pageerrors**, `probe-h0-battle-briefing` **3/3, 0 pageerrors**, and `probe-accessibility` **25 steps, 0 pageerrors**. Full battery remains deferred to the planned-work batch/release gate.

## ⚡ EARLIER — 2026-06-30 **PHASE F · DISEASE / ARMY MEDICINE WAR EFFORT SYSTEM FOCUSED-GATED (D169).** Disease and army medicine are live as a bounded War Effort system after the completed H0 surface pass. D170 later completed the H2 cutaway fallback shell.

> **WHAT SHIPPED (D169):** new `data/disease-medical.json` (`cw_disease_medical_v1`), new `src/63-disease-medical.js`, manifest registration after POW exchange, War Effort rendering/wiring in `src/20-president-render.js`, `src/30-president-shell.js`, and `src/99-h0-president-desk.js`, bridge integration in `src/85-battle-bridge.js`, lifecycle registration in `src/90-president-register.js`, focused `tools/probe-disease-medical.mjs`, and no-regression-suite inclusion. `civil_war_generals.html` was rebuilt from source.
>
> **SOURCE / HISTORY GUARDS:** existing repo source trails were enough; no web fetch was needed. The module teaches disease deaths, the Letterman evacuation system, sanitation, anesthesia/amputation practice, the U.S. Sanitary Commission, Barton/Dix/Walker relief and nursing labor, and medical limits. The framing rejects clean-war mythmaking and Lost Cause scarcity romance.
>
> **RUNTIME / BRIDGE:** War Effort now renders **Disease & Army Medicine** with sickness, wounds, treated men, disease deaths, pressure, side profile, practice/debate cards, and a **Prioritize medical relief** toggle. Default inactive medical relief returns exact zero. Active relief is explicit and capped at morale +2, fatigue relief 3, supply cost 2, and overall +1. It can mitigate sickness/wounds and active Campaign Kit disease/fatigue; it cannot write battle winners, casualty outputs, OOB, scenario data, or a scoreboard path. `C.medical` is additive/sanitized with no save-version bump.
>
> **VET:** syntax checks passed for touched JS/probe files; schema smoke printed `cw_disease_medical_v1 4 2`; build **GATE OK**; focused `probe-disease-medical` **8 steps, 0 pageerrors** with clean JSON readback. Adjacent probes passed: `probe-prisoner-exchange` **8 steps, 0 pageerrors** after a 90s screenshot-timeout harness fix, `probe-logistics-rail` **8 steps, 0 pageerrors**, `probe-manpower` **9 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, `probe-h0-president-desk` **3/3, 0 pageerrors**, and `probe-desk` **13 steps, 0 pageerrors**. Full battery remains deferred to the planned-work batch/release gate.

## ⚡ EARLIER — 2026-06-30 **PHASE H0 · AFTER-ACTION / FINAL REPORT COMMAND-SHELL FOCUSED-GATED (D168).** The H0 redesign now covers all ordered surfaces: main menu, President's Desk overview, campaign bridge briefing / side choice, strategic-turn interstitial, tactical HUD/settings, and after-action/final report. Next checkpoint: run the H0 batch/release gate if Aaron wants the full suite now, or continue to the next roadmap slice under the D160/D161 focused-gate policy.

> **WHAT SHIPPED (D168):** new `src/103-h0-after-action.js`, registered after the tactical HUD pass, wraps `aarRenderReport(C, opts)` and adds a dark command report shell around the existing report body. It preserves the D112/D119 report/final-screen contracts (`warWonScreen`, `#wwReport`, `#wwMainMenu`, `G.campaign=null`), the grade formulas and Reconstruction copy in `src/82-after-action.js`, the compact alternate-ending embed from `src/83-endings.js`, the D151 Soldier's Story tie-in, and D120 glossary decoration.
>
> **PROBE / VISUAL:** new `tools/probe-h0-after-action.mjs` covers both the live After-Action tab and final report at desktop/tablet/phone sizes, with screenshots for each. It guards no broadsheet shell font/class, command-grid texture, dark shell, summary chips/metrics, preserved report-card/divergence/endings/Soldier's Story content, glossary decoration and visible focus, final Main Menu wiring, campaign nullification, no horizontal overflow, and pageerrors. Phone screenshot review caught a probe-state issue where the glossary tooltip stayed open after the focus assertion; the probe now hides it before capture.
>
> **GUARDS:** no hand edit to `build/base.html`; generated `civil_war_generals.html` was rebuilt from source. D168 is presentation-only around an existing pure read-out: no grade formula, ending predicate, Soldier's Story data, codex/glossary lookup, save version, campaign outcome, combat model, scenario data, OOB, or balance number changed.
>
> **VET:** `node --check` passed for `src/103-h0-after-action.js`, `tools/probe-h0-after-action.mjs`, `src/82-after-action.js`, `src/83-endings.js`, and adjacent probe files. `node tools/build.mjs` printed **GATE OK**. Focused `probe-h0-after-action` passed **3/3, 0 pageerrors** with desktop/tablet/phone live-report and final-report screenshots. Adjacent probes passed: `probe-afteraction` **15 steps, 0 pageerrors**, `probe-endings` **24 steps, 0 pageerrors**, `probe-accessibility` **25 steps, 0 pageerrors**, `probe-glossary` **12 steps, 0 pageerrors**, optional `probe-codex` **18 steps, 0 pageerrors** with extended 8 GB local-Mac grace, `bootprobe` `ok=true` with `realErrors:[]`, `t1probe` **14 steps, 0 pageerrors**, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Full no-regression was not run under the current focused-gate policy.
>
> **NEXT RECOMMENDED RUN:** H0 batch/release gate if Aaron wants the full suite at this completed-H0 checkpoint; otherwise continue the ordered roadmap from `V1-CHECKLIST.md`.

## ⚡ EARLIER — 2026-06-30 **PHASE H0 · TACTICAL HUD / SETTINGS COMMAND-SHELL FOCUSED-GATED (D167).** The H0 redesign covers main menu, President's Desk overview, campaign bridge briefing / side choice, strategic-turn interstitial, and tactical HUD/settings. D168 later completed after-action/final report.

> **WHAT SHIPPED (D167):** new `src/102-h0-tactical-hud.js`, registered after the between-battle pass, late-wraps `fldBuildDom`/`fldRenderTop`, assignment-overrides `fldBar`, and assignment-overrides `_fldDrawerHTML` while preserving the tactical DOM ids, click handlers, hotkeys, fog/auto-pause toggles, settings drawer wiring, speed/elevation controls, selected-unit HUD path, side-relative friend/foe behavior, and tactical sim path. The field now uses a dark command-game shell with compact top status, selected-unit meters, command controls, a matching settings drawer, terrain-key styling, desktop/tablet/phone layout, visible focus, high-contrast compatibility, and phone-safe controls.
>
> **FIXES / HARNESS:** the focused probe initially caught HUD/control overlap on desktop/tablet and a false broadsheet-font assertion that treated `sans-serif` as serif. Screenshot review then caught terrain-key/control collision on tablet/phone and a phone state where focusing Settings hid the primary play control. The CSS and probe now guard HUD/control/key overlap, terrain-key font inheritance, primary control visibility before focus, and desktop drawer wiring. Assertions were strengthened, not weakened.
>
> **GUARDS:** no hand edit to `build/base.html`; generated `civil_war_generals.html` was rebuilt from source. D167 is a presentation-only field shell: no combat model, scenario data, OOB, save version, casualty/outcome path, or balance number changed.
>
> **VET:** `node --check` passed for `src/102-h0-tactical-hud.js` and `tools/probe-h0-tactical-hud.mjs`; `git diff --check` passed. `node tools/build.mjs` printed **GATE OK**. Focused `probe-h0-tactical-hud` passed **3/3, 0 pageerrors** with desktop/tablet/phone screenshots, selected-meter checks, preserved controls, visible focus, no broadsheet shell font including the terrain key, no HUD/control/key overlap, primary play-control visibility, and desktop drawer wiring checks. Adjacent probes passed: `probe-field` **13 steps, 0 pageerrors**, `probe-presets` **26 steps, 0 pageerrors**, `probe-campaign-link` **16 steps, 0 pageerrors**, `probe-csplayer` **16 steps, 0 pageerrors**, `probe-accessibility` **25 steps, 0 pageerrors**, `probe-h0-battle-briefing` **3/3, 0 pageerrors**, `bootprobe` `ok=true` with `realErrors:[]`, `t1probe` `ok=true` with `errors:[]` / `pageerrors:[]`, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Full no-regression was not run under the current batch-gate policy.
>
> **NEXT STATUS:** D168 later completed after-action/final report. H0's ordered surface pass is complete; run the H0 batch/release gate or `npm run vet:noreg` if Aaron asks for the full suite.

## ⚡ EARLIER — 2026-06-30 **PHASE H0 · BETWEEN-BATTLE INTERSTITIAL COMMAND-SHELL FOCUSED-GATED (D166).** The H0 redesign covers main menu, President's Desk overview, campaign bridge briefing / side choice, and the strategic-turn interstitial. D167 later completed tactical HUD/settings and D168 later completed after-action/final report.

> **WHAT SHIPPED (D166):** new `src/101-h0-between-battle.js`, registered after the battle-briefing pass, assignment-overrides `_pdInterstitialHTML` while preserving `pdGoDesk`, `pdGoBrief`, `pdGoOn`, `pdConcludeWar`, decision cards, `_brgArmySummaryHTML`, the D119 strategic-end offer, and existing `_pdShowTurnInterstitial` wiring. The between-battle surface is now a dark "Strategic Turn" command shell with next-field imagery, turn/side/next chips, treasury/battle/year metrics, latest dispatches, army-condition summary, strategic-end offer panel, responsive desktop/tablet/phone layout, visible focus, and high-contrast compatibility.
>
> **FIXES / HARNESS:** screenshot review caught a real D166 UI regression where D119 offer copy rendered literal `&mdash;` text. The module now escapes general text while preserving that existing author-controlled entity, and `tools/probe-h0-between-battle.mjs` asserts `noLiteralEntity`. Local Chrome also exposed the known full-`load` false-red in adjacent probes, so `tools/probe-desk.mjs` and `tools/probe-afteraction.mjs` now wait for `domcontentloaded`; desk screenshots also have explicit 90s timeouts. Assertions stayed intact.
>
> **GUARDS:** no hand edit to `build/base.html`; generated `civil_war_generals.html` was rebuilt from source. The new surface avoids `.gn-paper` and broadsheet shell typography while keeping broadsheet valid for press/editorial/dispatch/provenance surfaces. No combat model, scenario data, OOB, save version, or battle outcome path changed.
>
> **VET:** `node --check` passed for `src/101-h0-between-battle.js`, `tools/probe-h0-between-battle.mjs`, `tools/probe-desk.mjs`, and `tools/probe-afteraction.mjs`. `node tools/build.mjs` printed **GATE OK**. Focused `probe-h0-between-battle` passed **3/3, 0 pageerrors** with desktop/tablet/phone screenshots, interstitial wiring checks, strategic-offer checks, and `noLiteralEntity:true`. Adjacent probes passed: `probe-desk` **13 steps, 0 pageerrors**, `probe-afteraction` **15 steps, 0 pageerrors**, `probe-bridge` **6 steps, 0 pageerrors**, `probe-accessibility` **25 steps, 0 pageerrors**, `probe-h0-battle-briefing` **3/3, 0 pageerrors**, `probe-h0-president-desk` **3/3, 0 pageerrors**, `probe-h0-main-menu` **4/4, 0 pageerrors**, `bootprobe` `ok=true` with `realErrors:[]`, `t1probe` `ok=true`, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Full no-regression was not run under the current batch-gate policy.
>
> **NEXT STATUS:** D167 later completed tactical HUD/settings and D168 later completed after-action/final report. H0's ordered surface pass is complete; run the H0 batch/release gate or `npm run vet:noreg` if Aaron asks for the full suite.

## ⚡ EARLIER — 2026-06-30 **PHASE H0 · BATTLE BRIEFING / SIDE-CHOICE COMMAND-SHELL FOCUSED-GATED (D165).** The H0 redesign covers main menu, President's Desk overview, campaign bridge briefing, and tactical side choice. D166 later completed the between-battle interstitial.

> **WHAT SHIPPED (D165):** new `src/100-h0-battle-briefing.js`, registered after the Desk pass, assignment-overrides `bridgeBriefingHTML` and `fldScenarioSideChoice` while preserving the bridge ids (`brgBack`, `brgAuto`, `brgRealTime`, `brgToField`, `brg_*` prep toggles), `bridgeWireBriefing`, `data-brside` callbacks, the scene-imagery `scene-img` contract, `fldMatchupHtml`, and existing launch/return paths. The bridge briefing is now a dark command surface with field photo, visible launch controls, army meters, prep orders, responsive desktop/tablet/phone layout, visible focus, and high-contrast compatibility. The side-choice sheet is now a matching command surface with two side cards, objective/attacker chips, matchup board, clean role badges, and preserved back/callback behavior.
>
> **HARNESS REPAIRS:** local Chrome again exposed setup/capture false-reds, so the run repaired probes without weakening assertions: `tools/probe-h0-battle-briefing.mjs` uses explicit 90s screenshot timeouts, `tools/probe-bridge.mjs` waits for `domcontentloaded` and gives screenshots 90s, and `tools/bootprobe.mjs` waits for `domcontentloaded` plus explicit screenshot timeout. The app assertions remain intact.
>
> **GUARDS:** no hand edit to `build/base.html`; generated `civil_war_generals.html` was rebuilt from source. The new H0 surfaces avoid `.gn-paper`, broadsheet shell typography, and old glyph-first card language while keeping broadsheet available for press/editorial/dispatch/provenance surfaces. No combat model, scenario data, OOB, save version, or battle outcome path changed.
>
> **VET:** `node --check` passed for `src/100-h0-battle-briefing.js`, `tools/probe-h0-battle-briefing.mjs`, `tools/probe-bridge.mjs`, and `tools/bootprobe.mjs`. `node tools/build.mjs` printed **GATE OK**. Focused `probe-h0-battle-briefing` passed **3/3, 0 pageerrors** with desktop/tablet/phone screenshots and side-choice screenshot inspection. Adjacent probes passed: `probe-bridge` **6 steps, 0 pageerrors**, `probe-campaign-link` **16 steps, 0 pageerrors**, `probe-tactical-roster` **8/8**, `probe-scenes-imagery` **16/16**, `probe-accessibility` **25 steps, 0 pageerrors**, `probe-h0-main-menu` **4/4, 0 pageerrors**, `probe-h0-president-desk` **3/3, 0 pageerrors**, `probe-auto-resolve` **7 steps, 0 pageerrors**, `bootprobe` `ok=true` with `realErrors:[]`, `t1probe` `ok=true`, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Full no-regression was not run under the current batch-gate policy.
>
> **NEXT STATUS:** D166 later completed the between-battle interstitial, D167 later completed tactical HUD/settings, and D168 later completed after-action/final report. H0's ordered surface pass is complete; run the H0 batch/release gate or `npm run vet:noreg` if Aaron asks for the full suite.

## ⚡ EARLIER — 2026-06-30 **PHASE H0 · PRESIDENT'S DESK OVERVIEW COMMAND-SHELL FOCUSED-GATED (D164).** The interrupted dirty-tree WIP after the CPU death has been recovered, vetted, repaired where needed, documented, and finished. The H0 redesign now covers both the main menu and the President's Desk overview; D165 later completed battle briefing/side choice.

> **WHAT SHIPPED (D164):** new `src/99-h0-president-desk.js`, registered after `98-h0-main-menu.js`, overrides `openWarDept` and `presRenderEconomy` by assignment while preserving the existing Desk tab ids, click paths, close callback, `_wdTab`/`_wdRefresh` dispatch contract, and existing tab renderers. The War Effort overview now uses a dark command-center shell with executive status, next-field imagery, resource cards, status meters, dispatches, responsive desktop/tablet/phone layout, visible focus rings, high-contrast compatibility, and preserved rail/POW/manpower/morale/production deep blocks below the overview.
>
> **INTERRUPTION RECOVERY / HARNESS REPAIRS:** the dirty worktree already contained the D164 source/probe/generated WIP. Recovery preserved it, then fixed two false-red local harness problems without weakening assertions: `tools/probe-h0-main-menu.mjs` keeps screenshots mandatory but raises screenshot timeout from 12s to 30s, and `tools/probe-accessibility.mjs` waits for `domcontentloaded` instead of full `load`.
>
> **GUARDS:** no hand edit to `build/base.html`; generated `civil_war_generals.html` was rebuilt from source. The H0 desk shell avoids `.gn-paper` and broadsheet shell typography, but press/editorial/dispatch/provenance surfaces can still use broadsheet treatments under D145/D163/D164. Existing Desk tabs still wire through their old ids.
>
> **VET:** `node --check` passed for `src/99-h0-president-desk.js`, `tools/probe-h0-president-desk.mjs`, `tools/probe-h0-main-menu.mjs`, and `tools/probe-accessibility.mjs`. `node tools/build.mjs` printed **GATE OK**. Focused `probe-h0-president-desk` passed **3/3, 0 pageerrors** with desktop/tablet/phone screenshots and JSON readback clean. Adjacent probes passed: `probe-desk` **13 steps, 0 pageerrors**, `probe-h0-main-menu` **4/4, 0 pageerrors**, `probe-accessibility` **25 steps, 0 pageerrors**, `bootprobe` `ok=true` with `realErrors:[]`, `t1probe` `ok=true`, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Full no-regression was not run under the current batch-gate policy.
>
> **NEXT STATUS:** D165 later completed battle briefing/side choice, D166 later completed the between-battle interstitial, D167 later completed tactical HUD/settings, and D168 later completed after-action/final report. H0's ordered surface pass is complete.

## ⚡ EARLIER — 2026-06-29 **PHASE H0 · MAIN MENU COMMAND-SHELL PROTOTYPE FOCUSED-GATED (D163).** The main menu now starts the H0 UI/UX redesign with a modern command-game shell instead of a broadsheet app-shell. Aaron selected main menu first, focused redesign, prototype scope, and clarified that the broadsheet-look fidelity should be removed in favor of a more assertive modern war-video-game layout.

> **WHAT SHIPPED (D163):** new `src/98-h0-main-menu.js`, registered after `97-accessibility.js`, overrides `openMainMenu` by assignment while preserving the existing menu ids, click paths, saved-campaign path, new-campaign path, free-battle picker path, settings/load path, and injection anchors. The shell uses dark command panels, status chips, embedded scene imagery, compact badge controls, responsive desktop/tablet/phone layout, visible focus rings, high-contrast compatibility, and reduce-motion-safe transitions. Existing injected save/help/playstyle/accessibility/tactical controls keep their ids/handlers and are visually normalized inside the H0 wrapper.
>
> **GUARDS:** no hand edit to `build/base.html` or `civil_war_generals.html`; generated HTML was rebuilt from source. The H0 menu deliberately avoids `.gn-paper`, the broadsheet serif stack, and old visible sword-glyph treatment. Broadsheet remains available for press/editorial/dispatch/provenance surfaces under D145/D163.
>
> **VET:** `node --check src/98-h0-main-menu.js` and `node --check tools/probe-h0-main-menu.mjs` passed. `node tools/build.mjs` printed **GATE OK**. Focused `probe-h0-main-menu` passed **4/4, 0 pageerrors** with desktop/tablet/phone screenshots and JSON readback clean, including core action checks for Union/Confederate muster, free-battle picker, and saved-campaign President's Desk path. Adjacent probes passed: `menuprobe`, `probe-accessibility` **25 steps, 0 pageerrors**, `bootprobe` `ok=true` with `realErrors:[]`, and `diag-classic` `nonBlank:346`. Full no-regression was not run under the current batch-gate policy.
>
> **NEXT STATUS:** D164 later completed the President's Desk overview, D165 later completed battle briefing/side choice, D166 later completed the between-battle interstitial, D167 later completed tactical HUD/settings, and D168 later completed after-action/final report. H0's ordered surface pass is complete.

## ⚡ EARLIER — 2026-06-29 **PHASE H · MODERN UG:G UNIT-ASSET SLOTS + FORMATION FIGURES FOCUSED-GATED (D162).** The modern battlefield now has two new additive visual layers: local-only Tripo-compatible GLB/GLTF unit asset slots and procedural infantry formation figures. Tripo is not called at runtime; the canonical slots are disabled until optimized local files, detailed/Ultra source metadata, and license/provenance proof land. Formation figures are procedural and replace slabs only on high/rich render tiers. The full battery is deferred by the gate-batching update above. H0 later ran D163 main menu, D164 President's Desk, D165 battle briefing/side choice, D166 between-battle interstitial, D167 tactical HUD/settings, and D168 after-action/final report; the ordered H0 surface pass is complete.

> **WHAT SHIPPED (D162):** new `data/tripo-unit-assets.json`, `src/tactical/T23-tripo-unit-assets.js`, `src/tactical/T24-formation-figures.js`, `tools/import-tripo-unit-assets.mjs`, `tools/probe-tripo-unit-assets.mjs`, `tools/probe-formation-figures.mjs`, and asset-folder guidance in `assets/3d/README.txt`. `src/00-manifest.json` registers T23/T24 after terrain readability and before register modules. `tools/vet-no-regression.mjs` includes the importer/probes for the later batch battery. `tools/probe-render-richness.mjs` gained a scene timeout guard so render-richness failures report cleanly instead of stalling.
>
> **GUARDS:** Tripo/browser/site use, credit spend, uploads, Generate clicks, or prompt entry still require explicit Aaron approval. Runtime integration remains local-files-only. Canonical Tripo source slots require `geometryQuality=detailed`, `textureQuality=detailed`, `pbr=true`, `smartLowPoly=true`, and `ultraSourcePath`; all eight canonical slots are `enabled:false` with pending license fields. Low/rich-off tiers restore existing marker fallbacks.
>
> **VET:** syntax checks passed for T23, T24, importer, both probes, and the render-richness support edit. `node tools/import-tripo-unit-assets.mjs` passed with `records=8 enabled=0 detailedSlots=8 filesPresent=0 pendingLicense=8`. `node tools/build.mjs` printed **GATE OK**. Focused probes passed: `probe-formation-figures` **16/16, 0 pageerrors** and `probe-tripo-unit-assets` **15/15, 0 pageerrors**, with JSON readback clean and screenshots captured.

## ⚡ CONTINUE HERE — 2026-06-29 **PHASE F · POW EXCHANGE CARTEL COLLAPSE FOCUSED-GATED (D161).** Prisoner exchange collapse is now a bounded War Effort system: cartel function, detained burden, camp mortality risk, the 1863 USCT fault line, and a capped opt-in relief priority. It does not reopen the cartel, create a parallel campaign map, touch `build/base.html`, bump `_SAVE_VER`, or start Chattanooga/USCT playable battles. The full battery is deferred by the gate-batching update above. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D161):** new `data/prisoner-exchange.json` (`cw_prisoner_exchange_v1`), new `src/62-prisoner-exchange.js`, manifest registration, War Effort overview rendering/wiring, battle-bridge prisoner-relief inputs, lifecycle registration, focused `tools/probe-prisoner-exchange.mjs`, and no-regression-suite inclusion. Rebuilt `civil_war_generals.html` from `src/`; `build/base.html` stayed untouched.
>
> **SOURCE / HISTORY GUARDS:** existing repo source trails were enough; no web fetch was needed. The module teaches the Dix-Hill cartel, Confederate threats against captured Black soldiers, Lincoln's retaliation order, camp overcrowding, Andersonville, and Elmira. The copy rejects false equivalence: prisoner suffering on both sides is named, but cartel collapse is tied to slavery, Black soldier status, and Confederate policy.
>
> **RUNTIME / BRIDGE:** War Effort now renders a **Prisoner Exchange & Camps** block with pressure, exchange function, detained burden, camp mortality risk, side policy, timeline, camp notes, and source/debate cards. `prisonerExchangeBridgeBonus(C)` returns exact zero by default; the explicit **Prioritize relief** toggle can add at most morale +2, fatigue relief 2, supply cost 2, and overall +1. Relief can mitigate deaths/returns and add a small returned-prisoner manpower boost, but it cannot reopen the cartel. `C.prisoners` is additive/sanitized with no save-version bump.
>
> **VET:** syntax checks passed for touched JS/probe files; build **GATE OK**; importer/schema checks passed; focused `probe-prisoner-exchange` **8/8, 0 pageerrors** with clean JSON readback and corrected screenshot readback; adjacent probes passed for logistics rail **8/8**, manpower **9/9**, bridge **6/6**, and desk **13/13**. A full no-regression run was started and passed through `probe-divergence` with all completed probes `ok=true` and 0 pageerrors, then Aaron explicitly stopped the battery and deferred it until the planned-work batch/release gate.

## ⚡ CONTINUE HERE — 2026-06-29 **PHASE F · STRATEGIC LOGISTICS / RAIL NETWORK COMPLETE (D159).** Rail and supply now show up as strategic arteries in the War Effort screen without creating a parallel campaign map. The new system is citation-grade, default-safe, save-compatible, and bounded: inactive bridge effect is exact zero; active railhead prioritization has visible caps and rides existing production/engineering/blockade/War Room inputs. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D159):** new `data/logistics-rail.json` (`cw_logistics_rail_v1`), new `src/61-logistics-rail.js`, manifest registration, War Effort overview rendering/wiring, battle-bridge logistics inputs, lifecycle registration, focused `tools/probe-logistics-rail.mjs`, and no-regression-suite inclusion. Rebuilt `civil_war_generals.html` from `src/`; `build/base.html` stayed untouched.
>
> **SOURCE / HISTORY GUARDS:** existing repo source trails were enough; no web fetch was needed. The rail profile teaches the U.S. advantage in track mileage, equipment, repair capacity, and the USMRR/Haupt-style military railroad practice against Confederate fragmented companies, gauge breaks, equipment scarcity, iron-replacement collapse, and decaying service. The copy frames Confederate logistics as political economy and material constraint, not Lost Cause romance.
>
> **RUNTIME / BRIDGE:** War Effort now renders a **Rail & Supply Network** block with rail strength, repair tempo, depot reach, bottlenecks, route notes, and source/debate cards. Inputs are existing seams: production rail/food, blockade import factor, engineering construction/pontoons, War Room supply/nodes, next-battle route/theater. `logisticsBridgeBonus(C)` returns exact zero by default; the explicit **Prioritize railheads** toggle can add at most supply +7, fatigue relief 5, and overall +2. `C.logistics` is additive/sanitized with no save-version bump.
>
> **VET:** syntax checks passed for touched JS/probe files; build **GATE OK**; focused `probe-logistics-rail` **8/8, 0 pageerrors**; adjacent probes passed for production **4/4**, engineering **9/9**, bridge **6/6**, blockade **11/11**, manpower **9/9**, desk **13/13**, and Classic `nonBlank:346`. Full no-regression completed in resumed segments because D159 touched manifest/lifecycle/render/bridge surfaces: the first full run passed through `flags`; `probe-tactical-visuals` was green but crossed the old 420s wrapper cap, so `tools/vet-no-regression.mjs` now budgets 600s for it; resumed `tactical visuals` passed **ok=true, 0 pageerrors** at 478.8s; a transient Gettysburg load timeout cleared with standalone `probe-gettysburg` **16/16, 0 pageerrors** and the final `--from="gettysburg"` tail ended **VET NO-REGRESSION OK — 9 commands**, including `probe-full-campaign` **4/4, 0 pageerrors** and `diag-classic` `nonBlank:346`, `m3dActive:false`.

## ⚡ EARLIER — 2026-06-29 **PHASE I · FIFTH CITATION-GRADE SOLDIER REPLACEMENT SLICE COMPLETE (D158).** Soldier's Story now has five sourced named-person replacements: Elisha Hunt Rhodes, William McCarter, Sam R. Watkins, Joshua L. Chamberlain, and Alonzo H. Cushing. Cushing replaces generated Gettysburg Battery A command slot `ss:gettysburg:US:us_cushing_battery:cmd`. The canonical replacement pack now has 5 `Verified` records; the Women in the War lane remains separate at 7 records (6 Verified + 1 Disputed Velazquez). No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D158):** `data/soldier-replacements.json` now contains `person_gettysburg_us_battery_a_cushing` with rank `1st Lt.`, side `US`, Army of the Potomac / II Corps / Artillery Brigade / Battery A, 4th U.S. Artillery context, neutral Inferred persona calibration, `Verified` provenance, 3 source records, bio/source note, and no portrait metadata. Rebuilt `civil_war_generals.html` from source.
>
> **SOURCE / HISTORY GUARDS:** NPS places Battery A, 4th U.S. Artillery at the Angle on Cemetery Ridge during Pickett's Charge and names 1st Lt. Alonzo Cushing as its commander. Army.mil identifies Cushing as first lieutenant and commander of Battery A, 4th U.S. Artillery, II Corps, Army of the Potomac, killed July 3, 1863. The Congressional Medal of Honor Society corroborates rank, unit, action date/place, and the Medal of Honor citation trail. The record asserts no portrait/license claim, and the bio avoids a one-man-hero framing.
>
> **RUNTIME / PROBE:** Soldier detail cards render Cushing's sourced bio, source note, source disclosure, Battery A / 4th U.S. Artillery / II Corps / Pickett's Charge / the Angle context, and no portrait block. The replacement overlay preserves 603 registry rows and shifts raw counts 22 authored / 581 generated → 27 authored / 576 generated. `probe-loot-survival` asserts the raw-empty baseline, all five canonical records, Cushing's old-id alias lookup, rank/unit/battery/corps honesty, no-portrait honesty, hostile-pack rejection, and sourced UI rendering.
>
> **VET:** soldier replacement importer `records=5 verified=5 disputed=0`; women importer `records=7 verified=6 disputed=1`; build **GATE OK**; focused `probe-loot-survival` **11/11, 0 pageerrors**. The literal full `npm run vet:noreg` completed **VET NO-REGRESSION OK — 82 commands**, including `probe-loot-survival`, `probe-women-in-war`, `tactical roster`, `custom battle builder`, all battle probes, `sweep-all-battles`, `probe-full-campaign` **4/4, 0 pageerrors**, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Existing known noise only: the embed soft-budget warning and Classic's two tolerated 404 console lines.

## ⚡ EARLIER — 2026-06-29 **PHASE I · FOURTH CITATION-GRADE SOLDIER REPLACEMENT SLICE COMPLETE (D157).** Soldier's Story now has four sourced named-person replacements: Elisha Hunt Rhodes, William McCarter, Sam R. Watkins, and Joshua L. Chamberlain. Chamberlain replaces generated Gettysburg command slot `ss:gettysburg:US:us_20th_maine:cmd`. The canonical replacement pack now has 4 `Verified` records; the Women in the War lane remains separate at 7 records (6 Verified + 1 Disputed Velazquez). No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D157):** `data/soldier-replacements.json` now contains `person_gettysburg_us_20me_chamberlain` with rank `Col.`, side `US`, Army of the Potomac / V Corps / First Division / Vincent's Brigade / 20th Maine Infantry context, neutral Inferred persona calibration, `Verified` provenance, 4 source records, bio/source note, and no portrait metadata. Rebuilt `civil_war_generals.html` from source.
>
> **SOURCE / HISTORY GUARDS:** NPS order-of-battle data places the 20th Maine Infantry under Col. Joshua L. Chamberlain in Strong Vincent's Third Brigade, First Division, V Corps at Gettysburg. NPS Little Round Top material identifies the regiment's extreme-left position, low-ammunition crisis, and bayonet charge. NPS Civil War Soldiers and Sailors places the 20th Maine in the Gettysburg campaign and battle. American Battlefield Trust corroborates Chamberlain leading the 20th Maine in the broader defense of Little Round Top. The record asserts no company and no portrait/license claim, and the bio avoids a one-man-hero framing.
>
> **RUNTIME / PROBE:** Soldier detail cards render Chamberlain's sourced bio, source note, source disclosure, 20th Maine / Vincent / V Corps / Little Round Top / bayonet-charge context, and no portrait block. The replacement overlay preserves 603 registry rows and shifts raw counts 22 authored / 581 generated → 26 authored / 577 generated. `probe-loot-survival` asserts the raw-empty baseline, all four canonical records, Chamberlain's old-id alias lookup, no-company/no-portrait honesty, hostile-pack rejection, and sourced UI rendering.
>
> **VET:** soldier replacement importer `records=4 verified=4 disputed=0`; women importer `records=7 verified=6 disputed=1`; build **GATE OK**; focused `probe-loot-survival` **11/11, 0 pageerrors**. The literal full `npm run vet:noreg` completed **VET NO-REGRESSION OK — 82 commands**, including `probe-loot-survival`, `probe-women-in-war`, `tactical roster`, `custom battle builder`, all battle probes, `sweep-all-battles`, `probe-full-campaign` **4/4, 0 pageerrors**, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Existing known noise only: the embed soft-budget warning and Classic's two tolerated 404 console lines.

## ⚡ EARLIER — 2026-06-29 **PHASE I · THIRD CITATION-GRADE SOLDIER REPLACEMENT SLICE COMPLETE (D156).** Soldier's Story now has three sourced named-person replacements: Elisha Hunt Rhodes replaces `ss:bullrun1:US:us_burnside:pvt`, William McCarter replaces `ss:fredericksburg:US:us_irish:pvt`, and Sam R. Watkins replaces generated Chickamauga Confederate private slot `ss:chickamauga:CS:cs_cheatham_woods:pvt`. The canonical replacement pack now had 3 `Verified` records; the Women in the War lane remained separate at 7 records (6 Verified + 1 Disputed Velazquez). No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D156):** `data/soldier-replacements.json` now contains `person_chickamauga_cs_1tn_watkins` with rank `Private`, side `CS`, Army of Tennessee / Polk's Wing / Cheatham's Division / Maney's Brigade / 1st/27th Tennessee Infantry / Company H, 1st Tennessee Infantry context, neutral Inferred persona calibration, `Verified` provenance, 3 source records, bio/source note, and no portrait metadata. Rebuilt `civil_war_generals.html` from source.
>
> **SOURCE / HISTORY GUARDS:** NPS identifies Watkins as a private in Company H, First Tennessee Infantry, states the First Tennessee fought both days at Chickamauga, and explicitly cautions that *Co. Aytch* omits slavery from the war narrative. Project Gutenberg supplies the memoir/title trail for *"Co. Aytch," Maury Grays, First Tennessee Regiment*. NPS Civil War unit details identify the battlefield regiment as the consolidated 1st/27th Tennessee, assigned to Maney's Brigade, Army of Tennessee, and participating at Chickamauga. The record asserts the game slot's modeled Cheatham's Division / Polk's Wing context, but no portrait/license claim.
>
> **RUNTIME / PROBE:** Soldier detail cards render Watkins's sourced bio, source note, source disclosure, 1st/27th Tennessee / Company H / Maney / Cheatham / Chickamauga context, and no portrait block. The replacement overlay preserves 603 registry rows and shifts raw counts 22 authored / 581 generated → 25 authored / 578 generated. `probe-loot-survival` asserts the raw-empty baseline, all three canonical records, Watkins's old-id alias lookup, 1st/27th-vs-Company-H honesty, no-portrait honesty, hostile-pack rejection, and sourced UI rendering.
>
> **VET:** soldier replacement importer `records=3 verified=3 disputed=0`; women importer `records=7 verified=6 disputed=1`; build **GATE OK**; focused `probe-loot-survival` **11/11, 0 pageerrors**. The literal full `npm run vet:noreg` completed **VET NO-REGRESSION OK — 82 commands**, including `probe-loot-survival`, `probe-women-in-war`, `tactical roster`, `custom battle builder`, all battle probes, `sweep-all-battles`, `probe-full-campaign` **4/4, 0 pageerrors**, and `diag-classic` `nonBlank:346`, `m3dActive:false`.

## ⚡ EARLIER — 2026-06-29 **PHASE I · SECOND CITATION-GRADE SOLDIER REPLACEMENT SLICE COMPLETE (D155).** Soldier's Story gained William McCarter as the second sourced named-person replacement: he replaces generated Fredericksburg Irish Brigade private slot `ss:fredericksburg:US:us_irish:pvt`. The D155 checkpoint had 2 `Verified` replacement records. No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

## ⚡ EARLIER — 2026-06-28 **PHASE I · FIRST CITATION-GRADE SOLDIER REPLACEMENT SLICE COMPLETE (D154).** Soldier's Story got its first sourced named-person replacement: Elisha Hunt Rhodes replaces generated Bull Run private slot `ss:bullrun1:US:us_burnside:pvt`. The D154 checkpoint had 1 `Verified` replacement record. No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

## ⚡ EARLIER — 2026-06-28 **PHASE I/F · WOMEN IN THE WAR PRESENTATION LANE COMPLETE (D153).** The Campaign Kit now has a citation-grade women-in-war card lane beside Soldier's Story and the Army Register. It ships 7 records (6 Verified + 1 Disputed Velazquez), preserves the D153 dossier provenance/player copy/warnings, kept `data/soldier-replacements.json` empty at the D153 checkpoint, and does not create `ss:` rows or `replacePid` mappings. No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D153):** new `data/women-in-war.json` (`cw_women_in_war_v1`), new `src/38-women-in-war.js`, new `tools/import-women-in-war.mjs`, and new `tools/probe-women-in-war.mjs`. The section renders sourced cards for Wakeman, Cashier, Barton, Walker, Tubman, Dix, and disputed Velazquez with search, role/provenance filters, source/details disclosure, integrity warnings, and a visible Velazquez dispute note.
>
> **SEPARATION / HISTORY GUARDS:** every women record has `registryMappable.canMap:false`; no record carries `replacePid` or an `ss:` id; Soldier's Story replacement data remains `records:[]`. Velazquez remains `Disputed`, with no modeled CS unit and no upgrade to a sourced soldier row. Barton, Walker, Tubman, and Dix reuse existing codex portraits; Wakeman has no wartime portrait assertion.
>
> **GATES / PROBE:** `tools/build.mjs` delegates canonical women validation to `tools/import-women-in-war.mjs`, and `tools/vet-no-regression.mjs` now runs both the importer and `tools/probe-women-in-war.mjs`. The focused probe locks data separation, registry non-leakage, visible cards, Velazquez disputed copy, codex portrait reuse, no Wakeman portrait, filters, empty-data no-op, XSS escaping, focus styling, screenshot output, 0 pageerrors, and 0 bridge/tactical/after-action static leaks.
>
> **VET:** syntax checks passed for the new JS files; `node tools/import-women-in-war.mjs` passed with `records=7 verified=6 disputed=1`; the empty-pack `--write` path preserved 7 women records and `soldier-replacements.records:0`; build printed **GATE OK** with `women-in-war ✓`; focused `probe-women-in-war` passed **8/8, 0 pageerrors**, static scan `0 leaks / 14 files`, screenshot 490,400 bytes. Full no-regression coverage completed in split segments: initial suite passed through the Women in the War gate before a `probe-ai` cleanup timeout; standalone `probe-ai` passed **15/15, 0 pageerrors**; resume from `attacker ai` ended **VET NO-REGRESSION OK — 34 commands**, including `probe-full-campaign` **4/4, 0 pageerrors** and `diag-classic` `nonBlank:346`, `m3dActive:false`.

## ⚡ EARLIER — 2026-06-28 **PHASE I · CITATION-GRADE SOLDIER REPLACEMENT TOOLING COMPLETE (D152).** The Soldier's Story lane now has a versioned schema, canonical empty data pack, validator/importer, build gate, no-regression hook, and focused browser probe for replacing generated representative rows with sourced named people later. No sourced historical people were added. No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D152):** new `data/soldier-replacements.json` (`cw_soldier_replacements_v1`, empty `records`), new `SOLDIER-REPLACEMENT-FORMAT.md`, and new `tools/import-soldier-replacements.mjs`. The format requires a stable sourced `pid`, generated `ss:` `replacePid`, side/rank/branch/year/team, full persona attributes, `Verified` or `Disputed` provenance, and at least two independent sources.
>
> **REGISTRY SEAM:** `src/37-loot-survival.js` now validates replacement packs before applying them. A valid future record can replace exactly one current generated row, preserve registry length, move the row from Generated/Inferred to sourced Verified/Disputed, keep old-id alias lookup through `ssFindPerson`, and show `Sourced` in the Army Register. Empty or invalid canonical data leaves the current 603-person registry unchanged.
>
> **GATES / PROBE:** `tools/build.mjs` rejects malformed, prototype-polluted, under-cited, duplicate, generated-mislabelled, `Inferred`, bad-team, bad-year, bad-branch, and incomplete-persona replacement records before writing HTML. `tools/vet-no-regression.mjs` now includes the importer. `tools/probe-loot-survival.mjs` now covers canonical-empty behavior, hostile-pack rejection, and a temporary valid sourced fixture overlay.
>
> **VET:** syntax checks passed for the edited/new JS files; `node tools/import-soldier-replacements.mjs` passed with `records=0`; `node tools/build.mjs` printed **GATE OK** and rebuilt `civil_war_generals.html`; focused `probe-loot-survival` passed **11/11, 0 pageerrors**; slice gates passed: `probe-save-slots` **9/9**, `probe-bridge` **6/6**, `probe-full-campaign` **4/4**, `diag-classic` `nonBlank:346`; full `npm run vet:noreg` finished **VET NO-REGRESSION OK — 80 commands**; `git diff --check` clean. Direct main-loop audit verified CLI rejection for under-cited, `Inferred`, generated-mislabelled, duplicate-target, and prototype-key packs.

## ⚡ EARLIER — 2026-06-28 **PHASE I · SOLDIER'S STORY JOURNEY PERSISTENCE COMPLETE (D151).** The Campaign Kit now persists the selected person's career as a bounded Soldier's Story record: start entry, battle association, outcome/status history, promotion count, survival consequences, and after-action/final-report tie-in. No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **NEXT = D152 citation-grade replacement tooling; Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D151):** `src/37-loot-survival.js` now records a bounded career log for the active selected person. Entries include turn, battle id/name, outcome, status, rank-before/rank-after, casualty context, and a note. The journey state keeps start/current/last battle metadata, battle count, promotion count, status, and a sanitized `L.people[personId]` summary for save/load and UI readback.
>
> **STATUS / SURVIVAL / PROMOTION:** resolved battles can mark the selected person `alive`, `wounded`, or `captured` under deterministic low-risk rules. Wounded/captured states apply bounded survival consequences and tiny active-only bridge deltas; inactive Campaign Kit saves still produce exact zero. Generated low-rank rows can promote through `fldPromotePerson` into the saved person snapshot without mutating canonical `GAME_DATA.ratings`.
>
> **UI / REPORT:** the active-journey panel now shows selected person, status, last battle, battle count, promotions, and recent career entries. `src/82-after-action.js` includes a small display-only Soldier's Story section through `ssJourneyReportHTML` when a journey is active.
>
> **SAVE / PROBE:** restored journey state now sanitizes career arrays, bad statuses, bad keys, person snapshots, dual OVR fields, promotion counts, and tampered people summaries. `tools/probe-loot-survival.mjs` now covers D151 career-log persistence, save/load safety, status/survival effects, promotion hook behavior, no canonical ratings mutation, after-action/end-report tie-in, tamper sanitation, and the D149/D150 locked restart behavior.
>
> **VET:** `node --check src/37-loot-survival.js`, `node --check src/tactical/T14-ratings.js`, `node --check src/82-after-action.js`, and `node --check tools/probe-loot-survival.mjs` passed. `node tools/build.mjs` printed **GATE OK** and rebuilt `civil_war_generals.html` from source. Focused `node tools/probe-loot-survival.mjs` passed **10/10, 0 pageerrors** and wrote `tools/shots/probe-loot-survival.png` (279,520 bytes). Slice gates passed: `probe-save-slots` **9/9**, `probe-bridge` **6/6**, `probe-full-campaign` **4/4**, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Full `npm run vet:noreg` finished **VET NO-REGRESSION OK — 79 commands**; JSON readback clean; `git diff --check` clean.

## ⚡ EARLIER — 2026-06-28 **PHASE I · ARMY REGISTER / PERSON-UNIT AXIS COMPLETE (D150).** The Campaign Kit now turns the full 603-person current Soldier's Story registry into a searchable/filterable Army Register with person detail cards, provenance/source display, team hierarchy, and card-level journey start. No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **NEXT = D151 Soldier's Story journey persistence, then D152 citation-grade replacement tooling; Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D150):** `src/37-loot-survival.js` now renders every current registry person as an Army Register card with live search plus side/rank/provenance/unit filters. Selecting a card opens a detail card with OVR, grade, attack/defend split, Authored-vs-Generated source, Verified/Inferred provenance, latent command, source count, and team hierarchy.
>
> **PERSON-UNIT AXIS:** `src/tactical/T14-ratings.js` now preserves `army -> corps -> division -> brigade -> regiment -> company` in `fldPersonTeam`. Generated representative rows remain visibly `Generated` / `Inferred`; authored sourced rows show `Authored` / `Verified`. D150 does not fabricate named soldiers; D152 remains the validation/import lane for future citation-grade replacements.
>
> **UI / STATE:** Begin Journey works from the selector, detail card, and any register card. Once a Soldier's Story journey is active, all start controls render disabled as `Journey Active`, preserving D149's no-overwrite guard.
>
> **PROBE/GATE:** `tools/probe-loot-survival.mjs` now covers exact 603-card/full-selector count, search narrowing, side/rank/provenance/unit filters, detail-card selection, generated/Authored + Inferred/Verified display, team hierarchy, card journey start, survival activation, and locked restart UI.
>
> **VET:** `node --check src/37-loot-survival.js`, `node --check src/tactical/T14-ratings.js`, and `node --check tools/probe-loot-survival.mjs` passed. `node tools/build.mjs` printed **GATE OK** and rebuilt `civil_war_generals.html` from source. Focused `node tools/probe-loot-survival.mjs` passed **9/9, 0 pageerrors** and wrote `tools/shots/probe-loot-survival.png` (279,520 bytes). Slice gates passed: `probe-save-slots` **9/9**, `probe-bridge` **6/6**, `probe-full-campaign` **4/4**, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Full `npm run vet:noreg` finished **VET NO-REGRESSION OK — 79 commands**; JSON readback clean; `git diff --check` clean.

## ⚡ EARLIER — 2026-06-28 **PHASE I · LOOT/SURVIVAL HARDENING + GATE EXPANSION COMPLETE (D149).** The D148 Campaign Kit is now hardened against restored-save tampering, duplicate/overflow/stack exploits, inactive bridge leakage, survival tick timing abuse, and journey restart overwrites. No canonical battle data, tactical combat rules, `build/base.html`, item balance data, or save version changed. **NEXT = D150 Army Register / person-unit axis, then D151 Soldier's Story journey persistence, then D152 citation-grade replacement tooling; Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D149):** `src/37-loot-survival.js` now uses safe own-property checks, rejects prototype/meta keys in `equipped`, caps saved item-found notes, sanitizes journey snapshots/logs, requires literal boolean activation for survival/journey, normalizes future-tampered survival turn sentinels, reports actual accepted stack quantity, and blocks active-journey restarts unless a future explicit force path is added.
>
> **UI / STATE:** Campaign Kit Begin Journey controls now render disabled as `Journey Active` once a Soldier's Story is underway. Survival remains default-off; `lootSurvivalBridgeBonus(C)` still returns exact zero unless survival or a journey is explicitly active.
>
> **PROBE/GATE:** `tools/probe-loot-survival.mjs` now covers corrupt save shapes, duplicate/overflow/stack exploits, inactive and tampered bridge leakage, future-turn tick/forage tampering, save-slot import rejection, restored-save sanitation, full 603-person UI selector coverage, restart-blocking UI behavior, and a real Campaign Kit screenshot.
>
> **VET:** `node --check src/37-loot-survival.js` and `node --check tools/probe-loot-survival.mjs` passed. `node tools/build.mjs` printed **GATE OK** and rebuilt `civil_war_generals.html` from source. Focused `node tools/probe-loot-survival.mjs` passed **9/9, 0 pageerrors** and wrote `tools/shots/probe-loot-survival.png` (279,504 bytes). Slice gates passed: `probe-save-slots` **9/9**, `probe-bridge` **6/6**, `probe-full-campaign` **4/4**, and `diag-classic` `nonBlank:346`, `m3dActive:false`. Full `npm run vet:noreg` finished **VET NO-REGRESSION OK — 79 commands**; JSON readback clean; `git diff --check` clean.

## ⚡ EARLIER — 2026-06-27 **PHASE I · LOOT/SURVIVAL + SOLDIER'S STORY PLAY-AS-ANYONE MVP COMPLETE (D148).** The Campaign Kit is now playable: rarity-tiered loot/inventory, default-off survival pressure, full-current-registry Soldier's Story selection, and journey start all live behind additive gates. No canonical battle data, tactical combat rules, `build/base.html`, or hand-edited generated HTML changed. **NEXT = execute the `V1-CHECKLIST.md` consolidated Phase I continuation queue: D149 loot/survival hardening, D150 Army Register/person-unit axis, D151 Soldier's Story journey persistence, D152 citation-grade replacement tooling; Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D148):** new `data/loot-survival.json`, `src/37-loot-survival.js`, a `Campaign Kit` desk tab, and `tools/probe-loot-survival.mjs` wired into the normal no-regression suite. Battle/campaign outcomes call `lootOnResolve` to recover deterministic rarity-tiered loot; the player can inspect inventory, use consumables, equip kit/weapon/keepsake items, toggle survival, forage, and begin a Soldier's Story journey.
>
> **SURVIVAL GATE:** rations, exposure, disease, fatigue, forage, and morale ride `C.loot.survival`. Survival is off by default. `lootSurvivalBridgeBonus(C)` returns exact zero unless survival or a Soldier journey is active, and active effects are bounded input deltas only.
>
> **SOLDIER'S STORY:** `ssPersonRegistry` reuses `fldMaterializePerson`, `fldPersonTeam`, and `fldPromotePerson` to enumerate authored rating personas, generals/commission figures, and representative company officer/NCO/private rows for every current modeled tactical brigade token. The UI exposes the full current registry for play-as-anyone starts: 603 selectable people in the current build. Generated rows remain visibly `Inferred`; full citation-grade bios/portraits for every identifiable person remain a data-expansion pass, not a hidden claim.
>
> **BUG-HUNT FINDINGS FIXED:** registry creation no longer annotates canonical `GAME_DATA.ratings`; over-cap stack adds return `stack-full`; explicit zero survival meters survive load/init; the Campaign Kit UI now exposes the entire current person registry, not only the sample cards.
>
> **VET:** `node --check` passed for every new/edited JS file. `node tools/build.mjs` printed **GATE OK** and rebuilt `civil_war_generals.html` from source. Focused `node tools/probe-loot-survival.mjs` passed **8/8, 0 pageerrors**. Full no-regression completed green in resumed segments; after the final full-registry UI fix, `npm run vet:noreg -- --from="loot survival"` passed **60 commands**: `probe-full-campaign` **4/4, 0 pageerrors**, `diag-classic` `nonBlank:346`, `m3dActive:false`. Probe JSON readback clean; `git diff --check` clean.

## ⚡ EARLIER — 2026-06-27 **PHASE J · MOD-FRIENDLY CUSTOM SCENARIO PACKS COMPLETE (D147).** This completes the save/mod/share hardening lane that blocks sharing: custom scenarios now have a documented versioned JSON format, template export, multi-scenario pack export/import, empty-slot installation, and probe-locked rejection of tampered/duplicate/per-battle-fudge data. No canonical battle data, tactical combat rules, `build/base.html`, or hand-edited generated HTML changed. **NEXT = Phase I loot/survival spine + first playable loot/inventory/survival MVP; Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D147):** `src/tactical/T11-custom-battle.js` now exposes `fldCustomTemplateJson`, `fldCustomExportPack`, `fldCustomImportPackJson`, and `fldCustomInstallPack`, with UI buttons for Template, Export Pack, and Import Pack to Slots. Packs install only into empty local custom-battle slots and custom ids stay out of `fldScenarioRegistry()`.
>
> **FORMAT DOC:** new `CUSTOM-SCENARIO-FORMAT.md` documents the single-scenario `cw_custom_battle_v1` wrapper and the pack `cw_custom_battle_pack_v1` wrapper, with the V1 guardrails: single-phase only, `custom_` ids only, no custom `phases[]`, prototype keys scrubbed, battle-specific combat tuning keys rejected, and the universal artillery gun/crew model retained.
>
> **BUG-HUNT FINDING FIXED:** the importer rejected duplicate ids before install, but the public lower-level installer could be called directly with duplicate scenario ids. `fldCustomInstallPack` now rejects duplicate ids inside the batch before any write, and the focused probe calls that helper directly.
>
> **VET:** `node --check src/tactical/T11-custom-battle.js` and `node --check tools/probe-custom-battle-builder.mjs` passed. `node tools/build.mjs` printed **GATE OK** and rebuilt `civil_war_generals.html` from source. Focused `node tools/probe-custom-battle-builder.mjs` passed **10/10, 0 pageerrors**. Full `npm run vet:noreg` passed **78 commands** before the bug-hunt fix; after the fix, `node tools/vet-no-regression.mjs --from="custom battle builder"` passed **12 commands**, including `probe-full-campaign` **4/4, 0 pageerrors** and `diag-classic` `nonBlank:346`, `m3dActive:false`. Probe JSON readback clean; `git diff --check` clean; `build/base.html` and `src/00-manifest.json` untouched.

## ⚡ EARLIER — 2026-06-27 **PHASE J · SAVE / LOAD / SHARE HARDENING COMPLETE (D146).** This is a **strategic/meta hardening** milestone over the existing save system: no tactical/combat state, battle data, `build/base.html`, or hand-edited generated HTML changed. Named slots, export/import, stale/corrupt payload handling, menu reinjection, and the accessible one-turn undo path are now guarded by a focused Chrome probe and the normal no-regression suite. **NEXT = Phase J custom scenario packs, then Phase I loot/survival; Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D146):** `src/91-save-slots.js` now validates and clones saves before use, rejects stale/corrupt/tampered shapes, keeps named slot save/load/rename/delete behavior bounded, routes both pasted JSON and file import through the same hardened path, dedupes/reinjects the Save Manager menu entry after menu rebuilds, and captures a D35-style one-turn undo only for learner-friendly non-Ironman accessible campaigns.
>
> **BUG-HUNT FINDING FIXED:** the visible file-import path still delegated to the base `importSave`, while pasted JSON used the hardened validator. That left malformed file saves able to reach base `applySave`, including `settings` objects that shadowed `hasOwnProperty`. Fixed by adding `_slImportFile`, assigning a hardened `importSave` wrapper with an `_slHardened` marker, and extending `tools/probe-save-slots.mjs` to lock both the API and the rejection case.
>
> **PROBE/GATE:** new `tools/probe-save-slots.mjs` covers manager injection/dedupe, named slot operations, export/import text round-trip, invalid/stale/corrupt payload rejection, undo capture/load, hardened import marker, and no tactical state writes/redeclaration. `tools/vet-no-regression.mjs` now includes the save-slot probe in the normal gate.
>
> **VET:** `node --check src/91-save-slots.js`, `node --check tools/probe-save-slots.mjs`, and `node --check tools/vet-no-regression.mjs` passed. `node tools/build.mjs` printed **GATE OK** and rebuilt `civil_war_generals.html` from source. Focused `node tools/probe-save-slots.mjs` passed **9/9, 0 pageerrors**. Post-fix `node tools/vet-no-regression.mjs --from="save slots"` passed **51 commands**, including `probe-full-campaign` **4/4, 0 pageerrors** and `diag-classic` `nonBlank:346`, `m3dActive:false`. Probe JSON readback confirmed `ok=true` and `pageerrors=0`; `git diff --check` clean; `build/base.html` untouched.

## ⚡ EARLIER — 2026-06-27 **OPS/DESIGN · MODEL ROUTING, UI REDESIGN LAW, AND NORMAL GATE CLEANUP COMPLETE (D145).** This is a **docs/tooling/research** milestone: no gameplay/source/data/combat/save behavior changed. The repo now has one consistent model-routing contract, a real frontend redesign milestone because Aaron does not want broadsheet as the global shell, and a normal no-regression runner that includes the D143 full-campaign probe. **NEXT = Phase J save/mod/share hardening, then Phase I loot/survival; Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D145):** `START-HERE.md`, `AUTONOMOUS-RUN.md`, `AGENTS.md`, `CLAUDE.md`, `.clinerules`, and `.github/copilot-instructions.md` now say the main reasoning/integration loop stays on **5.5** for architecture, important code, user-facing text, history, UI/UX, accessibility judgment, bug hunts, final review, and any irreversible/quality-critical work. Token-efficient helpers are only for bounded packets; every helper/subagent/workflow call must explicitly set model and effort; smaller models never own architecture, combat balance, historical claims, UX direction, accessibility judgment, or final integration.
>
> **UI/UX LAW:** D145 supersedes "broadsheet everywhere." Future frontend work targets a more video-game-like historical command interface: brighter, rounder, more dramatic, icon-led, and centered on public-domain photos/illustrations where they clarify people/place/stakes. Broadsheet remains for press/editorial/dispatch/provenance surfaces. Constraints stay strict: Civil War gravity, anti-Lost-Cause framing, WCAG 2.2 AA, keyboard support, `reduceMotion`, CVD-safe cues, no unclear-license assets, and Intel UHD-617 performance. Research notes are saved at `docs/design/ui-redesign-research.md`; `V1-CHECKLIST.md` now has H0 with target screens/probes/screenshots/acceptance criteria.
>
> **GATE:** new `tools/vet-no-regression.mjs` + `package.json` scripts `vet:noreg`, `vet:noreg:list`, and `vet:full-campaign`. The runner serializes the existing probe suite, enforces probe JSON `ok`/pageerror summaries, checks `diag-classic`, writes `.tmp/vet-no-regression-*.log`, includes `tools/probe-full-campaign.mjs`, and kills timed-out child process groups instead of hanging forever. Heavy visual probes have explicit larger budgets.
>
> **VET:** syntax checks passed for the new/edited probe scripts and `package.json` parses. `node tools/build.mjs` printed **GATE OK**. The audit found and fixed real harness/gate issues instead of weakening probes: accessibility static-scan now strips comments/strings; `bootprobe` has bounded browser cleanup; `probe-phased-3d` isolates each battle page and has a 90s evaluate timeout; `probe-render-richness` isolates each scene in a fresh browser; `vet-no-regression` has per-probe timeout budgets and process-group cleanup. Focused probes passed: accessibility **25/25, 0 pageerrors**, phased-3D **64/64, 0 pageerrors**, render-richness **30/30, 0 pageerrors**. The normal no-regression gate completed green in resumed segments on the 8 GB Mac: build through visual-fidelity, render-richness, then weather through full-campaign and Classic. Final segment ended with `probe-full-campaign` **4/4, 0 pageerrors** and `diag-classic` `nonBlank:346`, `m3dActive:false`.
>
> **ROADMAP CLEANUP:** Phase J saves is now correctly a hardening/probe pass over existing `src/91-save-slots.js` named slots/export/import, with undo-last-turn still open. The next concrete gameplay flow is: save/mod/share hardening -> Phase I loot/survival spine -> first playable loot/inventory/survival MVP -> Soldier's Story bridge.

## ⚡ EARLIER — 2026-06-26 **PHASE J · FULL-CAMPAIGN PLAYTHROUGH PROBE COMPLETE (D143).** This is a **probe-only** milestone: no gameplay/source/data/build/combat/save behavior changed. The owner-mode campaign now has an end-to-end Chrome guard that drives the real generated game from first battle to final graded report through the existing bridge/campaign seams. **NEXT = H2 reenactment-footage decision or remaining Phase I/J polish; Q5 Chattanooga + Q6 USCT playable battles remain LAST.**

> **WHAT SHIPPED (D143):** new `tools/probe-full-campaign.mjs`. It uses the live single-file game and the existing `bridgeAutoResolve`, `campaignAdvance`, `openUpgrade`, `aarConcludeWar`, and `warWonScreen` seams. It does not create a second campaign simulator and does not touch `src/`, `data/`, `build/base.html`, or the generated HTML by hand.
>
> **COVERAGE:** the probe runs a dominant Union campaign through all 31 campaign battles (`sumter` -> `appomattox`) and a dominant Confederate campaign through all 28 battles (`sumter` -> `appomattox`). Each battle must win, increment campaign stats, append to `completed[]`, advance `idx`, clear recovery flags, and show the President's interstitial before continuing. The final chain win must render `#wwReport` / `Final Reckoning` and clear `G.campaign`.
>
> **BRANCHES LOCKED:** a controlled Bull Run loss verifies recovery/rematch behavior (`idx` held, recovery set, `flipAtk` consumed on the rematch, then recovery cleared). A mid-campaign `victoryReady="will"` verifies the negotiated-peace offer appears between battles and accepting it renders the final graded report.
>
> **VET:** baseline build **GATE OK**; focused `probe-full-campaign` **4/4, 0 pageerrors** with JSON confirming US 31/31, CS 28/28, recovery, and strategic ending; `node --check tools/probe-full-campaign.mjs` clean; `git diff --check` clean. D145 wires this probe into `npm run vet:noreg`.

## ⚡ EARLIER — 2026-06-26 **PHASE H · H5 increment 4 — FORTIFICATIONS DEPTH COMPLETE (D142), closing the locked D138 UG:G-fidelity modern-engine push.** This is a **D74 gameplay carve-out**, not presentation-only: the modern engine now has staged entrenchments (**rifle pits / hasty cover → full parapet → redoubt / earthwork**), facing-aware works (front strongest, flank reduced, rear bypass still matters), stronger/clearer prepared abatis when tied to own parapets/redoubts, visible 2D/3D earthworks, HUD status, a player charge-impetus commit lock, and H5-i3's swamp/town/fort arrays now feed the universal T0 cover/move hooks (no per-battle fudge). **NEXT = H2 reenactment-footage decision or Phase I/J polish; Q5 Chattanooga + Q6 USCT battles remain LAST.**

> **WHAT SHIPPED (D142):** `src/tactical/T13-engineering.js` now interprets `u.entrench` as staged works: early rifle pits/hasty cover, full parapet, then redoubt/earthwork. `fldEngCover(u, fromX, fromZ)` is stage + facing aware: frontal fire gets the earned bonus, flank fire is reduced, rear fire bypasses the works. Units that march off their own works lose the cover while the visible works fade. Abatis built by a brigade in its own parapet/redoubt is marked prepared, starts stronger, slows more, bites morale/disorder/fatigue harder, clears slower, and renders with clearer stakes/backing. 2D and 3D draw hasty/parapet/redoubt shapes; the HUD names the stage and front-cover multiplier; the end card teaches the Hess/Mahan fortification lesson.
>
> **CHARGE + TERRAIN SIM WIRING:** `src/tactical/T20-order-feel.js` adds the deferred player-only charge impetus lock: a 1.2s correction grace, then move/hold/queue/re-aim orders are refused until contact, target loss, fog loss, own rout, or own death. AI inline charges stay unchanged. `src/tactical/T0-field-sandbox.js` now wires D141 terrain through `fldCoverAt`/`fldMoveFactor`: fort strong cover + attacker friction, town hard cover + street friction, swamp soft concealment + major slow, with the old wall/woods/hill ladder retained. This is ONE universal combat model; no scenario outcome gate.
>
> **BUG-HUNT FINDING FIXED:** direct adversarial audit found the charge-lock predicate promised "rout/death releases" but did not test the charging unit's own state. Fixed `fldChargeLocked`/HUD to release on own rout/death and extended `probe-order-feel` to assert it (`releasedOnOwnRoutDeath:true`).
>
> **VET (GREEN, post-fix):** `node tools/build.mjs` → **GATE OK**; focused probes `probe-engineering-corps` 23/23, `probe-terrain-readability` 29/29, `probe-order-feel` 13/13, `probe-bullrun` 15/15, all 0 pageerrors; `probe-presets` 26/26; `probe-phased-ab` 20 comparisons / 0 diffs; full no-regression battery rerun after the final charge-lock fix passed (`bootprobe`, `t1probe`, core tactical, all current battle probes, visual/3D probes, and strategy probes); `diag-classic` reports `nonBlank:346`; `git diff --check` clean. Known diagnostic noise: `bootprobe ok=true errors=10` with no real failure bucket, and `diag-classic` prints two tolerated 404 console lines while still reporting the required nonblank result.

## ⚡ EARLIER — 2026-06-26 **PHASE H · H5 increment 3 — TERRAIN READABILITY: the modern (non-hex) engine now makes high/low ground — and EVERY topography type — EXPLICIT (D141), the THIRD step of the locked D138 arc.** Three player-chosen **elevation modes** (default **HILLSHADE** relief = T21's, no double-count · **CONTOURS** = iso-lines + elevation labels · **COLOR-BY-HEIGHT** = a CVD-safe **Viridis** hypsometric tint), cycled from the in-battle settings drawer + the **R** key + the legend's mode chip; an **always-on compact LEGEND** (mode chip · viridis gradient · a live **on-hover elevation+type readout** · a topography key of the types present); the 2D top-down finally shows relief it never had; and **swamp / town / fort** are now distinct render types. All PRESENTATION-ONLY → byte-identical. The hex engine IS Classic — untouched.

> **⭐ THE i3 DISCIPLINE (read first — the SAME as i2, NOT the i1 carve-out).** PRESENTATION-ONLY, held to the FULL D74 byte-identity law. NEW `src/tactical/T22-terrain-readability.js` wraps the render seams BY ASSIGNMENT (fld3dInit / fld3dBuildTerrain / fld2dInit / fldPointerMove / fldExit; carries the ._wx/._atmo/._rr/._vf markers forward), reads-only, never writes sim/`fldRng`/`_SAVE_VER`. The elevation mode is a sim-UNREAD `G.settings.fldElevMode` (the renderRich pattern — kept in `G.settings` only, NOT localStorage, so the documented "hillshade" DEFAULT holds every fresh session). The new **swamp/town/fort** terrain arrays (`__FIELD.terrain.swamps/towns/forts[]`) are **combat-INERT — read by NO sim/combat function** (a probe static-scan enforces it), so combat stays byte-identical even though terrain was ADDED → proven `probe-presets` 26/26 + `probe-phased-ab` 20/0-diff + `probe-bullrun` 15/15.
>
> **WHAT SHIPPED (D141) — T22 (≈630 lines) + thin T0/T6 seams.** (1) **3 modes**: HILLSHADE (3D = T21's slope-AO relief, T22 adds NOTHING to the 3D ground here so relief is never double-counted; 2D paints its own cached hillshade — the top-down had none); CONTOURS (a 3D `LineSegments` of marching-squares iso-lines + elevation-label sprites; 2D = offscreen-cached polylines); COLOR-BY-HEIGHT (a 3D viridis-tinted overlay mesh; 2D = cached viridis canvas). The 3D hypso+contour are PREBUILT once and `.visible`-toggled per mode (snappy, no rebuild, no camera jerk). (2) **Legend + hover** — `#fldElevLegend` (mode chip · viridis gradient bar · `#fldElevHover` live readout · topography key). (3) **swamp/town/fort** distinct in 2D + 3D (swamp = wet-teal disc + reeds; town = clustered buildings; fort = angular earthwork berm + bastions); a demo set in the sandbox showcase. T0 seams: `fldBuildTerrain` gains the demo arrays; `fld2dDraw` calls the guarded `fldTrDrawGround2d` after the field fill; `fldKey` gains the `R` branch. T6: the drawer gains an "Elevation display" cycle control.
>
> **AARON'S 3 FORKS (AskUserQuestion):** palette = **Viridis** · legend = **always-on compact panel** · topography = **add swamp/town/fort** (visual-now / sim-cover-wiring-later — that's what keeps i3 byte-identical).
>
> **BUG-HUNT (43 agents, 6 lenses × 3-skeptic refute + completeness critic):** 4 confirmed of 12 raw + 2 critic-new, **ALL FIXED before commit:** (perf) the 2D contour mode re-stroked ~600–1600 segments EVERY frame → now rendered ONCE to an offscreen canvas (2 batched strokes) + blitted; (a11y MED) the legend mode/collapse buttons dropped keyboard focus to `<body>` on the innerHTML rebuild → focus restored in the click handlers; (MED) the legend topography key + height-range went STALE on a phase advance → a topography-aware legend signature now refreshes on any terrain change in BOTH renderers; (LOW) dropped the localStorage round-trip; (LOW) removed a redundant double-dispose of the cloned hypso mesh; (LOW) the per-pointer hover classify early-outs on a coarse cell; (robustness) the base-called 2D hook gained a full-body try/catch. Critic independently confirmed combat-inertness + the fldExit dispose ORDER + the 3D contour orientation are all correct.
>
> **VET (GREEN — presentation-only, FULL D74):** `GATE OK`; `probe-terrain-readability` **26/26** (NEW); `probe-presets` 26/26 + `probe-phased-ab` 20/0-diff + `probe-bullrun` 15/15 (byte-identity + the golden snapshot's new combat-inert guard); `probe-visual-fidelity` 26/26 (T21 coexists, post-fix) + flags 36/36 + render-richness 30/30 + weather 30/30 + atmospherics 18/18 (3D health); `bootprobe` realErrors:[]; `diag-classic` nonBlank:346 + m3dActive:false (Classic untouched); 0 pe; `wcag-auditor` **AA** (15/15; gradient-bar border lifted for 1.4.11; `aria-live="off"` on the per-move readout confirmed correct). `probe-tactical-visuals` 8-scene stalled partway on the 8 GB Mac (documented heaviest-swiftshader memory stall, NOT a regression — its completed scenes incl. a clean 3D one). **Committed + pushed.**
>
> **⏳ NEXT — the rest of H5:** **i4 FORTIFICATIONS DEPTH** (deepen entrench/abatis/redoubt earthworks + **the charge-impetus commit-lock deferred from i1**; also the natural home to give swamp/town/fort their sim cover/move effects). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D141/D140/D139/D138/D74/D54 → V1-CHECKLIST "H5" → `src/tactical/T22-terrain-readability.js` + `tools/probe-terrain-readability.mjs` + memory `civilwar-terrain-readability` / `civilwar-modern-engine-ugg-fidelity`.

> **⭐ THE i2 DISCIPLINE (read first — the INVERSE of i1).** i1 was the deliberate D74 byte-identity CARVE-OUT (gameplay). **i2 is the opposite: PRESENTATION-ONLY, held to the FULL D74 byte-identity law.** NEW `src/tactical/T21-visual-fidelity.js` wraps the render seams BY ASSIGNMENT (the T16/T17/T18 pattern, OUTERMOST), writes vertex COLOUR only (never a position/sim field/`fldRng`/`_SAVE_VER`), and the headless AI-vs-AI stepper never enters a 3D seam → the per-battle outcomes stay **byte-identical: `probe-presets` 26/26 + `probe-phased-ab` 20/0-diff (final build)**.
>
> **WHY ADDITIVE, NOT A TONE-MAP FLIP (the r128 decision):** THREE is r128 (0.128.0), PRE the r152 colour-management overhaul. A grounded 5-agent recon proved a global ACES/sRGB flip would SPLIT the pipeline (the T16 smoke + T17 precip raw-`gl_FragColor` ShaderMaterials don't tone-map → smoke darkens / rain warms, a visible seam) + force re-tuning every hardcoded colour across T0/T10/T17/T18 → EXCLUDED. Real shadow maps (r128 InstancedMesh depth-pass blob + UHD-617 cost) + MeshStandard terrain (renders DARKER without an envMap under the linear pipeline) also excluded. The win is purely ADDITIVE + palette-preserving.
>
> **WHAT SHIPPED (D140) — five layers in T21, all gated by `fldVfOff()` (T18's `renderRich="off"` reverts the whole stack):** (1) **ground RELIEF** — a slope-AO recolour of the terrain VERTEX COLOURS after T18 (low ground darkens / crests lift, normalized to the battle's height range, + a steep-slope soil tint; latch `colA._vfAO`; positions untouched → units seat on analytic `fldTerrainH`); (2) per-brigade soft **CONTACT SHADOWS** (a MeshBasic ground pool, glued + slightly wider than the block); (3) gradient **SKY DOME** (a BackSide MeshBasic sphere, horizon re-tinted from the live weather fog colour each frame; `scene.background` RETAINED as the far-clip fallback) + a free **CSS VIGNETTE** (below the HUD, `pointer-events:none`, `aria-hidden`); (4) a stylized **rank MAP** (white-based CanvasTexture multiplied on each slab → massed-infantry read, CVD-safe); (5) Max-tier **PEG RANKS** (a per-brigade InstancedMesh, `!fldLow()` only, laid out once + x-scaled per frame, bounding-sphere-cullable). Aaron's 3 forks (AskUserQuestion): scope = **Max**, colour = **stay linear**, relief = **Moderate**.
>
> **BUG-HUNT (19 agents, 6 lenses × 3-skeptic refute + completeness critic → SAFE TO COMMIT, 0 confirmed, D74 confirmed STRUCTURALLY):** 4 raw findings — 2 refuted (mid-battle `renderRich`/`gfxQuality` toggles aren't reachable through the fld3d UI; `gfxQuality` drives the SEPARATE legacy `__M3D` engine), 2 perf nits APPLIED anyway as forward-looking polish (the peg IM gained a bounding sphere so r128 frustum-culls it off-camera — scaling to the bigger Q5/Q6 OOBs; a redundant per-frame shadow-opacity re-pin dropped).
>
> **VET (GREEN — presentation-only, FULL D74):** `GATE OK`; `probe-visual-fidelity` **26/26** (NEW — wrappers+markers · dome+fog-matched horizon+retained bg · vignette · relief reads high>low + positions untouched · glued shadow gap ~0.5 + rank map + peg IM count 22 + sphere · `fldLow()` peg gate · seed/sim invariance + `errN===0` · `renderRich="off"` full revert · 0 pe/texWarn · screenshot VISUALLY CONFIRMED); `probe-presets` **26/26** + `probe-phased-ab` **20/0-diff** (byte-identity, final build); `probe-render-richness` **30/30** + `probe-weather` **30/30** (two stale assertions ROOT-CAUSE-fixed, not weakened — the rr combat-scan now excludes T21 [it rides T18's public `fldRrOff()` opt-out]; T21 RETAINS `scene.background` rather than nulling it); `probe-flags` 36/36 + `probe-atmospherics` 18/18 + `probe-tactical-visuals` ok (3D stack coexists); `bootprobe` realErrors:[]; `t1probe` ok; `diag-classic` `nonBlank:346` + `m3dActive:false` (Classic untouched); 0 pageerrors; `wcag-auditor` N/A by construction (the only new DOM is the aria-hidden/pointer-events:none vignette below the HUD; CVD-safe; nothing animates). **Committed + pushed.**
>
> **⏳ NEXT — the rest of H5 (the D138 arc, in order):** **i3 TERRAIN READABILITY** (explicit high/low — default HILLSHADE relief; toggle to contour-lines+labels / color-by-height hypsometric tint; an elevation legend + on-hover readout; all topography distinct; **presentation-only → D74 byte-identical**, prove via probe-presets + probe-phased-ab; base = `fldBuildTerrain`/`fld3dBuildTerrain` + T12 + the new T21 relief + `tcDecorateMap`) · **i4 FORTIFICATIONS DEPTH** (deepen entrench/abatis/redoubt earthworks + **the charge impetus commit-lock deferred from i1**). Asset budget = self-contained core + optional HD pack. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D140/D139/D138/D74/D54 → V1-CHECKLIST "H5" → `src/tactical/T21-visual-fidelity.js` + `tools/probe-visual-fidelity.mjs` + memory `civilwar-modern-engine-ugg-fidelity` / `civilwar-terrain-readability`.

---

## ⚡ EARLIER — 2026-06-25 **PHASE H · H5 increment 1 — MOVEMENT & ORDERING FEEL: the modern (non-hex) engine moves & obeys like Ultimate General: Gettysburg (D139) — the FIRST step of the locked D138 arc.** Point + facing-handle move, **drag-onto-enemy charges THAT brigade**, **immediate + shift-queue** waypoint routes, a live ORDER GHOST in 2D + 3D, and **faster digging** (T13 70→42s). The deliberate D74 byte-identity CARVE-OUT (vetted as gameplay).

> **⭐ THE CARVE-OUT THAT MADE IT SAFE (read first).** H5-i1 is the deliberate **D74 byte-identity CARVE-OUT** (D138): movement/order changes alter the sim, so they're vetted as GAMEPLAY (a synthetic-gesture probe), not via byte-identity. The discipline that kept it clean: I changed ONLY the **player control surface** (a new module + guarded T0/T13 seams) and the dig RATE — never `fldAiUnit`/`fldSimStep`/`fldStepMovement`'s shared logic. Because no AI/scenario unit ever calls `fldOrderCharge` (the AI sets `u.order` inline), ever gets a `.queue` (only the player shift-drag creates it; the queue-advance guard is a strict no-op otherwise), or ever digs (T13 invariant), the **headless AI-vs-AI per-battle outcomes stay BYTE-IDENTICAL** — proven by `probe-presets` 26/26 + `probe-phased-ab` 20 comparisons / 0 diffs (vs HEAD, final build) + all 9 per-battle probes unchanged. The carve-out's room is the human's controls, not the AI's.
>
> **WHAT SHIPPED (D139):** NEW `src/tactical/T20-order-feel.js` — `fldResolveOrderGesture(sel, gst)` (the testable gesture→orders core: place+face · drag-to-charge · shift-queue · re-aim), the waypoint queue (`fldApplyOrder`/`fldEnqueueOrder`/`fldOrderQueueAdvance`), `fldEnemyAt`/`fldOrderHasHandle`/`fldHandleHit`, and the live ORDER GHOST — 2D (`fldOrderGhost2d`: dashed destination footprint + facing-handle arrow + stem + queued-route dots; charge = filled double-arrow + foe-ring; queue = dashed stem + "+" tick) and 3D (`fld3dEnsureGhost`/`fld3dSyncDrag`: a persistent group — ring + facing arrow + a charge foe-ring + a queue "+" tick, `depthTest:false`+`depthWrite:false`, per-mesh `renderOrder` 13/14). **T0 seams** (`src/tactical/T0-field-sandbox.js`): `fldOrderCharge(u,target)` (optional target; `target==null` → the VERBATIM nearest-enemy scan); `fldStepMovement` arrival → `fldOrderQueueAdvance` (queue hook, no-op without `.queue`); `fldPointerDown`/`fldPointerUp` rewired to the resolver + handle-hit; `fld2dDraw`/`fld3dInit`/`fld3dRender`/`fld3dDispose` ghost hooks; the HUD help line. **T13**: `DIG_TIME_BASE` 70 → **42** (full parapet ~42s @ Balanced; Arcade ~29s / Historian ~55s).
>
> **AARON'S 4 FORKS (AskUserQuestion):** (1) order model = **immediate + shift-queue**; (2) charge = **drag-onto-enemy** (no impetus commit-lock yet — deferred to i4); (3) facing UX = **point + facing handle**; (4) fortify = **faster/cheaper digging**.
>
> **BUG-HUNT (31 agents, 6 lenses × 3-skeptic refute + completeness critic → SAFE TO COMMIT, 0 must-fix, 0 HIGH, no byte-identity leak):** 7 confirmed render/pointer/UX/a11y findings, all minor; I fixed 4 in-increment under the standing AA/CVD-by-shape bar — **RR-1** 3D ghost `depthWrite:false` (was polluting the transparent depth buffer — the T12 convention); **RR-2** per-mesh `renderOrder` (group-level was a no-op); **A11Y-1** queue-vs-immediate 2D drag now reads by SHAPE (dashed stem + "+" glyph) not the CVD-collapsing amber/green hue; **A11Y-2** 3D ghost shape redundancy (charge foe-ring + queue "+" tick). Left **UX-2** (select-radius can shadow a handle in dense clusters) documented-acceptable for the 2-brigade sandbox → revisit with denser OOBs.
>
> **VET (GREEN — gameplay, NOT byte-identity, per the carve-out):** `GATE OK`; `probe-order-feel` **12/12** (the gameplay vetting — place+face, tap-keeps-facing, drag-onto-the-FAR-enemy charges that one, default-charge-still-nearest, shift-queue, queue-advance, re-aim, handle hit-test, fog-gated `fldEnemyAt`, dig-timing **42s vs 70s** logged, the no-AI-queue/no-AI-dig invariant over a 1936-step AI battle, 2D ghost render); `probe-presets` **26/26** + `probe-phased-ab` **20/0-diff** (byte-identity of the AI sim); 9 per-battle probes + field + fog all ok; `probe-engineering-corps` **22/22** (dig retune — relationship-based assertions); `bootprobe` realErrors:[]; `probe-flags` 36/36 + `probe-tactical-visuals` ok (3D scene healthy with the ghost group); `t1probe` 14/14; `diag-classic` `nonBlank:346`; 0 pageerrors; 2D ghost visually confirmed. **Committed + pushed.**
>
> **⏳ NEXT — the rest of H5 (the D138 arc, in order):** **i2 visual fidelity** (the polished HYBRID look — richer 3D terrain/lighting/smoke/unit detail, reuse run-h PBR/HDRI/post-FX) · **i3 terrain readability** (explicit high/low ground — default HILLSHADE, toggle to contour-lines+labels / color-by-height, legend + hover readout, all topography distinct; **presentation-only → D74 byte-identical**, prove via probe-presets + probe-phased-ab) · **i4 fortifications depth** (deepen entrench/abatis/redoubt earthworks + **the charge impetus commit-lock deferred from i1**). Asset budget = self-contained core + optional HD pack. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D139/D138/D74/D54 → V1-CHECKLIST "H5" → `src/tactical/T20-order-feel.js` + the T0/T13 seams + `tools/probe-order-feel.mjs` + memory `civilwar-modern-engine-ugg-fidelity`.

---

## ⚡ EARLIER — 2026-06-25 **PHASE H · H1 increment 5 — PD BATTLE-SCENE IMAGERY ON THE PRE-BATTLE BRIEFING (D137) + AARON LOCKED THE NEXT MAJOR ARC: THE UG:G-FIDELITY MODERN-ENGINE PUSH (D138).** The pre-battle briefing now banners a famous public-domain photograph of the actual field — Gardner's "Bloody Lane" dead at Antietam, O'Sullivan's "Harvest of Death" at Gettysburg, the Marye's-Heights stone-wall dead (Chancellorsville campaign), the Rappahannock pontoon bridges, the Vicksburg siege lines, Sudley Ford at Bull Run. 6 of 9 marquee battles imaged; malvern/shiloh/chickamauga documented imageless (no clean PD photo). Committed + pushed. **NEXT = H5-i1 MOVEMENT & ORDERING FEEL** (the modern engine's UG:G-style drag/click/direct + fortify)**

> **⭐ THE NEW DIRECTION (D138, Aaron, interactive 2026-06-25) — READ FIRST.** Aaron set the headline target: **the modern (non-hex) engine must look AND play better than Ultimate General: Gettysburg on PS4.** Two engines, two jobs: **hex = Classic, untouched**; **modern engine = NO hex boxes**, fluid UG:G-style **drag-click-and-direct** movement & charge orders + formation facing + **fortify/entrench**. Build order (AskUserQuestion): **(1) MOVEMENT & ORDERING FEEL FIRST** (gameplay/engine work — alters the sim, so NOT under the D74 byte-identical law; vet as gameplay) → (2) polished-HYBRID visual style → (3) terrain readability (default HILLSHADE; toggle to contour-lines + color-by-height; high/low legend + hover readout; all topography distinct; presentation-only → D74) → (4) fortifications depth. Asset budget = **self-contained core + optional HD pack**. Full law: V1-CHECKLIST "H5 — THE UG:G-FIDELITY MODERN-ENGINE PUSH" + DECISIONS D138 + memory `civilwar-modern-engine-ugg-fidelity` / `civilwar-terrain-readability`. The real-time engine base to push = `fldLaunch*` / `tactical/T*` (drag-to-maneuver); terrain base = `fldBuildTerrain`/`fld3dBuildTerrain` + T12 + T18 + `tcDecorateMap`.
>
> **WHAT SHIPPED (D137):** `tools/source-scenes-imagery.mjs` (NEW — Commons API PD sourcing + a scorer that penalizes maps/engravings/monuments/reenactments/postwar, keyed by campaign battle id → `assets/scenes/<id>.jpg` + `assets/scenes-imagery-provenance.json`) · `tools/prep-embed-assets.mjs` (+`scenes` category, 480px/q72/JPEG landscape) · `src/51-scenes-imagery.js` (NEW — `sceneImageHtml(battleId)` → a parchment-framed **captioned `<figure class="scene-img">`** banner read from `__ASSETS["scenes/<id>"]`, or `""`) wired into `85-battle-bridge.js` `bridgeBriefingHTML` as ONE guarded line at the head of the briefing (before the army columns). __ASSETS = 198 (+6 scenes); embed 2.36MB raw (< 3.0MB HARD).
>
> **COVERAGE (accuracy over completeness):** **6 of 9 marquee battles imaged** (bullrun1=Sudley Ford · antietam=Gardner Bloody-Lane dead · fredericksburg=Rappahannock pontoons · chancellorsville=Russell Marye's-Heights stone-wall dead · vicksburg=Shirley-House siege lines · gettysburg=O'Sullivan Harvest-of-Death). **3 IMAGELESS + documented** (no clean PD wartime photo): malvern, shiloh, chickamauga. **The VIEW discipline caught + rejected 3 non-photographs** (a 1917 birdseye panorama, the McElroy battle map, a book engraving) + a bullrun1 caption/image mismatch (stone-bridge→Sudley-Ford).
>
> **DIGNITY-SENSITIVE (the D135 Fort-Pillow precedent):** three plates are photographs of the dead — descriptive alt + a visible `<figcaption>` naming the cost plainly, anti-Lost-Cause, never glorifying; the right plate matched to the right field.
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** the helper reads ONLY `__ASSETS` + a static `SCENE_IMG` table, returns a string, never `fldRng`/sim/save; the bridge edit adds ONLY the guarded `sceneImageHtml(bd && bd.id)` call; the briefing is a strategy-layer screen. Proven: `probe-presets` **26/26** + `probe-phased-ab` **20 comparisons / 0 diffs**. No combat/T*/data/base/save file touched (`git diff --stat`).
>
> **BUG-HUNT (SAFE TO COMMIT, 27 agents — PD-licence + anachronism/dignity-VIEW + D74-purity + a11y + robustness lenses × adversarial-refute + a completeness critic that re-VIEWed all 6 plates):** 1 confirmed MED, FIXED — the gettysburg "Harvest of Death" credit was "Library of Congress" but the embedded plate is the **Smithsonian American Art Museum** scan (matching the provenance) → corrected; the fix did not over-reach onto fredericksburg (genuinely LoC). a11y AA: caption 7.59:1, credit 5.93:1.
>
> **VET (GREEN):** `GATE OK`; `probe-scenes-imagery` **16/16**; `probe-presets` **26/26**; `probe-phased-ab` **20/0-diff**; leaders 15/15 + usct 17/17 + arms 17/17 + photo-embed 22/22 + codex 18/18 coexist; flags 36/36 + weather 30/30 + render-richness 30/30 + audio 22/22 prove 3D health; `bootprobe` realErrors:[]; `t1probe` ok; `diag-classic` 346; visually confirmed (the Gardner Bloody-Lane photo banners the Antietam briefing). **Committed + pushed.**
>
> **⏳ NEXT — the H5 UG:G-fidelity push (movement feel first, D138) is the new top priority.** Remaining H1 imagery (flags-as-surviving-colours · H2 footage) + per-phase weather stay queued. **Q5 Chattanooga + Q6 USCT BATTLES LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D138/D137/D74/D54 → V1-CHECKLIST "H5" + Phase H (H1 i1–i5 ✅) → `src/51-scenes-imagery.js` + `tools/source-scenes-imagery.mjs` + `tools/probe-scenes-imagery.mjs` + `assets/scenes-imagery-provenance.json`.

---

## ⚡ EARLIER — 2026-06-25 **PHASE H ("make it come to life") · H1 increment 4 — PD LEADERS-IN-THE-FIELD IMAGERY: every Codex "People" entry now carries a famous public-domain WARTIME photograph — Lincoln at Antietam, Grant at Cold Harbor, Lee, Davis, Sherman, Jackson, Longstreet, Meade, Thomas, Sheridan, Bragg, Cleburne, Farragut, Stanton, Seward, Stephens, Barton, Dix, and Dr. Mary Walker. The 4 USCT entries (D135) keep their image; these 20 leaders get theirs. Aaron's "fully hands-off, edge-cases only" mode. Combat byte-identical, proven. NEXT in H1 = battle-scene briefing photographs (Gardner/Brady), or flags-as-surviving-colours, or H2 footage — same hands-off mode** (overnight run, D136)

> **THE HANDS-OFF MODE (D134, standing).** The 4th NEW-asset category in the same "fully hands-off, edge-cases only" mode — I source/vet/embed PD imagery autonomously (≥2 PD confirmations + period-accuracy + provenance + a per-image VIEW), surfacing only genuine ambiguities. **Decide-&-log scope split:** the H1 queue bundles "battle-scene / leaders-in-the-field"; I split it — THIS milestone = the 20 leader portraits on the Codex People cards; the **battle-scene photographs on the pre-battle briefings (`bridgeBriefingHTML`) are the immediately-NEXT milestone** (the dignity-sensitive Gardner/Brady corpse photos deserve their own careful pass).
>
> **WHAT SHIPPED:** `tools/source-leaders-imagery.mjs` (NEW — Commons API, PD/CC0-only, a scorer that penalizes paintings/engravings/statues/postwar + a 429-backoff, keyed by codex entry id → `assets/leaders/<id>.jpg` + `assets/leaders-imagery-provenance.json`) · `tools/recon-commons.mjs` (NEW — a small Commons title-recon helper) · `tools/prep-embed-assets.mjs` (+`leaders` category, 400px/q74/JPEG) · `src/52-leaders-imagery.js` (NEW — `leaderImageHtml(id)` → a period-parchment-framed **captioned `<figure class="leader-img">`** [img + `<figcaption>` = caption + holding-institution credit] or `""`) wired into `84-codex.js` `_cxEntryHTML` as **`usctImageHtml(en.id) || leaderImageHtml(en.id)`** — the USCT image (D135) WINS where one exists, else the leader portrait; ONE guarded line. __ASSETS = 192 (155 portraits + 8 weapons + 2 guns + 7 usct + 20 leaders); file 5.0MB→5.9MB.
>
> **DELIBERATELY COMPLEMENTARY (the overlap-check answer):** NOT the 155-image 128px BADGE `portraits` tier (D133, keyed by portrait key) — this `leaders` tier is a separate, larger 400px **codex-resolution** tier keyed by CODEX ENTRY ID, biased to "leader in the field" wartime photographs. Committed full-res sources capped at 2000px for repo hygiene (a Dix scan was 23MB); the deliverable uses the 400px embed tier; provenance records the re-fetch URLs.
>
> **INFORMATIVE (the D135 pattern):** real descriptive `alt` + a visible `<figcaption>`; captions citation-grade + anti-Lost-Cause (Stephens' Cornerstone Speech, Cleburne's emancipation proposal, Longstreet's vilification, Thomas the loyal Virginian, slavery named as the Confederacy's cause).
>
> **THE VIEW DISCIPLINE EARNED ITS KEEP — 11 of 20 wrong pulls corrected before embed** (the highest catch-rate yet): a **"Jefferson Davis" = the UNION Gen. Jefferson C. Davis** (a namesake, then a Frank Leslie's newspaper ENGRAVING cover → finally the genuine CDV); a **Stanton = a USCT soldier's family tintype**; a **Seward = his daughter Fanny**; group/montage photos (Meade, Sherman, Stephens, Bragg); McClellan = the Lincoln-tent photo (would dup Lincoln); Grant = a Harper's engraving → the iconic Cold Harbor field photo; Lee = the 3-figure 1865 porch → a solo 1863 portrait; Cleburne = an engraving → a photographic CDV. All re-pinned + re-VIEWed correct.
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** the helper reads ONLY `__ASSETS` + a static `LEADER_IMG` table (`_ldrEsc`-escaped), returns a string, never `fldRng`/sim/save; the codex edit adds ONLY the guarded `|| leaderImageHtml(...)` call. NO tactical/T*/combat/sim/data/base/save file touched (`git diff --stat`). Proven: `probe-presets` **26/26** + `probe-phased-ab` **20/0-diff**.
>
> **A LATENT D135 PROBE-BRITTLENESS surfaced + root-cause-fixed:** `probe-codex`'s XSS test asserted the WHOLE codex has zero `<img>` — but the codex legitimately embeds `<img src="data:…">` PD photos on People cards since D135; corrected to assert the **payload** is neutralized (escaped handlers; only `data:` imgs reach the DOM) — strictly stronger. 17/18→**18/18**.
>
> **BUG-HUNT (SAFE TO COMMIT, 0 must-fix):** the full 6-lens Workflow was env-throttled on the 8 GB Mac (the documented D124/D128/D132–D135 "verify/critic legs drag" scenario) → substituted by the 2 completed/salvaged lenses (PD-licence = all 20 PD; anachronism-VIEW = all identities cleared, no medium mis-claims) + a STANDALONE completeness-critic Opus agent (re-VIEWed the 6 highest-risk subjects + re-checked 3 licences live + D74/XSS/captions) + my own pre-embed VIEW of all 30+ candidates.
>
> **VET (PROPER VETTING, GREEN):** `GATE OK` (embed SOFT warning 2.07MB < HARD 3.0MB, logged); `probe-leaders-imagery` **15/15**; `probe-codex` **18/18**; `probe-presets` **26/26** byte-identity; `probe-phased-ab` **20/0-diff**; usct 17/17 + arms 17/17 + photo-embed 22/22 + weapons 8/8 + cannon 9/9 coexist; 3D health + byte-identity proven by **flags 36/36 + weather 30/30 + render-richness 30/30 + audio 22/22 + phased-3d (antietam+gettysburg)**; `bootprobe` realErrors:[]; `t1probe` ok; `diag-classic` `nonBlank:346`; `wcag-auditor` **AA** (caption 6.86:1 / credit 5.35:1). VISUALLY CONFIRMED (the expanded Lincoln Codex card shows the framed Antietam field photo + caption + "Library of Congress (Alexander Gardner)" credit). **ENV NOTE:** with Blender open + 8 GB RAM the heaviest swiftshader probes (`probe-tactical-visuals`, `probe-atmospherics` POINTS-shader, `probe-phased-3d` chickamauga/vicksburg `:launch`) stalled on memory — NOT a regression (the change is 3D-inert per git-diff; 4 other full 3D probes pass; the multi-phase combat byte-identity is proven headlessly by `probe-phased-ab`). **Committed + pushed.**
>
> **⏳ NEXT — more H1 (same "hands-off, edge-cases only" mode):** the **battle-scene photographs on the pre-battle briefings** (`bridgeBriefingHTML` in `src/85-battle-bridge.js`, keyed by `bd.id` — Gardner/Brady field photos, dignity-sensitive) · **flags as real surviving-colours photographs** (a teaching/codex display, distinct from T10's procedural unit-marker flags — do NOT replace T10). Then **H2 reenactment footage** (skippable, offline→procedural fallback — video embed will exceed EMBED_HARD 3.0MB → raise it deliberately + log, OR keep footage linked-only). The pipeline is ready: a sibling `source-*-imagery.mjs` + a `prep-embed-assets.mjs` category, run source+prep+build, read `__ASSETS["<cat>/<id>"]` from a presentation seam. **Per-phase weather** remains a queued combat-path candidate (Antietam dawn-fog→afternoon via `_fldPhaseView` in T8). **Q5 Chattanooga + Q6 USCT BATTLES (the playable scenarios, distinct from this imagery) LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D136/D135/D74/D54 → V1-CHECKLIST Phase H (H1 i1+i2+i3+i4 ✅) → `src/52-leaders-imagery.js` + `tools/source-leaders-imagery.mjs` + `tools/prep-embed-assets.mjs` + `tools/probe-leaders-imagery.mjs` + `assets/leaders-imagery-provenance.json`.

---

## ⚡ EARLIER — 2026-06-25 **PHASE H ("make it come to life") · H1 increment 3 — PD USCT IMAGERY: the Codex teaching cards + "The Ranks" manpower block now show famous public-domain photographs of the U.S. Colored Troops and their story — Company E of the 4th USCI at Fort Lincoln, Sgt. Carney with the colors, Col. Shaw, Frederick Douglass, Harriet Tubman, the Fort Pillow massacre engraving, and the "Come and Join Us Brothers" recruitment broadside. Aaron's "fully hands-off, edge-cases only" mode. All 7 PD-verified + VIEW-checked. Combat byte-identical, proven. NEXT in H1 = leaders/battle-scenes (Brady/Gardner) or flags-as-surviving-colours, or H2 footage — same hands-off mode** (overnight run, D135)

> **THE HANDS-OFF MODE (D134, standing).** The third NEW-asset category in the same "fully hands-off, edge-cases only" mode (D134) — I source/vet/embed PD imagery autonomously (≥2 PD confirmations + period-accuracy + recorded provenance + a per-image VIEW), surfacing only genuine ambiguities. USCT chosen first of the remaining H1 candidates: historically important, abundant unambiguous PD on LoC/NARA/Commons, and the game already carries rich citation-grade USCT teaching content (the Codex cluster + the manpower "double-swing" cards) that had NO imagery.
>
> **WHAT SHIPPED:** `tools/source-usct-imagery.mjs` (NEW — Commons API → keeps ONLY PD rasters → `assets/usct/<codex-id>` + `assets/usct-imagery-provenance.json`) · `tools/prep-embed-assets.mjs` (+`usct` category, maxPx 420 / q78 / JPEG) · `src/53-usct-imagery.js` (NEW — `usctImageHtml(id)` → a period-parchment-framed **captioned `<figure>`** [img + `<figcaption>` = caption + holding-institution credit] for an embedded image, or `""`) called from `84-codex.js` (`_cxEntryHTML` card body, by `en.id`) + `70-manpower.js` (`presManpowerBlock`, US side when `usctUnlocked`) — ONE guarded line each. __ASSETS = 172 (155 portraits + 8 weapons + 2 guns + 7 usct); file 4.6MB→5.0MB.
>
> **THE 7 IMAGES (all PD, all VIEW-verified period-accurate) → codex ids:** `united-states-colored-troops` = Company E, 4th USCI at Fort Lincoln (LoC); `54th-massachusetts-infantry` = Sgt. William H. Carney with the colors; `robert-gould-shaw` = the uniformed Union colonel; `frederick-douglass` = an 1860s CDV; `harriet-tubman` = the Powelson CDV; `nathan-bedford-forrest` = the **1864 Harper's Weekly "Massacre at Fort Pillow" engraving** (the dignity/teaching choice — centers the atrocity against surrendering USCT, anti-Lost-Cause, NOT a Forrest portrait); `system-manpower-pool-usct` = the 1863 "Come and Join Us Brothers" recruitment broadside (also shown in "The Ranks" once USCT organized). **The VIEW discipline earned its keep during sourcing:** the first Shaw pin resolved to "Robert Gould Shaw **2d**" — a striped-suit early-1900s NAMESAKE, not the colonel — caught on VIEW + re-sourced before embed.
>
> **INFORMATIVE, not decorative (the deliberate inverse of D134's arms cutouts).** The arms cutouts use `alt=""`+`aria-hidden` (the gun's name sits beside them). These USCT photos are INFORMATIVE — they show the people/events the prose teaches — so each carries a **real descriptive `alt`** (announced to SR) + a **visible `<figcaption>`** (subject + holding institution, citation-grade).
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** the helper reads ONLY `__ASSETS` + a static `USCT_IMG` caption/alt/credit constant, returns a string, never `fldRng`/sim/save; both call sites add ONLY the guarded `usctImageHtml(...)` call. Proven: `probe-presets` **26/26** byte-identity.
>
> **Bug-hunt (6 diverse-lens Opus finders incl. the live PD-licence-re-verify + the anachronism-VIEW lenses, × 3-skeptic refute + a completeness critic that VIEWED all 7 images + spot-checked 4 licences live → SAFE TO COMMIT, 0 blocking): 3 confirmed, ALL FIXED:** (MED, WCAG AA) the credit line `#4a3a1e`@`opacity:.72`/9.5px = 3.88:1 over the parchment gradient's dark end → fixed to SOLID `#5a4a2c`/10px (wcag-auditor re-verified **5.31:1**, +37% margin); (LOW) the Tubman provenance `periodNote` misdated the c.1868-69 POSTWAR Powelson CDV as "wartime-era" → corrected (the shipped CARD caption was already clean — dates only the 1863 raid); (LOW) the Tubman provenance `alt` "standing" → "seated" to match the image + the shipped module alt. PD lens confirmed all 7 PD; dignity lens confirmed the Fort Pillow framing sound + anti-Lost-Cause.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-usct-imagery` **17/17**; `probe-presets` **26/26** byte-identity; `probe-photo-embed` 22/22 + `probe-arms-imagery` 17/17 (sibling tiers coexist); full 3D suite (phased-ab 20/20-0diff · phased-3d 64/64 · weapons 8/8 · cannon 9/9 · atmospherics 18/18 · weather 30/30 · render-richness 30/30 · audio 22/22 · flags 36/36 · tactical-visuals OK) coexist; `bootprobe` realErrors:[]; `diag-classic` `nonBlank:346`; `wcag-auditor` **AA** (caption 7.85/6.83:1, credit 5.31:1). VISUALLY CONFIRMED (the expanded USCT Codex card shows the framed Company E photo + italic caption + "Library of Congress" credit above the prose). **Committed + pushed.**
>
> **⏳ NEXT — more H1 (same "hands-off, edge-cases only" mode; surface only genuine PD/anachronism/dignity ambiguities):** the remaining NEW-asset imagery categories — **battle scenes / leaders-in-the-field** (Brady/Gardner PD photos → the codex / battle briefings) · **flags as real surviving-colours photographs** (a teaching/codex display, distinct from T10's procedural unit-marker flags — do NOT replace T10). Then **H2 reenactment footage** (skippable, offline→procedural fallback — video embed will exceed EMBED_HARD 3.0MB → raise it deliberately + log, OR keep footage linked-only). The pipeline is ready: add a category to a sibling sourcing tool (the `source-usct-imagery.mjs` pattern) + `tools/prep-embed-assets.mjs` CATEGORIES, run source+prep+build, read `__ASSETS["<cat>/<id>"]` from a presentation seam. **Per-phase weather** remains a queued combat-path candidate (Antietam dawn-fog→afternoon via `_fldPhaseView` in T8 — prove byte-identity via probe-presets + probe-phased-ab). **Q5 Chattanooga + Q6 USCT BATTLES (the playable scenarios, distinct from this imagery) LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D135/D134/D74/D54 → V1-CHECKLIST Phase H (H1 i1+i2+i3 ✅) → `src/53-usct-imagery.js` + `tools/source-usct-imagery.mjs` + `tools/prep-embed-assets.mjs` + `tools/probe-usct-imagery.mjs` + `assets/usct-imagery-provenance.json`.

---

## ⚡ EARLIER — 2026-06-25 **PHASE H ("make it come to life") · H1 increment 2 — PD ARMS IMAGERY: the Armory and Cannon-Corps purchase cards now show real public-domain photographs of the period weapons + field guns (Smithsonian transparent cutouts for the small arms; an Edwin Forbes period sketch + a period photo for the guns). Aaron set "fully hands-off, edge-cases only" asset review. 10 of 14 models imaged; 4 imageless (documented). Combat byte-identical, proven. NEXT in H1 = another NEW-asset category (USCT / scenes) or H2 footage — same hands-off mode** (overnight run, D134)

> **THE AUTONOMY FORK (AskUserQuestion).** With H1/H2 cleared "Go now" (D133), Aaron set per-asset review to **"Fully hands-off, edge-cases only"** — I source/vet/embed autonomously from a PD whitelist (Library of Congress · National Archives · Wikimedia Commons PD-tagged · Internet Archive PD) with ≥2 PD confirmations + period-accuracy + provenance, surfacing ONLY genuine ambiguities. First NEW-asset category = **arms** (flags already render procedurally in T10; the Armory/Cannon-Corps cards had NO imagery + an unambiguous PD pool).
>
> **WHAT SHIPPED:** `tools/source-arms-imagery.mjs` (NEW — Commons API → keeps ONLY PD rasters → `assets/weapons|artillery/<id>` + `assets/arms-imagery-provenance.json`) · `tools/prep-embed-assets.mjs` (+`weapons` `format:'keep'` so the PNG cutouts keep ALPHA, +`artillery` `format:'jpeg'`, + a `.jpg`-output-extension footgun fix) · `src/54-arms-imagery.js` (NEW — `armsImageHtml(cat,id,name)` → a parchment-framed `<img>` for an embedded model, or `""`) called from `55-weapons.js` + `56-artillery.js` (ONE render line each). __ASSETS = 165 (155 portraits + 8 weapons + 2 guns); file 4.3MB→4.6MB.
>
> **COVERAGE (accuracy over completeness):** **10 of 14 imaged** — 8 weapons (springfield/enfield/sharps/spencer/henry/colt/lorenz/smoothbore = Smithsonian transparent cutouts) + 2 guns (napoleon = an Edwin Forbes period sketch, whitworth = a period photograph). **4 IMAGELESS** (cards render unchanged via the guarded helper, each in the provenance `_imageless`): richmond + howitzer12 (no clean PD image) + parrott10 + ordnance3in (the bug-hunt caught a wrong-class siege gun + a modern monument photo).
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** the helper reads ONLY `__ASSETS`, returns a string, never `fldRng`/sim/save; the 55/56 edits add ONLY the `armsImageHtml(...)` call (`git diff` = 1 line each, no purchase/score logic). Proven: `probe-presets` 26/26 byte-identity + `probe-weapons` 8/8 + `probe-cannon` 9/9.
>
> **Bug-hunt — the ANACHRONISM lens earned its keep** (6 Opus finders incl. a live PD-licence-re-verify lens + a per-image VIEW lens; D74-purity 0 findings; the standalone critic VIEWED every remaining image + re-checked each PD licence → **SAFE TO COMMIT**): 4 finders independently caught **parrott10 = a 200-pdr SIEGE Parrott (wrong class)** + **ordnance3in = a MODERN 2011 monument photo** → both made IMAGELESS; **henry** opaque-box JPEG → a Smithsonian transparent cutout; the prep `format:'jpeg'`-on-`.png` footgun → forces `.jpg` output; the card img `alt` duplicated the name → `alt=""`+`aria-hidden` (decorative). lorenz (correct/period/PD, a black-bg JPEG + small museum tag) kept as a documented cosmetic LOW.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-arms-imagery` **17/17**; `probe-presets` **26/26** byte-identity; `probe-weapons` 8/8 + `probe-cannon` 9/9; `probe-photo-embed` 22/22; full 3D suite (phased-ab 20/20-0diff · phased-3d 64/64 · atmospherics 18/18 · weather 30/30 · render-richness 30/30 · audio 22/22 · flags 36/36 · tactical-visuals 10/10) coexist; `bootprobe` realErrors:[]; `diag-classic` `nonBlank:346`; `wcag-auditor` **AA — no changes** (alt=""+aria-hidden is correct decorative treatment). VISUALLY CONFIRMED. **Committed + pushed.**
>
> **⏳ NEXT — more H1 (same "hands-off, edge-cases only" mode; surface only genuine PD/anachronism ambiguities):** another NEW-asset imagery category — candidates: **USCT** (the famous LoC United-States-Colored-Troops photos, for the USCT teaching content) · **battle scenes / leaders-in-the-field** (Brady/Gardner, for the codex/briefings) · **flags as real surviving-colors photos** (a teaching display, distinct from T10's procedural unit markers). Then **H2 reenactment footage** (skippable, offline→procedural fallback — note: video embed will exceed the EMBED_HARD 3.0MB cap → raise it deliberately + log, or keep footage linked-only). The pipeline is ready: add a category to `tools/source-arms-imagery.mjs` (or a sibling sourcing tool) + `tools/prep-embed-assets.mjs` CATEGORIES, run source+prep+build, read `__ASSETS["<cat>/<id>"]` from a new presentation seam. **Per-phase weather** remains a queued combat-path candidate (Antietam dawn-fog→afternoon via `_fldPhaseView` in T8 — prove byte-identity via probe-presets + probe-phased-ab). **Q5 Chattanooga + Q6 USCT BATTLES (the playable scenarios, distinct from USCT imagery) LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D134/D133/D74/D54 → V1-CHECKLIST Phase H (H1 increments 1+2 ✅) → `src/54-arms-imagery.js` + `tools/source-arms-imagery.mjs` + `tools/prep-embed-assets.mjs` + `tools/probe-arms-imagery.mjs` + `assets/arms-imagery-provenance.json`.

---

## ⚡ EARLIER — 2026-06-25 **PHASE H ("make it come to life") · H1 increment 1 — THE OFFLINE PORTRAIT TIER + THE D71 ASSET-INGESTION PIPELINE: the single-file deliverable's public-domain portraits now show even when it is opened STANDALONE (no `assets/` folder beside it) — the build inlines a small compressed tier into `__ASSETS`, and a `window.portraitFor` override prefers base hi-res when present, else the embedded tier, else the engraving. Aaron chose "Go on H1/H2 now" (AskUserQuestion). Combat byte-identical, proven. NEXT in H1 = a NEW-asset category (weapons/flags/scenes/USCT) or H2 footage — SURFACE each per-asset for licence + anachronism sign-off** (overnight run, D133)

> **THE GATED FORK — surfaced + cleared (AskUserQuestion).** H1/H2 (PD imagery + footage) is the ONE external-asset / network-fetch / IP-licensing / single-file-stressing roadmap item every doc flags as NOT unattended-safe. Aaron chose **"Go on H1/H2 now"** (present to review assets). **Increment 1 builds the D71 ingestion pipeline + proves it on the ALREADY-PD-vetted, in-repo portraits** — no NEW external fetch this increment (the 155 `assets/portraits/*.jpg` were downloaded + Commons-PD-gated in run-h); each subsequent category (weapons/flags/scenes/USCT/footage) fetches NEW assets → SURFACE each for per-asset sign-off.
>
> **THE GAP CLOSED:** the frozen base.html PD-photo layer (`_warmPhotos`) loads `assets/portraits/<key>.jpg` by RELATIVE PATH → the real photos showed only when served beside `assets/`; opened standalone they 404'd to the procedural engraving. (The in-game "131 portraits" were the engravings; 155 real PD photos sat unembedded on disk.)
>
> **WHAT SHIPPED (3 files + the tier):** `tools/prep-embed-assets.mjs` (NEW — macOS `sips` ≤128px/q64 → `assets/embed/portraits/`, 155 files/953KB, committed source) + `tools/build.mjs` (a stage Base64-inlines `assets/embed/**` → bare-name `var __ASSETS` with a HARD 3.0MB / SOFT 1.5MB budget + a dup-key gate + a `withFileTypes` non-file skip + a `"data:…"`-mask before the HEX_BOMB textual scan since `node --check` already proves parseability) + `src/21-photo-embed.js` (NEW module after `20-president-render.js` — overrides `window.portraitFor`: base hi-res relative photo → this embedded 128px tier (offline-safe, framed by a pixel-faithful copy of base `_framePhoto`) → the engraving; LAZY per-key framing with eager-warm ONLY when an offline probe-image 404s; + a `.lead-badge` MutationObserver upgrade). File 3.1MB→4.3MB.
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** touches NO combat/data/save file; reads only the new `__ASSETS` global + read-only DOM, never `fldRng`, no save-version bump — only swaps an `<img>` source. Proven: `probe-presets` 26/26 + `probe-phased-ab` 20/20 (0 diffs).
>
> **Bug-hunt (6 diverse-lens Opus finders; D74-purity lens 0 findings → byte-identity clean; the workflow's verify/critic legs dragged on the 8 GB Mac → substituted by inline code-verification + a standalone completeness-critic agent on the FIXED code, the D124/D128/D132 precedent; critic VERDICT: SAFE TO COMMIT):** **(MED, standout)** `_pheUpgrade` DOWNGRADED a base hi-res lead-badge to the 128px tier when served-from-root (a badge built after base hi-res warms carries no `data-portrait-photo` flag) → FIXED to skip any `<img>` already showing a JPEG (a src sniff), locked by a new NO-DOWNGRADE probe assertion; **(MED/LOW)** eager-warming all 155 at boot was pure waste served-from-root → FIXED to LAZY framing + an offline-gated eager warm; **(LOW ×3)** EISDIR on a stray sub-dir → `withFileTypes` skip; silent extension-collision key → a dup-key die; no per-file cap → a prep warn; **(MED honesty)** "offline-portable" scoped to PORTRAITS (3D terrain/HDRI/GLB still relative-path) → documented.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-photo-embed` **22/22** (offline portability proven by route-blocking `assets/portraits/` → photo still renders; NO-DOWNGRADE guard; the fallback chain; 0 pe); `probe-presets` **26/26** byte-identity; `probe-phased-ab` **20/20 (0 diffs)**; `probe-phased-3d` 64/64 · `probe-atmospherics` 18/18 · `probe-weather` 30/30 · `probe-render-richness` 30/30 · `probe-audio-ambience` 22/22 · `probe-flags` 36/36 · `probe-tactical-visuals` 10/10 — all coexist; `bootprobe` realErrors:[]; `t1probe` ok; `diag-classic` `nonBlank:346`. VISUALLY CONFIRMED (Lincoln's PD photo framed in the period brass mount, byte-confirmed offline). `wcag-auditor` N/A by construction (no new DOM — only an `<img>` src swap; alt text set by the callers, untouched). **Committed + pushed.**
>
> **⏳ NEXT — more H1 (each a NEW external-asset category → SURFACE for per-asset licence + anachronism sign-off):** weapons / flags / USCT / scenes imagery, then H2 reenactment footage (skippable, offline→procedural fallback). The pipeline is ready: add a category to `tools/prep-embed-assets.mjs` CATEGORIES + drop sources under `assets/<category>/`, run prep + build, then read `__ASSETS["<category>/<key>"]` from a new presentation seam. **Per-phase weather** remains a queued combat-path candidate (Antietam dawn-fog→afternoon via `_fldPhaseView` in T8 — prove byte-identity via probe-presets + probe-phased-ab). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D133/D132/D74/D54 → V1-CHECKLIST Phase H (H1 increment 1 ✅) → `src/21-photo-embed.js` + `tools/build.mjs` (the embed stage) + `tools/prep-embed-assets.mjs` + `tools/probe-photo-embed.mjs` + `.tmp/phase-h-design.md` (item 6 = H1/H2 imagery).

---

## ⚡ EARLIER — 2026-06-25 **PHASE H ("make it come to life") · BASE-ENGINE FIX — MULTI-PHASE 3D SCENE REBUILD: the live 3D battlefield now rebuilds when a multi-phase battle advances a phase — the new brigades become visible, the prior phase's meshes are disposed (no orphans, no GPU leak), the woods/walls/objective-beacon move to the new sector, and the camera re-frames it. Fixes the PRE-EXISTING base bug D131's bug-hunt surfaced. Combat byte-identical, proven 3 ways. NEXT in H = H1/H2 PD imagery + footage (SURFACE first — external assets)** (overnight run, D132)

> **The combat-path candidate Aaron chose (AskUserQuestion) over the H1/H2 external-asset layer (deferred — it's the one network/IP-licensing item that needs Aaron present to review each fetched asset, a poor fit for an unattended run).** **The bug (pre-existing, git-traced to predate T10/H1b — D131's bug-hunt SURFACED it):** the 4 multi-phase battles (Antietam · Gettysburg · Chickamauga · Vicksburg — 4 of 9 shipped; recon confirmed each phase declares its OWN distinct sector terrain + objective) advanced their phase in 3D by swapping in a FRESH unit cast + terrain + objective (`_fldAdvancePhase`→`_fldBuildPhase`, T8) but NEVER rebuilt the live 3D scene → new brigades INVISIBLE (no `_u3d` group), the prior phase's unit/officer/supply/arms/eng meshes ORPHANED, and the woods/walls/objective-beacon stranded at the OLD sector. (2D + the headless sim were unaffected → only real 3D play of a multi-phase battle showed it.)
>
> **Confirmed EMPIRICALLY first** (`tools/probe-phased-3d.mjs`, new): run against the pre-fix build it FAILED decisively (Antietam phase-1 `coverage=0` / `orphans=6`; the beacon `dz=65` off the new objective at phase 2) — both symptoms proven before a line changed.
>
> **What shipped:** new `fld3dRebuildPhaseScene()` (`src/tactical/T0-field-sandbox.js`), called from `_fldAdvancePhase` (`src/tactical/T8-phases.js`) ONLY when `__FIELD.mode3d` — re-runs the same per-phase build sequence as `fld3dInit` (terrain: fresh ground/objective-ring+beacon/walls/markers/woods/water + units + officers/supply/arms/flags/eng), then `_fld3dReaimPhase()` re-frames the survey camera onto the new objective (B-6 side-aware). The scene-level per-phase terrain meshes now route through a `_fld3dTrackPhase` tracker into `__FIELD._phaseScene[]`, disposed by `_fld3dDisposePhaseScene()` at the top of `fld3dBuildTerrain`; renderer/camera/lights/scene/groups persist; every unit-scoped builder already self-disposes.
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** the headless stepper (renderer 'none' → `mode3d` false) NEVER enters the rebuild → the sim is untouched. No combat/data/save file touched; no `_SAVE_VER`; never `fldRng`. **Proven THREE ways:** `probe-presets` 26/26; a new `tools/probe-phased-ab.mjs` headless A/B (4 battles × 5 seeds, committed pre-fix build vs fixed) = **20 comparisons, 0 diffs** (winner/casualties/phase-score/log/steps identical number-for-number; historically faithful — Antietam CS-tactical, Gettysburg/Vicksburg US, Chickamauga CS); + all 4 per-battle outcome probes green. Diff = `src/tactical/T0-field-sandbox.js` + `src/tactical/T8-phases.js` + `src/tactical/T13-engineering.js` (the bug-hunt fixes) + 2 new probes + the generated HTML.
>
> **Bug-hunt (6 diverse-lens Opus finders → 4 findings, ALL one GPU-leak class the rebuild introduced, ALL FIXED):** **(HIGH + MED, same)** `fld3dBuildEng` (T13) detached its group without disposing the berm/abatis/pontoon geometries+materials → the Vicksburg siege leaked them every advance → FIXED self-disposing (`_fld3dDisposeGroup` first; they're the group's children; does NOT touch water); **(MED)** `fld3dBuildWater` (T13) disposed its prior river only AFTER the no-river early-return → a river→no-river advance leaked it → FIXED by moving the dispose above the return; **(LOW)** the T8 catch fully swallowed exceptions, masking the very invisible-brigade failure → FIXED to `console.warn`. A completeness self-check confirmed EVERY builder the rebuild calls now self-disposes — the leak surface is fully closed. (The hunt's automated verify+critic legs were env-blocked — a hung 6th finder + an OOM on the 8 GB Mac during the focused re-run — so substituted by inline code-verification of all 4 findings + a standalone completeness-critic agent + the empirical re-vet; the D124/D128 precedent.)
>
> **Vet (PROPER VETTING, all GREEN, re-confirmed AFTER the T13 fixes):** `GATE OK`; `probe-phased-3d` **64/64** (all 4 battles, every phase: coverage=1 · 0 orphans · groupsChildren==units · the beacon re-seats on the new objective at every phase · woods rebuilt per sector · non-blank · 0 pe · 0 tex warnings); `probe-presets` **26/26** (byte-identity); `probe-phased-ab` **20/20** (0 diffs → **combat byte-identical**); `probe-atmospherics` 18/18 · `probe-weather` 30/30 · `probe-render-richness` 30/30 · `probe-audio-ambience` 22/22 · `probe-flags` 36/36 · `probe-tactical-visuals` 10/10 (T16–T19 + flags coexist); `bootprobe` realErrors:[]; `t1probe` ok; `diag-classic` `nonBlank:346`. VISUALLY CONFIRMED (the captured Burnside's-Bridge phase-2 shot now shows the fresh sector + visible Union brigades + the re-seated objective beacon under T17's dawn fog — previously empty). `wcag-auditor` N/A (no new DOM — only a camera re-aim + 3D mesh rebuild). **ENV:** `probe-engineering`/`probe-engineering-corps` are `waitUntil:'load'` → blocked by the documented THREE-CDN headless load-stall (D97/D110), NOT a regression — the eng/water 3D builders are exercised instead by the `domcontentloaded` `probe-phased-3d` (12 phase advances) + `probe-tactical-visuals`. **Committed + pushed.**
>
> **⏳ NEXT — H1/H2 (PD imagery + reenactment footage):** the `tools/build.mjs` tier/compress asset-ingestion pipeline (D71) + linked-assets-with-offline-fallback. **SURFACE for approval BEFORE starting** — the one external-asset / network / IP-licensing / single-file-stressing item (an external-audio-file/linked-asset layer is the same kind of fork → SURFACE first too). Other combat-path candidates: **per-phase weather** (Antietam dawn-fog→mid-morning→afternoon via `_fldPhaseView` in T8 — prove byte-identity via probe-presets + the A/B). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D132/D131/D74/D54 → V1-CHECKLIST Phase H → `src/tactical/T0-field-sandbox.js` (`fld3dRebuildPhaseScene`) + `src/tactical/T8-phases.js` (`_fldAdvancePhase`) + `tools/probe-phased-3d.mjs` + `tools/probe-phased-ab.mjs` + `.tmp/phase-h-design.md` (item 6 = H1/H2 imagery).

---

## ⚡ EARLIER — 2026-06-25 **PHASE H ("make it come to life") · H1b — FLAGS & INSIGNIA DEEPENING: every brigade now flies the flag its history records (First National at Bull Run, the ANV Southern Cross mid-war East, the Hardee disc in the West) and the Army-of-the-Potomac corps badges appear as their real Kearny-patch SHAPES (disc/trefoil/diamond/Maltese-cross/Greek-cross) — but ONLY on the 1863 fields where they were actually worn. Deepens the existing `src/tactical/T10-flags.js`. NEXT in H = H1/H2 PD imagery + footage (SURFACE first — external assets)** (overnight run, D131)

> **H1b deepens the flags layer that already existed (`src/tactical/T10-flags.js`; the D91 deepen-not-replace ethic) — and fixes three real defects found on recon:** the 2D path drew blank colour rectangles (it built an SVG + an `Image()` it never drew); corps-badge SHAPES were never rendered (only a text label, with a wrong map — II Corps a circle, V and VI both a generic "cross"); and badges showed on every battle regardless of date (an anachronism on the 1861-62 fields), with a roman-numeral substring bug mis-reading "II Corps" as "I Corps".
>
> **What shipped:** (1) **battle-AWARE flag selection** (`_FLD_BATTLE_META`) — **First National "Stars and Bars"** at First Bull Run (the ANV battle flag did NOT exist on 21 Jul 1861 — the smoke-confusion of the Stars and Bars with the U.S. flag THAT day is what prompted it) and in the early West (Shiloh); the **ANV battle flag (Southern Cross)** mid-war East; the **Hardee pattern** (blue + white disc) for the native Army of Tennessee; with **Longstreet's transferred Eastern brigades (Hood/Kershaw/Law) flying the ANV flag even at Chickamauga**; (2) **new patterns drawn for real** — first-national, hardee, polk (upright St-George cross, distinct from the ANV diagonal saltire), US cavalry guidon; (3) **corrected Kearny-patch corps-badge SHAPES as glyphs** — I=disc, II=trefoil, III=diamond, V=Maltese (a flared cross pattée), VI=Greek cross (distinct from V), XI=crescent, XII=star — division-coloured (1st=red/2nd=white/3rd=blue), with a light parchment **halo** so the shape silhouette reads on any background, and ALWAYS paired with a text label (CVD-safe: shape + label, never colour alone); (4) the **anachronism GATE** — corps badges (Hooker's Circular, 21 Mar 1863) appear ONLY at the Eastern post-Mar-1863 fields (Chancellorsville + Gettysburg) and NEVER on Bull Run / Malvern Hill / Antietam / Fredericksburg (where the corps existed but wore no badge yet — the HUD names the adoption date there as a teaching beat); (5) a **working 2D draw** (cached canvas blit, fog-gated) + a deepened **HUD** (flag thumbnail + badge swatch + a citation-grade provenance caption).
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** touches NO combat file. The render fns (`fldDrawFlags`/`fld3dBuildFlags`/`fldFlagHudSelected`) are reached only through the EXISTING T0 `typeof` seams (probes drive the sim headlessly and never call them). Reads sim READ-ONLY (`u.name/side/x/z/facing/alive/state`, `__FIELD.scenario/fog/units`) + `G.settings`; writes NO sim field, NEVER `fldRng`, no save-version bump. The install block wraps ONLY `fld3dBuildUnits`/`fld3dDispose` (texture disposal + a reinforcement re-skin) via a generic `_fldFlagKeep` marker-propagation helper (the D130 lesson). Diff = `src/tactical/T10-flags.js` (deepened) + `tools/probe-flags.mjs` (new) + the generated HTML. No combat/data/save file touched → 9 battles byte-identical seed-for-seed.
>
> **CITATION-GRADE (research+verify Workflow, 25 agents).** The fact-sheet CONFIRMED the core code and added the Chickamauga ANV-transplant split + the Vicksburg Western-flag default, and softened the badge-order citation ("General Orders No. 53" → "Hooker's Circular of 21 March 1863" — the surviving primary doc is a circular; the date is solid). ≥2 reputable sources per flag/badge/date (Encyclopedia Virginia · Smithsonian NMAH · Britannica · confederateflags.org · Tennessee State Museum · crwflags.com FOTW · Washington Times · Gettysburg Flag Works · ABT); anti-Lost-Cause.
>
> **Bug-hunt (6 diverse-lens Opus finders × 3-skeptic default-refute + a completeness critic; 46 agents; critic HIGH) → 1 confirmed + 6 critic areas → 3 in-scope fixes + 1 base bug SURFACED + 2 noted:** **(confirmed MED — a PRE-EXISTING base T0/T8 bug, git-traced to predate T10 → SURFACED, NOT fixed in this presentation-only milestone)** a phased-battle 3D phase-advance (`_fldBuildPhase`/`_fldAdvancePhase` in the combat-path T8) swaps in new unit ids but never rebuilds the `_u3d` 3D groups → new-phase units get no 3D group + the prior groups orphan (H1b only marginally enlarges the orphaned-mesh footprint by adding CanvasTextures). **Recommended dedicated fix:** call `fld3dBuildUnits()`+`fld3dBuildFlags()` from `_fldAdvancePhase` when `mode3d`. **(in-scope fixes, applied + re-vetted)** a reinforcement re-skin (reinforcements now fly textured flags), a badge silhouette halo (a dark-blue badge read poorly on the dark HUD), fog-gate probe coverage (no flag/badge betrays a hidden enemy). **(noted polish)** batteries fly a brigade flag; the US-guidon/CS-cavalry asymmetry.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-flags` **36/36** (battle-aware flags · the date gate · corrected shapes II=trefoil + V/VI distinct · roman parse · division colours · CS-never-badged · the Chickamauga split + Vicksburg flag · CVD-safe HUD + the teaching note · live 3D textured flags + badge meshes at Gettysburg · the Antietam gate builds NO badge mesh · **fog-of-war: no flag/badge betrays a hidden enemy** · byte-identity sim seed + fields invariant across a 200-frame burst · 0 tex warnings · 0 pe); `probe-presets` **26/26** (byte-identity → **combat byte-identical**); `probe-atmospherics` **18/18** · `probe-weather` **30/30** · `probe-render-richness` **30/30** · `probe-audio-ambience` **22/22** (T16/T17/T18/T19 all coexist); `probe-tactical-visuals` **10/10**; `bootprobe` realErrors:[]; `t1probe` 14/14; `diag-classic` `nonBlank:346`; `wcag-auditor` AA (no changes). **Committed + pushed.**
>
> **⏳ NEXT — H1/H2 (PD imagery + reenactment footage):** the `tools/build.mjs` tier/compress asset-ingestion pipeline (D71) + linked-assets-with-offline-fallback. **SURFACE for approval BEFORE starting** — it is the one external-asset / network-touching / IP-licensing / single-file-stressing item, not an unattended job; an external-audio-file/linked-asset layer is the same kind of fork → SURFACE first too. Also: a **per-phase weather** candidate (Antietam dawn-fog→mid-morning→afternoon via `_fldPhaseView` in T8 — a combat-path file, so prove byte-identity via probe-presets), and the discovered **phased-3D base bug** (above) as a dedicated base-engine fix. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D131/D130/D91/D74/D54 → V1-CHECKLIST Phase H (H3 i1✅ i2✅ i3✅ · H4✅ · H1b✅) → `src/tactical/T10-flags.js` + `tools/probe-flags.mjs` + `.tmp/phase-h-design.md` (item 6 = H1/H2 imagery).

---

## ⚡ EARLIER — 2026-06-25 **PHASE H ("make it come to life") · H4 — BATTLE-AMBIENCE AUDIO DEEPENING: a default-OFF, opt-in, stereo-spatialized procedural battle-soundscape bed — the "one continuous roar" of massed musketry, the ground-shaking bass of a cannonade, and the firing brigades' reports placed in the stereo field where the smoke actually rises — layered UNDER the existing T9 din+SFX, fog-gated so it never betrays a hidden foe. NEXT in H = H1b flags/insignia polish** (overnight run, D130)

> **H4 ships the audio "come-to-life" increment — and gives the battlefield its voice.** New `src/tactical/T19-battle-ambience.js`: a spatialized procedural bed UNDER T9's din+SFX that pairs 1:1 with the T16 gunsmoke (sound comes from where the smoke is) — (1) a continuous **rolling-fire wash** (looping band-passed noise + a slow swell-LFO, gain tracking battle intensity), (2) a **cannonade sub-bass rumble** (gated up by the count of brigades actually working their guns), (3) **located cannon/musket reports** (cannon = crack + body + rolling tail; musket = a volley "rip") panned in stereo by the firing brigade's field-x and **fog-gated** (a hidden foe makes no located sound). DEFAULT OFF; discoverable via a new accessible "Battle ambience" row on T9's audio panel; one-time captioned.
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** touches NO combat file. WRAPS `fld3dRender`/`fld2dDraw`/`fldExit`/T9's `_fldAudioPanelRows` BY ASSIGNMENT (a generic `keep()` propagates EVERY prior marker `._atmo`/`._wx`/`._t9audio`/T18's so sibling probes still see the chain). OWNS a separate AudioContext (the AUDIO_SCORE one-context-per-subsystem convention). Reads firing state READ-ONLY (`u._artFlash`/`u.targetId`/`u.ammo`/`u.state`/`u.x`/`u.id`/`u.side`/`u.alive`, `__FIELD.phase/fog/units`); writes NO sim field, NEVER `fldRng`, no `_SAVE_VER` (the `battleAmbience` flag is lazy-defaulted). The scheduling DECISIONS (intensity/fog-gate/pan) record in a `FLDAMB` state object whether or not a real audio device exists → fully testable headless. Diff = `src/tactical/T19-battle-ambience.js` (new) + `src/00-manifest.json` (T19 after T18) + `tools/probe-audio-ambience.mjs` (new) + the generated HTML.
>
> **ACCESSIBILITY / PERF (Intel UHD-617 floor):** **DEFAULT-OFF** (`battleAmbience !== true` → zero sound, never built — the byte-identical-mix default), **fully suppressed under `reduceMotion`** (the calm default), volume scaled by `battleLoud` (off→silent) + master capped low (≤0.20, under the din's 0.26) so it never overwhelms the cues ("never jarring"). Reports defer until the AudioContext is running (no first-gesture stampede); the bed self-silences on the 'over' screen (no idle-graph drain); source-voice pool bounded + all nodes disposed + the context suspended on `fldExit`. CVD-safe; the toggle row reuses T9's AA-vetted markup → `wcag-auditor` N/A by construction.
>
> **Bug-hunt (6 diverse-lens Opus finders × 3-skeptic default-refute verify + a completeness critic; 19 agents; critic confidence HIGH; "NO byte-identity violation, NO fog leak, marker-chain correct") → 1 confirmed + 2 critic edge-bugs + 1 LOW, ALL FIXED:** **(confirmed MED)** the one-time caption flag was burned BEFORE the caption rendered → a user enabling captions mid-battle never saw the cue → consumed only inside the captions-on branch; **(critic MED)** located reports scheduled against a SUSPENDED context (pre-gesture) → a stacked "stampede" on first click → gated on `ctx.state==="running"`; **(critic LOW)** the live graph idled on the 'over' screen → self-silence on `phase==="over"`; **(critic LOW)** StereoPanners diluted the voice cap → a separate panner pool (cap counts voices only). A lifecycle "stale per-phase state" finding was REFUTED 3/3 (singleton `__FIELD`, men-delta clamps, cooldowns expire). Probe strengthened 19→22.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-audio-ambience` **22/22** (opt-in activates + beds built + reports scheduled · pan ∈[-1,1] · separate panner pool · suspended-ctx defers · 'over' self-silence · sim-seed INVARIANT (240 pure ticks) · `errN===0` · accessible row · DEFAULT-OFF silent · reduceMotion suppressed · `battleLoud="off"` silent · fog-gated 5→0 control 3 · `fldExit` disposes); `probe-presets` **26/26** (byte-identity → combat byte-identical); `probe-atmospherics` **18/18** (T16 + marker chain) · `probe-weather` **30/30** (T17) · `probe-render-richness` **30/30** (T18) — all coexist; `probe-tactical-visuals` **10/10** (0 tex warnings); `bootprobe` realErrors:[]; `t1probe` 14/14; `diag-classic` `nonBlank:346`. (Audio can't be screenshotted — the probe verifies the WebAudio graph builds + every scheduling decision; the visual-confirmation analog for a sound layer.) **Committed + pushed.**
>
> **⏳ NEXT — H1b (flag/insignia polish, extend `src/tactical/T10-flags.js`):** broader CSA/US flag set + AotP corps badges on the selected-brigade HUD; CVD-safe (shape+label, never flag-colour alone). Then **H1/H2** PD imagery + reenactment footage (the asset-ingestion pipeline — **SURFACE for approval BEFORE starting**; external assets / network / licensing-IP, stresses the single-file deliverable; also note an external-audio-file/linked-asset layer is the same kind of fork → SURFACE first). **Per-phase weather** remains a candidate (Antietam dawn-fog→afternoon via `_fldPhaseView` in T8 — a combat-path file, so prove byte-identity via probe-presets). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D130/D129/D91/D74/D54 → V1-CHECKLIST Phase H (H3 i1✅ i2✅ i3✅ · H4✅) → `src/tactical/T9-audio.js` + `src/tactical/T19-battle-ambience.js` (the audio seam) + `src/tactical/T10-flags.js` (the H1b seam) + `.tmp/phase-h-design.md` (item 5 = H1b).

---

## ⚡ EARLIER — 2026-06-24 **PHASE H ("make it come to life") · H3-i3 — TERRAIN & UNIT RENDER RICHNESS: the battlefield itself made to feel alive — procedural ground texture (grass / dry-ochre / mud / tilled-furrow patches), woods that sway in the wind, banner cloth that ripples, marching columns that bob, selected brigades that pulse, and a fallen brigade that fades into the ground instead of snapping out; 2D + 3D. NEXT in H = H4 audio deepening** (overnight run, D129)

> **H3-i3 ships the next "come-to-life" increment — the field, the woods, and the men all gain texture + motion.** New `src/tactical/T18-render-richness.js`: (1) **3D ground vertex-colour enrichment** — procedural grass mottle + dry-ochre / damp-lush / bare-earth patches + tilled crop-row corduroy (VERTICES recoloured only, never positions → units still seat on the analytic `fldTerrainH`, never float/sink); (2) **woods wind-sway** (per-cone tilt-about-base on the woods `InstancedMesh`); (3) **banner cloth ripple** (per-unit flag twist/dip, whichever "flag" mesh is present — base or the T10 textured flag); (4) **selection-ring pulse** + a **march bob** for actively-moving brigades; (5) a **casualty fade-out** on death (the brigade settles toward the ground + fades over ~0.85s). Plus 2D-canvas equivalents: a CACHED soft field-grain (dry/lush/earth patches), a pulsing selection halo, the same casualty fade.
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** touches NO combat file. WRAPS `fld3dBuildTerrain` (enrich + capture) / `fld3dRender` (frame hook) / `fld3dSyncUnit` (per-unit polish) / `fld2dDraw` (2D) / `fldExit` (dispose) BY ASSIGNMENT (the T9/T16/T17 seam; propagates the `._wx`/`._atmo` markers so the chain stays introspectable). All variation is **analytic value-noise + wall-clock sines — NEVER `fldRng`**; reads ONLY render state + read-only sim fields (`u.x`/`u.z`/`u.alive`/`u.state`/`u.id`, `__FIELD.sel`); writes NO sim field, no `_SAVE_VER`. Proven by `probe-presets` **26/26** byte-identity AND a NEW per-burst sim-field-invariance assertion (x/z/men/morale/facing/formation unchanged across 200 render frames). **Byte-identical default: `renderRich="off"` (or a fresh launch with it set) → un-enriched ground + unswayed woods + flat flags.** Diff = `src/tactical/T18-render-richness.js` (new) + `src/00-manifest.json` (T18 after T17) + `tools/probe-render-richness.mjs` (new) + the generated HTML.
>
> **ACCESSIBILITY / PERF (Intel UHD-617 floor):** every MOTION effect (woods sway · flag ripple · selection pulse · march bob · casualty fade) is suppressed under `reduceMotion` — the STATIC ground texture STAYS (faithful detail, not motion; the T17 precedent). `renderRich="off"` disables the whole layer (motion reverts mid-battle; the 3D ground tint reverts on the next launch — the T16/T17 apply-time pattern, documented inline). Woods sway recomputes each instance matrix from a cached base (no drift); `fldLow()` trims sway amplitude + grain cell size; the **2D field-grain is cached to an offscreen canvas** (built once per field/scale, blitted with ONE `drawImage` — no per-frame fillrate on the weakest 2D-fallback GPU). CVD-safe (no colour-encoded info); no new DOM/text → `wcag-auditor` N/A by construction.
>
> **Bug-hunt (6 diverse-lens Opus finders × 3-skeptic default-refute verify + a completeness critic; 37 agents; critic confidence HIGH) → 1 substantive + 7 real LOWs, ALL FIXED:** **(the one substantive, MED/HIGH across 4 lenses, 3/3)** the casualty-fade `g.position.y -= (1-rec.fade)*5` was CUMULATIVE — the base `fld3dSyncUnit` early-returns for a dead unit and never re-seats its Y, so the per-frame decrement compounded to **~125 yards of sink** (frame-rate dependent), plunging the fading brigade through the terrain → **rewritten ABSOLUTE** off `fldTerrainH`, bounded ~5yd (probe now asserts `maxSink ≤ 8`; measured 4.96). Pure visual — no sim write, D74 intact. **(LOW fixes)** a **fog-ghost guard** (`rec.wasVis` — a fog-hidden enemy that dies no longer pops into view as a fading block); the **selection ring now fades with the corpse**; `_rrSetGroupOpacity` no longer flips the T10 flag's `transparent` flag (alpha-corruption fix); the **2D grain offscreen cache** (perf); `renderRich="off"` **mid-battle motion revert**; the probe **sim-field-invariance** snapshot; a counted + once-warned **`FLDRR_S.errN`** the probe asserts is 0. Probe strengthened **24→30**.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-render-richness` **30/30** (ground enriched [meanAbsDelta 0.0178, 92% verts] + positions untouched · woods sway/reset · flag ripple/reset · selection pulse · casualty fade visible+opacity-in-(0,1)+**BOUNDED-sink**+**fog-ghost-guarded**+hidden-when-done · sim seed + **sim-field INVARIANT** · `errN===0` · off/reduceMotion correct · 2D paints); `probe-presets` **26/26** (byte-identity → combat byte-identical, re-confirmed post-fix); `probe-atmospherics` **18/18** (T16 coexists); `probe-weather` **30/30** (T17 coexists); `probe-tactical-visuals` **10/10** (0 tex warnings); `bootprobe` realErrors:[]; `diag-classic` `nonBlank:346`; `t1probe` 0 fails. VISUALLY CONFIRMED. **Committed + pushed.**
>
> **⏳ NEXT — H4 (audio deepening, per D91/`.tmp/phase-h-design.md` item 4):** layer a battle-intensity-driven musketry/cannon ambience bed under the existing `src/tactical/T9-audio.js` SFX (default-off, captioned, accessible) — pairs with the T16 gunsmoke. Then **H1b** flag/insignia polish (extend `T10-flags.js`) → **H1/H2** PD imagery + reenactment footage (the asset-ingestion pipeline — **SURFACE for approval BEFORE starting**; external assets / network / licensing-IP, stresses the single-file deliverable). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Per-phase weather** also remains a candidate H3 add (Antietam dawn-fog → mid-morning → afternoon via `_fldPhaseView` in T8 — a combat-path file, so prove byte-identity). **Resume map:** START-HERE → this block → DECISIONS D129/D128/D127/D74/D54 → V1-CHECKLIST Phase H (H3 i1✅ i2✅ i3✅) → `src/tactical/T9-audio.js` (the H4 seam) + `.tmp/phase-h-design.md` + (for reference) the T16/T17/T18 wrap-by-assignment pattern.

---

## ⚡ EARLIER — 2026-06-24 **PHASE H ("make it come to life") · H3-i2 — WEATHER + TIME-OF-DAY ATMOSPHERE: procedural sky/light re-tint (background · fog · sun · hemisphere) + rain/snow precipitation per a citation-grade per-battle `weather` hint; 2D + 3D. The marquee visible win is Antietam's dawn Cornfield ground-fog. NEXT in H3 = i3 terrain/unit render richness** (overnight run, D128)

> **H3-i2 ships the next "come-to-life" increment — and gives every battle a sky matched to its history.** New `src/tactical/T17-weather.js` re-tints the 3D `scene.background`/`fog`/sun/hemisphere AND washes the 2D canvas to a resolved `{sky, time}` palette (sky ∈ clear/overcast/rain/fog/haze/snow; time ∈ dawn/morning/midday/afternoon/dusk), plus a procedural rain/snow `THREE.Points` cloud (the T16 shader pattern) + a 2D precip hatch, and a one-time accessible weather note via the existing role=status `fldScenarioBanner`. **The marquee: Antietam's pale warm dawn Cornfield ground-fog** (distant brigades fading into the mist, the objective still readable) — the one genuinely non-clear sky; the rest carry faithful time-of-day light (warm afternoons, cool mornings).
>
> **CITATION-GRADE DATA (research+verify Workflow, 18 agents).** The verifiers REFUSED to fabricate drama — Gettysburg `haze→clear` (Rev. Jacobs' instrument log: bright at the 2pm Pickett's-Charge window), Fredericksburg `fog→clear/afternoon` (the Rappahannock fog lifted ~10am, before the noon-to-dark assault — the note still teaches it), Shiloh `clear/morning` (the rain was OVERNIGHT), a misattributed Vicksburg source removed. **The honest finding: the "fog of war" at most of these battles was POWDER-SMOKE (already shipped via T16), not weather** — only Antietam's dawn fog is a real non-clear sky. Final hints (in `data/*.json`): bullrun1 clear/afternoon · malvernHill clear/afternoon · **antietam fog/dawn** · fredericksburg clear/afternoon · chancellorsville clear/morning · gettysburg clear/afternoon · shiloh clear/morning · vicksburg clear/midday(no-op) · chickamauga clear/afternoon.
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** touches NO combat file. WRAPS `fld3dInit`/`fld3dRender`/`fld2dDraw`/`fldExit` by ASSIGNMENT (the T9/T16 seam; propagates T16's `._atmo` marker so the chain stays introspectable). Reads ONLY the scenario's `weather` hint (`_scenTop.weather` multi-phase, else `scenData.weather`) + render state; OWN LCG — **never `fldRng`** (sim seed probe-proven unchanged across the wx render loop); no sim write, no `_SAVE_VER`. **THE BYTE-IDENTICAL DEFAULT: a missing hint — or `clear`/`midday` — resolves to `null` and applies ZERO change** (every shipped look preserved). Diff = `src/tactical/T17-weather.js` (new) + `src/00-manifest.json` (T17 after T16) + 9 `data/*.json` weather blocks + `tools/probe-weather.mjs` (new) + the generated HTML.
>
> **ACCESSIBILITY / PERF (Intel UHD-617 floor):** the static tint is motion-free → applies under `reduceMotion`; only PRECIPITATION (motion) is suppressed under it (probe-verified: tint kept, no `wxPrecip`); `G.settings.weather==="off"` disables the whole layer. Fog far-plane held **≥1500** in every palette so the objective never vanishes. One precip draw call + `fldLow()` budget (520/200) + a `gl_PointSize` driver clamp. CVD-safe; no new DOM/text (the note reuses the AA-vetted banner) → `wcag-auditor` N/A.
>
> **Bug-hunt (6 diverse-lens Opus finders × 3-skeptic default-refute + a critic; the critic leg STALLED → substituted by an inline completeness pass, the D124 precedent) → 1 HIGH + 1 LOW fixed:** **(HIGH)** the atmosphere note re-fired EVERY frame — `_wxLaunchKey` keyed on `__FIELD.seed` (which `fldRng` mutates each tick) → re-keyed on the stable `__FIELD.scenario` + a note-once probe lock; **(LOW, real)** inherited-property sky names (`constructor`/`__proto__`) bypassed the `!FLDWX.SKY[sky]` guard → `hasOwnProperty` validation + a malformed-input lock. Precip-dispose + malformed locked by new assertions; a probe test-pollution bug (synth scenes mutated shared `GAME_DATA`) caught + fixed (shallow-copy inject).
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-weather` **30/30** (byte-identical default · each palette applies · fog far ≥1500 real battle · `wxPrecip` built + DISPOSED · reduceMotion suppresses precip keeps tint · once-per-launch note · malformed safety · `gl_PointSize` clamp · sim seed UNCHANGED); `probe-presets` **26/26** (byte-identity → **combat byte-identical**); `probe-atmospherics` **18/18** (T16+T17 coexist); `probe-tactical-visuals` **10/10** (tinted battles render, beacons seated, 0 tex warnings); `bootprobe` realErrors:[]; `diag-classic` `nonBlank:346`; `t1probe` ok; broad no-regression GREEN in isolation. VISUALLY CONFIRMED. **Committed + pushed.**
>
> **⏳ NEXT — H3-i3 (terrain & unit render richness):** ground texture variation (grass/mud/crop procedural detail), subtle wind-sway on the woods cones (reduceMotion-off), banner cloth ripple (ties H1b flags), selection/charge motion polish, casualty fade — all procedural, same byte-identity seam. **Per-phase weather is a candidate i3 sub-item** (Antietam's dawn fog → mid-morning → afternoon, via `_fldPhaseView` in T8 — a presentation-only splice). The ordered Phase-H roadmap after i3: H4 audio → H1b flags → H1/H2 PD imagery+footage (**SURFACE first** — external assets). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D128/D127/D74/D54 → V1-CHECKLIST Phase H (H3 i1✅ i2✅) → `src/tactical/T17-weather.js` (the seam to extend: `fldWxResolve`/`_wxApplyTime`/`FLDWX.SKY` palettes) + `src/tactical/T16-atmospherics.js` + `tools/probe-weather.mjs` + `.tmp/phase-h-design.md`.

---

## ⚡ EARLIER — 2026-06-24 **PHASE H ("make it come to life") · H3-i1 — BATTLEFIELD GUNSMOKE ATMOSPHERICS: procedural black-powder smoke / artillery muzzle-belch / impact dust on the real-time tactical engine (2D + 3D), the period "fog of war." Aaron chose Phase H as the next pillar after E completed. NEXT in H3 = i2 weather + time-of-day** (overnight run, D127)

> **Phase E is COMPLETE (E1✅ E2✅ E3✅ E4✅); Aaron picked Phase H (AskUserQuestion).** H3-i1 ships the marquee battlefield-atmosphere increment — new `src/tactical/T16-atmospherics.js`: procedural gunsmoke that billows from firing lines + artillery, accumulates into a sector pall, drifts on a per-battle wind, and slowly clears; artillery muzzle-belch; impact dust. Renders in BOTH the 2D canvas (cached soft-sprite stamps) and the live 3D scene (one `THREE.Points` cloud + a tiny per-particle shader). Teaching tie-in: black-powder smoke literally WAS the fog of war.
>
> **ARCHITECTURE — combat byte-identical BY CONSTRUCTION (D74):** touches NO combat file. WRAPS `fld2dDraw`/`fld3dRender`/`fldExit` by ASSIGNMENT (the T9-audio seam). Reads firing/impact state READ-ONLY (`u.targetId`/`u.ammo`/`u._artFlash`/`u.underFire`/`u.facing`/`u.state`); uses its OWN private LCG — **never `fldRng`** (sim seed probe-proven unchanged); no sim write, no `_SAVE_VER`. Diff = `src/tactical/T16-atmospherics.js` (new) + `src/00-manifest.json` (T16 after T15) + `tools/probe-atmospherics.mjs` (new) + the generated HTML. **No combat/data/save file touched.**
>
> **ACCESSIBILITY / PERF (Intel UHD-617 floor):** fully suppressed under `reduceMotion` AND `G.settings.atmospherics==="off"` (probe-verified 0 particles); budget capped (200 high / 84 low via `fldLow()`); ONE `THREE.Points` cloud + a `gl_PointSize` clamp to the driver's `ALIASED_POINT_SIZE_RANGE` max; fog-aware (no smoke for a hidden enemy → never betrays a position). Neutral grayscale cue (CVD-safe); no new DOM/text → `wcag-auditor` N/A by construction.
>
> **Bug-hunt (14 agents, 6 lenses × default-refute + critic; invariantsHold=TRUE, HIGH) → 2 confirmed, both LOW, both FIXED:** (1) fog-leak gate had ZERO probe coverage (gate code was already correct, structurally identical to the shipping T0:1551/1820 fog-hide pattern) → added a geometry-independent **controlled fog-gate test** (silence all units, force only hidden enemies to fire → 0 smoke under fog, 21 with fog lifted = control); (2) 3D `gl_PointSize` could exceed the Intel driver cap (cosmetic) → in-shader `min(uMaxPoint,…)` clamp + probe assertion.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-atmospherics` **18/18**; `probe-presets` **26/26** (9-baseline byte-identity → **combat byte-identical**, seed-for-seed); `probe-tactical-visuals` **10/10** (render-path cross-check, 0 texture warnings, beacons seated, re-confirmed post-clamp); `bootprobe` `realErrors:[]`; `diag-classic` `nonBlank:346`; `t1probe` ok; no-regression sweep GREEN (field/fog/motion/arms/officers/engineering-corps/csplayer/tactical-roster/accessibility/command/camp). VISUALLY CONFIRMED (2D dense pall over the Hornet's Nest, brigades readable; 3D smoke at points of contact). **Committed + pushed.**
>
> **⏳ NEXT — H3-i2 (weather + time-of-day atmosphere):** procedural sky/lighting + optional rain/snow (reusing T16's Points infra) driven by a scenario weather/time hint — overcast/rain/fog/dawn-dusk per battle. **Design + data-scoping ready at `.tmp/phase-h-design.md`** (recon found scenarios carry weather only in PROSE → i2 must ADD a structured citation-grade hint field; unset → clear/midday → byte-identical default). The full ordered Phase-H roadmap (i2 weather → i3 terrain/unit richness → H4 audio → H1b flags → H1/H2 PD imagery+footage [SURFACE first — external assets]) is in that brief. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D127/D126/D74/D54 → V1-CHECKLIST Phase H (H3 partial) → `src/tactical/T16-atmospherics.js` (the seam to extend) + `tools/probe-atmospherics.mjs` + `.tmp/phase-h-design.md`. The atmospherics seam is mature: wrap `fld3dInit`/`fld3dRender`/`fld2dDraw` by assignment, read-only, reduceMotion/perf/fog-gated — extend it for i2 weather.

---

## ⚡ EARLIER — 2026-06-24 **E3-i2 THE DEDICATED FULL WCAG 2.2 AA AUDIT SWEEP — the exhaustive per-surface contrast/ARIA/focus/keyboard pass + root-cause fixes across every shipped surface (desk tabs · menus · battle HUD · overlays · tactical 2D/3D readouts). This COMPLETES E3 / S4 accessibility (i1+i2); NEXT = Phase F / I / H / J** (overnight run, D126)

> **The full AA sweep ships — and E3 closes.** i1 added the modes + the var-driven core; i2 is the exhaustive inline-hex/ARIA/focus sweep i1 deliberately deferred. **PURE presentation/markup — byte-identical combat BY CONSTRUCTION (D74)** (`probe-presets` 9-baseline byte-identity GREEN; no combat/RNG/save/`_SAVE_VER` touched). Diff = 29 `src/` files + `tools/probe-accessibility.mjs`.
>
> **KEYSTONE — two TEXT-ONLY token redefines.** Both `--rule #8a7350` (3.5–4.4:1 as the standard secondary-label, used ~40× across FROZEN base.html classes + module-inline `color:var(--rule)` headings) AND `--blood-lt #a83d33` (2.5–3.0:1 — the FIRE order button `.obtn.fire`, `#log .e.hit`, `.tagn`, `.verdict.lose`) are text-only in base.html, so an **always-on injected `:root{--rule:#a89066;--blood-lt:#d8745c}`** (in `src/97-accessibility.js` `_a11yCss`, applied at boot) fixes EVERY text use at once — sidestepping the inline-specificity no class override can beat, and fixing frozen base.html classes without editing base.html (`#a89066` = the exact tone base.html already uses for the same fix in its loot/readability cards). **LIGHT-MENU SAFE (verified):** `.gn-paper` defines its own `.gn-col-head{#5c3c18}`/masthead classes and references NEITHER token; HC mode re-redefines both at higher specificity so it still wins. Plus: a **universal `:focus-visible` fallback ring**, a **`.upg[aria-pressed=true]:focus-visible` !important override** (the inline pressed outline hid the focus delta), the base **`#toast` made a polite `role=status` region** (4.1.3), and **HC per-surface extensions** reaching `#wdContent`/`#wdTabs`/`#fldHud`/`#fldBar`/`#fldRoot`/sheet dialogs/control borders (dark-scoped → light menu untouched).
>
> **~50 module-inline colour fixes** (canonical status palette: green `#4a6b3a`→`#639452`/`#6a9a58`/`#6f9e5a`; red `#9c3b2e`→`#d07060`/`#d06862`/`#d8745c`/`#da6a5a`; fills→3:1 tones; rarity `#7a5cff`→`#9a86f0`/`#8a8276`→`#9a9184`) + **~16 ARIA/focus/keyboard fixes** (aria-pressed on the desk-tab toolbar [chosen over a 18-tab role=tablist to avoid an unimplemented roving-tabindex expectation] + lever/wildcard/prep-order/blockade toggles; aria-expanded+controls on every disclosure; the tutorial `#tutTitle` focus ring; the help "?" aria-label + the tactical-help modal background-inert + focus-return-to-trigger; the T0/T6 speed buttons value-in-name; the T0 2D unit side-letter glyph dark stroke-halo → ~15.7:1; a 2.5.8 target-size bump on the save-slot buttons). Authored by me + 4 disjoint-file Sonnet editors.
>
> **A real CSS group-opacity compositing bug** (a child `opacity:1` cannot escape a parent row's `opacity:.85`) was caught by self-review + the bug-hunt and fixed PROPERLY by moving the dimming to the LABEL span only in `_brgBar` (85) / `_morMeter` (33) / blockade `meter()` (60); the disabled weapon/artillery cards dim an inner wrapper, the unavailable REASON rendering as a full-opacity sibling.
>
> **Layered adversarial verification — each layer caught deeper misses:** a 28-agent audit Workflow (13 file-batch finders × default-refute verify × synthesis × critic) found **127 violations**; an 11-agent bug-hunt (6 diverse-lens finders × default-refute × completeness critic) found **3 confirmed + a HIGH critic gap** (the un-propagated opacity fix, 2 unlifted blockade greens, an `aria-hidden`-on-the-focused-trigger in the help modal, a vacuous probe step); a **report-only `wcag-auditor` confirm + two whole-`src/` contrast scans caught 7 MORE DOM-text misses** (`_cmdLeadWord` rating words, `_armRarityCol`/`_artRar` rarity labels, the T11 `.fld-cb-status.bad` error border, command's `#d66040` 3.92:1 on the lighter `#2e2816` card overlay) → ALL fixed + re-probed. Canvas/3D mesh/flag fills + decorative aria-hidden triple-encoded grade dots (D96) correctly held EXEMPT (DOM-AA scope).
>
> **`probe-accessibility` strengthened 17→25 steps** (the token-redefine cascade computed on `<html>` + reaching FROZEN base classes; the always-on + HC rules present; the toast live region; a numeric canonical-palette battery; a **RENDER-SAMPLE that instantiates `_brgBar`+`_morMeter` and reads each value span's EFFECTIVE composited opacity + ratio**; the HC desk/HUD black-ground lift; the HC-must-not-touch-`.gn-paper` guard). **Vet (PROPER VETTING, all GREEN):** `GATE OK`; `probe-accessibility` **25/25** 0 pe; `probe-presets` **26/26** (byte-identity → combat byte-identical); `bootprobe` realErrors:[]; `diag-classic` `nonBlank:346`; `t1probe` 14/14; ~30 UI/teaching/command/tactical probes PASS, 0 pe. **Committed + pushed.**
>
> **⏳ NEXT — Phase F / I / H / J (E is now COMPLETE: E1✅ E2✅ E3✅ E4✅).** Pick the next pillar: **F** content systems / **I** loot + the Soldier's Story (D93 prosopography — the EA-style play-as-anyone career mode), **H** the graphics/footage/audio "come-to-life" pass, **J** polish/meta. The symmetric AI-GM is the [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D126/D125/D124/D74/D54 → V1-CHECKLIST (Phase E ALL ✅; pick F/I/H/J) → the relevant `src/*.js` + `tools/probe-*.mjs` for the chosen pillar. The accessibility seam is mature: `src/97-accessibility.js` (`_a11yCss` always-on AA block + HC extensions) + `tools/probe-accessibility.mjs` (25 steps incl. the per-surface AA assertions) — extend these as new UI lands.

---

## ⚡ EARLIER — 2026-06-24 **E3-i1 THE ACCESSIBILITY HUB + the 4 a11y modes (high-contrast · CVD-safe · SR turn/battle narration · dyslexia text) — a new guarded `src/97-accessibility.js`. This is the FIRST half of E3 (S4 accessibility); NEXT = E3-i2 the dedicated FULL WCAG 2.2 AA audit sweep across every shipped surface** (overnight run, D125)

> **The accessibility-modes layer ships (E3-i1).** New guarded `src/97-accessibility.js` — a one-stop **Accessibility hub** with the four checklist modes + a reduced-motion mirror: **High contrast** (overrides the `:root` design tokens → brighter accents/rules/labels + a darker ground + a universal `:focus-visible` ring; the light-parchment main-menu broadsheet — already ~12:1 — is left untouched by construction), **Colour-blind safe** (drives the base-owned `cbAids` glyph+word redundancy; **cbAids is the single source of truth** — no separate latch), **Screen-reader narration** (a NEW *strategic*-layer `aria-live=polite` region `#a11yLive` + `a11yAnnounce` + a per-turn situation summary — the genuine gap, since only the tactical battle was narrated via `fldAnnounce`; default ON), **Dyslexia-friendly text** (a high-legibility sans stack + relaxed letter/word/line spacing; no external font — $0/single-file). Surfaced from a main-menu "♿ ACCESSIBILITY" button (observer-injected, no-latch) + a Play Your Way hand-off; boot-load from a `cw_a11y` bundle + a `prefers-contrast` auto-detect.
>
> **ARCHITECTURE — PURE settings/presentation; byte-identical combat BY CONSTRUCTION (D74):** writes ONLY non-combat a11y flags (`a11yContrast`/`a11yDyslexia`/`a11yNarrate`) + the base's OWN `reduceMotion`/`cbAids` mirrors + a private `cw_a11y` bundle (the new flags only). NO combat knob, NO RNG, NO `_SAVE_VER`. No combat/tick/resolve/bridge path references `a11y*` (probe static-scan). Diff = `src/97-accessibility.js` (new) + `src/00-manifest.json` (97 after 96) + `src/30-president-shell.js` (one guarded `a11yAnnounceTurn(C)` hook) + `src/95-playstyle.js` (the guarded hand-off row) + `tools/probe-accessibility.mjs` (new) + the generated HTML.
>
> **Bug-hunt (9-agent panel, 17 agents; critic HIGH) → 10 findings ALL FIXED + probe-locked/measured:** **(HIGH) I4-1** high-contrast made the LIGHT-parchment main menu unreadable (forced the transparent `.gn-col` black + brightened `.gn-deck`) → removed both (the menu is its own parchment theme using no `--vars`; measured headline **13.47:1** AAA, `.gn-col` transparent). **(HIGH NEW-1 + MED I3-1/I5-1)** a `&& G.campaign` save-gate + a one-directional `a11yCvd→cbAids` latch resurrected turned-off settings (the D123 anti-pattern) → **cbAids made the single source of truth** + the campaign gate removed (`saveLocal` co-writes unconditionally, like the frozen base Settings buttons). **(MED) I6-1** `a11yTurnSummary` checked `"CSA"` but the engine uses `"CS"` → mislabelled every player "Union" → fixed. **(MED/LOW)** overclaim wording softened; dyslexia spacing reaches `.pad span/li`; dead bundle seeds + dead desk branch removed; persistence asymmetry collapsed. `wcag-auditor` AA — no violations, no edits.
>
> **Vet (all GREEN):** `GATE OK`; `probe-accessibility` **17/17**, 0 pe; no-regression — `probe-presets` **26/26** (byte-identity; the global module load doesn't perturb combat), `probe-playstyle` 14/14, `t1probe` 14/14, `bootprobe` ok (realErrors:[]), `diag-classic` `nonBlank:346`, the E2-cluster probes green; all 0 pe. **VISUALLY CONFIRMED** (both modes transform the render; HC main menu 13.47:1). **Committed + pushed.**
>
> **⏳ NEXT — E3-i2 (the dedicated FULL WCAG 2.2 AA audit sweep):** the comprehensive per-surface pass across EVERY shipped surface (desk tabs, menus, the battle HUD, all overlays, the tactical 2D/3D readouts) — the systematic contrast/ARIA/focus/keyboard audit + fixes that i1's high-contrast mode deliberately did NOT attempt (i1 strengthened the var-driven core + focus + the already-high-contrast dark panels; i2 is the exhaustive inline-hex sweep). Run `wcag-auditor` (Sonnet) across the src UI files + an adversarial manual AA review; fix every finding at root; the E3 checklist box ticks when i2 lands. Then Phase F (content systems) / I (loot + Soldier's Story) / H (graphics/footage/audio) / J (polish). Symmetric AI-GM = the [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D125/D124/D123/D74/D54 → V1-CHECKLIST Phase E (E1✅ E2✅ E4✅; **E3 in progress — i1✅, i2 NEXT**) → `src/97-accessibility.js` (the modes + `a11yApply`/`_a11yCss` — the high-contrast token-override + dyslexia rules are the seam i2 extends per-surface) + the frozen `openSettings` (motion/CVD/sound) + `tools/probe-accessibility.mjs` (extend with the per-surface AA assertions).

---

## ⚡ EARLIER — 2026-06-24 **E2-i5 "THE REAL COST": a citation-grade HISTORICAL TEACHING layer over the tactical realism dials — a "What this costs in real life" expander in the Command & Realism picker + a deepened Play-Your-Way read-out. This COMPLETES E2 / S4 education (i1–i5); NEXT = E3 (the dedicated full WCAG 2.2 AA pass + the 4 a11y modes)** (overnight run, D124)

> **The teaching layer over the realism dials ships — and E2 closes.** The tactical difficulty/realism dials already shipped (B5/D69, `src/tactical/T6-presets.js`); E2-i5's value is EDUCATIONAL, not a new combat lever (SCOPE DECISION D124.1: chose the recommended NON-combat **Option B** — a teaching enrichment — over Option A, a new strategic-difficulty/AI-GM system that would overlap the deferred [hard] §12 AI-GM capstone). New `src/96-realism-teaching.js` — a PURE READ-OUT module teaching what each realism dial ABSTRACTS in real terms: **8 lever entries** (attrition · canister · supply · command-shock · scouting/LOS · experience · commander-skill · fog) **+ 3 bundle entries** (Arcade/Balanced/Historian), each `{title, models, cost, sources}`. Surfaced TWO ways, both typeof-guarded: (1) a **"What this costs in real life" expander** in the T6 picker (`#pvTeach` → `#rtmTeach`) with a contextual **"YOURS: <setting>" chip** per dial; (2) a deepened teaching read-out in the E2-i4 "Play Your Way" hub (`rtmHubReadout`). Period-broadsheet, CVD-safe, full keyboard/ARIA (aria-expanded/aria-controls + native `<details>`), reduce-motion-safe.
>
> **ARCHITECTURE — byte-identical combat BY CONSTRUCTION (D74):** reads FLDP labels + a passed config and RETURNS HTML; changes ZERO lever value; writes NOTHING (no G/localStorage/RNG/`_SAVE_VER`). `fldPresetCompute`/`fldPresetsApply`/`_fldClampCfg`/`fldPresetNeutral`/`fldPresetResolve` UNCHANGED; the T6 hook is UI-only (`_fldPresetHTML`/`_fldPresetWire`/the new `teach` state flag). `probe-realism-teaching` static-scans 22 combat-execution files (0 rtm leaks) AND extracts the 5 combat-read fns to assert they are rtm-clean. NO A/B sweep needed (no number changed); the `probe-presets` 9-baseline byte-identity gate stays GREEN. Content via a 22-agent research+verify Workflow (11 Opus historians × 11 verifiers; caught + fixed the Griffith misattribution, a fabrication-risk Meigs quote, and a Lost-Cause risk in the commander-skill entry; ≥2 verified sources each; anti-Lost-Cause).
>
> **⚠ BUG-HUNT DEGRADED BY A SUSTAINED ANTHROPIC API 529 OUTAGE — substituted, a real bug WAS caught, and the panel has SINCE BEEN RE-RUN CLEAN:** the original dedicated 9-agent panel failed three times running (all 529 over ~15 min). Per the HALT rule it was substituted by (a) a rigorous INLINE Opus adversarial pass across all 5 lenses, which **caught + fixed one real HIGH-severity defect — a regex LOOKBEHIND `(?<=\.)\s` in `rtmHubReadout`** (lookbehind is a parse-time SyntaxError on Safari < 16.4 → would have broken the WHOLE single-file game on those engines; the bootprobe passed only because it runs in Chromium) → replaced with a portable `". "` split; (b) the content's own 22-agent verify workflow; (c) the full empirical probe battery; (d) the wcag pass.
> **✅ FOLLOW-UP DONE (2026-06-24, next session, D124.2):** the queued 9-agent panel was **re-authored** (the prior session's saved script died with its session) and **re-ran clean** (6 diverse-lens Opus finders × default-refute verify × a completeness critic; critic confidence HIGH). It empirically re-verified every invariant (combat byte-identity, parse portability, XSS, robustness via node-execution, history web-verified) and surfaced **exactly ONE real defect (LOW): a dangling `aria-controls="rtmTeach"` on `#pvTeach`** — emitted unconditionally, but `#rtmTeach` is only in the DOM when the expander is OPEN (default collapsed → a dangling IDREF, WCAG 4.1.2/1.3.1). **FIXED** in `src/tactical/T6-presets.js` (emit `aria-controls` only when `s.teach`, mirroring the sibling `#pvAdv`; `aria-expanded` still announces state in both); `probe-realism-teaching` **strengthened** to assert no-dangling-ref-collapsed → valid-ref-open → dropped-on-recollapse (now 12/12). UI-only attribute change → combat byte-identical (`probe-presets` 26/26 NEUTRAL byte-identity GREEN). Committed + pushed.
>
> **Vet (all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-realism-teaching` **12/12**, 0 pe; no-regression — `probe-presets` **26/26** (9-baseline byte-identity gate), `probe-playstyle` 14/14, `probe-codex` 18/18, `probe-glossary` 12/12, `probe-tutorial` 15/15, `probe-desk` 13/13, `probe-decisions` 19/19, `probe-divergence` 14/14, `probe-afteraction` 15/15, `bootprobe` ok (realErrors:[]), `t1probe` 14/14, `diag-classic` `nonBlank:346`, all 0 pe. No combat-path change → 9 battles byte-identical. `wcag-auditor` AA. **Committed + pushed.**
>
> **⏳ NEXT — E3 (S4 accessibility):** the dedicated FULL WCAG 2.2 AA pass + the **4 a11y modes** (high-contrast, CVD-safe, SR turn/battle narration, dyslexia font) — the professional-bar accessibility milestone. Then Phase F (content systems) / I (loot + Soldier's Story) / H (graphics/footage/audio) / J (polish). Symmetric AI-GM = the [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D124/D123/D120/D74/D54 → V1-CHECKLIST Phase E (E1✅ E2✅ E4✅; **E3 NEXT**) → for E3: the frozen `openSettings` (motion/CVD/sound) + `src/95-playstyle.js` (the Historian/teaching hub) + the per-feature ARIA already baked in are the existing a11y surfaces; E3 is the systematic AA audit + the 4 dedicated modes as a new guarded module.

---

## ⚡ EARLIER — 2026-06-24 **E2-i4 PLAY-STYLE PRESETS + the D39 HISTORIAN SETTINGS LAYER: a "Play Your Way" hub — President (owner-mode focus) / General-Commander (battle focus) / Free Command, plus the teaching/realism toggles — surfaced as a President's-Desk tab AND a main-menu overlay. E2 (S4 education) continues; NEXT = E2-i5 difficulty/realism sliders** (overnight run, D123)

> **The play-emphasis + teaching-settings layer ships.** New `src/95-playstyle.js` — a **"Play Your Way"** hub: (1) **play-style presets** — **President** (owner-mode focus) / **General-Commander** (battle focus) / **Free Command** (the shipped neutral); (2) the **D39 Historian teaching layer** — the alt-history emergent-only toggle + a read-only Command-&-Realism read-out + hand-offs to the existing pickers. ONE panel body (`_psPanelHTML(ctx)`) on TWO surfaces: an **18th President's-Desk tab** ("Play Style", inline like Codex, ctx="desk") AND a **main-menu overlay** ("⚙ PLAY YOUR WAY", `openSheet`, ctx="menu"). Period-broadsheet styled, CVD-safe, full keyboard/ARIA, reduce-motion-safe.
>
> **D39.1 FAITHFUL — play-style is an EMPHASIS, never a hard gate** (both layers always available — the locked charter). Its genuine pure-UI consumer: `psDefaultDeskTab` sets which Desk tab the desk first lands on (Commander→Command; President/Free→the War Effort overview) — **unset/unknown → "economy" → byte-identical** for existing players (deskTabs whitelist). `openWarDept`'s `_wdTab="economy"` became `_wdTab=psDefaultDeskTab()`.
>
> **ARCHITECTURE — PURE SETTINGS LAYER; byte-identical combat BY CONSTRUCTION:** writes ONLY `G.settings.playStyle` (new) + `altHistoryEmergentOnly` (a SECOND surface for the divergence-tab flag) + a private `cw_playstyle` localStorage bundle (defaults-only boot, the T6 pattern). NO combat knob (the tactical levers stay owned by T6 — `fldPresetResolve` read-only; "Adjust the battlefield" hands off to `fldPresetMenu`; motion/CVD/sound hand off to the frozen `openSettings`). NO RNG, NO `_SAVE_VER` bump. **No tactical file in the diff; no combat/tick/resolve/bridge path references `ps*`** (grep + probe static-scan over `src/tactical/*.js` + 85/86/87/80) → 9 battles byte-identical. Diff = `src/95-playstyle.js` + `src/00-manifest.json` + `src/30-president-shell.js` (dispatch + both tab arrays + the tab btn + the default-tab read) + `src/81-divergence.js` (bundle-sync + a comment) + `src/92-help-overlay.js` (the same-root-cause observer de-latch) + `tools/probe-playstyle.mjs` + the generated HTML.
>
> **Bug-hunt (13 agents) → invariants RE-CONFIRMED (byte-identical combat · desk byte-identity · persistence · purity) → 5 findings ALL FIXED + probe-locked:** the keystone was a **MED main-menu observer LATCH** (`installed` one-shot) — the menu rebuilds on every `openMainMenu`, so the button (and, **same root cause, 92's How-to-Play + Guided Tour**) vanished after the first rebuild → **rewrote both observers to the canonical `fldInjectMenuButton` pattern** (no latch, dedupe by live DOM, re-inject every mutation; verified all three persist via a polling test). Plus: the emergent On/Off toggle had no accessible name (WCAG 4.1.2) → `aria-label`; weak menu hand-off names → `aria-label`s; the divergence toggle didn't sync the `cw_playstyle` bundle → `_psPersist()` call; the stale "the ONLY writer" comment fixed. **The 92 repair is an incidental same-root-cause fix, logged.** wcag-auditor AA (one non-text divider contrast fix, 1.89→3.57:1).
>
> **Vet (all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-playstyle` **14/14**, 0 pe; no-regression GREEN — `diag-classic` `nonBlank:346`, `probe-codex` 18/18, `probe-glossary` 12/12, `probe-tutorial` 15/15, `probe-desk` 13/13, `probe-decisions` 19/19, `probe-divergence` 14/14, `probe-afteraction` 15/15, `bootprobe` ok (realErrors:[]), `t1probe` 14/14 (all 0 pe). No tactical file → battles byte-identical. **Committed + pushed.**
>
> **⏳ NEXT — E2-i5 (DIFFICULTY/REALISM SLIDERS) — ⚠ READ THE SCOPE DECISION FIRST:** the LAST E2 item. **CRITICAL CONTEXT: the tactical difficulty/realism sliders ALREADY EXIST and are fully shipped** in `src/tactical/T6-presets.js` (B5/D69 — Recruit…Hardee × Arcade/Balanced/Historian + an Advanced per-lever expander: attrition/canister/supply/cmdShock/sight/veteran/aiSkill/fog/autoPause; `probe-presets` ~30 steps + a 9-baseline byte-identity gate). They are now surfaced from THREE places: the main-menu "⚙ Command & Realism" button (`fldInjectPresetButton`), the skirmish menu, AND the new E2-i4 Play Your Way hub's "Adjust the battlefield" hand-off (`fldPresetMenu`). **So "E2-i5 sliders" is scope-ambiguous, and the wrong reading wastes a milestone:**
> - **(A) a NEW strategic-difficulty / AI-GM system** (economy harshness, strategic-AI aggression) — this is COMBAT-ADJACENT and OVERLAPS the explicitly-deferred **[hard] §12 symmetric AI-GM capstone** (Aaron's locked queue puts that LAST). Do NOT build this under E2 without surfacing it as a fork.
> - **(B, RECOMMENDED) an EDUCATION-layer teaching ENRICHMENT of the already-shipped sliders** — keep it NON-combat: add a "what this realism level means" historical teaching expander to the T6 picker (the bundles' real-cost rationale), and/or deepen the Play Your Way hub's read-out, so E2 stays education-focused and avoids touching the combat path. Under this reading much of i5's *surfacing* is already done by i4; the new value is the teaching copy.
> - **⚠ If any real LEVER/balance change is made,** note that T6 IS a combat-path file (`fldPresetsApply` reads at `fldInitSim`): it MUST stay within the proven D74 "bounded INPUTS, no output gate" model, neutral=byte-identical, with a **headless A/B sweep + LOG every number** and `probe-presets` + the 9-baseline byte-identity gate GREEN. This is why E2-i5 is a heavier, combat-touching milestone best STARTED on a FRESH window (it was deliberately NOT started at the end of the D123 session — a clean pushed boundary — per Aaron's context-budget rule: never carry a combat-touching, scope-ambiguous milestone across a context cutover).
> - After E2-i5: **E3** the dedicated full WCAG 2.2 AA pass + the 4 a11y modes (high-contrast, CVD-safe, SR turn/battle narration, dyslexia font). Then Phase F/I/H/J. Symmetric AI-GM = [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D123/D122/D120/D91/D54/D39 → V1-CHECKLIST Phase E (E1✅ E4✅; E2 in progress, i1✅ i2✅ i3✅ i4✅) → `src/tactical/T6-presets.js` (`fldPresetMenu`/`FLDP` + `fldPresetsApply`/`fldPresetCompute`/`_fldClampCfg`) + `tools/probe-presets.mjs` + `src/95-playstyle.js` (the i4 hub).

---

## ⚡ EARLIER — 2026-06-24 **E2-i3 THE GUIDED TUTORIAL: a self-contained 7-step modal tour orients new players across all three layers (grand strategy · battles · the Codex seminar), launched from the welcome card, the How-to-Play panel, and a main-menu button** (overnight run, D122)

> **The onboarding layer ships.** New `src/94-tutorial.js` — a 7-step modal tour (`#tutOverlay`): the three layers · the President's Desk · the Great Decisions · the Battles · **the Codex & Glossary** (showcasing i1/i2) · Winning the War · Play Your Way. Back/Next/Skip + "Step N of M" + dots; full keyboard (Esc/←/→/Enter), a focus trap, ARIA dialog, reduce-motion-safe. Launched from 3 entry points wired in `src/92-help-overlay.js`: the first-launch welcome card, the How-to-Play panel, and a main-menu "GUIDED TOUR" button.
>
> **ARCHITECTURE — PURE UI / HELP LAYER; byte-identical combat BY CONSTRUCTION:** reads no sim state, writes only a localStorage "seen" flag; no combat/tactical/bridge path references the tutorial (grep + a probe static-scan over 20 combat files); the 3 launch hooks are `typeof`-guarded. Diff = `src/94-tutorial.js` + `src/00-manifest.json` + `src/92-help-overlay.js` (3 hooks + a "?"-modality guard) + `tools/probe-tutorial.mjs` + the generated HTML.
>
> **Bug-hunt (14 agents) → invariantsHold=FALSE initially → 8 findings (≈6 distinct) ALL FIXED + probe-locked:** the keystone was a real **focus-trap** bug (forward Tab never reached Skip — WCAG 2.4.3) → rewrote as a symmetric [Skip,Back,Next] cycle; plus the global "?" hotkey opening How-to-Play behind the modal → guarded; double-start focus-restore; background `aria-hidden`-under-modal; step-position in the title aria-label; and the 50ms pre-mount window dropped. wcag-auditor AA (added the Skip `:focus-visible` ring).
>
> **Vet (all GREEN):** `GATE OK`; `probe-tutorial` **15/15**, 0 pe; no-regression GREEN — `diag-classic` `nonBlank:346`, `probe-codex` 18/18, `probe-glossary` 12/12, `probe-desk` 13/13, `probe-decisions` 19/19, `probe-divergence` 14/14, `probe-afteraction` 15/15, `bootprobe` ok (realErrors:[]), `t1probe` 14/14 (all 0 pe). No tactical file → battles byte-identical. **Committed + pushed.**

---

## ⚡ EARLIER — 2026-06-23 **E2-i2 THE INLINE GLOSSARY: key terms in the teaching prose (After-Action + Your War vs History) now carry hover/tap/focus codex definitions, reading the same codex data. E2 (S4 education) continues; NEXT = E2-i3 the GUIDED TUTORIAL** (overnight run, D121)

> **⚠ INTERNET PAUSED MID-RUN (Aaron).** E2-i2 is fully vetted + **committed LOCALLY**, but **NOT pushed** (`git push` is internet). When Aaron says "resume", run `git push origin main` (the local `main` is ahead of `origin/main` by one commit — the D121 glossary). E2-i1 (codex, D120) was already committed AND pushed earlier (`6306ba1`) while internet was allowed, so it is safely on the remote.
>
> **What shipped (E2-i2):** an INLINE GLOSSARY — `src/93-glossary.js` DOM-decorates the rendered teaching prose of the two read-only teaching desk tabs (After-Action, Your War vs History): a TreeWalker wraps codex terms in inline `<button class="gl-term">` (dotted brass underline) + a shared `#glTip` tooltip on hover/focus/tap. Reads the SAME codex data as the i1 Codex tab (`cxGlossaryIndex`/`cxLookupShort`). The def rides each trigger's `aria-label` (SR gets it on focus; the tooltip is aria-hidden decorative). Matchers are built from DERIVED SURFACES (strip leading "The"/trailing "(...)", extract acronyms like USCT, + aliases) so the bare forms prose uses actually match. Hooked into `_wdRefresh` (guarded, scoped to afteraction/warvshistory; clears `data-gl-done` before each render of the reused `#wdContent`).
>
> **ARCHITECTURE — PURE READ-OUT; byte-identical combat BY CONSTRUCTION:** `glDecorate` reads `GAME_DATA.codex` and decorates rendered DOM; writes NOTHING to the sim. No combat/tactical/bridge path references `gl*` (grep-confirmed + a probe static-scan over 20 combat files). Diff = `src/93-glossary.js` + `data/codex.json` (3 bare-form aliases) + `src/00-manifest.json` + `src/30-president-shell.js` (the hook) + `tools/probe-glossary.mjs` + the generated HTML.
>
> **Bug-hunt (OFFLINE, no-internet per the pause; 26 agents) → invariantsHold=FALSE initially → 16 findings ALL FIXED + probe-locked:** the keystone was a real word-boundary bug (trailing `\b` made terms ending in `)` — USCT, Lost Cause, Contraband — dead matchers) fixed via the surface-derivation; plus one-term-per-node→multi-term, aria-label leads with visible text (2.5.3), touch tap-to-reveal, and a probe-rigor cluster (the probe had a non-existent fallback term + a vacuous boundary test). wcag-auditor AA (no edits).
>
> **Vet (all GREEN):** `GATE OK`; `probe-glossary` **12/12**, 0 pe; no-regression GREEN — `diag-classic` `nonBlank:346`, `probe-codex` 18/18, `probe-desk` 13/13, `probe-decisions` 19/19, `probe-divergence` 14/14, `probe-afteraction` 15/15, `bootprobe` ok (realErrors:[]), `t1probe` 14/14 (all 0 pe). No tactical file → battles byte-identical.
>
> **⏳ NEXT — E2-i3 (the GUIDED TUTORIAL):** extend the `src/92-help-overlay.js` spine (`_hpShowWelcome`/`_hpShowHelp`/`_hpShowTacticalHelp`) into a full guided, multi-step tutorial (orient the President's-Desk layers + the tactical battle; first-launch + on-demand; keyboard/SR-accessible; reduce-motion safe). Then **E2-i4** play-style presets (President / General-Commander + the D39 Historian settings overlay) → **E2-i5** difficulty/realism sliders (pair with `src/tactical/T6-presets.js` `fldPresetMenu`). Then **E3** the full WCAG 2.2 AA pass + the 4 a11y modes. Then Phase F/I/H/J. Symmetric AI-GM = [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D121/D120/D119/D91/D54 → V1-CHECKLIST Phase E (E1✅ E4✅; E2 in progress, i1✅ i2✅) → `src/92-help-overlay.js` (the tutorial spine) + `src/93-glossary.js` + `src/84-codex.js` + `data/codex.json`.

---

## ⚡ EARLIER — 2026-06-23 **E2-i1 THE MULTI-AXIS CODEX / GLOSSARY: the President's Desk gains a searchable, axis-filterable, cross-linked "Codex" tab — 60 citation-grade, anti-Lost-Cause entries (people · units & formations · terms & concepts · systems of the war). E2 (S4 education) is UNDERWAY; NEXT = E2-i2 the INLINE GLOSSARY** (overnight run, D120)

> **The teaching layer's reference spine ships.** Phase E's strategic arc (E1+E4) was complete; the locked next pillar is **E2 — S4 education**, built incrementally. **E2-i1 = the multi-axis CODEX:** a new `data/codex.json` (60 entries: people 24 · units 11 · terms 19 · systems 6; **all 60 Verified, each ≥2 real sources**) surfaced as the desk's 17th tab — searchable, axis-filterable, with expandable cited cards + "See also" cross-links. The codex `short` field is the single source of truth for the next increment's inline glossary (already exposed via `cxLookupShort`/`cxGlossaryIndex`).
>
> **E2 BUILD ORDER (decided & logged, D120):** **i1 codex DATA+tab (THIS — done)** → **i2 inline glossary** (hover/tap defs reading the codex `short`) → **i3 the full guided tutorial** (extend `src/92-help-overlay.js` — `_hpShowWelcome`/`_hpShowHelp`/`_hpShowTacticalHelp`) → **i4 play-style presets** (President / General-Commander + the D39 Historian settings overlay) → **i5 difficulty/realism sliders** (pair with the B-5 tactical effectiveness/realism levers in `src/tactical/T6-presets.js` `fldPresetMenu` — memory `civilwar-directive-tactical-engineering-sliders`). Codex leads because it is the data foundation the glossary reads.
>
> **ARCHITECTURE — PURE READ-OUT; byte-identical combat BY CONSTRUCTION:** new `src/84-codex.js` (`codexRenderTab`/`codexWireTab` + `_cx*` + `cxLookupShort`/`cxGlossaryIndex`). Reads `GAME_DATA.codex`, WRITES NOTHING (no campaign state / `saveLocal` / RNG / `_SAVE_VER`). **No tactical file touched; no combat/tick/resolve/bridge path reads `cx*`/`GAME_DATA.codex`** (grep-confirmed AND probe-locked by a static-scan over 20 combat files) → battles byte-identical by construction. Client-side filter is DOM show/hide (search keeps focus). Wired into `_wdRefresh`/`openWarDept` + both lockstep tab arrays in `src/30-president-shell.js`, exactly like the decisions/afteraction tabs. Manifest: `84-codex.js` between 83-endings & 85-battle-bridge.
>
> **Content (codex-content-research Workflow, 14 agents — 7 Opus historians WebSearch-grounded × 7 adversarial Opus verifiers):** the verify pass caught real errors — a fabricated Hess artillery-book title removed; **the Forrest/KKK error fixed (he was the Klan's first Grand Wizard, NOT its founder).** Anti-Lost-Cause throughout (slavery named as the cause; the Lost Cause named + countered; Fort Pillow/Forrest's Klan, Andersonville, Union hard-war destruction named on both sides; no valorization).
>
> **Bug-hunt (5 diverse-lens Opus finders incl. a citation/anti-Lost-Cause lens × default-refute Opus verify + a completeness critic; 26 agents) → invariantsHold=TRUE; 16 LOW/MED latent findings, ALL FIXED + probe-locked:** stale axis blurb on cross-link (CX-01); plural-grammar count (CX-02); garbled Lincoln "blind memorandum" quote repaired (CDX-03); a dedicated Andersonville/POW both-sides entry added (CDX-04); `.cx-head`/`#cxSearch` focus rings (scoped `<style>` — base.html is frozen); `#cxBlurb` `aria-live`; and the probe hardened to lock purity/XSS/anti-Lost-Cause-content/shell-dispatch/interaction/data-robustness + the combat-path static-scan. Plus a self-found consistency fix: USCT `~178,000 → ~180,000` (matches the game's convention; D117 precedent).
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · citations ✓`; `probe-codex` **18/18**, **0 pageerrors**; no-regression GREEN — `diag-classic` `nonBlank:346`, `probe-desk` 13/13, `probe-decisions` 19/19, `probe-divergence` 14/14, `probe-afteraction` 15/15, `bootprobe` ok (realErrors:[]), `t1probe` 14/14 (all 0 pe). No tactical file in the diff → 9 battles byte-identical by construction. `wcag-auditor` AA (2 pill-count contrast spans fixed; focus rings 7.49–7.80:1, axis chips 7.77–9.20:1).
>
> **OPERATING MODEL (locked, D116 [AARON] popup) — still in force:** EVEN FEWER interruptions; stop ONLY for an irreversible/money fork; LOG a contradiction and proceed. FULL 9-agent (here 26) bug-hunt every milestone. Frozen-loop overrides proceed autonomously. Phase E in the locked order.
>
> **⏳ NEXT — E2-i2 (the INLINE GLOSSARY):** hover/tap term definitions woven into the existing teaching prose, reading `cxLookupShort(term)` (already live). Candidate prose surfaces to enrich: `src/20-president-render.js`, the `src/32-decisions.js` cards, `src/82-after-action.js`, `src/81-divergence.js`. Build a small guarded term-matcher (wrap known codex terms in an accessible tooltip trigger; keyboard + SR friendly; reduce-motion safe; pure read-out). Then **E2-i3** tutorial → **E2-i4** presets+Historian overlay → **E2-i5** sliders → **E3** the full WCAG 2.2 AA pass + the 4 a11y modes. Then Phase F/I/H/J. Symmetric AI-GM = [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D120/D119/D118/D91/D54 → V1-CHECKLIST Phase E (E1✅ E4✅; E2 in progress, i1✅) → `src/84-codex.js` (`cxLookupShort`/`cxGlossaryIndex` — the glossary accessor) + `data/codex.json` (the 60 entries) + the teaching-prose surfaces above + `tools/probe-codex.mjs`.

---

## ⚡ EARLIER — 2026-06-23 **E4-i2 THE STRATEGIC WAR-END: a reached victoryReady (a negotiated peace / a recognized independence) now CONCLUDES the war through the graded after-action report — for a victory that did NOT complete the battle chain. Phase E's strategic arc (E1 alt-history + E4 victory) is now COMPLETE; NEXT = E2 education** (overnight run, D119)

> **The strategic arc CLOSES.** Before D119 the war ended only at battle-chain completion (`campaignAdvance` → `warWonScreen` at `idx>=chainLen`). Now a reached **`C.strategy.victoryReady`** — a **negotiated peace** (`enemyWill≤30` in 1864+ / `≤18` any year) or a **recognized independence** (recognition ≥60, CS) — can CONCLUDE the war through the SAME D112 graded report, surfaced as a **NON-FORCED OFFER** on the between-battles interstitial ("The war can be concluded — accept the peace / claim independence", or fight on). Built via the authorized `warWonScreen` override pattern (the frozen-loop override; D116 authorized this autonomously).
>
> **ARCHITECTURE — additive OFFER + the existing warWonScreen; byte-identical combat BY CONSTRUCTION:** new pure helpers in `src/82-after-action.js` — `aarStrategicEndAvailable(C)` (`"will"` either side / `"recognition"` **CS-ONLY** / null), `_aarEndFraming(C,reason)` (side-aware title/subtitle/verdict/offer copy), `aarStrategicEndOffer(C)`, `aarConcludeWar(reason)`. `warWonScreen` frames via `_aarEndFraming`; at chain completion (`_aarEndReason` null) it is the **byte-identical D112 "The War is Won / Victory!"** screen (base Main-Menu wiring + `G.campaign=null` verbatim). The offer is one guarded IIFE in `_pdInterstitialHTML` (`src/20-president-render.js`; returns `''` → byte-identical interstitial when `victoryReady` null), wired (`#pdConcludeWar`) in `_pdShowTurnInterstitial` (`src/30-president-shell.js`). **No tactical file touched; no combat/tick/resolve/bridge path reads `aarStrategicEnd*`/`_aarEndReason`/`aarConcludeWar`** (grep-confirmed) → 9 battles byte-identical by construction. No new save state; NO `_SAVE_VER` bump; NO RNG.
>
> **SIDE-CORRECT (anti-Lost-Cause):** `"recognition"` gated CS-only (the Union never wins by the Confederacy gaining recognition); `"will"` side-aware (US: "the rebellion lays down its arms"; CS: "Washington consents to let the South go"); a CS strategic victory framed honestly as the survival of a slaveholding republic, never valorized (the graded report body carries the Reconstruction accounting, keyed on `victoryReady`).
>
> **Bug-hunt (5 diverse-lens Opus finders × default-refute Opus verify + a completeness critic; 10 agents) → invariantsHold=TRUE; 1 confirmed LOW + 2 critic gaps (1 MED + 1 LOW), ALL FIXED + probe-locked:** **(MED)** the pre-existing `vicOnResolve` "will" toast hardcoded "Northern resolve is breaking" for BOTH sides — wrong for a US player; D119 makes it a live cue → side-aware. **(LOW)** the strategic-conclude exit left `_pdTurnAck=true` (skipping the next campaign's first interstitial) → reset at the `warWonScreen` chokepoint. **(LOW)** `warWonScreen`'s `!C` early-return now clears `_aarEndReason`.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-afteraction` **15/15** (+5 D119 steps), **0 pageerrors**; no-regression GREEN — `diag-classic` `nonBlank:346`, `probe-desk` 13/13, `probe-divergence` 14/14, `probe-victory` 12/12, `bootprobe` ok, `t1probe` 14/14 (all 0 pe). No tactical file in the diff → 9 battles byte-identical by construction. `wcag-auditor` AA (offer banner #cbb27a 7.81:1 / body 8.94:1 / button 8.54:1 — clean, no edits).
>
> **OPERATING MODEL (locked, D116 [AARON] popup) — still in force:** EVEN FEWER interruptions; stop ONLY for an irreversible/money fork; LOG a contradiction and proceed. FULL 9-agent bug-hunt every milestone. Frozen-loop overrides proceed autonomously. Phase E in the locked order.
>
> **⏳ NEXT — E2 (the locked Phase-E order; E1 alt-history + E4 victory now COMPLETE):** **E2 — S4 education:** a multi-axis **codex/glossary** (the war's people, units, terms, systems) + an **inline glossary** (hover/tap definitions) + the full **guided tutorial** + **play-style presets** (President / General-Commander + the D39 Historian settings overlay) + **difficulty/realism sliders** (pairs with the B-5 effectiveness/realism sliders — memory `civilwar-directive-tactical-engineering-sliders`). E2 is LARGE — build it incrementally (one sub-system per gated milestone), likely a citation-grade content workflow for the codex entries (anti-Lost-Cause, ≥2 sources). Then **E3** — the dedicated FULL WCAG 2.2 AA pass + the 4 a11y modes (high-contrast, CVD-safe, SR turn/battle narration, dyslexia font). Then Phase F (content) / I (loot + Soldier's Story) / H (graphics/footage/audio) / J (polish). The symmetric AI-GM is the [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D119/D118/D117/D116/D114/D91/D54 → V1-CHECKLIST Phase E (E1✅ E4✅; E2 next) → for E2: `src/30-president-shell.js` (the desk tabs + `_wdRefresh` dispatch — a codex tab slots here) + `src/92-help-overlay.js` (`_hpShowWelcome`/`_hpShowHelp`/`_hpShowTacticalHelp` — the tutorial/help spine) + `tactical/T6-presets.js` (`fldPresetMenu` — the existing tactical presets/sliders to extend) + `data/` (a new `codex.json` if the codex is data-driven, mirroring `decisions.json`).

---

## ⚡ EARLIER — 2026-06-23 **E1 INTERACTIVE HINGE-FORK DECISION CARDS: 4 citation-grade strategic forks at the war's turning points (the hard hand of war · the terms of reunion · the King Cotton gambit · peace or independence) whose choices STEER the war toward the now-complete alternate-ending spectrum — E1 (alt-history) is now substantially COMPLETE** (overnight run, D118)

> **The E1 "Remaining" item ships:** interactive **HINGE-FORK decision cards on the proven `decResolve`/`_decApply` engine** (the prompt's spec: *data/decisions.json, pure data → byte-identical by construction*). 4 new citation-grade strategic hinge cards (2 US, 2 CS — both-sides balance) in `data/decisions.json`, each a genuine fork at a war turning-point whose choices shape the war's TRAJECTORY toward the D114-D117 ending spectrum: **us-hard-war** ("The Hard Hand of War", 1864 — total war / conciliation / Grant's grind → the *hard-war collapse* road); **us-reconstruction-terms** ("The Terms of Reunion", 1864 — Ten Percent Plan / Wade-Davis / forty acres → *a Reconstruction that holds* / *forty acres enforced*); **cs-king-cotton** ("The King Cotton Gambit", 1861-62 — embargo / export / burn → *recognized independence* / *King Cotton*); **cs-peace-feelers** ("Peace or Independence", 1864 — negotiate / fight-on / husband → *a negotiated peace* / *Fabian survival*). All `trigger.hinge:true` (always surface, "⚐ a hinge of the war" badge), 3 options each, the full rich `card` multi-voice block + a **named-and-countered Lost-Cause MYTH voice**. The deck grows 8 → 12 (it had only ONE strategic hinge before — emancipation).
>
> **SCOPE (decide-&-log) — PURE DATA, no engine extension:** the ending-OPENER strategy levers (`armEnslaved`/`amnesty`/`enemyWill`/`recognition`) are combat-adjacent (feed manpower→bridge / auto-resolve) and already live on the Paths-to-Victory surface — writing them from `_decApply` would be a combat-adjacent engine change, contradicting the prompt's explicit "pure data." So the cards use ONLY the existing `_decApply` vocabulary (`weariness`/`capital`/`intervention`/`importFactor`/`strength` + `sets.emancipation`), which genuinely moves **momentum** (the dominant ending GATE — `vicMomentum` reads weariness+capital) + the foreign/blockade/manpower trajectory, and each option's prose NAMES the ending it steers toward. The wild/lever/recognition openers stay on the Paths-to-Victory surface (by design); decisions are the complementary narrative fork-surface. Only src touch = a 1-line `_decCAT_LABEL` extension (byte-identical for the 8 existing cards via the raw-key fallback; plain `&` in "Peace & Terms" avoids the D115 double-escape).
>
> **Citation-grade content (research+verify Workflow, 8 agents):** 4 Opus historians (WebSearch-grounded) + 4 adversarial verifiers; ALL corrections applied — fabrication-risk URLs → book/verified cites (the misattributed ABT "The Burning"; generic NARA/ABT landing pages; the 404'd Hampton Roads URL; the wrong bread-riot slug → the verified `bread-riot-richmond`); the Erlanger nominal-vs-realized fix (softened to "far less in net proceeds" — the precise net is contested); the Hammond "Cotton is king" quote restored; Gordon C. Rhea named for the Grant casualty arithmetic; the dubious Schweikart attribution dropped. All retained book cites verified genuine.
>
> **ARCHITECTURE — byte-identical combat BY CONSTRUCTION (D74):** no tactical file in the diff; `_decApply`/`_decClamp` UNCHANGED; the new cards use only the existing bounded/clamped effect fields (the same the 8 existing cards use) → no NEW combat path, no fudge (bounded strategic-state INPUTS). NO `_SAVE_VER` bump (additive plain data). Diff = `data/decisions.json` + `src/32-decisions.js` (the label map + 2 wcag contrast fixes) + `tools/probe-decisions.mjs` (5 D118 steps) + the generated HTML.
>
> **Bug-hunt (5 diverse-lens Opus finders incl. a citation/anti-Lost-Cause lens × default-refute Opus verify + a completeness critic; 6 agents) → invariantsHold=TRUE, ZERO confirmed defects** (the critic re-confirmed all 5 invariants: no fabrication [Hammond/Wade-Davis/bread-riot/Erlanger spot-checked real], accuracy, anti-Lost-Cause [each card a named+countered myth voice], byte-identical combat, render/escaping [the "Peace & Terms" double-escape avoided]). One critic-LOW (the contested Erlanger net figure) softened pre-commit.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-decisions` **19/19** (+5 D118 steps: schema+myth-voice · side-gate · year-gate [king-cotton closes 1863+] · bounded effects · cadence-unchanged), **0 pageerrors**; no-regression GREEN — `probe-desk` 13/13, `diag-classic` `nonBlank:346`. No tactical file in the diff → 9 battles byte-identical by construction. **`wcag-auditor` surfaced + AUTO-FIXED 2 PRE-EXISTING AA contrast failures** in the decision render (category label #8a7350 4.15:1 → #957d57; hinge badge #9c3b2e 2.75:1 → #cc5f50 text+border) — affected all 12 cards; a genuine a11y improvement this milestone surfaced.
>
> **OPERATING MODEL (locked, D116 [AARON] popup) — still in force:** EVEN FEWER interruptions; stop ONLY for an irreversible/money fork; LOG a contradiction and proceed with the best call. FULL 9-agent bug-hunt every milestone. Frozen-loop overrides (E4-i2) proceed autonomously. Phase E in the locked order.
>
> **⏳ NEXT — the locked Phase-E order (E1 alt-history is now substantially complete):** **(1) E4-i2** — wire `victoryReady` / a reached ending to an actual war-END screen via the frozen campaign-loop OVERRIDE (the `openWarDept`/`warWonScreen` redeclaration pattern; autonomy authorized, D116) so the graded after-action report (D112) fires for a non-chain-complete victory (negotiated peace / recognition). **(2) E2** — S4 codex/glossary + inline tutorial + play-style presets + difficulty/realism sliders. **(3) E3** — the dedicated FULL WCAG 2.2 AA pass + the 4 a11y modes. Then Phase F (content) / I (loot + Soldier's Story) / H (graphics/footage/audio) / J (polish). The symmetric AI-GM is the [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D118/D117/D116/D114(catalog)/D54 → V1-CHECKLIST Phase E → `src/80-victory.js` (`victoryReady`/`vicMomentum`/`warWonScreen` host) + `src/82-after-action.js` (`warWonScreen` override / `aarRenderReport`) + `src/30-president-shell.js` (`openWarDept`/the desk loop) + `src/32-decisions.js` + `data/decisions.json` (the E1 cards).

---

## ⚡ EARLIER — 2026-06-23 **E1 ALTERNATE ENDINGS (GROUNDED tier, US-half): the 8 plausible/long-shot Union end-states a well-played war can earn (a Reconstruction that holds · an early abolition war · hard war early · reunion without emancipation · the 1862 knockout · forty acres enforced · a compensated bloodless end · the general strike triumphant) — completing the labeled grounded→fantastical spectrum on BOTH sides** (overnight run, D117)

> **E1's grounded tier is now COMPLETE on both sides** (D116 shipped the CS-half; D117 ships the US-half — the prompt's split-into-two-vetted-increments directive, done). 8 GROUNDED Union end-states in `_END_CATALOG` (`src/83-endings.js`), authored exactly like the D116 ones — `tier:"plausible"/"longshot"` + a play-opened `precond(C)` + a performance `gate(C)` + a secure-hint + a citation-grade anti-Lost-Cause counterfactual. **Openers (each a genuine player choice/threshold, all CLOSED on a fresh campaign):** an emancipation war won (em.issued + won≥4) → reconstruction-holds (secured by the 1864 mandate + momentum); the radical-emancipation WILD → early-abolition; the hard-war WILD → hardwar-collapse; emancipation REFUSED → reunion-no-emancipation (named the moral failure it would have been, NOT a triumph); an overwhelming 1862 record (year≤1862 & won≥5) → 1862-knockout (year>1862 CLOSED); a radical mandated victory → forty-acres; an early settlement with emancipation in hand (em.year≤1862) → compensated-bloodless; the general-strike WILD → general-strike triumphant. 3 plausible + 5 longshot. **The US empty-state teaser was flipped so BOTH sides now advertise the full labeled spectrum** (plausible → long shot → fantastical). ⚠ the early-abolition opener keys off the `us-radical-emanc` WILD — NOT `M.usctUnlocked`, which AUTO-trips at year≥1863 (`70-manpower.js:132`); probe-locked.
>
> **THE D114 CATALOG IS NOW FULLY LIVE: 24/24 end-states (12 CS + 12 US)** read along one CVD-safe labeled spectrum.
>
> **ARCHITECTURE — PURE READ-OUT (D111/D112/D115/D116 pattern; byte-identical combat BY CONSTRUCTION):** the 8 entries reuse the existing `_end*` accessors + the D116 banded `endRenderSection` — no new accessor, no new colour, no new UI structure. `endScan` reads strategic state, writes nothing; **no tactical file touched; no combat/tick/resolve/bridge path reads `end*`** (re-grep-confirmed — only `81-divergence.js:293` + `82-after-action.js:331`, both render-only guarded embeds) → 9 battles byte-identical by construction. Diff = `src/83-endings.js` + `tools/probe-endings.mjs` + the generated HTML. NO RNG.
>
> **PROBE-GUARD FIX (the milestone's first task):** the stale D116 guard (the two US-fantastical-only assertions `Ues.indexOf('plausible, long shot')>=0`/`'Reconstruction that holds')>=0`→throw — correct only while US was fantastical-only, now FALSE-FAILING) was REPLACED with positive "US advertises the full spectrum" checks mirroring the CS side; the cross-side leak guards (US empty-state must NOT contain 'Maximilian'/'British war') were KEPT. +8 D117 probe steps added.
>
> **Bug-hunt (5 diverse-lens Opus finders incl. a citation/anti-Lost-Cause lens × default-refute Opus verify + a completeness critic; 8 agents) → invariantsHold=TRUE** (purity grep-clean · side-gate + fresh-campaign-zero · every cite spot-checked real & on-claim [Foner, Du Bois, McPherson, Grimsley, Sears; Compromise of 1877, Field Order No. 15 + Johnson's reversal, Frémont/Hunter revocations, the Peninsula Campaign, Lincoln's compensated-emancipation offers] · the WILD-vs-usctUnlocked distinction · defensive render). **1 confirmed MED, FIXED:** the `us-general-strike` counterfactual said "tens of thousands took up arms for the Union" — understating the canonical ~180,000 USCT figure AND inconsistent with the game's OWN after-action text (`82-after-action.js:163`) + the cited Du Bois → changed to "some 180,000 took up arms for the Union — roughly one in ten Union soldiers by war's end." (Critic gaps all LOW/non-blocking — the probe hand-sets `victoryReady`/`resolved1864`, but the critic independently confirmed the two year≤1862 endings are organically reachable via `victory.js:129`.)
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-endings` **24/24** (16 prior + 8 D117 steps), **0 pageerrors** (re-run GREEN after the citation fix); no-regression GREEN — `probe-divergence` 14/14, `probe-afteraction` 10/10 (the two guarded-embed hosts), `diag-classic` `nonBlank:346`. No tactical file in the diff → 9 battles byte-identical by construction. `wcag-auditor` AA (data-only increment; band labels 5.38/10.20/6.41:1, prose blends 9.46–11.41:1, chip borders ≥3:1, CVD word+glyph — no edits).
>
> **OPERATING MODEL (locked, D116 [AARON] popup) — still in force:** EVEN FEWER interruptions; stop ONLY for an irreversible/money fork; LOG a contradiction and proceed with the best call. The FULL 9-agent bug-hunt runs every milestone. Frozen-loop overrides (E4-i2) proceed autonomously. Phase E in the locked order.
>
> **⏳ NEXT — E1's last piece, then the locked Phase-E order:** **(1)** interactive **HINGE-FORK decision cards** on the proven `decResolve`/`_decApply` engine (`data/decisions.json`, pure data → byte-identical by construction) that STEER toward the now-complete 24-ending spectrum. **(2) E4-i2** — wire `victoryReady`/a reached ending to an actual war-END screen (the frozen campaign-loop OVERRIDE via the `openWarDept`/`warWonScreen` redeclaration pattern; autonomy authorized, D116) so the graded after-action report fires for a non-chain-complete victory. **(3) E2** — S4 codex/glossary + inline tutorial + play-style presets + difficulty sliders. **(4) E3** — the dedicated FULL WCAG 2.2 AA pass + the 4 a11y modes. Then Phase F (content) / I (loot + Soldier's Story) / H (graphics/footage/audio) / J (polish). The symmetric AI-GM is the [hard] §12 capstone (surface only if it forks). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's locked queue). **Resume map:** START-HERE → this block → DECISIONS D117/D116/D115/D114(catalog)/D54 → V1-CHECKLIST Phase E → `src/83-endings.js` (`_END_CATALOG`/`endScan`/`endRenderSection`/`_END_TIERS`) + `src/32-decisions.js` (`decResolve`/`_decApply`) + `data/decisions.json` + `tools/probe-endings.mjs`.

---

## ⚡ EARLIER — 2026-06-23 **E1 ALTERNATE ENDINGS (GROUNDED tier, CS-half): the 8 plausible/long-shot Confederate end-states a well-played war can earn (recognized independence · a negotiated peace · an emancipated Confederacy · stalemate/two-Americas · Fabian survival · a Trans-Mississippi redoubt · King Cotton coerces Europe · the Northwest secedes) — authored the SAME way as the fantastical tier + a CVD-safe LABELED SPECTRUM** (overnight run, D116)

> **E1's grounded tier ships, CS-half first** (the prompt's split-into-two-vetted-increments directive; D114 catalog). 8 GROUNDED Confederate end-states added to `_END_CATALOG` (`src/83-endings.js`) exactly like the D115 fantastical ones — `tier:"plausible"/"longshot"` + a play-opened `precond(C)` + a performance `gate(C)` + a secure-hint + a citation-grade anti-Lost-Cause counterfactual. **Openers (each a genuine player choice/threshold, all CLOSED on a fresh campaign):** recognition courted (≥30) → recognized-independence; Northern will cracking (enemyWill≤55) → negotiated-peace; the Cleburne arm-the-enslaved lever → emancipated-Confederacy (slavery named plainly, the founding creed gutted); a long even war (1864+, won≥3) → stalemate; the husbanding amnesty lever (1864+) → Fabian survival; an earned survival into 1865 (won≥3, no outright win) → the Trans-Mississippi redoubt; the burn-the-cotton gambit → King Cotton; the Copperhead gambit → the Northwest secedes. 4 plausible + 4 longshot.
>
> **ABOVE-AND-BEYOND — THE LABELED SPECTRUM (D114):** `endRenderSection` rewritten to band endings along a CVD-safe spectrum (Plausible → Long shot → Fantastical, `_END_TIERS`, AA palette 4.85/9.20/5.77:1; word label carries meaning, colour redundant). Grounded + fantastical now read as ONE honest spectrum; an unopened band is omitted.
>
> **ARCHITECTURE — PURE READ-OUT (D111/D112/D115 pattern; byte-identical combat BY CONSTRUCTION):** new accessors `_endEnemyWill`/`_endYear`/`_endLever`/`_endRecogForeclosed` (defensive; `_endEnemyWill` defaults HIGH=100 so a missing will never falsely opens a "cracking" path). `endScan` reads strategic state, writes nothing; **no tactical file touched; no combat/tick/resolve/bridge path reads `end*`** (grep-confirmed) → 9 battles byte-identical by construction. Diff = `src/83-endings.js` + `tools/probe-endings.mjs` + the generated HTML. NO RNG.
>
> **Bug-hunt (5 diverse-lens Opus finders incl. a citation/anti-Lost-Cause lens × default-refute verify + a completeness critic; 11 agents) → invariantsHold=TRUE** (all 8 counterfactuals + every cited book/author verified real; fresh-campaign-clean reproduced). **4 confirmed + 2 critic-additional, ALL FIXED + probe-locked:** (MED) cs-trans-mississippi opened from passage-of-time alone (`victoryReady==null` is the fresh default) → moved `won≥3` INTO the precond + a probe negative; (LOW) the US empty-state teaser promised plausible/long-shot tiers the US side can't reach until its grounded tier ships → side-aware teaser; (LOW) cs-fabian opened on `fortifyPorts` (the OPPOSITE of Fabian — it HOLDS ports) → amnesty-only; (LOW) negotiated-peace secureHint over-pinned Nov 1864 → softened; (critic-LOW ×2) trans-miss probe coverage added + cs-stalemate momentum band relaxed for reachability.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-endings` **16/16** (+8 D116 steps incl. the fix-locks), **0 pageerrors**; no-regression GREEN — `probe-divergence` 14/14, `probe-afteraction` 10/10 (the guarded-embed hosts), `diag-classic` `nonBlank:346`. No tactical file in the diff → battles byte-identical by construction. `wcag-auditor` AA (band/chip colours 5.15–9.96:1, no edits).
>
> **[AARON] popup mid-run (AskUserQuestion ×4, "clear future loops"):** (1) Phase E stays the locked order (hinge-forks → E4-i2 → E2 → E3). (2) **E4-i2 frozen-loop war-END = proceed autonomously** via the override pattern. (3) **Full 9-agent bug-hunt every time** (never lower the bar). (4) **EVEN FEWER interruptions** — stop ONLY for irreversible/money; LOG a contradiction and proceed with my best call.
>
> **✅ D117 (the US-half GROUNDED tier) SHIPPED — see the CONTINUE HERE block at the top of this file.** All 8 Union grounded end-states are live, the labeled spectrum is complete on both sides (24/24 catalog), the probe-guard was fixed + 8 D117 steps added (`probe-endings` 24/24), the 8-agent bug-hunt's one MED (the general-strike USCT count) was fixed, and it was committed + pushed. **NEXT = E1's last piece (the HINGE-FORK decision cards) → E4-i2 → E2 → E3** (the top block carries the live resume map).

---

## ⚡ EARLIER — 2026-06-23 **E1 ALTERNATE ENDINGS (fantastical tier): the 8 boldest alt-history RESULTS a well-played war earns (a British war · Maximilian's legions · Stonewall lives · the Golden Circle · the rapid-fire war · a Russo-American century · Lincoln lives Reconstruction · the 13th early) — each EARNED by gambit + performance, surfaced ★ Reached / ◇ Within reach in "Your War vs History"** (overnight run, D115)

> **The E1 deepening's first build ships, against Aaron's locked D114 catalog (his build-order pick = FANTASTICAL tier first).** A catalog of **8 fantastical alternate ENDINGS** (4 CS, 4 US), each **EARNED**: its `precond(C)` OPENS the path (a wild-card gambit played, an emancipation choice, or a reachable victory `S.victoryReady`) and its `gate(C)` SECURES it (war momentum `vicMomentum` / the recognition level / the won-count / the 1864 election won). `precond && gate` → **★ Reached**; `precond && !gate` → **◇ Within reach** + a "to secure it" hint. Surfaced as an "Alternate endings — the fantastical tier" section in the **"Your War vs History"** tab (D111) + a compact line in the **after-action report** (D112), each with a citation-grade counterfactual labeled *fantastical*, set beside what really happened. **CS:** A British war (Trent escalates) · Foreign legions land (Maximilian) · Stonewall lives · The Golden Circle (a slave-power Caribbean reach — named honestly, never valorized). **US:** The rapid-fire war · A Russo-American century · Lincoln lives Reconstruction · The 13th, early.
>
> **ARCHITECTURE — PURE READ-OUT (D111/D112 pattern; byte-identical combat BY CONSTRUCTION):** new `src/83-endings.js` (`_END_CATALOG` with per-entry `precond`/`gate` predicate fns · `endScan`→`{reached,near}` · `endRenderSection(C,{compact})` · `_endMomentum`/`_endWildPlayed`/`_endRecognition`/`_endWon`/`_endVictoryReady`/`_endElected`/`_endEmancipation`). `endScan` READS only strategic state, WRITES NOTHING; **no tactical file touched; no combat/tick/resolve path reads `end*`** (grep-confirmed) → battles byte-identical by construction. Wired via ONE guarded line each in `divRenderTab` (81) + `aarRenderReport` (82) — byte-identical when absent. Side-gated (US↔US, CS↔CS). NO RNG.
>
> **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + critic; 9 agents) → invariantsHold=TRUE** (all 8 counterfactuals re-verified accurate + anti-Lost-Cause). **2 confirmed, both FIXED + probe-locked:** **(MED)** the cs-british-war source double-escaped (`&amp;`→`&amp;amp;` through `_endEsc`) → plain `&` (the `_divWILD_HIST` convention); **(LOW)** the empty-state teaser hardcoded both sides' examples → side-filtered.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-endings` **9/9**, **0 pageerrors**; no-regression GREEN — `probe-divergence` 14/14, `probe-afteraction` 10/10 (the two guarded-wiring hosts), `diag-classic` `nonBlank:346`. No combat/tactical file in the diff → 9 battles byte-identical by construction. `wcag-auditor` AA (`#e0795a`/`#c9c06a`/`#9b8560` = 5.77/9.20/4.85:1, no change).
>
> **NEXT (E1 continues against the D114 catalog):** **(1)** the **GROUNDED tier** — the 16 plausible/longshot end-states (CS: Recognized independence · Negotiated peace 1864 · Emancipated Confederacy · Stalemate · Fabian survival · Trans-Miss redoubt · King Cotton coerces Europe · Northwest secedes; US: Reconstruction-that-holds · Early abolition 1862 · Hard-war collapse · Reunion-without-emancipation · 1862 knockout · Forty-acres-enforced · Compensated-bloodless · the general strike) authored the SAME way in `_END_CATALOG` (just `tier:"plausible"/"longshot"` + their precond/gate + counterfactuals). **(2)** interactive **HINGE-FORK decision cards** on the `decResolve`/`_decApply` engine (`data/decisions.json`) that STEER toward these endings. **(3) E4-i2** — wire `victoryReady`/a reached ending to an actual war-END screen (frozen campaign loop). **(4) E2** codex/tutorial; **(5) E3** the full WCAG-AA pass. The symmetric AI-GM ([hard]) remains the §12 capstone; **Q5 Chattanooga + Q6 USCT LAST**. **Resume map:** START-HERE → this block → DECISIONS D115/D114(catalog)/D111/D54 → V1-CHECKLIST Phase E → `src/83-endings.js` (`_END_CATALOG`/`endScan`/`endRenderSection`) + `src/81-divergence.js` + `src/82-after-action.js` (the guarded embeds) + `src/80-victory.js` (`_vicWILDCARDS`/`vicMomentum`/`victoryReady`) + `tools/probe-endings.mjs`. **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-23 **§12.3 THE ELECTION-SUPPORT RELIEF-BIND: a commissioned UNION political general (Banks/Butler/Sigel/McClernand) costs extra political capital to relieve before the 1864 vote — the bind Lincoln lived — relaxing once the verdict is in** (overnight run, D113)

> **The GM §12.3 command-politics keystone ships** (the bind the Commission move D109 was built around, now unblocked by the 1864-election state E4/D112). A commissioned POLITICAL general delivers his constituency's votes while he serves, so relieving him before the 1864 election forfeits that support: an ADDITIVE surcharge on his relief cost (`_cmdReliefCost`) that rises year over year toward the vote (1862<1863<1864) and FALLS to zero the moment `clock.resolved1864` is set. `surcharge = round((politicalValue/100)·windowByYear[year]·surchargeMax=14)`, bounded. A teaching tell on the active-general card names the constituency + the `+N capital` cost. Teaches §12.3 concretely: a fielded Banks both fields a low commandLeadership (49 vs Grant 87, D109) AND can't be cheaply dumped before November 1864. **Picked for an autonomous tick** over the frozen-loop E4-i2 (contained command-economy, no frozen-loop, no design fork); Aaron then reordered the queue — **E1 hinge-forks next, fantastical tier first.**
>
> **ARCHITECTURE — UNION-ONLY, byte-identical until you field a political general (D74 no-fudge):** new pure helpers in `src/35-command.js` (`_cmdElectionBindCfg`/`_cmdPoliticalValue`/`_cmdElectionWindow`/`_cmdElectionSupportSurcharge`); `_cmdReliefCost` adds the surcharge. It is read ONLY by `cmdAppoint` (relief economy) + `_cmdActiveCard` (display) — **NEVER by `commandLeadership` or any combat/bridge/tick path** (grep-confirmed) → the bridge + 9 battles byte-identical by construction. The surcharge is EXACTLY 0 for every starting-roster general, the entire CS side, and outside the window. Data: `ratings.json` `electionReliefBind` (Inferred + 2 sources). No new save state, no `_SAVE_VER` bump, NO RNG.
>
> **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + critic; 10 agents) → 3 confirmed, ALL FIXED:** **(MED, anti-Lost-Cause)** the bind + the "Lincoln lived this bind" tell had no side gate → a CS player fielding Floyd/Pillow got a fabricated Lincoln attribution (the CSA held no 1864 presidential election) → **gated Union-only** (`if (!C||C.side==="CS") return 0;`), CS now fully byte-identical (probe-locked). **(MED)** a tampered NaN `windowByYear` escaped the clamp (`typeof NaN==="number"`) → could poison `clock.capital` → restored the `isFinite` guard + hardened the surcharge guard (probe-locked; defensive — config is build-frozen). **(LOW)** "rises toward November 1864" prose overstated the year-granular window → softened.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-command` **76/76** (+7 D113 steps incl. the Union-only gate + the NaN safety), **0 pageerrors**; `diag-classic` `nonBlank:346`; `probe-desk` 13/13, `t1probe` 14/14 — 0 pe. No combat path reads the relief cost → 9 battles byte-identical by construction. `wcag-auditor` AA (`#cbb27a` tell 8.41:1, no change).
>
> **NEXT (Aaron-reordered queue):** **(1) E1 deepening** — curated alt-history HINGE-FORK cards on the proven `decResolve`/`_decApply` engine (`data/decisions.json`, pure data, byte-identical by construction), **fantastical tier FIRST** (Aaron's pick), against the **LOCKED 24-END-STATE alt-history catalog** Aaron just elicited (12 CSA + 12 Union end-states + 7 reward axes + a labeled plausible→fantastical spectrum — see the AskUserQuestion rounds; record as D114 when E1 builds). **(2) E4-i2** — wire `victoryReady` (negotiated peace/recognition) to an actual war-END (frozen campaign loop). **(3) E2** codex/tutorial/presets. **(4) E3** the full WCAG-AA pass. The symmetric AI-GM ([hard]) remains the §12 capstone; **Q5 Chattanooga + Q6 USCT LAST**. **Resume map:** START-HERE → this block → DECISIONS D113/D112/D111/D91/D54 → V1-CHECKLIST Phase E → `src/35-command.js` (the "§12.3 (D113)" block: `_cmdElectionSupportSurcharge`/`_cmdElectionWindow`/`_cmdReliefCost` + the `electionTell`) + `data/ratings.json` (`electionReliefBind`) + `src/32-decisions.js` (`decResolve`/`_decApply` — the E1 hinge-fork engine) + `data/decisions.json` (the E1 cards) + `tools/probe-command.mjs` (the D113 steps). **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-23 **E4 / S5 VICTORY: THE GRADED AFTER-ACTION REPORT — an A–F report card on the whole war (battlefield · treasury · diplomacy · the home front + the 1864 election · the war's purpose · high command), the divergence ledger read back, the human cost vs the real toll, and a Reconstruction coda** (overnight run, D112)

> **The strategic arc's closing teaching payoff ships.** With E1's divergence ledger live (D111), I advanced to **E4 / S5 victory** at the named next item — **the rich graded after-action report** (D31.6). A new module `src/82-after-action.js` renders a period **"report card"** on the whole war: an **A–F letter grade** (the D94 scale — neutral 64 = C, A+ ≥90 "Legendary", CVD triple-encoded letter + word + colour) for each domain the President actually ran — **The Battlefield** (win-rate + the casualty exchange), **The Treasury** (the price level vs the 1861 dollar), **Diplomacy & the Blockade** (the recognition contest, side-aware), **The Home Front** (public will + the 1864-election verdict), **the war's PURPOSE** (US = emancipation, graded by *timing*: declined < late < 1863; CS = an honest, **NON-graded** "Confederate Cause" panel — slavery stated plainly in the founders' own words, never valorized, excluded from the GPA), and **High Command** (`commandLeadership`). Plus a headline **overall grade**, a **read-back of the D111 divergence ledger** (`divScan`/`divIndex` — the index + the top entries), the **human cost** beside the war's true ~750,000 toll (Hacker 2011), and a forward-looking **RECONSTRUCTION coda** keyed to the emancipation choice + the war's trajectory (Foner). **Pick rationale (decide-&-log):** chose E4 over the symmetric AI-GM ([hard]) and deeper E1 forks because it is the arc's payoff, it *summarizes the D111 ledger* (D111's logged foundation), and it is a low-risk pure read-out + one authorized frozen override. NOT a new fork.
>
> **ARCHITECTURE — a PURE READ-OUT + one authorized frozen override (byte-identical combat BY CONSTRUCTION):** `src/82-after-action.js` (`aarGrade`/`_aarDomains`/`aarOverall`/`_aarReconstruction`/`aarRenderReport`/`aarRenderTab` + the `warWonScreen` OVERRIDE) READS already-resolved aggregate state (`C.stats`/`C.economy`/`C.blockade`/`C.morale`/`C.clock`/`C.strategy`/`C.president.emancipation` + `commandLeadership`/`moraleCompute`) and WRITES NOTHING to the sim (the only write anywhere is the base-preserved `G.campaign = null` in the override). **No tick, no own state; NO tactical file touched; not in any `_t1Resolve`/`_t1InitAll`; `aar*` grep-confirmed in EXACTLY 3 files (the module, the shell tab dispatch, the probe) with ZERO combat/resolve/tick hits → the combat layer + all 9 battles are byte-identical by construction.** Surfaced TWO ways: a live **"After-Action"** desk tab (the **16th**, pure read-out) wired in `src/30-president-shell.js`, AND the **war-end FINAL report** — `warWonScreen()` is overridden (the `openWarDept`/`openUpgrade` frozen-override pattern, D30.2) to show the graded report + the Reconstruction coda instead of the bare base dispatch, reproducing the base's Main-Menu wiring + nullify verbatim. Manifest: module + `warWonScreen` override (collision 2× ✓). Citation-grade anti-Lost-Cause inline prose (Hacker, Foner, McPherson, Howard Jones, Dew — Stephens's Cornerstone Speech; failures named on both sides).
>
> **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + a completeness critic; 7 agents) → critic invariantsHold = TRUE** (no-fudge + byte-identity + citations all independently confirmed). **1 confirmed LOW + 2 critic-additional LOW — ALL ONE ROOT CAUSE, FIXED + probe-locked:** the home-front domain read `moraleCompute().public` behind a `typeof`-only guard, but `typeof NaN === "number"` (and `_morClamp` is NaN-capable), so a non-finite public-will could (in an out-of-contract partial-save) leak "NaN of 100", desync the graded-domain count vs the GPA divisor, and mis-render the graded row as the NON-graded "honest accounting" panel → FIXED with an `isFinite` guard at the read (the D52.6 finite discipline) + the panel count hardened to match `aarOverall`; a new probe step stubs `moraleCompute → NaN` and locks it. The critic's one citation note (Gallagher's *Confederate War* is a popular-will thesis, off-target for "slavery was the cause") was tightened to McPherson alongside the primary Dew cite.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-afteraction` **10/10** (+ the NaN regression step), **0 pageerrors**; full no-regression GREEN — `diag-classic` `nonBlank:346`, `bootprobe` ok, `t1probe` 14/14, `probe-desk` 13/13, `probe-divergence` 14/14, `probe-victory` 12/12, `probe-command` 69/69, `probe-officers` 15/15 — all 0 pageerrors. **No tactical file in the diff** → the 9 battles byte-identical by construction (`probe-ratings`/`probe-camp` were the documented THREE-CDN `load`-stall, share no code path, GREEN at D111). `wcag-auditor` AA (1 contrast fix `#937b55`→`#9b8560`; all grade chips 5.67–9.62:1; CVD triple-encode confirmed).
>
> **NEXT** (the E strategic arc continues): **E4-i2** — wire `victoryReady` (negotiated peace / recognition) to an actual war-END (not just a display hint) so the final report fires for a non-chain-complete victory, + the **§12.3 election-support relief-bind** (now unblocked — the After-Action home-front domain surfaces the 1864-election state; a political general's `_cmdReliefCost` could spike before the election). Then **E1 deepening** (curated `trigger.hinge` alt-history fork cards in `data/decisions.json` on the proven `decResolve`/`_decApply` engine — no engine change), **E2** (S4 codex/glossary/tutorial/presets), **E3** (the dedicated FULL WCAG-AA pass). The symmetric AI-GM ([hard]) remains the §12 capstone; **Q5 Chattanooga + Q6 USCT LAST** (Aaron's queue). **Resume map:** START-HERE → this block → DECISIONS D112/D111/D91(roadmap)/D54(no-thumb) → V1-CHECKLIST Phase E → `src/82-after-action.js` (`aarGrade`/`_aarDomains`/`aarOverall`/`_aarReconstruction`/`aarRenderReport` + the `warWonScreen` override) + `src/81-divergence.js` (`divScan`/`divIndex` — read back) + `src/33-morale.js` (`moraleCompute` + the 1864 election state) + `src/35-command.js` (`commandLeadership`/`_cmdReliefCost` — the relief-bind hook) + `src/30-president-shell.js` (the "afteraction" tab) + `tools/probe-afteraction.mjs`. **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-23 **E1 / S3 ALT-HISTORY: "YOUR WAR vs HISTORY" — a living divergence ledger of every point your war has left the documented record, with the history each entry departs from + an emergent-only toggle** (overnight run, D111)

> **The Phase-E strategic arc opens.** The GM §12 depth-chart arc is COMPLETE (Appoint/Relieve · Promote · Corps · Commission · Division). Per the locked roadmap (D91: after C breadth → **E strategic S3–S5**; Chattanooga + USCT are Aaron's-queue-LAST), I pivoted to **E1 / S3 alt-history** and shipped its named flagship first increment: a new **"Your War vs History"** President's-Desk tab (the 15th) — a living, dated **DIVERGENCE LEDGER** that records every point at which the player's war has left the historical record. Each entry carries a **category** (Emancipation / Foreign / Northern politics / The war's trajectory / Wild cards), a **tier** (minor △ / major ◆ / radical ★ — distance from the record, CVD-safe glyph+word+colour), a short **when**, and the **historical counterfactual** ("In history, …") that is the lesson. A headline **DIVERGENCE INDEX** (0..100, a saturating tier-weight sum + a 5-step word ladder) measures how far the war has strayed. Plus the **emergent-only toggle** (`G.settings.altHistoryEmergentOnly`): ON withholds the explicit ahistorical WILD-CARD gambits so the war diverges ONLY through play. **Pick rationale (decide-&-log):** chose E1's tracker over (A) the symmetric AI-GM ([hard], the §12 capstone) and (B) the §12.3 election-support relief-bind — it's lower-risk (a pure read-out), it's the named E1 flagship, it builds on the existing S1e `C.strategy` seeds, and it's the foundation the **E4 graded after-action report** will later summarize.
>
> **ARCHITECTURE — a PURE READ-OUT (the D106 OOB-board / D99 matchup pattern; byte-identical combat by construction):** new module `src/81-divergence.js` (`divScan` detector / `divIndex` / `divRenderTab` / `divWireTab` / `divEmergentOnly` + the `_divWILD_HIST` counterfactual map). `divScan` READS only existing strategic state (`C.strategy` / `C.president.emancipation` / `C.blockade` / `C.clock` / `C.stats`) and WRITES NOTHING; the ONLY write anywhere is `divWireTab` flipping the global setting + `saveLocal`. **No tick, no per-campaign state of its own; NO tactical file touched → the combat layer + all 9 battles are byte-identical by construction.** The emergent-only toggle is one guarded line at the top of `_vicWildSection` (80-victory.js) — default OFF → the wild-card section renders EXACTLY as before. Wired as one tab in `src/30-president-shell.js` (dispatch + `tabs[]` + `openWarDept` buttons + click-wiring). Detected hinges (all from reliable existing state): emancipation declined/early-1861/1862/**late-1864-65** (US); CS arm-the-enslaved; foreign recognition (achieved / window-open-past-Antietam); the 1864 election repudiation (US) **/ the CS home-front break** (side-correct); the CS war-trajectory; each wild card played (plausibility tier → divergence tier). Citation-grade, anti-Lost-Cause counterfactuals (McPherson/Foner/Howard Jones/Levine/Robertson/Weber/Du Bois — the inline-prose trust model of the sibling `_vicWILDCARDS`; failures named even-handedly both sides; the no-thumb-on-the-scale D54 framing explicit).
>
> **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + a completeness critic; 11 agents) → critic invariantsHold = TRUE** (NO-FUDGE grep-confirmed: the div* symbols + the setting read appear in EXACTLY the 3 diff files, ZERO hits on any combat/resolve/tick path; BYTE-IDENTICAL-WHEN-OFF by git diff + probe). **2 confirmed + 4 critic-surfaced; resolved:** **(HIGH, FIXED)** `election-1864` lacked a `side==='US'` gate → a CS home-front break falsely fired the Northern-election entry with a Lincoln counterfactual → US-gated + a side-correct `cs-homefront-breaks` branch (probe-locked). **(MED, FIXED)** a **late** (1864/65) emancipation — reachable in play — was silently dropped → an `ey≥1864` "delayed" branch, a Union failing named plainly (probe-locked). **(MED, FIXED)** Davis's six-year term began **Feb 1862** (not "1861–1867") → CS counterfactual corrected. **(LOW, FIXED)** the toggle `aria-pressed` was inverted → a stable `aria-label` + correct state. **(MED, DOCUMENTED)** the inline-counterfactual citation-gate coverage gap → trust boundary documented in the module header. **(LOW, DECLINED w/ rationale)** a recognition side-gate — the recognition entry is side-neutral and historically correct for either player + unreachable for US by normal play → no defect.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; `probe-divergence` **14/14** (+2 bug-hunt regression steps), **0 pageerrors**; full no-regression GREEN — `diag-classic` `nonBlank:346`, `probe-desk` 13/13, `probe-victory` 12/12, `probe-command` 69/69, `t1probe` 14/14, `bootprobe` ok — all 0 pageerrors. **No tactical file in the diff** → the 9 battles byte-identical by construction (the headless `load` battle battery is the documented THREE-CDN env-stall, shares no code path). `wcag-auditor` AA (2 contrast fixes; CVD triple-encode + aria confirmed).
>
> **NEXT** (the E strategic arc + the GM capstone): **E1 deepening** — interactive HINGE-POINT forks (build on the decisions system) + curated alt-history scenarios; then **E4** — the rich graded after-action report (it summarizes THIS divergence ledger) + the §12.3 ELECTION-SUPPORT relief-bind (it needs the 1864-election state, now partly surfaced) → **E2** S4 codex/glossary/tutorial/presets → **E3** the dedicated FULL WCAG-AA pass. Or **(A) the symmetric AI-GM** ([hard], the §12 capstone — an enemy-command shadow wired to combat WITHOUT breaking byte-identity/no-fudge; T15 gives it a roster to draft over). **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → DECISIONS D111/D110/D91(roadmap)/D54(no-thumb) → V1-CHECKLIST Phase E → `src/81-divergence.js` (`divScan`/`divIndex`/`divRenderTab` + `_divWILD_HIST`) + `src/80-victory.js` (`C.strategy` seeds + `_vicWILDCARDS` + the `_vicWildSection` guard) + `src/30-president-shell.js` (the "warvshistory" tab) + `tools/probe-divergence.mjs`. **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-23 **Q12: THE DIVISION SUB-TIER — seat your generals into the DIVISION billets nested under each corps; a corps is only as good as its divisions** (overnight run, D110 — Codex code + Claude vetting/doc close-out)

> **The GM depth-chart's next rung is shipped** (RATING-SYSTEM-DESIGN §12.1 — Army → Corps → **DIVISION** → Brigade), built on the Q10 corps chart (D108). Beyond seating I–IV Corps, the President now SEATS pool generals into each seated corps's **3 DIVISION billets**, spending **political capital** (`C.clock.capital`, the existing GM currency). A division can be staffed ONLY under a SEATED corps (the real chain of command), so the depth chart is a TREE built top-down. Teaches that a corps was only as good as its divisions — A. P. Hill's "Light Division" and Hood's/Pickett's divisions were the cutting edge of Lee's corps, **as were the Union's hard-fighting divisions in Hancock's II Corps**.
>
> **⚠ PROVENANCE — this milestone was a CODEX HAND-OFF, closed out here.** The Q12 CODE + DATA + PROBE were authored in an interrupted Claude-Code session and **finished + committed (`7b64d2e`) + pushed by Codex** — a sound, builds-`GATE OK`, byte-identical-to-source feature — **but Codex ran NO adversarial bug-hunt and wrote NO doc trail** (D110 was absent from DECISIONS/HANDOFF/WAKE-UP/RUN-LOG/V1-CHECKLIST/RATING-SYSTEM-DESIGN). This session is the **close-out**: ran the missing bug-hunt, fixed its one finding, and wrote the full doc trail. **No code reverted** — the commit is sound; the follow-up adds vetting + docs + one copy fix.
>
> **HOW IT REACHES THE FIGHT (byte-identical until you seat a division; D74/D94 no-fudge):** seated division commanders only add a small BOUNDED lift to `commandLeadership` — an INPUT the bridge already reads — via `_cmdDivLift` = clamp(Σ(`effDivRating`−64)×0.03, ±2). `effDivRating` = the general's GM OVR (carries the Q9 promotion) minus a `belowGradePenalty` (4) when his grade is below the **preferredGrade** — a division was a **Maj. Gen.'s** billet in BOTH armies (distinct from the corps tier where a CS corps was a Lt. Gen.'s), so any Brig. Gen. seated over a division leads stretched until promoted. The lift is deliberately SMALLER than the corps lift (cap 2 vs 4) so the hierarchy holds: **army ±17 > corps ±4 > divisions ±2.** No division seated → Σ over zero slots → lift 0 → the sandbox + all 9 battles identical to HEAD `7d86fcf`. **ONE BILLET PER MAN** (army/corps/division mutually exclusive; seating-higher clears the division); **vacating a corps CASCADES** (orphaned divisions stop contributing lift — enforced at the READ layer `cmdDivSeated`/`_cmdDivLift`, cleaned on load-sanitize). `cmd.divisions = {corpsIdx:{divIdx:id}}` rides the save (no `_SAVE_VER` bump); `cmdInit` sanitizes a tampered record on load. Writes ONLY `cmd.divisions` + capital (build-gate 4d scans `35-command.js`). NO RNG. `data/ratings.json` `divisionCommand` (Inferred + 2 sources: Glatthaar, Weigley).
>
> **Bug-hunt (5 diverse-lens Opus finders × default-refute verify + a completeness critic) → 4 lenses CLEAN, 1 confirmed LOW (FIXED):** the division teaching exemplars were **all-Confederate** (A. P. Hill / Hood / Pickett) while the rating mechanism is genuinely side-neutral and the sibling corps tier named both sides — the exact Lost-Cause over-celebration pattern CLAUDE.md #4 guards against. FIXED copy-only (byte-identical): added the Union counterpart (Hancock's II Corps divisions, Gibbon/Barlow) to the `divFoot` UI string + the `divisionCommand` `_note`. The save-tamper lens ran an 18-vector battery against `_cmdDivClean` (all sanitize correctly); the no-fudge lens grep-confirmed no combat path reads `cmd.divisions`; escaping confirmed safe.
>
> **Vet (offline gates GREEN; the headless `load` suite ENV-BLOCKED):** `GATE OK · no-fudge ✓ · citations ✓`; committed HTML **byte-identical to the source build**; `probe-command` **69/69** (+11 Q12 steps), **0 pageerrors**; `diag-classic` `nonBlank:346`; `t1probe` + `bootprobe` ok. **No tactical file is in the D110 diff** → the 9 battles are byte-identical to Q11 **by construction**. **ENV:** the strategic no-regression `load` battery (`probe-desk/ratings/camp/officers/field/oob` + the 9 battle probes) was blocked by the documented **THREE-CDN headless `load`-stall** (jsdelivr rate-limited the headless client after the first clean load; re-running the known-good `probe-command` reproduced the stall) — these probes share NO Q12 code path and were all GREEN in the Q11 vetting hours earlier. **The doc-trail + balance fix are committed in the close-out commit on top of `7b64d2e`.**
>
> **NEXT** (the GM §12 arc + the strategic arc remain): **(A) the symmetric AI-GM** (the opponent drafts/promotes/seats/commissions too, difficulty-scaled, NEVER extra strength — ⚠ [hard]: an enemy-command shadow wired to combat WITHOUT breaking byte-identity / no-fudge; surface if it forks; T15 gives it a roster to draft over). Cross-theater **Transfer** still needs authored `theater` fields in `generals.json` (all absent). The §12.3 **ELECTION-SUPPORT relief-bind** for the commissioned political generals (needs the 1864-election state — pairs with the E victory/election work). Or PIVOT to the **E strategic arc** (S3 alt-history · S4 codex · the dedicated WCAG-AA pass · victory + graded report + Reconstruction) → Phase F/I/H/J. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → DECISIONS D110/D109/D108/D107/D106/D102(Q9)/D94*/D92/D74 → RATING-SYSTEM-DESIGN §12 (the depth-chart MOVES — Q9 promote · Q10 corps · Q11 commission · **Q12 division** shipped; AI-GM/Transfer/election-bind remain) → `src/35-command.js` (the "Q12 (D110)" block: `cmdSeatDivision`/`cmdVacateDivision`/`_cmdDivLift`/`_cmdDivClean`/`_cmdDivEffRating` + `cmdSeatCorps`/`cmdVacateCorps`/`cmdAppoint` — the GM spine) + `data/ratings.json` (`divisionCommand`) + `tools/probe-command.mjs` (the Q12 steps). **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-23 **Q11: THE COMMISSION MOVE — bring the documented POLITICAL GENERALS (Banks · Butler · Sigel · McClernand · Floyd · Pillow) into your officer pool for political capital; rank and constituency are not competence** (overnight run, D109)

> **The GM "Commission" move is shipped** (RATING-SYSTEM-DESIGN §12.2/§12.3 — "bring a new officer into the pool"). Beyond the generals the war hands you, the President now COMMISSIONS the documented POLITICAL GENERALS — **US Banks · Butler · Sigel · McClernand**, **CS Floyd · Pillow** — into his officer pool for **political capital** (`C.clock.capital`, the existing GM currency; no new resource, §12), after which they are appointable / promotable / seatable like any general. A new President's-Desk **"Commission an officer — the political generals"** section lists each with his **LOW** combat OVR beside his **HIGH** political value, his constituency, an honest one-line record, and a Commission control (gated on capital + the `maxCommissions` 3-slate). It fills the GM layer's central TEACHING keystone — §12.3's command-politics bind: these men were cheap to commission and weak in the field (Lincoln/Davis kept them for the factions, votes, and immigrant regiments they delivered, not their generalship), so once you appoint or seat one his low OVR drags the army's leadership (a commissioned Banks fields commandLeadership **49** vs Grant's **87**).
>
> **PICK RATIONALE (decide-&-log within the cleared NEXT menu):** chose Commission (option C) over the symmetric AI-GM (B, [hard]) and a DIVISION sub-tier (lower-risk but thinner) because it fills the missing teaching keystone (the political-general bind the §12.3 anti-gamey constraints are built around), reuses the proven `cmdAppoint`/`cmdSeatCorps`/`cmdPromote` byte-identical-until-used spine + the §8 person concept, and adds 6 new citation-grade figures. Scoped to a **pure pool-addition** (no economy/election fork); the full §12.3 ELECTION-SUPPORT relief-bind is the logged next increment (it needs the 1864-election state — here the relief COST is already taught via each officer's `relief` class through `_cmdReliefCost`).
>
> **ARCHITECTURE (byte-identical until you commission):** the commissionable officers live in a SEPARATE data array (`data/generals.json` → `sides[side].commissionPool`), so `_cmdSideGenerals` (the starting roster) + every byte-identity-critical path are UNTOUCHED. `cmdCommission` writes ONLY `cmd.commissioned` (an id list) + `C.clock.capital` + a reputation seed — NEVER the scoreboard (build-gate 4d scans `35-command.js`). With nobody commissioned, `_cmdRosterPlusCommissioned`===the bare roster → the appoint/promote/corps pools, `commandLeadership`, and the 9 battles are identical to HEAD `8eb913a`. `_cmdById` was widened to ALSO resolve the commission pool (a commissioned-then-appointed officer resolves everywhere); `_cmdByIdRoster` keeps the roster-only lookup for the byte-identity-critical seeds + the commission guard. `cmd.commissioned` rides the save; `cmdInit` seeds `[]` once + SANITIZES on load (drop dupes/bogus/roster-ids/over-cap; non-array→array — the Q9/Q8b/Q10 idiom). NO `_SAVE_VER` bump. NO RNG.
>
> **CITATION-GRADE CONTENT (12-agent research+verify Workflow):** 6 Opus historians + adversarial source-check + a completeness critic; all 6 dossiers verified (sources real & on-claim, records honest on BOTH sides — Banks/Red River, Butler/Fort Fisher, Sigel/New Market, McClernand/Vicksburg, Floyd & Pillow/Fort Donelson; genuine contributions credited — Butler's 1861 "contraband", Banks's Louisiana free-labor — no fabrication). The verifier's one HARD correction applied: **Pillow → Brig. Gen.** (his "Maj. Gen." was a Mexican-War US-volunteer / TN-state-militia title; in CSA service he rose only to brigadier — CLAUDE.md #4). Ratings stamped **Inferred** (calibrated); bios carry ≥2 real sources (gate-4e clean); persona command/tactical/discipline calibrate so `fldOfficerSkillSeed` rounds EXACTLY to each entry skill (oracle verified for all 6).
>
> **Bug-hunt: 12 Opus agents (5 diverse lenses × default-refute verify + completeness critic) → invariantsHold = TRUE** (the critic grep-confirmed no combat path reads `cmd.commissioned`, `cmdCommission` writes only `cmd.commissioned`+capital+reputation, byte-identity-when-empty, Pillow Brig. Gen., every string escaped, zero id-collision). **5 confirmed (2 MED + 3 LOW), ALL FIXED + the behavior-affecting ones probe-locked:** **(MED)** the commission economy was bypassable at the function level — `cmdAppoint`/`cmdSeatCorps`/`cmdPromote` accepted an un-commissioned pool officer via the widened `_cmdById` → a `_cmdByIdRoster`+`cmdCommissioned` guard added to all three (byte-identical for roster ids; probe-locked). **(MED)** a seated commissioned officer always rendered "I Corps" (string for-in key → `_cmdNum`→0) → `parseInt(kk,10)` (probe-locked). **(LOW)** the shared `cap` gate idiom passed NaN (`typeof NaN==='number'`; tampered-save only, pre-existing) → `&& isFinite(clk.capital)` at all 6 capital-gate sites + cmdAppoint debits the rounded `cap`. **(LOW)** `maxCommissions` fallback default 4→3. **(LOW)** a commissioned officer past his service window now reads muted "Commissioned · no longer in service" (was a green false "in your pool"). (Critic-"missed" were all CLEAN confirmations: render purity, id-collision-free, the combat-default reads only the roster, the seed calibration, the documented `load`-stall probe-env note.)
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; **combat byte-identity STRUCTURAL + EMPIRICAL** — a 132-cell A/B (`.tmp/q11-ab.mjs` vs HEAD `8eb913a`) over (side × appointee × date) confirms `commandLeadership`/`bridgeArmy.overall`/`leadership`/the auto-resolve margin reads byte-identical with nobody commissioned, **re-confirmed after the bug-hunt guards** (inert for roster ids), 0 pe; `probe-command` **58/58** (+8 Q11 steps + a bug-hunt gate-lock step); FULL no-regression GREEN — `probe-oob` 16/16, `probe-desk` 13/13, `probe-ratings` 21/21, `probe-camp` 8/8, `probe-officers` 15/15, `probe-field` 13/13, `t1probe` 14/14, `bootprobe` ok, the 9 battles + `probe-csplayer` green (bullrun 15 · antietam 16 · fredericksburg 22 · gettysburg 16 · chancellorsville 24 · malvern 24 · shiloh 29 · vicksburg 18 · chickamauga 18 · csplayer 16); `diag-classic` `nonBlank:346`; 0 pageerrors throughout; `wcag-auditor` AA (all new Commission-UI colours/opacities measured ≥ AA — no changes; the inactive-row nested-opacity note is covered by the WCAG inactive-component exemption). **ENV:** intermittent THREE-CDN headless `load`-stalls (vicksburg's 3D-readability step + the A/B) cleared by retry / a server restart — the documented gotcha; the `domcontentloaded` A/B harness is the authoritative byte-identity spine.
>
> **NEXT** (the GM §12 arc + the strategic arc remain): **(A) the symmetric AI-GM** (the opponent drafts/promotes/seats/commissions too, difficulty-scaled, NEVER extra strength — ⚠ [hard]: an enemy-command shadow wired to combat WITHOUT breaking byte-identity / no-fudge; surface if it forks). Cross-theater **Transfer** still needs authored `theater` fields in `generals.json` (all absent). A **DIVISION sub-tier** under each corps (extends Q10's byte-identical-until-moved pattern, lower-risk). The §12.3 **ELECTION-SUPPORT relief-bind** for the now-commissionable political generals (needs the 1864-election state — pairs naturally with the E victory/election work). Or PIVOT to the **E strategic arc** (S3 alt-history · S4 codex · the dedicated WCAG-AA pass · victory + graded report + Reconstruction) → Phase F/I/H/J. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → DECISIONS D109/D108/D107/D106/D102(Q9)/D94*/D92/D74 → RATING-SYSTEM-DESIGN §12 (the depth-chart MOVES — Q9 promote · Q10 corps · Q11 commission shipped; AI-GM/Transfer/division-tier remain) + §15 → `src/35-command.js` (the "Q11 (D109)" block: `cmdCommission`/`_cmdCommissionPool`/`_cmdRosterPlusCommissioned`/`_cmdCommissionHTML` + `_cmdById`/`_cmdByIdRoster` + the GM spine `cmdAppoint`/`cmdSeatCorps`/`cmdPromote`) + `data/generals.json` (`sides[side].commissionPool`) + `data/ratings.json` (`commission` + the 6 commission `generalPersonas`) + `tools/probe-command.mjs` (the Q11 steps) + `.tmp/q11-ab.mjs` (the A/B). **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-23 **Q10: THE CORPS DEPTH-CHART — seat your generals into corps command over the Order-of-Battle tree; who you place (and whether his rank fits the billet) shapes how well the whole army fights** (overnight run, D108)

> **The GM depth-chart MOVE is shipped, built on the D106 OOB tree** (RATING-SYSTEM-DESIGN §12.1/§12.2/§12.4 — "an army version of waiver/trading/transfer"). Beyond appointing the ARMY commander (the existing `cmdAppoint` billet), a new President's-Desk **"The Corps Command — Depth Chart"** section lets the President SEAT pool generals into the army's CORPS billets (I–IV Corps), spending **political capital** (`C.clock.capital` — the existing GM currency, no new resource). Each billet shows its seated commander (name + grade + OVR + a below-grade tell) with a Vacate control, or "Vacant" + a pool `<select>` + a Seat control (gated on capital); a live read-out shows the net leadership the staffed corps add. Teaches real command organization — Lee's First (Longstreet) and Second (Jackson) Corps vs the Army of the Potomac's churning corps commands; the rout of Howard's XI Corps at Chancellorsville.
>
> **PICK RATIONALE (decide-&-log within the cleared NEXT menu):** chose the depth-chart MOVES (option A) over the symmetric AI-GM (option B, flagged [hard]) because A extends the proven `cmdAppoint`/`cmdPromote` spine over the D106 substrate and is byte-identical-until-moved like Q9; the new officer-slot/vacancy concept the derived T15 corps lacked is the `cmd.corps` slot map + a side-aware grade-fit.
>
> **HOW IT REACHES THE FIGHT (no new combat path; D74/D94 no-fudge):** seated corps commanders only add a small BOUNDED lift to `commandLeadership(C)` — an INPUT the bridge already reads — via `_cmdCorpsLift(C)` = `clamp( SUM over seated slots of (effectiveCorpsRating − 64) × perSlotWeight, ±liftCap )` (0.05 / cap 4). `effectiveCorpsRating` = the general's GM OVR (carries the Q9 promotion lift) minus `belowGradePenalty` (6) when his CURRENT grade is below the **SIDE-AWARE** preferred grade. It reaches combat ONLY through the existing `commandLeadership → bridge LEADERSHIP facet` pipe, NEVER the scoreboard (build-gate 4d scans `35-command.js` + `tactical/T15-oob.js`). The army commander still weighs most (~±17); the corps lift (±4) is a secondary modifier (§27).
>
> **THE SIDE-AWARE GRADE-FIT (historically precise; the Q9 synergy):** a US corps was a **Maj. Gen.'s** command, a CS corps a **Lt. Gen.'s** (the CS authorized the Lt. Gen. grade Sept. 18, 1862, promoting Longstreet/Jackson that Oct. to command the ANV's new corps). So on the shipped roster only CS **Stuart** (a Maj. Gen.) seats below grade over a corps; promoting him to Lt. Gen. (Q9) removes the penalty. `data/ratings.json` `corpsCommand`: `slots 4 · seatCost 3 · preferredGrade {US:"Maj. Gen.",CS:"Lt. Gen."} · perSlotWeight 0.05 · liftCap 4 · belowGradePenalty 6 · Inferred · 2 sources`.
>
> **BYTE-IDENTICAL until you seat a corps:** no corps seated → lift 0 → `commandLeadership` unchanged → the sandbox + all 9 battles identical to HEAD `2d11bd7` (a vacant billet contributes 0, NOT a penalty). `cmd.corps` rides the save (no `_SAVE_VER` bump); `cmdInit` sanitizes a tampered/imported record on LOAD (drop invalid slots/ids/dead generals/the army commander; one corps per general; array→{}). The army commander can't double-hold (excluded from the pool; `cmdAppoint` clears his corps). The seated commanders also NAME the player's DERIVED corps on the OOB board (pure display — the predicted edge is UNCHANGED; the enemy is never named; a "Garrison" node is never named).
>
> **Bug-hunt: 11 Opus agents (5 diverse lenses × default-refute verify + a completeness critic) → invariantsHold = TRUE** (the critic independently grep-confirmed no combat path reads `cmd.corps`, seat/vacate write only `cmd.corps`+capital, byte-identity airtight, no fabricated officers, the 2 sources real). **3 confirmed (1 MED + 2 LOW), ALL FIXED:** **(MED)** the seat-log said "leads **above** his grade" for a BELOW-grade general → "below". **(LOW)** a real general could be named over a derived **"Garrison"** node (a fort/detachment, not a corps) → the attach now guards to `/ Corps$/` nodes only (probe-locked). **(LOW)** the CS Lt. Gen. grade date ("created Oct 1862") → corrected to the Sept. 18, 1862 authorization + the Oct. promotions + the Nov. 6 reorganization.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; **byte-identity STRUCTURAL + EMPIRICAL** — a 132-cell A/B (`.tmp/q10-ab.mjs` vs HEAD `2d11bd7`) over (side×appointee×date) confirms `commandLeadership`/`bridgeArmy.overall`/`leadership`/margin reads byte-identical with no corps seated; `probe-command` **49/49** (+9 Q10 steps), `probe-oob` **16/16** (+the Q10 step: names the derived CORPS, edge unchanged, the Garrison guard, enemy never named); FULL no-regression GREEN — `probe-desk` 13/13, `probe-ratings` 21/21, `probe-camp` 8/8, `probe-officers` 15/15, `probe-field` 13/13, `t1probe` 14/14, `bootprobe` ok, the 9 battles + `probe-csplayer` green; `diag-classic` `nonBlank:346`; 0 pageerrors; `wcag-auditor` AA (3 contrast fixes on the new UI). **ENV:** intermittent THREE-CDN load-stalls on the heaviest battle probes cleared by standalone retry; the `domcontentloaded` A/B harness is the authoritative spine.
>
> **NEXT** (the GM arc continues + the strategic arc remains): **(B) the symmetric AI-GM** (the opponent drafts/promotes/seats too, difficulty-scaled, NEVER extra strength — ⚠ [hard]: an enemy-command shadow wired to combat WITHOUT breaking byte-identity / no-fudge; surface if it forks). Cross-theater **Transfer** still needs authored `theater` fields in `generals.json` (all absent). **Commission** (bring a new officer into the pool — needs a person spine, R-5's `fldMaterializePerson`). Or **deepen Q10**: a seated-extreme balance sweep (does a full elite staff ever over-rotate a strategic edge? — the §27 ceiling), or a DIVISION sub-tier under each corps. Or PIVOT to the **E strategic arc** (S3 alt-history · S4 codex · the dedicated WCAG-AA pass · victory + graded report + Reconstruction) → Phase F/I/H/J. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → DECISIONS D108/D107/D106/D105/D102(Q9)/D94*/D92/D74 → RATING-SYSTEM-DESIGN §12 (the depth-chart MOVES — Q10 shipped; AI-GM/Transfer/Commission remain) + §13 (matchup) → `src/35-command.js` (the "Q10 (D108)" block: `cmdSeatCorps`/`cmdVacateCorps`/`_cmdCorpsLift`/`cmdCorpsCommanderFor` + `commandLeadership` +lift + `cmdAppoint`/`cmdPromote` — the GM spine) + `src/tactical/T15-oob.js` (`fldCampaignOOB` corps attach + `_fldOOBSideExact`) + `data/ratings.json` (`corpsCommand`) + `tools/probe-command.mjs` (9 Q10 steps) + `tools/probe-oob.mjs` (the Q10 step) + `.tmp/q10-ab.mjs` (the A/B). **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-23 **Q8b: THE CAVALRY RECONNAISSANCE — "scout the enemy" tier-reveals the enemy Order of Battle, scaled by your appointed general's cavalry** (overnight run, D107)

> **The camp loop's deferred SCOUTING (§15) is shipped, built on the D106 OOB board.** An **"Order a reconnaissance"** control inside the Command-desk Order-of-Battle board lets the President spend a small amount of **political capital** (`C.clock.capital` — the existing GM currency, also spent by appoint/promote; no new resource) to reveal the **enemy** order of battle to a TIER scaled by the appointed army commander's persona **`cavalry`** attribute. **Three tiers (§15):** **light** (unscouted — the passive D106 estimate: force grade band + rough count + the CTA) → **better** (scouted, cavalry < 65: the enemy commander NAMED + per-corps grade + a posture/terrain advisory, no per-brigade detail) → **full** (scouted, cavalry ≥ 65: the complete enemy OOB). With the 9 generals' cavalry, Grant (66) + Lee (70) → full, the rest → better; a no-appointee campaign uses the HISTORICAL DEFAULT commander's cavalry. Intel is **fresh per engagement** (the scout record is keyed to the next-battle id → reverts to light when the battle changes). Teaches cavalry-as-the-army's-eyes — Stuart's rides, and the Gettysburg intelligence vacuum when his cavalry was absent (a real Confederate command failure, not glorified).
>
> **PICK RATIONALE (decide-&-log within the cleared NEXT menu):** chose scouting over the GM depth-chart MOVES (both unblocked by D106) because its scaling input (the `cavalry` persona attribute, spread 50–70) + its `reveal` hook in `fldCampaignOOBHtml` BOTH already existed, `36-camp.js` explicitly deferred scouting to exactly this substrate, the D106 board already advertised a dead "Scout the enemy" CTA, and scouting is **pure display + an economy spend (never touches combat)** — the safest byte-identity. The corps-billet MOVES need a new officer-slot concept the derived T15 tree lacks — deferred next.
>
> **ARCHITECTURE (the D106 wall preserved):** the WRITE `cmdScout` (`src/35-command.js`) gates on capital like `cmdAppoint`, reads the appointee's cavalry via `_cmdGenPersona`, computes the tier via the pure `_cmdScoutTierForCavalry`, and writes ONLY `cmd.scout` + debits capital — never the scoreboard. **T15-oob.js stays a PURE READ-OUT** — `fldCampaignOOBHtml(C, {reveal, reconHtml})` is told the tier + the recon-control HTML by `cmdRenderTab`; T15 only ADDS the "better"-tier renderer (`_fldOOBSideScouted` + `_fldScoutPosture`). **No combat/resolve/tick/conditioning path reads `cmd.scout`** (grep-confirmed) → a campaign that never scouts is byte-identical to the pre-Q8b build, and the 9 tactical battles are untouched. `cmd.scout` rides the save (C.president serialized wholesale); `cmdInit` sanitizes a malformed/tampered record (drop→null; valid→bounded) — no `_SAVE_VER` bump. Data: a `scout` block in `data/ratings.json` (cost 3 · cavalryFullThreshold 65 · baselineCavalry 54 · 2 real sources). Build-gate 4d already scans both `35-command.js` + `tactical/T15-oob.js`. NO RNG.
>
> **Bug-hunt: 9 Opus agents (5 diverse lenses [no-fudge/byte-identity · economy/determinism/bounding · citation/anti-Lost-Cause/no-fabricated-officers · XSS/escaping · UI/WCAG/edge] × default-refute verify + a completeness critic) → invariantsHold = TRUE** (the critic independently grep-confirmed no combat path reads `cmd.scout`, cmdScout writes only `cmd.scout`+capital, the 9 battles byte-identical, no fabricated officers, citations real). **1 confirmed LOW + 2 critic-surfaced LOW; actionable ones FIXED:** **(LOW)** `cmdScout` gated on `round(capital)` but debited RAW `clk.capital` — the Q9 gate/debit rounding divergence `cmdPromote` was hardened against (byte-identical under today's integer-only capital paths, a latent + consistency bug) → debit `cap - cost` now. **(LOW, critic)** `_fldScoutPosture` ignored `C.flipAtk` (the alt-history attacker swap the resolver applies) → the "better"-tier posture could mis-describe who attacks after a recovery refight → now honors `C.flipAtk` with the SAME non-mutating `Object.assign({}, bd, {atk:swapped})` swap the resolver uses (strength/edge are attacker-symmetric, so only the posture line needs it; a probe locks it). **(LOW, deferred — critic agreed)** the cosmetic `cmd.scout.turn` stamp nothing reads.
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓ · citations ✓`; **combat byte-identity STRUCTURAL** (no combat path reads `cmd.scout`) + EMPIRICAL (all 10 battle probes land on their historical results, unchanged); `probe-oob` **15/15** (+the SCOUT TIERS step: light→better→full ladder, "better" names + per-corps + posture + NO per-brigade leak, the flipAtk posture-flip + non-mutation), `probe-command` **40/40** (+6 Q8b steps: tier mapping + fresh="light"; reveal-at-tier + debit-exactly-cost; gate + idempotence; stale→light; save-tamper; no-output-gate + unscouted-offers-button); FULL no-regression GREEN — `probe-desk` 13/13, `probe-ratings` 21/21, `probe-camp` 8/8, `probe-officers` 15/15, `probe-field` 13/13, `t1probe` 14/14, `bootprobe` ok, ALL 9 battles + `probe-csplayer` green (bullrun 15 · antietam 16 · fredericksburg 22 · gettysburg 16 · chancellorsville 24 · malvern 24 · shiloh 29 · vicksburg 18 · chickamauga 18 · csplayer 16); `diag-classic` `nonBlank:346`; 0 pageerrors throughout; `wcag-auditor` AA (1 contrast fix `#d66040`→`#e86840` on the new "Needs capital to scout" span — the existing `#d66040` usages elsewhere are the same sub-AA, **pre-existing/out-of-scope, logged for a future pass**). **ENV:** the run was PAUSED mid-vet on an Aaron "pause internet" directive (the battle probes fetch THREE from a CDN; the bug-hunt + wcag are API calls) and RESUMED cleanly; THREE-CDN load-stalls cleared by retry (the documented gotcha).
>
> **NEXT** (the GM §12 follow-up + the strategic arc remain): **(A) the GM depth-chart MOVES** — promote/relieve/**reassign** a named officer into a corps/division billet over the T15 tree (extends `cmdAppoint`/`cmdPromote`; needs a new **officer-slot concept** the derived T15 corps lack — the derived corps are formation-labeled with `commander:null`, so this means letting the player seat a pool general at a corps billet + a vacancy/rank model; the cleanest next GM increment, byte-identical-until-moved like Q9). **(B) the symmetric AI-GM** (the opponent drafts/promotes too, difficulty-scaled, NEVER extra strength — ⚠ [hard]: an enemy-command shadow wired to combat WITHOUT breaking byte-identity / no-fudge; surface if it forks). Cross-theater **Transfer** still needs authored `theater` fields in `generals.json` (all absent). Or PIVOT to the **E strategic arc** (S3 alt-history · S4 codex · the dedicated WCAG-AA pass · victory + graded report + Reconstruction) → Phase F/I/H/J. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → DECISIONS D107/D106/D105/D104/D94*/D92/D74 → RATING-SYSTEM-DESIGN §15 (scouting SHIPPED) + §12 (the depth-chart MOVES, the next increment) + §13 (matchup) → `src/35-command.js` (`cmdScout`/`_cmdScoutTierForCavalry`/`cmdScoutTier`/`cmdScoutControlHtml`/`cmdInit`-sanitize + `cmdAppoint`/`cmdPromote` — the GM spine to extend) + `src/tactical/T15-oob.js` (`_fldOOBSideScouted`/`_fldScoutPosture`/`fldCampaignOOBHtml` reveal switch — the pure read-out) + `data/ratings.json` (`scout` block) + `tools/probe-command.mjs` (the 6 Q8b steps) + `tools/probe-oob.mjs` (the SCOUT TIERS step). **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-22 **THE OOB-MAPPING SUBSTRATE — the strategic next-battle now maps to a structured Order of Battle; a read-only roster board ships on the Command desk** (overnight run, D106)

> **The shared blocker for BOTH the GM depth-chart MOVES and Q8b scouting is built** (RATING-SYSTEM-DESIGN §12.1/§13/§15). New module **`src/tactical/T15-oob.js`** maps the strategic next-battle (`_brgNextBattle`) to a STRUCTURED order of battle — **commander → corps → brigade** — for both sides, each billet carrying its OVR/grade/strength/provenance (the T14 rating spine). Surfaced as a read-only **"Order of Battle — the Next Engagement"** board on the President's-Desk Command tab. Per the run plan, scoped SMALL first: a read-only query layer + roster board, **no combat wiring** (the procedural skirmish `_fldCampaignSkirmishParams` in T2 is UNCHANGED). **No new fork surfaced** — `BATTLES[]` already carries real `cmdUS`/`cmdCS` and the 9 scenarios carry real brigade OOBs, so the substrate is buildable read-only + citation-clean without contradicting a shipped teaching.
>
> **TWO SOURCES, ONE SHAPE (`fldOOBForSide`/`fldCampaignOOB`/`fldCampaignOOBHtml`):** a battle WITH a hand-built scenario (`fldScenarioData`) → its REAL brigade OOB (real commanders + the authored provenance read via `_fldOOBProvFromNote`, reusing the Q7 reader `_fldOOBUnits`); a procedural battle → a DETERMINISTIC corps/brigade tree split from `bd.us`/`bd.cs`/year, seeded by id+side (NO RNG). Derived billets are FORMATION-LABELED (I Corps · 1st Brigade · Garrison) with `commander:null` — **NEVER a fabricated officer** (CLAUDE.md #4/D92). The army-commander headline names only the player's APPOINTED general (+ his `_cmdGenRating` OVR) or the historical `cmd<Side>` (Verified). **ENEMY-FUZZY (§15):** the board shows the PLAYER exact + the ENEMY fuzzy (grade + count + commander, no per-brigade detail) — the hook Q8b scouting DEEPENS via a higher `reveal` tier; the substrate returns FULL data for both sides for the GM/scouting to query.
>
> **THE WALL (byte-identical BY CONSTRUCTION):** a PURE READ-OUT called ONLY from `cmdRenderTab`. The generated-html diff vs committed HEAD `7df4372` is ALL-ADDITIONS (the T15 block + one desk line, **0 removed/modified lines**) → the sandbox + all 9 battles are byte-identical. build-gate 4d's `RATING_MODULES` now ALSO scans `tactical/T15-oob.js`.
>
> **Bug-hunt: 15 Opus agents (5 diverse lenses × default-refute verify + a completeness critic) → "NOT SAFE AS-IS" → 5 confirmed (1 HIGH + 4 MED), ALL FIXED + re-vetted; the hard invariants [no-fudge/byte-identity, determinism, no-fabricated-officers, XSS] independently CONFIRMED HOLD.** **(HIGH, anti-Lost-Cause)** a multi-phase battle's phase-0 (opening) OOB shown under the BARE name could forecast a CS edge for Gettysburg/Antietam/Vicksburg/Chickamauga (Union wins) — the Q7 failure resurfacing at the display layer; FIXED by threading `phased`/`phaseName` + scoping the headline/edge/note/footer to "the opening engagement / the opening clash". **(MED)** the build-gate change my T15 comments claimed was never applied (the first Edit silently failed) → `RATING_MODULES += T15` actually applied. **(MED)** the derived artillery battalion destroyed ~12-15% of a side's men (no redistribution) → FIXED by carving the gun arm off the TOP of corpsMen (conserves EXACTLY). **(MED)** a mixed "(Verified identity; Inferred strength)" note overclaimed as Verified → `_fldOOBProvFromNote` downgrades a mixed note to Inferred. **(MED, defensive)** a one-sided authored OOB could pit authored-vs-derived → `fldCampaignOOB` decides ONE source for both sides. + 2 LOW probe adds (bounding, enemy-no-leak).
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓` (4d scans T15) `· citations ✓`; **byte-identity 0 removed lines vs HEAD `7df4372`**; `probe-oob` **14/14** (incl. men-conservation 24000-exact · larger-army-renders-larger · phase-framing antietam→"The Cornfield" · enemy-no-leak · bounding · provenance-downgrade); FULL no-regression GREEN — `probe-command` 34/34, `probe-desk` 13/13, `probe-ratings` 21/21, `probe-camp` 8/8, `probe-officers` 15/15, `probe-field` 13/13, `t1probe` 14/14, `bootprobe` ok, ALL 9 battles + `probe-csplayer` green (bullrun 15·antietam 16·fredericksburg 22·gettysburg 16·chancellorsville 24·malvern 24·shiloh 29·vicksburg 18·chickamauga 18·csplayer 16); `diag-classic` `nonBlank:346`; 0 pageerrors; `wcag` AA. ENV: healthy window; one fredericksburg load-stall cleared by a standalone retry.
>
> **NEXT** (the substrate now UNBLOCKS both): **(B) the GM follow-up** (§12) — the **symmetric AI-GM** (the opponent drafts/promotes too, difficulty-scaled, NEVER extra strength; needs an enemy-command shadow wired to combat WITHOUT breaking byte-identity/no-fudge — [hard]; the OOB substrate now gives it a roster to draft over) + cross-theater **Transfer** (still needs authored `theater` fields in `generals.json` — all absent) + **Commission** + the corps/division **depth-chart MOVES** (promote/relieve/reassign over the T15 tree — extends `cmdAppoint`/`cmdPromote`) → **(C) Q8b SCOUTING** (tier-reveal the enemy OOB via `fldCampaignOOBHtml`'s `reveal` param, scaled by the `cavalry` rating). Either is now buildable on T15. Or PIVOT to the **E strategic arc** (S3 alt-history · S4 codex · the dedicated WCAG-AA pass · victory + graded report + Reconstruction) → Phase F/I/H/J. **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → DECISIONS D106/D105/D104/D94*/D92/D74 → RATING-SYSTEM-DESIGN §12 (the depth-chart MOVES) + §13 (matchup) + §15 (scouting tiers) → `src/tactical/T15-oob.js` (`fldOOBForSide`/`fldCampaignOOB`/`fldCampaignOOBHtml` — the substrate the GM/scouting query) + `src/35-command.js` (`cmdAppoint`/`cmdPromote`/`cmdRenderTab` — the GM spine + the board injection) + `src/85-battle-bridge.js` (`_brgNextBattle`/`bridgeArmy`) + `tools/probe-oob.mjs`. **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-22 **RATING SYSTEM LIVE DEV-TRAITS — a general's OVR now develops over the campaign, shaped by a hidden Madden potential ceiling** (overnight run, D105)

> **The last open item on the R-6 rating track is shipped** (RATING-SYSTEM-DESIGN §9-R-6 "reputation level-up → OVR still TODO" + D94-fork-#5 "Reputation→OVR dev-traits = LIVE in-campaign"). `cmdOnResolve` already evolved a general's reputation by battle outcome and `_cmdGenRating` (0.55·skill + 0.45·reputation) → `commandLeadership` already read it; D105 adds the **Madden DEV-TRAIT** that SHAPES that arc — a hidden potential **ceiling**, a **floor**, a development **rate** (rise vs fall), and an **attritionDrag** (the §15-R3 decline-with-heavy-attrition). **ACCURATE INPUTS, never an output gate (D74/D92)** — a dev-trait only shapes the general's REPUTATION (an INPUT the existing pipe consumes), it NEVER writes a casualty/winner; build-gate 4d now also scans `35-command.js` to make that structural.
>
> **THE KEYSTONE (byte-identical-when-off, the D104 pattern):** the `cmdOnResolve` seam is byte-identical for an UNASSIGNED general (or absent devTraits data) — the LITERAL pre-D105 path `clamp(cur+delta,5,98)`, rate 1, no drag (proven across ALL five outcome quadrants + both clamp rails). The deliberate, citation-grade change is the authored `data/ratings.json` `devTraits.assign` map (20 generals → one of **12 archetypes**). Ceiling/floor are RELATIVE to the seeded start (`devTrack.start`, fixed); every archetype keeps `ceilingAbove≥5 & floorBelow≥5` so the pre-D105 win-raises/loss-lowers invariant holds for all. **D105 touches NO tactical code → the 9 battles are byte-identical by construction** (`git diff` confirms the change is `35-command.js` + the `devTraits` data + the gate + the probe only; the tactical layer never runs `cmdOnResolve`).
>
> **ANTI-LOST-CAUSE THESIS:** the Union's elite are RISERS — Grant/Sherman/Sheridan (`rising_star`) + Thomas (`steady_rock`) develop to the summit, rated as high as the Confederacy's born legends Lee/Jackson (`established_legend`) — the honest claim that the Union *found and elevated* its best command, while the Confederacy's calcified (Bragg `quarrelsome`) and self-destructed (Hood `volatile_aggressor`). Command failures named plainly on BOTH sides (US McClellan `plateau` / Burnside·Hooker `overpromoted`; CS Bragg / Hood). All 20 stamped **Inferred** (a calibrated reading of a documented arc — gate-4e-exempt) with 1-2 real sources each; nothing laundered (Forrest's Klan + Longstreet's Lost-Cause slander named in the notes). New: a Command-desk **"Career Arc"** read-out (`_cmdCareerArcHTML`) — the dev-trait tier + the OVR arc (start→now→peak on a floor..ceiling band with ticks) + a trend verdict (Rising/Holding/Fading/Untested); triple-encoded, CVD-safe, **wcag AA**.
>
> **Bug-hunt: 29 Opus agents (5 diverse lenses [no-fudge · byte-identity · bounding/determinism · citation/balance · UI/probe] × default-refute verify + a completeness critic) → SAFE TO COMMIT** (the critic confirmed BOTH hard invariants HOLD via independent grep: `devTrack`/`reputation` are read by no combat code; the off-path is the literal pre-D105 arithmetic). 8 confirmed + 3 critic-missed (1 HIGH · 3 MED · 7 LOW), ALL fixed: **(HIGH, the only blocker)** a fabricated-by-misattribution Thomas citation ("Conquered into Liberty" is Eliot Cohen's, not Larry Daniel's) → corrected to Daniel's *Days of Glory: The Army of the Cumberland* (CLAUDE.md non-negotiable #4); **(MED)** the Burnside note "proved right at Fredericksburg" laundered a Union failure → rewritten to name the futile Marye's-Heights assaults plainly; **(MED, keystone)** `_cmdDevTrait`/`_cmdDevStart` consumed `devTrack.start` RAW → a tampered/stale save (start NaN/500) poisoned the clamp → now coerced+clamped at the read site, `cmdInit` SANITIZES the whole record on LOAD (the Q9 pattern), `_cmdCareerArcHTML` coerces the whole `tr` record; **(MED→the 4d coverage gap above);** **(LOW ×7)** byte-identical-off probe now covers all 5 quadrants + both rails; the off-test asserts assign restored; floor/drag steps assert the locked trait; the render step covers all 4 trend branches + battles=0; a new SAVE-TAMPER probe step; the balance check counts named-failures as (−,~).
>
> **Vet (PROPER VETTING, all GREEN):** `GATE OK · no-fudge ✓` (4d now scans 35-command.js) `· citations ✓` (the Thomas fix); `probe-command` **34/34** (9 D105 steps: data+balance · byte-identical-off all-quadrants+rails · assign-restored · rising-star-vs-plateau+ceiling-cap · floor-cap · attrition-drag-bounded · no-output-gate+devTrack-purity · save-tamper · render+4-trend-branches); `probe-ratings` **21/21** (the tactical rating engine + 9-battle badges intact — byte-identical by construction), `probe-desk` 13/13, `probe-camp` 8/8, `probe-officers` 15/15, `t1probe` 14/14, `bootprobe` ok; `diag-classic` `nonBlank:346`; **0 pageerrors**; `wcag-auditor` AA. ENV: a healthy memory window this session — probes ran foreground via a threaded server.
>
> **NEXT** (the D104 menu, unchanged — the rating track is now feature-complete through R-6 + dev-traits): **(A) the OOB-MAPPING SUBSTRATE** — the strategic-next-battle ↔ tactical-OOB mapping, the shared blocker that unblocks BOTH the GM depth-chart tree AND Q8b scouting (⚠ surface if it forces a NEW fork — it's a design-heavy substrate, the reason D105 was picked first) → **(B) the GM follow-up** (symmetric AI-GM + cross-theater Transfer [needs authored `theater` fields, all absent] + Commission + the corps/division depth-chart tree) → **(C) Q8b scouting** → the **E strategic arc** → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §9-R-6 (dev-traits row) + §14.6 (reputation→OVR live) + §12 (the GM depth-chart) → DECISIONS D105/D104/D94*/D92/D74 → `src/35-command.js` (the "D105 · LIVE DEV-TRAITS" block: `_cmdDevTrait`/`_cmdAttritionDrag`/`_cmdDevTrackUpdate`/`_cmdCareerArcHTML` + cmdInit devTrack seed + cmdOnResolve modulation) + `data/ratings.json` (`devTraits` — archetypes + assign) + `tools/probe-command.mjs` (the D105 steps) + `tools/build.mjs` (gate 4d's `RATING_MODULES`). **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-22 **RATING SYSTEM R-6 — the badge ASSIGNMENT to the 9 rosters: the rating system finally BITES in the historical battles** (overnight run, D104)

> **The rating system now changes combat in the real battles** (it was byte-identical-when-unassigned since R-3). **43 documented commander/unit traits** are assigned to the 9 shipped battles' brigade tokens via a central `data/ratings.json` `rosterBadges` map { scenarioId → unitId → [badgeKey] }; the seam stamps them through `fldBrSpec`→`fldMakeUnit` (a `.slice()` copy, null when absent → byte-identical); the existing R-3/R-4 engine (`fldBadgeFactor` static factor + `fldXFactorStep` surge, gated on `__FIELD.badges`, per-lever-capped) does the rest. **ACCURATE INPUTS, never an output gate (D74/D92)** — a documented trait nudges the universal levers and the result EMERGES; build-gate 4d still forbids the module writing cas/winner/men/sev. **Anti-Lost-Cause balance: CS 12 clear virtues : 6 flaws : 4 double-edged; US 11 virtues : 10 flaws** — the Union's command flaws (McClellan `the_slows`, Burnside `rigid_plan`, McClernand/Sherman `piecemeal`, green/surprised troops) are named as plainly as the Confederacy's virtues.
>
> **Design = a 10-agent citation Workflow** (one Opus historian per battle proposing source-cited badge→unit assignments + an anti-Lost-Cause/flip-risk critic; its 5 required edits applied). Notable calls: `the_slows` sits on Antietam `us_sturgis` (no McClellan army token exists; per D92 NOT forced onto every division — the P2 Burnside's-Bridge delay); `grand_charge` on Chickamauga `cs_johnson_gap` (the breakthrough that SUCCEEDED) and DECLINED on Pickett's repulsed charge; **Shiloh retuned** — the headless single-phase baseline is a ~tossup, so the Union's caught-napping flaw rides the SCENARIO (the CS surprise) not a combat penalty that would flip it against history, while the Hornet's-Nest STAND (Prentiss `rock_of_chickamauga` + W.H.L. Wallace `stonewall`) is modeled → US ~4/5. New: an accessible **badge-chip HUD** (`fldRatingBadgesHtml`, wcag PASS).
>
> **Bug-hunt: 16 Opus agents (5 lenses × default-refute verify + critic) → SAFE TO COMMIT** (both hard invariants — no output gate, byte-identical-when-off — re-verified intact). All 8 confirmed + 3 critic-missed were LOW/MED; fixes applied: `grand_charge`'s note de-overclaimed (the "doomed double-edge" downside is NOT modeled — it ships upside-only, deferred); the probe now resolves unit-ids in ALL 9 scenarios (rot guard); a latent `_spdMul` reset; `cs_colston_div`'s `stonewall` removed (role mismatch — Colston was attacking); the `_rosterNote` honesty pass (dual-badge tally + the always-on R-3 static-trigger disclosure + the flip framing).
>
> **Vet (PROPER VETTING, all GREEN):** GATE OK · no-fudge ✓ · citations ✓; **byte-identity harness IDENTICAL all 30** (sandbox + 9 battles × 3 seeds) badges-OFF vs committed HEAD `610ae6c` + **0 winner flips** (the only single-phase flip is Chancellorsville seed 1 US→CS = Jackson's flank march = Lee's victory, i.e. TOWARD history); **all 9 per-battle probes PASS with badges ON** (Antietam CS/US/CS 8/8 · Gettysburg Pickett-repulsed US 8/8 · Fredericksburg CS 8/8 · Vicksburg 8/8 · Chickamauga Rock-holds 8/8 · Shiloh 29/29 · Chancellorsville 24/24 · Malvern 24/24 · Bull Run ok); `probe-ratings` **21/21** (new R-6 step) · `probe-presets` 26 · `probe-arms` 23 · `probe-logistics` 14 (the 3 layer-isolation `_badgesOff` fixes — see below); `wcag` clean; `diag-classic nonBlank:346`; `bootprobe` ok; 0 pe. **ENV:** the 8 GB Mac was memory-starved (~400 MB free; the user's apps held the RAM) so headless-Chrome probes stalled/crashed intermittently — each was confirmed by retrying until a memory window opened; the `domcontentloaded` A/B harness was the reliable spine.
>
> **Probe note (the 3 fixes):** badges is now a default-ON optional combat layer that legitimately shifts bullrun1 (CS stonewalls → CS 8/8), which broke 3 LAYER-ISOLATION byte-identity guards (presets/arms/logistics) that hardcoded the pre-badge CS-5/8 / [4,6] baseline. Fix = add `__FIELD._badgesOff=true` to those guards (badges joins officers/logistics/arms in the optional-layer set turned off for the pristine baseline). The per-battle probes + csplayer/officers/etc. were unaffected (they want badges on / test the sandbox).
>
> **NEXT** (the D103 menu, unchanged): **(B) the GM follow-up** (symmetric AI-GM + cross-theater Transfer + Commission + the corps/division depth-chart tree — each needs absent substrate; consider building the **strategic-next-battle ↔ tactical-OOB mapping** first, it unblocks BOTH the depth-chart tree AND **(C) Q8b scouting**) → wire **live dev-traits** (reputation→OVR mid-campaign, half-built in `cmdOnResolve`) → the **E strategic arc** → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §4 (badge catalog) + §9 (R-6 row) + §12 (the GM depth-chart) → DECISIONS D104/D103/D94*/D92(accurate-inputs)/D74 → `data/ratings.json` (`rosterBadges` — the assignment map; `badgeDefs`) + `src/tactical/T14-ratings.js` (`fldScenarioRosterBadges`/`fldBadgeFactor`/`fldXFactorStep`/`fldRatingBadgesHtml`) + `src/tactical/T1-bull-run.js` (`fldBrSpec` stamp) + `tools/probe-ratings.mjs` (R-6 step) + `.tmp/r6-ab.mjs` (the A/B + byte-identity harness). **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-22 **RATING SYSTEM R-6 (citation-provenance) — the CATALOG-WIDE citation hardening + a permanent build-gate** (overnight run, D103)

> **The citation-grade non-negotiable is now machine-enforced, catalog-wide.** Before you can hand a brigade a combat-biting negative badge keyed to a documented flaw, that flaw must be properly sourced — so this milestone shipped the prerequisite to R-6's negative-badge assignment. **An audit found the "Verified-with-<2-sources" debt was NOT just badges (the logged D98–D101 framing) — it was 47 records across SIX data files** (badges 13 · personas 2 · weapons 9 · artillery 9 · engineering 8 · terrain 1 · cabinet 5). All now carry **≥2 independent, real, web-verified sources**. PURE METADATA + a little teaching-card text → **byte-identical sim** (all 10 deterministic battles IDENTICAL to committed HEAD `5b12916`; **no numeric combat field touched**, bug-hunt-confirmed).
>
> **THE KEYSTONE — a permanent build-gate (`tools/build.mjs` gate 4e):** recursively scans every `data/*.json`; any record whose `prov`/`provenance` is "Verified" (matched **trim+case-normalized on EITHER key** — so a `"Verified "` whitespace/case variant OR an `Inferred`-masks-`Verified` pairing cannot erode it) carrying a `sources`/`src` array of <2 → `die(5)`. **Verified firing on all 47 before the fix; passes after** (`GATE OK · parse ✓ · hex ✓ · collision ✓ · no-fudge ✓ · citations ✓`). The ~72 long descriptive forms ("Verified specs (…)", "Verified (high) — …") cite inline in prose and trim-lowercase to ≠ "verified", so they stay out of the structured gate **by design**. This mirrors the 4d no-fudge wall: a non-negotiable made un-erodable by the build.
>
> **THE CITATION WORKFLOW (the ultracode pattern):** 15 Opus agents — 7 domains → per-domain finders → **default-refute** adversarial verifiers → a completeness critic; sources **web-checked**; discipline = NEVER fabricate, **downgrade-to-Inferred over guess**. **Zero fabrication** (critic-confirmed); all citations real/on-claim/independent. Two judgment calls: **`beloved` held *Inferred*** (its solid cite is Symonds 1997 on Cleburne; the obvious Lee source Freeman 1934 is Lost-Cause-tinged and held out); **`grand_charge` upgraded *Verified*** (Stewart 1959 · Hess 2001 · Sears 2003). Anti-Lost-Cause cut both ways: `feared`'s 2nd cite swapped Wyeth 1899 (hagiography) → **Hurst 1993**; the negative badges on real men carry the firmest pairs (Sears+Rafuse, Marvel+O'Reilly, Cozzens+Daniel, McMurry+Sword). **Accuracy fixes folded in:** the Fredericksburg cover card's "almost impregnable, and a deathtrap" was **misattributed to the NPS** (traces to a fan site) → paraphrased; `massed_battery` note dropped Pelham; `earned_in_blood` → "14 of the 16 (Army)"; Springfield/art-cs-handicap precision softened.
>
> **Bug-hunt (34 Opus agents: 4 lenses [gate-logic · data-integrity · citation-soundness · probe/doc] × default-refute verify + critic; 16 confirmed, ALL LOW except 1 MED, all latent; critic: "R-6 is SAFE TO COMMIT — every citation web-verified real, no fabrication, gate green, byte-identity proven").** Fixed now: the **MED** (gate strict-equality evaded by `"Verified "`/case) + the `||`-masking LOW → the normalized both-key matcher (synthetic test **8/8**); the doc "47" count; the terrain co-attribution + feared optics. **Deferred-&-logged (NO live offender):** add the singular prose `source` key to the gate; a distinct-author independence check (every record already has ≥2 distinct authors); polish the other negative-badge second sources (defensible as-is).
>
> **Files:** `tools/build.mjs` (gate 4e + the `GATE OK` line); `data/{weapons,artillery,engineering,terrain-cover,cabinet,ratings}.json` (sources + a few text fixes; the cabinet 5 were applied by a research agent directly — reviewed + accepted); `tools/probe-ratings.mjs` (+1 catalog-invariant step, count-pin ≥26, normalized walk); `RATING-SYSTEM-DESIGN.md` (§4/§9 synced); regenerated `civil_war_generals.html`. The apply harness is `.tmp/apply-citations.mjs`; the byte-identity harness `.tmp/r6-verify.mjs` (base `.tmp/r6-base.html` = `git show HEAD:civil_war_generals.html`).
>
> **Vet:** `GATE OK · no-fudge ✓ · citations ✓` (hardened); gate matcher synthetic **8/8**; **byte-identity harness IDENTICAL all 10** vs committed HEAD `5b12916` + an R-6 live citation sanity (catalog clean, 0 offenders; the_slows V/2, beloved I/1, grand_charge V/3; 0 pe); `probe-ratings` **20/20** + `probe-engineering-corps` 22/22 + `probe-cabinet` 12/12 + `probe-weapons` 8/8 + `probe-cannon` 9/9 (retry: the documented `load` stall) + `probe-arms` 23/23 + `probe-decisions` 14/14 (0 pe each); `diag-classic` `nonBlank:346`. **ENV:** the `load` suite stalls intermittently on the THREE-CDN headless load (documented gotcha — start a threaded server + retry); the `domcontentloaded` byte-identity harness is authoritative.
>
> **NEXT** (pick one; decide-&-log within the cleared scope): **(A) the rest of R-6** — now that the catalog is citation-clean, ASSIGN the negative + positive badges to the 9 shipped rosters (this CHANGES combat — it is NO LONGER byte-identical; needs an A/B balance sweep + a historical-direction re-validation of all 9 battles, NOT an output gate per D92/D74) + wire live dev-traits (reputation→OVR mid-campaign, half-built in `cmdOnResolve`). **(B) the follow-up GM increment** (§12) — the symmetric AI-GM + cross-theater Transfer + Commission + the corps/division depth-chart tree; ⚠ each needs absent substrate (an enemy-command shadow wired to combat without breaking byte-identity/no-fudge [hard]; authored `theater` fields in `generals.json` [all absent]; the strategic-next-battle ↔ tactical-OOB mapping = the SAME blocker as **(C) Q8b SCOUTING**). Consider building the OOB-mapping substrate first — it unblocks BOTH the depth-chart tree AND scouting. Then the **E strategic arc** → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §4 (badge catalog + the D103 sourcing note) + §9 (the R-6 row) + §12 (the GM depth-chart) → DECISIONS D103/D102/D94*/D92(accurate-inputs)/D74 → `data/ratings.json` (`badgeDefs` — now fully sourced, ready to ASSIGN) + `src/tactical/T14-ratings.js` (`fldBadgeFactor`/`fldXFactorStep` — the badge→combat seams that go live when badges are assigned) + `tools/build.mjs` (gate 4e) + `tools/probe-ratings.mjs`. **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-22 **RATING SYSTEM Q9 — the GM PROMOTION economy (the first depth-chart MOVE: promote · multi-currency · merit gate)** (overnight run, D102)

> **The first real GM *move* beyond appoint/relieve is shipped.** On the President's-Desk Command tab there is now an **"Officer Corps — Promotions"** section: the President promotes a general one grade up a clean strategic ladder (**Brig. Gen. → Maj. Gen. → Lt. Gen. → General**), spending a **multi-currency** budget — **political capital** (`C.clock.capital`, the primary, shared with relief/appointment) **+ a NEW seniority pool** (`cmd.seniority`, accrues over the campaign, capped) — and **merit-gated** by reputation. PURE INPUT-SEEDING + display, **byte-identical until the player promotes** (sandbox + all 9 battles proven byte-for-byte identical to committed HEAD `fc1d251`). Built on the existing `cmdAppoint`/`_cmdReliefCost` spine + the R-5 `fldPromotePerson` latent-command concept; entirely in `src/35-command.js` (the "Q9 (D102)" block) + a `promotion` config in `data/ratings.json`.
>
> **THE MERIT GATE (the honest history, the keystone):** each target grade carries a reputation **meritBar**. EARNED (rep ≥ bar) → a confidence bump (+3 rep); ABOVE merit (rep < bar) → a ×1.8 capital surcharge **+ a jealousy reputation HIT (−4)**. Because OVR = 0.55·skill + 0.45·reputation, **over-promoting a favorite past his record can LOWER his OVR** — rank without merit doesn't make a good commander (teaches the real command politics Lincoln navigated; anti-Lost-Cause: merit, not grade, is competence). Leapfrogging a passed-over senior adds a seniority surcharge.
>
> **HOW IT REACHES THE FIGHT (no new combat path, no fudge):** a promotion lifts the general's effective skill by a small **BOUNDED** amount — `_cmdPromoteSkillLift` = (rankBase grade-delta × 0.92), clamped to **≤6** — added inside the EDITED `_cmdEffectiveSkill(gen, C)`, which flows ONLY through the existing `_cmdGenRating → commandLeadership →` the bridge LEADERSHIP facet (an INPUT the sim already reads; `commandMarginEdge` reads aggression/caution, untouched). `cmdPromote` writes ONLY `clk.capital`/`cmd.seniority`/`cmd.promotions`/`cmd.reputation` — **never the scoreboard** (D74/D94 wall). **0 lift when un-promoted → every pre-Q9 reading byte-identical** (live-sanity proven: a promotion stores "Lt. Gen.", lifts commandLeadership 79→80; the un-promoted skill reads identical with/without C for all 20 generals).
>
> **New fns (`src/35-command.js`):** `_cmdPromoCfg`/`_cmdRankBase`/`_cmdBaseGrade`/`_cmdGradeIdx`/`_cmdNextGrade`/`_cmdCurrentGrade`/`_cmdPromoteSkillLift`/`_cmdLeapfrogged`/`_cmdPromoteInfo`/`cmdPromote`/`_cmdPromoRow`/`_cmdPromotionsHTML`/`_cmdNum`; EDITED `_cmdEffectiveSkill(gen,C)` (+lift), `cmdInit` (seeds + load-clamps `seniority`, seeds `promotions`), `cmdOnResolve` (accrues `seniority` — invisible/byte-identical), `cmdRenderTab` (+section), `cmdWireTab` (+promote buttons); call sites `_cmdGenRating`/`cmdActiveTraits`/`_cmdActiveCard` now thread C. **Data:** the `promotion` block in `data/ratings.json` (ladder · meritBar · capitalCost · aboveMeritMul · seniority{CostBase,Leapfrog,Start,PerTurn,WinBonus,Cap} · earnedRepBonus · aboveMeritRepHit · skillLiftWeight · skillLiftCap · topCommandPreferredGrade).
>
> **Bug-hunt (7 Opus agents: 4 diverse-lens finders × default-refute verify + a completeness critic; 2 raw → 2 confirmed + 1 actionable critic gap, ALL LOW + LATENT; 0 HIGH/MED; critic: "SOUND, may commit — no-fudge/byte-identity/bounding/determinism/no-exploit all hold; no HIGH/MED missed").** ALL 3 fixed: (1) `cmdPromote` gated on `round(capital)` but spent raw → now debits the rounded `cap` (one lens; byte-identical under integer paths); (2) `_cmdBaseGrade`'s unrecognized-rank fallback "Maj. Gen." would over-grade a future non-named officer onto the General ladder → now "Brig. Gen." (lowest; hardens the deferred depth-chart path); (3) `cmdInit` now re-clamps a tampered/stale out-of-band `seniority` to the cap on LOAD. The critic's 4th note (the two `diag-classic` 404s) is the pre-existing documented tolerated asset-layer noise, not Q9.
>
> **Vet:** GATE OK (no-fudge ✓); `probe-command` **25/25** (9 new Q9 steps incl. the save-tamper LOAD re-clamp + the byte-identity micro-assert); **byte-identity harness IDENTICAL for all 10** vs committed HEAD `fc1d251` + Q9 live sanity (0 pe); `probe-ratings` 19/19 + `probe-desk` 13/13 + `probe-camp` 8/8 + `t1probe` 14/14 (0 pe each); `diag-classic` `nonBlank:346`; `bootprobe` ok; `wcag-auditor` 0 AA failures (fixed the new warning text `#c2502e`→`#d66040`, 4.51:1). **ENV:** the `load` suite stalled intermittently on the THREE-CDN headless load (documented gotcha — cleared by a fresh threaded server + retry); the `domcontentloaded` byte-identity harness (`.tmp/q9-verify.mjs`, base `.tmp/q9-base.html` = `git show HEAD:civil_war_generals.html`) is the authoritative no-regression proof.
>
> **NEXT = a FOLLOW-UP GM increment** completing §12 once its substrate exists — the **symmetric AI-GM** (the opponent drafts/promotes too, difficulty-scaled, never extra strength; needs an enemy-command shadow wired into the matchup/auto-resolve without breaking byte-identity or the no-fudge wall), cross-theater **Transfer** (needs authored `theater` fields in `generals.json` — currently all `?`), and **Commission** + the full **corps/division depth-chart tree** (needs the strategic-next-battle ↔ tactical-OOB mapping — the SAME blocker as **Q8b SCOUTING**, so these two pair naturally). **Or Q8b scouting** next. Then **R-6** negative-badge sweep across all 9 battles + the **catalog-wide badge-provenance sweep** (D98–D101 logged: many badgeDefs are Verified with <2 sources — fix via a citation research+verify workflow) + live dev-traits → the **E strategic arc** → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §12 (the depth chart + moves) + §14.3 (the political-capital/seniority numbers) + §15 (the GM-layer answers) → DECISIONS D102/D99/D94* → `src/35-command.js` (the Q9 block + `cmdAppoint`/`cmdPromote`/`_cmdEffectiveSkill` — the GM spine to extend) + `src/tactical/T14-ratings.js` (`fldPromotePerson`/`fldMaterializePerson`/`fldPersonTeam` — the R-5 person spine for the depth-chart tree) + `data/ratings.json` (`promotion` + `generalPersonas` + `rankBase`) + `tools/probe-command.mjs`. **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ EARLIER — 2026-06-22 **RATING SYSTEM R-5 — the PROSOPOGRAPHY SCALE-OUT (bulk per-person rows + lazy materialization + play-as-anyone)** (overnight run, D101)

> **The D93 "Soldier's Story" substrate the whole rating spine was built for is shipped.** A brigade can now be inspected down to its men, any person *materialized* + *promoted*, and a generated soldier carries a deterministic name + Inferred rating — all **PURE DATA + DISPLAY**, **byte-identical** (no shipped scenario assigns bulk persons; sandbox + all 9 battles proven IDENTICAL to committed HEAD `84c5e95`). Selecting a brigade now shows its **MEN OVR** (the men's prosopography mean) beside the R-2 combat OVR, with a provenance-hatched accent.
>
> **THE LAZY-MATERIALIZATION KEYSTONE (§8) — the whole point of R-5:** a brigade TOKEN carries ONE aggregate `fldMenMeanOVR(u,year)` (its men's mean OVR, derived **O(1)** from xp + arm-branch + year + a deterministic per-brigade-id jitter, clamped [20,88] — a brigade AVERAGE never reaches the Legendary individual ceiling). Per-person rows are **NEVER instantiated en masse:** `fldMaterializePerson(spec,year)` builds exactly ONE row on demand; `fldBrigadeMuster(u,n,year)` a **HARD-CAPPED (≤6)** representative sample (captain/sergeant/private from `data/ratings.json` `musterTemplate`) — **never `u.men` rows** (proven: a 100,000-man brigade still materializes 3). The mean is always recomputable, never the source of truth (so a future zoom-to-regiment is a roll-up of materialized rows).
>
> **New pure functions (`src/tactical/T14-ratings.js`, the R-5 section):** `fldMenMeanOVR` · `fldMaterializePerson` (reuses R-0 `fldDerivePerson` → headline + dual OVR + grade; generated → deterministic synth period name [`_fldSynthName`/`_FLD_FIRST`/`_FLD_LAST`, NO RNG] + Inferred + a `latentCommand` seed; authored → Verified pass-through) · **`fldPromotePerson(person,toRank)`** (the latent-command **play-as-anyone** promotion — lifts command/tactical/discipline/charisma by the rank-base delta; returns a NEW person, pure) · `fldPersonTeam` (the EA-style {side,army,corps} journey hook) · `fldProvenanceStyle` (the **Inferred-vs-Verified visual distinction** §10 — Verified SOLID / Inferred **HATCHED** `repeating-linear-gradient` / Disputed dashed; meaning rides pattern + glyph [✓/~/?] + word, NOT colour → CVD-safe) · `fldMusterHudLine` (the live compact line) · **`fldMusterRollHtml`** (the full "Muster Roll" panel — probe-vetted + ready for the journey-mode character sheet / Army Register). **Live wiring = ONE guarded line** in `fldRenderHud` (`src/tactical/T0-field-sandbox.js`). NO new combat path; NO RNG.
>
> **Bug-hunt (11 agents: 4 diverse-lens Opus finders × default-refute verify + a completeness critic; 6 raw → 2 confirmed + 2 critic, ALL LOW + LATENT; 0 HIGH/MED; completeness verdict: no no-fudge-wall write, no determinism leak, no lazy-materialization breach, no escaping gap).** ALL 4 fixed — defensive-copy hardening so a FUTURE bulk-person/career path can't corrupt canonical `GAME_DATA`: **(1)** `fldMaterializePerson` returned the AUTHORED persona BY REFERENCE → now shallow-copies persona at the materialize boundary; **(2)** `fldPromotePerson` with an unmapped/typo'd `toRank` silently mapped to the 64 anchor (+16 unearned lift + false officerTier) → now requires a real `rankBase` key, else a clean no-op clone; **(3)** the `sources` array was a second shared reference (propagated by promote) → `.slice()`'d on both paths; **(4)** the persona-less promote guard returned the input ref → `_fldClonePerson` for every no-op path (honors the "returns a NEW person" contract).
>
> **Vet:** GATE OK (no-fudge ✓); `probe-ratings` **19/19** (the R-5 step: one-mean-per-token; the LAZY invariant [sample ≤6 for 100,000 men]; generated=Inferred+synth-name+latentCommand; authored Verified pass-through; promotion surfaces command + is pure; team hook; Inferred-hatched vs Verified-solid distinct; render triple-encoded; purity; **+ the 4 hardening guards** `aliasGuarded:true`); **byte-identity harness IDENTICAL for all 10** vs committed HEAD `84c5e95` + R-5 live UI renders (`.tmp/r5-verify.mjs`, 0 pe); `diag-classic` `nonBlank:346`; `bootprobe`/`t1probe` ok; `probe-field` 13/13 + `probe-officers` 15/15 + `probe-command` 16/16 + `probe-desk` 13/13 + `probe-camp` 8/8 (0 pe each); `wcag-auditor` 0 AA failures (lifted 5 sub-AA opacity values `.55/.6→.65`). **ENV:** the `load` suite WORKED this session (threaded server) with intermittent THREE-CDN stalls cleared by a single retry; the `domcontentloaded` byte-identity harness is the authoritative no-regression proof.
>
> **NEXT = the post-R-5 GM DEPTH-CHART MOVES** (RATING-SYSTEM-DESIGN §12 — now UNBLOCKED by R-5's per-person spine): promote/transfer/commission spending political capital (`C.clock.capital`; extends `cmdAppoint`/`_cmdReliefCost`) + a **symmetric AI-GM** (the opponent drafts/promotes too — an arms race, difficulty-scaled) + the multi-currency economy (§14.3). **Or Q8b SCOUTING** (deferred — needs the strategic-next-battle ↔ tactical-OOB mapping). Then **R-6** negative-badge sweep across all 9 battles + the **catalog-wide badge-provenance sweep** (D98/D99/D100 logged: many badgeDefs are Verified with <2 sources — fix via a citation research+verify workflow) + live dev-traits → the **E strategic arc** → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). The full muster-roll inspect-expand / "Army Register" UI (the ready `fldMusterRollHtml` panel) is a later UI pass. **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §12 (the depth chart) + §14.3 (the political-capital numbers) + §5/§8 (the person spine) → DECISIONS D101/D99/D94* → `src/35-command.js` (`cmdAppoint`/`cmdRevert`/`_cmdReliefCost`/`_cmdGenRow` — the GM spine to generalize) + `src/tactical/T14-ratings.js` (`fldMaterializePerson`/`fldPromotePerson`/`fldPersonTeam`/`fldBrigadeMuster` — the R-5 person spine) + `data/ratings.json`. **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM Q8 — the between-battle CAMP LOOP (drilling + progression)** (overnight run, D100)

> **The agency that GROWS your rating over a campaign is shipped.** A new **"Camp" desk tab**: between battles the President DRILLS the army (4 foci — musketry/maneuver/entrenching/endurance — each toward a ceiling, costing fatigue; or **DELEGATE** to the field officer for a steady auto-sim gain) and reads the army's drill grade + the sitting general's OVR growth (PROGRESSION — his reputation already evolves in `cmdOnResolve`). PURE INPUT-SEEDING (no scoreboard), and **byte-identical until the player engages the camp** — proven, committed, pushed.
>
> **`src/36-camp.js` (new module):** `campInit` (idempotent; `C.president.camp` = drill{4 foci}, fatigue, delegate, **engaged**) · `campDrill(C,focus)` (raises a focus toward CEIL 90, +fatigue, sets engaged) · `campSetDelegate` · **`campTrainingBonus(C)` (THE BRIDGE SEAM** — per-facet deltas, ALL ZERO when no focus is drilled) · `campOnResolve` (rest sheds fatigue; a delegated army auto-drills; a clean win SEASONS / a battle this side bled the lion's SHARE erodes — gated on `engaged`) · `campRenderTab`/`campWireTab`. **Drilling/progression only — SCOUTING deferred to Q8b** (needs the strategic-next-battle ↔ tactical-OOB mapping; logged).
>
> **The bridge seam (`src/85-battle-bridge.js`):** drilling lifts firepower (musketry) / morale (maneuver) / supply (entrenching) + relieves fatigue (endurance) + a small overall sharpness — **bounded** (full-drill ≈ +8 overall, ≤14; fatigue DRAGS it) — EXACTLY 0 when undrilled → byte-identical. Wired: `campInit` in `_t1InitAll` (after cmdInit), `campOnResolve` in `_t1Resolve` (after cmdOnResolve), the "Camp" tab in `30-president-shell.js`, `"36-camp.js"` in the manifest. `C.president.camp` rides `serializeSave` + lazy-migrates via campInit — no `_SAVE_VER` bump.
>
> **THE KEYSTONE (byte-identity):** an `engaged` flag (false until the first drill/delegate) makes the WHOLE loop inert until the player opens the Camp (`campOnResolve` early-returns; `campTrainingBonus` is all-zero at zero drill) — a campaign that never camps conditions byte-for-byte as the pre-Q8 build. NO RNG (deterministic).
>
> **Bug-hunt (21 Opus agents, 15 raw → 1 confirmed MED + 4 critic), ALL fixed:** the real one — the "bloody" attrition test divided a raw men-count by `C.manpower.strength` (a 0-100 INDEX, also read *post-attrition*), so nearly every battle read "bloody" → the **win-seasons branch was DEAD** (drilled training only ever decayed, inverting the headline progression). FIXED to the casualty **SHARE** (own / both-sides total — unitless, order-independent; PIVOT 0.55), + the probe SEASONING step re-tooled to realistic casualties + a pyrrhic-win case. Plus: removed dead `lastTurn`; corrected an overstated comment; logged that drilling is a deliberate player-only lift (the AI doesn't drill; §27-bounded).
>
> **Vet:** GATE OK (no-fudge ✓); `probe-camp` **8/8**; **byte-identity harness IDENTICAL for all 10** vs committed Q7; `probe-bridge` 6/6 + `probe-conditioning` 6/6 (un-engaged conditioning unchanged) + `probe-command` 16/16 + `probe-desk` 13/13; `diag-classic` `nonBlank:346`; 0 pageerrors; `wcag-auditor` 0 AA failures. **Shipped across a context-budget handoff** (the new ≤25%-context rule adopted this session — memory `civilwar-context-budget-rule` + `~/.claude/CLAUDE.md` + START-HERE/AUTONOMOUS-RUN, committed `c926502`; the bug-hunt + fixes + this commit closed it out cleanly).
>
> **NEXT = R-5 — the PROSOPOGRAPHY SCALE-OUT** (RATING-SYSTEM-DESIGN §5/§8/§9 R-5): bulk-derive individual person rows (`fldDerivePerson` already exists — RANK_BASE/BRANCH_TILT/YEAR_DRIFT + deterministic seeded jitter) with **lazy materialization** (a brigade carries one `menMeanOVR`; per-person rows built only on inspect/play-as), latent-`command` promotion (the play-as-anyone hook), and the EA-style `team`/journey hook — Inferred OVRs visually distinct from Verified. Then the post-R-5 **GM depth-chart MOVES** (promote/transfer/commission + symmetric AI-GM, §12 — deferred from Q7) → **R-6** negative-badge sweep + the **catalog-wide badge-provenance sweep** (D98/D99 logged: many badgeDefs are Verified with <2 sources — fix via a citation research+verify workflow) + live dev-traits → the **E strategic arc** → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §5/§8/§9 → DECISIONS D100/D99/D94* → `src/tactical/T14-ratings.js` (`fldDerivePerson`/`fldPersonaOVR`/the §8 person record) + `data/ratings.json` + `tools/probe-ratings.mjs`. **Honor the context-budget rule (≤25% → stop + hand off).**

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM Q7 — the GM MATCHUP layer (dual Attack/Defend OVR + the pre-battle matchup screen)** (overnight run, D99)

> **The rating system now pays off at the two decision points — who to appoint, and which battle you're walking into — as a PURE READ-OUT (no sim change; sandbox + all 9 battles byte-for-byte identical to the committed R-4 build).**
>
> **Dual Attack/Defend OVR on the Command desk (`src/35-command.js`):** `_cmdGenDualOVR(C,gen)` carries the headline `_cmdGenRating` (the R-2 OVR) PLUS an Attack/Defend split — the situational truth beside "general competence." Tilt source: the documented persona's aggression/initiative & resolve/grit (64-anchored, via the new `fldDualTilt` in T14) when the general is rated; else his own aggression/caution from `generals.json` (50-anchored ×0.18). Surfaced as **ATK/DEF pills** on the active-general card + a line in **every appoint-pool row** (so the appointment compares fit — the audacious Jackson grades higher on the attack, the cautious McClellan on the defensive).
>
> **The pre-battle MATCHUP SCREEN on the tactical side-choice card (`src/tactical/T7-command-side.js` injects `fldMatchupHtml(sd)`; engine in `src/tactical/T14-ratings.js`):** each army's **FORCE OVR** (the men-weighted mean of its brigades' `fldUnitRatingOVR` over the authored OOB — the same metric the live HUD shows, aggregated) + total strength + strongest brigade + commander, with a labeled **predicted-edge bar** (strength × quality → a US/CS share; names + percentages in text). Pure forecast of the odds from the historical inputs the D74 model consumes — **never read at resolve-time.** Triple-encoded + CVD-safe (number + grade + word + %); `wcag-auditor` 0 AA failures.
>
> **⚠ ANTI-LOST-CAUSE FIX (the bug-hunt's HIGH):** a multi-phase battle's OOB is authored as per-SECTOR day-slices, so the first cut **summed all phases** and forecast a **CS edge at Gettysburg** — a battle the larger Union army WON (a Lost-Cause-flavored falsehood on a teaching tool). FIXED: `_fldOOBUnits` now scopes a multi-phase battle to the **OPENING phase (phase 0)** and `fldMatchupHtml` labels it "**the Opening Engagement**." That is historically honest — Gettysburg's phase-0 CS edge is **day 1, when the CS did drive the Union through the town**, not a verdict on the battle the Union won. The single-phase forecasts are all defensible (Fredericksburg "US clear edge" → the US had the numbers and still lost to the stone wall = *anti*-Lost-Cause; the posture/terrain note carries the contingency).
>
> **Vet:** GATE OK (no-fudge ✓); `probe-ratings` **18/18** (the Q7 step: dual tilt directional + fldDualOVR-consistent; single- AND multi-phase force OVR; the opening-phase scoping is STRICTLY a slice not the all-phase sum; the "Opening Engagement" framing + commander line + an explicit anti-Lost-Cause framing guard for Gettysburg; graceful "" for no-OOB); **byte-identity harness IDENTICAL for all 10** vs committed R-4; `probe-command` 16/16, `probe-csplayer` 16/16, `probe-desk` ok; `diag-classic` `nonBlank:346`; `bootprobe` ok; 0 pageerrors; `wcag-auditor` 0 AA failures. Bug-hunt: 21 Opus agents, 15 raw → 3 confirmed + 1 HIGH critic, ALL 4 fixed + re-vetted (the HIGH above; commander line phase-aware; anon dedup global counter; comment reworded off the unrated Thomas). Env: `load` worked this session (threaded server) with intermittent stalls handled by retries + the domcontentloaded byte-identity harness.
>
> **DECIDED-&-LOGGED (scope):** Q7 = the GM layer's *read-out* (matchup + dual OVR), fully buildable now on existing OOB+general data, byte-identical. The depth-chart *moves* economy (promote/transfer/commission + the symmetric AI-GM, §12) depends on **R-5's per-billet prosopography data** (no authored corps/division sub-commanders exist yet), so it is deferred to a post-R-5 GM increment — this slice ships the matchup payoff now; the loop proceeds to **Q8**.
>
> **NEXT = Q8 — the between-battle CAMP LOOP** (RATING-SYSTEM-DESIGN §15 "the camp loop"): **drilling** (each camp turn pick a focus — musketry/maneuver/entrenching/endurance — for a concordant skill+cohesion gain, or delegate to the officer for auto-sim gains; over-drilling risks fatigue), **scouting** (an auto-simmable recon phase tiered by the `cavalry` rating: light → strength+fuzzy grade, better → named badges+posture, great → the enemy OOB), and **progression** (attributes grow from combat XP + drilling; a hidden per-entity potential ceiling [Madden dev-trait]; decline with heavy attrition/age; reputation→OVR live in-campaign). Build it as a strategic-desk increment over `C.president`/`C.clock` + the rating spine; surface a "Camp" view. **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §15 (the camp-loop answers) + §14.6 (reputation→OVR dev-traits live) → DECISIONS D99/D94 → `src/35-command.js` (cmdOnResolve reputation evolution — the dev-trait hook) + `src/tactical/T14-ratings.js` (the rating spine) + the strategic-desk render (`src/30-president-shell.js`/`_wdRefresh` for a new tab) + `data/ratings.json`. After Q8: R-5 prosopography scale-out → the post-R-5 GM depth-chart MOVES → R-6 negative-badge sweep + the catalog-wide badge-provenance sweep + live dev-traits → the E strategic arc → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order).

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM R-4 — the X-FACTORS (the dramatic "in the zone" surge, inside the no-fudge wall)** (overnight run, D98)

> **The Madden layer now SWINGS a fight — provably without ever touching the scoreboard.** An X-Factor badge enters "in the zone" when its situational trigger fires and surges on ONE channel, hard-capped. Demonstrated: a single `foot_cavalry` badge on one CS sandbox brigade **flipped a deterministic US win into a CS win** (same seed), yet the total-casualty shift was only **17.2%** and the command aura peaked at **0.47 < the 0.9 wall** — the rating reaches the result EMERGENTLY through bounded inputs, never by writing `cas`/`victory`/`men`/`sev`.
>
> **`fldXFactorStep(dt)` (`src/tactical/T14-ratings.js`):** gated on `__FIELD.badges`; for each unit carrying an `rung:"xfactor"` badge (`_fldUnitXFactors`), `_fldXFactorInZone` tests activation (march_vigor=moving · his_offensive=moving/charging · ammo_low_defend=ammo<12 & not charging · last_stand_defend=holding & (low morale or a friendly routing within 220yd) · usct_assault/mass_assault=charging · flanking=cav & charging). In the zone it surges **ONE channel = the declared lever**: a NON-speed X-Factor sets `u._xfActive = fldRatingRealismCap(tier,"xfactor")` (arcade 1.45 / balanced 1.20 / historian 1.08); a SPEED X-Factor (Foot Cavalry / The Slows) sets `u._spdMul` only (capped [0.85,1.15]) and does NOT also surge the aura. One-shot ⚡ `fldAnnounce`+`fldScenarioBanner` (`_xfOn` rising edge); `_xfGlow` marker pulse; deterministic `_fldXfDecay`→1 out of zone. **NO RNG.** A unit with no X-Factor is wholly untouched → byte-identical.
>
> **`fldXFactorApplyCmd(u)`:** `u.cmdBonus = min(FLDO.CMD_BONUS_CAP, (cmdBonus||0)*_xfActive)` — pushes the summed aura toward the **0.9 wall, never beyond**; strict no-op unless active. Called in `fldSimStep` AFTER `fldOfficersStep` (aura summed first), gated on `badges` (NOT officers) so both surge channels share one gate.
>
> **Seams (`src/tactical/T0-field-sandbox.js`):** `fldSimStep` runs the surge + apply around `fldOfficersStep`; **`fldMoveFactor` clamps the COMBINED speed factor** (the `_spdMul` surge × the R-3 static speed-badge factor) to ONE documented band **[0.75,1.30]** (exactly 1 → skipped → byte-identical); `fld2dDraw` draws a guarded `_xfGlow` — a CVD-safe amber burst RING (the shape carries meaning) + a **vector-drawn** lightning bolt (no emoji-font tofu), `reduceMotion`-honored. **Data:** added `first_with_the_most` (Forrest, cav) → 7 X-Factors; none assigned to a shipped roster yet (R-6) → byte-identical.
>
> **Vet:** GATE OK (no-fudge ✓); `probe-ratings` **17/17** (the R-4 surge step + the NO-FUDGE REPLAY GATE: the surge bites + bounded + capped); the **`domcontentloaded` byte-identity harness IDENTICAL for all 10** (sandbox + 9 battles, R-4 build vs committed R-3 HEAD — the authoritative X-Factor-off no-regression proof); `tactical-visuals` 10/10 scenes 0 pe; `diag-classic` `nonBlank:346`; the 9 battle probes + field + logistics + bootprobe passed via `load` (when momentarily healthy). **⚠ ENV (still intermittent):** the standard `load` suite stalled on the THREE CDN this session — the systems probes (officers/command/presets/…) were `load`-blocked, but the byte-identity harness exercises the full `fldSimStep`→`fldXFactorStep`→`fldOfficersStep` path (officers ON) for all 10 and proves it byte-identical, covering their core concern. **Re-run them next session once `load` is healthy** (serve via ThreadingHTTPServer; if `load` still stalls, the durable fix = domcontentloaded+settle in the probe runner OR vendor THREE locally — a recommended small vetted milestone). **Bug-hunt** (23 Opus agents, 16 raw → 4 confirmed + 3 critic, 0 HIGH, all LATENT@R-6): all 7 fixed (combined-speed clamp; cmdBonus surge gated to non-speed [one channel/badge]; apply decoupled from officers; vector bolt; grand_charge prov→Inferred + dual-downside deferred-note); catalog-wide badge-provenance sweep logged for R-6.
>
> **NEXT = Q7 — the GM franchise layer** (RATING-SYSTEM-DESIGN §12–§14): the depth-chart roster (Army→Corps→Division→Brigade billets, each showing OVR+grade+badges + roll-up OVR) · roster moves (appoint/relieve/promote/transfer/commission) spending **political capital** (`C.clock.capital`, extends `cmdAppoint`/`_cmdReliefCost`) · the **dual Attack/Defend OVR** (`fldDualOVR` already exists — surface it) · the **pre-battle matchup screen** (corps-vs-corps OVR board, a labeled predicted-edge bar; pure display over numbers the sim already reads). Then Q8 camp loop → R-5 prosopography scale-out → R-6 negative-badge sweep + the badge-provenance sweep + live dev-traits → the E strategic arc → Phase F/I/H/J → **Q5 Chattanooga + Q6 USCT battles LAST** (Aaron's queue order). **Resume map:** START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §12 (the depth chart) + §13 (dual OVR + matchup screen) + §14.3 (the political-capital economy numbers) + §15 the GM-layer answers → DECISIONS D98/D97/D94 → `src/35-command.js` (`cmdAppoint`/`cmdRevert`/`_cmdReliefCost`/`_cmdGenRow` — the GM spine to generalize) + `src/tactical/T14-ratings.js` (`fldDualOVR`/`fldUnitRatingOVR`/`fldRatingGrade`) + `data/ratings.json` (`generalPersonas` + `dualOvrTilt`). Build R-4's siblings (Q7→Q8) on the now-complete badge+X-Factor engine.

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM R-3 — the badge engine (the first rating→combat seam) + the no-fudge build-gate** (overnight run, D97)

> **The first place a rating TOUCHES combat is shipped.** A guarded one-token factor seeds the levers the engine already reads; **byte-identical when off** (proven: sandbox + all 9 battles produce IDENTICAL deterministic results on the R-3 build vs the committed R-2 build).
>
> **`fldBadgeFactor(u, lever)` (`src/tactical/T14-ratings.js`):** guarded MULTIPLICATIVE one-token factor — identity 1.0 when `__FIELD.badges` off / no matching badge. Sums the unit's badges' signed `mag`s for a lever, gates by trigger (`_fldBadgeTrig`), CLAMPS the sum to the realism-scaled per-lever cap (`fldRatingRealismCap(tier,"badgeLever")` — arcade .16 / balanced .10 / historian .06). Negatives DAMP (≥1−cap), never enemy buffs. Plus `fldUnitCohesion` (rally term, 0 when unauthored).
>
> **Four guarded seams (`src/tactical/T0-field-sandbox.js`):** fire (`fldResolveFire`), rally+cohesion (`fldMoraleStep`), melee (`fldResolveMelee`), speed (`fldMoveFactor` — `_spdMul` reserved for the R-4 X-Factor surge). `__FIELD.badges` gate (default ON, `_badgesOff` hook) + `__FIELD.realismTier` (`T6 fldPresetsApply`).
>
> **Badge data (`data/ratings.json`):** 25 badgeDefs gain `fldLever` + signed `mag`. Ships Star/Superstar positives + the Verified negatives with real bite (McClellan Slows / Bragg Piecemeal / Burnside Rigid / Green Levies / Powder-Shy). **NOT yet assigned to shipped rosters** (that's the R-6 sweep) → battles byte-identical.
>
> **No-fudge OUTPUT-WALL build-gate (`tools/build.mjs` gate 4d):** scans the rating module for any `cas`/`aCas`/`bCas`/`.victory`/`.men`/`sev.*` write — **dot AND bracket notation** — build FAILS (exit 5) if found. Verified firing on planted `u.men=0`, `u["men"]=0`, `F.sev["attrition"]=2`; clean build prints `GATE OK · … · no-fudge ✓`.
>
> **⚠ VETTING — environmental deviation (READ before re-vetting):** the standard `load`-based probe suite was **blocked** this session — headless Chrome's `load` event never fires for the game page (the THREE-CDN `<script>` stalls in headless; it breaks the committed-green R-2 build identically; a trivial page fires `load` in 73ms; `domcontentloaded` loads everything incl. THREE in ~1.5s). Contributors: the single-threaded `python -m http.server` chokes on the page's ~170 local asset requests (**use a ThreadingHTTPServer**), + the THREE-CDN `load` stall (documented gotcha). R-3 was vetted via a **`domcontentloaded` byte-identity harness** (`.tmp/r3-verify.mjs`): sandbox + all 9 battles deterministic, R-3 build vs committed R-2 → **IDENTICAL** for all 10, + the full badge-logic assertions, 0 pageerrors. Bug-hunt (4 Opus lenses × verify + critic): 2 confirmed (gate-4d bracket bypass → fixed; phantom `sharpshooters` key → fixed). **NEXT SESSION: re-run the standard `load` suite (serve with `python3 -c "from http.server import SimpleHTTPRequestHandler,ThreadingHTTPServer; ThreadingHTTPServer(('',8765),SimpleHTTPRequestHandler).serve_forever()"`); if `load` still stalls, the fix is a domcontentloaded+settle wait in the probe runner or vendoring THREE locally.**
>
> **NEXT = R-4** (the X-FACTORS — the dramatic Madden surge): `u._xfActive` activation ("in the zone") that scales the **already-summed** `cmdBonus` aura, hard-capped by `fldRatingRealismCap(tier,"xfactor")` (never breaching `CMD_BONUS_CAP`); `abZap`/`fldAnnounce` one-shot + HUD glow; ship Foot Cavalry, Rock of Chickamauga, Bayonet!, First with the Most, Earned in Blood, The Slows. **The no-fudge replay gate:** deterministic seed-replay shows badges-on final casualties differ from badges-off only within the bounded lever delta; X-Factor never exceeds `CMD_BONUS_CAP`. Resume map: START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §4 (the X-Factor rows) + §6 (the `u._xfActive` seam: "scales the already-summed cmdBonus, clamped at CMD_BONUS_CAP") + §9 (R-4 asserts) → DECISIONS D97/D96/D95/D94* → `src/tactical/T14-ratings.js` (fldBadgeFactor + the realism caps) + `src/tactical/T3-officers.js` (`fldOfficersStep` — the cmdBonus aura sum, where `_xfActive` scales) + `src/tactical/T0-field-sandbox.js` (`fldAnnounce`/HUD glow) + `tools/probe-ratings.mjs`.

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM R-2 — the OVR read-out UI (Command-desk A–F grades + tactical HUD brigade OVR)** (overnight run, D96)

> **The Madden layer is now VISIBLE.** R-2 surfaces the rating numbers the R-1 spine already produces, graded **A–F**, across two surfaces — **PURE DISPLAY, no sim change** (full no-regression confirms all 9 battles byte-identical).
>
> **Command desk (`src/35-command.js`):** the active-general card headline shows the **officer OVR** (`round(_cmdGenRating)` — A+ ≥90 reachable, unlike the 42–88-clamped `commandLeadership`) + the **A–F grade letter** + the word, band colour as a decorative left-border accent, with the bridge value kept as a secondary "Fields command at NN" line; the appoint-pool rows show a colour dot + bold A–F letter + word + "(NN OVR)".
>
> **Tactical HUD (`src/tactical/T14-ratings.js` `fldRatingHudSelected` → `T0` `fldRenderHud`):** the selected brigade's computed OVR (`fldUnitRatingOVR`, pure read) + A–F grade, as a new guarded HUD line (byte-identical when ratings data absent).
>
> **WCAG 2.2 AA + CVD-safe:** triple-encoded everywhere — **number + letter-grade + word in high-contrast `--parch` text; colour DECORATIVE only** (aria-hidden stripes/dots). `wcag-auditor` PASSED all three new surfaces; two pre-existing sub-AA secondary labels lifted `--rule #8a7350 → #b3925e` (5.0:1 measured against the **real** `.sheet` gradient, not pure black — the bug-hunt caught the auditor's first #8d7551 being computed against #000 and still failing).
>
> **Vet:** GATE OK; `probe-ratings` 14/14 + `probe-command` 16/16 (new R-2 UI steps); FULL no-regression GREEN (28 probes byte-identical); diag-classic `nonBlank:346`; 0 pageerrors. Bug-hunt (3 Opus lenses × refute-verify + critic): 2 confirmed (false-AA contrast → fixed/honestly-measured) + 2 critic (a `/* */` audit comment leaking into the rendered DOM → moved out of the JS string, DOM-leak signature now 0; `gr.word`/`g.word` unescaped → escaped, defense-in-depth). All fixed + re-vetted.
>
> **NEXT = R-3** (the BADGE ENGINE — the first rating→combat seam): a guarded `fldBadgeFactor(u, lever)` (identity 1.0/0 when `__FIELD.badges` absent, mirroring `(u.cmdBonus||0)`) + the `u._spdMul` march seam + the `cohesion` rally extension in `fldMoraleStep`; the **global per-unit per-lever stacking cap** (realism-scaled via `fldRatingRealismCap`); the **no-fudge build-gate assertion** (no badge handler may write `cas`/`aCas`/`bCas`/victory/`tgt.men` or out-of-band `sev.*`); ship Star/Superstar + the Verified negatives (Bragg Piecemeal / McClellan Slows / Burnside Rigid / Green Levies / Powder-Shy). **Badges-off byte-identical; badges-on bounded + reversed by removing data.** Resume map: START-HERE → this block → `RATING-SYSTEM-DESIGN.md` §4 (the badge catalog) + §6 (engine wiring, the exact seam table) + §9 (R-3 probe asserts) → DECISIONS D96/D95/D94* → `src/tactical/T14-ratings.js` (`fldBadgeDef`/`fldRatingRealismCap` already defined) + `data/ratings.json` (`badgeDefs`/`badgeRungs`/`realismCaps`) + `src/tactical/T0-field-sandbox.js` (`fldResolveFire`/`fldMoraleStep`/`fldMoveFactor` — the `xpF`/rally/move lines to add the one-token factor) + `src/tactical/T3-officers.js` (`cmdBonus` aura) + `tools/probe-ratings.mjs`.

---

## ⚡ CONTINUE HERE — 2026-06-22 **RATING SYSTEM R-1 — officer derivation wired into the command + field pipes (the persona is now the source of truth)** (overnight run, D95)

> **The first WIRING increment of the D94 Madden layer is SHIPPED + vetted + pushed** (R-0 shipped the pure-function data spine on 2026-06-21; this is R-1). A documented persona now SEEDS the two pipes the engine already reads — strategic `gen.skill` and tactical officer `quality`/`radius`/`fate` — with the **no-fudge keystone intact** (the persona only sets INPUTS at build/make time; nothing is ever read at casualty/winner/rout time) and **every unrated entity byte-identical**.
>
> **Strategic** (`src/35-command.js`): `_cmdGenRating` now derives a general's `skill` from a linked persona via new `_cmdGenPersona`/`_cmdEffectiveSkill`; `cmdActiveTraits.skill` + the Command-desk Skill bar route through the same fn (ONE source of truth). The **9 strategic personas** (`generalPersonas` in `data/ratings.json`, keyed by the `generals.json` id — **CS** Lee/Jackson/A.S. Johnston/Bragg/Longstreet · **US** McClellan/Grant/Burnside/Meade, the principal commanders of the 9 D92-hardened battles) are calibrated so `round(0.45*command+0.35*tactical+0.20*discipline) === gen.skill EXACTLY` → commandLeadership/auto-resolve/conditioning/appoint-pool words stay **byte-identical** while the persona is the source.
>
> **Tactical** (`src/tactical/T3-officers.js`): `fldMakeOfficer` derives a field leader's `quality` (calibrated, aura shifts ≤0.005), `radius` (authored echelon + charisma nudge, clamped 160–290), `fate` (persona durability) — but ONLY via an **explicit `pid` opt-in (no `id` fallback)**, the fix for a real cross-scenario id collision (`ld_jackson` is reused at Bull Run / Antietam / Chancellorsville). The 7 Bull Run leaders are opted in (`data/bullrun.json`); every other scenario stays byte-identical.
>
> **Vet:** GATE OK; `probe-ratings` 13/13 (+3 R-1 steps incl. a collision guard), `probe-command` 15/15 (+source-of-truth guard); FULL no-regression GREEN (all 9 battles byte-identical incl. **chancellorsville 24/24** = collision fix proven, + roster/custom/fog/ai/autopause/campaign-link/logistics/arms/presets/csplayer/officers/field/engineering-corps/tactical-visuals); diag-classic `nonBlank:346`; 0 pageerrors. Adversarial bug-hunt (5 Opus lenses × refute-verify + critic): 0 confirmed finder findings, 1 LOW critic finding (single-source-of-truth) FIXED.
>
> **NEXT = R-2** (the OVR read-out UI: OVR + word + **A–F report-card grade** on the Command desk + a unit-inspect line; pure display, WCAG/CVD triple-encode — number+word+glyph, color decorative). Then R-3 badge engine → R-4 X-Factors → Q7 GM layer → Q8 camp loop → the E arc → Phase F/I/H/J pillars → **Chattanooga + USCT battles LAST** (Aaron's queue order, §2 of `OVERNIGHT-RUN-PROMPT.md`). Resume map: START-HERE → this block → `RATING-SYSTEM-DESIGN.md` (§3 bands + §10 the WCAG/CVD badge+tier UI for R-2) → DECISIONS D95/D94* → `src/35-command.js` (`_cmdActiveCard`/`_cmdGenRow` to surface OVR+grade) + `src/tactical/T14-ratings.js` (`fldRatingGrade`/`fldDualOVR`/`fldPersonaOVR`) + `data/ratings.json` (`generalPersonas`) + `tools/probe-ratings.mjs`.

---

## ⚡ ARCHIVED — 2026-06-21 **ROSTER HISTORY-ACCURACY HARDENING — all 9 shipped battles audited; 15 HIGH wrong-fact corrections** (Aaron-directed; the EA-Sports-roster groundwork) — D92

> **Aaron asked whether the full Chattanooga-grade rigor had been applied to the already-built battles. It had NOT for the 7 pre-Opus battles** (the Codex/early-built C1 Eastern marquee + Shiloh + Bull Run); only Vicksburg + Chickamauga had the Opus adversarial history pass. An adversarial audit (9 Opus historians + critic + the new consolidated `tools/sweep-all-battles.mjs` casualty-direction diagnostic) found **15 HIGH wrong facts shipped under "Verified" stamps** — mostly the **rank-at-the-battle** class — ALL FIXED + annotated in-file: **Malvern** (Hunt→Col., Jackson→Maj.Gen., Porter→Brig.Gen., Hooker→Maj.Gen.); **Chancellorsville** (Rodes→Brig.Gen., Berry→III Corps, Stonewall Bde→Colston's div, **dead officer Hazzard→Best**); **Shiloh** (Breckinridge→Brig.Gen., Bragg→RIGHT wing, Wallace died Apr 10); **Gettysburg** (Doubleday & Early→Maj.Gen., Stannard→I Corps); **Vicksburg** (M.L. Smith→Brig.Gen.). Plus ~12 medium/low. **Surfaced, NOT auto-fixed (genuine source conflict):** Bull Run Heintzelman + Antietam Mansfield.
>
> **LOCKED PRINCIPLE (Aaron, the input/output distinction — obey it):** outcomes emerge from **skill + weapon type + terrain + tactics** via the D74 universal model; history enters ONLY as the **default equipment** (accurate counts/guns/weapons/terrain/doctrine) so the **probable** result lands near history *because the inputs are true* — **NEVER an output gate forcing a casualty direction/winner.** DROPPED the planned Antietam balance refight + hard historical-direction probe gates; `sweep-all-battles.mjs` is a DIAGNOSTIC only; kept guards are **system validations** (cover↓casualties, rifled out-ranges smoothbore, more guns=more fire) that keep the game legible. (Memory `civilwar-roster-accuracy-principle`; DECISIONS **D92**.)
>
> **NEW PILLAR captured + scheduled (D93, Aaron): "The Soldier's Story"** — an EA-Sports-style prosopography DB (bio/stats/history/portrait/team for EVERY identifiable individual) + a **play-as-anyone whole-war journey mode** (pick any person, choose when+where to start, proceed from their **real rank/position**). **Build AFTER the C+E roadmap**, unified with the D91 loot/survival journey mode + interpersonal/romance. The D92 rank/position hardening is its first roster data (hard prerequisite for "start from real rank"). Granularity = exhaustive ("every named individual found").
>
> **Verification:** GATE OK; all 9 battle probes green (each battle's balance assertions still green → outcomes byte-preserved), roster 8/8, custom-builder 9/9, broad tactical-systems suite + boot/t1 green, diag-classic `nonBlank:346`, 0 pageerrors. Edits are labels/notes only except Antietam Benjamin 6→4 guns (accurate) + Shiloh Wood→Chalmers (stats kept).
>
> **⚡ COMMITTED 2026-06-21 (resumed session — the prior one died on a 529 API error mid-D92 with the work uncommitted).** Before committing, ran an **independent adversarial re-verification** (Opus historian per battle + critic) to catch any D92 "fix" that was itself wrong → found + fixed **4 more** (see DECISIONS *D92-verify*): Bull Run Elzey gloss (his own regiment = the 1st Maryland, not the Virginia regts); **Shiloh Ammen → Col.** (was wrongly Brig. Gen.; BG dated Jul 16 1862, after the battle); **Antietam Battery B `rifled→smooth`** (six 12-pdr Napoleons — its note already said "double canister"; antietam probe stayed 16/16, outcome-neutral); **Malvern Hill Hooker reverted Maj.Gen.→Brig.Gen.** (D92 over-promoted him by date-of-rank, but his MG commission *issued* Jul 26 1862 after the battle — the Seven Days OOB lists him BG, like Porter/Couch/Kearny). Re-vet GREEN, pushed to `adhanke-star/History-Video-Game` main.
>
> **NEXT (unchanged): CHATTANOOGA** — the US-offensive reversal (Orchard Knob → Lookout Mountain → Missionary Ridge; brief `.tmp/chattanooga-design.md`, verified fact sheet `.tmp/chattanooga-factsheet.json`), then **C3 USCT battles**, then the **E strategic arc**, then the Soldier's Story pillar.

---

## ⚡ CONTINUE HERE — 2026-06-21 **CHICKAMAUGA — the 3-phase Western epic shipped + adversarially hardened** (the woods → Longstreet's breakthrough → Thomas at Snodgrass Hill; probe 18/18) — D90

> **Chickamauga is playable and verified — the bloodiest battle of the Western theater, the fourth multi-phase epic, built on the T8 engine.** A new **⚔ BATTLE — CHICKAMAUGA (1863)** main-menu button (rank 60, after Vicksburg) opens the side-choice card; command Bragg/Longstreet's army or Thomas's defense across three sectors in sequence: **P0 The Battle in the Woods (Sep 19)** — a confused meeting engagement in blind timber; the Union line holds the road (historical). **P1 Longstreet's Breakthrough (Sep 20)** — the Wood gap order pulls a US division out of line at the moment Longstreet's column strikes the Brotherton farm; the CS pours through and routs the right (the decisive **scoreWeight-3** phase). **P2 The Rock of Chickamauga** — Thomas holds Snodgrass Hill / Horseshoe Ridge until dusk, reinforced by Granger/Steedman, saving the army. Anti-Lost-Cause: **contingency over genius** (a staff blunder + a timing coincidence, not martial superiority) and **the squandered victory** (Bragg fails to pursue → the siege of Chattanooga → the November reversal).
>
> **Framing (decide-&-log, no fork):** the C2 "Chickamauga/Chattanooga" item = a matched PAIR of separate 3-phase epics (the Antietam/Gettysburg precedent), each its own milestone. **Top-level `attacker: CS` / `defender: US` across all 3 phases** (the Army of the Cumberland fought on the defensive) — the CS player is the offensive, the US player holds. This sets **Chattanooga up as the US-offensive MIRROR** (the next milestone; design brief at `.tmp/chattanooga-design.md`, which also banks the engine-grain lessons from this build). Universal gun model only; **no per-battle damage fudge** (D74).
>
> **Balance (AI-vs-AI, 32 seeds, deterministic):** P0 US-holds 32/32 (mutual bloodbath ~2,400 each); P1 **CS breakthrough 32/32** (US routed cheaply, US:CS ~5.1); P2 **US-holds 32/32**, CS attacker pays **CS:US ~3.0** (the Fredericksburg failed-assault lesson); **aggregate CS 32/32** (CS 3 / US 2). A human US player who closes the Wood gap in P1 **overturns** the battle (the alt-history hinge). The CS won the field yet **bled MORE in aggregate** (~70k vs ~63k / 8 seeds) — the costly-victory teaching, probe-guarded.
>
> **Research+verify (Workflow) corrected before authoring:** Hood Sep 20 / Maj. Gen. (not Sep 19 / Lt. Gen.); Helm under Breckinridge (not Cleburne); Cheatham restored; Granger & W.H.T. Walker = Maj. Gen.; Loring removed; gap-reporter flagged (Kellogg vs Bond); "River of Death" shipped only as disputed legend.
>
> **Bug-hunt (10 Opus agents): 2 confirmed + 3 critic, all fixed.** HIGH (the Vicksburg wrong-rank-under-Verified class): **Charles Harker was a COLONEL at Chickamauga, not "Brig. Gen."** (like Hans Heg; the BG dated to the battle but came after) → corrected to **Col. Harker** + a fact-check guard. Plus 4 probe-rigor guards (P1 casualty-direction US:CS ≥2.5, P0 winner ≥7/8, P1 win-by-SEIZE "hold", aggregate winner-bled-more).
>
> **Verification:** GATE OK; `probe-chickamauga` **18/18**, 0 pe; full no-regression GREEN (all 8 battle baselines + roster 8 + field/officers/logistics/arms/presets/csplayer/campaign-link/fog/ai/autopause + **custom-builder 9** [EXPECTED baseline updated for `chickamauga` — the D86/D88 gotcha] + engineering-corps 22 + **tactical-visuals 10 scenes incl. live Chickamauga 2D/3D, texWarn 0**; diag-classic nonBlank 346; boot/t1 ok; 0 pageerrors); WCAG inherited-AA (no new color CSS). Pushed to `adhanke-star/History-Video-Game` main (D90).
>
> **Next:** **CHATTANOOGA** (the US-offensive reversal — Orchard Knob → Lookout Mountain "Battle Above the Clouds" → Missionary Ridge; build it from `.tmp/chattanooga-design.md`, which has the framing + the engine-grain lessons), then **C3 USCT battles** (the Crater · New Market Heights · Olustee · Nashville). **Resume map:** START-HERE.md → this block → WAKE-UP.md top block → newest RUN-LOG.md entry → DECISIONS.md D90 → `.tmp/chattanooga-design.md` → `data/chickamauga.json` (the template) + `src/tactical/T1-bull-run.js` (registry/rank) + `src/tactical/T8-phases.js` (multi-phase engine) + `tools/probe-chickamauga.mjs` / `tools/sweep-chickamauga.mjs` (probe/sweep templates).

---

## ⚡ CONTINUE HERE — 2026-06-21 **ENGINEERING CORPS — increment 3: PONTOON BRIDGING shipped + hardened → THE CORPS IS COMPLETE** (the pontoon train; player-only/byte-identical; probe 22/22) — D89

> **The Tactical Engineering Corps is DONE — all 3 of Aaron's locked "all of the above" (entrench + abatis + pontoons).** A NEW **opt-in river** (`__FIELD.terrain.rivers[]`) actually gates movement: a river is impassable except at a **crossing** — a **shallow ford** (passable, slow + fatiguing), a **deep ford** (fordable only at Arcade; needs a bridge at Balanced/Historian), or a player-laid **pontoon bridge** (key **N** / "Pontoon" button): an infantry pioneer detail on the near bank lays a bridge over realism-scaled time, opening a fast crossing. Surfaced via the **Skirmish → Ground → "River crossing"** option (+ an `opts.river` sandbox flag for the probe). The **B-5 realism slider** scales sapper speed, ford slow, AND fordable-vs-requires-pontoon; the strategic **Engineer Works Corps** (`bridgeArmy.engineering`) lays bridges faster on a campaign launch (the A1 anchor, via `__FIELD.engCorpsSpeed`). 2D slate-water band + boat-deck pontoons + ford stepping-stones + labels (CVD-safe, reduceMotion-safe); 3D water plane + pontoon boats/deck; a citation-grade Fredericksburg/Rappahannock + James-River-1864 teaching card.
>
> **Architecture (byte-identical):** the active gate REUSES the two T0 seams reserved for it — `fldEngMoveFactor` (ford slow) + `fldEngMoveGate` (returns a clamped bank point for an illegal water step). New T13 fns: `fldEngInstallRiver`, `fldEngCrossAt`, `_fldRiverNearest/_fldRiverBankPoint/_fldEngBridgeSite`, `fldSelPontoon`, the pontoon loop in `fldEngStep`, `fldEngDrawWater2d`, `fld3dBuildWater` + pontoon sync in `fld3dSyncEng`. **No shipped scenario declares `terrain.rivers` and the AI never lays a pontoon** → every AI-vs-AI baseline byte-for-byte unchanged (every read gated by `rv && rv.length` / `engPontoons`; reset per-launch + per-phase). One T0 seam edit: `fldStepMovement` now settles a no-progress river clamp to HOLD (the bug-hunt's soft-deadlock fix) — `eg`-gated, so riverless baselines are identical.
>
> **DECISIONS (decide-&-log, no fork surfaced):** Bull Run's creek/Stone Bridge stay COSMETIC (making them live would regress a shipped, probed battle + break byte-identity); the active river is the opt-in feature, and the teaching card cites the Rappahannock as the historical anchor. AI river-pathing / AI-built works is the named T13 extension point (player-tool this increment, like D87/D88), so a river skirmish frames the PLAYER as the attacker forcing the crossing (river installed between the player's home edge and the objective, either side) while the AI defends the far bank.
>
> **Bug-hunt (19 agents, Opus): 8 confirmed + 6 critic, all fixed.** HIGH: the gate's origin test let a unit on an open ford **sidestep into deep water and wade the band** → block entering blocked water from anywhere except already-blocked water + a midpoint sample (tangential bends). HIGH: the **3D water plane was buried under the opaque terrain** → raised to a thin on-ground sheet (+ fords lifted, no z-fight). HIGH: a unit ordered across impassable water **marched in place forever, fatigue→100** → settle to HOLD at the bank. HIGH: the river skirmish was degenerate for a CS player → river placed by player side. MED: silent crossing refusal → aria-live halt announce + HUD line. LOW: dupe-span message; pontoon `side` + per-side cap.
>
> **Verification:** build GATE OK; `probe-engineering-corps` **22/22**, 0 pe; full no-regression **GREEN (21 probes byte-identical: field 13 · bullrun 15 · shiloh 29 · malvern 24 · antietam 16 · fredericksburg 22 · chancellorsville 24 · gettysburg 16 · vicksburg 18 · roster 8 · visuals ok [texWarn 0] · custom 9 · fog 13 · ai 15 · autopause 13 · campaign-link 16 · officers 15 · logistics 14 · arms 23 · presets 26 · csplayer 16; diag-classic `nonBlank:346`; boot/t1 ok; 0 pageerrors)**; WCAG-AA on all new UI (≥4.5:1, CVD-safe). Also added the long-missing B/X/N engineering keys to the help overlay.
>
> **Next (the Engineering Corps is complete):** Phase C tactical BREADTH — **Chickamauga / Chattanooga** (the Western theater continues after Shiloh + Vicksburg), then **C3 USCT battles** (the Crater · New Market Heights · Olustee · Nashville). Then D hex engine → E strategic S3–S5. **Resume map:** START-HERE.md → this block → WAKE-UP.md top block → newest RUN-LOG.md entry → DECISIONS.md D89 → `src/tactical/T13-engineering.js` (the INCREMENT 3 section: rivers + pontoons) + the T0 seams (grep `fldEng`/`rivers`/`engPontoons`) → `tools/probe-engineering-corps.mjs`.

---

## ⚡ CONTINUE HERE — 2026-06-21 **ENGINEERING CORPS — increment 2: ABATIS / OBSTACLES shipped + hardened** (the axe; player-only/byte-identical; probe 15/15) — D88

> **The Engineering Corps is 2 of 3 increments done.** A selected player infantry brigade now builds an **abatis** — a capped timber belt across its front (key **B** / "Abatis" button); enemies forcing it are slowed, disordered once per crossing (morale + fatigue), and fire raggedly until they recover; a nearby brigade **clears** a lane (key **X**). Build/clear time, slow, and disorder all read the **B-5 realism slider** (`fldEngRealism()`). 2D stake-line + 3D crossed-timber (fog-gated), HUD lines, and a Mahan-sourced teaching end-card. **Player-only → every AI-vs-AI baseline is byte-identical.**
>
> **Provenance:** the abatis code was authored 2026-06-20 but left uncommitted/unvetted when the prior **Codex** session ran out of usage (its only landed work was the browser-probe Seatbelt guard — committed separately as `679ee7d`). This session vetted it through the full gate + bug-hunt and pushed it (`02b2dd8`).
>
> **Architecture:** the abatis half of `src/tactical/T13-engineering.js` + guarded T0 seams (`fldMoveFactor` gains `u`, `fldResolveFire` fire factor, `fldStepMovement` move-gate, `fldInitSim` reset, `fldOnOver` end-card, DOM buttons + keys B/X) + a **T8 `_fldBuildPhase` per-phase reset** (`fldEngPhaseReset`). Every seam is an exact identity with no live belt.
>
> **Bug-hunt (16 agents, Opus): 8 confirmed + 4 critic, all fixed.** HIGH: belts leaked across phases in multi-phase battles (Vicksburg/Antietam/Gettysburg) → `fldEngPhaseReset` (keeps `_engUsed` for the end-card). MED: abandoned belts never decayed → fade like abandoned entrenchments. MED: render ignored fog → fog-gated 2D+3D. LOW: same-tick id collision → monotonic `_engObsSeq`. LOW: routing-unit morale dock → routing guard. **Kept (deliberate):** entrench+abatis on one brigade (historically correct — Fredericksburg/Cold Harbor apron) and one disorder shock per crossing.
>
> **Verification:** build GATE OK; `probe-engineering-corps` **15/15** (added: multi-phase reset, abandoned decay, unique ids, routing immunity); full no-regression **GREEN (50 probes, 0 pageerrors, diag-classic `nonBlank:346`)**. Also fixed a D86 leftover: `probe-custom-battle-builder` was red since Vicksburg shipped (EXPECTED registry omitted `vicksburg`) → 9/9.
>
> **Next (the LAST engineering increment):** **PONTOON bridging** — a NEW river/water terrain feature + a bridge-laying engineer step + a crossing gate (fordable vs requires-pontoon, realism-gated); the Bull Run terrain already has Young's Branch / the Stone Bridge to build against. Then C tactical breadth (Chickamauga/Chattanooga, C3 USCT). **Resume map:** START-HERE.md → this block → WAKE-UP.md top block → newest RUN-LOG.md entry → DECISIONS.md D88 → `src/tactical/T13-engineering.js` (header + the abatis fns) + the T0/T8 seams (grep `T13`/`fldEng`) → `tools/probe-engineering-corps.mjs`.

---

## ⚡ CONTINUE HERE — 2026-06-20 **ENGINEERING CORPS — increment 1: FIELD ENTRENCHMENTS shipped** (the spade; byte-identical; probe 8/8) — D87

> **The Tactical Engineering Corps has begun.** Aaron locked the full corps ("all of the above" — entrench + abatis + pontoons), so it ships as one module `src/tactical/T13-engineering.js` in vetted increments. Increment 1 = FIELD ENTRENCHMENTS: a player brigade ordered to Entrench (key **E** + control-bar button + help copy) digs in over ~70 sim-seconds for earned, facing-aware parapet cover; marching off abandons the works. Reads the B-5 realism slider (Arcade fast/×1.43, Balanced ×1.62, Historian slow/×1.81 — *later raised by H5-i4's staged works to shipped top-stage maxima ×1.53/×1.76/×1.99; annotation D236, ledger E01*). 2D dirt parapet + 3D dirt berm (low-tier aware, reduceMotion-safe).
>
> **Architecture:** 9 guarded byte-identical T0 seams (fldResolveFire/fldResolveMelee cover, fldSimStep step, fldKey E, control-bar button, fldRenderHud line, fld2dDraw + 3D build/sync hooks, fld3dDispose ref-nulling) + the T13 module + `tools/probe-engineering-corps.mjs` (8/8). Entrenchment is **player-only** this increment (no scenario/AI digs), and `fldEngCover` returns EXACTLY 1 for any non-entrenched unit — so every AI-vs-AI baseline is byte-for-byte unchanged.
>
> **Bug-hunt (17 agents):** 1 confirmed — cover was omnidirectional; fixed facing-aware (front full / flank half / rear none) so flanking counters works. Critics triaged: melee charger-exposed is the correct base convention; the multi-phase 3D works self-heal via fld3dSyncEng stale-disposal; the fog-ghost-works case is deferred to the AI/scenario-entrench increment (noted in the T13 header).
>
> **Verification:** build GATE OK; probe-engineering-corps 8/8; full no-regression suite byte-identical (field/bullrun/shiloh/fog/ai/autopause + all battle probes + officers/logistics/arms/presets/csplayer/campaign-link); tactical-visuals 8/8 (textureWarnings:0); diag-classic nonBlank 346; boot/t1 ok; 0 pageerrors.
>
> **Next (the rest of "all of the above"):** ABATIS / obstacles (a fldEngMoveFactor slow seam + cross-belt disorder step + engineer placement), then PONTOON bridging (a NEW river/water terrain feature + bridge-laying + crossing gate) — each its own vetted increment extending T13. The T13 module header lists the EXTENSION POINTS. **Resume map:** START-HERE.md → this block → WAKE-UP.md top block → newest RUN-LOG.md entry → DECISIONS.md D87 → src/tactical/T13-engineering.js (header) + the T0 seams (grep "T13"/"fldEng") → tools/probe-engineering-corps.mjs.

---

## ⚡ CONTINUE HERE — 2026-06-20 **VICKSBURG: the 3-phase river-fortress SIEGE shipped + adversarially hardened** (the last Codex project, finished) — D86

> **Vicksburg is the first SIEGE in the game and the last open Codex project, now finished and vetted.** Built as a three-phase battle on the T8 multi-phase engine — **Stockade Redan (May 19) → Forlorn Hope / Great Redoubt (May 22) → the Saps and the Mine vs the 3rd Louisiana Redan (June 25)**. New `data/vicksburg.json` + registry line in `src/tactical/T1-bull-run.js` (menu rank 55) + `tools/probe-vicksburg.mjs` (18 steps) + the new `tools/sweep-vicksburg.mjs` balance harness. Universal gun-count model only; no per-battle damage fudge. Anti-Lost-Cause: dug out, not starved out.
>
> **Finish work over the Codex draft:** (1) the readability probe's WebGL screenshot was switched from the hanging `page.screenshot()` to the proven render→`canvas.toDataURL()` capture; (2) `tools/probe-tactical-roster.mjs` was updated to guard `vicksburg` (the Codex left it out, so the menu-order step had been failing).
>
> **Adversarial bug-hunt (25 agents) confirmed + fixed:** the **Hebert→M.L. Smith** historical misattribution at the Stockade Redan (Hebert relocated to his real 3rd-Louisiana-Redan sector); the **inverted May-assault casualties** (the dug-in defender was bleeding more than the exposed attacker — rebalanced toward fire parity + the fortress's historical gun edge so the assaults now cost the attacker US:CS 1.93 / 2.64, the redans still hold 8/8, the siege wins 8/8, the city falls 8/8 with a clear US 3 / CS 2 margin); a new **casualty-direction probe gate** so it can't re-invert; the **T12 GPU leak** (per-sync materials/geometries now disposed via a `__M3D._t12Disp` registry), a **low-tier halo gate**, and a **WCAG dark-pill** behind the strength count.
>
> **Verification:** build GATE OK; `probe-vicksburg` 18/18, 0 pe (assaultCostUStoCS 1.96 / 2.71, totalGuns 56); `probe-tactical-roster` 8/8; bullrun 15/15; malvern-hill 24/24; antietam 16/16; fredericksburg 22/22; chancellorsville 24/24; gettysburg 16/16; shiloh 29/29; field 13/13; officers 15/15; logistics 14/14; arms 23/23; presets 26/26; csplayer 16/16; campaign-link 16/16; `probe-tactical-visuals` 8/8, 0 pe, `textureWarnings:0`; bootprobe ok (10 tolerated 404s); t1probe ok; diag-classic `nonBlank:346`; `git diff --check` clean. Sim engine T0–T8 untouched.
>
> **Known limitation (LOW, deferred):** passive (player-side, non-`autoBoth`) headless resolution is side-asymmetric — a demo-only artifact (real play has a human commanding). Noted for a future passive-AI pass.
>
> **Next Codex-owned task (non-battle):** the **Tactical Engineering Corps** on the field (pontoons / entrenchments / abatis / obstacle-clearing) interacting with the B-5 difficulty/realism sliders — Aaron's run-k directive (V1-CHECKLIST Phase F + B-5 pointer). Other open items: C3 USCT battles, S3 alt-history, S4 codex/glossary, the comprehensive WCAG pass, the full-campaign playthrough probe.
>
> **Resume map:** `START-HERE.md` → this block → `WAKE-UP.md` top block → newest `RUN-LOG.md` entry → `DECISIONS.md` D86 → `V1-CHECKLIST.md` Phase C/F → `data/vicksburg.json`, `src/tactical/T8-phases.js`, `src/tactical/T1-bull-run.js`, `src/tactical/T12-3d-readability.js`, `tools/probe-vicksburg.mjs`, `tools/sweep-vicksburg.mjs`.

---

## CONTINUE HERE — 2026-06-19 leftover gate closed + 3D objective beacon complete — C4/Malvern/Chancellorsville/custom-builder vetted, visual probe 8/8

> **Critical first job is complete.** Codex inspected the dirty working tree before starting new work. The uncommitted D77-D84-era C4/Malvern/Chancellorsville/custom-builder/roster/visual/help/docs state matched prior live docs, so it was treated as lower-model/session leftovers and brought to a vetted state rather than reverted. No unrelated user/project changes were reset.
>
> **One issue was found and fixed:** `tools/probe-gettysburg.mjs` had a probe-output bug where `sandboxPhases` was read after launching Antietam, so the printed summary misleadingly showed Antietam phase data for a single-objective Gettysburg assertion. The probe now captures Gettysburg's single-objective phase state before the Antietam control launch and reports `antietamPhases` separately. No gameplay behavior changed.
>
> **New Codex-owned graphics/readability pass:** `src/tactical/T0-field-sandbox.js` now builds a small procedural 3D `objectiveBeacon` at the scenario objective in real-time 3D. It uses Three.js primitives/materials only; no external assets, textures, browsing, downloads, accounts, or Blender. It does not change simulation, balance, AI, input, 2D rules, or Classic.
>
> **Visual probe expanded:** `tools/probe-tactical-visuals.mjs` now captures Shiloh, Malvern Hill, Gettysburg, and Chancellorsville in 2D and 3D (8 captures). Every 3D scene asserts the `objectiveBeacon` exists at the objective coordinates, while retaining the blank-canvas/pageerror/texture-warning gates. Current result: **8/8 ok**, 0 pageerrors, `textureWarnings:0`.
>
> **Verification:** `node tools/build.mjs` → GATE OK; `node tools/probe-custom-battle-builder.mjs` → 9/9; `node tools/probe-tactical-roster.mjs` → 8/8; `node tools/probe-chancellorsville.mjs` → 24/24; `node tools/probe-malvern-hill.mjs` → 24/24; `node tools/probe-shiloh.mjs` → 29/29; `node tools/probe-gettysburg.mjs` → 16/16; `node tools/probe-antietam.mjs` → 16/16; `node tools/probe-fredericksburg.mjs` → 22/22; `node tools/probe-bullrun.mjs` → 15/15; `node tools/probe-field.mjs` → 13/13; officers/logistics/arms/presets/csplayer/campaign-link all green; `node tools/probe-tactical-visuals.mjs` → 8/8; broad legacy strategy/tactical sweep (`ai`, `autopause`, `fog`, `auto-resolve`, `armory-field`, `blockade`, `bridge`, `cab-exploit`, `cabinet`, `cannon`, `command`, `conditioning`, `cover`, `decisions`, `desk`, `economy`, `engineering`, `manpower`, `morale`, `motion`, `portraits`, `press`, `production`, `targeting`, `victory`, `weapons`) all green; `node tools/bootprobe.mjs` → ok with known tolerated resource 404 console noise; `node tools/t1probe.mjs` → ok; `node tools/diag-classic.mjs` → Classic paints, `nonBlank:346`; `git diff --check` → clean; `node tools/build.mjs --check` → GATE OK.
>
> **Known residuals:** benign Chromium readback/performance warnings may appear in screenshot paths, and pre-existing resource/favIcon 404 console messages remain tolerated in boot/Classic artifacts. Blender is closed and should stay closed unless an explicit future Phase-H asset job truly requires it.
>
> **Resume map:** `START-HERE.md` → this block → `WAKE-UP.md` top block → newest `RUN-LOG.md` entry → `DECISIONS.md` D85-D82 → `V1-CHECKLIST.md` Phase C/H notes → `src/tactical/T0-field-sandbox.js`, `src/tactical/T1-bull-run.js`, `src/tactical/T11-custom-battle.js`, `tools/probe-tactical-visuals.mjs`, and the focused battle/custom probes. **Next Codex-owned task:** Phase C Western breadth, especially Vicksburg/siege architecture and probes. Secondary graphics follow-up: improve 3D unit label/selection readability now that objective location is clear.

---

## ⚡ CONTINUE HERE — 2026-06-18 Custom Battle Builder C4 complete ✅ — single-phase authoring, import/export, slots, launch gate green

> **Custom Battle Builder C4 is built and verified.** New `src/tactical/T11-custom-battle.js` adds a first-class main-menu builder flow for player-authored real-time tactical scenarios. It covers scenario name/date/place, sides/briefs, objective, field size, terrain hills/woods/walls/markers, OOB units, artillery/gun counts, reinforcement/deployment timing, optional leaders, supply trains, teaching cards, provenance, JSON import/export, shareable JSON, and six local persistence slots.
>
> **Architecture decision:** C4 is intentionally **single-phase V1**. Phase authoring remains deferred until a UI for `phases[]` can be proven as safe as authored historical phase data. Custom scenarios do **not** enter `fldScenarioRegistry()`; `fldScenarioData(id)` only falls back to `fldCustomScenarioData(id)` for explicit `custom_...` IDs. That preserves the exact historical battle roster while still launching valid custom scenarios through the same single-phase `fldScenarioInit` data path. Field size is reset every launch via `FLD.BASE_FIELD_W/H`, then a scenario may apply `field.w/h` safely.
>
> **Validation guardrails:** required metadata, safe unique IDs, malformed/unsafe JSON keys, duplicate IDs, bounds and numeric clamps, objective placement, reinforcement timing, side ownership, leader attach ownership, realistic artillery crew counts (12-40 per gun), forbidden combat/damage keys, and `phases[]` are all blocked. Custom artillery uses the universal `guns` model only; no per-custom-battle damage fudge was added.
>
> **Focused probe:** `tools/probe-custom-battle-builder.mjs` verifies builder render, validation failures, valid scenario creation, export/import round trip, UI malformed-import rejection, slot save/list/load/delete, explicit custom-ID launch contract, universal gun model, field-size reset, no phase leak, no Classic `G.battle`, and no historical registry contamination. Current result: **9/9 steps ok, 0 fail**, with `tools/shots/probe-custom-battle-builder.json/png`.
>
> **Verification:** `node tools/build.mjs` → GATE OK; `node tools/probe-custom-battle-builder.mjs` → 9/9; `node tools/probe-tactical-roster.mjs` → 8/8; `node tools/probe-field.mjs` → 13/13; `node tools/probe-bullrun.mjs` → 15/15; `node tools/probe-malvern-hill.mjs` → 24/24; `node tools/probe-antietam.mjs` → 16/16; `node tools/probe-fredericksburg.mjs` → 22/22; `node tools/probe-chancellorsville.mjs` → 24/24; `node tools/probe-gettysburg.mjs` → 16/16; `node tools/probe-shiloh.mjs` → 29/29; `node tools/probe-officers.mjs` → 15/15; `node tools/probe-logistics.mjs` → 14/14; `node tools/probe-arms.mjs` → 23/23; `node tools/probe-presets.mjs` → 26/26; `node tools/probe-csplayer.mjs` → 16/16; `node tools/probe-campaign-link.mjs` → 16/16; `node tools/probe-tactical-visuals.mjs` → 6/6; `node tools/bootprobe.mjs` → ok=true with known/tolerated 404 resource console entries; `node tools/t1probe.mjs` → ok=true; `node tools/diag-classic.mjs` → Classic paints, nonBlank 346; `git diff --check` → clean.
>
> **Docs/checklist status:** `RUN-LOG.md`, `HANDOFF.md`, `WAKE-UP.md`, `MASTER-TASK-LIST.md`, `V1-CHECKLIST.md`, and `DECISIONS.md` now mark C4 complete. Eastern marquee C1 remains complete; C2 Western still has Vicksburg, Chickamauga/Chattanooga, Atlanta/March, Franklin/Nashville; C3 USCT battles remain open.
>
> **Historical note:** this older block pointed to the retired `MASTER-TASK-LIST.md` and the D63 Codex-vs-DeepSeek/Cline routing. D145 supersedes it: `START-HERE.md` + `V1-CHECKLIST.md` are live, 5.5 owns architecture/history/UX/accessibility/final integration, and helpers only receive explicit model+effort bounded packets. Loot status remains explicit: Armory/Cannon have loot-style rarity tiers, but standalone Phase I loot/survival is not built.
>
> **Resume map:** `START-HERE.md` → this top block → `WAKE-UP.md` top block → newest `RUN-LOG.md` entry → `V1-CHECKLIST.md` Phase C → `DECISIONS.md` D84-D82 → `src/tactical/T11-custom-battle.js`, `src/tactical/T0-field-sandbox.js`, `src/tactical/T1-bull-run.js`, `tools/probe-custom-battle-builder.mjs`, `tools/probe-tactical-roster.mjs`, and the scenario probes. **Next Codex-owned task:** continue Phase C breadth with Western theater/Vicksburg architecture, C3 USCT scenario design, or another priority-picker item.

---

## ARCHIVED — 2026-06-18 Chancellorsville completion gate ✅ — superseded by the Malvern Hill block above

> **Tactical cohesion mini-pass complete.** The stale first-launch/help/tactical controls copy now matches the live real-time engine: current battle roster (First Bull Run, Antietam, Fredericksburg, Chancellorsville, Gettysburg, Shiloh, Skirmish), click/Shift-click/A selection, drag-to-place/facing, L/C/H/F/Enter orders, 1/2/3 = 1x/2x/4x, V/P/G/Tab/Esc. The in-battle tactical help dialog is now aria-modal, focusable, Tab-trapped to its close control, and Escape closes the dialog without also exiting the battle.
>
> **HUD declutter shipped:** the giant pause overlay is now a small top-right status chip; the tactical top bar is split into compact title/clock/phase/objective/status chips; multi-phase sector state moved into a dedicated `#fldSector` chip; objective text no longer repeats the huge phase label; reinforcement banners are narrower, shorter-lived, and ellipsized with a tooltip.
>
> **New visual probe + warning gate:** `tools/probe-tactical-visuals.mjs` launches the live `__FIELD` engine and captures Shiloh + Gettysburg in **2D and real-time 3D**: `tools/shots/visual-shiloh-2d.png`, `visual-shiloh-3d.png`, `visual-gettysburg-2d.png`, `visual-gettysburg-3d.png`, plus `tools/shots/probe-tactical-visuals.json`. Pixel probes confirm all four scenes render nonblank/readable with 0 pageerrors. The probe now FAILS if the known Three.js warning `THREE.WebGLRenderer: Texture marked for update but image is undefined` appears.
>
> **3D flag texture fix:** `src/tactical/T10-flags.js` replaced the immediate SVG `TextureLoader().load(svg)` + `tex.needsUpdate` flow with procedural cached canvas-backed `CanvasTexture` maps. Replaced flag mesh texture maps are disposed, and wrapper hooks dispose flag maps before unit rebuild and scene teardown. The generated `civil_war_generals.html` T10 block was aligned so browser probes exercise the fix. No gameplay simulation code changed.
>
> **Roster integration probe:** Codex added `tools/probe-tactical-roster.mjs` and made `fldScenarioMenuOrder()` explicit: First Bull Run → Antietam → Fredericksburg → Chancellorsville → Gettysburg → Shiloh, then future scenarios by full date. The probe verifies exact registry ids, data/menu/side-card contracts, single-vs-multi-phase shapes, gun-count artillery for post-Bull-Run battles, DOM button order/idempotency, both side-choice callbacks, and renderer-none launches for all six historical battles. No gameplay simulation code changed.
>
> **Chancellorsville finished after the interrupted run:** `data/chancellorsville.json` is registered in `src/tactical/T1-bull-run.js` and guarded by `tools/probe-chancellorsville.mjs`. The battle is a single-phase Jackson flank-march scenario with CS attacker / US defender, Chancellor crossroads objective, Wilderness/Hazel Grove/Fairview/breastworks terrain, gun-model artillery, leaders/fates, reinforcements, side cards, teaching/codex, deterministic AI-vs-AI resolution, CS-favored-but-not-scripted balance, and no Classic contamination.
>
> **Verification:** `node tools/build.mjs` → GATE OK; `node tools/probe-tactical-roster.mjs` → 8/8, 0 pageerrors; `node tools/probe-chancellorsville.mjs` → 24/24, 0 fail; `node tools/probe-bullrun.mjs` → 15/15, 0 pageerrors; `node tools/probe-shiloh.mjs` → 29/29, 0 pageerrors; `node tools/probe-gettysburg.mjs` → 16/16, 0 pageerrors; `node tools/probe-tactical-visuals.mjs` → 4/4 captures ok with clean per-scene pageerrors/console arrays; `node tools/diag-classic.mjs` → Classic paints, nonBlank 346. Current repo history already includes Gettysburg, Shiloh, T10 flags, T9 audio, portraits, and save/help weekend polish; docs below this block may still contain older milestone prose. Known residual console noise: benign Chromium `ReadPixels` warnings from the roster screenshot path and the pre-existing favicon 404 in roster/diag-classic.
>
> **Blender status:** Blender is closed and should stay closed for current code/browser/Three.js work. Reopen only for an explicit future Phase-H asset job. No current probe, flag fix, or tactical implementation depends on Blender.
>
> **Universal AI implementer standards:** any VS Code agent implementing this project must follow `START-HERE.md`'s Universal AI implementer standards and `AUTONOMOUS-RUN.md` §1A. Mandatory loop: repo-ground first, make small reversible edits, self-audit the diff and outputs, run the proper gates, learn from failures by updating repo-visible docs/probes, and report files/commands/results honestly. D145 routing applies: helpers may only run explicit model+effort bounded packets and must stop on ambiguity or any red gate.
>
> **Archived resume map:** this Chancellorsville-era block is preserved for history. It is superseded by the Malvern Hill completion block above and DECISIONS D82.
>
> **Archived prompt:** completed by the Malvern Hill/D82 pass above; do not reuse it as the current next task.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE C-2 · ANTIETAM — the FIRST MULTI-PHASE EPIC ✅ SHIPPED + PUSHED (bug-hunt to finish)** → NEXT = finish the Antietam bug-hunt, then DEMO-POLISH / graphics (Aaron's weekend demo)

> **Antietam — the bloodiest day, as a 3-phase epic — is built, vetted (except the bug-hunt), and pushed** (DECISIONS **D76**). A NEW gated engine capability (sequential phases in one scenario) + the citation-grade 3-phase scenario: **the Cornfield → the Sunken Road "Bloody Lane" → Burnside's Bridge**. New `src/tactical/T8-phases.js` + surgical T0/T1 seams + `data/antietam.json` + a registry line + `tools/probe-antietam.mjs`. Deliverable ≈**1814.7KB**.
>
> **✅ THE MULTI-PHASE ENGINE (T8, gated on `__FIELD.phases`):** a `data.phases[]` scenario routes (T1) to `_fldScenarioInitPhased`/`_fldBuildPhase`; the `fldCheckVictory` intercept (`_fldPhaseResolved`) records each phase + carries casualties/score forward + advances (headless auto-advances so `fldStepN` keeps stepping; UI shows a period-broadsheet inter-phase card); the last phase ends with the aggregate winner (`_fldBattleWinner`, weighted score + draw band). A per-phase `scenData` VIEW makes the officers (T3) + logistics (T4) layers build the phase cast/trains with ZERO change to those modules. **Byte-identical: every seam is `if (__FIELD.phases)` (null for non-phased)** — the full no-regression suite is byte-identical fresh (field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · presets 26 · csplayer 16 · arms 23 · fredericksburg 22 · boot ok · t1 14 · diag-classic 346).
>
> **✅ BALANCE (deterministic, 8 seeds):** historically faithful per-phase outcomes — Cornfield CS-held 8/8, Sunken Road US-carried 8/8, Burnside's Bridge CS-held 8/8 → aggregate **CS 2-1**; the end-screen teaches the tactical draw / strategic Union check / **the Emancipation Proclamation**. A human can rewrite any sector. probe-antietam **16/16, 0 pe**; wcag clean (4 fixes).
>
> **⚠ THE ONE OPEN GATE:** the **adversarial bug-hunt (7 lenses) was LAUNCHED but did not finish before commit** (committed under a time constraint to preserve the playable milestone). **FIRST NEXT-SESSION TASK: re-run the Antietam bug-hunt** (`.tmp/bughunt-antietam.json` if it landed, else re-launch the 7-lens workflow on T8 + the seams + `data/antietam.json`), fix any confirmed finding, re-probe (`probe-antietam` + the no-regression suite), and amend the commit / push a follow-up. Part A's identical hunt found 0 real bugs, so risk is low, but the gate is not formally closed.
>
> **What's playable:** a new main-menu **⚔ BATTLE — ANTIETAM (1862)** button opens a side-choice card, then plays the bloodiest day as three sequential battles — fight the Cornfield, then (after a period transition card showing the running tally + the next sector's history) the Sunken Road, then Burnside's Bridge; the cost carries forward and the end-screen scores the day phase-by-phase with the emancipation turning point. Command either army across all three. (Bull Run, Fredericksburg, Skirmish, Command & Realism unchanged.)
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` Phase C → `DECISIONS.md` **D76** (Antietam multi-phase) + **D75** (the gun model) + **D74** (the Phase-C clearing decisions) + **D73** (the data-driven scenario template) → the multi-phase engine in `src/tactical/T8-phases.js` (`_fldScenarioInitPhased`/`_fldBuildPhase`/`_fldPhaseResolved`/`_fldBattleWinner`/`_fldInterphaseCard`/`_fldPhasesEndHtml`) + the T0 seams (grep `__FIELD.phases`) + the T1 route/registry + `data/antietam.json` (the 3-phase template) + `tools/probe-antietam.mjs` (16/16) → the research dossier `.tmp/antietam-dossier.md` + the design `.tmp/B-antietam-multiphase-design.md`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE C-1.5 · THE UNIVERSAL ARTILLERY GUN MODEL ✅ SHIPPED + VETTED + PUSHED** → IMMEDIATE NEXT = ANTIETAM as the MULTI-PHASE epic (D74 #1/#2)

> **The D74 "universal-combat" artillery cleanup — the FIRST Antietam sub-milestone (do FIRST) — is complete** (DECISIONS **D75**). Fredericksburg's per-battle **"volume-of-fire proxy men"** fudge (the C-1/D73 batteries inflated `men` to 500/440/500 to fake the massed crest fire) is REPLACED by a CONSISTENT gun-count model used identically in every battle. Deliverable ≈**1749.6KB**.
>
> **✅ THE GUN MODEL (T5):** `FLDA.GUN_FIRE_WEIGHT` = 27 (men-equivalent fire-strength per gun, UNIVERSAL). `fldArtFireStr(u)` returns the fire-strength numerator (`fldResolveFire` divides by 1500): a battery with a GUN COUNT → `guns * 27 * clamp(men/maxMen)` (rated fire from the gun count, crew-scaled so a battery fades smoothly as cannoneers fall); infantry / arms-off / a LEGACY no-`guns` battery → `u.men` EXACTLY as before. `guns` carried data→unit via `fldMakeUnit` (T0) + `fldBrSpec` (T1); wired at `fldResolveFire`'s `strF`. CALIBRATED so a 6-gun battery → 162 ≈ Bull Run's realistic 160-men/6-gun Griffin & Ricketts → the model REPRODUCES the existing batteries (consistent, not merely gated). The gun count is army-size-invariant "hardware" (strategic strength flows through the Cannon-Corps `_canisterScale`, not raw volume — documented).
>
> **✅ BYTE-IDENTITY:** the gun path is reached ONLY by a battery declaring `guns` (Fredericksburg's 3 crest batteries); every baseline has none → `fldArtFireStr` returns `u.men` → byte-identical. **field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · presets 26 · csplayer 16 · arms 23** — all byte-identical, 0 pe; bootprobe ok; t1probe 14; diag-classic **346**.
>
> **✅ FREDERICKSBURG RE-TUNED VIA HONEST DESIGN (not fudge):** the 3 batteries now carry real gun counts (Washington Artillery 9 · Marye's grand battery 16 · Alexander 9 = **34 historical guns**) + realistic crew (200/350/200). The CS edge EMERGES from the wall's cover + the massed gun count + the cautious-attacker-dies-exposed geometry. RE-VET: **CS 8/8**, casualty ratio **2.06:1** (better than the proxy's 1.91), the wall measurably **load-bearing** (cuts CS loss 18-23%, 5/5 CS wins), the crest guns **survive 8/8** + fire — **probe-fredericksburg 22/22, 0 pe**. Bug-hunt: **0 confirmed finder bugs + 5 critic gaps ALL FIXED** (the `u.men0` probe typo → maxMen; a battery-survival guard; a crew-scaling assertion; the campaign-semantics + legacy-HUD notes). WCAG clean.
>
> **IMMEDIATE NEXT = ANTIETAM (Sep 17, 1862) as the MULTI-PHASE epic (D74 #1/#2).** Build the NEW gated `phases[]` engine capability (sequential phase objectives; default single-objective path = Fredericksburg/Bull Run byte-identical) → use it for the 3 phases: **the Cornfield → the Sunken Road "Bloody Lane" → Burnside's Bridge**. The citation-grade research **dossier is drafted** (`.tmp/antietam-dossier.md`, 8-agent workflow, anti-Lost-Cause, corrections applied) and the **engine design is drafted** (`.tmp/B-antietam-multiphase-design.md`: discrete sequential sub-battles, casualty+score carry-over, weighted-aggregate scoring with a draw band, the inter-phase broadsheet transition card). Author `data/antietam.json` (gun-model artillery) + one `fldScenarioRegistry` line + `tools/probe-antietam.mjs`. Standalone menu battle (D74 #4). **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` Phase C → `DECISIONS.md` **D75** (the gun model) + **D74** (the Phase-C clearing decisions) + **D73** (C-1 Fredericksburg, the data-driven template) → the gun model in `src/tactical/T5-arms.js` (`FLDA.GUN_FIRE_WEIGHT`/`fldArtFireStr`) + the `fldResolveFire` seam (T0) + the `guns` plumbing (`fldMakeUnit`/`fldBrSpec`) + `data/fredericksburg.json` (the de-fudged batteries) + `tools/probe-fredericksburg.mjs` (22/22) → the Part-B drafts in `.tmp/`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE C-1 · FREDERICKSBURG (Marye's Heights) ✅ SHIPPED + VETTED + PUSHED** → NEXT = the next Eastern marquee battle (Antietam or Gettysburg, D69)

> **C-1 — Fredericksburg, the SECOND data-driven tactical battle — is complete** (DECISIONS **D73**). The tactical engine (mature after B1–B6) is now BATTLE-AGNOSTIC: a new historical battle = a `data/<battle>.json` file + one registry line, NO new combat code. New `data/fredericksburg.json` + `tools/probe-fredericksburg.mjs` + the T1/T7 data-driven scenario-UI generalization + a small gated T0 engine seam. Deliverable ≈**1743.6KB**.
>
> **✅ C-1 — the data-driven scenario spine + Fredericksburg:** `fldScenarioRegistry()`/`fldScenarioData(id)` (T1) map any id → `GAME_DATA.<battle>`; `fldScenarioInit` builds ANY registered id from the same shape (Bull Run byte-identical). The UI is generalized: `fldInjectScenarioButtons` iterates the registry (Bull Run keeps its bespoke marquee button; others build from `data.menu`) and returns the last button so the skirmish/preset buttons anchor after the block; `fldLaunchBattle` fires the briefing for ANY scenario; `fldScenarioSideChoice(id,go)` reads `sd.sides` (a per-battle side-choice card; `fldBullRunSideChoice` is now an alias); `fldBriefObjectiveHtml` keeps the keyword skeleton + a data flavor (`sd.brief`); `fldScenarioEndHtml` keeps the Bull Run branch byte-identical (shared `_fldScenarioEndBox`) + a data `sd.endNote` branch. `bullrun.json` gained inert `sides`/`brief`.
>
> **✅ THE DOOMED-FRONTAL-ASSAULT POSTURE (the headline mechanic — a scenario-gated T0 seam):** the B-1 attacker AI is *competent* (fog-off it column-rushes + presses the mass bayonet), which Fredericksburg's assault was NOT. `data.assaultDoctrine:"cautious"` → `__FIELD._atkCautious` makes the AI attacker advance IN LINE across the open glacis and trade but never column-rush nor carry the wall (Burnside's piecemeal disaster). `var aggressive = !__FIELD.fog && !__FIELD._atkCautious` gates the assault/column branches → **default off → Bull Run/sandbox/skirmish/campaign BYTE-IDENTICAL.** A HUMAN Union player is unbound (the alt-history hook). Fredericksburg keeps **fog OFF** (the morning fog had LIFTED — clear fields of fire) and wins the defense by the cautious posture + the stone wall (cover 1.7) + massed crest canister — a DIFFERENT, battle-faithful lever from Bull Run's fog (no contradiction of "fog aids the defender").
>
> **VETTING CAUGHT A REAL HIGH (fixed pre-commit):** the bug-hunt (5 diverse-lens Opus finders × default-refute verify + a completeness critic → 4 confirmed + 5 critic gaps) empirically traced that `fldAiDefender` holds the objective's FORWARD FACE (`obj.z+obj.r*0.55`) — my first geometry put that forward of the wall, so the AI marched the defending line OFF the wall into the open on tick 1 (the 1.7 cover unused; the static-terrain probe missed it). **Fixed (the Bull Run pattern):** `obj.z` 375→331 (the forward face now lands ON the wall in cover) + `ATK_CAUTIOUS_HOLD` 0.6→0.85 (the cautious attacker holds in the OPEN just outside the wall's cover band — only the attacker exposed). Added a LIVE cover assertion to the probe. Also fixed: the side-card aria role hardwired US=attack → threaded the data `attacking` flag.
>
> **✅ FULLY VETTED:** GATE OK (1743.6KB) → **probe-fredericksburg 20/20, 0 pe** → full no-regression GREEN (field 13 · bullrun 15 · fog 13 · autopause 13 · ai 15 · campaign-link 16 · officers 15 · logistics 14 · arms 23 · presets 26 — all BYTE-IDENTICAL; **csplayer 16/16**; bootprobe ok; t1probe 14/14; diag-classic nonBlank **346**; strategic spot-check green) → balance A/B (**CS 8/8**; the slaughter **≈1.91:1** Union casualties; the wall load-bearing; the defending line holds the wall in cover live while the attacker dies exposed on the open glacis) → bug-hunt fixed + re-verified → wcag clean (0 fixes; 1 pre-existing global `.title-sub`/`--rule` contrast issue noted for the Phase-E WCAG pass). Committed + pushed.
>
> **What's playable:** a new main-menu **⚔ BATTLE — FREDERICKSBURG (1862)** button opens a side-choice card — lead the Union assault across the open glacis (and try to do what the Army of the Potomac could not), or hold the stone wall for the Confederacy as charge after charge breaks before it. The AI assault is the doomed frontal attack; the wall + the pre-sighted crest guns repel it. (The ⚔ FIRST BULL RUN button + the ⚔ SKIRMISH + ⚙ COMMAND & REALISM menus are unchanged.)
>
> **IMMEDIATE NEXT = ANTIETAM (Sep 17, 1862) as a MULTI-PHASE epic (DECISIONS D74).** Four Aaron-locked decisions clear the next runs: **(1)** next battle = **Antietam** (chronological; the Cornfield → the Sunken Road "Bloody Lane" → Burnside's Bridge); **(2)** scope = **multi-phase epics** — build a NEW gated engine capability (sequential phases/objectives in one scenario, a `phases[]` data block; default = the single-objective path → Fredericksburg/Bull Run byte-identical) and use it for Antietam's three phases; **(3)** the **UNIVERSAL-COMBAT BALANCE PRINCIPLE** — ONE shared, consistent per-unit damage model across EVERY battle (never tuned per-battle); outcomes EMERGE from scenario DESIGN (real OOB/terrain/cover/tactics); casualty ratios follow the player's/AI's tactics, not a forced historical number, but historical tactics → ~the historical result. **DO FIRST:** replace Fredericksburg's inflated artillery "volume-of-fire proxy men" (a per-battle fudge that violates D74) with a CONSISTENT gun-count/battery-weight artillery model, re-tune FB via geometry/OOB (not fudge), and RE-VET; **(4)** new battles stay **standalone menu battles** (no new campaign wiring). Then the rest of C1 → **C2 Western · C3 USCT · C4 the custom-battle builder**. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63): set model+effort EXPLICITLY — Sonnet low/med on mechanical legs (Explore/wcag/summarize), Opus high ONLY on bug-hunt finders/verifiers/critic.**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` Phase C (C1 partially ticked — Fredericksburg ✅) → `DECISIONS.md` **D73** (C-1) + **D69** (Phase-C order) + **D72** (B-6) + **D67** (fog-ON precedent) → the data-driven spine in `src/tactical/T1-bull-run.js` (`fldScenarioRegistry`/`fldScenarioData`/`fldScenarioInit`/`fldInjectScenarioButtons`/`fldScenarioEndHtml`) + `src/tactical/T7-command-side.js` (`fldScenarioSideChoice`/`fldBriefObjectiveHtml`) + the cautious-attacker seam in `src/tactical/T0-field-sandbox.js` (`fldAiAttacker` + `__FIELD._atkCautious` + `FLD.ATK_CAUTIOUS_HOLD`) + `data/fredericksburg.json` (the authored template) + `data/bullrun.json` (the reference) + `tools/probe-fredericksburg.mjs`. Everything is on disk and pushed. The B-6 CONTINUE-HERE block follows below.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE B-6 · CS-PLAYER "COMMAND EITHER SIDE" ✅ SHIPPED + VETTED + PUSHED — PHASE B COMPLETE** → NEXT = Phase C tactical breadth (Eastern marquee first)

> **PHASE B-6 — command EITHER army — is complete** (DECISIONS **D72**), and with it **Phase B (tactical depth) is DONE (B1–B6).** New module `src/tactical/T7-command-side.js` + surgical seams in T0/T1/T3 + `tools/probe-csplayer.mjs`. Deliverable ≈**1702KB**.
>
> **✅ B-6 — the CS-player tactical mode:** you can now command EITHER side in a real-time battle. Pick the Confederacy and you **DEFEND** Henry House Hill while the **B-1 attacker AI** presses you uphill — which is the whole point of fork #10 (it makes the B-1 doctrine player-facing). B-6 is a GENERALIZATION, not a new sim layer: `__FIELD.playerSide` is made authoritative in `fldInitSim` (precedence: explicit `opts.playerSide` > skirmish `playerSide` > a CS campaign via `_fldCamp().side` > `"US"`); the input layer (`fldPlayerSel`/select-all/cycle/drag) + the render/HUD **fog VIEWER** (2D markers, ghosts, 3D sync, friend/foe line) were a literal `"US"` → now read `fldPlayerSide()`; `fldBrSpec` (T1) flips the scenario AI flags off the player side; `fldPlayerSide()` (T3) resolves a CS **campaign LIVE** from `G.campaign.side` first, then the explicit per-launch side. New T7 UI: a period **side-choice card** (Lead the Union–ATTACK / Hold for the Confederacy–DEFEND), a side-aware briefing/objective + "your war vs history" + end-screen "you" line, a player-home-edge 3D camera, a dignified full-name friend/foe HUD ("4 Confederate vs 1 Union").
>
> **VETTING (the headline):** vetting caught a **real CS-campaign regression** I introduced — the first `fldPlayerSide()` rewrite trusted a stale `__FIELD.playerSide` and broke the officer-roster/fog contract (the roster/fog read it mid-setup, before a relaunch); `probe-officers` flagged it → fixed by resolving a CS campaign LIVE from `G.campaign.side`. Then the **52-agent bug-hunt → 11 confirmed + 7 critic gaps**: fixed **(HIGH)** a skirmish "Fight Again" soft-lock for a CS player (rematch now REPLAYS the stashed launch spec + the sandbox OOB assigns `ai` by playerSide), **(MED)** a US-hardwired 3D camera, **(MED)** a fog-leak via reinforcement banners/announce/auto-pause (now gated on player-visibility, fog-OFF byte-identical), **(MED)** a render-fog probe gap (added a deterministic step), + LOW polish; deferred-with-note the pre-existing/convention items.
>
> **✅ FULLY VETTED:** GATE OK (1702.3KB) → **probe-csplayer 16/16, 0 pe** → full no-regression GREEN (10 tactical baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346) → CS defense winnable 8/8 fog-ON + **live stacked still CS 8/8 (B-6 did not move the balance)** → wcag clean (2 contrast fixes). Committed + pushed.
>
> **What's playable:** the **⚔ FIRST BULL RUN** menu button now opens a side-choice card — lead the Union assault (history) or **hold the hill for the Confederacy** against the B-1 attacker AI; the ⚔ SKIRMISH side pick is fully wired; a CS campaign commands the CS. The CS defense is winnable, the field renders from your side, fog shows only what YOU scout, and the briefing/HUD/end-screen all speak from the army you command.
>
> **IMMEDIATE NEXT = PHASE C · TACTICAL BREADTH (D69):** more real-time battles on the data-driven engine, **Eastern marquee FIRST** — Antietam, Fredericksburg, Gettysburg, Chancellorsville (then Western C2, USCT C3, the custom-battle builder C4). The engine is mature (B1–B6); C is mostly new scenario DATA (OOBs + terrain + reinforcement timelines + teaching cards) on the proven T0–T7 stack, the bullrun1 pattern. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).** Also this session: the Gemini-report review (D71) — keep our engine/assistant/art choices; 3 Phase-H/publish notes folded in.
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1–B6 all ticked; Phase C next) → `DECISIONS.md` **D72** (B-6) + **D71** (the Gemini-report review) + **D70/D69/D67/D66/D65/D64/D62/D61** → `src/tactical/T7-command-side.js` (`fldBullRunSideChoice`/`fldBriefObjectiveHtml`/`fldPlayerOutcomeLine`/`_fldSideName`) + the seams (grep `fldPlayerSide`/`__FIELD.playerSide` across T0/T1/T3) + `tools/probe-csplayer.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-16): **PHASE B-5 · DIFFICULTY/REALISM PRESETS ✅ SHIPPED + VETTED + PUSHED** → NEXT = B-6 CS-player "command either side"

> **PHASE B-5 — the Command & Realism layer — is complete** (DECISIONS **D70**). New gated `src/tactical/T6-presets.js` + guarded seams in T0/T3/T4/T5 + a Difficulty row in the T2 skirmish setup + `tools/probe-presets.mjs`. Deliverable ≈**1684KB**.
>
> **✅ B-5 — difficulty + realism presets:** **TWO AXES** — an **AI tier** (Recruit / Regular / Veteran / Hardee) × a **realism bundle** (Arcade / Balanced / Historian), plus an **"Advanced"** per-lever expander. Read ONCE at `fldInitSim` (`fldPresetsApply`, before the fog/auto-pause precedence) → sets `__FIELD.sev`{attrition·canister·supply·cmdShock·sight·veteran} + aiSkill/aiResolve/aiCushion → **WIRES the already-existing B-1..B-4 knobs** (attrition→fire/melee cas · canister→`fldArtFireMult` · supply→`fldSupplyReserve` · cmdShock→`fldOfficerFalls` · sight→`fldUnitSight` · veteran→`fldMoraleStep` · aiSkill→`fldAiAttacker`/`fldAiDefender` thresholds · the Recruit cushion+brittle-enemy→`fldMoraleStep`). Mostly WIRING + a UI, not new sim (D69).
>
> **SMARTER-NOT-CHEATING, CODE-ENFORCED:** `aiResolve` HARD-capped ≤1.0 (never an enemy steadiness buff — in `_fldClampCfg`, not just the data table); the player cushion exists ONLY at a GENUINE Recruit (dropped if the AI-sharpness chip is tuned off the tier); **Hardee = sharper DECISIONS (aiSkill), not stat bonuses**; **Veteran = aiSkill 1.0 / no handicap = the shipped opponent.** **BYTE-IDENTITY:** no preset → NEUTRAL config (all 1.0 / 0), every seam ×1.0/+0 → the 9 tactical baselines stay byte-identical; **"Balanced" == today's shipped CS 8/8 (live stacked, fog ON) SEED-FOR-SEED.** The tactical **Engineering Corps stays SPLIT** (D69) — B-5 ships the sliders + hooks, not the corps.
>
> **✅ FULLY VETTED:** adversarial bug-hunt **76 agents → 13 confirmed + 6 critic gaps, ALL FIXED** (HIGH: drawer-Escape-tore-down-the-battle → `stopPropagation`+`fldKey` guard; HIGH: picker ARIA radiogroup→group+aria-pressed; MED: a malformed-preset clamp `_fldClampCfg`; MED: the fog-"scenario" V-toggle wipe → no-op at apply; MED: drawer focus-jump/stacked-listener/resume-behind-modal; + 7 probe gaps). **probe-presets 26/26, 0 pe** · full no-regression GREEN (9 tactical baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346) · wcag clean (3 contrast fixes). Committed + pushed.
>
> **What's playable:** a new main-menu **"⚙ COMMAND & REALISM"** button + a Difficulty row in the ⚔ SKIRMISH setup open a period-broadsheet picker (pick an AI tier × realism bundle, or open Advanced for per-lever chips); the choice persists and governs EVERY real-time battle (campaign "Fight in real time", skirmish, the standalone Sandbox + First Bull Run). In any battle, the control bar's **"⚙ Settings"** (key **G**) opens a focus-trapped drawer with live fog/auto-pause/speed toggles + the current difficulty. Pick **Recruit × Arcade** for the gentle on-ramp, **Hardee × Historian** for the unforgiving real thing — the AI never cheats.
>
> **IMMEDIATE NEXT = PHASE B-6 · CS-player "command either side"** (fork #10 — `fldPlayerSide` already honored from B-2): command EITHER side, so you DEFEND as the CS while the B-1 attacker AI presses you. Makes the attacker doctrine player-facing; doubles tactical replayability. Then **Phase C** breadth (Eastern marquee first — Antietam/Fredericksburg/Gettysburg/Chancellorsville on the data-driven engine, D69). **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1–B5 ticked; B6 next) → `DECISIONS.md` **D70** (B-5) + **D69** (the B-5 design clear) + **D68** (the Q&A) + **D67/D66/D65/D64/D62/D63/D61** → `src/tactical/T6-presets.js` (`FLDP`; `fldPresetCompute`/`fldPresetsApply`/`_fldClampCfg`; the picker `fldPresetMenu` + the drawer `fldOpenSettingsDrawer`) + the per-layer seams (grep `__FIELD.sev`/`aiSkill`/`aiResolve`/`aiCushion` across T0/T3/T4/T5) + `tools/probe-presets.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE B-4 · DISTINCT ARM ROLES ✅ SHIPPED + VETTED + PUSHED** → NEXT = B-5 difficulty/realism presets

> **PHASE B-4 — artillery + cavalry get real ROLES — is complete** (DECISIONS **D67**). New `src/tactical/T5-arms.js` + guarded T0/T1/T2 seams + `data/bullrun.json` (Stuart `role:flank`, `defaultFog:true`) + `tools/probe-arms.mjs`. Deliverable ≈**1646KB**.
>
> **✅ B-4 — the arms of the service:** **Artillery** fires CANISTER up close (a shotgun — murderous in the open, defeated by works/woods) and a long-range BOMBARDMENT beyond; `ARM.art`=0.35 → overrun in melee; the **battery doctrine is ASYMMETRIC** — a DEFENDING battery sits sited-safe + displaces early, an ATTACKING battery pushes FORWARD to a canister post and is catchable (Griffin/Ricketts, whose loss broke the Union attack). **Cavalry** runs scout / flank / screen / **raid** (a raider drains the enemy B-3 ammunition train); mounted shock (1.4) shatters a disordered line but breaks on a braced one (0.9). The **Cannon Corps you BUY** conditions the field battery (Napoleon→canister, Whitworth→long-range, via `_fldArtProfile`/the A1 bridge). Gated behind `__FIELD.arms` (sticky `_armsOff` → the 8 baselines, incl. bullrun1's art+cav, byte-identical).
>
> **TWO THINGS VETTING CAUGHT (the headline):** **(1) a real BYTE-IDENTITY bug the design missed** — the FROZEN base engine defines `const ARM={…melee…}` at `build/base.html:634` (art 0.40 / cav 1.05), so the old melee was reading REAL values; my first refactor dropped them to 1.0 when arms-off → shifted the balance. **Fixed:** arms-OFF now falls back to the base `ARM` table (verified vs the committed build — bare & logistics both CS 5/8, byte-identical). **(2) a stacked-balance FORK (surfaced to Aaron):** the per-layer probes each isolate the others, so the STACKED live config (officers+logistics+arms) was never measured — it's **Union-favoured in clear weather** (committed D66 already CS 3/8 fog-OFF; B-4 → 0/8). Fog-ON is CS 8/8 ("fog aids the defender" intact). **Aaron's call: First Bull Run now DEFAULTS to FOG ON** (the historically faithful battle; live stacked config → CS 8/8). Fog stays a toggle; an explicit setting still wins.
>
> **✅ FULLY VETTED:** adversarial bug-hunt **31 agents → 2 distinct confirmed (3D-mesh-on-reinforcement, fog HUD leak) + 6 critic gaps, ALL FIXED** (incl. 6 new probe steps). **probe-arms 23/23, 0 pe** · full no-regression GREEN (8 baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346 · strategic spot-check green) · wcag clean. Balance: arms-only fog-OFF CS 7/8 · **LIVE stacked default (fog ON) CS 8/8**. Committed + pushed.
>
> **What's playable:** unchanged entry points (campaign "Fight in real time ⚔" · the ⚔ SKIRMISH menu · the standalone Sandbox + First Bull Run). The difference: **batteries and cavalry fight as themselves** — a battery flays a line in the open with canister but is overrun if you let infantry reach it; the forward Union guns at Henry House Hill are exposed (history); cavalry scouts, screens, flanks, charges a broken line, and can raid the enemy trains. **First Bull Run now opens under fog** (press **V** to clear it) — smoke and broken ground, where the defender holds the hill.
>
> **IMMEDIATE NEXT = PHASE B-5 · difficulty/realism presets — DESIGN CLEARED (DECISIONS D69):** new gated `src/tactical/T6-presets.js` read at `fldInitSim` from `G.settings.tacticalPreset` + per-lever values → sets the per-layer gates/severities (existing knobs → WIRING + a UI). **Curated 4-5 sliders + an "Advanced" expander** (AI level · fog · attrition · supply strictness · canister lethality; expander = command-shock/auto-pause/LOS/AI-leashes/xp). **Ladder = Recruit/Regular/Veteran/Hardee × Arcade/Balanced/Historian; "Balanced" = today's shipped balance (the byte-identical default, incl. Bull-Run-fog-ON).** Smarter-not-cheating (an easy-tier cushion only). `probe-presets.mjs` asserts neutral==today + each preset's gates. **The tactical ENGINEERING CORPS is SPLIT to its OWN later milestone** (Aaron's directive; consumes these sliders; memory `civilwar-directive-tactical-engineering-sliders`). Then **B6** CS-player "command either side" (fork #10) → **Phase C** breadth (Eastern marquee first — D69). **D68 = a 30-question design Q&A**: expansive v1, zoomable scale, wide-open alt-history + deeper citations, custom-builder-early, loot→core-pillar; **hex=Classic (no new engine)**, AA-essentials, tooltips-onboarding. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1–B4 ticked; B5 next) → `DECISIONS.md` **D67** (B-4) + **D68** (the Q&A) + **D66/D65/D64/D62/D63/D61** → `src/tactical/T5-arms.js` (`FLDA`; `fldArmMelee`/`fldArtFireMult`/`fldAiArtillery`/`fldAiCavalry`/`fldArmsStep`) + the base-ARM fallback in T0 `fldResolveMelee` + the `defaultFog` precedence (T0 `fldInitSim` / T1 `fldScenarioInit`) + `tools/probe-arms.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE B-3 · IN-BATTLE LOGISTICS ✅ SHIPPED + VETTED + PUSHED** → NEXT = B-4 distinct arm roles

> **PHASE B-3 — the ammunition economy on the real-time field — is complete** (DECISIONS **D66**). New `src/tactical/T4-logistics.js` + guarded T0 seams + the Bull Run `supply` block + `tools/probe-logistics.mjs`. Deliverable ≈**1608KB**.
>
> **✅ B-3 — supply trains + resupply + exhaustion:** each side fields a rear **ammunition train** with a finite **reserve**; a DISENGAGED brigade within reach refills (draining the reserve); a brigade OUT of cartridges holding the objective fires dry → bayonet (the 20th Maine); a SPENT brigade (fatigue>84) drags. The **attacker's train sits far behind its start line** (it outruns its supply) and the **defender's just behind the objective** → balance-neutral. **Ties strategic supply:** the reserve scales with `bridgeArmy().supply`; a raid-supply order cuts the enemy's. Gated + BYTE-IDENTICAL behind `__FIELD.logistics` (the 7 baselines + officers set sticky `_logisticsOff`).
>
> **THE FOG FIX (the headline — a HIGH bug-hunt finding, surfaced to Aaron):** with **fog ON**, logistics first INVERTED "fog aids the defender" (D58/D64) — Bull Run AI-vs-AI fog-ON **CS 8/8 → 0/8** (under fog the attacker cycled fresh brigades to its safe far-rear train and out-sustained the long fire-trade). Aaron's first pick (a defender's-depot rule) provably did nothing (the defender wasn't the ammo-constrained side); the fix that works (Aaron-approved) is **choking the ATTACKER's resupply under fog** (`ATK_FOG_RESUPPLY`=0 — an attacker advancing blind can't run its trains forward). **Result: logistics is balance-NEUTRAL in BOTH fog states — fog-OFF CS 5/8, fog-ON CS 8/8** (each = the no-logistics baseline; fog-aids-defender preserved). A FOG REGRESSION GUARD probe step locks it.
>
> **✅ FULLY VETTED:** adversarial bug-hunt **64 agents → 9 confirmed + 2 critic gaps, ALL ADDRESSED** (1 HIGH fog + 6 LOW probe-fidelity/correctness: probe boundary test, two-sided balance band, asymmetry + fog-reveal + supply-curve probe steps, reinforcement-only-side train). **probe-logistics 14/14, 0 pe** · full no-regression GREEN (7 baselines byte-identical · bootprobe ok · t1probe 14/14 · diag-classic 346) · wcag clean (1 HUD-contrast fix). Committed + pushed.
>
> **What's playable:** unchanged entry points (campaign "Fight in real time ⚔" · the ⚔ SKIRMISH menu · the standalone Sandbox + First Bull Run). The difference: each side now has a rear **ammunition train** (a ❈ wagon marker + a faint resupply ring); a brigade out of close action near its train refills, the reserve is finite, and a brigade out of cartridges on the objective fires dry then bayonets; the HUD shows the reserve ("ample / running low / …") + a selected brigade's ammo/resupply/spent status. Turn on Fog (V) and the defender still holds the hill — the attacker outruns its supply.
>
> **IMMEDIATE NEXT = PHASE B (continued):** **B4** distinct arm roles (artillery canister/long-range; cavalry scout/flank/screen/raid) — **design drafted** (`.tmp/B4-arm-roles-design.md`: the `ARM`-melee dangling hook to fill; Bull Run already fields Griffin/Ricketts as `art` + Stuart as `cav`; the Cannon-Corps→field-battery bridge; the cav-raid↔B-3-train tie) · **B5** difficulty/realism presets · **B6** the CS-player "command either side" mode (fork #10). **B5+B6 also sketched** (`.tmp/B5-B6-design-sketch.md`). **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1 + B2 + B3 ticked; B4 next) → `DECISIONS.md` **D66** (B-3) + **D65/D64/D62/D63/D61** → `src/tactical/T4-logistics.js` (`FLDL` incl. `ATK_FOG_RESUPPLY`; `fldBuildSupply`/`fldSupplyRearPos`/`fldLogisticsStep`/`fldUnitInCloseAction`/`fldLogisticsAiUnit`) + `tools/probe-logistics.mjs`. Everything is on disk and pushed.
>
> **Session-env note:** global CC permissions = **bypassPermissions** (`~/.claude/settings.json`); MJI data/tooling/transcripts **purged** from this machine (personal-projects-only now); `~/.claude/CLAUDE.md` rewritten accordingly. None of that affects the game build.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE B-2 · OFFICERS & COMMAND ✅ SHIPPED + VETTED + PUSHED** → NEXT = Phase B continues (B3 logistics · B4 arm roles · B5 presets · B6 CS-player)

> **PHASE B-2 — leadership ON the field — is complete** (DECISIONS **D65**; charter fork: officers/command). New `src/tactical/T3-officers.js` + guarded T0 seams + the real Bull Run command cast in `data/bullrun.json.leaders`.
>
> **✅ B-2 — `fldOfficersStep` + the officer layer:** named leaders ride with their army and project a **COMMAND RADIUS** — brigades inside it recover morale faster, hold longer against the rout (capped save), and rally sooner — and **CAN BE HIT**: exposure near the firing line → wound (aura halves) → fall (aura dies + a one-time **command shock**, the general-down rout trigger). **Ties the named-generals layer (S2 m5):** a campaign commander's quality + name come from `bridgeArmy().leadership` / `cmdActiveGeneral()`; scenarios carry the cast (McDowell/Heintzelman vs Beauregard/Johnston/Bee/Bartow/Jackson). **Gated + BYTE-IDENTICAL:** behind `__FIELD.officers`; the 6 existing probes set sticky `_officersOff` → `(u.cmdBonus||0)`=0 → all baselines unchanged. **Bug-hunt: 52 agents → 1 confirmed + 7 critic gaps, ALL FIXED** — the big one was an over-lethal death model (army commanders who *survived* First Bull Run died every game); fixed via per-leader **historical `fate`** + risk **decay** + wound recovery + a **cover** term (the Jackson reverse-slope lesson) → army cdrs now survive **10/10**; also the Bee/Jackson anachronism (gated + contested framing), the CS-player hardcoding (`fldPlayerSide`, prep for B6), a balance moderation, and fog memory for fallen leaders. **Balance (officers ON, Bull Run AI-vs-AI):** CS **7/8** fog-OFF (the Henry House Hill command stand; US still breaks 1/8) · **8/8 fog-ON** (fog still aids the defender). **probe-officers 15/15**; full no-regression GREEN (6 baselines byte-identical, diag-classic 346, boot/t1, 0 pageerrors). Deliverable ≈**1585KB**.
>
> **What's playable:** unchanged entry points (campaign "Fight in real time ⚔" · the ⚔ SKIRMISH menu · the standalone Sandbox + First Bull Run). The difference: **named generals now ride the field** — a faint command ring + a mounted-officer marker; brigades under command steady and rally; a general riding the line too long can be **wounded or fall**, and his loss shocks the troops near him. At First Bull Run you see Beauregard, Johnston, Bee, Bartow, and (at 135s) Jackson — and the cost of leading from the saddle. The HUD shows your field officers' status; a selected brigade shows its commander + whether it's in command range.
>
> **IMMEDIATE NEXT = PHASE B (continued):** **B3** in-battle logistics (ammo + fatigue depth + supply) · **B4** distinct arm roles (artillery canister/long-range; cavalry scout/flank/screen/raid) · **B5** difficulty/realism presets · **B6** the **CS-player "command either side" mode** (fork #10 — now that `fldPlayerSide` is honored, this is the natural next). Then **Phase C** breadth → **D** full hex → **E** strategic S3–S5 → **H** graphics/footage/audio → **I** loot → **J** polish. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1 + B2 ticked; B3 next) → `DECISIONS.md` **D65** (B-2) + **D64/D62/D63** + D54/D55/D61 → `src/tactical/T3-officers.js` (`fldBuildOfficers`/`fldOfficersStep`/`fldOfficerHazard`/`fldOfficerFalls`/`fldPlayerSide`) + `data/bullrun.json.leaders` + `tools/probe-officers.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE B-1 · the smarter ATTACKER AI ✅ SHIPPED + VETTED + PUSHED** → NEXT = Phase B continues (B2 officers · B3 logistics · B4 arm roles · B5 presets · B6 CS-player)

> **PHASE B (tactical depth) is underway. B1 — the smarter DEFENDER-FAVORED ATTACKER AI — is complete** (DECISIONS **D64**; charter fork #2). The complement to the P1b-iii defender: a real attacker doctrine `fldAiAttacker` in `src/tactical/T0-field-sandbox.js`, dispatched to the scenario attacker (the symmetric sandbox + the defender stay byte-identical — unreachable when `__FIELD.attacker===null`).
>
> **✅ B1 — `fldAiAttacker`:** a covered defender wins the long-range fire trade, so the attacker CONCENTRATES on the weaker flank, CLOSES, and ASSAULTS (melee + numbers beat fire + cover). **Two locks (fork #2):** (a) **GRADUAL** per-unit local-concentration commit (no knife-edge global flip — fixed the prototype) and (b) **FOG AIDS THE DEFENDER** — under fog the attacker NEVER mass-assaults / column-rushes (it can't trust its sight of the hidden reserves), only exploits visibly-broken units + probes in line. **Balance A/B (8 seeds, Bull Run AI-vs-AI, CS=defender wins):** both-generic fog-OFF **2/8** (the logged gap) → both-doctrines fog-OFF **6/8** (CS-favored but competitive — US breaks 2/8) / fog-ON **8/8** (fog aids the defender — the prototype's inversion tuned out); defender casualties **4592** vs the passive attacker's 1276 (~3.6×, a real bloody assault). **Bug-hunt 4 finders → 2 findings, BOTH FIXED** (a picket-unblind hole that broke the fog lock → gated the assault directly on `!fog`; a dead-constant doc nit → deleted). **probe-ai 15/15**; tactical no-regression GREEN (field 13/13, bullrun 15/15, fog 13/13, autopause 13/13, diag-classic paints); 0 pageerrors. Deliverable ≈**1552KB**.
>
> **What's playable:** unchanged entry points (campaign "Fight in real time ⚔" · the ⚔ SKIRMISH menu · the standalone Sandbox + First Bull Run). The difference: in any asymmetric battle the **ATTACKER AI now fights for real** — it masses on a flank and presses the bayonet (fog OFF) instead of trading fire it loses; under fog it advances cautiously, so the defender's hidden reserves matter. AI-vs-AI Bull Run is now a bloody, historically-faithful Confederate-favored fight rather than a Union walkover.
>
> **IMMEDIATE NEXT = PHASE B (continued):** **B2** officers/command (leaders: command radius + morale bonus, can be hit; ties named-generals) · **B3** in-battle logistics (ammo + fatigue depth + supply) · **B4** distinct arm roles (artillery canister/long-range; cavalry scout/flank/screen/raid) · **B5** difficulty/realism presets · **B6** the **CS-player "command either side" mode** (fork #10 — makes this attacker AI player-facing: you defend, the AI attacks). Then **Phase C** breadth → **D** full hex → **E** strategic S3–S5 → **H** graphics/footage/audio → **I** loot → **J** polish. **PUSH per milestone after PROPER VETTING (D53-OPS). Subagent models = HYBRID (D63).**
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A + B1 ticked; B2 next) → `DECISIONS.md` **D64** (B1) + **D62/D63** + D54/D55/D61 → `ATTACKER-AI-PROPOSAL.md` (the prototype + the locked answers) → `src/tactical/T0-field-sandbox.js` (`fldAiAttacker`/`fldAiDefender`/`fldAiUnit`) + `tools/probe-ai.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): **PHASE A · CONNECT THE TWO LAYERS ✅ SHIPPED + VETTED + PUSHED** → NEXT = Phase B (tactical depth)

> **The V1-CHECKLIST drives the run (D61).** Phase A — the connective tissue between the owner-mode GRAND STRATEGY desk and the real-time TACTICAL engine — is **complete** (DECISIONS **D62**). The two layers were disconnected; now the army your war built FIELDS in the real-time fight, and the fight's result flows back into the campaign.
>
> **✅ PHASE A — A1 conditioning · A2 fight-from-bridge + FREE skirmish · A3 result feedback — SHIPPED.** New `src/tactical/T2-campaign-link.js` + guarded T0/T1 seams + a bridge-briefing button (`src/85`). **A1:** `bridgeArmy(C)` scales the player's tactical brigades (men/morale/fatigue) + re-arms from the bought loadout, **anchored 74 / ±12% — identical to Classic-hex (`_a6Condition`) and auto-resolve**, so all three modes balance equivalently; reinforcements conditioned too. **A2:** a **"Fight in real time ⚔"** button on the pre-battle briefing (beside Auto-resolve + the Classic "To the Field") — First Bull Run (US) routes to the `bullrun1` scenario, every other battle to a CONDITIONED procedural brigade fight; plus a **"⚔ SKIRMISH (custom)"** main-menu setup (side / size / ground / year / fog). **A3:** the fight's **REAL casualty fractions** (men lost / fielded, counting reinforcements) drive `startBattleRuntime`+`_arApplyCasualties`+`campaignAdvance`→`_t1Resolve` — a real-time win/loss is **substitutable** for a Classic/auto-resolved result (win advances, loss recovers, casualties feed the strategic tick); deterministic; abort (Esc mid-battle) returns to the briefing, re-launchable. **Adversarial bug-hunt: 46 agents → 7 root-cause findings (incl. 3 HIGH: Esc-discards-the-result, no-focus-trap, flipAtk-lost-on-abort), ALL FIXED + re-probed.** WCAG-AA clean (2 trivial fixes). **probe-campaign-link 16/16**; full no-regression GREEN (tactical 5 suites + 22-probe strategy suite + bootprobe + t1probe + diag-classic paints, 0 pageerrors); deliverable ≈**1545KB**.
>
> **What's playable:** run the President's Desk → at any campaign battle's pre-battle briefing, choose **Auto-resolve**, **Fight in real time ⚔** (the new tactical engine, conditioned by your war, result feeds back), OR **To the Field (Classic)** (the hex battle). The main menu also has **⚔ SKIRMISH (custom)** for a free real-time engagement (pick side/size/ground/year/fog) + the standalone Tactical Sandbox + First Bull Run. The strategic desk (13 tabs, S1–S2) + Classic hex battles are unchanged.
>
> **IMMEDIATE NEXT = PHASE B (TACTICAL DEPTH, P2–P5):** **B1** the smarter **defender-favored ATTACKER AI** (concentrate/close/assault — prototype in `ATTACKER-AI-PROPOSAL.md`/`.tmp/attacker-ai-prototype.patch`; keep Bull Run defender-favored + **tune OUT the fog inversion** so fog stays consistent with the D58 teaching — fork #2) · **B2** officers/command (command radius + morale bonus, can be hit) · **B3** in-battle logistics (ammo+fatigue depth + supply) · **B4** distinct arm roles (artillery canister/long-range, cavalry scout/flank/screen/raid) · **B5** difficulty/realism presets · **B6** the **CS-player "command either side" mode** (you defend, AI attacks — fork #10). **PUSH RULE: per-milestone after PROPER VETTING (D53-OPS).** **Subagent models: HYBRID routing (D63)** — Opus on reasoning legs, Sonnet on mechanical legs.
>
> **Resume map:** `START-HERE.md` → `V1-CHECKLIST.md` (Phase A ticked; Phase B next) → `DECISIONS.md` **D62** (Phase A) + **D63** (model routing) + D54/D55/D61 → `MODERN-UGG-PLAN.md` → `src/tactical/` (T0 sandbox+P1b · T1 bull-run · **T2 campaign-link**) + `src/85/86/87` (the bridge) + `tools/probe-campaign-link.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (2026-06-15): TACTICAL ENGINE P0 ✅ · P1a Bull Run ✅ · P1b-i FOG ✅ · P1b-ii AUTO-PAUSE ✅ · **P1b-iii ROLE-AWARE AI (smarter CS DEFENDER) ✅ SHIPPED** → NEXT = the smarter ATTACKER AI, then the rest of P1b

> **The run-k charter reprioritized the real-time TACTICAL ENGINE ahead of S3–S5 (D54/D55).** S2 is COMPLETE (m1–m5; the block below). The tactical engine is now being built in gated, probed, adversarially-bug-hunted, pushed slices: **P0 sandbox (D56) → P1a First Bull Run (D57) → P1b-i fog (D58) → P1b-ii active auto-pause (D59) → P1b-iii role-aware AI (D60, just shipped).**
>
> **✅ TACTICAL P1b-iii — ROLE-AWARE AI: a smarter CONFEDERATE DEFENDER — SHIPPED + VERIFIED + PUSHED** (DECISIONS **D60**). The named complement to fog: the shipped AI was symmetric (both sides "advance toward the objective"), so the CS *defender* played like an attacker — the root of the logged "AI-vs-AI leans Union" gap (D57). Now `fldAiUnit` **dispatches** on `__FIELD.attacker`: the scenario DEFENDER runs the new `fldAiDefender` (hold the objective's forward face in cover via a **stable defensive frame**; fighting withdrawal to the reverse slope; counterattack a *catchable* disordered attacker on a **dual leash**; hold points **clamped inside `obj.r`**); the ATTACKER + BOTH sandbox sides run the verbatim-unchanged `fldAiGeneric`. The sandbox has `attacker===null` → `fldAiDefender` unreachable → **byte-behavior-identical** (probe-field 13/13). Deterministic + in the headless sim path → **fully probe-testable**. **Balance A/B (8 seeds):** old AI CS 0/8 fog-OFF, 1/8 fog-ON → doctrine 8/8 / 8/8, via historical **denial/timeout** wins. Adversarial bug-hunt **27 agents → 10 confirmed (6 engine + 4 probe-fidelity), ALL FIXED**. probe-ai **11/11**; full no-regression GREEN (probe-field 13/13, bullrun 15/15, fog 13/13, autopause 13/13 + strategy suite + bootprobe + t1probe + diag-classic, 0 pageerrors); deliverable ≈**1509KB**. In `src/tactical/T0-field-sandbox.js` + `tools/probe-ai.mjs`.
>
> **What's playable:** open `civil_war_generals.html` → main menu Field Dispatches has **"⚔ Tactical Sandbox (Beta)"** + **"⚔ BATTLE — FIRST BULL RUN (1861)"**. Bull Run: briefing → take command of the Union → drive for Henry House Hill before Jackson/Early/Elzey arrive by rail. Toggles: **V** fog (default OFF), **P** auto-pause (default ON). The CS now defends the hill intelligently (holds in cover, fighting-withdraws, counterattacks) — a real opponent. Speed 1×/2×/4×, active pause, a 2D offline fallback. The strategic owner-mode desk (13 tabs, S1–S2) + Classic hex battles are unchanged.
>
> **IMMEDIATE NEXT = TACTICAL P1b (continued):** the **smarter ATTACKER AI** (concentration/flanking — the complement that restores AI-vs-AI variety now that the defender is strong; the logged lever, NOT weakening the defender) · the **hex-mode toggle** (Classic frozen) · **full owner-mode bridge conditioning** into the scenario + a **tactical-only FREE mode** · **deeper 3D** (reuse run-h PBR/HDRI/post-FX). Then P2–P5, the loot/survival texture, then S3 alt-history · S4 education/codex/WCAG-AA · S5 victory + Reconstruction · the graphics/footage layer. **PUSH RULE: commit + `git push origin main` per milestone ONLY after PROPER VETTING** (GATE OK + full no-regression + diag-classic + 0 pageerrors + bug-hunt-fixed) — DECISIONS **D53-OPS**.
>
> **Resume map:** `START-HERE.md` → `AUTONOMOUS-RUN.md` §2 (the tactical m0–m4 block) + §8 charter → `DECISIONS.md` D54/D55 + **D56–D60** → `MODERN-UGG-PLAN.md` → `src/tactical/` (T0 sandbox+all P1b systems · T1 bull-run) + `tools/probe-{field,bullrun,fog,autopause,ai}.mjs`. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run k (overnight, 2026-06-14): S2 COMPLETE — m1 CABINET ✅ + m2 DECISIONS ✅ + m3 MORALE ✅ + m4 PRESS ✅ + m5 COMMAND ✅ SHIPPED → NEXT = S3 alt-history

> **The §8 charter governs the run** (`AUTONOMOUS-RUN.md` §8 + `DECISIONS.md` D30–D41). Battle layer A1–A6 done (run j). S2 is built INCREMENTALLY — one sub-system per gated + empirically-probed + adversarially-bug-hunted + committed + **pushed** milestone. **Owner directive (this session): fold web-search/fetch grounding into the content-research workflows** (real public sources → adversarially verified). Blender stays CLOSED — art is code-only by charter (§8.8); nothing's lost.
>
> **✅ S2 m1 — THE CABINET & ADVISOR SYSTEM (R25 linchpin) SHIPPED + VERIFIED + PUSHED** (commit on `main`; DECISIONS **D49**). The S0 stub → the FULL date-aware historical cabinet: `data/cabinet.json` (US 13 / CS 15; the churn is mechanical) + `src/31-cabinet.js` (overrides `presRenderCabinet`/`presWireCabinet`; `cabInit`/`cabOnResolve` registered in `90`). Per principal advisor (war/treasury/state/navy): a war-state-keyed **one-line rec** + one-click **Heed** (bounded once-per-turn nudge) or **Delegate** + expandable **why** + an **ambition tell** for the self-servers (Chase/Toombs/Blair). Date-aware holders make the churn legible (Davis's revolving War office, Benjamin's 3-post rotation). Content = the web-grounded `s2-cabinet-research` workflow (21 agents, 2× adversarial verify), anti-Lost-Cause. Bug-hunt 62 agents → 8/19 confirmed, all fixed (D49.1–.8). `cabinetLeadership(C)` exposed but **not yet consumed by the bridge** — that wiring lands with the generals milestone. probe-cabinet **12/12**; 16 probe suites + diag-classic green; 0 pageerrors. Deliverable ≈1130KB.
>
> **✅ S2 m2 — EXECUTIVE DECISIONS + the pendingChoices loop SHIPPED + VERIFIED + PUSHED** (DECISIONS **D50**). `data/decisions.json` (8 web-grounded cards) + `src/32-decisions.js`: a non-blocking decision deck surfaced in the between-battles interstitial AND a new **"Decisions" desk tab** (the 11th). `decOnResolve` owns the queue at the §8.68 cadence (≤2 new non-hinge/turn; the hinge always surfaces); `decResolve` applies bounded/clamped/logged effects. **EMANCIPATION = a dated Union hinge** (issue / go-radical-now / refuse) wired to the **manpower USCT gate** (`C.president.emancipation`: issue → unlocks even pre-1863; refuse → never; no-decision → the historical 1863 default = zero regression). Civil-liberties + home-front cards both sides (habeas US+CS, Vallandigham, NYC draft riots, press censorship, Richmond bread riot, twenty-slave law). Bug-hunt 56 agents → 12/17 fixed (2 HIGH: decResolve idempotency, capital floor-only). probe-decisions **14/14**, desk **13/13**, manpower **9/9**; 0 pageerrors.
>
> **✅ S2 m3 — THE THREE-LAYER MORALE SYSTEM + the 1864 election hinge SHIPPED + VERIFIED + PUSHED** (DECISIONS **D51**). `src/33-morale.js` + `C.morale`: troop / leader / public-will, computed fresh each turn (`moraleCompute`) with the interactions baked in (leadership lifts troops; the accumulating casualty toll [D35.6] + inflation sink the public; victories/recognition lift it). Surfaced as **"The Nation's Will"** in the War Effort overview with an 1864-election forecast. **The election → the war's end (D41.2):** `moraleOnResolve` (after clk, before vic) feeds the verdict into the existing `C.strategy.enemyWill` — re-elected → −8 (grind the enemy toward your negotiated-peace win); **repudiated → +12 + a durable public-will hit** (you can win battles and lose the war). A computed READOUT with ONE bounded sim coupling — writes nothing to the frozen clock/economy/manpower (zero regression). Bug-hunt 29 agents → 7/8 fixed (2 HIGH: migration-safe + atomic election latch). probe-morale **10/10**; probe-victory **12/12**; 0 pageerrors.
>
> **✅ S2 m4 — THE PRESS / PUBLIC-OPINION SYSTEM SHIPPED + VERIFIED + PUSHED** (DECISIONS **D52**). `data/press.json` (12 real web-grounded papers) + `src/34-press.js` + a new **"The Press"** desk tab (the 12th, period broadsheet). Each paper's LEAN (radical/administration/copperhead/fire-eater/abolitionist/war-democrat) sets how it reacts to the war's turns; `pressOnResolve` (before morale) shifts each paper's FAVOR; the aggregate SENTIMENT feeds `moraleCompute`'s public-will layer (`pressTerm`, anchored at 50 = neutral until reacted, so zero perturbation at init). Greeley/Garrison/Medill/Curtis + the Copperhead sheets + the Richmond Examiner savaging Davis; anti-Lost-Cause (BOTH presses divided). Bug-hunt 44 agents → 7/13 fixed (MED: a corrupt-`null`-headline crash → `pressInit` filters entries). probe-press **10/10**, desk **13/13**, morale **10/10**; full no-regression green; 0 pageerrors.
>
> **✅ S2 m5 — COMMAND / NAMED-GENERALS SHIPPED + VERIFIED + PUSHED** (DECISIONS **D53**). `data/generals.json` (20 generals, US 10 / CS 10) + `src/35-command.js` + `C.president.command` + a new **"Command"** desk tab (the 13th). Appoint / promote / relieve the principal-army commander; each carries skill/aggression/caution traits, an EVOLVING reputation (win ↑ / loss ↓), and ambition (the McClellan relief-cost gate on `clock.capital`, with a free "restore the historical command" escape). **THE KEY WIRING (done):** `commandLeadership(C)` = `0.55·skill + 0.45·reputation` blended with `cabinetLeadership`, **anchored at 64**, now drives the bridge LEADERSHIP facet (the `src/85` line-90 `leadership=64` placeholder, live since D49.7) + a bidirectional `bridgeArmy.overall` edge (0 at the anchor → fresh/default command = byte-equivalent Classic) + a `commandMarginEdge` auto-resolve nudge. **The m3 leader-morale layer deliberately STAYS on `cabinetLeadership`** (D53 decoupling — folding the general in would double-count the cabinet AND confound the cabinet-isolating morale probe). Date-aware iconic-army DEFAULT (US Scott→McClellan→Burnside→Hooker→Meade→Grant; CS J.E. Johnston→Lee; the West/Halleck/Sheridan/Thomas/Sherman appointable but null-default). Content = web-grounded `s2-generals-research` (10 agents), anti-Lost-Cause (Forrest's bio names Fort Pillow + KKK + slave-trading; Lee excellent-not-infallible; Grant top-tier). Bug-hunt 20 agents → **9/12 fixed** incl. 2 HIGH (a `bridgeArmy↔cabinet` recursion landmine → `_brgLeadGuard` re-entrancy guard; Thomas/Sherman Western-theater defaults nulled; post-war fallback continuity). probe-command **14/14**; full no-regression GREEN (bridge 6/6 · conditioning 6/6 · auto-resolve 7/7 · cabinet 12/12 · morale 10/10 · desk 13/13 · all 16+ suites + diag-classic); 0 pageerrors. Deliverable ≈1392KB.
>
> **IMMEDIATE NEXT = S3 (alt-history engine):** tiered divergence + hinge-points + the emergent toggle + curated scenarios, deterministic-by-performance with consensus framing + the "your war vs history" tracker (GRAND-STRATEGY §3 / §S3). Then **S4** education/codex/tutorial/WCAG-AA · **S5** victory + graded report + Reconstruction · then the **run-k owner directives**: the graphics/footage "make everything come to life" linked-assets layer (offline fallback, memory `civilwar-graphics-footage-directive`) + the hunting/fishing/town-looting "Borderlands × Oregon Trail" survival texture (memory `civilwar-gameplay-survival-loot`). **PUSH RULE (Aaron, this session): commit + `git push origin main` to `adhanke-star/History-Video-Game` per milestone ONLY after PROPER VETTING** (GATE OK + full no-regression + diag-classic + 0 pageerrors + bug-hunt-fixed) — see DECISIONS **D53-OPS** + `START-HERE.md` + `AUTONOMOUS-RUN.md` §1.
>
> **The desk has Cabinet (10th) + Decisions (11th) + The Press (12th) + Command (13th) tabs; the overview shows War Production · The Ranks · The Nation's Will.** Modules `src/10/20/30/31/32/33/34/35/40/50/55/56/57/58/59/60/70/80/85/86/87/90`. Tick order in `_t1Resolve`: clk → econ → wr → blockade → prod → eng → manpower → mr → pres → **cab → cmd → dec → press → morale** → vic → bridge. Data: `data/{cabinet,decisions,press,generals,economy,diplomacy,manpower-teaching,artillery,engineering,terrain-cover,weapons}.json` → `GAME_DATA`. **Cross-system wiring patterns now in place** (each is the same `_t1Resolve`-ordered, bounded, readout+one-coupling shape): decision → `C.president.emancipation` → manpower USCT gate (m2); morale's 1864 verdict → `C.strategy.enemyWill` (m3); press sentiment → `C.morale.public` (m4); **the sitting general → `commandLeadership` → the bridge LEADERSHIP facet (m5, anchored at 64)**. S2 is COMPLETE.

---

## ⚡ CONTINUE HERE — run j (overnight, 2026-06-14): ⚑ BATTLE LAYER A1–A6 ✅ COMPLETE (war playable end-to-end) → NEXT = the STRATEGIC ARC S2

> **The §8 charter governs the run** (68 Aaron-locked decisions; `AUTONOMOUS-RUN.md` §8 + `DECISIONS.md` D30–D41 — read §8 FIRST). Unblockers: gated frozen-engine overrides AUTHORIZED · halt only for irreversible/money/external-account · **GitHub backup LIVE (`adhanke-star/History-Video-Game`, private — push every milestone)** · adversarial bug-hunt before every commit · auto-tune balance+log · max systems shipped.
>
> **⚑ THE BATTLE LAYER IS COMPLETE — the owner-mode war is PLAYABLE END-TO-END.** All shipped + pushed; each gated + empirically probed + adversarially bug-hunted; full no-regression green; 0 pageerrors; Classic never regressed:
> **A1 Cannon Corps** `da74ad9` (D42) · **A2 Engineering Works Corps** `efad4c6` (D43, 4 Verified cards) · **A3 Terrain cover** `b21b5d7` (D44, 1st frozen-engine override) · **A4 Armory→battlefield** `47ae217` (D45, `genForce` override) · **A5 Targeting** `3959b65` (D46, verified) · **A6a Battle-day conditioning** `88f878c` (D47, `startBattleRuntime` override) · **A6b Bridge auto-resolve** `149d07e` (D48, the playable-end-to-end keystone). 16 probe suites green.
>
> **The playable loop:** pick a side → run the President's Desk (economy/production/blockade/diplomacy/manpower + the three buildable corps: small arms, Cannon Corps, Engineering Works) → field a CONDITIONED army (the bridge) → at each battle **Auto-resolve OR Fight** (weapons bought + terrain cover + conditioning all real on the day) → the result drives manpower/clock/strategy/enemyWill/victory → win via the chain or a path-to-victory.
>
> **IMMEDIATE NEXT = the STRATEGIC ARC, charter-expanded (§7 item 2 / AUTONOMOUS-RUN §2).** **S2** = the FULL historical cabinet (§8.22) + one-line-rec/delegate/why advisor UX (§8.54) + 3-layer morale (§8.61; public-will-decides-late §8.67) + the 1864 election + the press/public-opinion system (§8.57) + command: appoint/promote/relieve generals (§8.58) + emancipation as a dated decision (§8.27) + civil-liberties dilemmas (§8.62) + named generals (§8.24) + the curated home-front event deck (§8.25). **Content may be recoverable via the cached s2-content-research workflow — see `AUTONOMOUS-RUN.md` §7 item 7** (else re-run a fresh advisor+decision-card research workflow). Then S3 alt-history · S4 education/codex/tutorial/WCAG-AA · S5 victory + graded report + Reconstruction · content systems · theaters · tactical P0–P5.
>
> **Full morning summary: `WAKE-UP.md` (root).** To run it: fresh chat → paste the `START-HERE.md` kickoff prompt → it loads `AUTONOMOUS-RUN.md` §2 (A1–A6 ✅, S2 next) + D42–D48 → continue the §3 loop at S2. Everything is on disk and pushed.

---

## ⚡ CONTINUE HERE — run i progress (S0 + S1 COMPLETE: a/b/c/d SHIPPED) → next: S2 (updated 2026-06-13)

> **S1 is COMPLETE — the full asymmetric economy.** S1a finance/inflation · S1b production/logistics · S1c cotton/blockade/diplomacy · **S1d manpower/conscription (just shipped).** `src/70-manpower.js` + `G.campaign.manpower`: the replacement-ratio collapse (US 1.0 holds; CS 0.9→0.1 by 1865 → armies melt), Union draft-as-stimulus, +180k USCT pool at 1863, immigrant inflow, CS desertion; **`C.manpower.strength` is the army-strength index the S5 battle bridge reads.** Surfaced as "The Ranks" in the War Effort overview. Content-complete via an 11-agent workflow → `data/manpower-teaching.json` (5 multi-voice cards + 9-claim audit; no sim correction needed). **VERIFIED:** probe-manpower 9/9 (20-turn arc to 1865: US strength 100 / CS 53-melting / ratio 0.1 / pool 0); full no-regression suite green (desk 12/12, economy 8/8, production 4/4, blockade 11/11, diag-classic paints, 0 pageerrors).
>
> **NEXT = S2** (`GRAND-STRATEGY-PLAN` §3/§4/§16/§25, R25 the linchpin): the **advisor/cabinet system** — generalize the Treasury/Diplomacy/War per-domain Delegate toggle + the ambient "Why it mattered" teaching into a full system where each secretary auto-manages + recommends + TEACHES; wire `president.pendingChoices` curated non-blocking decision cards (the S0 stub); 3-layer morale (troop/leader-reputation+ambition/public); wire the 1864 election (`clk.resolved1864`) to consequences. The 10 economy/diplomacy/manpower card sets + the desk are the foundation to build on. Then S3 alt-history · S4 education/codex · S5 victory + the battle bridge.
>
> Eight desk tabs now: The War Effort (overview: treasury/home-front meters + War Production + The Ranks blocks) · The Treasury · Diplomacy · War Room · 1864 Clock · Muster Roll · Cabinet · Theater Map. Modules `src/10/20/30/40/50/60/70/90`. Tick order in `_t1Resolve`: clk → econ → wr → blockade → prod → manpower → mr → pres. Data: `data/{economy,diplomacy,manpower-teaching}.json` → `GAME_DATA`.

---

### (prior) ⚡ CONTINUE HERE — S0 + S1a + S1b + S1c SHIPPED → S1d

> **S1c cotton/blockade/foreign just shipped + gated + committed.** `src/60-blockade.js` + `G.campaign.blockade`: the 1861 King-Cotton self-embargo trap (rev≈0), blockade strangulation (capture 0.10→0.33, ports fall, a dynamic `importFactor` that WIRED the S1b prod placeholder — off>full arms), cotton revenue→funds, foreign recognition (1862 famine window → foreclosed 1863) → `clock.intervention`; blockade-depth toggle (full/flat/off, R14); new 8th **Diplomacy** desk tab. Content-complete via an 11-agent research+verify+audit workflow → `data/diplomacy.json` (5 multi-voice cards + a 9-claim numbers audit that corrected Erlanger 72%→45% in `economy.json`) + `HISTORICAL-DATA-DIPLOMACY.md`. **VERIFIED:** probe-blockade 11/11 (16-turn arc), full no-regression suite green (desk 12/12, economy 8/8, production 4/4, diag-classic Classic paints, 0 pageerrors). **NEXT = S1d manpower/conscription** (`data/economy.json` → `manpower`): recruitment pools, the draft (US 1863 / CS 1862), the late-war **CS replacement-ratio collapse 1.0→0.1** (the war-ender), immigrant pool, enslaved-labor base (ties to S2 emancipation/contraband); feeds the pre-battle strength bridge (S5). New `src/70-manpower.js`, register a `manpowerOnResolve` tick. Then S2.

---

### (prior) ⚡ CONTINUE HERE — S0 + S1a + S1b SHIPPED → S1c

> **For a long unsupervised run, read `AUTONOMOUS-RUN.md`** — the operating manual for the self-perpetuating 12-hour build loop (prime directive, loop steps, phase roadmap, guardrails, recovery).

**Strategic-first build underway. Three milestones shipped + gated + committed locally this run (local commits only — Aaron: no GitHub push):**
- **S0 — President's-Desk owner shell** (commit `673894f`): zero-dep **build system** (`build/base.html` frozen run-h monolith + ordered `src/*.js` modules + `tools/build.mjs` → concat → splice before the engine-end anchor → in-memory parse/hex/collision gate → write `civil_war_generals.html`). `G.campaign.president` state (sibling of clock/muster/warroom; rides the save, no `_SAVE_VER` bump). War Department **expanded in place** into a 7-tab desk; between-battles **auto-surface/one-click-skip interstitial**. Cabinet = 4 engraved advisors (stub).
- **S1a — finance core** (commit `3a6aad6`): build is now **data-driven** (`tools/build.mjs` injects `data/*.json` as `GAME_DATA`). `data/economy.json` + `HISTORICAL-DATA-ECONOMY.md` from a citation-grade 8-agent research workflow. `src/40-economy.js` = 3 funding levers (bonds/taxes/press) with an **emergent inflation asymmetry** (US anchored ×1.13 / CS spiral ×87.5 over 12 turns ≈ historical ~90×; inflation→`clock.weariness`). "The Treasury" tab; Secretary delegated by default; teaching card.
- **S1b — war production** (commit `b2179d0`): `src/50-production.js` extends the War Room nodes → asymmetric matériel (US well-found; CS iron-ceiling + import + irreversible rail decay → ragged/hungry; hunger→weariness). Probe-verified 12-turn US rail100/equip100 vs CS rail42/equip24/arms4/food19%. War Production block in the War Effort overview.

**OPERATING PARAMETERS (Aaron-locked, run i — govern the whole run):** run the whole arc (S0→S5→tactical; stop only at unresolvable fork/blocker/done) · **always decide & log** (forks → `DECISIONS.md`, never pause mid-run) · **auto-commit AND push** at gated milestones (no remote yet → local only) · **content-complete each phase** (real data + PD images per system).

**TOOLCHAIN (proven this run):** `node tools/build.mjs` (then bootprobe/t1probe/probe-desk/probe-economy/diag-classic — START a `python3 -m http.server 8765` yourself + confirm 200 first; probes' auto-spawn races; bootprobe goto=60s). **READ the JSON in `tools/shots/` + the PNGs.** Gotchas: no literal comment-closer in JS block comments (bit the build script + a module — scan `src/` before building); bare-name globals `G`/`GAME_DATA` (never `window.G`); `portraitFor` → `window.portraitFor` from modules. Decisions: `DECISIONS.md`. Run detail: `RUN-LOG.md` §"run i".

**NEXT — finish S1, then S2 (all data already in `data/economy.json`; full plan in `AUTONOMOUS-RUN.md` §4):**
- **S1c cotton/blockade/foreign** — King Cotton self-embargo trap + blockade strangulation schedule + runner economics + Erlanger loan; foreign-recognition pressure inputs (feeds S2 diplomacy). Blockade-depth = toggle (R14).
- **S1d manpower/conscription** — recruitment pools, draft, the late-war CS replacement-ratio collapse (1.0→0.1) that ends the war; immigrant pool; enslaved-labor base (ties to S2 emancipation/contraband).
- Wire economy consequences into the pre-battle conditioning bridge (§3/§11) and the cabinet advisors (each secretary surfaces its domain's choices + teaches).
- **Then S2** — executive decisions + 3-layer morale + the 1864 election + the **advisor/cabinet system** (the linchpin R25: auto-manage + recommend + TEACH, per-domain delegation — the Treasury delegate toggle is the seed pattern).

**To continue:** read this CONTINUE-HERE block, then `DECISIONS.md`, `RUN-LOG.md` §"run i", `data/economy.json` + `HISTORICAL-DATA-ECONOMY.md`, and the design law `GRAND-STRATEGY-PLAN.md` (esp. §2 economy, §27 balance). Build S1b next with the same module+gate+probe+commit discipline. The full design orientation is below.

---

# HANDOFF — "The Civil War" — MASTER new-chat handoff (run h → next build)

**Written 2026-06-13 (run h, Opus 4.8). THE single entry point for the next session.** Personal project — NOT MJI. Full autonomy, Ultracode on, $0/free-CC0, build bar 200/100. **To start the next chat: paste the §PASTE block at the bottom.** Everything else here orients you.

---

## 1 · THE PROJECT IN ONE PARAGRAPH
"The Civil War" is becoming **the definitive teaching wargame: Ultimate General: Gettysburg's combat FEEL + Paradox-grade strategic DEPTH + a museum's historical RIGOR** — all three, no compromise. You are the **President (Lincoln or Davis)** running the entire war (economy, politics, diplomacy, morale, generals, the home front) AND commanding the battles in a real-time, gridless, drag-to-maneuver tactical engine — wrapped in a PhD-level history + alternate-history teaching layer where scholarly consensus is the *favored-but-not-guaranteed* path. **The governing design law (Aaron's central tension, resolved): the simple low-micro UG:G battle is the always-fun CORE that stands alone; ALL depth is optional — auto-managed by characterful advisors, delegable per-domain, preset-gated, fluidly dial-able; education is ambient, never blocks play, and confers real in-game mastery (so learning and fun pull the SAME direction).** One engine must serve the pure-battle player, the grand-strategist, and the PhD-historian simultaneously.

## 2 · WHERE THINGS STAND (run h, verified)
- **Classic (hex, turn-based) is FROZEN/finished** — the complete hex lineage + its 3D-skin "Modern." NEVER regress it (`tools/diag-classic.mjs` every splice).
- **Shipped + GPU-verified this run (all reusable):** procedural unit motion; **131 real PD-photo leader portraits** (`assets/portraits/`, license-gated, side-split for Anderson/Gregg) with a closure refactor + upgraded engraving fallback; a fuller period UI pass (drawn order-icons, engraved frames, brass portrait mat). Plus the inherited cinematic 3D foundation (PBR terrain, HDRI, ACES/bloom post-FX, weather, props, camera) and the adaptive period audio score.
- **The shipped "Modern" is only a 3D re-skin of the hex-turn engine** — it does NOT play like UG:G. The real-time tactical engine + the grand-strategy owner-mode are **NOT built yet** — that's the next work.
- **DESIGN: 63 decisions LOCKED** across a 35-round popup design session (Aaron). Fully captured in `GRAND-STRATEGY-PLAN.md` (Parts I–III). **No new-engine code written yet** — the next chat builds from the spec.

## 3 · THE DOCS (read in this order — you have zero prior context)
1. **`GRAND-STRATEGY-PLAN.md`** — THE DESIGN LAW. All 63 locked decisions: Part I (rounds 1–10: player/economy/executive/morale/alt-history/upgrades/education/victory/modes/first-build), Part II (rounds 11–20: diplomacy/slavery/politics/naval/map/generals/soldiers/intel/events/replay), Part III (rounds 21–33: **the depth↔simplicity balance** + presentation), §27 **the balance principle**, §13/§24/§28 decision indexes.
2. **`HISTORICAL-DATA.md`** — citation-grade content (211KB, 18-agent research): First Bull Run full OOB (Union + CSA), weapons/ballistics with ranges, USCT (structure/battles/criticism), the historiography framework, PD image sources. Feeds OOBs, weapon stats, debate cards, the codex.
3. **`MODERN-UGG-PLAN.md`** — the real-time tactical battle engine spec (built SECOND, after the strategic layer).
4. **`RUN-LOG.md` §"run h"** — what shipped + the pivot + this design session; skim §"run g"/§"run f" for the 3D foundation.
5. **`REVIEW-QUEUE.md` top** + **`GRAPHICS-RUN-CONTINUE.md` §TOOLCHAIN/GOTCHAS** — the splice/gate/real-GPU-shot toolchain + the bare-name-globals / async-THREE / override-by-redeclaration lessons.

## 4 · ARCHITECTURE DECISION (run h — important)
**Modularize via a zero-dependency build step.** The scope (50–100k+ lines) outgrows the single 14k-line file. Author the new engine in **source modules**; a tiny **node concat script** assembles the one shippable `civil_war_generals.html` (keep the $0/portable/open-and-play deliverable + `play.command`). Set this up FIRST in S0. Classic + the existing engine stay intact through the transition (move them into the build later, carefully, or keep the current HTML as the base the build appends to). Gate the BUILT output (parse/hex/Classic-regression). This replaces the append-only-single-file splice model for new work — but the gate discipline carries over.

## 5 · BUILD ORDER & PHASES (Aaron: STRATEGIC-FIRST, confirmed twice)
**Phase group S (grand-strategy owner-mode) FIRST, then phase group P (real-time tactical).** Battles use the EXISTING engine via a clean bridge until the UG:G engine swaps in.
- **S0 — scaffold:** set up the module/build system; expand the War Department (`G.campaign.{clock,muster,warroom}`) into the President's-desk shell (period war-room UI); strategic-turn loop; save. Layered scenario onboarding (start ~1861, limited toolset).
- **S1 — economy** (full asymmetric model + finance/inflation/blockade).
- **S2 — executive decisions + 3-layer morale + the 1864 election; the advisor/cabinet system** (the linchpin: auto-manage + recommend + TEACH, per-domain delegation).
- **S3 — alt-history engine** (tiered divergence + hinge-points + emergent toggle + curated scenarios; deterministic-by-performance with consensus framing; the "your war vs history" tracker).
- **S4 — education layer** (multi-voice debate cards + deep-dives + codex from HISTORICAL-DATA.md; ambient teaching; difficulty/realism sliders; play-style presets; full a11y).
- **S5 — victory/defeat** (asymmetric paths incl. **negotiated peace for both sides**, military-only hard-loss) + all campaign scopes; wire existing battles through the bridge → **playable full owner-mode war.**
- **THEN P0–P5** (`MODERN-UGG-PLAN.md`): the real-time UG:G tactical engine (First Bull Run vertical slice first), swapping in behind the bridge.
Each phase: gate + real-GPU self-verify (probes you READ) + Classic no-regression + left in a working, polished state (ship-quality iteratively).

## 6 · THE BALANCE PRINCIPLE (design law — apply to EVERY system)
The simple, low-micro UG:G battle is the always-fun core that works on its own. Every deep system is optional — auto-managed by characterful advisors, delegable per-domain, preset-gated, fluidly dial-able mid-campaign. Education is ambient, never blocks play, and confers real mastery (learning is rewarded by winning, not a chore-score). Presets + sliders let each player set their own balance. **If a feature forces depth on a battle-first player, it's wrong; if it denies depth to a strategist/historian, it's wrong.** (Full statement: GRAND-STRATEGY-PLAN §27.)

## 7 · TOOLCHAIN, GATES, GOTCHAS (proven — reuse)
- **Gates every change:** `.bak` → parse (`node --check` the built/extracted `<script>`) → invalid-hex grep `0x[0-9a-fA-F]*[g-z]` = 0 → collision-grep new fns = 0. With the new build step, gate the assembled output.
- **Real-GPU self-verify (READ the PNGs/probes):** `tools/shot-postfx.mjs`, `tools/shot-gpu.mjs`, `tools/shot-ui.mjs`, `tools/probe-motion.mjs`, `tools/probe-portraits.mjs`, `tools/bootprobe.mjs`, `tools/diag-classic.mjs`. Real GPU = **Intel UHD 617** via headed Chrome `--use-angle=metal`. A real-time sim/strategic loop needs an EMPIRICAL probe (sample state over frames), not just screenshots. Stale server: `lsof -ti tcp:8765 | xargs kill`.
- **Perf target:** smooth on the Intel UHD 617 at Auto tier; instancing/LOD/perf-tiers mandatory.
- **CRITICAL gotchas:** `G`/`HEX`/`PALETTE`/`THREE_BASE` are bare-name lexical globals (never `window.G`); THREE loads async (no top-level `new THREE.*`); override-by-redeclaration (last def wins); never a literal `*/` in a block comment; honor `reduceMotion` + `gfxQuality`; **NEVER regress Classic**; $0/free-CC0; Verified/Inferred provenance on all history.
- **Asset pipeline (historical run-H note; current top block supersedes live setup):** PD images via the run-h portrait pattern (Wikipedia REST summary → Commons license API PD-gate → download → `sips` resize). Extend to weapons/flags/USCT. Blender MCP remains available for future explicit 3D asset jobs (PolyHaven CC0 / Sketchfab), but Blender does not need to stay open unless a Phase-H asset task starts; free Hyper3D trial is drained.

## 8 · WORKING NORMS (Aaron)
- Full autonomy; build continuously; surface **questions only at a genuine design fork** — and Aaron LOVES **popup rounds of 3 questions, Recommended = option 1** (use AskUserQuestion). Aaron tests/feels much later — self-verify everything.
- Terse, direct; recommendation + reason on suggestions; push back on gaps; no emojis unless he uses one.
- The **modes & toggles, all-play-styles** spine governs everything; **protect everything (no cuts)** is the scope ambition (build iteratively, but aim to deliver the full vision).

## 9 · §PASTE (paste as the FIRST message of the new session)
> Build the next phase of "The Civil War" (personal project, NOT MJI; full autonomy, Ultracode on, $0/free-CC0, build bar 200/100). Read `HANDOFF.md` fully, then `GRAND-STRATEGY-PLAN.md` (THE design law — 63 locked decisions across Parts I–III + §27 the balance principle), `HISTORICAL-DATA.md` (citation-grade content), `MODERN-UGG-PLAN.md` (the tactical engine, built second), `RUN-LOG.md` §"run h", `REVIEW-QUEUE.md` top, and `GRAPHICS-RUN-CONTINUE.md` §TOOLCHAIN/GOTCHAS. The game is THREE-in-one: UG:G real-time combat feel + Paradox-depth owner-mode grand strategy (you are President Lincoln/Davis) + PhD-level history & alternate-history teaching. THE design law: the simple low-micro UG:G battle is the always-fun core that stands alone; ALL depth is optional (auto-managed by characterful cabinet advisors who also TEACH, delegable per-domain, preset-gated, fluidly dial-able); education is ambient, never blocks, and confers real in-game mastery. BUILD ORDER = STRATEGIC-FIRST: build the full grand-strategy Owner-Mode war + alt-history + teaching layer (GRAND-STRATEGY-PLAN §12 phases S0→S5) by EXTENDING the War Department (`G.campaign.*`), wrapping the existing battle engine via a clean bridge; the real-time UG:G tactical engine (MODERN-UGG-PLAN P0→P5, First Bull Run slice first) swaps in afterward. ARCHITECTURE: modularize now via a tiny zero-dependency node concat build step that assembles the single shippable civil_war_generals.html (set this up in S0); keep the $0/portable one-file deliverable; Classic (hex/turn) is FROZEN — never regress it (`tools/diag-classic.mjs` every change). Gate every change (`.bak` + parse + hex + collision on the built output); self-verify on the real GPU (Intel UHD 617, headed Chrome `--use-angle=metal`) with probes you READ; Aaron tests much later. Honor the bare-name-globals / async-THREE / no-`*/`-in-comments gotchas, the modes-&-toggles spine, Verified/Inferred provenance, and "protect everything (no cuts)." Confirm you've read HANDOFF.md + GRAND-STRATEGY-PLAN.md, give a 3-line plan for S0 (module/build scaffold + President's-desk shell), then build continuously; surface questions only at a genuine design fork (Aaron likes popup rounds of 3 with Rec first).
