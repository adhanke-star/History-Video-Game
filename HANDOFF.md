# HANDOFF — "The Civil War"

**⚡ AMENDMENT — 2026-07-21, D508: THE NINE-GAP ROAD EVIDENCE PASS RETAINS `NEEDS_MORE_RESEARCH`; LANE-019 IS CONTRACT/UNOWNED AND ROAD DATA, RUNTIME, AND GAMEPLAY REMAIN CLOSED.** `docs/design/strategic-road-transport-research-packet.md` now records 42 sources, 33 nodes, 15 candidate rows (13 Verified · 2 Inferred · 0 Disputed), 11 non-links, nine interchange candidates, and all seven D503 theaters. It resolves Stony Creek/Dinwiddie/Boydton, D503's Columbia/Goldsboro omission, the negative Louisville-Nashville-Chattanooga through-road question, and two separate Arkansas operations. Exact Williamsport, Sherman-limit, Meridian, and CT-30 evidence does not cure five remaining research families: Potomac crossings, Sherman intervals, New Orleans, Marshall-Shreveport, and exact CT-29/CT-36 rows. No runtime/data/tool/generated/save/gameplay byte moved. **EXACT NEXT:** no road implementation is authorized. A future fresh ChatGPT/Codex 5.6 Sol Ultra TOP LOOP may take standalone LANE-019 DRIVE only for a third docs/research pass limited to those five families; it must preserve D503 boundaries, owner separation, unassigned endpoints, operation-specific limits, and explicit non-links, then declare exactly one permitted verdict and release the lane. No simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D507: THE BOUNDED ROAD SOURCE PASS SHIPS WITH `NEEDS_MORE_RESEARCH`; ROAD DATA, RUNTIME, AND GAMEPLAY REMAIN CLOSED.** `docs/design/strategic-road-transport-research-packet.md` records 22 sources, 26 nodes, 11 candidate rows (7 Verified · 4 Inferred · 0 Disputed), 11 non-links, eight interchange candidates, all seven D503 theaters, and nine material gaps. Supported historical operations remain segment- or operation-specific; the Petersburg off-load point, Columbia/Goldsboro mapping, Arkansas second family, Marshall-Shreveport route, Missouri/Texas rows, and reusable-service evidence are incomplete. No runtime/data/tool/generated/save/gameplay byte moved. **EXACT NEXT:** LANE-019 is CONTRACT/unowned with the road verdict binding. No read-only or playable road slice is authorized. A future ChatGPT/Codex 5.6 Sol Ultra TOP LOOP may take a standalone DRIVE only under a new docs/research contract limited to the nine named gaps; it must not infer endpoints, compose operations into service, or edit road data/runtime. No simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D506: ARC 7 SLICE 2A SHIPS THE IMMUTABLE READ-ONLY TRANSPORT-EVIDENCE SUBSTRATE; TRANSPORT PLAY REMAINS CLOSED.** LANE-019 now carries exact injected evidence for 27 rail, 15 inland-water, two bounded sea, four interchange, and 18 non-link rows behind final no-UI fail-closed module 115. Build is `GATE OK` at data 65, territories 36/36, modules 111 with 115 last, and suite 140; focused plan/runtime are 11/11 + 11/11; adjacent board/accessibility/Mayhem/Custom Builder are 13/13 + 27/27 + 24/24 + 16/16 with clean artifacts and unchanged desktop/390 board screenshots. Both binds isolated one exact evidence-integrity red and restored data/game `7138a61b…`/`d278c30f…`; base/save shape held. No UI, state, save, movement, ownership, capacity, economy, AI, battle, or tactical behavior shipped. **EXACT NEXT:** LANE-019 is CONTRACT/unowned for D505's docs/research-only bounded road source pass; a fresh ChatGPT/Codex 5.6 Sol Ultra TOP LOOP takes DRIVE in a standalone committed ledger edit, authors only the citation-grade road packet and its declared verdict, and stops. No road data/runtime or transport gameplay is authorized; no simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D505: THE COMPLETE SLICE-2A TRANSPORT AUDIT IS DOCS-ONLY; LANE-019 IS AN UNOWNED CONTRACT FOR A READ-ONLY EVIDENCE SUBSTRATE.** D504 is `74237b9273f4640f031a8c46041a98e1d96809a3`. D505 accounts for all 29 D497 RE rows, all 29 D499 WE rows, and all 36 territories. Rail totals: 27 retained, 1 context-only (RE-28), 1 non-link (RE-29), 0 omitted; RE-06/22 also support bounded interchanges. Water totals: 15 inland retained, 2 bounded sea retained (WE-24/26 only), 1 interchange (WE-29), 5 non-link/omitted, 6 context-only. Verdicts: `SEA_READY_BOUNDED_ROWS_WE_24_WE_26_ONLY` and `ROAD_REQUIRES_BOUNDED_SOURCE_PASS`; roads have zero authorized service rows and a complete future research contract. **EXACT NEXT:** a fresh ChatGPT/Codex 5.6 Sol Ultra TOP LOOP takes LANE-019 DRIVE in a standalone committed ledger edit, then implements only `data/conquest-transport-evidence.json` (`cw_conquest_transport_evidence_v1`) plus final no-UI `src/115-conquest-transport.js`, manifest/build validation, two new focused probes, mechanically proved planning/Mayhem pins, generated HTML, and closeout docs. Exact IDs are 27 rail, 15 inland-water, 2 sea, 4 interchanges, and 18 non-links; D505 and LANE-019 carry the full mappings/fields/teeth. Expect data 65, modules 111 with 115 last, suite 140. It must deeply freeze or fail the whole substrate to `null`, create zero state/save/UI/gameplay behavior, pass the focused probes plus board 13/13, accessibility 27/27, Mayhem 24/24, Custom Builder 16/16, both isolated binds, base/save-shape/scope checks, commit/push, and post-commit planning gates 13/13 + 24/24. No road service, movement, ownership/control/condition, capacity, cut/repair, economy/reward, AI, battle, Chronicle, career, builder, or tactical effect; no simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D504: ARC 7 SLICE 1 SHIPS THE READ-ONLY 36-TERRITORY BOARD; LANE-019 MOVES TO DOCS-ONLY SLICE-2A LAW-DRAFT.** The D504 implementation commit containing this entry adds the exact 36-row D503 registry, assigns all 35 rail + 41 water + 2 Missouri battle-library anchors exactly once, enrolls fail-closed module 114 last, and exposes only an accessible read-only H0 board with the exact status `read-only foundation; conquest play not yet enabled`. Build is `GATE OK` at schema/data 64, registry 36/36, suite 140; focused/adjacent gates are plan 10/10, board 13/13, accessibility 27/27, Mayhem 24/24, Custom Builder 16/16 with clean fresh artifacts. Aaron's authorized Mayhem change is only the mechanical 63 -> 64 data pin plus D504 history. Bind A isolated the combined fail-closed tooth 1/13 and restored source/game MD5 `14a6ea…`/`ba68eb…`; Bind B isolated the provenance-integrity tooth 1/10 and restored data/game `7dc405…`/`ba68eb…`; frozen base remains `c9db83fa…`. **EXACT NEXT:** in the same ChatGPT/Codex 5.6 Sol Ultra TOP LOOP, perform docs-only Slice 2A: classify every RE-01..RE-29 and WE-01..WE-29 row against CT-01..CT-36, preserve all current owners and explicit non-links, decide the source-backed sea subset, choose `ROAD_REQUIRES_BOUNDED_SOURCE_PASS` unless exact road rows are genuinely supported, and author D505's smallest honest read-only substrate CONTRACT or bounded source-pass CONTRACT. Only `COORDINATION.md`, `DECISIONS.md`, `HANDOFF.md`, and a genuinely necessary non-design-changing §8 clarification may move. No runtime/data/tool/generated/save/frozen-base byte may move; no simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D503: AARON LOCKED ARC 7 PACKAGE A; THE FINAL CONQUEST LAW AND SLICE-1 CONTRACT ARE COMPLETE, AND RUNTIME HAS NOT BEGUN.** Aaron's answer was "execute option a." `docs/design/unlocked-but-judged-design.md` §8 now binds 36 exact operational catchments; 24 two-month turns plus a Jan-Apr 1865 endgame; sourced physical availability separated from player-authored territory/node/service receipts; territory control, node operation, and unowned service condition; separate rail/water/road/sea registries under one evidence envelope with explicit interchanges/non-links; Historical dated plausibility versus Mayhem turn-1 eligibility for source-supported services only; one movement/attack per army, bounded transport/cuts/repairs/bypasses; existing-economy muster/theater/directive rewards; Council/terrain/hex, save, AI, bridge/result, Chronicle, War Career, and Custom Builder boundaries; and the exact territory-board → transport → Council → terrain → hex → Chronicle ladder. **EXACT NEXT:** LANE-019 is CONTRACT/unowned for Slice 1 only. A fresh ChatGPT/Codex 5.6 Sol Ultra TOP LOOP takes DRIVE in a standalone committed ledger edit, then creates the 36-row source-backed registry and a read-only list-first/schematic accessible board shell under §§8.21-8.22. Allowed runtime files are exactly the new territory data/module/probes plus manifest, H0 menu, build validator, proven planning-pin updates, generated HTML, and closeout docs. Missing/malformed data or enablement yields zero entry-point trace. Slice 1 has no campaign start, movement/control writes, economy/rewards, Council, terrain assets, hex action, AI, resolver routing, Chronicle receipt, career effect, save field, casualty, winner, score, or tactical output. Create and green both probes in the implementation commit; prove the two isolated binds; run focused/adjacent gates; stop after Slice 1. The full 140-row battery is at the first playable transport-loop checkpoint. No simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D502: ARC 6 RESEARCH IS COMPLETE; ARC 7 RUNTIME IS HALTED BEHIND AARON'S DESIGN CHOICES.** Final adjudications are `ARC_6_RESEARCH_COMPLETE` · `ARC_7_REQUIRES_DEDICATED_DESIGN_LAW` · `ARC_7_RUNTIME_CLOSED`. D488 politics/election, D497 rail, D499 strategic water, and D501 medicine are all shipped final feeders; their remaining gaps remain explicit limits. D455 remains the sole conquest law-draft owner and now carries §8, the interim design-law process contract; eight stale mapping-table arc labels are corrected against its canonical arc list, with no product choice moved. Evidence can prove dated topology/control events and non-links but cannot select territory/hex grain, time/control, a common transport interface, capacity/economy/objectives, save, AI, or battle coupling. D159, blockade, diplomacy, Western Theater, bridge/D74, auto-resolve, Chronicle/divergence, War Career, tactical terrain, and Custom Builder ownership remain intact. LANE-019 `conquest-design-law` is CONTRACT/design-only and unowned; no runtime lock exists. **EXACT NEXT / HALT:** a fresh Aaron-led ChatGPT/Codex 5.6 Sol Ultra session presents 2-3 coherent architecture packages and obtains Aaron's locks for exact territory grain, turn/control model, shared-versus-separate transport interface, movement/capacity/cuts/repairs, campaign economy/reinforcement/objectives, hex interaction, save compatibility, and AI/bridge coupling. Extend only D455/D502 §8 into the final law, convert LANE-019 to a separate Slice-1 contract, commit/push docs only, run build/diff + both planning probes, and STOP before runtime. The maximum candidate Slice 1 after law is a fail-closed source-backed territory registry plus read-only accessible board shell; no movement, state writes, economy, objectives, terrain assets, resolver, AI, Chronicle, career, or save fields. No simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D501: THE MEDICINE/HOSPITALS/DISEASE PACKET IS SHIPPED WITH `READY_FOR_SOLDIER_DEPTH_LAW`.** `docs/design/medicine-hospitals-disease-research-packet.md` carries 38 historical source records and 36 claim rows (22 Verified · 14 Inferred · 0 Disputed), plus 10 dated care-chain categories, 12 narrative categories, 14 institutional cases (8 Verified · 6 Inferred), and 11 person cases (4 Verified · 7 Inferred). The evidence floor covers AOP-to-1864 ambulance reform, documented care/evacuation paths, camp exposure and separate disposition states, women in distinct roles, Black medical authority/labor, USCT unequal care, and refugee institutions; Chimborazo and western cases are bounded rather than forced into symmetry. Confederate western/Trans-Mississippi, refugee-camp primary depth, Company K files, and prison medicine remain explicit gaps. Company K examples may inform categories but are not strict named content until their underlying records are retrieved. D169, Human Cost, canonical people/primary-source owners, War Career, LANE-002, D497/D499, D74, and every protected runtime/data/probe/save/asset/generated surface hold; no probability, outcome table, treatment action, medical mechanic, personal fate, or joined transport schema is authorized. **EXACT NEXT:** D502, a separate docs-only ARC 6 release reconciliation and ARC 7 scope adjudication. Reconcile D455 and the four shipped feeder packets against existing strategic/tactical ownership; decide whether ARC 6 is complete and whether ARC 7 first needs a dedicated conquest design-law artifact. Do not choose consequential territory, turn-scale, economy, movement-capacity, objective-reward, hex, save, or implementation architecture; HALT for Aaron if committed law/evidence cannot settle a necessary choice. LANE-018 remains SHIPPED; LANE-002 5b retains S-04; no LANE-019 exists and no simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D500: ARC 8 JUSTIFIES ONE CITATION-GRADE MEDICINE/HOSPITALS/DISEASE PACKET; D169 STAYS THE SOLE STRATEGIC MEDICAL SYSTEM.** Adjudication is exactly `PACKET_JUSTIFIED`. D455 names the nonduplicative consumer—primary-source daily-life and person-safe wounding/medicine narratives for ARC 8 after War Career's participation guard—while D169 owns the aggregate `C.medical` ledger, medical-relief priority/caps, Campaign Kit teaching, formulas, saves, and UI; Human Cost owns aggregate gravity/mourning; women-in-war owns rich noncombatant/support records; LANE-002 owns `ss:` overlays. **EXACT NEXT:** author `docs/design/medicine-hospitals-disease-research-packet.md` as a separate D501 docs/research milestone. External historical research is authorized for that packet only under D500's complete source-register/claim-register/care-chain/narrative-category/institutional-case/individual-case/consumer-eligibility/dignity contract. Verified requires two independent non-tertiary families; named claims need claim-specific identity/service evidence; required coverage includes Letterman/ambulance transition, one documented care/evacuation path, disease exposure, convalescence/return/disability/discharge/death limits, women's institutional labor, and Black/USCT/contraband-refugee care, plus Confederate and western limitations where supported. Never create probabilities, outcome tables, treatment actions, hospital mechanics, personal fates from aggregate rates, duplicate canonical records, or `ss:` support-figure mappings. Verdict exactly one `READY_FOR_SOLDIER_DEPTH_LAW` / `NEEDS_MORE_RESEARCH` / `DO_NOT_MODEL`; no ARC 8 runtime. LANE-018 remains SHIPPED; LANE-002 5b retains S-04; no LANE-019 exists and no simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D499: THE ARC 6 STRATEGIC WATER-TRANSPORT PACKET IS SHIPPED WITH `READY_FOR_CONQUEST_LAW`.** `docs/design/strategic-water-transport-research-packet.md` carries 32 historical source records and 95 claim rows (41 nodes · 29 edges · 13 chokepoint/closure/reopening rows · 12 transfer cases; 54 Verified · 41 Inferred · 0 Disputed). Seven transfer cases independently cover D498's required families. The packet proves a bounded core while refusing false reach: Arkansas Post's cutoff route, Little Rock, Shreveport, Potomac capacity, Charleston/Savannah routine transfer, and Texas/Galveston remain limited or omitted; Fort Monroe arrival is not a Yorktown water edge; Mobile and Wilmington entrance/port/city states stay distinct. D159, blockade, Real Diplomacy, Western Theater, tactical naval research, D74, and D497 rail ownership all hold. Rail/water categories are compatible evidence only—no joined schema, territories, mechanics, values, runtime, data, probe, save, asset, or generated surface is authorized. **EXACT NEXT:** D500, a separate docs-only medicine/hospitals/disease scope adjudication choosing exactly one `PACKET_JUSTIFIED` / `EXISTING_RESEARCH_SUFFICIENT` / `NEEDS_PRELIMINARY_SOURCE_INVENTORY` after reconciling D169, the shipped disease-medical/human-cost/codex/women-in-war surfaces, D455 §§4f/6/7, both conquest packets, and LANE-002 read-only. Do not author the medicine packet in D500; external research is not yet authorized. LANE-018 stays SHIPPED; no LANE-019 exists; LANE-002 5b retains S-04; no simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D498: ARC 6 NEEDS ONE BOUNDED STRATEGIC WATER-TRANSPORT PACKET.** The battle-build naval/river packet already answers tactical buildability; shipped blockade + Real Diplomacy already own economics, runners, recognition, ports-by-year teaching, and foreign pressure; D159/Western Theater already own aggregate `N`-theater and river/rail readouts. The non-duplicative ARC 7 gap is dated water reach: navigable river/coast links, ports and landings, rail-water junctions, passage chokepoints, control/closure/reopening, amphibious transfers, and sourced limits. D498 therefore authorizes `docs/design/strategic-water-transport-research-packet.md` under a two-independent-family floor, with stable node/edge/case registers compatible with D497's evidence categories but no joined schema. Blockade status enters only when it changes transport reach. Naval combat, fleet gameplay/stats/AI, blockade formulas, territory design, movement values, and all runtime/data/probe/save/asset/generated changes stay closed. **EXACT NEXT:** a fresh docs/research session authors that packet under D498's full geography/date/source/schema/acceptance/exclusion/validation/HALT charter, commits and pushes it separately, and runs build + `git diff --check` + both planning probes. Medicine adjudication follows only after the water packet is clean and shipped. LANE-018 stays SHIPPED; no LANE-019 exists; LANE-002 5b retains S-04; no simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D497: THE ARC 6 STRATEGIC RAIL CONQUEST PACKET IS SHIPPED WITH `READY_FOR_CONQUEST_LAW`.** `docs/design/strategic-rail-conquest-research-packet.md` carries 26 source records and 73 claim rows (35 nodes · 29 edges · 9 operational cases; 61 Verified including one explicit D159 reuse · 9 Inferred · 3 Disputed). It supplies dated eastern/western/border/port/Trans-Mississippi candidate topology, explicit non-links and physical/interchange breaks, and cut/repair/reroute/bypass/concentration cases without selecting territories or gameplay values. Corrections preserved: Richmond-Petersburg's direct connection dates to Aug. 1861; Manassas Gap ends at Strasburg rather than Harpers Ferry; the Weldon cut degraded through a wagon bypass before later isolation; Texas/Trans-Mississippi routes remain incomplete and the T&NO Orange-removal claim is Disputed. D159 remains the sole aggregate logistics/bridge owner. No runtime/data/probe/asset/generated surface moved; suite 140, schema 63, `_SAVE_VER=1`, `MAYHEM_PUBLIC_READY=true`, and frozen base `c9db83fa99230ffb95bdfdfe059f3fb9` hold. **EXACT NEXT:** a fresh docs-only strategic naval/river/blockade scope adjudication. Reconcile the new rail packet, battle-build naval/river packet, shipped blockade/trade/diplomacy and D159 `N`-theater surfaces, strategic water reach/interchange evidence, and D455 §5-§7; then record whether `docs/design/strategic-water-transport-research-packet.md` is justified. Do not author the packet in the adjudication commit and do not design naval combat, ARC 7 mechanics, or mutate protected surfaces. LANE-018 stays SHIPPED; no LANE-019 exists; LANE-002 5b retains S-04. No simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D496: THE ARC 6 RAIL PACKET IS JUSTIFIED ONLY AS CONQUEST-TOPOLOGY RESEARCH.** D159 already ships aggregate rail asymmetry, repair/gauge/rolling-stock teaching, battle-ladder route friction, the capped opt-in bridge, save/UI behavior, and its 8-step suite guard; the battle-build library answers tactical buildability. The non-duplicative ARC 7 gap is dated territory-scale rail topology: candidate nodes/edges, termini and breaks, rail-river-port interchange, control/cut/repair change, and sourced operational validation. Rail and naval stay separate packets but must expose compatible evidence categories for a later ARC 7 shared-interface adjudication; neither packet may choose mechanics. No LANE-019 was created; no runtime/data/probe/asset/generated output moved; LANE-018 stays SHIPPED and LANE-002 5b retains its S-04 reservation. **EXACT NEXT:** fresh-session research-only authorship of `docs/design/strategic-rail-conquest-research-packet.md` under D496's full scope, source floor, schema, acceptance, validation concept, and HALT contract. External historical research is authorized for that packet only. Do not design ARC 7 runtime, choose the final territory graph, assign gameplay capacities/costs, or mutate protected surfaces; if the packet cannot finish in one session/provider, commit a Contract Relay lane before handoff. No simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D495: THE STANDING T4 BRIGADE FLAG-MARKER CANDIDATE IS STALE AND STRUCK.** D131/D197 already discharge the proposed procedural field-marker capability through T10: battle/date/theater/army-aware 2D+3D colours on every visible brigade, CVD-safe shape+label redundancy, selected-unit HUD treatment, fog-gated enemy identity, reinforcement/phase re-skinning, and texture/mesh disposal, with `probe-flags` owning the relevant historical/render/fog/purity/lifecycle teeth. T21/T24 add presentation depth but expose no missing flag-marker seam. The only distinct flag-related candidate is surviving-colours imagery, which stays CLOSED pending its own per-asset source/license/provenance/dignity/accessibility/media-budget charter. Flagship-unit stewardship state must not select or reveal tactical identity; a future presentation-only cross-reference may be read-only only. No LANE-019 was created; no runtime/probe/data/asset/generated output moved; suite 140 and `_SAVE_VER=1` hold. **EXACT NEXT:** fresh-session, docs-only scope adjudication for the ARC 6 strategic rail research packet (D455 §7's first remaining rail-before-naval dependency). Reconcile the shipped logistics/rail system and existing battle-build research first; do not research, charter implementation, or edit runtime until the adjudication proves a non-duplicative bounded packet. LANE-002 5b retains its S-04 fresh-context reservation; no simultaneous edits.

**⚡ AMENDMENT — 2026-07-21, D494: ARC 5 IS RELEASED; LANE-018 IS SHIPPED.** The full serialized 140-row no-regression checkpoint is green at battery code SHA `fc8ccc212babcc291739cc040f0fe9d07dc92fd7`. Its two real reds were root-fixed and separately shipped as D492 (`politics.json` closed-world schema enrollment, 63/63) and D493 (Mayhem's D491 data-count/applySave pins, 24/24). Antietam was the only environmental flake: the 360-second harness timeout produced no fresh artifact, so stale evidence was rejected; the isolated rerun passed 17/17 with zero pageerrors before resuming at Gettysburg. War-career passed 46/46 in 612.7s; visual-fidelity passed 49/49 in 363.7s. The cutoff audit read 140 fresh JSON artifacts: all parsed, all `ok:true`, zero failed steps, zero pageerrors. Suite 140 · schema 63 · `_SAVE_VER=1` · `MAYHEM_PUBLIC_READY=true` · frozen base md5 `c9db83fa99230ffb95bdfdfe059f3fb9`; all AD-7 pins held. **EXACT NEXT:** fresh-session, ledger-only charter of the standing T4 brigade flag-marker tail (Phase H1b; CVD-safe runtime-drawn markers; zero embed bytes). Do not implement until the new lane/lock and its acceptance contract are committed; LANE-002 5b remains reserved and ARC 6 packets remain session-fillers.

**⚡ AMENDMENT — 2026-07-21, D493: THE ARC 5 BATTERY'S SECOND REAL RED IS ROOT-FIXED AT `mayhem mode`.** The restarted run passed every earlier row through save-slots, then the one `EXCLUSIONS + BASELINES + SUITE` tooth correctly caught two stale pre-D491 pins: data count 62→63 and the additive `applySave` signature `201fa746ea8e8755`→`820f02da7a3e6341`. Only those two values moved; the tooth still independently binds frozen base, `_SAVE_VER=1`, roster/builder counts, suite 140, and row placement. Mayhem reran 24/24 and adjacent save-slots 17/17 with zero pageerrors; node check and build GATE OK passed. **EXACT NEXT: after this fix is committed, rerun the mayhem plan probe, then resume `export TMPDIR="$PWD/.tmp" && node tools/vet-no-regression.mjs --from="mayhem mode"` serialized and ALONE through row 140; read every current artifact JSON; only full green flips LANE-018 SHIPPED.**

**⚡ AMENDMENT — 2026-07-21, D492: THE ARC 5 BATTERY'S FIRST REAL RED IS ROOT-FIXED AT `data schemas`.** `politics.json` parsed but the closed-world schema validator correctly refused it as unclassified (62/63); the narrow fix enrolls its three shipped top-level owners in the meta-family map. The failed row reran 63/63; both adjacent importers passed; node check, build GATE OK, and diff check passed. Runtime/data/pins hold; suite 140, `_SAVE_VER=1`, and frozen base `c9db83fa` hold. LANE-018 remains VERIFY/unowned. **EXACT NEXT: rerun `export TMPDIR="$PWD/.tmp" && npm run vet:noreg` from the beginning, serialized and ALONE; read every current artifact JSON; root-fix reds at exact labels; only full green flips LANE-018 SHIPPED.**

**⚡ AMENDMENT — 2026-07-21, D491: LANE-018 SLICE 3 IS SHIPPED — THE 1864 ELECTION BIND.** New `data/politics.json` + `src/74-politics.js` carry the two-cycle teaching/data contract, the US/CS mirror, and the one bounded post-clock interlink. The 1864 press term applies only in the pre-election 1863 window at ±4 weariness; the 1862 card permits ±1 capital; neutral 50, absent config, non-US, out-of-window, and resolved paths are exact zero; each cycle is one-shot. Clock election ownership, press ownership, D113, D74 outputs, `_SAVE_VER=1`, suite 140, and frozen base all hold. Owner teeth economy 10/10 · divergence 16/16 · endings 26/26 · command 100/100 · save-slots 17/17; adjacents press 10/10 · morale 10/10 · afteraction 19/19; all artifacts `ok:true`, zero failed steps/pageerrors. Both binds bit exactly and restored md5-identically. Schema 62→63. LANE-018 is VERIFY/unowned. **EXACT NEXT: SLICE 4 — `export TMPDIR="$PWD/.tmp" && npm run vet:noreg`, the 140-row ARC 5 release battery serialized and ALONE; war-career 900s, visual-fidelity 600s; read every artifact JSON; root-fix reds at exact labels; only full green flips LANE-018 SHIPPED with the battery SHA. No simultaneous edits.**

## ⚡ HANDOFF — 2026-07-19 (morning) **D467: ARC 3 IS OPEN — LANE-014 `graphics-uplift` FULLY CHARTERED AT DRIVE (`489cfc4`) AND SLICE 1 SHIPPED (`6f62352`): THE ASSETS3D PROVENANCE WALL — ALL 30 STAGED 3D FILES VERIFIED BYTE-IDENTICAL TO IDENTIFIED POLY HAVEN CC0 ORIGINALS.** The charter (Aaron's queue pick (a); the COORDINATION §4 TOP-LOOP resolution recorded: Fable 5, Aaron-confirmed over the kickoff's Opus 4.8 routing note): six slices (asset ledger/license audit → terrain texturing T32 → HDRI sky/lighting T33 → ground-level camera T34 → formation density/LOD + Tripo slot enablement → the release-checkpoint battery), TEN adjudications (THREE pinned at 0.128.0 — the frozen base owns THREE_BASE incl. the already-enrolled GLTFLoader/RGBELoader; the progressive-enhancement fail-closed law — asset/CDN absence ⇒ byte-identical current visuals, offline single-file untouched; graphics bytes BARRED from the zero-headroom embed budget — assets/3d gets its OWN ledgerClasses wall; analytic terrain heights inviolate — the vertex-Y==fldTerrainH tooth stays green UNAMENDED all arc; the suite stays 138 through the arc — every tooth lands in an existing graphics row; AD-7 plan-probe re-pins ride EVERY slice; the perf caps are LOCKED at 360 calls/1400 objects; the T-module wrap-by-reassignment seam law — new modules T32+, T0 bodies and frozen base never edited; Aaron-gated model production that never blocks the lane), and two packet §7 ERRATA recorded (the mapping table's "ARC 3 loot content packs" cell + the R-7 rider both route to ARC 4 per the arcs-list backbone). SLICE 1: charter recon (`wf_1c520994-6d8`) surfaced ~98 MB of terrain PBR textures tracked in the PUBLIC repo with no recorded provenance — the audit CLEARED it: the committed `tools/fetch_polyhaven_terrain.mjs` asset map + the live api.polyhaven.com md5+size records matched ALL 27 terrain maps byte-for-byte at 1k, and the 3 unattributed env HDRIs were identified by md5 search across the full 980-asset Poly Haven catalog (sky_day = syferfontein_18d_clear_puresky · sky_dusk = belfast_sunset_puresky · sky_overcast = overcast_soil_puresky, all 2k) — 30/30 Verified CC0-1.0 with named authors, NOTHING removed. Ships: the 30-row `assets/3d/provenance.json` · `data/media-budget.json` 1.8→1.9 (the requireAssets3dProvenanceLedger policy + the hd-terrain-models ledgerClasses caps pinned to the audited totals — terrainTextures 27/102,540,283 B · envHdri 3/13,324,229 B · unitModels/modelSources 0-until-chained per adjudication 10) · `tools/probe-media-budget.mjs` 13→17 steps (1:1 enumeration BOTH ways + out-of-class refusal · per-row license-clear/Verified/authors + a FULL md5+byte re-hash of the tree every run · class caps + undeclared-class refusal · the outside-the-embed-pipeline structural guard) with an assets3d metrics readback · the README provenance-law section, the 2K→1k factual correction, and the adjudication-10 MODEL SOURCING PLAN. Binds A (license-flip) and B (unledgered fixture) bit at EXACTLY their steps with md5-proven restores; node --check ×5 · build GATE OK · schema 60/60 · focused 17/17 (embed metrics byte-unmoved, soft-warning only) · adjacents import-tripo exit 0 + report 17/17; the AD-7 re-pins rode the commit (game f0228c4b→11099dac · dataTree e33afffc→23ccef52 at all three plan-probe sites; base/manifest/suite/srcTree/focused hold) and BOTH plan probes re-ran green post-commit (13/13 · 24/24). Counts hold 27/60/1,632/138; `_SAVE_VER=1`; frozen base `c9db83fa` untouched; ZERO src moved. Aaron's other picks this session: drive on Fable 5; the D460 Drew's-regiment field-both adjudication stays STANDING. **LANE-014 rests at CONTRACT (unowned — the lock released at this closeout, the D171/D307 law; the ratified contract + all ten adjudications live in the COORDINATION.md lane entry and are THE law for slices 2-6). EXACT NEXT: SLICE 2 — terrain texturing (new module T32) per the committed LANE-014 contract IN FULL: audited albedo (+normal/rough ONLY if the chosen material supports them inside the perf walls) applied to the `fld3dBuildTerrain` ground mesh keyed to the 9 analytic region predicates (clear/field/woods/hills/ridge/town/road/swamp/fort), BLENDED with the preserved T18 grain + T21 AO vertex-colour passes (their latches/teeth stay green or re-pin with documented chains in the same commit); a Lambert→Phong/Standard material upgrade ONLY with profile evidence; low-tier off-or-one-cheap-map; vertex Y untouched; fail-closed byte-identical current ground when the texture fetch is absent/blocked; teeth in probe-terrain-readability + probe-visual-fidelity + probe-tactical-visuals; ≥2 predeclared binds; browser probes SERIALIZED on the 8 GB Mac or run in a full-access non-Seatbelt cloud session. The next session takes DRIVE with a committed edit BEFORE any code move.** *[AMENDED in-flight 2026-07-19 (afternoon), D468 — the all-day ladder P1: SLICE 2 IS SHIPPED. T32 terrain texturing landed per the contract clause verbatim (the region-keyed audited-albedo bake on the Lambert ground map; fail-closed off/low/file-protocol/absent; T18/T21 passes preserved and showing through; teeth in probe-terrain-readability 35/35 + probe-visual-fidelity 28/28 + tactical-visuals 10/10; binds A/B md5-proven; AD-7 re-pins game→9fca6932 · manifest→bf29b44f · srcTree→03c2cdba; counts hold 27/60/1,632/138; LANE-014 DRIVE held by Claude Code (Fable 5) for the day ladder). EXACT NEXT: the ladder's P2 — the CRATER RUNTIME (charter its lane ledger-only FIRST, then ONE atomic commit per the D464 spec §8-§9: data/crater.json with one-family strengths 8,500/6,100, the Mahone Brig. Gen. lock, the no-Connecticut roster law, the blast as true starting state, schema 60→61, scenarios 27→28, suite 138→139 appended at the END, +3×units at every register pin site per the spec §8 inventory, probe-crater.mjs authored WITH the commit (probe-cold-harbor the template, ≥2 binds), and the adjudication-2 forbidden-scan flips in the petersburg/spotsylvania/wilderness plan probes in this commit only — plus the D466 lesson's sibling sweep: bare tokens AND regex-source forms, registry-order and fldScnBtn_ DOM chains tree-wide).]* *[AMENDED in-flight 2026-07-19 (afternoon), D469 — the all-day ladder P2: THE CRATER RUNTIME IS SHIPPED (LANE-015 chartered at `2b19f20`, the runtime in ONE atomic commit per the D464 spec §8-§9 verbatim). Scenario 28 at rank 71.5; schema 61; suite 139 (crater at the END + a 600s budget); register 1,632→1,671 at every site; probe-crater 20/20 with the direction battery CS-holds 6/8 AND US-bleeds 8/8 at the fair baseline and the machinery tooth stamping battleId `crater` directly; binds A (Mahone tamper)/B (single-source card) md5-proven at 5a5e5132; the adjudication-2 flips (crater token out, fort-stedman/overlandCampaign KEPT, disk scans flipped to presence) + the Kennesaw→Atlanta→Crater→CedarCreek→Franklin re-pins riding the commit; AD-7 game→1757fdbf · dataTree→3cd4ccb2 · srcTree→bab9cca1 · suite→7b36f51e · focused→bb7a1bc9. LANE-015 at VERIFY pending the P7 battery. EXACT NEXT: the ladder's P3 — the OLUSTEE RUNTIME per the D465 spec §8-§9 (rank 65.5 slotting chattanooga 65 → fortPillow 66, whose rank-66 chain + chattanooga-adjacency teeth re-pin with documented chains IN the commit; the honest Union defeat from schedule inputs; the Colquitt/Barton/Reed rank locks with Reed's death as the Feb 26/27 spread; the dual-designation law — the 6th USCHA trap; the contested-aftermath teaching law; 8th USCT 49/188/73=310 of 565; counts move 28→29 · 61→62 · suite→140 with documented chains at every pin site; probe-olustee.mjs authored WITH the commit, ≥2 binds; AD-7 re-pins + plan-probe re-runs after) — completing C3 ENTIRELY.]* *[AMENDED in-flight 2026-07-19 (evening), D470 — the all-day ladder P3: THE OLUSTEE RUNTIME IS SHIPPED AND C3 IS COMPLETE (Fort Pillow D463 · the Crater D469 · Olustee D470 — the full D455 SS3 massacre-treatment family is playable). LANE-016 chartered at `5dbda85`, the runtime in ONE atomic commit per the D465 spec §8-§9 verbatim: scenario 29 at rank 65.5; schema 62; suite 140 (olustee at the END + 600s); register 1,671→1,710 at every site; probe-olustee 20/20 with the direction battery CS-breaks 7/8 AND US-bleeds 8/8 at the fair baseline (the honest defeat emergent from the piecemeal schedule) and the machinery tooth at battleId `olustee` direct; binds A (Colquitt tamper)/B (single-source card) md5-proven at 75f32bf6; the chattanooga→olustee→fortPillow re-pins across probe-fort-pillow's rank regex, the spotsylvania/wilderness registry-order chains, and the kennesaw/spotsylvania/wilderness DOM chains; AD-7 game→21a5216d · dataTree→4bbdebe5 · srcTree→4564d84d · suite→cf5de9f6 · focused→78633570. LANE-015 + LANE-016 at VERIFY pending the P7 battery. EXACT NEXT: the ladder's P4-P6 [IF-ROOM] LANE-014 slices (HDRI sky T33 → ground camera T34 → formation LOD), shedding from the BACK as room requires, then P7 — the MANDATORY day-end battery (suite 140, TMPDIR set, serialized, ALONE, war-career 900s, every artifact JSON read, reds root-fixed at exact labels; on green flip LANE-015/LANE-016 SHIPPED and LANE-014 per its slice state).]* *[CLOSEOUT 2026-07-19 (night): Aaron called the stop mid-battery. FINAL STATE: D468+D469+D470 all SHIPPED and pushed; C3 COMPLETE; LANE-014 at CONTRACT (slices 3-6 remain, lock RELEASED); LANE-015/LANE-016 at VERIFY (locks RELEASED — runtimes shipped, only the battery-gated ledger flip remains). THE BATTERY: ~87/140 rows green across the resumed segments with ZERO unresolved reds — three root-fixes landed at exact labels (`38451fb`/`a622485`/`2e82956`: probe-war-career's suite-count tooth 138→140, BOTH the named check AND the silent list.length conjunct in result.ok — the D443 AD-6 class; plus a turn-one-smoke slow-Mac timeout and a war-career net::ERR_INTERNET_DISCONNECTED CDN blip, both re-run green). EXACT NEXT: finish the battery `export TMPDIR="$PWD/.tmp" && node tools/vet-no-regression.mjs --from="render richness"` ALONE/serialized, read EVERY artifact JSON, root-fix any red at its exact label (never weaken); on full green flip LANE-015+LANE-016 SHIPPED with the battery SHA, then LANE-014 slices 3-5 per the committed contract (T33 HDRI sky → T34 ground camera → T24 formation LOD) and slice 6 (the ARC 3 release battery).]* *[AMENDED in-flight 2026-07-19/20 (night), D471 — the post-D470 ladder P1: THE BATTERY IS COMPLETE AND THE FLIPS ARE DONE. The resumed segments ran to the END (every row exit 0, EVERY artifact JSON ok/0-pageerrors, "VET NO-REGRESSION OK") at battery SHA `c9934a0`. Three root-fixes at exact labels: `4e4593f` RUNTIME — T23's async GLB template-arrival apply routes through the PUBLIC wrapped fld3dSyncUnit seam, closing the real one-frame orphan-figures window the tripo hideBaseMarker tooth caught (frame-traced; AD-7 game→b26238de · srcTree→cc403e85 at all plan-probe sites); `b1c4a4a` probe-kennesaw's REGISTRY-order chain gains Olustee and `c9934a0` probe-franklin's gains Crater (both the D466 two-teeth-one-swept class; a programmatic tree-wide sweep of every registry-indexOf + fldScnBtn-DOM adjacency pair against the live 29-battle order proves ZERO stale sites remain). One environment flake (vicksburg 360s in-battery timeout after a 538s row) re-run green alone at 116.8s. **LANE-015 + LANE-016 are SHIPPED at `c9934a0` (D471) — ARC 2 / C3 FULLY RELEASED.** Counts 29/62/1,710/140 · `_SAVE_VER=1` · frozen base `c9db83fa` untouched · AD-7 head: game b26238de · dataTree 4bbdebe5 · srcTree cc403e85 · manifest bf29b44f · suite cf5de9f6 · focused 65e9c873. EXACT NEXT: LANE-014 SLICE 3 — HDRI sky + derived lighting (new module T33) per the committed contract clause IN FULL (take DRIVE with a committed ledger edit BEFORE any code move; the audited env HDRIs via the already-enrolled RGBELoader keyed to weather/time; the vfSky dome gains the equirect map with the live fog tint preserved; light COLOURS become HDRI-derived PRECOMPUTED constants; NO PMREM/IBL; low/reduceMotion static tint; HDR absent ⇒ gradient dome byte-identical; owner probe-visual-fidelity, adjacents probe-weather + probe-atmospherics; ≥2 predeclared binds md5-proven; T32's loader/fail-closed idiom the template; AD-7 re-pins + both plan probes re-run post-commit).]* *[AMENDED in-flight 2026-07-20, D472 — the post-D471 continuation ladder P1: SLICE 3 IS SHIPPED. DRIVE re-taken at `5d6d955` (the §4 resolution: Fable 5) BEFORE any code move; the FIRST CODE MOVE smoke proved raw RGBE renders-but-near-night in the r128 linear pipeline (92 sky colours, 0 warnings), so the WIP bank (`lane-014-s3-wip` 06200ba, deleted after landing — the D463 pattern) was adopted with TWO logged amendments: T33 pre-decodes each HDRI once to an LDR canvas (linear × precomputed EXPOSURE day 0.336/dusk 0.590/overcast 0.645 → gamma-2.2 LUT → CanvasTexture; below-horizon fades to white so the under-horizon dome keeps today's exact look), and the dusk LIGHTS row was RECOMPUTED under the one reproducible rule (derived 50/50 authored-canonical-palette; the bank's dusk row reproduced under no rule) — every constant reproduces from the shipped `tools/derive-hdr-palette.mjs` (day #feefc7/#dee0dc/#55534d · dusk #ffdb9e/#dfdef0/#5e4f44 · overcast #efeadf/#d3d5d5/#57524a; intensities NEVER touched, capture/restore on detach). Teeth: probe-visual-fidelity 28→39 (T33 purity scan · wrappers/carry · day-attach presence · injected dusk + rain→overcast keying · derived-colour constants pinned as literals · intensities-untouched · reduceMotion detach/restore · low no-map · route-BLOCKED-FIRST fail-closed scene · off-inert; the existing matchesFog tooth = the coupling tooth, green UNAMENDED; 39/39 green, artifact read, 6 scenes 0 pageerrors/0 texture warnings) + a documented 600s vf timeoutFor line (292s standalone, the D454/D471 slow-row class). Binds A (missing day .hdr: presence + derived-colours red EXACTLY, blocked-scene fail-closed tooth GREEN)/B (post-render dome-colour overwrite: matchesFog red EXACTLY) md5-proven. Adjacents probe-weather + probe-atmospherics green. AD-7 re-pins game→c72c7585 · manifest→2fdf5fb3 · srcTree→b0a88e93 · suite→69681d6f at all four plan-probe sites (base/dataTree/focused hold); both plan probes re-run green post-commit. Counts hold 29/62/1,710/140. EXACT NEXT: the ladder's P2 — LANE-014 SLICE 4, the ground-level camera (new module T34) per the committed contract clause IN FULL: settings-gated mode default OFF = today's orbit byte-identical; brigade-follow/ground-inspect through the existing OrbitControls seam + the two T0 reposition sites (~2585/~2605, wrapped never edited); camera y clamped ≥ fldTerrainH + eye height; keyboard-operable; reduceMotion ⇒ no damping/auto-glide; addons only from the same three@0.128.0 tree (prefer none, no pointer lock); owner probe-visual-fidelity, adjacent probe-tactical-visuals (sim byte-identity across the render burst); binds: clamp-break red EXACTLY · default-on red EXACTLY; AD-7 re-pins + both plan probes re-run post-commit.]* *[AMENDED in-flight 2026-07-20, D473 — the post-D471 continuation ladder P2: SLICE 4 IS SHIPPED. T34 ground-level camera per the contract clause verbatim: `G.settings.groundCam==="on"` gates the whole layer (DEFAULT OFF — the probe dispatches T+arrows and proves camMoved 0, params held, mode never activates); when on, T enters ground inspect at fldTerrainH+EYE(22) or brigade FOLLOW behind a selection (BACK 90 along facing; reduceMotion ⇒ JUMP, never glide); arrows walk/turn (T + arrows are free of the whole T30 action table; the listener defers to editable targets and consumed keys); everything through the EXISTING OrbitControls in PARAMETER MODE (minDistance 120→8, maxPolarAngle 0.47π→0.55π, restored on exit; NO addon, NO pointer lock, enableDamping NEVER written); the per-frame clamp floors camera.y at terrain+EYE; T again restores the EXACT pre-entry camera/target/params (float-exact); the two wrapped T0 reposition commands (_fld3dReaimPhase/fldCamFrameSelected) DROP the mode and win. Teeth: probe-visual-fidelity 39→49 (T34 purity scan incl. no-pointerLock/no-addon/no-damping-write · 4 wrappers+carry · default-OFF byte-identity · eye-height enter · keyboard walk/turn · clamp-floor recovery · exact exit restore · follow+rm-jump · reposition authority · active-mode sim byte-identity; 49/49, 7 scenes, 0 pageerrors/0 texture warnings, ~416s inside the D472 600s budget). Adjacent probe-tactical-visuals 10/10. Binds A (clamp disabled → EXACTLY the clamp tooth red, y 13.45 < floor 36.25) and B (default-on → EXACTLY the default-OFF tooth red, camMoved 607.8) md5-proven. AD-7 re-pins game→584e5c6f · manifest→4625dca9 · srcTree→a7d2eef4 at all four plan-probe sites (base/dataTree/suite/focused hold); both plan probes re-run green post-commit. Counts hold 29/62/1,710/140. EXACT NEXT: the ladder's P3 [IF-ROOM] — LANE-014 SLICE 5, formation density/LOD (T24 extension) per the committed contract clause IN FULL: distance-based figure LOD (near tier richer instanced detail; far tier current; fldLow() tier UNCHANGED), INF_CAP density revisited INSIDE the locked walls (≤360 draws/≤1400 objects low tier), the E20 no-per-frame-instanceColor-upload guard preserved, pegs/slab/GLB three-way residency intact; Tripo slots FIXTURE-ONLY (adjudication 10 — real model production Aaron-gated, do NOT generate/download/enable a real slot); owners probe-formation-figures + probe-tripo-unit-assets + probe-intel-uhd617-profile (caps green on kennesaw 17); note the 4e4593f T23 seam fix when touching the interplay; binds: near-LOD-resident-at-low-tier red EXACTLY · license-pending-slot-enable red EXACTLY; then P4 — SLICE 6, the ARC 3 release battery (suite 140, TMPDIR set, serialized, ALONE, war-career 900s, every artifact JSON read, reds root-fixed at exact labels; on green flip LANE-014 SHIPPED with the battery SHA, or leave the lane per its slice state if P3 shed).]* *[CLOSEOUT 2026-07-20 (D474+D475): P3 WAS SHED FROM THE BACK (context safe-stop law) AND THE MANDATORY DAY-END BATTERY IS COMPLETE — 140/140 ACROSS DOCUMENTED RESUMED SEGMENTS AT SHA `a7c9e7e`. Rows 1-72 green in the first leg (at the D473 tree); ONE red at row 73 `field sandbox` root-fixed at its exact label (`a7c9e7e` D474 — probe-field's GEA-03 tooth scans String(fldCamFrameSelected) and saw T34's legitimate wrap; T34's wrapped reposition commands now expose the underlying command as `_gcDelegate` (the GEA-05 delegate idiom) and the tooth scans the delegate PLUS a new wrap-without-delegate failure conjunct — strengthened, never weakened; the D443/D453/D471 class, fourth instance, tree-wide sweep proves the only site); the resumed leg `--from="field sandbox"` ran rows 73-140 to the END — 68/68 exit 0, 68/68 artifact JSONs ok/0-pageerrors, zero timeouts, "VET NO-REGRESSION OK", a direct sweep of every shots artifact zero-red; visual fidelity (49/49) + render richness re-verified AGAINST the fixed build. SLICES 3-4 ARE VERIFIED AT RELEASE GRADE. **LANE-014 rests at CONTRACT (slices 1-4 SHIPPED · slices 5-6 remain · unowned — the DRIVE lock RELEASED at this closeout).** AD-7 head: game bcfd6454 · dataTree 4bbdebe5 · srcTree 8395da0a · manifest 4625dca9 · suite 69681d6f · focused (probe-war-career) 65e9c873; counts 29/62/1,710/140; `_SAVE_VER=1`; frozen base `c9db83fa` untouched. EXACT NEXT: LANE-014 SLICE 5 — formation density/LOD (T24 extension + T23 fixture-only slot enablement) per the committed contract clause IN FULL (take DRIVE with a committed ledger edit BEFORE any code move; distance-based figure LOD — near tier richer instanced, far tier current, fldLow() tier UNCHANGED; INF_CAP revisited INSIDE the locked walls ≤360 draws/≤1400 objects low tier; the E20 no-per-frame-instanceColor-upload guard preserved; pegs/slab/GLB three-way residency intact; Tripo slots FIXTURE-ONLY, adjudication 10 — real model production Aaron-gated, never generate/download/enable a real slot; owners probe-formation-figures + probe-tripo-unit-assets + probe-intel-uhd617-profile with caps green on kennesaw 17; NOTE the 4e4593f T23 async-seam root-fix when touching the T23/T24 interplay; binds: near-LOD-resident-at-low-tier red EXACTLY · license-pending-slot-enable red EXACTLY; AD-7 re-pins + both plan probes re-run post-commit), THEN slice 6 — the ARC 3 release checkpoint battery that alone flips this lane SHIPPED.]* *[AMENDED in-flight 2026-07-20, D476 — the post-D475 ladder P1: SLICE 5 IS SHIPPED. DRIVE re-taken ledger-only at `4ab1b43` (the §4 resolution: Claude Code / Fable 5) BEFORE any code move. T24 distance LOD per the contract clause verbatim: the near set lives in its OWN scene-level `ffNearLayer` shared instanced group (7 meshes — 8-seg bodies/kepis, 10×7 heads, rifles/bayonets, knapsacks, bedrolls) so the far layer keeps EXACTLY 5 meshes and both layerMeshCount==5 teeth stayed green UNAMENDED; density uplift NEAR_CAP 66 vs far INF_CAP 42 unchanged (a 4th near rank keeps the line compact; the far layout is byte-current at n≤48); NEAR_IN 430/NEAR_OUT 490 hysteresis; tier flips zero the vacated slots; fldLow() ALWAYS far — the low tier builds no near layer (the walls hold by construction: uhd617 26/26, kennesaw low 117≤360 calls · 169≤1400 objects, E68 values untouched); lazy build (+7 high-tier draws only); E19 reset + fldExit dispose/null BOTH layers; E20 latch extended to the near set; sim byte-identity proven across a burst finishing IN the near tier; the near-tier screenshot visually verified. T23 runtime license wall (the enablement leg, FIXTURE-ONLY per adjudication 10 — canonical pack untouched, 0 slots enabled, nothing generated/downloaded): fldUnitGlbSlot refuses enabled records without license.status=clear (licenseBlocked stat); fixture-proven — the pending-enabled record listed FIRST never requests/loads/attaches, the clear record attaches (modelId proof). envMap NOT taken (profile-evidence-only clause; none gathered). Teeth: probe-formation-figures 19→23 · probe-tripo-unit-assets 14→17 (the three-way residency tooth strengthened with the near-park conjunct) · artifacts read 0 pageerrors/0 texture warnings · import gate exit 0. Binds A (near set force-built at low tier → EXACTLY the LOD-low-branch tooth red, 22/23) and B (canonical records[0] enabled with pending license → the import gate red EXACTLY at "enabled records require license.status=clear", exit 1) — restores md5-proven (T24 01ff23b5 · game a234c52a · data 95cd0c93), re-runs green. AD-7 re-pins game bcfd6454→a234c52a · srcTree 8395da0a→7cc295df at all four plan-probe sites (base/dataTree/manifest/suite/focused hold); both plan probes re-run post-commit. Counts hold 29/62/1,710/140; `_SAVE_VER=1`; frozen base `c9db83fa` untouched. EXACT NEXT: the ladder's P2 — LANE-014 SLICE 6, the ARC 3 RELEASE CHECKPOINT: the full serialized battery (suite 140; `export TMPDIR="$PWD/.tmp"`; `npm run vet:noreg` ALONE on the machine; war-career 900s; read EVERY artifact JSON — ok + 0 pageerrors; reds root-fixed at their exact labels, slow-Mac timeouts/CDN blips re-run first per the D454/D471/D474 precedent; probe-visual-fidelity is a documented 600s slow row). On green flip LANE-014 SHIPPED with the battery SHA — ARC 3 CLOSES.]* *[AMENDED in-flight 2026-07-20, D477 — the post-D476 mega-ladder P1: THE BATTERY IS COMPLETE AND ARC 3 IS CLOSED. The full 140-row suite ran serialized/ALONE at SHA `7ac44aa` across THREE documented legs (rows 1-101 · rows 102-108 · rows 109-140, "VET NO-REGRESSION OK"); two reds were both proven environment flakes re-run green ALONE with ZERO tree changes (row 102 tactical-roster screenshot 30s timeout after a 542s row → 8/8; row 109 antietam 360s in-battery budget → 77s/17 steps — the D454/D471 slow-Mac class, third and fourth instances); the battery-window sweep read 139 artifact JSONs — every one ok, ZERO pageerrors. **LANE-014 IS SHIPPED IN FULL (all six slices: D467 · D468 · D472 · D473 · D476 · this battery) — ARC 3 IS CLOSED AND FULLY RELEASED.** Counts hold 29/62/1,710/140 · `_SAVE_VER=1` · frozen base `c9db83fa` untouched · AD-7 head unchanged (game a234c52a · dataTree 4bbdebe5 · srcTree 7cc295df · manifest 4625dca9 · suite 69681d6f · focused 65e9c873). EXACT NEXT: the mega-ladder's P2 — the ARC 4 CHARTER: LANE-017 loot-and-badges (packet §4c+§4d + the badge-chip/rarity-visual media slices + the R-7 reconsideration rider), authored ledger-only in COORDINATION.md with DRIVE taken in the same commit (the LANE-014 489cfc4 precedent), then SLICE 1 (one rarity language) per that contract.]* *[AMENDED in-flight 2026-07-20, D478 — the post-D476 mega-ladder P2+P3: THE ARC 4 CHARTER IS COMMITTED AND SLICE 1 IS SHIPPED. LANE-017 chartered at DRIVE (`023ec91`, ledger-only: design law §4c+§4d, TEN adjudications incl. the R-7 rider RESOLVED — bundled into the coverage-sweep slices with its own teeth and a no-flip re-proof — and NINE slices each with owners + predeclared binds). SLICE 1 (D478): `data/loot-survival.json` is THE canonical tier map — per-tier glyphs (•▲◆★) + RESERVED hexes #9a9185/#6f9e5b/#9a86f1/#b8863c (the first one-language wall run caught the old palette colliding with the app-wide generic gold/green accents in 40+ src files; the tiers now own dedicated values one step off, visually imperceptible, contrast ≥4.5:1 re-proven, weights untouched) + the `rungTiers` badge-rung mapping (star→uncommon · superstar→rare · xfactor→legendary); `src/37` ships cwTierInfo/cwRungTierInfo (fail-closed; the #9a9185 default is the one allowed src literal) and the glyph+label-redundant card chip; T14's rung glyph tints from cwRungTierInfo (helper absent ⇒ byte-identical chip). TEETH: probe-loot-survival 12→16 (tier resolution · chip redundancy · contrast · the tree-wide ONE-LANGUAGE WALL) 16/16 · probe-ratings 22→23 (canonical tint + redundancy) 23/23, artifacts read 0 pageerrors; adjacents probe-accessibility 27 ok (generic-accent pins untouched — the decoupling's point) + probe-tactical-roster 8/8. BINDS: A T14-hardcode → the wall red naming tactical/T14-ratings.js:#b8863c EXACTLY (restore 49208054 md5-proven, 16/16); B glyph-stripped → the redundancy tooth red EXACTLY (restore a527600d + game 9dd15ca2 md5-proven, 16/16). AD-7 re-pins game a234c52a→9dd15ca2 · dataTree 4bbdebe5→b3b323fa · srcTree 7cc295df→ce48e9ae at all four plan-probe sites (base/manifest/suite/focused hold); both plan probes re-run post-commit. Counts hold 29/62/1,710/140; `_SAVE_VER=1`; frozen base `c9db83fa` untouched. EXACT NEXT: LANE-017 SLICE 2 — drop feel (§4c.3) per the committed contract IN FULL: rarity-colored drop announcements + card-flip reveal on new drops + inventory sort/filter (rarity/kind/battle) + legendary glow, ALL reading the slice-1 canonical map (no new hex anywhere — the wall enforces it); reduceMotion ⇒ NO flip/glow animation (instant reveal); drops unchanged in content/weights (presentation only, sim/save byte-identity probed); owner probe-loot-survival; binds: (a) reduceMotion ignored (animation class still applied) → the rm tooth red EXACTLY; (b) sort mutates the SAVED inventory order → the byte-identity tooth red EXACTLY; AD-7 re-pins + both plan probes re-run post-commit.]* *[AMENDED in-flight 2026-07-20, D479 — mega-ladder leg 2 P1 (DRIVE re-taken ledger-only at `1ece449`, the §4 resolution: Claude Code / Fable 5): SLICE 2 IS SHIPPED. Drop feel per the contract clause verbatim: the additive-sanitized `recentDrops` record (absent NEVER created — legacy saves byte-identical; the `_pdLog` record text verbatim), the Latest Recovery announcement panel with canonical-map glyph+label-redundant chips, the one-time flip reveal + legendary glow (runtime `cwLootFeelCss` keyframes, colour via CSS var from the data map, ZERO new hexes — the wall green), BOTH fully withheld under reduceMotion (instant reveal, zero animation classes, every chip/card still rendered), and view-side sort/filter (recovery-order/rarity/kind/battle sorts + rarity/kind/battle facets + the "N of M items" count line; the view sorts a decorated COPY with stable idx tiebreak — the saved order NEVER mutated; stale prefs self-reset; use/equip wiring delegated on #lootInvBlock). TEETH: probe-loot-survival 16→20 (announcement-canonical + record-unchanged · flip/glow default AND rm-inert · sort/filter correctness + saved-order byte-identity after the FIRST sort and at the end · sim/save byte-identity vs an untouched twin + weights pinned 2/1/0) 20/20, artifact read, 0 pageerrors; adjacents probe-ratings 23/23 + probe-save-slots 16/16, artifacts read. BINDS: A rm-conjunct stripped → the rm tooth red EXACTLY ("reduceMotion still carries 2 flip/glow animation class(es)"); B in-place sort → the saved-order byte-identity tooth red EXACTLY + the sibling twin-divergence tooth catching the same class at its own label; restores md5-proven (src/37 1689c4a2 · game b74053aa), re-runs 20/20. AD-7 re-pins game 9dd15ca2→b74053aa · srcTree ce48e9ae→4eed52e8 · journey a527600d→1689c4a2 at all four plan-probe sites (base/dataTree/manifest/suite/focused hold — no data edit); both plan probes re-run post-commit. Counts hold 29/62/1,710/140; `_SAVE_VER=1`; frozen base `c9db83fa` untouched. EXACT NEXT: the ladder's P2 — LANE-017 SLICE 3, the badge presentation layer (§4d.1) per the committed contract IN FULL: Madden-style badge chips on general + brigade cards, a badge gallery, X-Factor showcase with LIVE activation state (reads the existing `_xfActive`/`_xfGlow`, never writes), hover/tap provenance (badge def + source basis text); keyboard-operable and screen-reader-labelled; chips render from badgeDefs/rosterBadges without new data fields; zero sim writes (the D74 output-wall scan extended to the new render path); owner probe-ratings, adjacents probe-command + probe-accessibility; binds: (a) showcase active with no trigger fired → the state-mirror tooth red EXACTLY; (b) a chip rendering a badge id absent from badgeDefs → the defs-integrity tooth red EXACTLY; AD-7 re-pins + both plan probes re-run post-commit.]* *[AMENDED in-flight 2026-07-20, D480 — mega-ladder leg 2 P2: SLICE 3 IS SHIPPED. The badge presentation layer per the contract clause verbatim, all pure reads inside the D74 wall (zero data edits, zero T0 edits): T14's `fldBadgeCardHtml` Madden-style cards (rung glyph tinted via cwRungTierInfo, polarity sign, provenance VISIBLE + hover title + SR aria, tabindex 0), `fldBadgeGalleryHtml` (unit cards with unresolved ids REFUSED fail-closed; null → the full 26-def catalog grouped by tinted rung sections), `fldXfShowcaseHtml` (LIVE state mirrored from the `_xfOn`/`_xfGlow` runtime latch reads-only — armed → IN THE ZONE ⚡ → re-armed, state spoken in aria), the T29-idiom disclosure (two native aria-correct buttons + always-in-DOM panels + delegated toggles + S22 focus preservation) appended inside `fldRatingBadgesHtml`, and src/35's `_cmdDevChipHTML` pool-row dev-trait chip from EXISTING devTraits (no new data fields; "" unassigned = byte-identical; the active card already carries the D105 Career Arc). No animation ⇒ no reduceMotion gate (T29 precedent). TEETH: probe-ratings 23→27 (defs-integrity + provenance + catalog · live state-mirror both ways + render-write scan · disclosure aria + dev chip · the zero-sim-writes purity sweep) 27/27, artifact read, 0 pageerrors; adjacents probe-command 94/94 + probe-accessibility 27/27. BINDS: A showcase unconditionally active → the state-mirror tooth red EXACTLY; B ghost-id fallback card → the defs-integrity tooth red EXACTLY ("expected 2 cards, got 3"); restores md5-proven (T14 8bb999f4/f2ca3e85 at the D480 label · game f7bb9cce). AD-7 re-pins game b74053aa→f7bb9cce · srcTree 4eed52e8→7c23e51d at all four plan-probe sites (base/dataTree/journey/manifest/suite/focused hold); both plan probes re-run post-commit. Counts hold 29/62/1,710/140; `_SAVE_VER=1`; frozen base `c9db83fa` untouched. EXACT NEXT: the ladder's P3 [IF-ROOM] — LANE-017 SLICES 4+5, the COVERAGE SWEEP + R-7 (§4d.2, two theater batches, the research-heavy leg) per the committed contract IN FULL: rosterBadges + X-Factors from the original 9 battles to ALL 29 scenarios (~20 new battle rosters), every row ≥2-source Verified anti-Lost-Cause balanced (helpers may GATHER from HISTORICAL-DATA*.md + the battle packets; the TOP LOOP verifies every claim — adjudication 10); R-7 situational gating per adjudication 9 as a separate commit-internal step with its own teeth (T14 caps kept, documented situations only, exceptions logged never silent); the 8-seed A/B no-flip battery per touched battle (the Stones River/Perryville/Wilson's Creek/McDowell + massacre-family direction guards stand); teeth in probe-ratings + touched battles' probes; binds: single-source row → the source tooth red EXACTLY · gated trigger past the T14 cap → the cap tooth red EXACTLY; shed from the BACK as context requires — never thin a slice; then P-END, the day-end battery (suite 140, TMPDIR, ALONE, serialized, every artifact JSON read, reds root-fixed at exact labels) + the D412 closeout.]* *[AMENDED 2026-07-20, D481 — the interrupted mega-ladder RESUMED in a fresh Fable session (the leg-2 cutoff left the DRIVE lock held; git was clean/pushed at 6518764): SLICE 4 (the eastern coverage sweep) + THE R-7 RIDER ARE SHIPPED. R-7 per adjudication 9: _fldBadgeTrig gates the situational static triggers on engine-observable state (defend posture · unbloodied · opening clock · committed advance · low ammo · fortified ground via fldInFort OR the new 45yd wall-segment reach) under the ABSENT-STATE always-on compatibility law; ONE adjudication-9 exception logged (last_stand_defend static stays always-on — gating flickered under mid-hold reposition orders and broke probe-chickamauga's sourced ≥7/8 P2 tooth, restored green); law in RATING-SYSTEM-DESIGN §16. The sweep: rosterBadges 9→19 scenarios (+26 rows, 24 Verified/2 Inferred) with the per-row citation law MADE DATA (the new rosterBadgeProv sibling + the card 'This assignment:' provenance line, fail-closed byte-identical when absent). Research: 20 agents (10 Sonnet gather → 10 Opus default-refute), 41→9 refute-drops→top-loop adjudication; EMPIRICAL bisect drops per the D391 practice (Cold Harbor rejects ANY mechanical badge — ships display rally only; the Crater drops disciplined-on-Elliott) — all logged in the _rosterNote D481 addendum. Teeth: probe-ratings 27→31 (R-7 both-ways + gated-cap · coverage floor with the pinned 10-id western PENDING list slice 5 empties · per-row source floor · row-prov display) + probe-shiloh's C11 alias tooth reshaped to its D235 scope (a D480 latent — the disclosure catalog legitimately carries grand_charge's Chickamauga note; strengthened own-card conjunct, 32/32). The A/B instrument tools/ab-badge-direction.mjs ships with completion+activation asserts (all 19 battles: zero silent R-7 deactivations) and direction RECORDED — its first-run OFF-majority heuristic was self-refuted (Wilderness OFF-majority CS vs the documented US-holds law) and THE TOUCHED BATTLES' OWN PROBES are the direction authority: shiloh 32/32 · wilderness · cold-harbor · crater · petersburg · chickamauga · gettysburg all green at the final tree, 0 pageerrors. Binds A (two layers: build-gate 4e exit-5 refusal + the source-floor tooth red EXACTLY on the record-removed variant) and B (gated-cap exemption → the gated-stack conjunct red EXACTLY, 'got 1.25') md5-proven (data 76f6a311 · T14 0492809f · game 59f2f617). AD-7 re-pins game→59f2f617 · dataTree→462b0df9 · srcTree→9ee5bf37 at all four plan-probe sites; both plan probes re-run green post-commit. Counts hold 29/62/1,710/140; _SAVE_VER=1; frozen base c9db83fa untouched. EXACT NEXT: LANE-017 SLICE 5 — the western/trans-Mississippi batch per the committed contract IN FULL (the research workflow wf_8f5ed36c-1a6 is COMPLETE and banked — adjudicate its 10 batches with the same default-refute discipline, write the rows + prov, EMPTY the coverage-floor PENDING list in probe-ratings in the same commit, run the touched western battles' own probes as the direction authority + the A/B activation evidence + ≥2 binds, re-pin AD-7, then P-END: the day-end battery (suite 140, TMPDIR, ALONE, serialized, every artifact JSON read, reds root-fixed at exact labels) + the D412 closeout.]* *[AMENDED 2026-07-20, D482-BATTERY + D483 — THE P-END BATTERY IS COMPLETE AND GREEN: leg 1 (rows 1 -> terrain readability) + ONE root-fix at the exact label (the terrain decor-leak scan matched a literal token in the R-7 T14 COMMENT — code always used the T0 universal hooks; comment reworded, no tooth weakened, the D482 follow-up commit 7e399a6 with game/srcTree re-pins) + leg 2 resumed --from green to row 140 (VET NO-REGRESSION OK); the battery-window sweep read 142 artifact JSONs — every ok, ZERO pageerrors. Battery SHA 7e399a6 — SLICES 4+5 ARE RELEASE-GRADE VERIFIED. THEN D483 (Aaron-directed, same day): the FLAG-CARD portrait default — src/22-flag-portrait-fallback.js wraps portraitFor; no-photo people now get a side-themed flag card (US 34-star colors · CS First National, deliberately not the battle flag — logged adjudication, Aaron may override) instead of the egg engraving; photos never downgraded; teeth in probe-portraits (the D483 flagOk conjunction) + probe-photo-embed (the chain tooth strengthened); binds A (side-strip -> sideDistinct red EXACTLY) + B (jpeg guard removed -> photoNeverDowngraded red EXACTLY) md5-proven; AD-7 game->e7ff100e · srcTree->15570ebc · manifest->9312db81. EXACT NEXT: LANE-017 SLICE 6 — soldier-tier badges (§4d.3) per the committed contract IN FULL (historical-record badges for Verified register soldiers SOLID vs earned-through-play career badges HATCHED/Inferred; both through the same capped levers; the register/journey UI shows the distinction; owners probe-loot-survival + probe-war-career; binds: a play-earned badge rendered solid -> the distinction tooth red EXACTLY · a solid badge on a non-Verified record -> the Verified tooth red EXACTLY; take the next session's DRIVE with a committed ledger edit BEFORE any code move if the lock was released, else continue the held DRIVE).]* *[AMENDED 2026-07-20, D484 — SLICE 6 IS SHIPPED (the held DRIVE continued): soldier-tier badges per the contract clause verbatim. data/ratings.json gains soldierBadgeDefs (14: 8 historical + 6 career, every fldLever in the existing vocabulary) + soldierBadges (48 rows on 39 of 42 Verified carriers, EVERY row >=2 named sources from the record's OWN source list — gate 4e enforces; drops logged: Sherman/Howard/Griffin single-source, Webb's MoH -> command_at_the_crisis, Beaty's same-institution CMOHS caveat flagged) + _soldierBadgeNote. T14: fldSoldierBadgeDef/fldSoldierBadges + fldSoldierBadgeFactor clamped at the SAME realismCaps badgeLever wall as fldBadgeFactor (exact-wall clamp probed at 1.10; no combat line consumes the keys — sim inputs did not move, no A/B owed). src/37: cwCareerBadges (PURE derived from the journey career log — nothing stored, _SAVE_VER=1, D149 shape untouched) + cwSoldierBadgeRows (SOLID requires row AND carrier Verified) + _ssSoldierBadgesHTML (the §10 solid/hatched/dashed provenance idiom + cwRungTierInfo tint) in the register detail + journey panel. TEETH: loot-survival 20->24 (24/24, artifact read, 0 pageerrors) · war-career 45->46 (46/46, suite 140/140) · adjacents ratings 31/31 + save-slots 16/16 · both cooked SETUPs preparsed. ONE root-fix riding: the D478 one-language wall caught the D483 src/22 COMMENT carrying all four reserved tier hexes (the D482 comment-token class, FIFTH instance; code clean, comment reworded, scan untouched) + war-career's stale 138 suite.expected DISPLAY fields aligned to the 140 teeth. BINDS A (career chip forced solid -> the distinction tooth red EXACTLY) + B (carrier-Verified conjunct dropped -> the Verified-only tooth red EXACTLY) md5-proven (src/37 9655bfff · game e99e6ac5), re-run 24/24. AD-7: game e7ff100e->e99e6ac5 · dataTree 00f8c1fe->c3c28fd6 · srcTree 15570ebc->d79696ce · journey 1689c4a2->9655bfff · focused 65e9c873->e2acf99a at all four plan-probe sites; both plan probes re-run green post-commit. Counts hold 29/62/1,710/140. Design law: RATING-SYSTEM-DESIGN §17. EXACT NEXT: LANE-017 SLICE 7 — named legendary artifacts + mode-split drops (§4c.1) per the committed contract IN FULL: citation-grade NAMED items (documented presentation swords, a Henry rifle, battle flags — each with sourced provenance text, >=2 sources); Historical provenance-locked to the correct battle/unit (the item CANNOT drop at a wrong battle, probed); Mayhem general pool per the D416 law; effects ONLY via the existing capped equip path; the artifact tier enters the canonical map here IF adopted; owner probe-loot-survival, adjacent probe-open-history-mayhem; binds: (a) a named artifact drops at a non-provenance battle under Historical -> the lock tooth red EXACTLY; (b) an artifact's second source dropped -> the source tooth red EXACTLY; AD-7 re-pins + both plan probes re-run post-commit.]* *[AMENDED 2026-07-20, D482 — SLICE 5 IS SHIPPED AND §4d.2 IS COMPLETE: rosterBadges covers ALL 29 registered scenarios (19→29; +27 western rows, 26 shipped after the empirical drop; rosterBadgeProv 53 records 49V/4I; the coverage floor flipped to the clean full registry, PENDING list EMPTIED with its chain). The second 20-agent workflow (wf_8f5ed36c-1a6) fed the same default-refute→top-loop pipeline (13 of 46 refute-dropped); THE INVERTED NEAR-PARITY LAW held (Stones River ships display-only); the massacre-family battles ship guard-aligned rows only; ONE empirical drop (horseman on Forrest at Fort Donelson — the P2 breakout guard fell to 4/8, green without it; the third chaotic-inversion instance). All 10 western battles' own probes green at the final tree (franklin = the slow-Mac harness-timeout class, fifth instance, re-run green alone); probe-ratings 31/31 at 29/29 coverage; western A/B activation evidence zero silent deactivations; binds A+B md5-proven (data ed6f8386 · T14 0492809f · game 5e3b9b71); AD-7 re-pins game→5e3b9b71 · dataTree→00f8c1fe (srcTree holds 9ee5bf37); both plan probes green post-commit. Counts hold 29/62/1,710/140; _SAVE_VER=1; frozen base untouched. EXACT NEXT: P-END — the MANDATORY day-end battery (suite 140; export TMPDIR=$PWD/.tmp; npm run vet:noreg ALONE on the machine, serialized; war-career 900s; probe-visual-fidelity 600s; read EVERY artifact JSON ok+0-pageerrors; reds root-fixed at exact labels, slow-Mac timeouts/CDN blips re-run alone first per the D454/D471/D477/D482 precedent) + the D412 closeout (RUN-LOG entry · HANDOFF head-stack archival check · lane state per the battery: LANE-017 stays DRIVE mid-ladder — slices 6-9 remain: soldier-tier badges §4d.3 → named legendaries §4c.1 → sets/variety §4c.2+5 → the ARC 4 release battery that alone flips the lane SHIPPED).]* *[AMENDED 2026-07-20, D485 — SLICE 7 IS SHIPPED (the held DRIVE continued): named legendary artifacts + mode-split drops per the contract clause verbatim. data/loot-survival.json gains the `artifact` tier (glyph ❖ · a reserved hex one step off the app accents, contrast ≥4.5:1, the D478 one-language wall + contrast teeth auto-extend) + FOUR Verified named objects, each ≥2 distinct sources + sourced blurb + artifact:{battleId,battle,side,unit}: the 28th Virginia battle flag (gettysburg — Marshall Sherman/1st Minnesota MoH, MNHS custody), the 4th USCT national colors (newMarketHeights — Fleetwood's MoH citation), Cleburne's kepi (franklin — Tennessee State Museum), the Jo Daviess County Grant sword (vicksburg — the 'Hero of the Mississippi' counterguard tie, March 1864 presentation stated plainly; slot weapon so both equip-slot classes stay probed). Research per adjudication 10: two workflows (5 Sonnet gathers → 5 Opus default-refuters + the Henry second-family hunt), top-loop re-verify on the anchor rows; HONEST DROPS LOGGED — the packet's Henry-rifle example DROPPED (single Jacobson family; Cox/Shellenberger silent AND one family; the auction provenance not citation-grade), the Cleburne sword RESHAPED to the kepi, the Hilton/Veal vignette excluded. src/37: _lootItemEligible + _lootWeightedPick(seed,C,B) — Historical pool-eligibility ONLY at the provenance battle (malformed ruleset flags fail closed; the D418 kernel is the ONLY mode authority), Mayhem general pool per D416; wrong-battle Historical pools BYTE-IDENTICAL (the shipped deterministic-drop teeth held UNMOVED; weights 2/1/0 held); the artifact card provenance line (battle · unit · stamp + sources in hover/SR); the glow class widened to {legendary, artifact} with the D479 tooth's documented chain; effects ride _lootEquippedEffect + the bridge clamp using ONLY existing keys at existing magnitudes; nothing stored, _SAVE_VER=1. TEETH: probe-loot-survival 24→28 (provenance lock: wrong-battle 2×500-seed sweep + 5 malformed shapes + anchor eligibility + 400-resolve end-to-end · Mayhem general pool: same-seeds split both ways + all four pooled + wrong-battle landing + artifact glow default/rm-inert · equip-lever caps: vocabulary/magnitude walls + per-key carry + the ±cap clamp · the node-side source floor, gate-4e sibling) 28/28, artifact read, 0 pageerrors (first run = the cold-start fonts-stall screenshot class, warm re-run green); adjacents probe-mayhem-mode 24/24 + probe-ratings 31/31 + probe-save-slots 16/16; build GATE OK (citations 4e over the four Verified rows) + schema 62/62 + cooked-SETUP preparse. BINDS A (Historical conjunct killed → the lock tooth red EXACTLY, with the Mayhem twin-conjunct sibling at its own label — the D479 precedent) + B (second source dropped → gate 4e exit-5 naming the record AND the source-floor tooth red EXACTLY — the D481 two-layer precedent) md5-proven (src/37 73a817a8 · data 347c2d0c · game 27e73f38), final 28/28. AD-7 re-pins game e99e6ac5→27e73f38 · dataTree c3c28fd6→dcf6da5b · srcTree d79696ce→b7648a67 · journey 9655bfff→73a817a8 at all four plan-probe sites (base/manifest/suite/runtime/command/focused hold); both plan probes re-run post-commit. Counts hold 29/62/1,710/140. MAYHEM_PUBLIC_READY untouched (the packet's 'stays false' phrasing is a stale D418-era carry — the flag is true since D420; the operative law 'this lane never flips it' honored). EXACT NEXT: LANE-017 SLICE 8 — set collections + item variety (§4c.2+§4c.5, may split at drive time) per the committed contract IN FULL: themed kits with completion effects through the existing condition/supply levers; item variety from real quartermaster/ordnance records; salvage flow; captured arms as the CS supply channel; economy hooks; survival-mode as a campaign-setup choice (default unchanged); scope confirmed against room at drive time — shed from the back, never thin; owner probe-loot-survival; ≥2 predeclared binds md5-proven; AD-7 re-pins + both plan probes re-run post-commit; then SLICE 9 — the ARC 4 release battery (suite 140, TMPDIR set, npm run vet:noreg ALONE serialized, war-career 900s, probe-visual-fidelity 600s, every artifact JSON read, reds root-fixed at exact labels) that alone flips LANE-017 SHIPPED.]* *[AMENDED 2026-07-20, D486 — SLICE 8 IS SHIPPED UNSPLIT (the held DRIVE continued; the P0 recon sized every sub-feature onto an existing seam, Aaron's popup option (a) adopted): set collections + item variety + salvage + the CS captured-arms channel + read-only economy hooks + the campaign-setup kit choice per the contract clause verbatim. data/loot-survival.json: 4 sets (bonus keys inside the item-effect vocabulary at ≤-wall magnitudes, the node cap tooth enforces on disk) + 9 Inferred quartermaster/ordnance variety items (22 total; honest-provenance floor probed) + the salvage config (Arms/Supply/Medicine, CS Arms ×3 — captured arms as the CS supply channel) + economyHooks (dear 1.5/ruinous 3.0/cooldown 3). src/37: possession-derived set completion PURE (nothing stored, reversible, absent-set byte-identical) entering ONLY via _lootEquippedEffect under the same bridge ±cap; lootSalvage once-per-turn costed; lootRequisition + the sutler price line READING C.economy.inflation only (absent ⇒ fair fail-closed; JSON-snapshot equality probed); lootOnResolve routes a CS VICTORY's last pick through the salvage pool — the Union path replay-proven BYTE-IDENTICAL; cards gain the provenance stamp; salvageTurn/reqTurn ride the D149 sanitizer. src/107: the picker Campaign Kit checkbox (default UNCHECKED) threading _mhStartToken/_mhPendingStart normalized `survival === true` at every hop, applied after base init; kernel authority untouched. TEETH probe-loot-survival 28→35 (sets · variety stamp · salvage/CS-tilt · resolve-channel replay · economy READ-ONLY · setup default · the node set-cap wall + honesty floor) 35/35, 0 pageerrors; adjacents save-slots 16/16 · ratings 31/31 · economy 8/8 · mayhem-mode 24/24 · accessibility 27/27; build GATE OK · schema 62/62. Deterministic-pin movement per the slice-7 caution: RESOLVE REWARD + D485 anchor values moved as EVIDENCE-ONLY fields (grep-proven unpinned externally); the weights tooth held 2/1/0 UNMOVED. BINDS A (set bonus −4→−12 past wall 8 → the set-cap tooth red EXACTLY) + B (the setup default flipped at the REAL seam — the wrapper normalization; the first candidate conjunct did not red because the normalization fail-closes it, logged honestly) md5-proven (data 2822a8fe · src/107 6582a0ff · game 67fbe534), final 35/35. AD-7 game 27e73f38→67fbe534 · dataTree dcf6da5b→5de65a85 · srcTree b7648a67→8174d79d · journey 73a817a8→214fb6e5 at all four plan-probe sites; both plan probes re-run post-commit. Counts hold 29/62/1,710/140. EXACT NEXT: SLICE 9 — the ARC 4 RELEASE BATTERY (export TMPDIR="$PWD/.tmp" && npm run vet:noreg — suite 140, ALONE on the machine, serialized; war-career 900s; probe-visual-fidelity 600s documented slow row; read EVERY artifact JSON ok+0-pageerrors; reds root-fixed at their exact labels, slow-Mac timeouts/CDN blips re-run ALONE first per the D454/D471/D477/D482 precedent; never weaken a tooth). On full green flip LANE-017 SHIPPED with the battery SHA in the same docs pass — ARC 4 CLOSES; then the Aaron-ratified [IF-ROOM] tail in order (T1 stale-docs sweep → T2 ARC 6 politics research packet → T3 GEA-01/S44 → T4 brigade flag markers), shed from the back.]* *[AMENDED 2026-07-20, D487 — THE BATTERY IS COMPLETE AND ARC 4 IS CLOSED. The full 140-row suite ran serialized/ALONE at battery SHA `fec05a3` across SIX documented legs ("VET NO-REGRESSION OK"); the artifact sweep read 168 JSONs — every one ok, ZERO pageerrors. ONE real red root-fixed at its exact label (`fec05a3`): probe-command's four NaN/undefined render teeth tripped on the literal 3-char token INSIDE the D483 flag-card canvas-PNG base64 payloads inlined in cmdRenderTab — image bytes, not rendered text, a D483-LATENT false-positive surfaced by the first battery since the wrap landed (probe-command was not in D483's focused gates); the teeth now scan with opaque data-URI payloads STRIPPED (precision-strengthened, bite proven both ways), the class swept to probe-afteraction/divergence/endings, commandProbe re-pinned 5ffd40fd→048bfed3 with its chain, both plan probes green post-commit. FOUR slow-Mac environmental flakes each re-run green ALONE (t1probe — concurrent-workflow contention, lesson logged: batteries get the machine ALONE even against browser-free helper workflows · bridge 360s · weather frame-timing · fredericksburg 360s). **LANE-017 IS SHIPPED IN FULL (D478-D487, all nine slices) — ARC 4 IS CLOSED AND FULLY RELEASED.** Counts 29/62/1,710/140 · `_SAVE_VER=1` · frozen base `c9db83fa` untouched · AD-7 head: game 67fbe534 · dataTree 5de65a85 · srcTree 8174d79d · journey 214fb6e5 · manifest 9312db81 · suite 69681d6f · focused e2acf99a · commandProbe 048bfed3. BANKED: the ARC 6 politics research workflow `wf_854e0760-ef2` COMPLETE (13/13 — six default-refuted topics + the election-bind repo data-needs inventory); GEA-01/S44 found ALREADY DISCHARGED (FIXED IN D423). EXACT NEXT: the ARC 6 POLITICS PACKET — adjudicate `wf_854e0760-ef2`'s journal with the top-loop default-refute discipline and author the citation-grade packet (docs-only; the battle-build-research idiom: source register · election-system scope 1862/1864 · war-weariness/press/emancipation inputs · the ARC 5 election bind's exact data needs · two-source floor · READY_FOR_SPEC verdict + probe-teeth recommendations) — it unblocks ARC 5 GM COMPLETION per the D455 ladder; then T4 brigade flag markers (Phase H1b) as the remaining ratified tail candidate.]* *[AMENDED 2026-07-20, D488 — THE ARC 6 POLITICS PACKET IS SHIPPED (docs-only, no lane lock): `docs/design/politics-election-research-packet.md` — the banked `wf_854e0760-ef2` journal adjudicated at the top loop (78 rows: 57 CONFIRMED · 16 WEAK · 0 REFUTED; the one unrefuted cs-politics synthesis row adjudicated Inferred as the §4 anti-Lost-Cause guardrail; Verified ONLY on ≥2 independent non-tertiary families — Wikipedia relay-only, repo corroboration never counted; NINE honest drops/corrections logged in the packet §7, headline: the Foote 1862→1864 quote weld, the McClellan-newspaper misattribution, the 13th-Amendment switcher arithmetic); top-loop spot re-verification 6-of-9 fetches landed, all faithful, ONE refuter overclaim caught (Miller Center never prints 212-21/LA-TN — re-anchored on the NARA official record). VERDICT: READY_FOR_SPEC with 8 remaining traps (Dubin/Martis + Benton + Neely + Mitchell pins et al.). The ARC 5 bind's §3 data-needs law: C.clock stays the ONE owner (one-shot support≥60 resolve), press.sentiment is the missing input wired via a src/* post-clkOnResolve interlink byte-identical at neutral-50, the divergence US/CS side-gate is law (no CS election existed), the D113 electionReliefBind is a DIFFERENT bind (collision trap) and the lever template, recommended minimal shape data/politics.json (1862 teaching-only + 1864 press-term + CS mirror cards), NO new lever class, §17 parity if any decision UI ships. Counts hold 29/62/1,710/140; AD-7 head unmoved (docs-only). EXACT NEXT: charter ARC 5 GM COMPLETION per the D455 ladder — a ledger-only COORDINATION lane charter (AI-GM persona choice at setup · muster-roll UI (fldMusterRollHtml is probe-vetted) · the 1864 election bind per the packet's §3/§3.5 with the §5 probe-teeth recommendations as the teeth basis), DRIVE taken in the charter commit (the LANE-014/017 precedent); T4 brigade flag markers (Phase H1b, CVD-safe runtime-drawn, zero embed bytes) stays the ratified tail candidate behind it; LANE-002 5b keeps its own fresh-context opener reservation (S-04).]* *[AMENDED 2026-07-20, D489 — ARC 5 IS OPEN: LANE-018 `gm-completion` CHARTERED AT DRIVE (`358bf35`, ledger-only — twelve adjudications + the four-slice ladder; the packet §3/§3.5 verbatim for the election bind; both plan probes green post-commit) AND SLICE 1 IS SHIPPED (the muster-roll desk surface): `src/tactical/T35-muster-desk-ui.js` (manifest 108 modules) renders the probe-vetted T14 muster panel as a T29-idiom disclosure on the Command desk's Order-of-Battle board through the new T15 player-flagged guarded seam (`_fldOOBSideExact(o,accent,isPlayer)` — the flag passed at EXACTLY ONE call site; the enemy column renders ZERO disclosure at every scout tier, the Q8b intel law; fail-closed "" on malformed shape/absent machinery; in-place DOM toggle, open-state module-local, nothing rides the save). TEETH probe-oob 17→21 (presence per brigade · enemy absence ×3 tiers · fail-closed ×5 + helper-absent zero-trace · in-place toggle + rebuild coherence + campaign purity) 21/21, artifact read, 0 pageerrors; adjacents ratings 31/31 + command 94/94 + accessibility 27/27; binds A (enemy call site flagged → the ENEMY-ABSENCE tooth red EXACTLY) + B (shape guards killed → the FAIL-CLOSED tooth red EXACTLY) md5-proven (T15 351aa60d · T35 93561497 · game 7b83d48b); two environment flakes documented re-run green alone (a teardown hang after a green artifact; a wedged :8765 server). AD-7 re-pins game 67fbe534→7b83d48b · srcTree 8174d79d→3ce634af · manifest 9312db81→a6699981 at all four plan-probe sites (base/dataTree/journey/suite/focused/runtime/command/commandProbe hold); both plan probes re-run green post-commit. Counts hold 29/62/1,710/140; `_SAVE_VER=1`; frozen base `c9db83fa` untouched. EXACT NEXT: LANE-018 SLICE 2 — the AI-GM persona choice at setup + the Transfer symmetry rider, per the committed charter clause IN FULL: a campaign-setup control (the D486 src/107 normalized-token idiom; keyboard-operable, labelled) offering Historical persona (commanderMode=historical — the `_cmdHistoricalDefault` tenure windows; Davis keeps Bragg, Lincoln's chain cycles to Grant; teaching copy cites the tenure data) vs Competitive optimizer (role-scored selection PLUS the theater-fit symmetry — the shadow's effective ratings pay the SAME bounded cross-theater friction the player pays through the existing `_cmdTransferMalus`-class caps; the "no hidden Transfer" readout updates to disclose it honestly); ABSENT choice ⇒ today's tier-derived behavior BYTE-IDENTICAL (fail-closed normalization); the Historical persona never pays friction; enemy leadership is a bridge input so every moved value ships the honest A/B log and the touched battles' own probes are the direction authority; teeth owner probe-command, adjacents probe-oob + probe-save-slots + probe-accessibility; binds: (a) the absent-choice default flipped at the normalization seam → the byte-identity tooth red EXACTLY; (b) the friction malus past its cap → the cap tooth red EXACTLY; AD-7 re-pins + both plan probes re-run post-commit; the DRIVE lock is HELD by this session (D171 same-chat continuation) — a fresh session re-takes it with a committed ledger edit if released at a boundary.]* *[AMENDED 2026-07-20, D490 — SLICE 2 IS SHIPPED (the held DRIVE continued): the AI-GM persona choice at setup + the Transfer symmetry rider per the committed charter clause verbatim. The picker gains the labelled native Enemy-command-persona select (default neutral; no data-mh-mode, no announce — the picker count/announce pins hold; 390px-safe) with teaching copy citing the tenure record, threaded _mhGmPersona-normalized fail-closed at every D486 hop (_mhOpenTerms/_mhArmTerms/_mhStartToken/_mhPendingStart/the startCampaign wrapper/the _t1InitAll wrapper → cmdSetAiGmPersona AFTER base init); cmd.aiGmPersona rides D149 additive (malformed DROPPED on load, absent NEVER created — legacy saves byte-identical); cmdEnemyShadow honors the persona: Historical ⇒ commanderMode=historical over the _cmdHistoricalDefault tenure windows, ZERO friction (the player-side law mirrored); Competitive ⇒ the role-scored sort with every candidate paying _cmdAiGmFriction (0 natural/Multi/no-data — the player fail-safe mirrored; else EXACTLY _cmdTransferMalus(), the ONE [0,6] clamp) and the chosen cross-theater commander paying the same malus on shadow leadership inside [42,88]; persona/personaFriction ride the snapshot ONLY when chosen; the "Pure AI-GM readout; no hidden Transfer." line byte-identical at default, replaced with the honest disclosure under a persona; the aiGm _note stale-blocker sentence replaced (adjudication 2). CHARTER ERRATUM recorded (the D489-adjudication-1 class): "Davis keeps Bragg through his documented tenure" is half-stale — cs-bragg commandFrom is null; the shipped single-slot tenure chains are the US chain→Grant (March 1864) and CS Johnston→Lee (June 1862); the operative mechanism shipped verbatim, the teaching copy cites what the data records, NO new sourced claim authored. TEETH probe-command 94→99 (absent-choice byte-identity incl. set-then-remove round-trip + cmdInit-never-creates · persona BOTH ways incl. the 1861-08 tenure-divergence fixture · the friction exact-cap clamp (runaway 40 ⇒ 6) · save additive-sanitized ×3 malformed · the setup control labelled/keyboard + the token thread e2e incl. tampered-token refusal + the Mayhem carry) 99/99, artifact read, 0 pageerrors; adjacents oob 21/21 + save-slots 16/16 + accessibility 27/27 + mayhem-mode 24/24; build GATE OK · schema 62/62 · node --check ×3 · the cooked SETUP preparsed via the template-literal VM cook (raw extraction false-alarms on template escapes — cook the literal first). BINDS A (the normalization-seam default flip → the byte-identity tooth red EXACTLY, 98/99 others green) + B (the malus clamp 6→9999 → the cap tooth red EXACTLY, "got 40", 98/99) md5-proven (src/35 a69d6249 · game b2b23ed2), final 99/99. HONEST A/B: ZERO values moved at the absent choice (the byte-identity tooth the proof; the default 1863-07 shadow cs-lee/leadership-84 unmoved); chosen-persona movement is the player-opted feature bounded ≤6 on a [42,88] input; no battle data touched, no direction battery owed. AD-7 game 7b83d48b→b2b23ed2 · dataTree 5de65a85→fa4ce39d · srcTree 3ce634af→cfa7648d · command f3ad1450→a69d6249 · commandProbe 048bfed3→d861722c at all four plan-probe sites (base/manifest/journey/suite/focused/runtime hold); both plan probes re-run green post-commit. Counts hold 29/62/1,710/140; _SAVE_VER=1; frozen base untouched; suite stays 140. EXACT NEXT: LANE-018 SLICE 3 — the 1864 ELECTION BIND (the load-bearing slice), per the committed charter clause IN FULL (the packet §3/§3.5 VERBATIM per adjudication 3): NEW data/politics.json (press.json conventions) — cycles.1862-midterm teaching-only card + at most a bounded capital nudge, NO game-state gate, BOTH halves taught, the seat swing displayed as the 28-31 range (adjudication 5); cycles.1864-presidential documenting the shipped support formula + the bounded press term via the interlink + the §2b Verified anchors (the referendum framing · the August nadir → Mobile Bay/Atlanta/the Valley reversal · 212-21/~55% from the NARA record · the close pivotal states); the CS mirror rows (§2f) rendered ONLY through the src/81-divergence.js:162-175 side-gate (a CS campaign NEVER renders election language). The NEW src/* politics interlink module per adjudication 3: registered post-clkOnResolve in src/90's _t1Resolve chain (the econOnResolve/csFinance idiom, ONE guarded typeof call added), reading press.sentiment → a bounded capped weariness delta (+ the bounded 1862 capital nudge), ZERO delta at neutral 50, module INERT byte-identical absent data/politics.json, writing NEITHER clock.elected NOR clock.resolved1864 (C.clock stays the ONE owner — the one-shot support≥60 resolve in frozen base.html is never edited); NO new lever class (D74 absolute); the D113 electionReliefBind is a DIFFERENT shipped bind (the collision tooth owed) and the lever-shape template; sourced soldier-vote/emancipation/13th-Amendment cards + endings hooks per adjudication 6 (teaching-first — a MECHANICAL soldier-vote term is a HALT; every row ≥2 independent non-tertiary families or Inferred/Disputed DISPLAYED; the §6 traps gate exact-number rows). TEETH: the packet §5's TEN recommendations ARE the basis (formula-neutrality/fail-closed · one-shot · single-writer scan · side-gate both ways · D74 output-wall extension · D113 collision · teaching-honesty direction guards · source-floor gate-4e sibling · §17 parity IF a decision UI ships (none chartered) · legacy-save byte-identity), landing in existing owners per adjudication 7 (probe-divergence the side-gate/cards · probe-endings the accessor/hooks · probe-command the D113 collision · probe-economy/probe-save-slots the interlink neutrality + legacy-save — exact placement at drive). BINDS: (a) the neutrality conjunct killed (neutral-50 or absent-config still injects a delta) → the formula-neutrality tooth red EXACTLY; (b) the side-gate inverted (a CS campaign renders election language) → the side-gate tooth red EXACTLY. GATES per the standing slice law (FIRST build SOLO long-timeout · node --check touched · cooked-SETUP preparse · focused owners + adjacents with artifact JSON reads · binds exact-red + md5 restores · git diff --check · commit -F · push · AD-7 re-pins by VERBATIM EXTRACTION of the probes' hash functions — NOTE the NEW data file moves dataTree AND the schemas/dataCount pins 62→63 in BOTH plan probes' PIN blocks and the build schema gate, document the chain · both plan probes re-run POST-COMMIT). The DRIVE lock is HELD by this session (D171 same-chat continuation); a fresh session re-takes it with a committed ledger edit if released at a boundary.]*

## ⚡ HANDOFF — 2026-07-19 (overnight night 2; superseded by D467) **D463-D466: ARC 2 IS COMPLETE AND FULLY RELEASED — THE FORT PILLOW RUNTIME SHIPPED, BOTH REMAINING SPECS COMMITTED, THE 138/138 RELEASE BATTERY GREEN, LANE-012 + LANE-013 SHIPPED/CLOSED.** P4 (D463 `9a0d4e9` + `dffd3bc`): the Fort Pillow RUNTIME landed in ONE atomic commit from the banked safe-stop draft (data md5 `804b75ab`; the WIP branch `lane-013-p4-wip` deleted after landing) — single-phase attacker-seize per the D462 spec verbatim, CS 1,500 Cimprich vs US 557-encoded-535 NPS with ranges disclosed, the massacre NEVER in-scenario (the shipped D457 no-quarter machinery is the only resolution path — the new probe's machinery step proves all 24 reward families refused under Historical with a non-vacuous control, the offer stamping from the captured chain with the documented stable-id fallback, and the declined path taking no consequence), five ≥2-source teaching cards with the JCCW conclusion verbatim, the dignity imagery law absolute, counts 26/59/1,617/137 → **27/60/1,632/138** with documented chains at EVERY pin site (17 register sites · both-baselines EXPECTED · the TEN absence-tooth flips with fortHenry/naval/leetown/kernstown halves KEPT · the AD-7 plan-probe re-pins), probe-fort-pillow 19 steps ALL OK with the 8-seed direction battery **8/8 CS-seizes AND 8/8 US-losses-exceed-CS at the fair 1,500 baseline** (the sourced-range lever never needed), binds A (Forrest grade) + B (card source) md5-proven. P5 (D464 `e3fcc8b` · D465 `6663449`): the CRATER and OLUSTEE battle-build specs committed docs-only (Crater: one-family strengths 8,500/6,100, the Mahone Brig. Gen. lock, the no-Connecticut roster law, the qualitative-toll + McClellan-caution law, adjudication-2 flips reserved to its runtime commit — verified untouched; Olustee: rank 65.5, the honest Union defeat from schedule inputs, the Colquitt/Barton/Reed rank locks, the dual-designation law, the contested-aftermath law) + the two HISTORICAL-DATA.md corrections with D465 chains. ARC 3 chartered ledger-only as LANE-014 `graphics-uplift` (LAW-DRAFT, unowned, `fba0b61`); the [IF-ROOM] Crater runtime was DECLINED under the charter's budget rule. P6 (D466, the release): the full battery ran ALONE/serialized to **138/138 GREEN** at final head `ca1a219` with **139 artifact JSONs read — every ok, ZERO pageerrors**; FIVE reds root-fixed at their exact labels, no tooth weakened (`8b082e6` the S44 western-theater copy update the tooth exists to force · `7531e15` the war-career frozen-src/82 pin acknowledging D457, the D443/D453 precedent's third instance · `813aa53` the two main()-side `/fort.?pillow/` disk scans split, the regex-source-form sweep lesson recorded · `7b3edca` spotsylvania's DOM chronology sibling · `ca1a219` kennesaw's own chain teeth — the chattanooga-adjacency sweep now provably exhausted). **LANE-012 SHIPPED (ARC 1 closed) · LANE-013 SHIPPED (ARC 2 closed) · LANE-014 LAW-DRAFT/unowned.** Counts rest at 27/60/1,632/138; game `f0228c4b`; `_SAVE_VER=1`; frozen base `c9db83fa` untouched. **EXACT NEXT (three candidates, Aaron's pick): (a) LANE-014's full acceptance contract (ledger-only first) then ARC 3 DRIVE; (b) the CRATER RUNTIME charter per the D464 spec §8-§9 (one atomic commit incl. the adjudication-2 forbidden-scan flips); (c) the OLUSTEE RUNTIME charter per the D465 spec §8-§9. No lane is owned; the next session takes DRIVE with a committed edit before any code move.**

**THE LIVE-HEAD SINGLE-SOURCE RULE (D412):** the top ⚡ block below is THE one canonical live boundary for the whole project. Every other canonical doc (START-HERE, WAKE-UP, AUTONOMOUS-RUN §2, V1-CHECKLIST's live gate) carries at most a 3-5 line summary plus a pointer here; RUN-LOG stays the chronological log. This file keeps the latest ⚡ block plus exactly ONE prior; older superseded blocks move byte-verbatim to `legacy/HANDOFF-ARCHIVE.md` at each release (newest at top), gated by the probe-anchor inventory. Full rules: `START-HERE.md` §Naming convention.

**Older handoff blocks (D462 and earlier, plus the run-h scaffold sections):** moved byte-verbatim to [`legacy/HANDOFF-ARCHIVE.md`](legacy/HANDOFF-ARCHIVE.md) — newest at top (D462 moved at D467; D458 moved at D466; D457 moved at D462; D456 moved at D458; D454 moved at D457; D423 moved at D454; D422 moved at D453; D421 moved at D423; D418 and D417 moved at D420; D413 moved at D418; D412 at D417; D411 at D413; D410 → run-h in D412). Nothing was deleted or paraphrased beyond the standing "(superseded by …)" label flip.
