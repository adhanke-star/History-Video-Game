# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D543 next-lane=LANE-022 state=CONTRACT owner=none -->
> **Live status (D543, 2026-07-23):** `LANE-022` **Slice 3 SHIPS** — repair costs FINITE engineering capacity, and the logged Engineering Corps directive is now load-bearing. Clearing a cut runs through the shipped `conquestSupplySetCondition`, consulting a pure capacity reader over `C.engineering.levels` scaled by the **B-5 slider** (`fldPresetResolve().attrition`, `[0.6,1.4]`) plus a per-pass ledger `C.conquest.supply.repair`; a repair the corps cannot afford, or lacks the branch for, is REFUSED so the cut stands — the standing decision (a Construction-1 corps restores only **2 of 27** cut rail arteries per pass). **Non-conquest play stays byte-identical.** The lane releases to `CONTRACT` / `none`.
>
> **Boundary:** `src/61-logistics-rail.js` only, plus its two probes and five mechanical pin re-anchors. Game `4764b1fc`→`98f3feaf0de89b3b47eda6b1347dacd0`, srcTree `5f6d3332`→`c4fc64ebe6d49d9cdfc79885b4c05d8b`, suite **142** (no probe added), manifest 112, data 65, `_SAVE_VER` **1** (purely additive save), `build/base.html` frozen. **D543 replaces D541 as the ARC 7 product head**; LANE-019 stays unrewritten.
>
> **Authority:** design-law §4 Slice 3 + the logged Engineering Corps / B-5 directive. Three A/B legs — two zero-diff (direction battery `18f609d0…`, `probe-full-campaign` `a38185fd…`) and one honest conquest-ON leg adjudicated under D92 (a repair restores friction 40→7 and depotReach toward TRACED; pontoons restore water, Construction restores rail; Historian 28 / Balanced 37 / Arcade 40 capacity). Containment fails CLOSED at the ruleset seam both directions; Bind D542-B1 redded only `CONTAINMENT-B` with a byte-identical restore. Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-23, D543: LANE-022 SLICE 3 SHIPS — REPAIR COSTS FINITE ENGINEERING CAPACITY, THE ENGINEERING CORPS IS LOAD-BEARING, AND THE B-5 SLIDER GOVERNS MAGNITUDE, WHILE NON-CONQUEST PLAY STAYS BYTE-IDENTICAL; THE LANE RELEASES TO CONTRACT/NONE.** D542 is committed and pushed clean at `475543cebc4d9026b382c61cfef51283396183cc`. Slice 3 landed exactly as contracted. **D543 replaces D541 as the ARC 7 product head, and LANE-022 releases to `CONTRACT` / `none`.**

**What shipped.** Clearing a cut is no longer free: `conquestSupplySetCondition`'s clear-path now consults a pure capacity reader `conquestRepairCapacity(C)` over the shipped `C.engineering.levels` (Construction + Pontoon branches, read purely via `_lgEngLevel` — no `engInit` mutation on a read path) scaled by the B-5 realism lever, plus a per-pass ledger `C.conquest.supply.repair` materialized lazily by `_lgRepairLedger`. A repair the corps cannot afford this pass, or lacks the branch for, is REFUSED so the cut stands. The Construction Corps clears a cut RAIL line; the Pontoon Train restores a cut WATER crossing (roads wait for Slice 5, so "clearing" acts only on a Slice-2 cut condition). A pure reader `conquestSupplyRepairReport(C)` and the extended `_lgSupplyBlockHTML` teach the standing decision; the unwired internal `_lgSupplyRepairReset(C)` is the per-pass boundary the Slice 6/7 loop will call at turn advance. `_lgSupplyView` gained one field, `repairSpent`. No fourth public mutator, no second logistics/engineering owner, no second save owner, no new clock (invariant F). No module added (manifest 112), no data change (65), `build/base.html` frozen.

**Finite capacity is the point (design law §5).** A Construction-1 corps has capacity 10 and each rail repair costs 4, so it restores only **2 of 27** cut rail arteries in one pass and the report marks the pass `exhausted` — the standing decision *which artery do you restore, or accept the longer water route?* is real and probed. The B-5 slider governs magnitude: a full Construction-3 / Pontoon-3 corps has capacity **37** at Balanced, **28** at Historian (harder, slower rebuild) and **40** (clamped) at Arcade — the exact `T13 fldEngRealism` polarity, read from `fldPresetResolve().attrition` clamped `[0.6,1.4]`.

**A/B — two zero-diff legs, one honest leg.** Leg 1 (conquest OFF) the 24-scenario × 8-seed direction battery `18f609d07b1190904ec0c11e4ca64675` BYTE-IDENTICAL to the `5ecc00b` baseline (0 diffs); leg 2 `probe-full-campaign` `a38185fd371a7f181250eff3a6cbf76a` BYTE-IDENTICAL (0 diffs). Leg 3 (conquest ON, in-page, both columns, 0 pageerrors): repairing the cut US/E rail line moved it SEVERED→TRACED, friction **40→7** and depotReach **58→70**, charging spent 4; a water cut was unrepairable with Construction only and restored with pontoons — adjudicated under D92 as accurate-inputs consequences (a restored line lowers friction toward its TRACED value), never tuned, every value inside the shipped caps.

**Bind and save.** Bind D542-B1 mutated the containment allowlist (`{ mayhem: 1 }` → `{ mayhem: 1, historical: 1 }`) and ran the focused probe WITHOUT rebuilding: ONLY `CONTAINMENT-B` redded (17/18, exit 1), every other tooth — including CONTAINMENT-A, the new REPAIR step and SAVE SHAPE — held; restore byte-identical (`src/61` md5 `1ccd972e5a4f71407d37ddd0d0eb0eaa` pre == post), rebuild returned the identical game md5 at 18/18 green. Save is **purely additive** — `C.conquest.supply.repair` rides the existing envelope, none of the seven `tools/save-shape.json` functions moved, `_SAVE_VER` stays 1; a legacy campaign round-trips byte-identically and thirteen malformed payloads (including four malformed repair ledgers) each fail closed.

**Gates, all green with every artifact read:** build `GATE OK`; focused **18/18** (0 pageerrors, 0 realErrors); plan 10/10; adjacent logistics-rail 8, logistics 15, bridge 6, conditioning 9, engineering-corps 23/23, conquest-board 13/13, conquest-state 15/15, conquest-transport 18/18, campaign-link 19, auto-resolve 10, save-slots 17/17, command 100, presets 27 — all exit 0, 0 pageerrors; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest layer 8/8, doc coherence 5/5; `git diff --check` clean. `probe-engineering` FATALs on the reproducible headless-Chrome fonts-stall screenshot timeout (environmental, memory-documented) — its gameplay steps ran, the engineering surface (`src/57`/`src/85`/`src/86` + `data/engineering.json`) is BYTE-UNCHANGED, and `probe-engineering-corps` 23/23 cross-validates the corps gameplay; not a Slice 3 regression. Game `4764b1fccb40c473edd871621497f62b` → `98f3feaf0de89b3b47eda6b1347dacd0`, srcTree `5f6d33325fa5feb04f6d5b11e3f5a3b7` → `c4fc64ebe6d49d9cdfc79885b4c05d8b`, re-anchored at FIVE disk-verified sites (three generated-game, two srcTree); suite md5, focused md5, manifest and frozen base HOLD. **LANE-019 is deliberately unrewritten** (transport plan 12/12). **EXACT NEXT:** under D514, reload the ledger and contract LANE-022 Slice 4 (blockade/sea edge — blockade state gates the two bounded sea services, reusing shipped blockade state with no new owner) in a committed DRIVE take, or issue the capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.

**⚡ AMENDMENT — 2026-07-23, D542: LANE-022 TAKES CLAUDE CODE DRIVE FOR SLICE 3 — REPAIR PLUS FINITE ENGINEERING CAPACITY, WHERE THE LOGGED ENGINEERING CORPS DIRECTIVE LANDS LOAD-BEARING (CONTRACT ONLY; NO RUNTIME BYTE MOVED).** D541 is committed and pushed clean at `5ecc00bdb6f5fc65d9e26c32d51fb49371e81d2b`. This is the contract-before-teeth commit the Contract Relay's hard rule requires: LANE-022 carries the complete Slice 3 acceptance contract as committed prose, and the acceptance teeth land in the SAME commit as the code that greens them. Nothing in `src/`, `data/`, `build/`, the manifest, the suite, the save shape or the generated deliverable moved here, so game `4764b1fccb40c473edd871621497f62b`, srcTree `5f6d33325fa5feb04f6d5b11e3f5a3b7`, suite 142, manifest 112, data 65, `_SAVE_VER` 1 and the frozen base `c9db83fa99230ffb95bdfdfe059f3fb9` do NOT move and are NOT re-pinned. **D541 remains the ARC 7 product head until Slice 3 ships.**

**What Slice 3 contracts.** Clearing a cut is no longer free: it costs FINITE engineering capacity, and multiple simultaneous cuts exceed one pass's capacity, forcing the standing decision the ladder is built to teach — *which artery do you restore, or accept the longer water route?* (design law §5). The Construction Corps rebuilds a cut RAIL line; the Pontoon Train restores a cut WATER crossing. Roads stay absent until Slice 5, so "clearing restores a road" (design §4) is implemented here as clearing a Slice-2 cut rail/water service condition, NEVER as authoring a road. Repair EXTENDS the shipped `conquestSupplySetCondition` clear-path plus a capacity ledger `C.conquest.supply.repair` — invariant F holds: NO fourth public mutator, NO second logistics/engineering owner, NO second save owner, NO new clock. Capacity is a pure reader `conquestRepairCapacity(C)` over the shipped `C.engineering.levels` (Construction + Pontoon branches — the Engineering Corps made load-bearing) scaled by the B-5 slider; a pure reader `conquestSupplyRepairReport(C)` surfaces the standing decision.

**The B-5 slider governs magnitude (invariant I — the live symbol confirmed from src, not invented).** Repair magnitude reads the shipped effectiveness/realism lever `fldPresetResolve().attrition` — the exact lever `T13`'s `fldEngRealism` already couples to engineering (Arcade 0.7 fast / Balanced 1.0 / Historian 1.3 slow), clamped to `[0.6, 1.4]` identically. Higher realism ⇒ lower repair capacity ⇒ harder engineering, the same polarity as the tactical dig-speed coupling. Guarded (`typeof fldPresetResolve === "function"`, neutral 1.0 when no preset); no `sev.`, `G.` or `window.` in the seam region.

**Bounded channel, containment and save, all unchanged in law.** `logisticsBridgeBonus` keeps its caps (`supply ≤ 7`, `fatigueRelief ≤ 5`, `overall ≤ 2`) and `wr.supply` keeps its 0.15 troop-morale weight; supply stays a bounded CONDITIONING INPUT — repair NEVER writes casualties, morale, victory, score or RNG, and Slice 3 creates NO new combat channel. Containment reuses the same `_lgTraceRuleset` allowlist gate: `conquestRepairCapacity` / `conquestSupplyRepairReport` gate through `_lgSupplyView`, so authored repair/capacity state fails CLOSED on the evidence-gated ruleset before any substrate read, and `CONTAINMENT-B` stays the ONE tooth owning the gated-ruleset query and the allowlist source assertion. The capacity/repair state is control/condition-class and must NEVER enter the CF-2 memo. Save stays **purely additive**: `C.conquest.supply.repair` rides the existing envelope, none of the seven `tools/save-shape.json` functions is touched, and `_SAVE_VER` stays 1 as a CONSCIOUS decision (the D447/GEA-12 precedent). A pre-Slice-3 save loads, round-trips and re-serializes BYTE-IDENTICALLY, and the sanitizer fails CLOSED on a malformed repair payload.

**Gates owed at the shipping commit:** `node --check` on every touched file; `node tools/build.mjs` GATE OK; artifact readback of the new repair/capacity symbols inside the built 61 module region; the two LANE-022 probes; the adjacent logistics/bridge/conditioning/engineering/board/state/transport/campaign-link/auto-resolve/save-slots/command/presets probes; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest 8/8, doc coherence 5/5; **three** A/B legs — leg 1 the 24-scenario × 8-seed direction battery byte-identical to the `5ecc00b` baseline `18f609d07b1190904ec0c11e4ca64675`, leg 2 `probe-full-campaign` byte-identical to `a38185fd371a7f181250eff3a6cbf76a`, and leg 3 the conquest-ON leg which WILL move as repairs restore friction, with both columns logged, the direction stated, and the movement adjudicated under D92 as an accurate-inputs consequence (a restored line lowers friction toward its TRACED value — never tuned, never an output gate); ONE declared negative bind on the containment allowlist redding ONLY `CONTAINMENT-B` with an md5-proven byte-identical restore; `git diff --check`. Five pin sites re-anchor (three generated-game, two srcTree); suite/focused/manifest/base pins hold and no probe is added, so the suite stays 142. **EXACT NEXT:** implement D542 exactly as contracted, ship it under its own D###, then release LANE-022 and contract Slice 4 (blockade/sea edge) or issue the capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
