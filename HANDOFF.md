# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D539 next-lane=LANE-022 state=CONTRACT owner=none -->
> **Live status (D539, 2026-07-23):** `LANE-022` **Slice 1 SHIPS** — `_lgRoute`'s static per-battle lookup becomes a REAL TRACE over the read-only 36-territory board for conquest carriers on the open ruleset, closing the GEA-11 defect at its named seam. Read-only: `applied:false`, `friction` unmoved, every downstream logistics owner untouched, and **both A/B legs zero-diff**. The lane releases to `CONTRACT` / `none`.
>
> **Boundary:** `src/61-logistics-rail.js` only, plus two new probes and mechanical pin re-anchors. Game `859637ed`→`45278110cb73ea4719fa41ffef7682f9`, srcTree `003d308a`→`08f95d9e9311e90313cc5b7a930f9380`, suite **140 → 142**, manifest 112, data 65, `_SAVE_VER` 1, `build/base.html` frozen. **D539 replaces D525 as the ARC 7 product head**; LANE-019 is deliberately unrewritten and every one of its boundary sentences stays exact.
>
> **Authority:** D537 ruling 4 — read-only first, so the trace's correctness proof is never mixed with a sim-affecting A/B. Containment fails CLOSED at the ruleset seam on OWN, data-valued descriptors; the honest ELEVEN-component result is recorded, not cured. Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-23, D539: LANE-022 SLICE 1 SHIPS — THE STATIC `_lgRoute` LOOKUP BECOMES A REAL TRACE OVER THE 36-TERRITORY BOARD, READ-ONLY AND BYTE-IDENTICAL ON BOTH A/B LEGS; THE LANE RELEASES TO CONTRACT/NONE.** D538 is committed and pushed clean at `f4df08e302d3ff057f99110e22fd14068ab53ed7`. Slice 1 landed exactly as contracted, with one contracted-scope hardening the acceptance teeth forced. The GEA-11 defect is closed at its named seam: `_lgRoute` keeps its shipped six keys computed exactly as before and gains ONE guarded tail (`var traced = conquestSupplyTrace(C, null); if (traced) out.trace = traced;`) backed by eight pure helpers and four authored constants in `src/61-logistics-rail.js`. `logisticsSnapshot`, `logisticsBridgeBonus`, `logisticsSetPriority`, `logisticsOnResolve`, `presLogisticsBlock` and `logisticsWireOverview` are textually and behaviourally UNCHANGED. No module added (manifest 112, `116-conquest-state.js` last), no data change (65), no `C.conquest` field, no save-shape movement, `build/base.html` frozen at `c9db83fa99230ffb95bdfdfe059f3fb9`.

**Read-only by construction, not merely by measurement.** `applied` is `false`, `tracedFriction` is consumed by nothing, and `friction` keeps its shipped static number everywhere — so the capped bridge (`supply ≤ 7`, `fatigueRelief ≤ 5`, `overall ≤ 2`), the 0.15 `wr.supply` troop-morale weight, camp and loot are byte-identical. The decisive fact: **`campaignKind` exists on disk only inside `src/116-conquest-state.js`'s detached factory/view** — no live campaign, save, load or menu path sets it — so the new branch is unreachable from 100% of shipped play, making the seam the non-negotiable #2 guarded no-op idiom.

**THE DEFECT THE TEETH CAUGHT — fixed in the code, never in the tooth.** The first implementation read `rv.id`/`rv.version` directly, so an INHERITED ruleset prototype opened the containment gate. The shipped `_trRulesetId` (`src/115`) and `_ccsRecord` (`src/116`) already refuse exactly that class, and a new gate may not fall short of the discipline beside it. `_lgTraceRuleset` now requires OWN, DATA-VALUED descriptors on `id` and `version` before consulting the allowlist, which also refuses accessors without invoking them. The tooth proves ELEVEN non-admitted shapes fail closed — both sides on the gated ruleset, wrong case, wrong version, two prototype-key names, null, absent, inherited, accessor (invocation counter at zero) and a half-inherited pair.

**Evidence.** Build `GATE OK · doc-coherence ✓ · parse ✓ · hex ✓ · collision ✓ · no-fudge ✓ · citations ✓ · women-in-war ✓ · save-shape ✓`; artifact readback confirmed all twelve trace symbols inside the built 61 module region (14,147 bytes) with zero gated-ruleset literal there. **A/B both legs ZERO-DIFF:** leg 1, the standing 24-scenario × 8-seed direction battery, artifact `18f609d07b1190904ec0c11e4ca64675` at `0829d8d` AND at this head (`ok=true failures=0 pageerrors=0`); leg 2, `probe-full-campaign`, `a38185fd371a7f181250eff3a6cbf76a` at both heads (`ok=true steps=4 pageerrors=0`). **Bind D538-B1** mutated ONE value in the containment allowlist and ran the focused probe WITHOUT rebuilding: ONLY `CONTAINMENT-B` redded (exit 1, 11/12), every other tooth held; restore byte-identical (md5 `b33c131501dbd38096d79916ca6c3b3c` pre == post) and the rebuild returned the identical game md5 at 12/12 green. The behavioural half was separately demonstrated — mutated source yields a real gated-ruleset trace (`rulesetId:"historical"`, 1 segment, route 7 keys) where restored source yields `null` and 6 keys. Gates: focused 12/12 (0 pageerrors, 0 realErrors), plan 10/10, adjacent logistics-rail 8 · logistics · bridge 6 · conditioning 9 · conquest-board 13/13 · conquest-state 15/15 · conquest-transport 18/18 · campaign-link 19 · auto-resolve 10 · save-slots 17/17 · command 100 (all exit 0, 0 pageerrors), Mayhem 13/13, War Career 24/24, transport 12/12, conquest 8/8, doc coherence 5/5, `git diff --check` clean. Game `859637ed`→`45278110cb73ea4719fa41ffef7682f9`, srcTree `003d308a`→`08f95d9e9311e90313cc5b7a930f9380`, suite md5 `69681d6f`→`a1cb6e7347155b8705614b83cc0c32d3`, focused `a4be754c`→`fe99b4b06a1264e12015359bb1a5aded`, re-anchored with chained D538 comments at TWENTY-ONE disk-verified sites across six files (twelve suite-count, three game, two srcTree, two suite-md5, two focused-md5 — the commit message's "eighteen" undercounted and is corrected here); suite **140 → 142** (war career 38 / mayhem 57 hold); old values grepped out of `tools/` before push.

**THE HONEST RESULT, recorded rather than cured.** The 44 sourced services project into **ELEVEN disconnected components** (18 / 5 / 4 / 2 and seven singletons) because 16 rows name a single territory and contribute no edge. The default `US`/`E` route resolves in ONE rail segment (`CTS-R-02`, the Verified `RN-02`→`RN-03` Washington–Manassas corridor, `tracedFriction` 7); `CS`/`E` does not resolve at all (`reachable:false`, reached 5). That is pinned as a probe tooth and is the measured reason Slice 5 authors the road layer — never cured by inventing a service, window, interchange or endpoint. Every Historical negative stays exact and **LANE-019 is deliberately unrewritten** (transport plan 12/12). `tools/probe-desk-pacing-plan.mjs` was deliberately not touched: a superseded, non-enrolled LANE-020 probe, already red at this head, whose own suite pin now reads stale at 140 — recorded honestly rather than edited.

**Docs and release:** REVIEW-QUEUE's GEA-11 row records the D539 delivery; the V1 LANE-022 line gains a checked Slice 1 sub-item; DECISIONS/RUN-LOG/HANDOFF/mirrors sync; D537's head archives byte-verbatim under the two-head rule. **LANE-022 releases to `CONTRACT` / `none`** and **D539 replaces D525 as the ARC 7 product head.** **EXACT NEXT:** under D514, reload the ledger and either contract LANE-022 Slice 2 (control/service receipts and cuts — the first slice where supply bites, and the first that must adopt `tracedFriction`, flip `applied`, and therefore own the first sim-affecting A/B) in a committed DRIVE take, or issue the load-bearing capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.

**⚡ AMENDMENT — 2026-07-23, D538: LANE-022 TAKES CLAUDE CODE DRIVE FOR SLICE 1 — THE READ-ONLY TRACED SUPPLY ROUTE (CONTRACT ONLY; NO RUNTIME BYTE MOVED).** D537 is committed and pushed clean at `0829d8dedabbc8e8943ea534eaa279c713d06b81`. This is the contract-before-teeth commit the Contract Relay's hard rule requires: the lane carries the full Slice 1 acceptance contract as committed prose, and the acceptance teeth land in the SAME commit as the code that greens them. Nothing in `src/`, `data/`, `build/`, the manifest, the suite, the save shape or the generated deliverable moved here, so the pinned game/srcTree hashes do not move and are NOT re-pinned.

**The contract.** `_lgRoute(C, bd)` at `src/61-logistics-rail.js:51` returns a STATIC per-battle friction over a fixed battle chain — the exact GEA-11 defect that `docs/design/conquest-supply-chain-design.md` §3 names as the single structural change of the whole ladder. D538 contracts ONE guarded tail on it: when the carrier declares an exact conquest campaign kind AND the open ruleset, `_lgRoute` attaches one extra derived `trace` field carrying a real path across the read-only 36-territory board — source depot → rail/inland-water/sea segments → the army's territory — while its shipped six keys stay computed exactly as today. Everything downstream is untouched by construction: `logisticsSnapshot`, `logisticsBridgeBonus`, `logisticsSetPriority`, `logisticsOnResolve`, `presLogisticsBlock` and `logisticsWireOverview` keep their current text and behavior, so no snapshot field, no capped-bridge field (`supply ≤ 7`, `fatigueRelief ≤ 5`, `overall ≤ 2`) and no DOM byte moves in ANY campaign, conquest included. The trace carries `applied:false` and a derived `tracedFriction` that NOTHING consumes — Aaron's D537 ruling 4 read-only-first, so the trace's correctness proof is never mixed with a sim-affecting A/B. No new module (manifest stays 112 / `116-conquest-state.js` last), no data change (65 files), no `C.conquest` field, no save-shape movement.

**Containment is structural, and it is the declared bind target.** One ruleset gate returns a view ONLY for the open ruleset and returns `null` for every other value — the sourced ruleset included — BEFORE any board or transport-evidence read happens, so the boundary fails CLOSED at the ruleset seam rather than by label or filter. `src/115-conquest-transport.js` is READ, never written and never namespace-shared. The authored Mayhem content is exactly: two depots (`US` → `CT-01`, `CS` → `CT-05`), four theatre fronts (`E` → `CT-03`, `W` → `CT-20`, `TM` → `CT-32`, `N` → `CT-19`), four per-mode segment costs, and the projection of each evidence row's existing `territoryRefs` into a directed graph honouring that row's existing `direction`. Every Historical negative stays exact — zero road service with `ROAD_REQUIRES_BOUNDED_SOURCE_PASS`, `CTI-01`..`CTI-04` permanently `INTERCHANGE_WINDOW_UNADJUDICATED` and not consumed by the trace at all, New Orleans-origin and `CT-36` closed, the D503 endpoint quarantine, the Potomac/operation-composition/Sherman-chain negatives, and Boonville, Arrow Rock and Glasgow unassigned. No qualitative `dateText` is ever parsed into a machine-readable window (D526 bars it absolutely; the `CTS-S-02`/`WE-26` Fort Fisher row proves a generic parser reverses the source).

**The honest Slice-1 result, contracted in advance.** The 44 sourced services resolve into ELEVEN disconnected components (`CT-01..CT-04`; `CT-05/06/07/09/10`; `CT-11..CT-28`; `CT-35/36`; and seven singletons), so most traced pairs return `reachable:false` — the default `US`/`E` pair resolves in one rail segment (`CTS-R-02`, the Verified Washington–Manassas corridor) and the `CS`/`E` pair does not resolve at all. That is the CORRECT recorded outcome and the precise reason Slice 5 authors the road layer. It is never cured by inventing a service, window, interchange or endpoint.

**Gates owed at the shipping commit:** `node --check` on every touched file; `node tools/build.mjs` GATE OK; artifact readback of the trace symbols inside the built 61 module; the two NEW probes (`tools/probe-conquest-supply-plan.mjs` filesystem-first, `tools/probe-conquest-supply.mjs` focused — node-side `node:vm` teeth over the on-disk `src/115` + `src/114` + `src/61` so a source mutation reds WITHOUT a rebuild, plus in-page teeth against the built deliverable); both enrolled in `tools/vet-no-regression.mjs`, moving the suite 140 → 142 with every suite-count pin in `tools/` re-anchored by chained D538 comment in the same commit; the adjacent logistics/bridge/conditioning/board/state/transport/campaign-link/full-campaign/auto-resolve/save-slots/command probes; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest 8/8, doc coherence 5/5; **A/B leg 1** — the standing 24-scenario × 8-seed direction battery byte-identical to the `0829d8d` baseline artifact md5 `18f609d07b1190904ec0c11e4ca64675`; **A/B leg 2** — `tools/probe-full-campaign.mjs` byte-identical to baseline `a38185fd371a7f181250eff3a6cbf76a`; ONE declared negative bind on the ruleset gate redding ONLY the node-side containment tooth with an md5-proven byte-identical restore; `git diff --check`. The full serialized 140-row battery stays owed at Slice 7. The suite-excluded D528/D530/D532 research guards are neither rerun nor edited, and `tools/probe-desk-pacing-plan.mjs` is a superseded non-enrolled LANE-020 probe that is not a gate. **EXACT NEXT:** implement D538 exactly as contracted, ship it under its own D###, then release LANE-022 to `CONTRACT` / `none` and contract Slice 2 or issue the capacity relay. ARC 7 Historical movement, Historical roads and E46 remain blocked; D525 remains the product head until Slice 1 ships.
