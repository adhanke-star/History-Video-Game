# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D520 next-lane=LANE-019 state=DRIVE owner=ChatGPT/Codex -->
> **Live status (D520, 2026-07-22):** existing LANE-019 is under ChatGPT/Codex DRIVE for one state-free ARC 7 Transport Movement foundation: a pure physical-service evidence query inside the existing read/freeze transport owner.
>
> **Boundary:** D520 changes only routing/law documents plus the stale D506 plan-boundary tooth; runtime remains untouched. The slice may expose sourced rail, inland-water, and bounded-sea rows by immutable ruleset view, but may create no topology, adjacency, current-availability claim, state, save, UI, order, movement, capacity, receipt, AI, economy, or battle coupling. Roads remain unavailable.
>
> **Authority:** `HANDOFF.md`'s first ⚡ block and LANE-019's D520 exact contract own the slice. D514's work-conserving loop remains binding; LANE-020 stays `CONTRACT` / `none` at its independent-turn dependency.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-22, D520: LANE-019 TAKES DRIVE FOR ONE PURE PHYSICAL-SERVICE EVIDENCE QUERY; STATEFUL OR PLAYABLE TRANSPORT REMAINS SEPARATELY SLICED.** D519 released LANE-020 clean at `9cea762adc4534ee74747d97f2f9df10030b6cd1`, then the live ARC 7 law, full LANE-019 contract, current transport data/runtime, campaign/save owners, and overlapping movement/economy/battle seams were re-inventoried. The smallest dependency-ready step is not a new module, topology, campaign state, or visible move. Existing `src/115-conquest-transport.js` already owns the immutable read/freeze transport surface; a new module 116 would duplicate that owner and move manifest/build/count tails for no independent boundary.

D520 therefore contracts exactly one second pure reader in `src/115-conquest-transport.js`: `conquestTransportPhysicalServices(rulesetView)`. Input must be an exact two-field `{id,version}` object with id `historical` or `mayhem` and version 1. The function never reads `G`, `C`, settings, storage, DOM, or another ruleset source. It first requires the existing fail-closed `conquestTransportNormalized()` substrate and returns either `null` or a deeply frozen exact `{rulesetId,services}` object. Service order is the existing 27 rail, then 15 inland-water, then two bounded-sea rows. Historical preserves every qualitative status and verbatim `dateText`; Mayhem retains only rows whose already sourced `mayhemPhysicalEligibility` is true. Roads, the four interchanges, and the eighteen non-links never become selectable services.

This is an evidence/physical-candidate query only. The data has no machine-readable Historical turn window, player control, node operation, service condition, or receipt input, so the slice may not synthesize `open`, `available now`, adjacency, endpoint path, movement legality, capacity, current side, control, cut, repair, bypass, economy, theater, directive, save, AI, battle, or result authority. It adds no `C.conquest`, campaign kind, UI, start action, order, mutation, state/save discriminator, `_SAVE_VER` move, manifest row, data field, or suite row. D159 logistics, Transfer, Western Theater, blockade/economy/engineering, bridge/auto-resolve, ladder campaign, and every explicit non-link remain separate.

The implementation allowlist is `src/115-conquest-transport.js`; the existing `tools/probe-conquest-transport.mjs` and `tools/probe-conquest-transport-plan.mjs`; generated `civil_war_generals.html` only through the normal build; mechanical generated/source hash pins in `tools/probe-open-history-mayhem-plan.mjs` and `tools/probe-war-career-loop-plan.mjs` only if verbatim evidence proves they moved; and milestone documents. Data stays 65, manifest stays 111 with 115 last, suite stays 140, and save shape/version do not move. Every other runtime, data, tool, manifest/build owner, H0/menu, campaign/save, movement/economy/AI/battle/tactical surface, `build/base.html`, and `tools/save-shape.json` is barred.

Focused transport proof grows exactly 11→18. New teeth prove exact ruleset-view input and invalid/extra/wrong-version failure; exact 44-row rail/water/sea order and deep freezing; Historical qualitative/date preservation without a legal-now field; Mayhem filtering through a synthetic otherwise-valid false-eligibility normalized view; roads/interchanges/non-links excluded as services; absent/malformed substrate failure; and repeated-call purity with zero `G`/`C`/save/storage/DOM/board movement. Bind 2B removes only the Mayhem physical-eligibility filter, so only the false-eligibility tooth may red; source and generated output must restore byte-for-byte before a clean rerun. Required gates are syntax, normal build `GATE OK`, transport plan, focused transport with fresh JSON, conquest layer plan/board, Mayhem plan, War Career plan, bind isolation/restore, doc coherence, allowlist, and `git diff --check`; no full battery is owed before the first playable transport loop.

The D506 filesystem plan was already 10/11 at the clean D519 head because its final tooth still demanded the pre-D514 phrase that no transport runtime was authorized. D514 explicitly superseded only that global boundary by authorizing separately contracted non-road work. This D520 routing commit changes that one stale tooth into an exact LANE-019 DRIVE/owner/contract/road-unavailable assertion; it does not weaken evidence, count, owner, or no-gameplay teeth and changes no product runtime.

**EXACT NEXT:** implement only `conquestTransportPhysicalServices` under the allowlist above; grow focused proof exactly 11→18; run Bind 2B and byte-restore; rerun every declared focused/adjacent gate with fresh artifact readback; sync D521 evidence; commit, push, fetch, and prove clean parity. Then reload LANE-019 and separately contract the next dependency—never smuggle `C.conquest`, save, UI, or a legal movement order into this reader. No simultaneous edits.

**⚡ AMENDMENT — 2026-07-22, D519: LANE-020 RELEASES TO CONTRACT/UNOWNED AT ITS REAL SLICE-5 DEPENDENCY; ARC 9 CLAIMS NO BATCH-TURN SHIPMENT OR RELEASE CHECKPOINT.** D518 is committed and pushed at `3fef22515c097bc48c013984b391030c4a6bd880`, with clean fetched parity against `origin/main`. Slices 1–4 remain shipped and green exactly as recorded in D515–D518. This standalone relay changes documentation and lane ownership only: no runtime source, focused or adjacent probe, generated game, mechanical count/hash pin, save shape/version, suite row, or product behavior moves.

Production still exposes no independent sequential strategic-turn entrypoint before the next real battle. `campaignAdvance` reaches the sole ordered `_t1Resolve` only after receiving a real battle result; the next campaign boundary is another battle. Inventing winner/type/battle authority or silently delegating combat would violate the exact-resolver, stop-before-battle, no-hidden-consequence, historical-consequence, and D74 laws. LANE-020 therefore moves from ChatGPT/Codex `DRIVE` to `CONTRACT` / `none`. Slice 5 remains unchecked, unenrolled, and unclaimed; its future focused transition remains 31→43 and its future release-suite transition remains 140→141. Re-take requires a separately gated independent sequential-turn owner with explicit pending-choice, battle, war-end, election/dated-hinge, error, and historical-consequence stop signals.

No browser gate or full release battery is owed or run for this blocker-only relay. The release preserves the D518 31/31 focused proof, isolated 30/31 Bind S4, 140-row suite, `_SAVE_VER=1`, and all protected owners and counts without recharacterizing the blocked fifth slice as `SHIPPED`. The D514 completion loop now advances the independent ARC 7 non-road conquest work while LANE-020 remains safely available for a later lawful re-take.

**EXACT NEXT:** reload the live ARC 7 law and LANE-019, then make a separate committed contract amendment and DRIVE take for the smallest dependency-ready Transport Movement slice using only the Verified rail, inland-water, and two bounded-sea registries. Preserve all explicit non-links and render road service unavailable; do not invent any road edge, capacity, interchange, eligibility, or geography. Only after that owner-safe routing boundary may runtime or probe work begin. No simultaneous edits.
