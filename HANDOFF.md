# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D526 next-lane=LANE-019 state=CONTRACT owner=none -->
> **Live status (D526, 2026-07-22):** the Historical transport-window prerequisite is source-blocked; LANE-019 is `CONTRACT` / `none` after a bounded 44-service plus four-interchange audit.
>
> **Boundary:** the shipped 10 `eligible`, 21 `conditional`, and 13 `operation-specific` date strings are qualitative evidence, not machine-readable physical windows or legal-now authority. No runtime/data/state/save/UI behavior moved; D525 remains the product head.
>
> **Authority:** `HANDOFF.md`'s first ⚡ block and LANE-019's D526 finding own the boundary. D514 remains binding; the next ARC 7 work requires a separately contracted, mode-bounded source adjudication, while LANE-020 stays `CONTRACT` / `none` at its independent-turn dependency.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-22, D526: QUALITATIVE TRANSPORT DATES CANNOT BECOME HISTORICAL LEGALITY; THE PHYSICAL-WINDOW PREREQUISITE IS SOURCE-BLOCKED.** D525 is committed and pushed clean at `9b94e3f9723f4e1bfd8f90e7c7a924730f141b61`. Package A, all 44 shipped physical-service descriptors, the four separate interchanges, the D497/D499 source packets, and the detached calendar/state/transport owners were re-audited. The service surface divides exactly into ten current `eligible`, 21 `conditional`, and 13 `operation-specific` qualitative records. Their verbatim `historicalEligibility.dateText` preserves source evidence; it is not a machine-readable physical-availability interval, legal-now decision, opening condition, or player-state transition. No service or interchange is runtime-ready from the current export.

Package A §8.30 records the exact row partition. The ten `eligible` plus 21 `conditional` rows are docs-only `QUALITATIVE_OR_MIXED_EVIDENCE_ONLY`: their prose can combine physical existence with control, use, damage, rebuilding, endpoint loss, fragmentation, or condition. The thirteen water/sea operation rows are `OPERATION_OBSERVATION_ONLY`: they preserve a bounded passage or approach but cannot become routine standing service. `CTI-01` through `CTI-04` are `INTERCHANGE_WINDOW_UNADJUDICATED`; a service date cannot be inherited by a distinct handling connection. These labels are audit dispositions only, not future JSON/runtime enum values.

`CTS-S-02` / `WE-26` is the binding parser counterexample. Its “through 15 Jan. 1865; closed thereafter” phrase describes the Cape Fear approach closing to blockade runners after Fort Fisher; the same row separately records the joint operation's actual Union landing and fort capture. Treating it as a generic physical-service end date reverses the source meaning. No parser or future law may substitute control, capture, military use, cut/damage, service condition, blockade state, or a named operation for physical existence. Exact source dates stay separate from the two-month campaign calendar and may not be rounded to a turn boundary. Unresolved or disputed claims remain unavailable.

**Architecture/dependency verdict:** create no cross-mode packet, parallel table, second registry, schema, cursor, parser, or runtime owner. New source claims belong in the existing D497 rail or D499 water packet; only a later separately contracted and source-proved pass may extend the existing evidence JSON and transport owner. A current-turn cursor lacks a state holder; opening territory/node/service values are unsourced; receipt payload/precondition/transition law is unchosen; and a route query would require forbidden topology or prose interpretation. Those candidates are rejected as premature, not implemented as empty mechanical slices.

D526 changes only Package A law, the existing transport planning proof, routing/history/checklist documents, and the byte-verbatim HANDOFF archive. No product source, data, manifest, build source, generated game, save shape/version, suite enrollment, UI, state, receipt, topology, movement, or existing runtime owner changes. Normal build and `--check` are `GATE OK` at data 65, territories 36, transport 27/15/2/4/18, manifest 112 with 116 last, and suite 140. Final static proof is doc coherence 5/5, transport plan 12/12, conquest plan 8/8, Mayhem plan 13/13, and War Career plan 24/24; syntax, `git diff --check`, exact 11-file allowlist, protected no-diff audit, and the 5,870-byte D524 archive comparison pass. Independent Sol-high re-audit reports PASS with no remaining blocker. No browser or full 140-row battery is owed for this docs/planning-only finding; the first playable-loop release checkpoint remains deferred.

LANE-019 remains `CONTRACT` / `none`; D525 is still the product head, and D526 is a bounded prerequisite finding rather than playable conquest or a lane-level `SHIPPED` claim. **EXACT NEXT:** under D514, D527 must make a separate committed docs/research-only DRIVE take for the first mode-bounded source adjudication, beginning with the 27 rail services. It must use the existing rail packet; distinguish construction/physical existence from canonical control, operation, and service condition; preserve exact dates and the two-family source floor; retain every unresolved row; examine but not prematurely authorize the rail-facing evidence for `CTI-01` through `CTI-04`; define a row-by-row audit/probe and exact allowlist; and stop before data/runtime/current-turn/state/receipt/topology/movement/live-save work. If rail evidence remains insufficient, quarantine it and continue the next independent D514 item. LANE-020 remains separately blocked; no simultaneous edits.

**⚡ AMENDMENT — 2026-07-22, D525: THE PURE DETACHED CONQUEST CAMPAIGN CALENDAR SHIPS; LANE-019 RELEASES BEFORE OPERATIONAL STATE OR LIVE INTEGRATION.** D524's exact contract/DRIVE take is pushed at `7adcc28ececc543119d2dba21bbd26e682030333`. Existing sole owner `src/116-conquest-state.js` now additionally exposes exactly one global, `conquestCampaignCalendar()`. It has declared arity zero; any supplied argument returns `null` before inspection or coercion. A valid call returns a fresh deeply frozen Array of exactly 25 records: 24 exact two-month `regular` intervals from Jan-Feb 1861 through Nov-Dec 1864, followed by one exact Jan-Apr 1865 `endgame` interval. Every record and date object is a fresh ordinary exact-key own-data object; all nested values and the Array are frozen, and no identity is shared within or across calls.

The schedule is immutable detached law only. Ordinal 25 remains display order, not a normal twenty-fifth turn, action, resolution boundary, current/default/next/legal-now value, or eligibility decision. The query reads or writes no `G`, `C`, settings, storage, DOM/UI, board, transport, save, or alternate authority; it has no ruleset branch, source-date transformation, Historical/service availability, or operational field. D523's strict factory/view code is unchanged; every factory retains the exact empty extensible `conquest` namespace, and the calendar attaches to neither root nor namespace. No module, manifest row, data, build source, suite row, save owner, or public entry path moved.

The suite-excluded focused proof retains the original ten D523 teeth and adds exactly five, now green 15/15 with `ok:true`, zero failed steps, errors, pageerrors, or realErrors. Bind B changed only the unique `CONQUEST_CALENDAR_BIND_B:ENDGAME_APRIL` end-month literal from 4 to 3 after proving one source anchor and one probe value assertion. The rebuilt mutated artifact exited 1 at exactly 14/15 with only `CALENDAR ENDGAME: exact bounded Jan-Apr 1865 row; ordinal grants no turn authority` red and exact runtime error `bounded endgame interval drifted`; all other teeth and browser error arrays stayed clean. Restoration returned source/game MD5 exactly to `45246d89bb479f7a7c5934c38cd78f65` / `269ecc62eed83e6220c42c0efab40d38` and SHA-256 exactly to `898f5b96d791e6546880cff7306d2add5df8e30572aaa55ad2dc5b773c61ab64` / `ffd73d7dc593dfdd157af39601f8505b2319591cea30ed830717c043dde80c4f`, followed by clean 15/15. Independent read-only Sol-high audit reports PASS with no blockers.

Normal build is `GATE OK` at data 65, conquest territories 36/36, transport 27/15/2/4/18, manifest 112 with 116 last, and suite 140. Source-tree MD5 is `9f4ffbff132bf2b22b1174538b9a9311`; manifest MD5 remains `4cc094911506cf5af9929cf79d25dc6c`; frozen base remains `c9db83fa99230ffb95bdfdfe059f3fb9`; save shape and `_SAVE_VER=1` hold. Final planning/focused adjacency is doc coherence 5/5, transport plan 11/11, conquest plan 8/8, open-history Mayhem plan 13/13, War Career plan 24/24, state 15/15, transport 18/18, board 13/13, Mayhem mode 24/24, and save slots 17/17. Fresh browser artifacts are all `ok:true` with zero failed/page/real errors. The full 140-row battery remains deferred until the first playable conquest loop.

LANE-019 returns to `CONTRACT` / `none`; D525 is a pure prerequisite, not playable conquest and not a lane-level `SHIPPED` claim. **EXACT NEXT:** under D514, D526 must re-read Package A, the full lane, the existing foundation/calendar/transport owners, and current live/save owners; separately adjudicate the smallest dependency-ready prerequisite and commit an exact owner-safe contract/DRIVE take or a bounded prerequisite finding before implementation. Do not infer opening control, node operation, service condition, armies, Historical availability, receipt grammar, current/default turn, migration, topology, movement, or a live start/load/save-acceptance path. LANE-020 remains independently blocked on a sequential strategic-turn owner. No simultaneous edits.
