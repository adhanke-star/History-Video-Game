# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D524 next-lane=LANE-019 state=DRIVE owner=ChatGPT/Codex -->
> **Live status (D524, 2026-07-22):** LANE-019 is under ChatGPT/Codex DRIVE for one pure detached campaign-calendar query in the existing sole conquest-state owner.
>
> **Boundary:** D523 remains the runtime head; D524 changes routing/law plus the transport-plan current-contract tooth only. D525 may add one zero-argument immutable 25-interval schedule query to `src/116`; it adds no `C.conquest` field, current/default turn, live start/load/save/UI, eligibility, control, service, army, order, movement, or operational behavior.
>
> **Authority:** `HANDOFF.md`'s first ⚡ block and LANE-019's D524 exact contract own the slice. D514's work-conserving loop remains binding; LANE-020 stays `CONTRACT` / `none` at its independent-turn dependency.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-22, D524: LANE-019 TAKES DRIVE FOR ONE PURE DETACHED CONQUEST CAMPAIGN CALENDAR; OPERATIONAL STATE AND LIVE INTEGRATION REMAIN CLOSED.** D523 is committed and pushed clean at `b59532cf1092b3987a94ace1830ef897fa9a95f2`. The Package A law and current owners were re-inventoried before this take. The frozen ladder `startCampaign` immediately launches a battle; `campaignAdvance`, `CHAINS`/`C.idx`, mutable `C.clock`, and President date are battle-derived lifecycle owners; save/apply/slot/undo/Ironman are broad acceptance owners. None may supply the conquest calendar. The only next prerequisite whose values are completely fixed without inventing control, node/service state, armies, Historical windows, receipts, or migration is §8.13's schedule: 24 two-month intervals from Jan-Feb 1861 through Nov-Dec 1864 plus one bounded Jan-Apr 1865 endgame interval.

**Architecture/overlap verdict:** extend existing sole conquest foundation owner `src/116-conquest-state.js`; do not create module 117, reuse or modify the ladder/Clock/President owners, or attach static calendar data to `C.conquest`. D523's factory/view and exact empty future-expandable factory namespace remain byte-for-byte behaviorally unchanged. Storing a derived immutable schedule in `C.conquest` would duplicate configuration into future saves; a detached pure query keeps fixed law outside mutable campaign state. Operational-state schema, receipt validators, and live/save integration are rejected as premature because their exact fields, initial values, transition grammar, referential rules, and acceptance path are not yet lawfully determined.

D525 may add exactly one global, `conquestCampaignCalendar()`, with declared arity zero. Any call with `arguments.length !== 0` returns `null` without coercion, throw, or effect. A valid call returns a fresh deeply frozen Array of exactly 25 distinct records. Each record is an ordinary `Object.prototype` object with exact ordered own enumerable data keys `ordinal`, `start`, `end`, and `kind`; `start` and `end` are distinct ordinary objects with exact ordered own enumerable data keys `year` and `month`. No hidden/symbol/accessor extra exists. Every array, record, and date pair is frozen; no nested identity is shared within one result or across calls.

Months are inclusive 1-based integer calendar labels. Records 1-24 have exact `kind:"regular"`; for ordinal `n`, year is `1861 + floor((n-1)/6)`, start month is `1 + 2*((n-1)%6)`, and end month is start plus one. Record 25 is exact `kind:"endgame"`, start `{year:1865,month:1}`, end `{year:1865,month:4}`. `ordinal` is schedule/display order only: ordinal 25 is not a twenty-fifth normal turn, grants no action or resolution boundary, and establishes no current/default/next/legal-now meaning. The query exposes no label/id, resolution function, source-date transformation, ruleset branch, Historical or service window, eligibility, or availability.

The calendar reads or writes no `G`, `C`, settings, storage, DOM, UI, board, transport, save, or alternate authority. It does not attach to or serialize through the factory/view, and adds no current turn, advance/mutation, start/load/continue action, migration, control, node operation, service condition, army, order, movement, adjacency/topology, cut/repair, capacity, economy, theater, directive, AI, battle/result, Chronicle, career, builder, or tactical behavior. Exact historical source dates remain on their evidence rows; this schedule never rounds or interprets them.

**D525 proof/allowlist:** change only existing `src/116-conquest-state.js`; grow existing suite-excluded `tools/probe-conquest-state.mjs` exactly 10→15 while preserving the first ten D523 teeth; update only mechanically proved generated/source pins in the transport/current-boundary, Mayhem, and War Career planning probes; rebuild `civil_war_generals.html` normally; and synchronize milestone law/routing/archive docs. No new module, data, probe, build source, manifest row, suite row, UI, or save owner is allowed. Data remains 65, manifest remains 112 with 116 last, suite remains 140, `_SAVE_VER=1`, save shape, and frozen base hold.

The five new focused teeth are: zero-arity API and strict extra-argument failure; the exact 24 regular interval formula/order/bounds; exact Jan-Apr 1865 endgame semantics including the ordinal limitation; exact prototypes/keys/descriptors/deep freeze and fresh non-shared identities across calls; and ruleset/external-authority purity plus absence of current/legal/operational semantics or factory attachment. Bind B changes only the uniquely anchored endgame end-month literal from `4` to `3`; the anchor must occur exactly once before mutation, only the endgame tooth may assert month 4, and the result must be exactly 14/15 with only that tooth red. Restore both source and generated HTML byte-identically, rerun 15/15, then run syntax, normal build `GATE OK`, doc coherence, transport/conquest plans and browser probes, Mayhem mode/plan, save slots, War Career plan, fresh artifact readback, exact allowlist, and `git diff --check`. The full 140-row battery remains deferred to the first playable conquest loop.

This D524 commit changes routing/law and the transport-plan current-contract tooth only; D523 remains the product/runtime head. LANE-019 is `DRIVE` / ChatGPT/Codex from clean D523, and no simultaneous edits are permitted. **EXACT NEXT:** after D524 is committed, pushed, fetched, and proven clean, implement only the detached calendar query under this allowlist as D525; grow the focused proof exactly 10→15; execute Bind B and byte restore; rerun every declared gate; synchronize D525 evidence; commit, push, fetch, and prove clean parity. Then reload the ledger under D514—never attach the schedule to campaign state or infer the still-undefined operational/save layers.

**⚡ AMENDMENT — 2026-07-22, D523: THE DETACHED CONQUEST IDENTITY/STATE SERIALIZATION FOUNDATION SHIPS; LANE-019 RELEASES BEFORE LIVE OR OPERATIONAL STATE.** D522's routing/representation contract is pushed at `bfbe64b55a452e84c50b379db2617b2fc6317d06`. New sole owner `src/116-conquest-state.js` is enrolled last as module 116 and exposes exactly `conquestCampaignFoundation(startView)` and `conquestCampaignFoundationView(candidate)`. The factory accepts only exact own-data `{side,ruleset}` views with `Object.prototype` or `null` prototypes, side `US`/`CS`, and exact Historical/Mayhem version-1 ruleset views. It returns exact ordered keys `side`, `campaignKind`, `ruleset`, and `conquest`: immutable `{id:"conquest",version:1}` campaign identity, the existing Mayhem-owned frozen ruleset value, and an exact empty `Object.prototype` namespace whose root descriptor is locked while the object remains extensible for a later contract.

Every prototype, own name, symbol, and descriptor is checked before descriptor values are read. Custom/inherited authority, hidden or symbol extras, accessors, extra/missing keys, wrong scalars, malformed/revoked proxies, and unsupported ids/versions fail `null` without accessor invocation or input mutation. Ruleset ownership remains exact: the factory calls `mayhemInit(carrier,id,"new")` only on a disposable local carrier, verifies the resulting frozen enumerable non-writable/non-configurable ruleset descriptor/value, transfers only that descriptor/value, and discards every incidental carrier receipt. `mayhemInit` never touches the final root. The view accepts only an extensible ordinary root in one internally consistent descriptor mode—locked factory form or ordinary JSON-round-trip form—then rejects mixed modes, non-extensible roots, locked-but-extensible nested identity objects, frozen/non-extensible factory namespaces, legacy/operational fields, and all malformed channels. Its only success is a new deeply frozen exact snapshot.

The ten-step suite-excluded focused proof is green 10/10 with `ok:true`, zero failed steps, errors, pageerrors, or realErrors. Independent implementation review found one real escape—the view accepted a frozen factory `conquest` namespace—and the code plus a permanent negative tooth were hardened before final evidence. Additional exact meta-state negatives cover prevented-extension roots and locked-but-extensible nested values. The controlled serialization step uses only a test-local `serializeSave` fixture and restores `G` exactly; production save acceptance, load/apply, slots, import/export, undo, bookmarks, Ironman, migration, and War Career `runId` remain untouched.

Bind A removed only the unique `candidateKind.version !== 1` validation. The focused proof exited 1 at exactly 9/10, with only `REJECTION / NO MUTATION: legacy, malformed, mixed, and invalid discriminator fail closed` red and exact error `invalid discriminator accepted`; errors/pageerrors/realErrors remained zero. Restoration returned source/game MD5 exactly to `6e43ecb55961f4d85682212f1565670c` / `c2a7b0cbeb5b93c2b786b094f5047d0c` and SHA-256 exactly to `49f7261b6c0df3d2f36698ac7d37a5e8c1a4d04e155a4d00d4ec63c1c558b41a` / `9e26cac19265cf9d6d686bd88802faf13211c4361d8e9997d0bfbcb565232107`, followed by a clean 10/10 rerun. Manifest MD5/SHA-256 is `4cc094911506cf5af9929cf79d25dc6c` / `d85bc76c5e582638f8837db297f1e643e886880d5a1b6a2680e35f59996e05f2`; source-tree MD5 is `45aea5f83596828491b711f72e82e7eb`.

Normal build is `GATE OK`: data 65, territories 36, transport 27/15/2/4/18, manifest 112 with 116 last, and suite 140. Final planning/focused adjacency is transport plan 11/11, conquest plan 8/8, open-history Mayhem plan 13/13, War Career plan 24/24, conquest state 10/10, transport 18/18, board 13/13, Mayhem mode 24/24, and save slots 17/17; every fresh browser artifact is `ok:true` with no failed/page/real errors. `_SAVE_VER=1`, save shape, frozen base, data, build source, existing save/Mayhem/board/transport owners, and every live UI/runtime authority hold. The full 140-row battery remains deferred to the first playable conquest loop.

No `G`, settings, storage, DOM, UI, campaign entry, live save/load acceptance, migration, topology, adjacency, territory control, node operation, service condition, army, date, order, movement, capacity, cut/repair, economy, AI, battle/result, Chronicle, career, builder, or tactical behavior ships. LANE-019 returns to `CONTRACT` / `none`; D523 is a detached foundation, not playable conquest and not a lane-level `SHIPPED` claim. **EXACT NEXT:** under D514, D524 must first re-read Package A, the full lane, the current sole owners, and the now-shipped detached shape; separately adjudicate the smallest dependency-ready state/integration prerequisite and either commit an exact owner-safe contract/DRIVE take or record a bounded prerequisite. Do not infer opening control, node operation, service condition, armies, Historical availability windows, migration, topology, movement, or a live start/load/save path. No simultaneous edits.
