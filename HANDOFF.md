# HANDOFF — "The Civil War"

**THE LIVE-HEAD SINGLE-SOURCE RULE (D412):** the top ⚡ block below is THE one canonical live boundary for the whole project. Every other canonical doc (START-HERE, WAKE-UP, AUTONOMOUS-RUN §2, V1-CHECKLIST's live gate) carries at most a 3-5 line summary plus a pointer here; RUN-LOG stays the chronological log. This file keeps the latest ⚡ block plus exactly ONE prior; older superseded blocks move byte-verbatim to `legacy/HANDOFF-ARCHIVE.md` at each release (newest at top), gated by the probe-anchor inventory. Full rules: `START-HERE.md` §Naming convention.

## ⚡ HANDOFF — 2026-07-17 **D417: HISTORICAL AND MAYHEM ARE NOW A BIND-TESTED DUAL-RULESET CONTRACT; MAYHEM MAY SCORE, CREDIT CASUALTIES, ALTER RESULTS, REWARD ANY DECLARED ACTION, AND GRANT FACTION/IDENTITY-TAG POWERS WITHOUT A MORAL GPA; LANE-007 IS CONTRACT/UNOWNED.** Aaron's D416 override superseded D382's universal consequence-only/never-scored/dignity-lock interpretation before runtime; the stale uncommitted surrender research was removed and pushed history retained. D417 ships `docs/design/open-history-mayhem-mode-design.md` (md5 `bc54781364ab9da94fdb46a70fd42ae4`) plus suite-excluded `tools/probe-open-history-mayhem-plan.mjs` (md5 `c28938141ca02d57bd2bcb8aba528eb1`, 13/13). **Historical** preserves the current cited teaching game and old restrictions. **Mayhem — Break the Timeline** unlocks every historical gameplay/content guardrail: previously excluded battles/people/sides/eras/tech/policies/surrender/no-quarter/atrocity branches may be playable and declared effects may change score, casualty credit, winner/result, victory progress, resources, loot, promotion, reputation, achievements, and side/faction/identity tags; no symmetry/plausibility/citation/dignity gate steers it back to the record. The mode label prevents fiction from masquerading as Verified history but is not a per-card warning or judgment. Mayhem AAR grades performance/rewards/style only; no moral/plausibility GPA; history compare is optional/off by default. The owner is exact `C.ruleset={id:"historical"|"mayhem",version:1}`, immutable per timeline, campaign-serialized at `_SAVE_VER=1`, independent from `G.settings`/difficulty/realism/playStyle/`altHistoryEmergentOnly`; missing/malformed values fail closed Historical; Historical enters Mayhem only through a named fork; standalone launches carry a local snapshot. One closed-world, atomic, idempotent effect/receipt pipeline may perform full Mayhem authority through existing domain owners; imported content names allowlisted ids and never code. D74 remains one-engine discipline, not a Mayhem outcome ban. Identity/cultural/faction power uses composable tags, not one global biological race scalar. The declared bind changed only `DECLARED_ACTIONS_MAY_SCORE`→`DECLARED_ACTIONS_NEVER_SCORE`: exactly MAYHEM AUTHORITY red, 12/13 green, exit 1; design/probe restored md5-identically, final 13/13. Planning moved no runtime/data/manifest/suite/generated/base byte: game `9d7d91078dd8fceea847f1c2aff4dc5f`, base `c9db83fa99230ffb95bdfdfe059f3fb9`, manifest `7924da858de403cac58caabf8c9fcce8`, suite `4bcdc6f252389a4bfd6bed269b52f8f0`, 24/54/1512/24, suite 130 row 38, `_SAVE_VER=1`; build GATE OK; all thirteen existing coordination plan probes green; no browser/full battery owed; D398 remains latest full release battery. **EXACT NEXT: LANE-007 Slice A.** From the clean D417 Desktop boundary, take DRIVE in a committed ledger-only edit; add `src/107-mayhem-rules.js` after 106; implement the exact campaign owner/readers/sanitizer; wrap `_openMusterChoice`/`startCampaign`/`_t1InitAll` without touching frozen base so the mode attaches before init; sanitize after `applySave`; extend save slots and H0 readouts; prove all H0/legacy US/CS routes, legacy/bad fallback, immutability, save/import/undo/cross-slot isolation, no global-setting authority, and Historical byte-equivalence in new `tools/probe-mayhem-mode.mjs`; enroll one suite row (130→131); run node checks, build, focused probe, save-slots, H0 main menu, playstyle, divergence, full campaign, JSON/pageerror readback, six byte-restored binds, and diff check. Keep the picker behind the single fail-closed `MAYHEM_PUBLIC_READY=false`; public paths remain Historical until Slice C ships the real no-quarter score/casualty/reward/tag proof and no-judgment result readout. Commit + push Slice A, keep the lane DRIVE, point exactly to Slice B, and do not run the full battery. Work only in `/Users/aaronhanke/Desktop/Video Game`; no simultaneous edits by any provider.

## ⚡ HANDOFF — 2026-07-17 **D413: MATTERS OF STATE ARE LIVE — THE FIRST POLITICAL CAPABILITY IS EARNABLE AND CONSUMED; EVERYTHING SHORT OF THE UNLOCK VISIBLY DEFERS; LANE-005 IS CONTRACT/UNOWNED.** D413 (LANE-005 take `a3aec520c3f8e4e823d453011488a7447906c040` from clean D412 boundary `70e0f990bbe8d9b37636b5484ebf5f8245467683`) implemented the D408 §17 contract UNCHANGED (design §17 the law; DECISIONS D413 the record): (1) the pure receipt-derived reader `warCareerDecisionAccess(C)` in src/106 — unlock requires BOTH reconstructed current-person `general-command` (`_wcCareerAuthority` ok at that band) AND a latest independently validated qualifying current-person receipt with canonical `battleYear >= 1864` (`_WC_POLITICAL_DATE_YEAR`, ONE law site, bind token `WAR_CAREER_POLITICAL_DATE_BIND:QUALIFYING_RECEIPT_YEAR_1864_OR_LATER` at the site); `_wcCalculateAdvancement` derives `latestQualifying` (highest validated chain rung) on every read and NOTHING is persisted; `warCareerCapabilities.nationalDecisions` flips to the reader's verdict while cabinet/appointments/resources stay false; live clock/date, saved booleans/scalars, rank text, rapport, names, and source rewriting never grant (forgery proven to fail closed and sanitize away live). (2) The shared seam guards in src/32 — `decRenderTab` and `decInterstitialHTML` keep every pending card fully readable (situation/options/teaching/provenance/sources/Why) and add one focusable `tabindex="-1"` defer note per card naming the missing date, authority, or both; every native Decide button stays keyboard-focusable with `aria-disabled="true"` + `aria-describedby` → the note; `_decWireCards` re-reads the law at activation and moves focus to the explanation instead of resolving; direct `decResolve` refuses before `_decApply`; `decOnResolve` is deliberately ungated (queue initializes/expires/enqueues while locked — proven live with `us-press-censorship` surfacing at 1864, expiring at 1865, re-enqueueing at 1864). Legacy/no-career renders byte-identically (zero lock bytes on bypass; probe-decisions 19/19, probe-desk 13/13, probe-full-campaign 4/4 green); the Desk and between-battle routing literals (openWarDept/_wdRefresh dispatch, `H0_DESK_TABS` `["decisions", "Decisions"]`, `h0iDecisions(C)`) are proven UNEDITED in the built game. (3) ONE new focused browser row (`D408 MATTERS OF STATE + VISIBLE DEFER`: legacy bypass · both-missing · Rhodes rungs 1-6 = date-only (latest 1863) · nashville-private = authority-only (latest 1864, junior command) · rung 7 = unlocked with `nationalDecisions:true`, two green save roundtrips, no persisted derived field, and a wired Decide resolving exactly once) → **War Career 44/44 browser + 30/30 static, zero pageerrors/realErrors**; (4) the declared plan-probe transition only — `D408_CONTRACT_ALLOWED` gains exactly `src/32-decisions.js`; focused-structure pin 42→43 literal steps; plus ONE discovered-at-gate item (the D411 loot-pin class, documented in DECISIONS D413): the changed-path allowlist predates the D412 HISTORY ARCHIVAL RULE, so the five `legacy/*-ARCHIVE.md` closeout-archival targets were admitted with a documented-history comment; **plan 24/24 with all names/order exact**. **Moved hashes documented old→new: game `7de51b310e09a710eb83ade276952203` → `9d7d91078dd8fceea847f1c2aff4dc5f`, srcTree `a48ceb72a951d516404f5eec29ec2d2b` → `a4a46fbcff478c239de037f4a63105a4`, src/106 `91bd8cd3c80e59b510726e29a16c89bb` → `8e09ebbf56ba3433712f91936f438e5d`, focused probe `5e856b3f21e371f867ce99f848c0a155` → `b7d6246e10357afc2a4e8f07f8c5dcea`; base/manifest/suite/Command/Command-probe/src-37/dataTree/T2/T3/Auto/After-Action pins NEVER moved; Command 94/94 byte-identical; suite 130 with War Career row 38; 24/54/1512/24; `_SAVE_VER=1`; D398 remains the latest full release battery; `npm run vet:noreg` was not run (not owed in D413).** Five byte-restored negative binds bit EXACTLY the new D408 row at 43/44 each (A: aria-disabled/describedby emission removed while decResolve stayed guarded → `disabled:0, described:0`; B: `_WC_POLITICAL_DATE_YEAR = 1863` → the requiredYear-1864 tooth; C: any-band authority accepted → `missingAuthority:false` on a rank-and-file career; D: no-career path gated → `legacy access law moved`; E: direct `decResolve` guard deleted → `direct decResolve applied while locked`); every restore returned src/32 `922d6fd051c3ca310d0d2959e4018cfd`, src/106 `8e09ebbf56ba3433712f91936f438e5d`, and the game md5-identically before the final green rerun; red teeth never landed. Cabinet, appointments, resources, combat/balance, relationships, data, manifest, frozen base, T2/T3/Auto, After Action, save version, President/H0/legacy shells, and Slice F all stayed closed. **LANE-005 is CONTRACT/unowned. EXACT NEXT: no queued build work remains on this lane — D408 §17 is shipped at 44/44; Slice F stays closed; next Slice-E/political work (or any other queue selection) only on Aaron's explicit direction (see START-HERE Priority picker / DECISIONS D382).** Work only in `/private/tmp/codex-vg-recovery-019f62fe`; never touch the dataless Desktop checkout; no simultaneous edits by any provider.



**Older handoff blocks (D412 and earlier, plus the run-h scaffold sections):** moved byte-verbatim to [`legacy/HANDOFF-ARCHIVE.md`](legacy/HANDOFF-ARCHIVE.md) — newest at top (D412 moved at the D417 release; D411 at D413; D410 → run-h in D412). Nothing was deleted or paraphrased beyond the standing "(superseded by …)" label flip.
