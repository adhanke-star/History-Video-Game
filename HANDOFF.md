# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D538 next-lane=LANE-022 state=DRIVE owner=Claude -->
> **Live status (D538, 2026-07-23):** `LANE-022` takes **Claude Code DRIVE** for **Slice 1 — the READ-ONLY traced supply route**. `_lgRoute`'s static per-battle lookup gains one guarded conquest tail that traces a real path across the read-only 36-territory board; the capped bridge, every downstream logistics owner, and the sim stay byte-identical.
>
> **Boundary:** Ledger-only — no `src/`, `data/`, `build/`, manifest, suite, save-shape or generated byte moved in this commit, so the pinned game/srcTree hashes do NOT move and are not re-pinned. D525 remains the ARC 7 product head; LANE-019 is deliberately untouched and every one of its boundary sentences stays exact.
>
> **Authority:** D537 ruling 4 — Slice 1 is READ-ONLY so the trace's correctness proof is never mixed with a sim-affecting A/B. Containment fails CLOSED at the ruleset seam; Historical transport movement, Historical roads and the four `CTI-*` interchange faces stay blocked permanently.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-23, D538: LANE-022 TAKES CLAUDE CODE DRIVE FOR SLICE 1 — THE READ-ONLY TRACED SUPPLY ROUTE (CONTRACT ONLY; NO RUNTIME BYTE MOVED).** D537 is committed and pushed clean at `0829d8dedabbc8e8943ea534eaa279c713d06b81`. This is the contract-before-teeth commit the Contract Relay's hard rule requires: the lane carries the full Slice 1 acceptance contract as committed prose, and the acceptance teeth land in the SAME commit as the code that greens them. Nothing in `src/`, `data/`, `build/`, the manifest, the suite, the save shape or the generated deliverable moved here, so the pinned game/srcTree hashes do not move and are NOT re-pinned.

**The contract.** `_lgRoute(C, bd)` at `src/61-logistics-rail.js:51` returns a STATIC per-battle friction over a fixed battle chain — the exact GEA-11 defect that `docs/design/conquest-supply-chain-design.md` §3 names as the single structural change of the whole ladder. D538 contracts ONE guarded tail on it: when the carrier declares an exact conquest campaign kind AND the open ruleset, `_lgRoute` attaches one extra derived `trace` field carrying a real path across the read-only 36-territory board — source depot → rail/inland-water/sea segments → the army's territory — while its shipped six keys stay computed exactly as today. Everything downstream is untouched by construction: `logisticsSnapshot`, `logisticsBridgeBonus`, `logisticsSetPriority`, `logisticsOnResolve`, `presLogisticsBlock` and `logisticsWireOverview` keep their current text and behavior, so no snapshot field, no capped-bridge field (`supply ≤ 7`, `fatigueRelief ≤ 5`, `overall ≤ 2`) and no DOM byte moves in ANY campaign, conquest included. The trace carries `applied:false` and a derived `tracedFriction` that NOTHING consumes — Aaron's D537 ruling 4 read-only-first, so the trace's correctness proof is never mixed with a sim-affecting A/B. No new module (manifest stays 112 / `116-conquest-state.js` last), no data change (65 files), no `C.conquest` field, no save-shape movement.

**Containment is structural, and it is the declared bind target.** One ruleset gate returns a view ONLY for the open ruleset and returns `null` for every other value — the sourced ruleset included — BEFORE any board or transport-evidence read happens, so the boundary fails CLOSED at the ruleset seam rather than by label or filter. `src/115-conquest-transport.js` is READ, never written and never namespace-shared. The authored Mayhem content is exactly: two depots (`US` → `CT-01`, `CS` → `CT-05`), four theatre fronts (`E` → `CT-03`, `W` → `CT-20`, `TM` → `CT-32`, `N` → `CT-19`), four per-mode segment costs, and the projection of each evidence row's existing `territoryRefs` into a directed graph honouring that row's existing `direction`. Every Historical negative stays exact — zero road service with `ROAD_REQUIRES_BOUNDED_SOURCE_PASS`, `CTI-01`..`CTI-04` permanently `INTERCHANGE_WINDOW_UNADJUDICATED` and not consumed by the trace at all, New Orleans-origin and `CT-36` closed, the D503 endpoint quarantine, the Potomac/operation-composition/Sherman-chain negatives, and Boonville, Arrow Rock and Glasgow unassigned. No qualitative `dateText` is ever parsed into a machine-readable window (D526 bars it absolutely; the `CTS-S-02`/`WE-26` Fort Fisher row proves a generic parser reverses the source).

**The honest Slice-1 result, contracted in advance.** The 44 sourced services resolve into ELEVEN disconnected components (`CT-01..CT-04`; `CT-05/06/07/09/10`; `CT-11..CT-28`; `CT-35/36`; and seven singletons), so most traced pairs return `reachable:false` — the default `US`/`E` pair resolves in one rail segment (`CTS-R-02`, the Verified Washington–Manassas corridor) and the `CS`/`E` pair does not resolve at all. That is the CORRECT recorded outcome and the precise reason Slice 5 authors the road layer. It is never cured by inventing a service, window, interchange or endpoint.

**Gates owed at the shipping commit:** `node --check` on every touched file; `node tools/build.mjs` GATE OK; artifact readback of the trace symbols inside the built 61 module; the two NEW probes (`tools/probe-conquest-supply-plan.mjs` filesystem-first, `tools/probe-conquest-supply.mjs` focused — node-side `node:vm` teeth over the on-disk `src/115` + `src/114` + `src/61` so a source mutation reds WITHOUT a rebuild, plus in-page teeth against the built deliverable); both enrolled in `tools/vet-no-regression.mjs`, moving the suite 140 → 142 with every suite-count pin in `tools/` re-anchored by chained D538 comment in the same commit; the adjacent logistics/bridge/conditioning/board/state/transport/campaign-link/full-campaign/auto-resolve/save-slots/command probes; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest 8/8, doc coherence 5/5; **A/B leg 1** — the standing 24-scenario × 8-seed direction battery byte-identical to the `0829d8d` baseline artifact md5 `18f609d07b1190904ec0c11e4ca64675`; **A/B leg 2** — `tools/probe-full-campaign.mjs` byte-identical to baseline `a38185fd371a7f181250eff3a6cbf76a`; ONE declared negative bind on the ruleset gate redding ONLY the node-side containment tooth with an md5-proven byte-identical restore; `git diff --check`. The full serialized 140-row battery stays owed at Slice 7. The suite-excluded D528/D530/D532 research guards are neither rerun nor edited, and `tools/probe-desk-pacing-plan.mjs` is a superseded non-enrolled LANE-020 probe that is not a gate. **EXACT NEXT:** implement D538 exactly as contracted, ship it under its own D###, then release LANE-022 to `CONTRACT` / `none` and contract Slice 2 or issue the capacity relay. ARC 7 Historical movement, Historical roads and E46 remain blocked; D525 remains the product head until Slice 1 ships.

**⚡ AMENDMENT — 2026-07-23, D537: AARON DECISION SESSION — THE FULL OPEN-FORK LEDGER IS RATIFIED IN 14 RULINGS; ARC 7 BUILDS ON THE MAYHEM RULESET WITH A TRACED SUPPLY CHAIN (NEW LANE-022 AT CONTRACT); E57 IS THE SECOND LANE; ARC 9 CLOSES COMPLETE; THE SEED'S SECOND "KEYSTONE" IS DISSOLVED AS ALREADY-SHIPPED.** D536 is committed and pushed clean at `d5f5515`. This was an interactive decision/planning session, not a build: nothing in `src/`, `data/`, `build/` or the generated deliverable moved, so the pinned game/srcTree hashes do not move and are NOT re-pinned.

**THE MATERIAL FINDING — the seed's second keystone does not exist on disk.** The session seed named E53-v2 attacker tactical parity as a live KEYSTONE ("HONESTLY RED at D269, revert-file-halted; fork is Aaron's") gating E46, the E47 flips and PM2/PM3, with three unsolved mechanisms and evidence in `.tmp/e53-build-d269/EVIDENCE.md` + `DECISION-NEEDED-e53-build.md`. Live disk contradicts every part: **D272** locked the doctrine (A+B retained, C dropped, mass-capture governor scoped E53-active-only, Bull Run cautious-v2 + an accurate `bullrun1` row), resolving all three named mechanisms, and deleted the fork file as resolved; **D273 SHIPPED** the build (`src/tactical/T26-attacker-parity.js`, manifest-enrolled, full battery green ×2 byte-identical); **D275** shipped E54's pocket-collapse valve *and* made the E47 Shiloh/Gettysburg flips live; **D277** closed the parity arc; **D288** closed E59. The cited fork file is absent from disk exactly as D272 recorded. `V1-CHECKLIST.md:81` was correct. Per the HALT rule, disk wins and the contradiction is recorded. The family's only survivor is **E46**, honestly red-halted twice (D264, D276). Harvest corrections verified the same way: **E55** resolved D265, **E58** resolved D273, **E50** resolved D353 (the line-215 filing is the stale original), **S47** resolved D373 not D372, **GEA-07/08/09/10/12/13/14** shipped D444–D451 and settled D453 with stale ledger rows, **GEA-11** superseded by D455 — and **E54 is genuinely OPEN**, since D275 shipped a narrower valve while the rally branch at `src/tactical/T0-field-sandbox.js:537-552` is verbatim unimplemented.

**THE 14 RATIFIED RULINGS.** (1) **ARC 7 keystone — build the transport/supply ladder on the MAYHEM ruleset now**; Historical movement stays evidence-gated, because D528 (rail 0/19/7/1), D530 (water+sea 0/11/6/0) and D532 (roads 2 cured / 4 unresolved) closed three citation-grade passes at zero established physical windows and D526's `CTS-S-02`/`WE-26` Fort Fisher row proves a generic parser reverses the source meaning. Rejected: relaxing the Historical floor; a sixth pass as the primary route; closing transport outright. (2) **Roads — sourced seed plus authored network, Mayhem-only**: `RD-SI06`/`RD-SI13` as sourced labeled exemplars that teach road FAILURE, the rest openly authored and visibly marked. (3) **`CTI-01`..`CTI-04` — Mayhem-only interchanges**; Historical keeps all four unadjudicated permanently. (4) **Supply slice 1 is READ-ONLY** — the traced route changes no outcome and the sim stays byte-identical. (5) **Blockade severs the sea edge**, gating the two bounded sea services as the Confederate import edge with no new owner; contested river control is deferred as a second ARC. (6) **E57 is the second contracted lane.** (7) **E46 re-attempts AFTER E57** — both red-halts failed on the 220s clock E57 normalizes. (8) **E54 parked on a NEW trigger** (a battery redding on rout-cycling or an unrealistic missing column); all three original customers are dead. (9) **ARC 9 Slice 5 CLOSED `NOT_NEEDED` with evidence** — D515 measured p95 25.2/15.4 ms with optimization `NOT_NEEDED` and D517 already collapsed the click paths, so **ARC 9 is COMPLETE**. (10) **E61 collapse terminal = WAR-WEARINESS METER.** (11) **ARC 8 next = soldier POV in owner-mode battle receipts.** (12) **R-7/E33 SCHEDULED** as a fail-open correctness defect under the D92 accurate-inputs principle. (13) **D460 Leetown SCHEDULED.** (14) **NEAR-tier directives:** brigade flags + corps badges, surrender/no-quarter (consequence-only, never scored), women Phase F (re-scope against the shipped D385/D386 arc first), franchise-mode divergence scope — with **Engineering Corps and the naval engine absorbed into the ARC 7 ladder** as slices 3 and 4.

**THE RATIFIED BUILD ORDER:** (1) LANE-022 conquest Mayhem supply ladder, slices 1→7 → (2) E57 → (3) E46 → (4) R-7/E33 → (5) E61 → (6) ARC 8 soldier POV → (7) D460 Leetown → (8) NEAR-tier directives by available lane.

**Docs and release:** `docs/design/conquest-supply-chain-design.md` NEW (the Aaron-ratified law: the finding, the containment boundary, the `_lgRoute` seam, the seven-slice ladder, mode-economics teaching, risk/control table). **LANE-022 opens at `CONTRACT` / `none`** with the Slice 1 acceptance contract written; **LANE-019 is deliberately untouched** because `tools/probe-conquest-transport-plan.mjs` pins its segment tooth-for-tooth. COORDINATION §6 closes S-01/S-02/S-03/S-04 and opens **S-05** carrying the ratified order plus a new decision-quality floor item (11): **a session seed is not authority — reconcile every asserted boundary against live disk before planning on it.** REVIEW-QUEUE flips the seven GEA rows, GEA-11, E33, E57, E61, and annotates E50 and E54. D535's head archives byte-verbatim under the two-head rule. **EXACT NEXT:** take DRIVE in LANE-022 in a committed ledger edit and implement Slice 1 — the read-only traced route — behind a conquest-OFF byte-identity leg, a fresh-campaign A/B, and the two-direction containment tooth. ARC 7 Historical movement, Historical roads, and E46 remain blocked; D525 remains the product head.
