# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D547 next-lane=LANE-022 state=CONTRACT owner=none -->
> **Live status (D547, 2026-07-23):** `LANE-022` **Slice 5 SHIPS** — the **authored Mayhem road layer**. ONE authored constant `_LG_ROADS` (seventeen rows — two sourced labelled exemplars of road FAILURE, fifteen openly authored segments) is merged into the memoized base projection by one pure helper inside `_lgTraceBase`, turning the sourced substrate's **eleven disconnected components into ONE board of 36 territories** at the highest-friction mode (`road` cost 6 against rail 3 and river 2). `_lgTraceProject`/`_lgTraceGraph` stay SOURCED-ONLY and byte-unchanged, so D539's eleven-component / 44-service proof still holds as the substrate-immutability tooth. Roads cut and repair through the SHIPPED Slice-2/3 mechanism (the `_lgSupplyView` sanitizer's id pattern widens; nothing else moves), landing design law §4's "clearing restores a road". Depot-preferred source selection becomes cheapest-available-source. **D547 replaces D545 as the ARC 7 product head, and LANE-022 releases to `CONTRACT` / `none`.**
>
> **Boundary:** game `0a5286c3b79c8011a6903ceb23772d80` → `77d96c4384cc5c818efe5f296a3c4e21`, srcTree `7bcb0579d4e432950897500e7f0e5846` → `ad0b2811db8e8323fdb70438c84893a4`, re-anchored at FIVE disk-counted sites (three generated-game, two srcTree). Suite **142**, manifest 112, data 65, `_SAVE_VER` **1**, `build/base.html` frozen at `c9db83fa99230ffb95bdfdfe059f3fb9` — none move, none re-pinned. LANE-019 stays unrewritten (transport plan 12/12).
>
> **Authority:** design-law §4 Slice 5 + D537 ruling 2, implementing the committed D546 contract. Three A/B legs: legs 1-2 BYTE-IDENTICAL (`18f609d0…` 29 scenarios × 8 seeds, `a38185fd…`), leg 3 conquest-ON measured in both columns from the same on-disk seam and adjudicated under D92. Bind D546-B1 redded ONLY `CONTAINMENT-B` (19/20, exit 1) with an md5-proven byte-identical restore. **FIVE tooth reshapes were needed where D546 contracted THREE** — the two extra were forced by the slice and are named in D547, not passed off as contracted. **Three defects in the inherited working tree were caught by these gates and fixed**: a srcTree re-pin computed against a superseded draft, the pin tooth that structurally could not catch it, and two probe artifacts recording the opposite of what their own assertions proved. Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-23, D547: LANE-022 SLICE 5 SHIPS — THE AUTHORED MAYHEM ROAD LAYER JOINS THE ELEVEN SUBSTRATE COMPONENTS INTO ONE BOARD OF 36 TERRITORIES AT THE HIGHEST-FRICTION MODE, ROADS CUT AND REPAIR THROUGH THE SHIPPED MECHANISM, AND THE LANE RELEASES TO CONTRACT/NONE.** D546 is committed and pushed clean at `d73cf57d99e2933ee2439793028ffa98b7ed2be8`. Slice 5 landed as D546 contracted, with **two tooth reshapes the slice forced beyond the three contracted** and **three defects in the inherited working tree caught by these gates and fixed** — all four facts are stated plainly below rather than absorbed into a clean-sounding summary. **D547 replaces D545 as the ARC 7 product head, and LANE-022 releases to `CONTRACT` / `none`.**

**What shipped.** The seam is `src/61-logistics-rail.js` only. ONE authored constant `_LG_ROADS` — seventeen rows, two sourced labelled exemplars (`RD-SI06`, `RD-SI13`) and fifteen openly authored `RDA-*` segments, every authored row marked `authored, not sourced` — is merged into the memoized base projection by one pure helper `_lgTraceRoadAdj` inside `_lgTraceBase`. `_lgTraceProject`/`_lgTraceGraph` stay SOURCED-ONLY and byte-unchanged, which is precisely why D539's eleven-component / 44-service proof keeps working untouched as the substrate-immutability tooth (measured both columns: sourced components `[18,5,4,2,1,1,1,1,1,1,1]` and 44 sourced services in BOTH, roaded base ONE component of 36). Roads are bidirectional and their ids sort after every sourced `CTS-*` under the shipped `_lgTraceEdgeOrder`, so a road can never displace an equal-hop sourced segment. Roads cut and repair through the SHIPPED `conquestSupplySetCondition` over the SAME `C.conquest.supply.cut` map — the `_lgSupplyView` sanitizer's id pattern widens to `^(?:CTS-[RWS]-\d\d|RD-SI\d\d|RDA-\d\d)$` and NOTHING else about it moves (it still fails CLOSED, and the mutator still refuses any id absent from `base.services`) — which finally lands design law §4's "clearing restores a road". `_LG_REPAIR.cost.road = 3`, the cheapest repair in the game and the Construction Corps' work. Depot-preferred first-wins becomes cheapest-open-walk among sources that can ACTUALLY source, depot winning ties, with the Slice-4 blockade-`SEVERED` branch kept exactly and a new `importClosed` flag telling the player when a sealed port put them on the long road. No fourth mutator, no second owner, no new field on `C.conquest`, no new module (manifest 112), no data change (65), no new probe (suite 142). `build/base.html` frozen.

**A/B — two zero-diff guard legs, one honest leg measured in both columns.** Leg 1, the standing direction battery (`tools/sweep-all-battles.mjs`, **29 scenarios × 8 seeds** — the packet's long-carried "24×8" is stale; disk says 29), artifact `18f609d07b1190904ec0c11e4ca64675` BYTE-IDENTICAL to the `ca6b63e` baseline, `ok:true`, 0 failures, 0 pageerrors, **0 diffs**. Leg 2, `probe-full-campaign`, artifact `a38185fd371a7f181250eff3a6cbf76a` BYTE-IDENTICAL, `ok:true`, steps 4, 0 pageerrors, **0 diffs**. Leg 3 was measured with a throwaway `node:vm` harness that rebuilt the SAME on-disk seam twice — column A from HEAD's `src/61`, column B from the working tree — so both columns are measurement, not recollection; the harness was deleted before the commit. Every movement D546 contracted in advance was measured and matched exactly: `US/E` default `TRACED f=7` → `TRACED f=7` over the same single `CTS-R-02:rail` (route unchanged; the trace object gains `importClosed:false`); `US/W`→CT-20 and `US/TM`→CT-32 `SUBSTRATE_GAP` → `SEVERED 40`; `US/N`→CT-19 `SUBSTRATE_GAP` → `TRACED 19` over `CTS-R-01:rail + RDA-14:road` (the National Road) `+ RDA-15:road` (the Ohio valley road); `CS/E` `SUBSTRATE_GAP` → `TRACED 10` over the single `RDA-01:road`; `CS/W`→CT-20 `TRACED 16` sea-sourced from CT-12 → `TRACED 16` depot-sourced from CT-05 via `RDA-04` (friction identical, source moved); `CS/TM`→CT-32 and `CS`→CT-35 `SUBSTRATE_GAP` → `TRACED 33` across seven segments, three of them road — exactly the "costs nearly the severed ceiling" teaching D546 wrote; `CS`→CT-08 `SUBSTRATE_GAP` → `TRACED 10`; and the blockade lever on `CS`→`CT-11` **8 open / 40 sealed → 8 open / 19 sealed with `importClosed:true`**. Two further rows moved as the reshaped teeth contracted: `CS`→CT-20 sealed `SEVERED 40` → `TRACED 16` (the Great Valley road is inland; a blockade shuts a COASTAL door, not an interior one), and `US`→CT-11 `SUBSTRATE_GAP` → `SEVERED 40` **identically in both blockade states**, which is the law the old US fixture was standing in for. Containment HELD in both columns (`historical` returns `null`). **Adjudicated under D92**: every value is the graph walk over the shipped authored opening control map plus the authored road network and the shipped cost table — nothing tuned, nothing gated on an output, every value inside the shipped caps. The `US` SEVERED-at-40 rows are the largest movement and are correct: Nashville and Little Rock are Confederate in the authored 1861 opening, and an enemy-held DESTINATION severs the line under the SHIPPED D540/D541 traversal law. The Union genuinely had no supply line into Confederate Tennessee in 1861, and the state flips to `TRACED` as the player takes the ground — which is the entire point of a conquest layer.

**Bind and save.** Bind D546-B1 mutated ONE value in the containment allowlist (`var _LG_TRACE_RULESETS = { mayhem: 1 };` → `{ mayhem: 1, historical: 1 }`) and ran the focused probe WITHOUT rebuilding: ONLY `CONTAINMENT-B` redded (19/20, exit 1, "the containment allowlist is no longer the exact single-id allowlist"), every other tooth held including `CONTAINMENT-A` and the new road step. Restore byte-identical — `src/61` md5 `8d5725d318ad1d7e9ff13ad551f1e87c` pre == post — and the rebuild returned the identical game md5 `77d96c43` at 20/20 clean green. Save is **purely additive with nothing new to serialize**: `_LG_ROADS` is a module-scoped authored CONSTANT, not player state, and a road cut rides the existing `cut` map as one more key, so `C.conquest` gains no field, none of the seven functions hashed in `tools/save-shape.json` moved, and **`_SAVE_VER` stays 1 as a conscious decision**. The gating legacy-save tooth is green precisely because the sanitizer's accepted key set widened: `SAVE SHAPE` reports `saveVer:1, additive:true, legacyByteIdentical:true, badPayloadsRejected:13, signatures:7`, and `probe-save-slots` 17/17 confirms.

**FIVE tooth reshapes were needed where D546 contracted THREE — the two extra are named, not absorbed.** Contracted in advance: (1) the `NO NEW AUTHORITY` blanket `RD-` ban in the focused probe, replaced by the strictly tighter positive rule that every road id the seam can emit is one of the two cured `RD-SI` ids or the authored `RDA-` namespace, with the four UNRESOLVED `RD-E1*` rows still banned outright; (2) the identical ban in the plan probe, replaced the same way, plus a conscious `authored, not sourced` marker re-pin 6 → 7; (3) `SUBSTRATE GAP IS FIRST-CLASS` → `THE ROAD LAYER CLOSES THE SUBSTRATE GAP`. **FORCED by the slice, both strictly tighter, neither weakened to pass:** (4) `BOUNDED OUTCOME` — the `cs-substrate-gap` scenario is now `cs-road-traced`, and the no-penalty law was generalised from a single fixture into an invariant asserted over EVERY measured row (`r.state !== "SUBSTRATE_GAP" || r.applied === false`); (5) `BLOCKADE SEA EDGE`, node-side AND in-page — sealed Charleston re-pinned 40 → 19 with `importClosed:true`, the Western front re-pinned to trace overland in BOTH blockade states, the US-unaffected assertion re-pinned off the `SUBSTRATE_GAP` fixture onto the actual law (the blockade moves the US side by exactly nothing, and no US line may carry a sea segment or flag a closed import), and the port-lost case re-pinned from `SUBSTRATE_GAP` to the dearer interior fallback.

**Three defects in the inherited working tree, all caught by these gates and fixed.** (a) **A stale srcTree pin that would have shipped red.** Both srcTree pins were set to `a7376020b178ce910c511bb80dee171c`, a value corresponding to NO tree on disk; `treeMd5` reproduces the committed D544 pin `7bcb0579` exactly from HEAD, so the walk is sound and the re-pin was simply computed against a superseded draft of `src/61` and never recomputed. It redded `probe-war-career-loop-plan` at 22/24 ("srcTree D411-complete lock moved"). Corrected to the measured `ad0b2811db8e8323fdb70438c84893a4` at both sites. (b) **The tooth that should have caught it structurally could not.** `probe-conquest-supply-plan`'s srcTree assertion compared the two pins only to EACH OTHER, so a stale-but-self-consistent pin passed there and redded only downstream — exactly backwards for the lane's pin-integrity tooth. It now verifies srcTree against the live `src/` tree the same way the game pin verifies against the built deliverable, using the identical path+content walk; bind-tested by restoring the stale value, which redded EXACTLY that one tooth (9/10, exit 1) with a byte-identical restore. (c) **Two probe artifacts recorded the opposite of what their own assertions proved** — the `BLOCKADE SEA EDGE` step NAME still said "a sealed blockade SEVERS it" while its reshaped tooth proves the interior fallback, and the same step's return object hardcoded `portLost:"SUBSTRATE_GAP"` from the Slice-4 build while the assertion above it required `TRACED`. A green artifact that contradicts its own tooth is worse than a red one; both now report the measurement (`portLost:{state:"TRACED",depot:"CT-05",friction:19}`). Also corrected: four of the five pin sites carried no D546 chain comment, against the lane's own D538/D542/D544 practice; all five now chain.

**One contract-prose correction, adjudicated from shipped behaviour.** D546 recorded the roaded base as "60 services (44 sourced + 16 road edges)". Disk ships **61**, and 61 is right: `base.services` has always been the ROW roster, not the edge roster — the shipped sourced roster is 44 ROWS of which **16 project no edge** (`CTS-S-02` Wilmington and fifteen others), which is the very precedent D546 cites to justify `RD-SI13` shipping edgeless. The contract prose conflated rows with edges; the probe pins 61 with that reasoning recorded inline.

**Gates, all green with every artifact read.** Build `GATE OK · doc-coherence ✓ · parse ✓ · hex ✓ · collision ✓ · no-fudge ✓ · citations ✓ · women-in-war ✓ · save-shape ✓` (exit 0; the 2.42MB raw-embed WARNING is pre-existing); `node --check` clean on all five touched JS/MJS; artifact readback confirmed `_LG_ROADS`, `_lgTraceRoadAdj`, `_lgRoadById`, `_lgTraceCost`, `RD-SI06`, `RD-SI13`, `RDA-01`, `RDA-15`, `importClosed`, `Wagon road`, `Binnaker` and `Johnson's plantation` inside the built deliverable with zero gated-ruleset literal in the allowlist. Focused **20/20** (0 pageerrors, 0 realErrors); plan **10/10**; adjacent logistics-rail 8, logistics 15, bridge 6, conditioning 9, engineering-corps 23, blockade 11, conquest-board 13, conquest-state 15, conquest-transport 18, campaign-link 19, auto-resolve 10, save-slots 17, presets 27, command 100 — every one exit 0 with `ok:true`, `failed:[]`, `pageerrors:[]`, `realErrors:[]` and a freshness check; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest layer 8/8, doc coherence 5/5; `git diff --check` clean. `probe-engineering` was NOT run — it is the reproducible headless-Chrome fonts-stall `page.screenshot` FATAL, not a regression, and `probe-engineering-corps` 23/23 cross-validates that surface. The full serialized 142-row battery stays owed at Slice 7. **EXACT NEXT:** under D514, reload the ledger and contract LANE-022 Slice 6 (legal-order AI — the AI's order filter must honour supply legality under the SAME rules as the player, reusing `conquestSupplyTrace`/`_lgSupplyView` with no second resolver and no AI-only supply path) in a committed DRIVE take, or issue the capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.

**⚡ AMENDMENT — 2026-07-23, D546: LANE-022 TAKES CLAUDE CODE DRIVE FOR SLICE 5 — THE AUTHORED MAYHEM ROAD LAYER, SEEDED ON THE TWO CURED ROWS AS SOURCED LABELLED EXEMPLARS OF ROAD FAILURE, JOINING THE ELEVEN SUBSTRATE COMPONENTS INTO ONE BOARD AT THE HIGHEST-FRICTION MODE (CONTRACT ONLY; NO RUNTIME BYTE MOVED).** D545 is committed and pushed clean at `ca6b63e0a62aec44775fe681f707a81006de7c46`. This is the contract-before-teeth commit the Contract Relay's hard rule requires: LANE-022 carries the complete Slice 5 acceptance contract as committed prose, and the acceptance teeth land in the SAME commit as the code that greens them. Nothing in `src/`, `data/`, `build/`, the manifest, the suite, the save shape or the generated deliverable moved here, so game `0a5286c3b79c8011a6903ceb23772d80`, srcTree `7bcb0579d4e432950897500e7f0e5846`, suite 142, manifest 112, data 65, `_SAVE_VER` 1 and the frozen base `c9db83fa99230ffb95bdfdfe059f3fb9` do NOT move and are NOT re-pinned. **D545 remains the ARC 7 product head until Slice 5 ships.**

**The citation pass that shaped it (default-refute, surfaced, corrections carried).** Every element of the design-law §4 Slice-5 sentence was verified against the ORIGINAL Official Records text (Internet Archive `warofrebellion014701rootrich`; Report No. 32 read back at printed pp. 336-342) and the Platter diary (DLG `dlg_zlpd_ccp001`) before a byte was authored. BOTH cured rows SURVIVE as **Verified** and the D532 locators were genuinely read back, not manufactured. Four wordings had drifted and are corrected in the authored copy rather than propagated (the **Lawtonville roads** not the town; **Coosawhatchie Swamp** inland, not the railroad depot town — a drift that would have put a road segment on the wrong side of the board; the source's own fence-rail corduroy construction rather than "miles of corduroy"; the midnight halt at **Johnson's plantation beyond Laurel Hill** under rain lasting "nearly the entire day"). A fifth reading was REFUTED and changes the build: `RD-SI13`'s route (Cheraw → Laurel Hill → toward Fayetteville) anchors to NO board territory at its origin and is neither CT-11 nor CT-13, so it ships with **no endpoints and no edge** rather than being forced onto geography it does not support — the honest D503-consistent result and the substrate's own shipped behaviour for a single-territory row. Recorded and surfaced, not acted on: Platter was adjutant of the 81st Ohio, Second Brigade of Corse's OWN division, so D532's "2/2 independent families" floor is weaker in COMMAND independence than it reads — cured at zero cost by citing Logan's cross-command Report No. 11 (pp. 224-225) alongside Corse; and the FROZEN D511 register row carries Platter's Buford's Bridge where Corse says Rivers' Bridge. LANE-019 stays unrewritten.

**What Slice 5 contracts.** ONE authored constant `_LG_ROADS` — seventeen rows, two sourced labelled exemplars and fifteen openly authored segments — merged into the memoized base projection by one pure helper inside `_lgTraceBase`. `_lgTraceProject`/`_lgTraceGraph` stay SOURCED-ONLY and unchanged, so D539's eleven-component / 44-service proof keeps working untouched as the substrate-immutability tooth. The eleven islands become ONE connected board of 36 territories at mode `road` cost 6 — double rail, triple river — so a road line is a real but expensive fallback and a six-segment all-road haul costs exactly the severed ceiling. Roads are cut and repaired through the SHIPPED Slice-2/Slice-3 mechanism over the same `cut` map (the sanitizer's id pattern widens; nothing else moves), which finally lands design law §4's "clearing restores a road" promise. `_SAVE_VER` stays 1; no new field, no fourth mutator, no second owner.

**The Slice-4 rule this slice amends, and the movements contracted in advance.** Depot-preferred source selection was byte-identity scaffolding; with roads it would select a strictly worse line — measured, `CS`→`CT-11` would fall from the 1-segment sea import at friction **8** to a 4-segment interior road at **19** purely by preference order, silently destroying the blockade lever. The rule becomes cheapest-open-walk among sources that can ACTUALLY source, depot winning ties, with the blockade-`SEVERED` branch kept exactly. A uniform-cost walk was BUILT AND MEASURED as the alternative and returned identical results on every pair, so `_lgTraceWalk` is deliberately NOT changed. From the same pre-authoring instrumentation, contracted so they cannot be rationalised afterwards: `US`→W and `US`→TM move from un-applied `SUBSTRATE_GAP` to `SEVERED` at 40 (Nashville and Little Rock are Confederate in the authored 1861 opening and an enemy-held destination severs under the SHIPPED D540/D541 traversal law — the Union genuinely had no line into Confederate Tennessee in 1861, and it flips to `TRACED` as the player takes the ground); `US`→N becomes `TRACED` 19 over the National Road and the Ohio valley road; `CS`→E becomes `TRACED` 10; `CS`→TM and `CS`→Marshall become `TRACED` 33; and the blockade lever on `CS`→`CT-11` becomes 8 open / 19 sealed instead of 8 / 40. All adjudicated under D92 as accurate-inputs consequences, never tuned. **EXACT NEXT:** implement D546 exactly as contracted, ship it under its own D### with the three A/B legs and the one declared bind, then release LANE-022 and contract Slice 6 (legal-order AI). ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
