# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D545 next-lane=LANE-022 state=CONTRACT owner=none -->
> **Live status (D545, 2026-07-23):** `LANE-022` **Slice 4 SHIPS** — the blockade / sea edge. For a CS carrier the held coastal ports that ORIGINATE a sourced sea service become supply **sources** beside the interior depot, and the shipped blockade lever gates them: `conquestSupplyTrace` reads `C.blockade.portsOpen`, so an **open** runner port TRACES the import and a **sealed** blockade (`portsOpen 0`) SEVERS it. `CS→CT-11` (Charleston via `CTS-S-01`) moves friction **8↔40** and the Western front `CS→CT-20` **16↔40** as the blockade opens and seals, while US play and the CS interior line stay **byte-identical**. Reuses shipped `C.blockade` state — **no new owner, no new field, `_SAVE_VER` 1, no authored constant**. The lane releases to `CONTRACT` / `none`.
>
> **Boundary:** `src/61-logistics-rail.js` only, plus its two probes and five mechanical pin re-anchors. Game `98f3feaf`→`0a5286c3b79c8011a6903ceb23772d80`, srcTree `c4fc64eb`→`7bcb0579d4e432950897500e7f0e5846`, suite **142** (no probe added), manifest 112, data 65, `_SAVE_VER` **1**, `build/base.html` frozen. **D545 replaces D543 as the ARC 7 product head**; LANE-019 stays unrewritten.
>
> **Authority:** design-law §4 Slice 4 + Aaron's ratified sea-import-port source model. Three A/B legs — two zero-diff (direction battery `18f609d0…`, `probe-full-campaign` `a38185fd…`) and one honest conquest-ON leg adjudicated under D92 (open TRACES the CS import, sealed SEVERS at the 40 ceiling; both columns logged). Containment fails CLOSED at the ruleset seam both directions; Bind D544-B1 redded only `CONTAINMENT-B` with a byte-identical restore. Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-23, D545: LANE-022 SLICE 4 SHIPS — THE BLOCKADE / SEA EDGE GATES THE TWO SOURCED SEA SERVICES AS THE CONFEDERATE IMPORT EDGE, MODELLED AS BLOCKADE-GATED COASTAL SOURCES, WHILE NON-CONQUEST PLAY STAYS BYTE-IDENTICAL; THE LANE RELEASES TO CONTRACT/NONE.** D544 is committed and pushed clean at `9b1363f`. Slice 4 landed exactly as contracted. **D545 replaces D543 as the ARC 7 product head, and LANE-022 releases to `CONTRACT` / `none`.**

**What shipped.** The seam is `src/61-logistics-rail.js` only. For a CS carrier `conquestSupplyTrace` now builds a source list — the interior depot `_LG_TRACE_DEPOT[side]` FIRST (depot-preferred, so every shipped route is byte-identical), then every held coastal port that ORIGINATES a sourced sea service (the from-end of a sea-mode edge — CT-12 Savannah). The first source whose OPEN-graph walk reaches the target wins. A pure reader `_lgSeaImportOpen(C)` reads the shipped `C.blockade.portsOpen`: while a runner port is open (`portsOpen > 0`) the import source carries; when the blockade seals every port (`portsOpen 0` — the Fort Fisher state) the import lands nothing, so a base-reachable front SEVERS at the authored ceiling (40) rather than becoming a `SUBSTRATE_GAP` — the substrate CAN carry it, the blockade closed it. The interior depot's overland supply is NEVER gated. No fourth mutator, no second logistics/blockade owner, no new save owner, no new clock, no authored constant, no new module (manifest 112), no data change (65). `build/base.html` frozen.

**The finding that shaped it (packet-vs-disk, Aaron-ratified).** The Slice-4 packet assumed a CS route already traverses a sea service; disk showed the sole sea edge `CTS-S-01` (CT-12→CT-11, also the only in-edge to CT-11) sits in the Deep-South component while both fixed depots sit elsewhere, so gating the sea mode in the `blocked` predicate alone was a byte-for-byte no-op. Surfaced per the HALT rule; Aaron ratified the **sea-import-port SOURCE model**. `CTS-S-02` (CT-10 Wilmington) is a single-territory service that projects no edge, unchanged.

**A/B — two zero-diff legs, one honest leg.** Leg 1 (conquest OFF) the 24-scenario × 8-seed direction battery `18f609d07b1190904ec0c11e4ca64675` BYTE-IDENTICAL to the `caf3855` baseline (0 diffs); leg 2 `probe-full-campaign` `a38185fd371a7f181250eff3a6cbf76a` BYTE-IDENTICAL (0 diffs). Leg 3 (conquest ON, in-page, both columns, 0 pageerrors): at caf3855 both CS coast targets were `SUBSTRATE_GAP` (no import route existed; shipped friction held). In this build, blockade OPEN → `CS→CT-11` (Charleston) `TRACED` friction **8** via `CTS-S-01:sea`, and the Western front `CS→CT-20` (Nashville) `TRACED` friction **16** via rail from Savannah; blockade SEALED (`portsOpen 0`) → both `SEVERED` friction **40**, `severedBy` naming `CTS-S-01`. Direction: tightening the blockade RAISES friction (worsens CS supply) — adjudicated under D92 as an accurate-inputs consequence (a sealed blockade closing a real import line), every value derived from the graph walk plus the shipped `portsOpen`, never tuned, never an output gate, every value inside the shipped caps.

**Bind and save.** Bind D544-B1 mutated the containment allowlist (`{ mayhem: 1 }` → `{ mayhem: 1, historical: 1 }`) and ran the focused probe WITHOUT rebuilding: ONLY `CONTAINMENT-B` redded (19/20, exit 1), every other tooth — including `CONTAINMENT-A` and the new `BLOCKADE SEA EDGE` step — held; restore byte-identical (`src/61` md5 `4aa935355487feec3a0780abba7316b4` pre == post), rebuild returned the identical game md5 `0a5286c3` at 20/20 green. Save is **purely additive with nothing to serialize** — the sea edge reads `C.blockade`, which already rides the existing envelope, so `C.conquest` gains no field, none of the seven `tools/save-shape.json` functions moved, and `_SAVE_VER` stays 1; `probe-save-slots` 17/17 confirms.

**Gates, all green with every artifact read:** build `GATE OK`; focused **20/20** (0 pageerrors, 0 realErrors); plan 10/10; adjacent logistics-rail 8, logistics 15, bridge 6, conditioning 9, blockade 11, conquest-board 13/13, conquest-state 15/15, conquest-transport 18/18, campaign-link 19, auto-resolve 10, save-slots 17/17, command 100, presets 27 — all exit 0, 0 pageerrors; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest layer 8/8, doc coherence 5/5; `git diff --check` clean. Game `98f3feaf0de89b3b47eda6b1347dacd0` → `0a5286c3b79c8011a6903ceb23772d80`, srcTree `c4fc64ebe6d49d9cdfc79885b4c05d8b` → `7bcb0579d4e432950897500e7f0e5846`, re-anchored at FIVE disk-verified sites (three generated-game, two srcTree); suite md5, focused md5, manifest and frozen base HOLD (no probe or module added). **LANE-019 is deliberately unrewritten** (transport plan 12/12). **EXACT NEXT:** under D514, reload the ledger and contract LANE-022 Slice 5 (the authored Mayhem road layer, seeded on `RD-SI06`/`RD-SI13` as sourced labelled exemplars of road FAILURE) in a committed DRIVE take, or issue the capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.

**⚡ AMENDMENT — 2026-07-23, D544: LANE-022 TAKES CLAUDE CODE DRIVE FOR SLICE 4 — THE BLOCKADE / SEA EDGE, GATING THE TWO SOURCED SEA SERVICES AS THE CONFEDERATE IMPORT EDGE (CONTRACT ONLY; NO RUNTIME BYTE MOVED).** D543 is committed and pushed clean at `caf3855162c666a677ff58fa85373edb9a2e6f13`. This is the contract-before-teeth commit the Contract Relay's hard rule requires: LANE-022 carries the complete Slice 4 acceptance contract as committed prose, and the acceptance teeth land in the SAME commit as the code that greens them. Nothing in `src/`, `data/`, `build/`, the manifest, the suite, the save shape or the generated deliverable moved here, so game `98f3feaf0de89b3b47eda6b1347dacd0`, srcTree `c4fc64ebe6d49d9cdfc79885b4c05d8b`, suite 142, manifest 112, data 65, `_SAVE_VER` 1 and the frozen base `c9db83fa99230ffb95bdfdfe059f3fb9` do NOT move and are NOT re-pinned. **D543 remains the ARC 7 product head until Slice 4 ships.**

**The finding that shaped Slice 4 (packet-vs-disk, surfaced and ratified).** The Slice-4 packet assumed a CS supply route already traverses a sea service; live disk contradicts it. The two sourced sea services are `CTS-S-01` (CT-12 Savannah → CT-11 Charleston — the ONLY sea edge, and the ONLY in-edge to CT-11) and `CTS-S-02` (CT-10 Wilmington, a single-territory service that projects NO graph edge). The sea edge sits in the 18-node Deep-South component while both fixed depots (CS `CT-05`, US `CT-01`) sit in other components, so no `conquestSupplyTrace` from either depot uses either sea service — gating it in the `blocked` predicate alone would be a byte-for-byte no-op. Aaron ratified (2026-07-23) the **sea-import-port source model** instead: the import edge is modelled as blockade-gated coastal SOURCES.

**What Slice 4 contracts.** For a CS carrier every held coastal port that ORIGINATES a sourced sea service (the from-end of a sea edge — CT-12 Savannah) becomes a supply SOURCE beside the interior depot, so a front the depot cannot reach overland across the sourced substrate (the Slice-5 rail gap) can be supplied from the coast. The shipped blockade lever gates it: `conquestSupplyTrace` reads `C.blockade.portsOpen` and, while a runner port is open (`portsOpen > 0`), the import carries (TRACED); when the blockade seals every port (`portsOpen 0` — the Fort Fisher state) the import lands nothing and any import-sourced front SEVERS at the authored ceiling (40). The interior depot's overland supply is NEVER gated, so US play and the CS interior line stay byte-identical and the default CS/E route stays `SUBSTRATE_GAP`. Measurable both directions on `CS→CT-11` (Charleston, via `CTS-S-01`, 1 sea segment) and the Western front `CS→CT-20` (import-sourced rail from Savannah); `CS→CT-19` stays a gap (Cairo is not reachable from CT-12 in the directed substrate).

**No new owner, no new field, no new authority (invariants E/F/G/H/I).** The change lives entirely inside `conquestSupplyTrace` plus one pure reader `_lgSeaImportOpen(C)`: no fourth mutator, no second logistics/blockade store, no new save owner, no new clock, no authored constant. It reads the SHIPPED blockade STATE only (`portsOpen`), never a sea service's period text (D526's Fort Fisher `dateText` counterexample stays untouched), and derives the import ports from the sourced sea edges (no new authored geography). `src/115`/`src/114` stay byte-frozen; the two seaServices are READ, never rewritten. Save is **purely additive with NOTHING to serialize** — the sea edge reads `C.blockade`, which already rides the existing envelope, so `C.conquest` gains no field and `_SAVE_VER` stays 1 (a CONSCIOUS decision, the D447/GEA-12 precedent).

**Bounded channel and containment unchanged in law.** `logisticsBridgeBonus` keeps its caps (`supply ≤ 7`, `fatigueRelief ≤ 5`, `overall ≤ 2`) and `wr.supply` keeps its 0.15 troop-morale weight; the sea edge is a bounded CONDITIONING INPUT feeding the capped bridge — it NEVER writes casualties, morale, victory, score or RNG, and Slice 4 creates NO new combat channel. Containment reuses the same `_lgTraceRuleset` allowlist gate: the sea-import logic lives inside `conquestSupplyTrace`, which fails CLOSED on the evidence-gated ruleset before any board read; `CONTAINMENT-A` gains the open-ruleset sea-import assertion and `CONTAINMENT-B` stays the ONE tooth owning the gated-ruleset query and the allowlist source assertion — a bind that reds two teeth is too broad (the D539/D541/D543 lesson). Blockade state is control-class and NEVER enters the CF-2 memo.

**Gates owed at the shipping commit:** `node --check` on every touched file; `node tools/build.mjs` GATE OK; artifact readback of the new sea-import symbols inside the built 61 module region; the two LANE-022 probes; the adjacent logistics/logistics-rail/bridge/conditioning/**blockade**/board/state/transport/campaign-link/auto-resolve/save-slots/command/presets probes; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest 8/8, doc coherence 5/5; **three** A/B legs — leg 1 the 24-scenario × 8-seed direction battery byte-identical to the `caf3855` baseline `18f609d07b1190904ec0c11e4ca64675`, leg 2 `probe-full-campaign` byte-identical to `a38185fd371a7f181250eff3a6cbf76a`, and leg 3 the conquest-ON leg which WILL move as the blockade opens and closes the CS sea route, with both columns logged, the direction stated, and the movement adjudicated under D92 as an accurate-inputs consequence (a sealed blockade closing a real import line — never tuned, never an output gate); ONE declared negative bind on the containment allowlist redding ONLY `CONTAINMENT-B` with an md5-proven byte-identical restore; `git diff --check`. Five pin sites re-anchor (three generated-game, two srcTree); suite/focused/manifest/base pins hold and no probe is added, so the suite stays 142. **EXACT NEXT:** implement D544 exactly as contracted, ship it under its own D###, then release LANE-022 and contract Slice 5 (the authored Mayhem road layer) or issue the capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
