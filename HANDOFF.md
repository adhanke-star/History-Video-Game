# HANDOFF — "The Civil War"

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D540 next-lane=LANE-022 state=DRIVE owner=Claude -->
> **Live status (D540, 2026-07-23):** `LANE-022` takes **Claude Code DRIVE for Slice 2 — control/service receipts and cuts**, the FIRST sim-affecting slice of the ARC 7 ladder. **CONTRACT ONLY — no runtime byte moved here.** Territory control and per-segment service condition become real state in one namespace; a cut segment degrades `depotReach` for every army downstream; `applied` flips true and `tracedFriction` is adopted into `_lgRoute`'s `friction`.
>
> **Boundary:** contract-before-teeth per the Contract Relay hard rule — `src/`, `data/`, `build/`, the manifest, the suite, the save shape and the generated deliverable are untouched, so game `45278110cb73ea4719fa41ffef7682f9`, srcTree `08f95d9e9311e90313cc5b7a930f9380`, suite **142**, manifest 112, data 65, `_SAVE_VER` 1 and the frozen base `c9db83fa99230ffb95bdfdfe059f3fb9` do NOT move and are NOT re-pinned. **D539 remains the ARC 7 product head until Slice 2 ships.**
>
> **Authority:** D537 ruling 1 and `docs/design/conquest-supply-chain-design.md` §4 Slice 2. Containment stays fail-closed at the ruleset seam in BOTH directions; the ELEVEN-component substrate gap is made a first-class, TAUGHT case that never becomes a penalty, not cured. Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
<!-- LIVE-HEAD-SUMMARY:END -->

**⚡ AMENDMENT — 2026-07-23, D540: LANE-022 TAKES CLAUDE CODE DRIVE FOR SLICE 2 — CONTROL/SERVICE RECEIPTS AND CUTS, THE FIRST SIM-AFFECTING SLICE OF THE LADDER (CONTRACT ONLY; NO RUNTIME BYTE MOVED).** D539 is committed and pushed clean at `a613bacb2e369fbac335ca0fb9611c4bce89c1ea`. This is the contract-before-teeth commit the Contract Relay's hard rule requires: LANE-022 carries the complete Slice 2 acceptance contract as committed prose, and the acceptance teeth land in the SAME commit as the code that greens them. Nothing in `src/`, `data/`, `build/`, the manifest, the suite, the save shape or the generated deliverable moved here, so game `45278110cb73ea4719fa41ffef7682f9`, srcTree `08f95d9e9311e90313cc5b7a930f9380`, suite 142, manifest 112, data 65, `_SAVE_VER` 1 and the frozen base `c9db83fa99230ffb95bdfdfe059f3fb9` do NOT move and are NOT re-pinned. **D539 remains the ARC 7 product head until Slice 2 ships.**

**What Slice 2 contracts.** Territory control and per-segment service condition become REAL STATE in ONE namespace, `C.conquest.supply` (`{schema, control:{"CT-nn":side}, cut:{"CTS-X-nn":1}}`), read by one pure fail-closed reader that never writes and written by exactly two guarded mutators. `C.logistics` stays the sole logistics store and every shipped logistics owner keeps its ownership — invariant F, LANE-020's no-second-owner law generalized. The trace computes a BASE walk over the full open graph (Slice 1's walk, unchanged) and a LIVE walk filtered by cuts and control. A segment is usable by side S iff it is not cut AND neither endpoint is held by S's enemy, with the depot friendly — supply lines extend INTO contested ground, but an enemy-held territory astride the line severs it. **This is the first slice where supply bites:** `applied` flips true and `tracedFriction` is adopted into `_lgRoute`'s `friction`, which is the single sim-affecting line in the whole slice.

**THE JUDGMENT THAT SHAPES THE SLICE — the eleven-component gap must not become a penalty.** D539 measured that the 44 sourced services project into ELEVEN disconnected components, so most traced pairs have no path and `CS`/`E` does not resolve at all. The tempting implementation flips `applied` unconditionally and lets an unreachable route carry `tracedFriction:100`, collapsing supply for any army the sparse substrate cannot reach. That is wrong on the project's own terms: the gap is OUR evidence gap — five passes returned zero established physical windows and the authored road layer waits for Slice 5 — while roads were in fact universal and always available (design law §5). Penalising the player because our data lacks roads would invent history in reverse and is exactly the output gate D92 forbids. So the resolution is THREE bounded states: `TRACED` (applied, live friction), `SEVERED` (applied, a flat authored bounded ceiling, carrying `severedBy` — the exact service ids that broke the base path, so the player always sees WHY), and `SUBSTRATE_GAP` (**not** applied — friction keeps its shipped number, and the reason string names Slice 5). A cut the player can cause is a real consequence; a hole in our sources is not.

**Bounded channel, containment and save, all unchanged in law.** `logisticsBridgeBonus` keeps its caps (`supply ≤ 7`, `fatigueRelief ≤ 5`, `overall ≤ 2`) and `wr.supply` keeps its 0.15 troop-morale weight; supply stays a bounded CONDITIONING INPUT that never writes casualties, morale, victory, score or RNG, and Slice 2 creates NO new combat channel. Containment reuses the D539 `_lgTraceRuleset` allowlist gate, so authored control/condition state fails CLOSED at the ruleset seam before any substrate read, with exactly ONE tooth owning the gated-ruleset query and the allowlist source assertion INSIDE it (a bind that reds two teeth means the tooth is too broad — the D539 lesson). `serializeSave` returns `{ver, when, settings, campaign}`, so `C.conquest.supply` rides the existing envelope: the change is **purely additive**, none of the seven `tools/save-shape.json` functions is touched, and `_SAVE_VER` stays 1 as a conscious decision. Legacy-save byte identity is the gating tooth.

**The two carry-forwards are resolved with evidence, not deferred.** **CF-2** was measured rather than assumed (the D515 precedent): `logisticsSnapshot` costs **0.037 ms** on a non-conquest carrier and **15.44 ms** on a conquest carrier — 414×, ~89 ms per conquest turn, doubling under the live walk. Measurement REFUTES the packet's specific diagnosis: the adjacency build over 44 services / 27 keys / 51 edges costs **0.14 ms**; the real cost is substrate re-normalization inside `conquestTransportNormalized()` at **12.10 ms** per call. The memo is authorized and scoped to the immutable, control/condition-INDEPENDENT base projection only, keyed on ruleset id plus injected-pack reference identity, so a stale memo across a cut is structurally impossible. **CF-1** was adjudicated against git: at `0829d8d` LANE-020 was already `CONTRACT` (its own D519 release), the manifest was already 112 (D523), and `src/00-manifest.json`'s sha256 already drifted — three reds PREDATE D539, and only the suite pin (140 → 142) is this session's doing. Resolution is option (a) RE-PIN of exactly those two 140s, restoring 6/9 and leaving the three structural reds named rather than silently rewritten, because they belong to LANE-020 and this session does not own that lane.

**Gates owed at the shipping commit:** `node --check` on every touched file; `node tools/build.mjs` GATE OK; artifact readback of the new control/condition symbols inside the built 61 module region; the two LANE-022 probes; the adjacent logistics/bridge/conditioning/board/state/transport/campaign-link/auto-resolve/save-slots/command/presets probes; plan probes Mayhem 13/13, War Career 24/24, transport 12/12, conquest 8/8, doc coherence 5/5; **three** A/B legs — leg 1 the 24-scenario × 8-seed direction battery byte-identical to the re-captured `a613bac` baseline `18f609d07b1190904ec0c11e4ca64675`, leg 2 `probe-full-campaign` byte-identical to `a38185fd371a7f181250eff3a6cbf76a`, and leg 3 the conquest-ON leg which WILL move, with both columns logged, the direction stated, and the movement adjudicated under D92 as an accurate-inputs consequence; ONE declared negative bind on the containment allowlist redding ONLY `CONTAINMENT-B` with an md5-proven byte-identical restore; `git diff --check`. Five pin sites re-anchor (three generated-game, two srcTree); suite/focused/manifest/base pins hold and no probe is added, so the suite stays 142. **EXACT NEXT:** implement D540 exactly as contracted, ship it under its own D###, then release LANE-022 and contract Slice 3 (repair + finite engineering capacity) or issue the capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.

**⚡ AMENDMENT — 2026-07-23, D539: LANE-022 SLICE 1 SHIPS — THE STATIC `_lgRoute` LOOKUP BECOMES A REAL TRACE OVER THE 36-TERRITORY BOARD, READ-ONLY AND BYTE-IDENTICAL ON BOTH A/B LEGS; THE LANE RELEASES TO CONTRACT/NONE.** D538 is committed and pushed clean at `f4df08e302d3ff057f99110e22fd14068ab53ed7`. Slice 1 landed exactly as contracted, with one contracted-scope hardening the acceptance teeth forced. The GEA-11 defect is closed at its named seam: `_lgRoute` keeps its shipped six keys computed exactly as before and gains ONE guarded tail (`var traced = conquestSupplyTrace(C, null); if (traced) out.trace = traced;`) backed by eight pure helpers and four authored constants in `src/61-logistics-rail.js`. `logisticsSnapshot`, `logisticsBridgeBonus`, `logisticsSetPriority`, `logisticsOnResolve`, `presLogisticsBlock` and `logisticsWireOverview` are textually and behaviourally UNCHANGED. No module added (manifest 112, `116-conquest-state.js` last), no data change (65), no `C.conquest` field, no save-shape movement, `build/base.html` frozen at `c9db83fa99230ffb95bdfdfe059f3fb9`.

**Read-only by construction, not merely by measurement.** `applied` is `false`, `tracedFriction` is consumed by nothing, and `friction` keeps its shipped static number everywhere — so the capped bridge (`supply ≤ 7`, `fatigueRelief ≤ 5`, `overall ≤ 2`), the 0.15 `wr.supply` troop-morale weight, camp and loot are byte-identical. The decisive fact: **`campaignKind` exists on disk only inside `src/116-conquest-state.js`'s detached factory/view** — no live campaign, save, load or menu path sets it — so the new branch is unreachable from 100% of shipped play, making the seam the non-negotiable #2 guarded no-op idiom.

**THE DEFECT THE TEETH CAUGHT — fixed in the code, never in the tooth.** The first implementation read `rv.id`/`rv.version` directly, so an INHERITED ruleset prototype opened the containment gate. The shipped `_trRulesetId` (`src/115`) and `_ccsRecord` (`src/116`) already refuse exactly that class, and a new gate may not fall short of the discipline beside it. `_lgTraceRuleset` now requires OWN, DATA-VALUED descriptors on `id` and `version` before consulting the allowlist, which also refuses accessors without invoking them. The tooth proves ELEVEN non-admitted shapes fail closed — both sides on the gated ruleset, wrong case, wrong version, two prototype-key names, null, absent, inherited, accessor (invocation counter at zero) and a half-inherited pair.

**Evidence.** Build `GATE OK · doc-coherence ✓ · parse ✓ · hex ✓ · collision ✓ · no-fudge ✓ · citations ✓ · women-in-war ✓ · save-shape ✓`; artifact readback confirmed all twelve trace symbols inside the built 61 module region (14,147 bytes) with zero gated-ruleset literal there. **A/B both legs ZERO-DIFF:** leg 1, the standing 24-scenario × 8-seed direction battery, artifact `18f609d07b1190904ec0c11e4ca64675` at `0829d8d` AND at this head (`ok=true failures=0 pageerrors=0`); leg 2, `probe-full-campaign`, `a38185fd371a7f181250eff3a6cbf76a` at both heads (`ok=true steps=4 pageerrors=0`). **Bind D538-B1** mutated ONE value in the containment allowlist and ran the focused probe WITHOUT rebuilding: ONLY `CONTAINMENT-B` redded (exit 1, 11/12), every other tooth held; restore byte-identical (md5 `b33c131501dbd38096d79916ca6c3b3c` pre == post) and the rebuild returned the identical game md5 at 12/12 green. The behavioural half was separately demonstrated — mutated source yields a real gated-ruleset trace (`rulesetId:"historical"`, 1 segment, route 7 keys) where restored source yields `null` and 6 keys. Gates: focused 12/12 (0 pageerrors, 0 realErrors), plan 10/10, adjacent logistics-rail 8 · logistics · bridge 6 · conditioning 9 · conquest-board 13/13 · conquest-state 15/15 · conquest-transport 18/18 · campaign-link 19 · auto-resolve 10 · save-slots 17/17 · command 100 (all exit 0, 0 pageerrors), Mayhem 13/13, War Career 24/24, transport 12/12, conquest 8/8, doc coherence 5/5, `git diff --check` clean. Game `859637ed`→`45278110cb73ea4719fa41ffef7682f9`, srcTree `003d308a`→`08f95d9e9311e90313cc5b7a930f9380`, suite md5 `69681d6f`→`a1cb6e7347155b8705614b83cc0c32d3`, focused `a4be754c`→`fe99b4b06a1264e12015359bb1a5aded`, re-anchored with chained D538 comments at TWENTY-ONE disk-verified sites across six files (twelve suite-count, three game, two srcTree, two suite-md5, two focused-md5 — the commit message's "eighteen" undercounted and is corrected here); suite **140 → 142** (war career 38 / mayhem 57 hold); old values grepped out of `tools/` before push.

**THE HONEST RESULT, recorded rather than cured.** The 44 sourced services project into **ELEVEN disconnected components** (18 / 5 / 4 / 2 and seven singletons) because 16 rows name a single territory and contribute no edge. The default `US`/`E` route resolves in ONE rail segment (`CTS-R-02`, the Verified `RN-02`→`RN-03` Washington–Manassas corridor, `tracedFriction` 7); `CS`/`E` does not resolve at all (`reachable:false`, reached 5). That is pinned as a probe tooth and is the measured reason Slice 5 authors the road layer — never cured by inventing a service, window, interchange or endpoint. Every Historical negative stays exact and **LANE-019 is deliberately unrewritten** (transport plan 12/12). `tools/probe-desk-pacing-plan.mjs` was deliberately not touched: a superseded, non-enrolled LANE-020 probe, already red at this head, whose own suite pin now reads stale at 140 — recorded honestly rather than edited.

**Docs and release:** REVIEW-QUEUE's GEA-11 row records the D539 delivery; the V1 LANE-022 line gains a checked Slice 1 sub-item; DECISIONS/RUN-LOG/HANDOFF/mirrors sync; D537's head archives byte-verbatim under the two-head rule. **LANE-022 releases to `CONTRACT` / `none`** and **D539 replaces D525 as the ARC 7 product head.** **EXACT NEXT:** under D514, reload the ledger and either contract LANE-022 Slice 2 (control/service receipts and cuts — the first slice where supply bites, and the first that must adopt `tracedFriction`, flip `applied`, and therefore own the first sim-affecting A/B) in a committed DRIVE take, or issue the load-bearing capacity relay. ARC 7 Historical transport movement, Historical roads, the four `CTI-*` faces and E46 remain blocked.
