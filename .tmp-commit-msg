D479 LANE-017 slice 2: DROP FEEL — announcements, flip reveal, sort/filter, legendary glow

Presentation only; the reward path is byte-identical and the one-language wall
stays green:
- src/37: the additive-sanitized L.recentDrops record (D149 law — absent never
  created, legacy saves byte-identical), the Latest Recovery announcement panel
  (canonical cwTierInfo chips, glyph+label redundant), the one-time flip reveal
  + legendary glow (runtime cwLootFeelCss keyframes, colour via CSS var from the
  data map, zero new hexes), both fully withheld under reduceMotion (instant
  reveal), and view-side inventory sort/filter (rarity/kind/battle + count line;
  the view sorts a decorated copy — the saved order never mutated; delegated
  use/equip wiring on #lootInvBlock).
- probe-loot-survival 16 -> 20: announcement-canonical + _pdLog record
  unchanged; flip/glow default AND rm-inert; sort/filter correctness + the
  saved-order byte-identity tooth; sim/save byte-identity vs an untouched twin
  with drop weights pinned 2/1/0. 20/20, 0 pageerrors, artifact read.
- Adjacents: probe-ratings 23/23, probe-save-slots 16/16.
- Binds (md5-proven restores to src/37 1689c4a2 / game b74053aa): A rm-conjunct
  stripped -> the rm tooth red EXACTLY; B in-place sort -> the saved-order
  byte-identity tooth red EXACTLY (+ the sibling twin-divergence tooth, same
  violation class, its own label).
- AD-7 re-pins: game 9dd15ca2 -> b74053aa, srcTree ce48e9ae -> 4eed52e8,
  journey a527600d -> 1689c4a2 at all four plan-probe sites;
  base/dataTree/manifest/suite/focused hold (no data edit).
- Docs: DECISIONS D479, COORDINATION LANE-017 slice history + resume pointer
  (slice 3 next), HANDOFF top-block in-flight amendment.

Counts hold 29/62/1,710/140; _SAVE_VER=1; frozen base c9db83fa untouched.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
