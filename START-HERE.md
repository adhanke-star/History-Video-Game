# START HERE — "The Civil War" (master index)

**This is the ONE file to open first. It tells every new chat exactly which documents are CURRENT and which are history, and the order to read them. If a doc isn't listed under "Canonical" below, treat it as archived — do not follow it as live strategy.**

The game: a teaching wargame in three layers — **owner-mode grand strategy** (you are President Lincoln/Davis: economy, blockade, manpower, diplomacy, politics) + **real-time UG:G-style battles** (the tactical engine, built last) + a **PhD-level history & alternate-history seminar**. Personal project, $0/single-file, build bar 200/100.

---

## ⭐ The source of truth — read these in this order (a fresh chat has zero context)
1. **`START-HERE.md`** ← you are here (the map).
2. **`AUTONOMOUS-RUN.md`** ← **THE operating manual.** Live state (§2), the build loop (§3), phase roadmap (§4), guardrails (§5), and the **outstanding BACKLOG (§7)**. *This governs the run.* If you read one file, read this.
3. **`DECISIONS.md`** — every design fork already resolved (don't relitigate; append new ones here).
4. **`GRAND-STRATEGY-PLAN.md`** — the **design LAW**: 63 locked decisions + §27 the balance principle. Honor verbatim.
5. **`src/00-manifest.json` + every `src/*.js`** — the code you extend (the four-function module contract + the `_t1InitAll`/`_t1Resolve` override pattern).
6. **`tools/build.mjs` + `tools/probe-*.mjs`** — the build gate + the empirical probes (the verification discipline).
7. `git log --oneline` — the commit history (each phase is one commit).

## 📚 Canonical reference (current — pull the section a task needs; don't bulk-read)
- **`V1-CHECKLIST.md`** — the APPROVED, ordered v1 feature-complete roadmap (run-k Q&A, DECISIONS **D61**). **This is the build roadmap autonomous runs drive to completion** (Phase A connect-layers → B tactical depth → C breadth → D full hex → E strategic S3–S5 → H graphics/footage/audio → I loot → J polish). Open items + the locked operating model live here + in D61.
- **`MODERN-UGG-PLAN.md`** — the real-time tactical battle engine spec (the UG:G layer, built LAST, phases P0–P5).
- **`RUN-LOG.md`** — chronological build log (newest run at top).
- **`HANDOFF.md`** — the new-chat handoff (the ⚡ CONTINUE-HERE block mirrors `AUTONOMOUS-RUN.md` §2).
- **`CUSTOM-SCENARIO-FORMAT.md`** — the current versioned custom-battle/scenario-pack share format for the C4 builder.
- **`HISTORICAL-DATA.md`** (211KB — OOBs, weapons, USCT, historiography), **`HISTORICAL-DATA-ECONOMY.md`**, **`HISTORICAL-DATA-DIPLOMACY.md`** — citation-grade content. Grep per system; never bulk-read.
- **`REVIEW-QUEUE.md`** — priorities + locks.
- **`DEPLOY.md`** — how the single-file deliverable ships.
- **`data/*.json`** — game data (`economy`, `diplomacy`, `manpower-teaching`, `weapons`), injected as `GAME_DATA`.
- **`civil_war_generals.html`** — the GENERATED, playable deliverable (open-and-play; rebuilt by `tools/build.mjs`; never hand-edit).
- **`build/base.html`** — the FROZEN run-h foundation (Classic + 3D engine). **NEVER edit.**

## 🗄️ Legacy (in `legacy/` — superseded run-g/run-h material; reference only, NOT live strategy)
Old kickoff prompts, the pre-modularization plans, the graphics/3D/Blender/asset notes, and earlier handoffs. They explain *how we got here* but do **not** describe current strategy — `AUTONOMOUS-RUN.md` §7 supersedes all of them. Files: `legacy/{AUTONOMOUS-RUN-PROMPT, NEXT-CHAT-PROMPT, NEXT-RUN-PLAN, CC-KICKOFF, GRAPHICS-RUN-PROMPT, GRAPHICS-RUN-CONTINUE, MODERN-UGG-KICKOFF, DESIGN-BIBLE, BUILD-PLAN, 3D-ASSET-PLAN, ASSET-PROMPTS, BLENDER-MCP-SETUP, BLENDER-CONNECT-STEPS, AUDIT-PROTOCOL, GENERALS_HANDOFF, PHASE4_HALT, PLAYTEST-LOG}.md`.

---

## Naming convention (going forward — keep the root clean)
- **`START-HERE.md`** — the only entry point. Update it whenever a doc's status changes.
- **One operating manual** (`AUTONOMOUS-RUN.md`), **one design law** (`GRAND-STRATEGY-PLAN.md`), **one decisions log**, **one run-log**, **one handoff**. Do not spawn parallel "PLAN/PROMPT/KICKOFF/CONTINUE" variants — append to the canonical file instead.
- Content files are prefixed **`HISTORICAL-DATA-<topic>.md`**; code is in **`src/NN-name.js`** (NN = load order); data in **`data/<topic>.json`**; tools in **`tools/<verb>-<topic>.mjs`**.
- Anything superseded goes to **`legacy/`** immediately, so the root only ever shows what's current.
- **Proposed deeper structure (next session, optional):** `docs/operating/` · `docs/design/` · `docs/research/` · `docs/handoff/` — deferred so the build's paths and the read-order references don't churn mid-flight; the root/`legacy` split already removes the confusion.

## Git / saving — commit AND push to GitHub, but ONLY after PROPER VETTING
The remote is **LIVE**: `origin` → **github.com/adhanke-star/History-Video-Game** (private), branch `main`. **Every milestone is committed AND pushed (`git push origin main`) — but ONLY after PROPER VETTING passes** (Aaron standing instruction, 2026-06-14):

> **PROPER VETTING is TIERED and BATCHED (D160 + D176):** build playable features and graphics forward with focused gates instead of paying the full battery after every slice. Every commit still starts from `git status`, the source/doc read order, source-only edits (`src/` + `data/`; never `build/base.html`, never hand-edit `civil_war_generals.html`), `node tools/build.mjs` → **GATE OK**, any relevant importer/schema gate, the milestone's focused probe, JSON/pageerror readback, `git diff --check`, and a clean commit/push. For JS/runtime changes, also run `node --check` on touched JS/probe files plus 1-3 directly adjacent probes. Keep `tools/vet-no-regression.mjs` intact, but during Aaron's explicit same-chat queue loop do **not** run full `npm run vet:noreg` after each queued item; run it at the end-of-queue planned-work batch/release checkpoint or when Aaron explicitly asks.

Never push an unvetted / red / regressed / pageerror build. `tools/shots/*` and `.tmp/` are gitignored, so the pending-changes view stays at ~0; `git log` + the GitHub remote are the durable record.

## Universal AI implementer standards — Claude / Codex / Copilot / DeepSeek / Cline / any VS Code agent
**Each AI tool has a native entrypoint file that points to THIS file as the single source of truth — `CLAUDE.md` (Claude Code), `AGENTS.md` (Codex), `.github/copilot-instructions.md` (GitHub Copilot), `.clinerules` (Cline / DeepSeek).** They are thin pointers (the canonical read-order + the hardest non-negotiables); if the read-order or the non-negotiables below change, update them too. Every model implementing Aaron's instructions follows the same operating contract, regardless of vendor or model size:

- **Repo-ground first:** read `START-HERE.md`, `HANDOFF.md` top block, `WAKE-UP.md` top block, current `RUN-LOG.md` top entries, latest `DECISIONS.md`, `V1-CHECKLIST.md`, `src/00-manifest.json`, and the files/probes directly touched by the task before editing.
- **Work from reversible steps:** inspect before changing, keep edits scoped, preserve user/unrelated changes, make source edits in `src/` and data edits in `data/`, and touch generated `civil_war_generals.html` only when the repo's build/probe reality requires it and the source path is also aligned.
- **Recursive repo-learning loop:** read prior decisions → form a small hypothesis/plan → implement the smallest safe slice → run probes/builds → study failures and screenshots/JSON → correct root causes → write the lesson back into `DECISIONS.md`, `RUN-LOG.md`, `HANDOFF.md`, `WAKE-UP.md`, or the relevant checklist. "Learning" means repo-visible notes and improved probes; never rely on private hidden memory.
- **Self-audit every pass:** compare the diff against the user request, scan for unintended gameplay/simulation changes, check accessibility/reduceMotion/performance implications, read probe JSON instead of assuming, and search for warnings/pageerrors/regressions before reporting success.
- **Vetting is mandatory, tiered, and batched:** `node tools/build.mjs` or `--check` as appropriate, relevant importer/schema gates, focused probe(s), JSON/pageerror readback, and `node --check` plus adjacent probes for JS/runtime changes. Do not run the heavy full-suite battery after every queued item; keep building with focused gates, then run `npm run vet:noreg` at the end-of-queue planned-work batch/release checkpoint or on explicit request. If any focused or batch gate fails, fix the root cause; do not weaken the probe to pass.
- **Cloud/full-access default:** Aaron granted dangerous/full-access permissions on 2026-06-27. Prefer Codex Cloud or another non-Seatbelt full-access session for implementation and browser probes; if `CODEX_SANDBOX=seatbelt`, restart/reconfigure rather than bypassing `tools/guard-probe-browser.mjs`. After any cloud commit/push, fast-forward the local Mac repo before local work resumes: `git fetch origin && git status --short --branch && git pull --ff-only origin main`. Stop first if the Mac has dirty local edits.
- **Quality bar:** no placeholder history, no fabricated citations, no unclear-license assets, no accounts/trials/downloads unless explicitly approved, no broad refactors without need, and no visual/UI work that is inaccessible, unreadable, or unverified on real output.
- **Model routing:** the main reasoning/integration loop stays on the highest-quality model, specifically **5.5** for complex architecture, important code, user-facing text, historical judgment, UI/UX design decisions, accessibility judgment, bug hunts, final review, and any irreversible or quality-critical work. Token-efficient models may be used only for bounded helper packets where quality cannot be harmed: `rg`/search summaries, file inventories, probe-log summaries, mechanical doc syncs, exact-schema data cleanup, first-pass source gathering, and repetitive fixture/probe scaffolding. Every helper/subagent/workflow call must explicitly set model **and** effort; smaller models produce packets, and 5.5 verifies. Smaller models never own architecture, combat balance, historical claims, UX direction, accessibility judgment, or final integration.
- **Reporting discipline:** every handoff/final report names files changed, commands run, pass/fail results, unverified areas, residual risks, and any docs updated. Do not claim completion for work that was not run or inspected.
- **Fresh-chat boundary (D171, Aaron, 2026-06-30):** auto-condense is a safety net for finishing one already-bounded milestone, not the default way to carry the project across groups. At every clean committed+pushed milestone, and before starting a new execution group/phase, STOP and surface a **paste-ready continuation prompt** for a fresh chat unless Aaron explicitly says to continue in the same chat. If context is near ~70-80% consumed, a browser-heavy gate is next, or the run starts depending on stale summarized context, finish the current gate, refresh `HANDOFF.md` + `WAKE-UP.md`, and stop at the clean boundary. The continuation prompt must include `cd`, `git fetch origin && git status --short --branch && git pull --ff-only origin main`, current HEAD, exact next group/item, read order, queue locks, and gate sequence.

## Priority picker + model routing — use when Aaron says "pick from the list"
When Aaron asks a new chat to choose from a priority list, use this list (cross-check `V1-CHECKLIST.md` for the ordered roadmap). *(`MASTER-TASK-LIST.md` was retired 2026-06-21 — it duplicated this picker + the roadmap + the live head; it is now a pointer stub.)* These are the work items Codex is expected to handle better than DeepSeek/Cline because they require architecture, balance judgment, cross-module repo reasoning, probe design, or high-risk regression control.

**Latest live head (2026-07-03):** D219 continues Priority 1 Phase I Soldier's Story scale-out after D214 parked Group 5 Phase H as complete-enough. Francis C. Barlow now replaces the generated Antietam / Caldwell's Brigade command slot (`ss:antietam:US:us_barlow:cmd`) as the twelfth `Verified` replacement, with Colonel rank, 61st and 64th New York Infantry, Caldwell's Brigade / First Division / II Corps context, and the Sunken Road enfilading-fire story grounded in NPS Barlow/OOB pages, Barlow's Official Records report, and the Antietam on the Web officer profile. The record asserts no brigadier-rank-at-Antietam, no single-company claim, no portrait asset, and keeps neutral inferred ratings. Phase H remains **not** the automatic next loop: its media budget guard, Intel UHD-617 profile, H0/H2/audio/flags/readability probes, and D198-D213 3D resource polish are enough to park it until Aaron explicitly reopens media/profile work. **Priority 1** remains bounded Phase I Soldier's Story scale-out; **Priority 2** is an explicit media-budget/profile decision before real surviving-colours/PD asset, H2 footage, HDRI/model media, or optional-pack work; **Priority 3** is Group 6 meta/deferred tooling; **Priority 4** is GM/Transfer only after honest `theater` fields; **Priority 5** is battle-build last and only after Aaron go/no-go. Current embedded core remains above the soft warning but below hard/review caps. M8 battle-build remains locked. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.

**Codex / top-model 5.5 preferred**
- **Priority 1 - Phase I Soldier's Story scale-out:** more citation-grade named bios/portraits/unit detail and richer career trajectory, preserving D152-D158/D172/D214/D215/D216/D217/D218/D219 honesty and the separate women-in-war lane.
- **Priority 2 - media-budget decision checkpoint before reopening Phase H media:** before real surviving-colours/PD asset work, H2 footage, HDRI/model media, or optional-pack work, surface the embed-budget/provenance/performance tradeoff and get Aaron approval.
- **Priority 3 - Group 6 meta/deferred tooling:** reusable historical-data layer, source organization, hotpath profiling, and embedded-media budget tooling; no broad refactor without a small blast radius and a clear gate.
- **Priority 4 - GM/Transfer follow-up:** author honest `theater` fields only if a bounded source/probe plan exists; any AI-GM-to-outcome wiring is a separate no-fudge decision.
- **Priority 5 - battle-build queue:** M8, Chattanooga, Atlanta/March, Franklin/Nashville, and USCT playable battles remain last unless Aaron explicitly gives go/no-go/reorders.
- **Deferred/v2:** Phase D full hex tactical mode and custom-battle phase authoring/editor remain deferred, not active v1 priorities.

**Latest completion summary:** D219 shipped the twelfth Soldier's Story sourced replacement. Francis C. Barlow is now the verified Antietam / 61st and 64th New York / Caldwell's Brigade row, with no brigadier-rank-at-Antietam claim, no single-company assertion, no portrait assertion, and neutral inferred ratings. D214's Phase H closeout/routing park remains live: D193-D213 Phase H is valuable guard/probe history, but parked rather than the next automatic loop. Treat older "Recently completed" paragraphs below as historical backlog context; the live next task is Priority 1 above: another bounded Phase I Soldier's Story scale-out slice unless Aaron redirects. M8 battle-build requires Aaron go/no-go first.

**Recently completed:** D219 added Francis C. Barlow as the twelfth citation-grade Soldier's Story replacement; D218 added Samuel N. Benjamin as the eleventh; D217 added Francis A. Waller as the tenth; D216 added Orion P. Howe as the ninth; D215 added John Cook as the eighth; D214 parked Group 5 Phase H as complete-enough and added Leander Stillwell as the seventh; D213 completed low-tier marker-body layer perf polish; D212 completed high-tier marker-body resource polish; D211 completed low-tier marker-pole layer perf polish; D210 completed high-tier marker-pole resource polish; D209 completed idle selection-ring resource polish; D208 completed high-tier marker-topper resource polish; D207 refreshed the priority ladder; D206 completed low-tier marker-topper resource polish; D205 completed low-tier marker-topper perf polish; D204 completed atmospheric smoke draw-range perf polish; D203 completed hidden peg-fallback resource polish; D202 completed base 3D marker geometry perf polish; D201 completed render-richness selection-ring perf polish; D200 completed formation-figures perf polish; D199 completed visual-fidelity contact-shadow perf polish; D198 completed terrain-overlay perf polish; D197 completed battle-colours HUD readability polish; D196 completed H2 cutaway readability polish; D195 completed audio/readability polish. D194 completed the Group 5 Intel UHD-617 profile/readout guard: executable profile probe, media-budget performance policy, actual Intel Metal renderer artifact, focused profile/media/H2/formation gates, and doc sync. D193 completed the first Group 5 Phase H guard: executable media budget, H2 video lock, optional-HD-pack boundary, focused media-budget probe, adjacent H2/scenes probes, and doc sync. D192 completed the planned Group 4 batch/release gate with full-suite split coverage and clean JSON artifact inspection. D191 shipped Western-theater strategic readouts as focused-gated readout-only War Effort/H0 Desk and Theater Map ledgers with exact-zero bridge behavior and no battle-build/OOB leakage. D190 shipped human-cost-with-gravity as a focused-gated readout-only War Effort/H0 Desk ledger with exact-zero bridge behavior and no battle-build/OOB leakage. D189 shipped real diplomacy as a focused-gated War Effort/Diplomacy-tab system with exact-zero default bridge behavior, capped side-specific priorities, no direct overall bonus, and no battle-build/OOB leakage. D188 shipped CS finance toolkit as a focused-gated Confederate-side-only War Effort system with exact-zero default bridge behavior, capped costly finance priorities, no direct overall bonus, and no battle-build/OOB leakage. D187 shipped M7 lived-slavery + Black combat agency as a focused-gated pure teaching readout with Jacobs/Hughes source cards, slavery-as-lived-institution codex framing, and Black combat-agency/USCT cross-links. D185 shipped M6 home-front politics/economy; D184 shipped M5 keystone cards as a focused-gated pure teaching readout with Second Founding / Age of Emancipation codex cards plus Faust/Hacker and Fellman/Sutherland debate-card coverage. D183 shipped M4 source-criticism / under-told voices / Native rewrite as a focused-gated pure teaching readout with the Native thread now Verified but still barred from playable-battle/OOB extrapolation. D182 shipped M3 Reconstruction/memory as a focused-gated pure teaching readout with new source cards, amendment-date codex/coda updates, and Pollard-vs-Stephens memory manufacture. D181 shipped M2 Documents / Read the Document plus visible manpower debate-card scholarship. D180 shipped M1 corrections/fabrication fixes. D179 shipped flagship named units as a focused-gated War Effort system with exact-zero default bridge behavior, capped unit stewardship, and no playable-battle/OOB/replacement-lane leakage. D178 shipped under-told perspectives as a focused-gated War Effort system with exact-zero default bridge behavior and D153 women-lane separation; D183 later upgraded its Native-nations thread from Inferred to Verified while keeping it narrow. D177 shipped irregular war / civilian security as a focused-gated War Effort system with exact-zero default bridge behavior. D176 clarified queue-loop vetting: do not run full `npm run vet:noreg` after every queued item; defer the long audit to the end-of-queue batch/release gate or explicit request. D175 shipped hard war / civilian protection as a focused-gated War Effort system with exact-zero default bridge behavior. D174 completed the Group 3 H0 batch/release gate: all six H0 probes are now included in `npm run vet:noreg`, and the inspected full battery passed in split segments with no `ok=false`, `FATAL`, pageerrors, or realErrors. D173 shipped the first Group 2 GM follow-up: a deterministic symmetric AI-GM command shadow/readout over the current enemy roster, with no Transfer, no hidden commissions, no save-state enemy command object, and no scoreboard/output writes. D172 added the sixth citation-grade Soldier's Story replacement: Strong Vincent replaces one generated Gettysburg brigade-command row with sourced Little Round Top / V Corps / Vincent's Brigade context and no unsupported portrait/rank/company claim. D170 added the H2 skippable field cutaway fallback shell with existing embedded PD scene stills, procedural fallback for imageless battles, disabled video slots, and focused/adjacent gates. D169 added the Phase F disease/army-medicine War Effort system with exact-zero default bridge behavior and focused/adjacent gates. D168 completed the H0 after-action/final-report command-shell slice, finishing the ordered H0 surface set; D167 completed the H0 tactical HUD/settings command-shell slice; D166 completed the between-battle interstitial; D165 completed the H0 battle briefing / side-choice command-shell slice; D164 recovered and finished the interrupted H0 President's Desk overview slice after the CPU death; D163 completed the H0 main-menu command-shell prototype. D162 added local-only Tripo-compatible unit asset slots plus procedural formation figures; D161 added POW exchange/camp pressure; D159 added rail logistics. D154-D158 plus D172, D214, D215, D216, D217, D218, and D219 added twelve citation-grade Soldier's Story replacements after D152 tooling (Rhodes, McCarter, Watkins, Chamberlain, Cushing, Vincent, Stillwell, Cook, Howe, Waller, Benjamin, Barlow). D153 completed the separate Women in the War presentation lane. D148-D152 shipped and hardened the Campaign Kit / Army Register / Soldier's Story journey spine. D147 completed custom-scenario sharing, D146 hardened save/load/share slots, and D145 made the D143 full-campaign probe part of `npm run vet:noreg` while refreshing model-routing and UI-redesign law. H5's UG:G-fidelity push is complete through D142: movement/orders, visual fidelity, terrain readability, and fortifications depth.
- **Western theater roster/order/probe framework:** lock Shiloh → Vicksburg → Chickamauga/Chattanooga → Atlanta/March → Franklin/Nashville ordering and guardrails.

**Token-efficient helper models suitable only when packeted and verified by 5.5**
- Draft source dossiers from specified URLs/books into a fixed template.
- Fill teaching-card copy after Codex defines schema, sources, and acceptance criteria.
- Mechanical help-copy updates after roster changes.
- Add simple probe assertions copied from an existing probe pattern.
- Run exact gate commands and paste exact outputs.
- Screenshot cataloging / artifact inventory.
- Small JSON cleanup with strict acceptance criteria.

**Current loot status:** D148 shipped the standalone Phase I Campaign Kit MVP: rarity-tiered drops/inventory, use/equip/forage controls, default-off survival, and a first playable Soldier's Story journey picker. D149 hardened that spine with restored-save sanitation, exploit probes, strict active gates, restart blocking, and screenshot/UI assertions. D150 turned the 603-person current registry into a searchable/filterable Army Register with detail cards, provenance/source display, team hierarchy, and card journey start. D151 made that selected-person journey persistent across battles/saves/reports with status, promotion, survival, and career-log state. D152 added the citation-grade replacement lane; D154-D158 plus D172, D214, D215, D216, D217, D218, and D219 now populate it with 12 `Verified` records while preserving the separate Women in the War lane. `MASTER-TASK-LIST.md` is retired.

---

## ▶ PASTE THIS INTO A NEW CHAT (zero-context, bounded fresh-chat build run — top-model 5.5 / ultracode / high effort)

**CURRENT D219 LIVE HEAD OVERRIDE (2026-07-03): D219 shipped the twelfth citation-grade Soldier's Story replacement: Francis C. Barlow for the Antietam / Caldwell's Brigade command slot. D214 already parked Group 5 Phase H as complete-enough after D193-D213. Start Priority 1 next: another bounded Phase I Soldier's Story scale-out slice, not another automatic Phase H zero-byte polish loop. Do not add surviving-colours/PD assets, H2 footage, HDRI/model media, or optional-pack work without an explicit media-budget/profile decision. Do not start M8 battle-build without Aaron go/no-go. Q5 Chattanooga, Atlanta/March, Franklin/Nashville, and Q6 USCT playable battles remain LAST.**

**Live head as of this edit (2026-07-03): D219 shipped Barlow as the twelfth Soldier's Story replacement; D214 remains the Phase H park/closeout routing update. Use `HANDOFF.md`'s top block + `git log` for the exact live state as milestones ship; the next assigned item remains Priority 1, another bounded Phase I Soldier's Story scale-out slice, with Phase H media additions, M8, and the last battle queue still locked behind explicit Aaron decisions.**

## ▶ PASTE THIS INTO A NEW CHAT (remaining-work map + model-routing plan — Codex / Cline / DeepSeek friendly)

> **High effort.** You are Codex working in `~/Desktop/Video Game`. Your task is **not to implement yet**. First produce a practical remaining-work map for the whole project that keeps 5.5 on reasoning-heavy coding, history, UX, accessibility, and final integration, while packaging simpler work so token-efficient helpers can execute it safely from clear instructions.
>
> **Load context first:** read `START-HERE.md` including the Universal AI implementer standards, `HANDOFF.md` top block, `WAKE-UP.md` top block, `RUN-LOG.md` top entry, `V1-CHECKLIST.md`, latest `DECISIONS.md` entries, `src/00-manifest.json`, and the relevant `src/tactical/*.js` / `tools/probe-*.mjs` files. Note that as of 2026-06-19 the D77-D84 leftover gate is closed, the 3D flag texture warning is fixed, `probe-tactical-visuals` fails on that warning, and the visual probe now captures 8 scenes with 3D objective-beacon assertions. Blender is closed; do not require Blender unless a future Phase-H asset task explicitly needs it.
>
> **Output required:** make a table of all remaining V1 work grouped by phase (`C`, `D`, `E`, `F/G`, `H`, `I`, `J`). For each work item include: complexity (`S/M/L/XL`), risk (`low/med/high`), dependencies, files likely touched, required probes, whether it needs historical research, and the **recommended model/tool owner**.
>
> **Routing rules:** assign **5.5** to architecture, simulation design, balance tuning, historical claims, UI/UX direction, accessibility judgment, cross-module changes, bug hunts, probe design, build-system work, and anything touching tactical/strategic contracts. Assign token-efficient helpers only to prepackaged bounded tasks such as documentation refreshes, straightforward data entry after 5.5 has written the schema/spec, mechanical probe-output summarization, CSS copy tweaks with exact before/after instructions, and test fixture additions with exact expected results. Every helper/subagent/workflow call must explicitly set model and effort.
>
> **For each helper-suitable task**, write an implementation packet that a bounded helper can understand: goal, exact files, exact constraints, explicit model+effort, step-by-step edits, acceptance criteria, commands to run, "do not touch" list, expected JSON/output checks, rollback guidance, and a self-audit checklist. Do not ask those models to invent architecture, rebalance combat, choose history claims, design UX/accessibility direction, or make broad refactors. Require them to stop and report if the packet becomes ambiguous or a gate turns red.
>
> **For Codex tasks**, write a shorter implementation brief: problem, design choices to decide, likely seams, risk list, probes to create/extend, and the full vetting gate. Keep the plan grounded in the current repo. End with the recommended next 3 milestones in order and why.

> **ultracode — high effort.** You are top-model **5.5** driving a bounded fresh-chat execution group for **"The Civil War"**, a single-file teaching wargame at `~/Desktop/Video Game` (a personal project — NOT MJI). $0 / single-file (several MB is fine) / no build step beyond `tools/build.mjs`. You have **ZERO context — everything lives on disk; load it first.** The roadmap (`V1-CHECKLIST.md`, DECISIONS **D61/D171**) is fully cleared, so you don't re-litigate scope — you **build the assigned slice beautifully, gate it, commit/push it, and stop with the next prompt**. **Bar: best appearance AND function possible — go above and beyond the basics in EVERY aspect. Nothing ships ugly, minimal, or barebones.**
>
> **CURRENT D219 OVERRIDE:** D219 is the latest implementation milestone. Group 5 Phase H remains parked / complete-enough after the D193-D213 media-budget/profile/readability/perf guard stack. The live item remains Phase I Soldier's Story scale-out: D219 added Francis C. Barlow as the twelfth `Verified` replacement, mapped to the Antietam / Caldwell's Brigade command slot with no brigadier-rank-at-Antietam, single-company, or portrait assertion. The next assigned item is Priority 1: another bounded Phase I Soldier's Story scale-out slice unless Aaron redirects. M8 battle-build requires Aaron go/no-go first.
>
> **STEP 1 — LOAD CONTEXT (in order, READ them):** (1) **`START-HERE.md`** (this map), including the Universal AI implementer standards. (2) **`HANDOFF.md` top block** for the newest live head. (3) **`WAKE-UP.md` top block** for the human-readable current state. (4) **`V1-CHECKLIST.md`** — the approved roadmap/status. (5) **`DECISIONS.md`** — read the **latest D## first** (currently D219/D218/D217/D216/D215/D214/D213/D212/D211/D210/D209/D208/D207/D206/D205/D204/D203/D202/D201/D200/D199/D198/D197/D196/D195/D194/D193), then pull older decisions only as the task requires (D176/D171 for gate/boundary law, D145 for model/UI/gate law, D138 for the UG:G-fidelity arc, D91/D93 for Phase I, D74 for universal combat, D61 for the roadmap, D54/D55 for tactical scope). Append new decisions, never relitigate. (6) **`AUTONOMOUS-RUN.md`** §1–§3 + §8, especially §1A. (7) `MODERN-UGG-PLAN.md` + `GRAND-STRATEGY-PLAN.md` §27. (8) `src/00-manifest.json` + the `src/*.js` you'll extend + `tools/build.mjs` + relevant `tools/probe-*.mjs`. (9) `git log --oneline -15` + any `.tmp/*design*.md` drafts the handoff points to.
>
> **STEP 2 — THE OPERATING MODEL (DECISIONS D61 — obey it):**
> - **Build the V1 checklist from the live head**, not from stale phase memories. Consult `HANDOFF.md`'s top block + `git log` for what just shipped. Current broad direction: D214 parked Group 5 Phase H after the D193-D213 guard stack, and D219 continued Phase I Soldier's Story scale-out. Start **Priority 1: another bounded citation-grade Soldier's Story named-person/story slice** unless Aaron explicitly redirects. **Do not restart the automatic Phase H zero-byte loop. M8 battle-build requires Aaron go/no-go first. Q5 Chattanooga + Q6 USCT playable battles remain LAST** unless Aaron explicitly reorders them. Full detail per item = `V1-CHECKLIST.md`.
> - **Clear-then-continuous inside the current group.** The MAJOR forks are already cleared. So **decide-&-log sub-choices within the bounded checklist item and keep moving**, but stop at the clean committed+pushed boundary before a new execution group unless Aaron explicitly orders same-chat continuation. **PAUSE + surface (`AskUserQuestion` if Aaron is present, else `PushNotification` + write `~/Desktop/Video Game/DECISION-NEEDED-<topic>.md`) ONLY** for a NEW fork that: is uncovered by the checklist, **contradicts a prior shipped decision/teaching** (the live precedent: **B-3's logistics broke the D58/D64 "fog aids the defender" teaching** under fog — surfaced, and Aaron chose the fix; D66), is irreversible/costs money, or is so ambiguous a wrong guess wastes a whole milestone. On such a pause, keep making progress on OTHER cleared items if any; else stop and notify.
>
> **STEP 3 — THE PER-MILESTONE LOOP (run it for each checklist item, in order):** plan the item from `V1-CHECKLIST.md` → (a research workflow ONLY if citation-grade content is genuinely missing) → author NEW `src/**/*.js` module(s) that **EXTEND** the engine via **guarded, no-op-when-inactive seams** (or, where authorized, OVERRIDE a frozen `build/base.html` fn via a NEW module — **never edit `build/base.html`**; new tactical work lives in `src/tactical/`) → `node tools/build.mjs` until **`GATE OK`** → write/extend `tools/probe-<item>.mjs`, **start `python3 -m http.server 8765` yourself when the probe does not auto-start**, run probes **foreground with `2>/dev/null`**, and **READ `tools/shots/*.json`** (never assume) → run the batched D160/D176 gate (always focused/importer/schema as relevant; `node --check` + adjacent probes for JS/runtime; full `npm run vet:noreg` only at the end-of-queue planned-work batch/release checkpoint or explicit request) → require **0 pageerrors in every probe actually run** → for any balance change, run a **headless A/B sweep + LOG every number** → use adversarial review scaled to the risk, with heavy bug-hunt panels saved for batch/release or high-risk shared systems → run a targeted accessibility check on new UI → **commit + `git push origin main`** (remote `adhanke-star/History-Video-Game`, private) ONLY after the focused gate for that slice passes → append `RUN-LOG.md`/`DECISIONS.md`, refresh `AUTONOMOUS-RUN.md` §2 + `HANDOFF.md`'s top block + tick the `V1-CHECKLIST.md` box + refresh root **`WAKE-UP.md`** → **stop at the clean boundary and return a next-chat prompt before starting the next execution group, unless Aaron explicitly ordered same-chat continuation**. The intent is momentum inside a bounded slice, with the heavy battery at planned batch/release checkpoints and fresh chats between groups.
>
> **SUBAGENT MODEL ROUTING (D145 supersedes D63 wording):** the main session stays on **5.5** for architecture, important code, user-facing text, historical judgment, UI/UX decisions, accessibility judgment, bug hunts, final review, and any irreversible or quality-critical work. Every helper/subagent/workflow call must explicitly set model **and** effort. Use token-efficient models only for bounded helper packets (`rg`/search summaries, inventories, probe-log summaries, mechanical doc syncs, exact-schema data cleanup, first-pass source gathering, repetitive fixture/probe scaffolding). Smaller models never own architecture, combat balance, historical claims, UX direction, accessibility judgment, or final integration; they produce packets that 5.5 verifies.
>
> **THE ABOVE-AND-BEYOND BAR (apply to EVERY milestone — non-negotiable):** **(Function)** robust edge-handling (null/empty/NaN/extreme), determinism where probes need it, no regressions ever (`diag-classic`), the Intel UHD-617 perf floor (instancing/LOD/culling/perf presets), genuinely deep simulation + teaching (citation-grade, multi-voice, Verified/Inferred tags, anti-Lost-Cause). **(Appearance)** D145 supersedes broadsheet-everywhere: build a more video-game-like historical command interface — brighter, rounder, more dramatic, icon-led, and centered on public-domain photos/illustrations where they clarify people/place/stakes. Preserve Civil War gravity; do not glamorize death or the Confederacy. Broadsheet remains for press/editorial/dispatch/provenance surfaces, not the whole app shell. Smooth animation must **honor `reduceMotion`**; HUD/UI must be polished and legible; **full WCAG 2.2 AA baked in AS YOU BUILD** (keyboard, visible focus, ≥4.5:1 text / ≥3:1 non-text, ARIA + live regions, CVD-safe cues never color-alone).
>
> **GUARDRAILS / TOOLING:** NEVER regress Classic; run `diag-classic` when visual/engine/Classic-adjacent paths are touched and as part of every full-suite gate. Frozen-engine overrides = new modules only, gated, reverted on any regression. New tactical work = **guarded seams in T0/T1 + a new module, no-op when the scenario/campaign context is inactive** (so the standalone sandbox/Bull-Run/fog/auto-pause/AI probes stay byte-identical). `_SAVE_VER` bumps OK with idempotent lazy migration. Bare-name globals (`G`/`GAME_DATA`/`__FIELD`/`FLD`, not `window.*`); THREE loads async (no `new THREE.*` at chunk top level); never put a literal comment-closer inside a block comment (parse-bomb). **Use `Workflow`/subagents aggressively.** **Tooling gotchas:** the task-output tmpfs throws a **spurious "0MB free" ENOSPC** — run probes foreground with `2>/dev/null` + `export TMPDIR="$PWD/.tmp"`, READ `tools/shots/*.json` directly, verify commits via `git log`, and clear `/private/tmp/claude-501/*/tasks/*.output` (`-mmin +2`, sparing the in-flight file) if Bash blocks; **foreground `sleep` is blocked** (use `run_in_background` or an `until`-loop); for offline headless **sweep** scripts use `waitUntil:'domcontentloaded'` (the THREE-CDN `'load'` wait times out offline); **run probe batteries SEQUENTIALLY** (one server, shared `shots/*.json`).
>
> **CONFIRM in ≤4 lines, then GO (don't wait on me):** (a) latest commit + what's playable now; (b) the first `V1-CHECKLIST.md` fresh-chat group/item you're starting (per `HANDOFF.md`'s top block — currently Priority 1 Phase I Soldier's Story scale-out, with Phase H media additions, M8, and Q5/Q6 battle-build work still locked until Aaron approves) + your gate plan (surface a fork only if it hits a NEW/contradicting one per D61); (c) your artifact-inspection plan for stdout/JSON/pageerrors; (d) confirm the D171 loop (gate → docs if needed → commit/push → stop at clean boundary with the next prompt unless Aaron explicitly orders same-chat continuation). Then run the bounded gate cleanly.
