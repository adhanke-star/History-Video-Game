# START HERE — "The Civil War" (master index)

<!-- LIVE-HEAD-SUMMARY:BEGIN -->
<!-- LIVE-HEAD decision=D528 next-lane=LANE-019 state=CONTRACT owner=none -->
> **Live status (D528, 2026-07-22):** LANE-019 is `CONTRACT` / `none` after the exact docs/research-only 27-row rail physical-window audit closed at zero established, 19 snapshot-only, seven unresolved, and one disputed; all four rail faces remain unadjudicated interchanges.
>
> **Boundary:** D525 remains the product head. No transport data, runtime, state, save, UI, topology, route, movement, or generated product changed, and no Historical rail service is legal-now ready.
>
> **Authority:** `HANDOFF.md`'s first ⚡ block and LANE-019's D528 delivery record own the boundary. Under D514, reload the ledger and separately contract the next dependency-ready prerequisite; roads remain unavailable and LANE-020 remains independently blocked.
<!-- LIVE-HEAD-SUMMARY:END -->

**D514 completion rule:** the project now runs a work-conserving completion loop. `HANDOFF.md` owns the algorithm and terminal condition; `V1-CHECKLIST.md` owns every live item; ordinary milestone/phase/browser boundaries do not stop a healthy run.

**This is the ONE file to open first. It tells every new chat exactly which documents are CURRENT and which are history, and the order to read them. If a doc isn't listed under "Canonical" below, treat it as archived — do not follow it as live strategy.**

The game: a teaching wargame in three layers — **owner-mode grand strategy** (you are President Lincoln/Davis: economy, blockade, manpower, diplomacy, politics) + **real-time UG:G-style battles** (the tactical engine, built last) + a **PhD-level history & alternate-history seminar**. Personal project, $0/single-file, build bar 200/100.

---

## ⭐ The source of truth — the TRIMMED MANDATED READ ORDER (D412; a fresh chat has zero context)
1. **`START-HERE.md`** ← you are here (the map + the rules).
2. **`COORDINATION.md` — the RELEVANT lane only** ← **MANDATORY SESSION-START RELAY CHECK.** Read §1-§4 (the rules) plus the lane your task touches, in full: owner, state, acceptance contract, probe design, resume pointer; reconcile with live git. Never drive a lane owned by the other tool. Do not bulk-read the other lanes.
3. **`HANDOFF.md` top ⚡ block** ← **THE one canonical live boundary** (LIVE-HEAD SINGLE-SOURCE RULE below). What shipped last, exact pins, and the exact next action live here in full, once.
4. **The task's own law docs + probes** — the relevant `docs/design/*` spec/packet, `GRAND-STRATEGY-PLAN.md`/`MODERN-UGG-PLAN.md` sections, `src/00-manifest.json` + the `src/*.js` you'll touch, `tools/build.mjs` + the relevant `tools/probe-*.mjs`. Pull only what the task needs.
5. **`DECISIONS.md` — the latest entry** (newest D### first; append, never relitigate; pull older entries only as the task requires).

**Pull-on-demand (no longer mandatory at session start):** `AUTONOMOUS-RUN.md` (operating manual — current loop §3 and guardrails §5; old roadmaps/backlogs are labeled history), `WAKE-UP.md` (human-readable pointer), `RUN-LOG.md` (chronology), `V1-CHECKLIST.md` (the current task map), `git log --oneline`. Read them when the task calls for them, not by default.

## 📚 Canonical reference (current — pull the section a task needs; don't bulk-read)
- **`V1-CHECKLIST.md`** — the D513-current approved roadmap: shipped ARC baseline, outstanding ARC 7-9 work, parked proposals, and explicit v2/deferred work. It is the only task checklist; the full live boundary remains in HANDOFF.
- **`MODERN-UGG-PLAN.md`** — the real-time tactical battle engine spec (the UG:G layer, built LAST, phases P0–P5).
- **`RUN-LOG.md`** — chronological build log (newest run at top; keeps the current month — older entries live byte-verbatim in `legacy/RUN-LOG-ARCHIVE-2026H1.md`, D412).
- **`HANDOFF.md`** — **THE single source of the live boundary**: its first ⚡ amendment is the one canonical live head (D510-enforced LIVE-HEAD SINGLE-SOURCE RULE); every other routing doc carries the checked summary + pointer. Keeps the latest block + one prior; older blocks live byte-verbatim in `legacy/HANDOFF-ARCHIVE.md`.
- **`CUSTOM-SCENARIO-FORMAT.md`** — the current versioned custom-battle/scenario-pack share format for the C4 builder.
- **`HISTORICAL-DATA.md`** (211KB — OOBs, weapons, USCT, historiography), **`HISTORICAL-DATA-ECONOMY.md`**, **`HISTORICAL-DATA-DIPLOMACY.md`**, **`HISTORICAL-DATA-ARTILLERY.md`** — citation-grade content. Grep per system; never bulk-read.
- **`COORDINATION.md`** — the **Contract Relay**: cross-tool lane locks (Claude ⇄ Codex handoffs). Lane states LAW-DRAFT→CONTRACT→DRIVE→VERIFY→SHIPPED; the hard rule — **red probe teeth never land in git; the lane carries the committed contract, and teeth ship in the same commit as the fix that greens them**; role-based (not model-name) routing. Open lanes + resume pointers live there. *(D356)*
- **`REVIEW-QUEUE.md`** — the standing **AUDIT findings ledger** (the memory between audit runs; every fix flips its line to fixed-in-D###). Historical Fable/Opus audit entries remain evidence only; current routing is D336 / `AUDIT-PROMPT.md`. The run-e/f/g/h priorities/locks it used to carry are archived in `legacy/REVIEW-QUEUE-2026-06-runs-efgh.md`; current priorities + locks live in `V1-CHECKLIST.md` + this file. *(S16/S35, D236)*
- **`SOLDIER-REPLACEMENT-FORMAT.md`** — the canonical import-lane format for Phase I Soldier's Story citation-grade replacements (live through the current Priority-1 work).
- **`RATING-SYSTEM-DESIGN.md`** — design law for the D94 OVR/ratings layer (T14).
- **`docs/design/battle-mode-parity-design.md`** — the AARON-LOCKED battle-mode parity design law (E42/E43/E45 + the attacker-AI fork, design session 2026-07-03; committed by the arc's M1, D246-D248). *(`ATTACKER-AI-PROPOSAL.md` was RESOLVED as superseded-by-D64 and retired to `legacy/` in D248 — it was never an open fork after D64 productionized it.)*
- **`docs/design/`** — durable design/research packets that survive a fresh clone. The ladder includes Chattanooga, Kennesaw, Franklin, Nashville, Gaines' Mill, **New Market Heights (D363 spec → D364 playable)**, **Stones River (D365 spec → D366 playable)**, **Cedar Creek (D375 spec → D376 playable)**, **Cross Keys/Port Republic (D377 spec → D378 playable)**, **Five Forks (D379 spec → D380 playable)**, **Fort Donelson (D383 spec → D384 playable)**, **Elkhorn Tavern (D387 spec → D388 playable → D389 release)**, **Spotsylvania (D390 spec → D391 playable)**, **the Wilderness (D392 spec → D393 playable → D394 release)**, and **Petersburg's initial assaults (D396 spec → D397 playable → D398 release)**. Read the relevant `battle-build-research/` packet before every spec; preserve source/OOB gates and source-derived direction guards. The naval-river and trans-Mississippi packets carry their committed spec-time addenda.
- **`docs/design/war-career-loop-design.md`** — War Career design law and retained acceptance history. The playable spine through Slice F is shipped through D438; D512 contracted one read-only report slice in `unlocked-but-judged-design.md` §4f, D513 shipped it, and LANE-005 is now CONTRACT/unowned with no deeper medicine work authorized.
- **`docs/design/open-history-mayhem-mode-design.md`** — Historical/Mayhem ruleset law. The public A-C bundle shipped through D420; LANE-007 is SHIPPED and `MAYHEM_PUBLIC_READY=true`. Later Chronicle/content depth belongs to the D455 ARC roadmap.
<!-- S43/D350: superseded pre-D341 catalog snapshot retained in source history only.
- **`docs/design/`** — durable design/research packets that must survive a fresh clone. Current battle/design law packets include `battle-mode-parity-design.md`, `chattanooga-battle-build-spec.md` (D325 durable Chattanooga spec, implemented as playable runtime in D326), `kennesaw-battle-build-spec.md` (D330 durable Kennesaw spec, implemented as playable runtime in D331), and `franklin-battle-build-spec.md` (D332 durable Franklin spec, implemented as playable runtime in D333). **`docs/design/battle-build-research/` (D327 + D329, now COMPLETE)** is the durable battle-build research library — a README index + one citation-grade research packet per remaining lane, each with a source register, OOB/rank traps, playable-shape recommendation, D74 no-fudge risks, probe teeth, and a READY_FOR_SPEC/NEEDS_MORE_RESEARCH/DO_NOT_BUILD_NOW verdict. **Read the relevant packet before writing any new battle spec**; guarded by `tools/probe-battle-build-research.mjs` (**15/15**). The 11 lanes: D327's Atlanta/March, Franklin/Nashville, USCT, naval/river, 1864-65 attrition; D329's `eastern-1862`, `shenandoah-1862`, `shenandoah-1864`, `western-gaps`, `trans-mississippi`, `appomattox-campaign`. **Every buildable battle/campaign is now researched.** Its **`built-battles/` subfolder (D328)** is the companion research/audit of the 10 ALREADY-BUILT battles for Codex revision — 9 of 10 SOLID_AS_IS, with Antietam needing one outcome-neutral rank-label fix (`us_richardson.commander` Brig. Gen. → Maj. Gen.). **⚠ `trans-mississippi` is BLOCKED pending Aaron's D183 two-tier go/no-go** ("M8 battle-build still needs Aaron go/no-go"); its READY_FOR_SPEC means "writable once the gate clears," not "start now." **Direction-guard caution:** Stones River, Perryville, Wilson's Creek and McDowell invert "the winner bleeds less" — never assume US < CS.
-->
- **`OPUS-PLAYBOOK.md`** — the current top-model playbook (D336; filename retained for compatibility): effort/routing policy, autonomy and evidence rules, standard run-prompt snippets, and session-start checklist for ChatGPT/Codex.
- **`AUDIT-PROMPT.md`** — the paste-ready ChatGPT 5.6 Sol full-spectrum audit + improve kickoff prompt (re-runnable; its ledger lives in `REVIEW-QUEUE.md`).
- **`DEPLOY.md`** — how the single-file deliverable ships.
- **`data/*.json`** — game data (`economy`, `diplomacy`, `manpower-teaching`, `weapons`), injected as `GAME_DATA`.
- **`civil_war_generals.html`** — the GENERATED, playable deliverable (open-and-play; rebuilt by `tools/build.mjs`; never hand-edit).
- **`build/base.html`** — the FROZEN run-h foundation (Classic + 3D engine). **NEVER edit.**

## 🗄️ Legacy (in `legacy/` — superseded run-g/run-h material; reference only, NOT live strategy)
Old kickoff prompts, the pre-modularization plans, the graphics/3D/Blender/asset notes, and earlier handoffs. They explain *how we got here* but do **not** describe current strategy — `V1-CHECKLIST.md` owns the task map and `HANDOFF.md` owns the live boundary. Files: `legacy/{AUTONOMOUS-RUN-PROMPT, NEXT-CHAT-PROMPT, NEXT-RUN-PLAN, CC-KICKOFF, GRAPHICS-RUN-PROMPT, GRAPHICS-RUN-CONTINUE, MODERN-UGG-KICKOFF, DESIGN-BIBLE, BUILD-PLAN, 3D-ASSET-PLAN, ASSET-PROMPTS, BLENDER-MCP-SETUP, BLENDER-CONNECT-STEPS, AUDIT-PROTOCOL, GENERALS_HANDOFF, PHASE4_HALT, PLAYTEST-LOG, OVERNIGHT-RUN-PROMPT, REVIEW-QUEUE-2026-06-runs-efgh, ATTACKER-AI-PROPOSAL, FABLE-5-PLAYBOOK, FABLE-AUDIT-PROMPT}.md` *(the overnight prompt + run-e/f/g/h review-queue moved in D236; ATTACKER-AI-PROPOSAL retired in D248 — resolved as superseded by D64; the two Fable playbook/audit docs retired in D286 when Fable left the subscription — replaced by `OPUS-PLAYBOOK.md` / `AUDIT-PROMPT.md`)*. **D412 adds the standing HISTORY ARCHIVES** — byte-verbatim superseded head-stacks, newest at top: `legacy/{HANDOFF,WAKE-UP,START-HERE,AUTONOMOUS-RUN,V1-CHECKLIST}-ARCHIVE.md` + `legacy/RUN-LOG-ARCHIVE-2026H1.md` (see the HISTORY ARCHIVAL RULE below).

---

## Naming convention (going forward — keep the root clean)
- **THE LIVE-HEAD SINGLE-SOURCE RULE (D510-enforced):** `HANDOFF.md`'s first ⚡ amendment is the only full live boundary. The delimited live-summary block is mirrored byte-for-byte in HANDOFF, START-HERE, WAKE-UP, AUTONOMOUS-RUN, V1-CHECKLIST, and RUN-LOG; `tools/probe-doc-coherence.mjs` checks it before every build.
- **THE HISTORY ARCHIVAL RULE (D510-enforced):** HANDOFF keeps exactly the current amendment plus one prior. Superseded heads move byte-verbatim to `legacy/<DOC>-ARCHIVE.md`, newest at top, after a probe-anchor inventory. Other canonical routing docs keep one live-summary block and may retain only clearly labeled historical material. Historical `NEXT` text is never current authority.
- **THE TRIMMED READ ORDER (D412):** the mandated session-start read is the ⭐ list above (START-HERE → COORDINATION relevant lane → HANDOFF top block → task law docs/probes → DECISIONS latest entry). WAKE-UP/RUN-LOG/V1-CHECKLIST detail/AUTONOMOUS-RUN §3+ are pull-on-demand. The four AI entrypoint pointers carry the same order; if it changes, update all four in the same commit.
- **`START-HERE.md`** — the only entry point. Update it whenever a doc's status changes.
- **One operating manual** (`AUTONOMOUS-RUN.md`), **one design law** (`GRAND-STRATEGY-PLAN.md`), **one decisions log**, **one run-log**, **one handoff**. Do not spawn parallel "PLAN/PROMPT/KICKOFF/CONTINUE" variants — append to the canonical file instead.
- Content files are prefixed **`HISTORICAL-DATA-<topic>.md`**; code is in **`src/NN-name.js`** (NN = load order); data in **`data/<topic>.json`**; tools in **`tools/<verb>-<topic>.mjs`**.
- Anything superseded goes to **`legacy/`** immediately, so the root only ever shows what's current.
- **Proposed deeper structure (next session, optional):** `docs/operating/` · `docs/design/` · `docs/research/` · `docs/handoff/` — deferred so the build's paths and the read-order references don't churn mid-flight; the root/`legacy` split already removes the confusion.

## Git / saving — commit AND push to GitHub, but ONLY after PROPER VETTING
The remote is **LIVE and PUBLIC** (Aaron approved publishing 2026-07-03, D237): `origin` → **github.com/adhanke-star/History-Video-Game**, branch `main`, with the playable game served by GitHub Pages at **https://adhanke-star.github.io/History-Video-Game/**. **Every milestone is committed AND pushed (`git push origin main`) — but ONLY after PROPER VETTING passes** (Aaron standing instruction, 2026-06-14; a push now also updates the public URL):

> **PROPER VETTING is TIERED and BATCHED (D160 + D176):** build playable features and graphics forward with focused gates instead of paying the full battery after every slice. Every commit still starts from `git status`, the source/doc read order, source-only edits (`src/` + `data/`; never `build/base.html`, never hand-edit `civil_war_generals.html`), `node tools/build.mjs` → **GATE OK**, any relevant importer/schema gate, the milestone's focused probe, JSON/pageerror readback, `git diff --check`, and a clean commit/push. For JS/runtime changes, also run `node --check` on touched JS/probe files plus 1-3 directly adjacent probes. Keep `tools/vet-no-regression.mjs` intact, but during Aaron's explicit same-chat queue loop do **not** run full `npm run vet:noreg` after each queued item; run it at the end-of-queue planned-work batch/release checkpoint or when Aaron explicitly asks.

Never push an unvetted / red / regressed / pageerror build. `tools/shots/*` and `.tmp/` are gitignored, so the pending-changes view stays at ~0; `git log` + the GitHub remote are the durable record.

## Universal AI implementer standards — Claude / Codex / Copilot / DeepSeek / Cline / any VS Code agent
**Each AI tool has a native entrypoint file that points to THIS file as the single source of truth — `CLAUDE.md` (Claude Code), `AGENTS.md` (Codex), `.github/copilot-instructions.md` (GitHub Copilot), `.clinerules` (Cline / DeepSeek).** They are thin pointers (the canonical read-order + the hardest non-negotiables); if the read-order or the non-negotiables below change, update them too. Every model implementing Aaron's instructions follows the same operating contract, regardless of vendor or model size:

- **Repo-ground first (trimmed, D412):** read the ⭐ mandated order — `START-HERE.md`, the relevant `COORDINATION.md` lane, `HANDOFF.md` top block (the live head), the task's own law docs/probes (`src/00-manifest.json` + files/probes directly touched), and the latest `DECISIONS.md` entry — before editing. `WAKE-UP.md`/`RUN-LOG.md`/`V1-CHECKLIST.md` are pull-on-demand.
- **Contract Relay first:** whenever `COORDINATION.md` exists, read the relevant lane in full immediately after `START-HERE.md`, before selecting work, planning, spawning helpers, or editing. Verify its owner/state/resume pointer against live git; do not edit a lane owned by the other tool. SessionStart hooks surface an index reminder but never replace this read. *(Aaron, 2026-07-10.)*
- **Work from reversible steps:** inspect before changing, keep edits scoped, preserve user/unrelated changes, make source edits in `src/` and data edits in `data/`, and touch generated `civil_war_generals.html` only when the repo's build/probe reality requires it and the source path is also aligned.
- **Recursive repo-learning loop:** read prior decisions → form a small hypothesis/plan → implement the smallest safe slice → run probes/builds → study failures and screenshots/JSON → correct root causes → write the lesson back into `DECISIONS.md`, `RUN-LOG.md`, `HANDOFF.md`, `WAKE-UP.md`, or the relevant checklist. "Learning" means repo-visible notes and improved probes; never rely on private hidden memory.
- **Self-audit every pass:** compare the diff against the user request, scan for unintended gameplay/simulation changes, check accessibility/reduceMotion/performance implications, read probe JSON instead of assuming, and search for warnings/pageerrors/regressions before reporting success.
- **Vetting is mandatory, tiered, and batched:** `node tools/build.mjs` or `--check` as appropriate, relevant importer/schema gates, focused probe(s), JSON/pageerror readback, and `node --check` plus adjacent probes for JS/runtime changes. Do not run the heavy full-suite battery after every queued item; keep building with focused gates, then run `npm run vet:noreg` at the end-of-queue planned-work batch/release checkpoint or on explicit request. If any focused or batch gate fails, fix the root cause; do not weaken the probe to pass.
- **Cloud/full-access default:** Aaron granted dangerous/full-access permissions on 2026-06-27. Prefer Codex Cloud or another non-Seatbelt full-access session for implementation and browser probes; if `CODEX_SANDBOX=seatbelt`, restart/reconfigure rather than bypassing `tools/guard-probe-browser.mjs`. After any cloud commit/push, fast-forward the local Mac repo before local work resumes: `git fetch origin && git status --short --branch && git pull --ff-only origin main`. Stop first if the Mac has dirty local edits.
- **Quality bar:** no placeholder history, no fabricated citations, no unclear-license assets, no accounts/trials/downloads unless explicitly approved, no broad refactors without need, and no visual/UI work that is inaccessible, unreadable, or unverified on real output.
- **Model routing (D336 + D361 clarification):** in ChatGPT/Codex use **5.6 Sol at Ultra (the highest effort available in that UI)** as the main loop for architecture, important code, user-facing text, historical judgment, UI/UX and accessibility decisions, adversarial review, final verification, and other quality-critical work. Configure helper model and effort explicitly when controls exist. If the surface has no per-helper controls, inheriting helpers are allowed for bounded, output-insensitive evidence work such as read-only inventories, log/artifact summaries, gate execution, and independent mechanical audits. State the inheritance once. The main loop verifies all helper output and retains architecture, code ownership, combat balance, historical claims, UX/accessibility judgment, final synthesis, integration, and the commit. The durable behavior/routing snippets are in **`OPUS-PLAYBOOK.md`** (filename retained); the full-spectrum kickoff is **`AUDIT-PROMPT.md`**.
- **Reporting discipline:** every handoff/final report names files changed, commands run, pass/fail results, unverified areas, residual risks, and any docs updated. Do not claim completion for work that was not run or inspected.
- **Work-conserving completion loop (D514; supersedes the D171/D307 stop cadence):** after each focused-green, documented, committed, pushed milestone, reload the live completion ledger and immediately start the next eligible item—even across phases, browser gates, full-suite checkpoints, or reversible design forks. Keep one owner per lane and contract each new slice before implementation. If one item blocks, record the exact blocker and continue another independent item. Stop only when the ledger is exhausted, every remainder shares one hard blocker, a destructive/paid/account/licensing action needs authority, a genuine source/design contradiction cannot be solved conservatively, or capacity requires a clean load-bearing relay.
- **Mandatory session closeout + provider relay (Aaron, 2026-07-11; D514 cadence):** every actual session closeout ends with a fenced, disk/git-grounded continuation prompt, but a normal milestone/phase boundary is no longer a closeout. Continue while capacity is healthy. At a D514 terminal/hard-stop or capacity boundary, include recipient/top loop, repo, fetch/status/HEAD/origin and pull-only-when-clean startup, clean/dirty WIP, exact active/next item, read order, lane ownership/locks, resume command, gates, completion-loop authority, and no-simultaneous-edits rule. Route to the provider with capacity before hard exhaustion.
- **Load-bearing continuation-prompt standard (Aaron, 2026-07-11):** treat the prompt as a **zero-context execution packet**, not a closing summary. Match or exceed the operational detail of the prompt that opened the current run when one exists. Embed: objective + terminal condition; exact startup + expected git boundary; read order; provider/model/helper routing; scoped phases + commit boundaries; target files/seams + do-not-touch list; acceptance invariants, counts, historical/source locks + expected state transitions; every gate command + expected stdout/JSON/pageerror/artifact readback; negative binds, A/B logging + byte-for-byte restore law; HALT criteria; docs/ledger sync; commit/push/lane release; and final-report fields. Naming a spec or prior prompt is not a substitute for carrying its critical contract. Prompt fidelity overrides brevity and response-length defaults. Ban shorthand such as "as discussed," "follow the earlier prompt," or "run the gates in the spec." Before delivery, compare the prompt against the current opener and live contract; if a capable fresh agent could make a different scope, phase, or gate choice, the prompt is too thin.

## Priority picker + model routing — use when Aaron says "pick from the list"
When Aaron asks a new chat to choose from a priority list, use this list (cross-check `V1-CHECKLIST.md` for the ordered roadmap). *(`MASTER-TASK-LIST.md` was retired 2026-06-21 — it duplicated this picker + the roadmap + the live head; it is now a pointer stub.)* These are the work items Codex is expected to handle better than DeepSeek/Cline because they require architecture, balance judgment, cross-module repo reasoning, probe design, or high-risk regression control.

**Model naming note (D336, 2026-07-09):** ChatGPT's visible top-model label is **5.6 Sol**. Select it at **Ultra** (the highest effort available in that UI) for quality-critical autonomous work. Older 5.5/Opus/Fable references document prior runs only; do not copy them into new ChatGPT/Codex kickoff prompts. See `OPUS-PLAYBOOK.md`.

*Older prior heads (D454 → D392) and the commented-out superseded snapshots moved byte-verbatim to [`legacy/START-HERE-ARCHIVE.md`](legacy/START-HERE-ARCHIVE.md) — D454 moved at D456; D453 moved at D454; D422 moved at D423; D417 moved at D420; D413 moved at D418; D412 at D417; D411 at D413; D410 → D392 in D412 (HISTORY ARCHIVAL RULE below).*

The task list lives only in [`V1-CHECKLIST.md`](V1-CHECKLIST.md). Under D514, every unchecked item in its outstanding, formerly parked, and formerly deferred sections is authorized for autonomous adjudication and completion; cross-check the first ⚡ amendment and relevant lane before editing. Historical archived checkboxes do not reopen shipped work.

**Current priority order:**

1. Complete ARC 7 with source-backed rail/water/bounded-sea transport first; render unsupported roads unavailable rather than inventing them.
2. Complete ARC 8 soldier depth, selecting citation-grade LANE-002 batches autonomously and contracting deeper narrative slices before runtime.
3. Close remaining review findings, formerly parked proposals, and formerly deferred depth by severity, dependency, and player/teaching value.
4. Re-take ARC 9 Slice 5 only after an independently gated sequential-turn owner exists; finish with the full release battery and repeat audit/repair until the ledger is empty.

**Model naming note (D336/D510):** ChatGPT's top loop is 5.6 Sol at Ultra for architecture, historical judgment, UX/accessibility, balance, integration, and final verification. Helpers are bounded mechanical readers/runners when the surface cannot set a lower tier.

**Output-insensitive helper packets suitable only when bounded and verified by the 5.6 Sol Ultra main loop**

- Inventory specified source URLs, files, symbols, schemas, or scenario ids without drafting claims.
- Run exact gate commands and return exact outputs.
- Catalog screenshots, JSON artifacts, warnings, logs, and pageerrors.
- Compare a mechanical diff against exact acceptance criteria without editing it.
- Independently audit counts, registration seams, generated-file parity, and doc cross-references.

**Soldier-depth feeder status:** LANE-002 5b is CONTRACT/unowned with 42 Verified overlays and 1,437 open generated rows. A future batch needs a selected citation-grade candidate set; the open count is not a mandate to fill every row.
